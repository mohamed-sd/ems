# برومت: إنهاء اعتماد المشروع على بيئةِ جهازٍ بعينه (WAMP ↔ XAMPP)

> **سياق التشخيص (مرجعٌ للمنفّذ — كلُّ سطرٍ هنا مُتحقَّقٌ منه فعليًّا لا مُفترَض):**
> المشروع يُطوَّر على جهازين: الأول **WAMP** ‏(MySQL 8.4.7 · قاعدة `equipation_manage`
> الحيّة · 371 جدولًا)، والثاني **XAMPP** ‏(MariaDB 10.4.32 · PHP 8.2.12). كلُّ تحديثٍ
> يُدفَع من الأول ينكسر عند الثاني بأعطالٍ مختلفة. الأعطال ليست صدفةً — لها سبعة
> جذورٍ محدّدة، وكلُّها في المستودع لا في الجهازين.

| # | الجذر | الدليل المُتحقَّق منه |
|---|---|---|
| ج١ | `.env.example` ينقصه **16 مفتاحًا** يقرأها الكود فعلًا | `EMS_DDL_FREEZE` · `MYSQL_BIN_DIR` · `EMS_APACHE_DIR` · `EMS_CAPACITY_SOURCE` · `EMS_CAP_OUTBOX` · `EMS_BASELINE_GATE` · `EMS_BASELINE_GATE_CONTRACTS` · `EMS_CONTAINER_GATE_MODE` · `EMS_NAV_UNIFIED_ROLES` · `EMS_PRICE_ADJUST` · `EMS_PROBE_SAFETY` · `EMS_ROLE_BOARD_ROLES` · `EMS_ROTATION_AUTOTRANSFER` · `EMS_SUBSTITUTE_LADDER` · `EMS_SUPPLIER_DOC_GATE` · `TICKETS_CRON_KEY` |
| ج٢ | افتراضاتُ مساراتٍ مطلقةٍ لـ XAMPP مثبّتةٌ في الكود | `admin/includes/db_tools.php:20` ← `ems_env('MYSQL_BIN_DIR', 'C:/xampp/mysql/bin')` · `admin/includes/db_tools.php:385` ← `ems_env('PHP_BIN', 'C:/xampp/php/php.exe')` — كلاهما خاطئٌ على WAMP، و`MYSQL_BIN_DIR` غيرُ موثَّقٍ في القالب أصلًا |
| ج٣ | `migrate.php` يفشل برسالةٍ مُضلِّلةٍ عند غياب `DB_MIGRATOR_*` | التشغيل الفعلي أعطى: `[migrate] FATAL: تعذّر إنشاء جدول التتبّع: CREATE command denied to user 'ems_app'@'localhost'`. والسبب أن `ems_app` صلاحياتُه `SELECT, INSERT, UPDATE, DELETE, CREATE TEMPORARY TABLES, LOCK TABLES, EXECUTE` بلا `CREATE` — تصميمٌ مقصود. والمسارُ الصحيح موجودٌ سلفًا في `database/migrate.php:53-64` لكنّ الرسالة لا تدلّ عليه |
| ج٤ | `database/schema/schema.sql:8926` يحمل ترتيبًا حصريًّا لـ MySQL 8 | `SET collation_connection = 'utf8mb4_0900_ai_ci';` — MariaDB لا تعرفه، فيتوقّف الاستيراد عنده. مصدرُه `app/Install/SchemaDumper.php:163` الذي ينسخ ترتيبَ المنظور من الخادم كما هو |
| ج٥ | لا يوجد فاحصُ بيئةٍ واحد | `tools/` فيه مسابرُ متخصّصةٌ فقط: `n26_infra_probe` · `schema_drift` · `nav_seven_guard` · `uat_hardening_verify` … ولا واحدَ منها يجيب سؤال «لماذا لا يعمل عندي؟» |
| ج٦ | `.claude/launch.json:6` يثبّت مسار PHP الخاص بـ WAMP | `"runtimeExecutable": "C:\\wamp64\\bin\\php\\php8.3.28\\php.exe"` — يكسر إعداد الطرف الآخر |
| ج٧ | عطلٌ حيٌّ في `role_board`: عمودٌ غيرُ موجود | `logs/php_errors.log` ← `Unknown column 'ed.operation_id' in 'where clause'`. وجدولُ `equipment_drivers` أعمدتُه: `id, company_id, equipment_id, employee_id, start_date, end_date, shift_type, status` — **لا `operation_id` فيه**. الاستعمالُ الخاطئ في `includes/role_board.php` أسطر **386 · 388 · 389 · 536 · 648** |

> **سابقةٌ مُثبَتةٌ تدلّ على النمط:** الالتزام `7f3064b` أضاف في `.htaccess` السطر
> `php_value auto_prepend_file "C:/wamp64/www/ems/includes/session_bootstrap.php"`،
> فانهار المشروع على كل جهازٍ لا يملك ذلك المسار حرفيًّا. أزاله `5e55e36` وبقي
> التوثيق في `docs/nfr/N-26_infra_readiness_ar.md:30`. **هذا البرومت يمنع تكرار
> النمط لا يعالج حادثةً واحدة.**

---

## المهمة

**النطاق:** قابليةُ نقل المشروع بين بيئتَي التطوير فقط. لا إعادةَ تصميمٍ ولا
ميزاتٍ جديدة ولا لمسَ منطقِ العمل — عدا العطل ج٧ فهو خطأٌ صريحٌ يُصلَح.

**المعيارُ الحاكم:** بعد هذا البرومت، يجب أن يكفيَ الطرفَ الآخر أمرٌ واحدٌ ليعرف
**بالضبط** ما ينقص جهازه، وأن ينجح `git pull` دون أن يكسر شيئًا عنده.

**قاعدةٌ لا تُخرَق:** لا مسارَ مطلقًا (`C:/…`) ولا اسمَ حزمةٍ (`xampp` / `wamp64`)
في أي ملفٍّ متتبَّعٍ في git بعد التنفيذ — عدا التعليقاتِ التوضيحية والوثائق.

---

## الجزء الأول — فاحصُ البيئة (العمود الفقري · نفّذه أولًا)

أنشئ **`tools/doctor.php`** — أمرٌ واحدٌ يشخّص الجهاز كاملًا. هذا أهمُّ مخرجات
البرومت: بدونه يبقى كلُّ عطلٍ عند الطرف الآخر رسالةً وتخمينًا.

```bash
php tools/doctor.php
```

يفحص ويطبع تقريرًا عربيًّا مرتّبًا، كلُّ بندٍ منه ✓ أو ✗ **مع الإجراء المطلوب نصًّا**:

1. **PHP** — الإصدارُ ≥ 8.2 (يفرضه `phpoffice/phpspreadsheet ^5.4` في `composer.json`؛
   وقد سُجِّل فعلًا `Composer detected issues… You are running 8.1.25`)،
   والامتداداتُ اللازمة: `mysqli` · `mbstring` · `json` · `zip` · `gd` · `openssl`.
2. **`.env`** — موجودٌ ومقروء؟ وأيُّ مفاتيحَ يقرأها الكود وتنقصه؟ (قارِنْ آليًّا
   مقابل `.env.example` — لا قائمةً مكتوبةً بيدك تتقادم).
3. **الاتصال بالقاعدة** — ينجح؟ وباسمِ أيِّ مستخدم؟
4. **محرّك القاعدة** — MySQL أم MariaDB؟ والإصدار؟ **مع تحذيرٍ صريحٍ عند
   MariaDB** يشرح أثرَه (راجع الجزء الرابع).
5. **الترحيلات المعلَّقة** — العددُ وأسماءُ أولِ خمسةٍ منها. وإن غاب
   `DB_MIGRATOR_USER` فقُلْ ذلك هنا صراحةً قبل أن يصطدم به.
6. **سلامةُ التنقّل** — هل `main/my_workspace.php` مسجَّلٌ في `modules` و`nav_items`؟
   (الأعدادُ المرجعيةُ على القاعدة السليمة: `modules` 219 · `nav_items` 661 ·
   `role_permissions` 1079 · و«مساحة عملي» في 25 صفًّا). هذا الفحصُ وحدَه يفسّر
   عرضَي «السايدبار ناقص» و«الداشبورد القديمة» — وكلاهما بياناتٌ لا كود.
7. **الكتابة** — `logs/` و`storage/` قابلان للكتابة؟
8. **قابليةُ النقل** — `.htaccess` خالٍ من `auto_prepend_file` ومن أي مسارٍ مطلق
   (نفسُ الشرط المحروس في `tests/nfr_infra_test.php:45`).

**رمزُ الخروج:** `0` إن كان كلُّ شيءٍ ✓، و`1` إن وُجد ✗ — ليصلح للأتمتة.
**لا يكتب شيئًا ولا يُعدِّل قاعدةً** — تشخيصٌ محض. و**يعمل حتى لو فشل الاتصال
بالقاعدة** (يتخطّى بنودَها ويكمل) — وإلا صار عديمَ الفائدة في أسوأ الحالات.

---

## الجزء الثاني — إغلاقُ الجذور ج١ إلى ج٣ و ج٦

### ج١ · أكمِل `.env.example`
أضِف الستةَ عشرَ مفتاحًا الناقصة، كلٌّ بسطرِ شرحٍ عربيٍّ وقيمةٍ افتراضيةٍ آمنةٍ
(الافتراضُ الآمن = سلوكُ اليوم عند غياب المفتاح، لا سلوكٌ جديد). **استخرجْ
القائمةَ آليًّا** من `ems_env('…')` في الكود ثم قارِنْ — لا تكتفِ بجدول ج١ أعلاه.

### ج٢ · أزِلْ افتراضاتِ XAMPP من `admin/includes/db_tools.php`
السطران 20 و385. الافتراضُ الجديد **يُكتشَف وقتَ التشغيل** لا يُثبَّت:
- `PHP_BIN` → `PHP_BINARY` أولًا (وهو دقيقٌ دائمًا في CLI)، ثمّ `.env`، ثمّ بحثٌ
  في `PATH`، وأخيرًا فشلٌ برسالةٍ واضحةٍ تطلب ضبطَ `PHP_BIN`.
- `MYSQL_BIN_DIR` → `.env`، ثمّ `PATH`، ثمّ المواضعُ الشائعةُ للحزمتين **بالفحص
  لا بالافتراض** (`is_dir`)، وإلّا فشلٌ برسالةٍ واضحة.
ولا تُسقِطْ صامتًا إلى مسارِ حزمةٍ بعينها.

### ج٣ · اجعلْ رسالةَ `migrate.php` قابلةً للتنفيذ
عند فشلِ إنشاء `schema_migrations` بسبب صلاحيات، لا تطبعْ خطأَ MySQL الخام.
اطبعْ سببًا وحلًّا: أن اتصالَ التطبيق `ems_app` بلا `CREATE` **بتصميمٍ مقصود**،
وأن الحلَّ ضبطُ `DB_MIGRATOR_USER` و`DB_MIGRATOR_PASS` في `.env` (المسارُ مبنيٌّ
سلفًا في `database/migrate.php:53-64`)، مع مثالِ `GRANT` جاهزٍ للنسخ. وأضِفْ
فحصًا **استباقيًّا** قبل أول DDL: إن غاب الزوجُ فتوقّفْ بهذه الرسالة فورًا.

### ج٦ · حيِّدْ `.claude/launch.json`
أزِلْ تثبيتَ `C:\wamp64\bin\php\php8.3.28\php.exe`. اجعلْه `php` من `PATH`، أو
وثّقْ أن الملفَّ محليٌّ لكلِّ جهاز. وراجعْ `.claude/settings.json` — فيه مساراتُ
`c:/xampp/…` في قائمة الأذونات (أسطر 23-34)؛ ليست ضارّةً بالتشغيل لكنّها من
نفس النمط.

---

## الجزء الثالث — العطل الحيّ ج٧

في `includes/role_board.php` أسطر **386 · 388 · 389 · 536 · 648** يُستعمل
`equipment_drivers ed` بشرطِ `ed.operation_id`، والعمودُ **غيرُ موجود**. الجدولُ
يربط **معدّةً بموظّف** (`equipment_id` · `employee_id`) لا عمليةً بموظّف.

المقصودُ من البطاقتين («تعمل بلا مشغّلٍ نشط» و«عاملةٌ بلا مشغّل») واضحٌ من
عنوانيهما. **استنتجِ الربطَ الصحيحَ من المخطّط الحيِّ لا من التخمين**: افحصْ كيف
تصل `operations` إلى `equipment_drivers` (الأرجحُ عبر معدّةِ العملية)، ثمّ صحّح
الخمسةَ المواضع. وتحقّقْ أن العدَّ الناتجَ منطقيٌّ على بياناتٍ حقيقيةٍ لا أن
الاستعلامَ «لم يعد يرمي خطأ» فحسب.

---

## الجزء الرابع — حسمُ محرّك القاعدة (قرارُ مالكٍ — اسألْ قبل التنفيذ)

هذا الجذرُ **لا يُغلَق بالتزامٍ في المستودع** لأنه اختلافُ منتجَين. اعرضِ
الخيارين واطلبِ القرار:

**(أ) توحيدُ الجهازين على MySQL 8.4 — المُوصى به.** القاعدةُ الحيّةُ مبنيّةٌ عليه.
الطرفُ الثاني يُبقي XAMPP لأباتشي وPHP، ويستبدل محرّكَ القاعدةِ وحدَه. عندها لا
يلزم أيُّ تنازلٍ في المخطّط، ويبقى `schema.sql` كما هو. المطلوبُ من المنفّذ:
توثيقُ الاشتراط في `docs/SETUP_ar.md` + جعلُ `doctor.php` يُخرج تحذيرًا عاليَ
الوضوحِ على MariaDB.

**(ب) دعمُ MariaDB 10.4 أيضًا.** يوجب معالجةَ ج٤ (`schema.sql:8926`) في المُصدِّر
`app/Install/SchemaDumper.php:163` بتحويل ترتيباتِ MySQL-8 الحصرية إلى مقابلٍ
محمولٍ (`utf8mb4_unicode_ci` — وهو نفسُ ترتيبِ الاتصالِ المفروضِ في `config.php:234`)،
ثمّ إعادةُ توليد `schema.sql` وتحديثُ `MANIFEST.json`. وينبغي أن تُصارَح بأن هذا
**التزامٌ دائم**: 34 عمودَ `JSON` و32 عمودًا محسوبًا و169 قيدَ `CHECK` تحتاج تحقّقًا
مستمرًّا على المحرّكين، وسلوكُ `JSON` يختلف بينهما اختلافًا حقيقيًّا.

**لا تنفّذْ (ب) دون قرارٍ صريحٍ من المالك.**

---

## الجزء الخامس — دليلُ الإقلاع والحارس

### `docs/SETUP_ar.md` — من صفرٍ إلى نظامٍ يعمل
صفحةٌ واحدةٌ مختصرةٌ يتبعها القادمُ الجديدُ دون أن يشرح له أحد. لا تكرّرْ
`docs/INSTALL_ar.md` — أحِلْ إليه للتفصيل. الترتيبُ الملزِم:

1. المتطلّبات (PHP ≥ 8.2 · محرّكُ القاعدةِ المُقرَّر) → **`php tools/doctor.php`**
2. `.env` من `.env.example` — وما الذي يجب تعبئتُه فعلًا
3. `php database/install.php --check --config=install.json` ثمّ التنصيب
4. `php database/migrate.php up` — **ومصيدةُ `DB_MIGRATOR_USER`**
5. **بعد كل `git pull`: شغّلْ `migrate.php up`.** واشرحْ لماذا: السايدبارُ يُبنى
   من جدول `nav_items` باستعلامٍ حيٍّ في `includes/unified_nav.php:66`، فالشاشاتُ
   الجديدةُ **بياناتٌ لا كود** — كودُها يصل بالسحب، وتسجيلُها لا يصل إلا بالترحيل.
6. مَن يدخل من بوّابة الشركات يخرج ويدخل بعد الترحيل — الصلاحياتُ مخزَّنةٌ في
   الجلسة في `company/auth.php:304`. أمّا البوّابةُ الرئيسيةُ فتقرأ حيًّا.

### `tests/portability_test.php` — حتى لا يعود النمط
اختبارُ انحدارٍ على نسقِ `tests/nfr_infra_test.php` القائم، يفشل إن:
- ظهر مسارٌ مطلق (`[A-Za-z]:[\\/]`) أو اسمُ حزمة (`xampp` / `wamp64`) في أيِّ ملفٍّ
  متتبَّعٍ من نوع `.php` أو `.htaccess` أو `.json` أو `.ini` — عدا التعليقاتِ
  والوثائقِ و`vendor/`.
- وُجد مفتاحٌ يقرأه الكود بـ`ems_env()` وليس في `.env.example` **(الفحصُ آليٌّ
  مقارنةً — هذا هو الحارسُ الحقيقي لـ ج١، والقائمةُ اليدويةُ تتقادم فورًا)**.
- عاد `auto_prepend_file` إلى `.htaccess`.

---

## قبل التنفيذ

اعرضْ خطةً موجزةً: الملفّاتُ التي ستُنشأ، والتي ستُعدَّل، وبنودُ `doctor.php`
النهائية، **وقرارُك المقترحُ في الجزء الرابع مع سببه**. انتظرِ الموافقة.

## التحقّق (إلزامي — لا يُقبل «يبدو صحيحًا»)

1. `php tools/doctor.php` على هذا الجهاز ← كلُّ البنود ✓ ورمزُ الخروج `0`.
2. `php tests/portability_test.php` ← ناجح.
3. `php tests/nfr_infra_test.php` ← ما زال ناجحًا (لا انحدار).
4. `php database/migrate.php status` ← يعمل، أو يفشل **بالرسالة الجديدة الواضحة**
   لا بخطأ MySQL الخام.
5. ج٧: افتحْ لوحةَ الأدوار وتأكّدْ أن `logs/php_errors.log` **خالٍ** من
   `Unknown column 'ed.operation_id'`، وأن عدَّ البطاقتين منطقيٌّ على بياناتٍ حقيقية.
6. `git grep -nIE '[Cc]:[\\/]{1,2}(wamp64|xampp)'` على الملفّات المتتبَّعة ←
   لا نتيجةَ خارج التعليقاتِ والوثائقِ و`vendor/`.
7. **محاكاةُ الطرف الآخر:** انسخِ المستودعَ إلى مجلدٍ آخرَ تمامًا بلا `.env`،
   وشغّلْ `php tools/doctor.php` ← يجب أن يعمل ويشخّص النقصَ بوضوحٍ **دون أن
   ينهار**. هذا هو الاختبارُ الحقيقيُّ للبرومت كلِّه.

## ما لا يجوز فعلُه

- **لا تُضِفْ `.env` إلى git** — مُستثنًى في `.gitignore:7` بحقّ، وفيه كلمةُ المرور.
- لا تُضعِفْ صلاحياتِ `ems_app` — انعدامُ `CREATE` تصميمٌ مقصود (ADR-04)، والحلُّ
  مستخدمُ ترحيلٍ منفصلٌ لا توسيعُ صلاحياتِ التطبيق.
- لا تُشغِّلْ DDL خارج `database/migrations` — قيدُ ADR-03، والغلافُ الوحيد
  `ems_runtime_ddl()` في `config.php:530`.
- لا تُعِدْ توليدَ `schema.sql` إلّا إن أُقرَّ الخيارُ (ب) صراحةً.
- لا تُصلحْ ج٧ بحذفِ الشرطِ أو كتمِ الخطأ — البطاقتان لهما معنًى تشغيليٌّ يجب
  أن يبقى صحيحًا.
