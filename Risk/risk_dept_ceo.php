<?php
/**
 * Risk/risk_dept_ceo.php — المخاطر المؤسسية (زاوية مكتب الرئيس والنواب)
 * ─────────────────────────────────────────────────────────────────────────
 * ⇐ INJ-0412
 *
 * **ولماذا لا تُثبَّت لها وحدةُ هيكل**: بقيةُ أغلفةِ العائلةِ تُثبّت زاويتَها على
 * `org_units.unit_code` لأنَّ لكلِّ إدارةٍ وحدةً. ومكتبُ الرئيسِ **ليس إدارةً
 * في الهيكلِ** — لا صفَّ له في `org_units`، وزاويتُه بطبعِها **المحفظةُ
 * الكاملةُ**: RU-01 «المخاطر المؤسسية والاستراتيجية» تنظر عبرَ الإداراتِ كلِّها
 * لا داخلَ واحدةٍ منها.
 *
 * فلو اختُرعت له وحدةٌ لصار الرئيسُ يرى **إدارةً واحدةً اسمُها مكتبُه** —
 * وهو عكسُ المطلوب. والمكوّنُ يدعم المحفظةَ الكاملةَ سلفًا: `risk_full_portfolio()`
 * في `_risk_common.php` تمنحها للدور 9 ولإدارةِ المخاطرِ وللسوبر.
 *
 * ◆ ولا يُوسَّع نطاقُ أحدٍ هنا: الغلافُ **لا يمنح** المحفظةَ، بل يمتنع عن
 *   **تضييقِ** الزاوية. فمَن دخلها بلا محفظةٍ كاملةٍ يبقى على زاويةِ إدارتِه
 *   كما يحكم `_risk_common.php` — والحارسُ يمنع مَن لا صلاحيةَ عرضٍ له أصلًا.
 */
require_once __DIR__ . '/_risk_common.php';

/* لا تثبيتَ زاويةٍ — المحفظةُ الكاملةُ لمن يملكها، وزاويةُ إدارتِه لمن لا يملك.
   وحذفُ `$_GET['unit']` يمنع تمريرَ وحدةٍ بالمسار فتُقرأ الشاشةُ إدارةً بعينها. */
unset($_GET['unit']);

require __DIR__ . '/dept_risk_space.php';

/* حزمةُ الحالاتِ الدنيا (بوابة ٩): تحميلٌ وفراغٌ وخطأٌ — مخفيةٌ افتراضًا
   ويُظهرها منطقُ الشاشةِ عند حالِها. الدالةُ من ux_components التي تُحمِّلها القشرة. */
if (function_exists('ems_states_bundle')) {
    echo ems_states_bundle('لا بيانات مخاطر لهذه الإدارة بعد',
                           'تسجل المخاطر من سجلها المركزي فتظهر هنا');
}
