<?php
/** FinRequests/my_requests.php — تحويلٌ إلى الشاشة الموحَّدة.
 * ═══════════════════════════════════════════════════════════════════════════
 * ◆ دُمجت «طلباتي المالية» في `request_form.php` (2026-08-21) فصارت شاشةً
 *   واحدةً تجمع بطاقاتِ الإحصاءِ وفلاترَ البحثِ والقائمةَ ونموذجَ الإنشاءِ
 *   وسجلَّ الطلب — على نمطِ شاشةِ العملاء. فلا معنى لبابَين إلى رحلةٍ واحدة.
 *
 * ◆ ويبقى هذا الملفُّ **مُحوِّلًا لا شاشة**: روابطُ محفوظةٌ وإشعاراتٌ قديمةٌ
 *   ومسارُ `back=my_requests.php` في `request_actions.php` تصل إليه، فيَعبر
 *   بها إلى وجهتها بدل صفحةِ خطأ. و`?id=` يُمرَّر كما هو فيفتح سجلَّ الطلب.
 *
 * ◆ **وكلُّ نقرةٍ تُعَدّ** في `nav_redirects` (E-15/E-23 · SPEC-00 §3-②) —
 *   والحذفُ النهائيُّ قرارُ مالكٍ بعد أن يثبت العدّادُ صفرَه مدةً كافية.
 *
 * ◆ ولا حارسَ صلاحياتٍ هنا بقصد: الحارسُ في الوجهةِ، وإجراؤه مرتَين يعني
 *   قياسَ شاشتَين لواحدة. والتحويلُ لا يكشف بيانًا.
 * ═══════════════════════════════════════════════════════════════════════════
 */
require_once __DIR__ . '/../includes/session_bootstrap.php'; // مخزن الجلسات المشترك — يسبق session_start()
session_start();
if (!isset($_SESSION['user'])) {
    header('Location: ../login.php');
    exit();
}

include '../config.php';
require_once __DIR__ . '/../includes/route_redirect.php';

$id = isset($_GET['id']) ? intval($_GET['id']) : 0;
ems_route_redirect($conn, 'FinRequests/my_requests.php',
    'request_form.php' . ($id > 0 ? '?id=' . $id : ''));
