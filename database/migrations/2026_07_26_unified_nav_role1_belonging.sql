-- ═══════════════════════════════════════════════════════════════════════════
-- قاعدة التبعية — تصحيح بذر الدور 1 (قرار المالك 2026-07-26)
-- ───────────────────────────────────────────────────────────────────────────
-- «الدورُ يحدد الروابطَ التي تتبع له، والصلاحيةُ تحدد أتظهر الصفحةُ له أم لا.»
--
-- فالظهور شرطان معًا: ① صفٌّ في nav_items (التبعية) ② can_view=1 (الترشيح).
-- والتبعيةُ للدور 1 هي قائمتُه المرئية اليوم نفسها (ملكيتُه + بوابة الطلبات
-- + منحُ المالية المعروضة له) — أما الستُّ التي كانت صلاحيتُها صامتةً بلا
-- رابطٍ (شاشاتُ إداراتٍ أخرى) فليست تابعةً له: تُعطَّل active=0 وتبقى صفوفًا
-- قابلةً للتفعيل الفردي من شاشة الإدارة متى قرر المالك.
--
-- النتيجة: التحويل محافظٌ 1:1 — صفرُ رابطٍ جديد، صفرُ رابطٍ مفقود؛
-- الجديدُ الوحيد: الأبوابُ الستة وفحصُ الصلاحية وتوحيدُ المكرر.
-- ═══════════════════════════════════════════════════════════════════════════

UPDATE `nav_items` SET `active` = 0
WHERE `role_id` = 1 AND `route` IN (
  'Employees/employees.php',                    -- تتبع الموارد البشرية (4)
  'Equipments/equipments_fleet.php',            -- تتبع الأسطول (3)
  'Equipments/fleet_models.php',                -- تتبع الأسطول (3)
  'Equipments/fleet_depreciation_profiles.php', -- تتبع الأسطول (3)
  'main/project_users.php',                     -- تتبع إداراتٍ أخرى (2·3·4·5·6)
  'Timesheet/timesheet_type.php'                -- تتبع مدير الموقع (5)
);
