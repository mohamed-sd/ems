-- ═══════════════════════════════════════════════════════════════════════════
-- H-09-③ · المسارُ الإنتاجي (المشروعيون والمشغّلون) — 2026-07-30
-- البطاقة: docs/specs/H-09_3_production_path.md
-- المصدر: ENT-01 §3-② («**سجلُّ الوحدات القانوني بعد اكتمال أحكام الأطراف** ·
--         أساسُ عقده (منفَّذٌ · استعدادٌ · حضورٌ · طنٌ · نقلةٌ · مترٌ · مركّب)
--         والحافزُ من قاعدته») · §4-الحوافز («الأساسُ … ثم **السقفُ والحدُّ
--         الأدنى**، ثم توزيعُه على مستفيديه بنسبهم Σ=100٪») · PLAN-01 §6.1-③
-- ───────────────────────────────────────────────────────────────────────────
-- لا جداولَ جديدة: المصادرُ كلُّها مبنيةٌ سلفًا —
--   · `unit_party_awards` (وحدةُ الطرف وكميتُه — بُنيت مع المروحة)
--   · `unit_entries` (السجلُّ القانوني بحالاته)
--   · `incentive_rules` + `incentive_allocations` (H-08-③)
-- والناقصُ **نوعا سطرٍ** يميّزان مولَّداتِ هذه الشريحة عمّا قبلها.
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE `payroll_lines`
  MODIFY COLUMN `line_kind`
    ENUM('component','overtime','absence_deduction','production','incentive')
    NOT NULL DEFAULT 'component'
    COMMENT 'نوعُ السطر — production/incentive مولَّدا المسار الإنتاجي (H-09-③)';
