-- ═══════════════════════════════════════════════════════════════════════════
-- D02 §3.5: إكمال حالات الوردية — الثلاث الناقصة ذات الأثر المالي
-- ───────────────────────────────────────────────────────────────────────────
-- سجلّ الدوام يسجّل اليوم ثمانيًا من حالات الوردية الخمس عشرة، ومنها أهمُّها
-- ماليًّا (الاستعداد بسبب العميل · توقف العميل · المعلّق للاعتماد). والناقص
-- ثلاثٌ لكلٍّ منها حكمٌ تعاقديٌّ مختلفٌ عن «عطل أخرى» التي تبتلعها اليوم:
--
--   ⑤ توقف على المورد   — لا مستحقَ له، وقد ينقلب خصمًا/تحميلًا عليه (§3.5)
--   ⑭ توقف مخطط        — يحسمه جدول سياسة العقد: غير مستحقة/جزئية/مشمولة
--   ⑮ قوة قاهرة        — بند العقد يسوّيها حالةً بحالة (تعليق/تمديد/إعفاء)
--
-- بلا هذه الأعمدة تُسجَّل الثلاث في «ساعات أعطال أخرى» فتضيع مسؤوليتُها —
-- ولا يمكن لسياسة الاستحقاق (المرحلة اللاحقة) أن تحكم على ما لا تميّزه.
--
-- ⚠️ البادئة ts_ إلزامية وليست تجميلًا: شاشة Timesheet/timesheet_details.php
-- تستعمل `SELECT *` مع خمسة جداولَ مربوطة، وfetch_assoc يحتفظ بآخر قيمةٍ
-- للاسم المكرر — فتُدهَس أعمدةُ الدوام بما بعدها (t.status مدهوسٌ بـ
-- project.status وt.type بـequipments.type وt.shift_hours بـoperations.shift_hours
-- اليوم فعلًا). البادئة تُخرج الأعمدة الجديدة من دائرة التصادم كلها.
--
-- إضافةٌ محضة: لا حذفَ ولا إعادةَ تسمية ولا مساسَ بصفٍّ قائم. القيمة صفرٌ
-- افتراضًا فلا يتغيّر أي مجموعٍ محسوبٍ في التقارير العشرين.
-- التطبيق بعميل utf8mb4 عبر database/migrate.php حصرًا.
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE `timesheet`
  ADD COLUMN `ts_supplier_stop_hours`  DECIMAL(6,2) NOT NULL DEFAULT 0.00
    COMMENT 'D02 §3.5 ⑤ توقف على المورد — لا مستحقَ له وقد ينقلب خصمًا'
    AFTER `other_fault_hours`,
  ADD COLUMN `ts_planned_stop_hours`   DECIMAL(6,2) NOT NULL DEFAULT 0.00
    COMMENT 'D02 §3.5 ⑭ توقف مخطط — يحسمه جدول سياسة العقد'
    AFTER `ts_supplier_stop_hours`,
  ADD COLUMN `ts_force_majeure_hours`  DECIMAL(6,2) NOT NULL DEFAULT 0.00
    COMMENT 'D02 §3.5 ⑮ قوة قاهرة — تسويةٌ حالةً بحالة وفق بند العقد'
    AFTER `ts_planned_stop_hours`;
