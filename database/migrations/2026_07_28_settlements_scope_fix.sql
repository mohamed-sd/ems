-- ═══════════════════════════════════════════════════════════════════════════
-- تضييقُ نطاق شاشة التسويات: إخراجُ إدارة التشغيل — 2026-07-28
-- ───────────────────────────────────────────────────────────────────────────
-- الترحيلُ السابق (settlements_screen) منح إدارةَ التشغيل (1) عرضَ التسويات
-- قياسًا على شاشة المستخلصات — وهو **قياسٌ خاطئ**: المستخلصُ يقوم على وحدات
-- التشغيل فمن حقّه أن يراها، أما تسويةُ المورد فمستندٌ ماليٌّ بين إدارة الموردين
-- والمالية (UX-05 §4)، ولا يضيف اطلاعُ التشغيل عليه شيئًا لمهمته.
--
-- وقاعدةُ الدستور «التبعيةُ تحدد القائمة والصلاحيةُ ترشّح» تقتضي أقلَّ امتيازٍ
-- يكفي المهمة — ومنحٌ بلا مهمةٍ حملٌ معرفيٌّ لا فائدةَ فيه.
--
-- كشفه حارسُ التنقّل (`unified_nav_test`): يؤكّد عددَ عناصر الدور الرائد
-- تأكيدًا مطلقًا، فأيُّ منحٍ زائدٍ له يُسقطه — وهو الغرضُ من ذلك التأكيد.
--
-- الرجوع: إعادةُ الصفّين للدور 1 بـcan_view=1.
-- ═══════════════════════════════════════════════════════════════════════════

DELETE FROM `nav_items`
 WHERE `role_id` = 1 AND `route` = 'Suppliers/settlements.php';

DELETE FROM `role_permissions`
 WHERE `role_id` = 1
   AND `module_id` = (SELECT `id` FROM (SELECT `id` FROM `modules`
                       WHERE `code` = 'Suppliers/settlements.php') m);
