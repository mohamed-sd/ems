-- ═══════════════════════════════════════════════════════════════════════════
-- N-01 · تحصينُ دفتر عطالة المروحة بنيويًّا — 2026-07-30
-- ───────────────────────────────────────────────────────────────────────────
-- **الفجوة المقيسة**: `fin_event_links` هو دفترُ عطالة المروحة (EffectFanout
-- §6.1-③: وجودُ الصف = الأثرُ تولّد فلا يتكرر) لكنه كان **بلا أي فهرسٍ فريد** —
-- فالعطالةُ فحصٌ تطبيقيٌّ وحدَه (existingEffects) يلتفّ عليه سباقُ طلبين
-- متزامنين أو كاتبٌ جديدٌ ينسى الفحص.
--
-- **المفتاح**: (الشركة × نوع الأب × مرجعه × نوع الأثر) — وهو حرفيًّا مفتاحُ
-- الفحص التطبيقي القائم (existingEffects يفهرس بـeffect_type لكل أب).
-- **القياس قبل التثبيت**: صفرُ تكرارٍ في الصفوف الخمسين القائمة.
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE `fin_event_links`
  ADD UNIQUE KEY `uq_link_parent_effect`
      (`company_id`, `parent_kind`, `parent_ref`, `effect_type`);
