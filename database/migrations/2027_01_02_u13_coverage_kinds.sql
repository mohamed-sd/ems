-- update0013 · أصنافُ التغطيةِ الثلاثةُ الجديدة: catalogue · uat · harness
-- ═══════════════════════════════════════════════════════════════════════════
-- كان `coverage_kind` يعرف ستةَ أصناف، و`seed` فيها تخلط ثلاثةَ أشياءَ مختلفة:
--
--   ① بندًا **مرجعيًّا بطبعِه** (وصفٌ وظيفيٌّ · بندُ ميثاق) لا يُشتقُّ منه حكمٌ
--     آليٌّ أصلًا — واكتمالُه أن يكون مسجَّلًا مقروءًا.
--   ② سيناريوَ قبولٍ **يُنفَّذ بيدِ المستخدمِ** في محضرِ القبول — ولا يُؤتمت.
--   ③ اختبارًا **يُنفَّذ آليًّا** ولم يكن قد نُفِّذ بعد.
--
-- وخلطُها تحت اسمٍ واحدٍ يجعل «مكتوبٌ ولم يُنفَّذ» و«مرجعٌ لا يُنفَّذ» سواءً —
-- فلا يُعرف ما بقي عملًا مما تمَّ بطبعِه. والتفريقُ يُظهر المتبقيَ الحقيقي.
--
-- ◆ گوتشا مُثبَتة: كتابةُ قيمةٍ خارجَ الـENUM **تُبتلع صامتةً** فيصير العمودُ
--   فارغًا — وقعت فعلًا على 244 صفًّا قبلَ هذا الترحيل.
--
-- المصدر: قرارُ تنفيذٍ ضمنَ update0013 · البند ③ «تحويلُ ما يستحقُّ إلى سلوك»
-- idempotent: MODIFY يُعيد التعريفَ نفسَه بلا ضرر.

ALTER TABLE `gov_doc_registry`
  MODIFY COLUMN `coverage_kind`
    ENUM('table','service','screen','guard','harness','catalogue','uat','seed','none')
    NOT NULL DEFAULT 'none'
    COMMENT 'harness = يُنفَّذ آليًّا · catalogue = مرجعٌ بطبعِه · uat = بيدِ المستخدم · seed = مكتوبٌ لم يُنفَّذ بعد';
