<?php
/**
 * Tickets/dept_inbox.php — مُحوِّلٌ إلى تبويبِ «موجَّهة لإدارتي»
 * ═══════════════════════════════════════════════════════════════════════════
 * ◆ كانت شاشةً مستقلّةً («صندوقُ بلاغاتِ الإدارة» · «بلاغات إدارتي») مربوطةً
 *   بـ34 دورًا من 35 — وهي في حقيقتها **مرشِّحٌ** على `ticket_workstreams`
 *   بوحدةِ دورِ المستخدم: نفسُ استعلامِ `ticket_workstreams_board.php` مقصورًا
 *   على وحدةٍ واحدةٍ وبأعمدةٍ أقل.
 *
 * ◆ وثلاثةُ عيوبٍ قِيست فيها قبل الدمج:
 *   ① **بلا حارسِ صلاحيةٍ إطلاقًا** — جلسةٌ وشركةٌ فقط. و`action_guard` المركزيُّ
 *      لا يغطّيها لأنّه يعمل على نقاطِ AJAX وحدَها.
 *   ② رابطُ رقمِ البلاغِ يوجِّه إلى `tickets_list.php?open=` وهو **معامَلٌ ميّت**.
 *   ③ بلا فلاترَ ولا DataTable ولا تصدير — والجدولُ ساكنٌ `data-no-dt`.
 *
 * ◆ فنُقلت تبويبًا داخلَ `tickets_list.php` لتَرِثَ حارسَ `can_view` والفلاترَ
 *   وزرَّ Excel، ويبقى للمستخدمِ **بابٌ واحدٌ** للبلاغات. والملفُّ يبقى مُحوِّلًا
 *   لا يُحذف: 34 صفَّ تنقلٍ ومرجعياتُ `modules` والإشاراتُ المحفوظةُ تقصده.
 *
 * ◆ ولا يُترك مسارٌ عامٌّ بلا وظيفةٍ ولا حراسة: التحويلُ يسبقه فحصُ الجلسة،
 *   والحارسُ الحقيقيُّ ينفُذ في الوجهةِ نفسِها.
 * ═══════════════════════════════════════════════════════════════════════════
 */
require_once __DIR__ . '/../includes/session_bootstrap.php'; // مخزن الجلسات المشترك — يسبق session_start()
session_start();
if (!isset($_SESSION['user'])) { header('Location: ../login.php'); exit(); }

header('Location: tickets_list.php?tab=dept', true, 302);
exit();
