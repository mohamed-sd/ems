# EMS — قواعدُ العملِ في هذه الشجرة

> **قاعدةٌ لا يُنفِذها حاجبٌ زينة.** في هذه الشجرة 1,782 ملفَّ وثيقةٍ، ولم يمنع
> أيٌّ منها العطبَ الذي كلّف 547 رابطًا ميتًا. فكلُّ قاعدةٍ هنا **لها حاجبٌ
> مذكورٌ باسمِه** — وما لا حاجبَ له ليس قاعدةً بل رجاء.

---

## ① العارضُ يشتقُّ من الحارس — ولا يقرِّر من سجلٍّ موازٍ

سطحٌ يُظهِر روابطَ شاشاتٍ **لا يقرِّر ما يُعرَض** من `role_permissions` أو أيِّ
سجلٍّ آخر. يسأل **الحارسَ نفسَه** الذي يحكم الوصول:
`get_module_permissions` · `check_page_permissions` · `ems_template_nav_state`.

**المعادلة**: `Rendered Sidebar = Active Workspace Placements ∩ User Authorized Screens`
— و«المُصرَّحُ به» هو **ما يفتحه الحارسُ**، لا ما سجّله جدولٌ موازٍ.

**لماذا**: هذا النمطُ بعينِه أنتج **547 رابطًا يُعرَض ويردُّه بابُه** في **خمسةِ
مواضعَ**؛ وُجدت اثنتان بالبحثِ عن أسماءِ دوالَّ معروفة، والأربعُ الباقيةُ
بالمصادفة. **والبحثُ عن الفحصِ الخطأ لا يجد غيابَ الفحص.**

**الحاجب**: `tests/nav_parallel_register_ratchet.php` · و`tests/perm01_render_vs_guard.php`
(‏يُعدِّد الأسطحَ ولا يستوردها قائمة — فسطحٌ جديدٌ يُرسِّب حتى يُصنَّف عمدًا).

---

## ② مصدرٌ يُضاف ⇒ مصدرٌ يُحذف

لا يُنشأ جدولُ «مصدرِ حقيقةٍ» جديدٌ إلّا بحذفِ آخرَ أو بإعلانِ تاريخِ إعدامِه.
وجدولُ **مصالحةٍ** بلا تاريخِ انتهاءٍ **ممنوع**: إن احتجتَ جدولًا يوفّق بين
مصدرَين فذاك اعترافٌ بأنَّ أحدَهما زائد — فاكتب في الهجرةِ أيُّهما يموتُ ومتى.

**لماذا**: في القاعدة 1,279 جدولًا · **393 فارغًا تمامًا** · و173 لحملةٍ واحدة
(`repair01_*`). وفي `tools/` **803 سكربتٍ** — أدواتُ كشفِ الخلافِ صارت أكبرَ من
التطبيقِ الذي تفحصه (283 ألفَ سطرٍ مقابل 107 آلاف).

**الحاجب**: `TenantRegistry` يردُّ جدولًا غيرَ مصنَّفٍ (‏مُثبَتٌ: ردَّ جدولًا
أنشأتُه في 2028_06_06 حتى سُجِّل).

---

## ③ الهويّةُ مقفولةٌ في القاعدة لا مرصودةٌ باختبار

الشاشةُ **صفٌّ واحدٌ** في `modules` — و`UNIQUE(modules.code)` يمنع غيرَه.
والموضعُ في قائمةِ دورٍ (‏تسميةٌ · مجموعةٌ · ترتيبٌ · مالك) في `nav_items`
و`nav_workspace_placements`، **لا في جدولِ الهويّة**.

**لماذا**: كانت سبعُ شاشاتٍ بـ25 هويّة (‏`main/project_users.php` بعشر)، و**31%**
من أزواجِ (شاشةٍ × دور) تُعطي حكمًا متناقضًا بحسبِ النسخةِ المُلتقَطة. وحادثتانِ
موثَّقتانِ: لوحةٌ حُجبت عن كلِّ الأدوارِ إلّا واحدًا · وشاشةٌ حُرست بصلاحيةِ
شاشةٍ تشاركها الاسم.

---

## ④ صفرٌ لا يُقرَأ نجاحًا حتى يُثبَت أنه مقيس

كلُّ مقياسٍ يحمل **حارسَ مفردة**: «صفرُ مفحوصٍ» يُرسِّب لا يمرّ. وكلُّ حاجبٍ
يحمل **ضابطًا سالبًا**: يُكسَر الحالُ عمدًا ويُتحقَّق أن العدَّ **تحرَّك**.

**لماذا** — أربعُ حوادثَ مقيسةٍ في جولةٍ واحدة:
- «صفرٌ — تطابقٌ تامّ» ومفاتيحُ المُصيِّرِ بلا `.php` فلم تطابق شيئًا (والحقيقةُ 454)
- «صفرُ هدفٍ مبنيّ» من مقارنةِ `x.php` بـ`x` (والحقيقةُ 343 من 343 مبنيّة)
- حاجبٌ أخضرُ يمسح سطحًا واحدًا من ستّة (وفي الشجرةِ 918 رابطًا بلا حراسة)
- «ثلاثةُ مستخدمين عالقون» وهم **حسابات محذوفة** (‏المرشِّحُ لم يُصفِّ `is_deleted`)

---

## ⑤ قِسْ بالتصيير لا بالجدول

سطحٌ يُقاس **بما يظهر في جلسةِ مستخدمٍ حقيقيّ**، لا بما في جدولِه. صفٌّ قائمٌ
في `nav_items` قد لا يُصيَّر، وحكمٌ على غيرِ المُصيَّرِ حكمٌ على غيرِ محلِّه.
والقوّادُ **ينادي الدالّةَ نفسَها** ولا يُعيد بناءَ استعلامِها — فإعادةُ الكتابةِ
تقيس ما كتبناه نحن لا ما يراه المستخدم.

---

## ⑥ ما يخصُّ الهجرات

- **هجرةُ PHP سكربتٌ قائمٌ بذاته** يُعلن ما فعل ويختم بـ`ems_migration_recorded`
  — لا ملفٌّ يُرجع دالّة. و**مُعاوَدةٌ**: تشغيلٌ ثانٍ يجد صفرَ عملٍ ويخرج بنجاح.
- **لا DDL داخلَ معاملة**: `ALTER` يُحدث التزامًا ضمنيًّا في MariaDB — فمحاكاةٌ
  «مرتدّة» تلتزم فعلًا. (‏وقع مقيسًا · وغيّر بياناتِ إنتاج.)
- **حكمٌ يُسجَّل يُسمّي مداه**: حكمُ ملاحةٍ بلا `current_workspace` لا يُجيب سؤالَ
  «لماذا اختفى من هذه القائمة؟» — فيُرَدُّ عند بوّابةِ صفرِ الفقد.

---

## ⑦ حدودُ ما يُقرَّر بلا مالك

يُوثَّق **الواقعُ** (‏«هذه الشاشةُ تُبلَغ بالنقر — تستقبل `id`») بلا إذن.
ويُرفَع **قرارُ التصميم** (‏«أين تقع في القائمة؟») إلى مالكِ المجال:
`nav_legacy_disposition.decided_level` = `L2_DOMAIN_OWNER` أو `L4_OWNER`.
وكتابةُ حكمٍ باسمِ المالكِ **تجعل السجلَّ يكذب**.

---

## أوامرُ التحقّق

```bash
php tests/perm01_render_vs_guard.php          # تطابقُ العارضِ والحارس (6 أسطح)
php tests/nav_parallel_register_ratchet.php   # سقّاطةُ السجلِّ الموازي
php database/migrate.php status               # الدفترُ والقرص
php tools/round_closeout.php                  # بوّاباتُ إغلاقِ الجولة
```
