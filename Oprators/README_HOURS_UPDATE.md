# تحديث نظام التشغيل - إضافة حقول ساعات العمل

## 📅 تاريخ التحديث
17 فبراير 2026

## 🎯 الهدف
إضافة حقول جديدة إلى نظام إدارة التشغيل لتتبع:
1. **إجمالي ساعات العمل الكلية للآلية** (total_equipment_hours)
2. **عدد ساعات الوردية** (shift_hours)

## 📋 التغييرات المطبقة

### 1. تحديث قاعدة البيانات
**الملف:** `database/add_hours_to_operations.sql`

تم إضافة عمودين جديدين إلى جدول `operations`:
- `total_equipment_hours` DECIMAL(10,2) - إجمالي ساعات العمل للآلية
- `shift_hours` DECIMAL(10,2) - عدد ساعات الوردية

### 2. تحديث نموذج الإضافة
**الملف:** `Oprators/oprators.php`

**الحقول المضافة:**
```html
<div>
    <label><i class="fa fa-clock"></i> عدد ساعات العمل الكلية للآلية</label>
    <input type="number" name="total_equipment_hours" step="0.01" value="0" required />
</div>

<div>
    <label><i class="fa fa-hourglass-half"></i> عدد ساعات الوردية</label>
    <input type="number" name="shift_hours" step="0.01" value="0" required />
</div>
```

### 3. تحديث معالج البيانات (POST Handler)
تم تحديث استعلام INSERT لتضمين الحقول الجديدة:
```php
$total_equipment_hours = floatval($_POST['total_equipment_hours']);
$shift_hours = floatval($_POST['shift_hours']);

INSERT INTO operations (..., total_equipment_hours, shift_hours, ...)
VALUES (..., '$total_equipment_hours', '$shift_hours', ...)
```

### 4. تحديث عرض الجدول
تم إضافة عمودين جديدين في جدول العرض:
- عمود "ساعات العمل الكلية"
- عمود "ساعات الوردية"

## 🔧 خطوات التثبيت

### الخطوة 1: تحديث قاعدة البيانات
قم بتنفيذ الاستعلام SQL:
```bash
# افتح phpMyAdmin
# اختر قاعدة البيانات: equipation_manage
# اذهب إلى تبويب SQL
# قم بتنفيذ محتوى ملف: database/add_hours_to_operations.sql
```

أو باستخدام سطر الأوامر:
```bash
mysql -u root -p equipation_manage < database/add_hours_to_operations.sql
```

### الخطوة 2: التحقق من التحديث
1. افتح صفحة التشغيل: `Oprators/oprators.php`
2. تأكد من ظهور الحقلين الجديدين في النموذج
3. جرب إضافة تشغيل جديد مع إدخال قيم لساعات العمل والوردية
4. تحقق من ظهور القيم في الجدول

## 📊 البيانات الجديدة

### الحقول المضافة

| الحقل | النوع | الوصف | القيمة الافتراضية |
|-------|------|-------|-------------------|
| `total_equipment_hours` | DECIMAL(10,2) | إجمالي ساعات العمل الكلية للآلية | 0.00 |
| `shift_hours` | DECIMAL(10,2) | عدد ساعات الوردية للمعدة | 0.00 |

### مثال على الاستخدام
```
المعدة: حفار CAT 320
ساعات العمل الكلية: 8000.50
ساعات الوردية: 12.00
```

## 🎨 واجهة المستخدم

### نموذج الإضافة
- ✅ أيقونات توضيحية للحقول
- ✅ تسميات عربية واضحة
- ✅ حقول مطلوبة (required)
- ✅ دعم الأرقام العشرية (step="0.01")

### جدول العرض
- ✅ عرض ساعات العمل الكلية
- ✅ عرض ساعات الوردية
- ✅ تنسيق رقمي واضح
- ✅ قيمة افتراضية "0" للسجلات الفارغة

## 🔍 الفحص والاختبار

### 1. اختبار الإضافة
```php
// تأكد من وجود القيم في POST
var_dump($_POST['total_equipment_hours']);
var_dump($_POST['shift_hours']);
```

### 2. اختبار العرض
```sql
-- تحقق من البيانات في قاعدة البيانات
SELECT id, equipment, total_equipment_hours, shift_hours 
FROM operations 
ORDER BY id DESC 
LIMIT 10;
```

### 3. اختبار التحديث
```sql
-- تحديث قيمة موجودة
UPDATE operations 
SET total_equipment_hours = 100.50, shift_hours = 8.00 
WHERE id = 1;
```

## ⚠️ ملاحظات مهمة

1. **القيم الافتراضية:** الحقول الجديدة لها قيمة افتراضية `0.00`
2. **السجلات القديمة:** السجلات الموجودة ستحتوي على `0.00` للحقول الجديدة
3. **التحقق من الإدخال:** يتم التحقق من صحة القيم باستخدام `floatval()`
4. **الدقة:** يدعم النظام رقمين عشريين (مثال: 123.45)

## 🔄 التحديثات المستقبلية المقترحة

1. **حساب تلقائي:** احتساب ساعات العمل تلقائياً بناءً على تاريخ البداية والنهاية
2. **تقارير:** إضافة تقارير لمراقبة ساعات العمل للمعدات
3. **تنبيهات:** تنبيه عند تجاوز الحد الأقصى للساعات
4. **مخططات:** عرض مخططات بيانية لساعات العمل

## 📞 الدعم
في حالة وجود مشاكل:
1. تحقق من تنفيذ SQL بنجاح
2. تأكد من وجود الحقول في قاعدة البيانات
3. راجع ملف الأخطاء (error log)
4. تحقق من صلاحيات قاعدة البيانات

## ✅ قائمة التحقق

- [ ] تم تنفيذ ملف SQL بنجاح
- [ ] الحقول ظاهرة في نموذج الإضافة
- [ ] يمكن إدخال البيانات بنجاح
- [ ] البيانات تظهر في الجدول
- [ ] القيم العشرية تعمل بشكل صحيح
- [ ] البيانات تُحفظ في قاعدة البيانات

---
**آخر تحديث:** 17 فبراير 2026  
**الإصدار:** 1.0  
**المطور:** EMS Development Team
