# مجلد الاختبارات — CLI فقط، محجوب كليًا عن الويب (ADR-02).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
