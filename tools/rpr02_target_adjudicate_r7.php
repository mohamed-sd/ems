<?php
/**
 * tools/rpr02_target_adjudicate_r7.php — R7 · المصالحةُ اليدويّةُ للبقيّة (١٤)
 * ═══════════════════════════════════════════════════════════════════════════
 * `FINAL_CLOSE` البندان ④⑥: 14 هدفًا بلغ سقفُ تغطيتِه الثلثَين فلم تنطبق عليه
 * قواعدُ `R1`..`R6` الآليّةُ — **وبقاؤه بلا حكمٍ يُيتِّم متطلبَه** (14 متطلبًا
 * بلا حالة). `RPR-02` §٥·٣: كلُّ هدفٍ ينتهي إلى حكمٍ واحد.
 *
 * ◆ **R7 حكمُ وظيفةٍ لا تشابهُ حروف**: كلُّ حكمٍ هنا من مرشَّحين سمّاهم القياسُ
 *   الآليُّ في `match_witness`، ومن مسحٍ موجبٍ للسجلِّ عن سطحِ الوظيفةِ نفسِها
 *   (المسار والكيان والحبّة) — والشاهدُ يذكر القناةَ ويُسمّي المرفوضَ وسببَه.
 * ◆ ⛔ ولا يكتب فوقَ حكمٍ قائم — `verdict IS NULL` شرطُ الكتابة.
 *
 * التشغيل: php tools/rpr02_target_adjudicate_r7.php [--apply]
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED);
mb_internal_encoding('UTF-8');
define('EMS_CLI', true);
$ROOT = dirname(__DIR__);
require_once $ROOT . '/config.php';
while (ob_get_level()) ob_end_clean();
$conn = $GLOBALS['conn']; mysqli_set_charset($conn, 'utf8mb4');
$APPLY = in_array('--apply', $argv, true);

/* اللقطة من نافذة القياس المفتوحة إن وُجدت وإلا من الالتزام */
$sid = null;
$r = $conn->query("SELECT snapshot_id FROM repair01_freeze_snapshot WHERE released_at IS NULL ORDER BY frozen_at DESC LIMIT 1");
if ($r && $r->num_rows) $sid = $r->fetch_assoc()['snapshot_id'];
if (!$sid) $sid = 'GIT-' . trim(shell_exec('git -C ' . escapeshellarg($ROOT) . ' rev-parse --short HEAD'));

/* ── الأحكامُ الأربعةَ عشرَ — [الحكم، الشاشة، الشاهد] ───────────────────── */
$J = array(
 'TGT-0001' => array('MATCHED','SCR-0119',
   'R7 · مرشحان بتغطية 100٪ والفيصل الوظيفة والحبة: الهدف «لوحة المالية» بحبة «مؤشر×فترة قراءة حية» = SCR-0119 «لوحة الادارة المالية» (DEP-05 · LIVE_READ) وهي لوحة الادارة نفسها؛ والمرفوض SCR-0144 «لوحة النسب المالية» لوحة نسب فرعية بحبة LIST لا لوحة الادارة'),
 'TGT-0008' => array('NOT_BUILT','',
   'R7 · سجل بنود فاتورة العميل (LINE بند×فاتورة) لا مقابل له: المبني في الفوترة راسي الحبة — SCR-0110 «فاتورة المطالبة واحالتها» ROW راس فاتورة وSCR-0070 «الفاتورة الضريبية» LIST؛ ومرشح الثلثين الوحيد SCR-0059 «بنود عقد العميل» كيانه بنود العقد لا الفاتورة — فلا سجل ابناء لبنود الفاتورة على القرص'),
 'TGT-0046' => array('NOT_BUILT','',
   'R7 · سجل قرارات الهيكل التنظيمي (ROW قرار هيكلي) لا مقابل له: SCR-0006 «الهيكل التنظيمي والمسميات» عارض بنية LIVE_READ لا سجل قرارات؛ وسجلات القرار التنفيذي المبنية (SCR-0779 الاستراتيجية · SCR-0781 الاجتماعات · SCR-0782 المتابعة) عامة النوع بلا نطاق قرار هيكلي مسمى — فلا سجل مخصوص'),
 'TGT-0116' => array('NOT_BUILT','',
   'R7 · مصالحة الاعيان الممولة (بين اصول DEP-04 وتمويل DEP-03) لا مقابل لها: المبني SCR-0093 «الاعيان الممولة» (Equipments/fin_assets.php · DEP-03 · ROW) سجل الاعيان نفسه لا سطح مصالحة بين السجلين — والمسح الموجب عن «مصالحة» لا يجد سطحا حيا لها'),
 'TGT-0136' => array('MATCHED','SCR-0380',
   'R7 · «سجل المحاولات الممنوعة» = SCR-0380 «المحاولات الممنوعة» (DEP-08 · Governance): المالك نفسه والاسم بتغطية الثلثين والحقيقة واحدة (guard_denials)؛ وحبة التصميم «سطر رفض» صفوفها يكتبها محرك الحراس آليا فسطحها الصحيح عارض سجل LIVE_READ — والخلاف الصنفي خلاف تحقيق لا خلاف هوية'),
 'TGT-0190' => array('MATCHED','SCR-0028',
   'R7 · «تقارير المراجعة» (IAF · تقرير×مهمة) = SCR-0028 «تقارير الجهة المشرفة» (Audit/iaf_reports.php · IAF · ROW): هوية المسار iaf_reports والمالك نفسه والحبة توافق؛ والمرفوض SCR-0455 «تقارير المراجعة الداخلية» تحت WS-MY اسقاط شخصي للحقيقة نفسها لا سطحها المصدري'),
 'TGT-0224' => array('MATCHED','SCR-0607',
   'R7 · «التايم شيت اليومي» (DEP-11 · معدة×وردية×يوم) = SCR-0607 «تسجيل التايم شيت والانتاج في الموقع» (Timesheet/timesheet.php · DEP-12): شاشة الادخال اليومي المصيرة نفسها ومالكها DEP-12 الابن المباشر لمالك الهدف (parent_code=DEP-11)؛ وكاتب الحبة ROW توأمها الخدمي SCR-0012 (api/controllers/timesheet.php) بالتسمية القانونية نفسها — والمرفوضون الاربعة عوارض وحوكمة لا شاشة الادخال'),
 'TGT-0226' => array('MATCHED','SCR-0014',
   'R7 · «اعتماد الوحدات» (DEP-11 · دفعة اعتماد×يوم) = SCR-0014 «ما ينتظر اعتمادي» (Approvals/hours_approval.php · DEP-08): صندوق اعتماد ساعات الوحدات الحي وخلف الشاشة الارثية Timesheet/aprovment.php؛ ملكيته المسجلة DEP-08 لان قناة الاعتماد حوكمية — فرق ملكية مقيس يذكر ولا يمنع المطابقة؛ والمرفوض SCR-0587 «اعتماد الوحدات والاداء المعتمد» كشف وحدات الموردين (DEP-02) كيان اخر'),
 'TGT-0230' => array('MATCHED','SCR-0431',
   'R7 · «الاقفال الشهري للتشغيل» (DEP-11 · شهر×ادارة) = SCR-0431 «الاقفال الشهري للوحدة» (Operations/monthly_close.php · ROW): هوية المسار Operations/monthly_close والوظيفة اقفال التشغيل الشهري والصنف يوافق؛ ملكيته المسجلة DEP-05 لان الاقفال قيد مالي — فرق ملكية مقيس يذكر'),
 'TGT-0253' => array('MATCHED','SCR-0555',
   'R7 · «خطط المعالجة — Treatment» (DEP-09) = SCR-0555 «اجراءات معالجة المخاطر» (Risk/risk_treatments.php · DEP-09): المالك نفسه وهوية المسار risk_treatments والوظيفة واحدة؛ والمرفوض SCR-0017 «خطط المعالجة ومتابعتها» تحت IAF معالجات ملاحظات المراجعة كيان اخر'),
 'TGT-0259' => array('MATCHED','SCR-0551',
   'R7 · «تقارير المخاطر الدورية» (DEP-09 · سطر مشتق) = SCR-0551 «تقارير المخاطر والتحليلات» (DEP-09): المالك نفسه وسطح التقارير الدورية عينه؛ وحبة التصميم «سطر مشتق» تتحقق قراءة حية للاسقاط المشتق — خلاف تحقيق لا هوية'),
 'TGT-0277' => array('MATCHED','SCR-0050',
   'R7 · «المطالبات والتسليم للمالية» (DEP-01) = SCR-0050 «سجل المطالبات والتسليم للمالية» (Contracts/claims.php): تغطية الاسم 100٪ والمسار في وحدة العقود والوظيفة تسليم المطالبات للمالية؛ ملكيته المسجلة DEP-05 (الطرف المستلم يملك سجل الاستلام) — فرق ملكية مقيس يذكر؛ وحبة التصميم ROW يكتبها مسار المطالبة والسطح عارضه'),
 'TGT-0356' => array('MATCHED','SCR-0615',
   'R7 · «لوحة النقل والترحيل» (DEP-15 · مؤشر×فترة قراءة حية) = SCR-0615 «لوحة ادارة النقل» (Transport/transfer_dashboard.php · DEP-15 · LIVE_READ): لوحة الادارة نفسها بمالكها وحبتها؛ والمرفوضان مخاطر النقل وحوكمة النقل سطحا نطاق اخر يشاركان الاسم ثلثيه'),
 'TGT-0417' => array('NOT_BUILT','',
   'R7 · «لوحة القوى التشغيلية» (DEP-13 · مؤشر×فترة قراءة حية) لا مقابل لها: مرشحا الثلثين مخاطر القوى وحوكمة القوى سطحا نطاق اخر؛ وSCR-0657 «لوحة الاحتياج والتغطية» لوحة تغطية مخصوصة لا لوحة الادارة؛ وSCR-0462 لوحة معممة لم يثبت تصييرها لهذه الادارة — فلا لوحة ادارة مخصوصة على القرص'),
);

echo "═══ R7 — المصالحةُ اليدويّةُ للأربعةَ عشرَ · لقطة $sid" . ($APPLY ? '' : ' · DRY') . " ═══\n";
$n0 = (int)$conn->query("SELECT COUNT(*) c FROM repair01_target_universe WHERE verdict IS NULL OR verdict=''")->fetch_assoc()['c'];
echo "  بلا حكمٍ قبلُ: $n0\n";
$st = $conn->prepare("UPDATE repair01_target_universe SET verdict=?, screen_id=?, match_method='R7_MANUAL_ADJUDICATION', verdict_witness=?, verdict_snapshot=?, verdict_at=NOW() WHERE target_uid=? AND (verdict IS NULL OR verdict='')");
$cnt = array();
foreach ($J as $uid => $j) {
    list($v, $scr, $wit) = $j;
    $cnt[$v] = (isset($cnt[$v]) ? $cnt[$v] : 0) + 1;
    if ($APPLY) {
        $w = $wit . ' · لقطة ' . $sid;
        $st->bind_param('sssss', $v, $scr, $w, $sid, $uid);
        if (!$st->execute() || $st->affected_rows !== 1) echo "  ✘ $uid لم يُكتب (" . $conn->error . ")\n";
    }
    echo "  $uid ⇒ $v" . ($scr ? " → $scr" : '') . "\n";
}
$n1 = (int)$conn->query("SELECT COUNT(*) c FROM repair01_target_universe WHERE verdict IS NULL OR verdict=''")->fetch_assoc()['c'];
foreach ($cnt as $k => $v) echo "     $k = $v\n";
echo "  بلا حكمٍ بعدُ: $n1\n";
echo ($APPLY && $n1 === 0) ? "✔ كلُّ هدفٍ له حكم\n" : '';
