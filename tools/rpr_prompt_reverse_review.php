<?php
/**
 * tools/rpr_prompt_reverse_review.php — المراجعةُ العكسيّةُ لبرومتِ RPR-02/03
 * ═══════════════════════════════════════════════════════════════════════════
 * **مراجعةٌ عكسيّةٌ بمعنى `RPR-02` §٥**: تبدأ من **المُنتَجِ** (ملفُّ البرومت)
 * وتسأل: هل يحمل كلَّ ما نصَّ عليه **المُصمَّم** (الأمران)؟ — لا العكس.
 *
 * ◆ **والمقامُ يُعلَن**: كلُّ مطلبٍ في الأمرَين صفٌّ هنا بمرساةٍ نصّيّة، **ومن
 *   لا مرساةَ له لا يُدَّعى حملُه**. فقائمةٌ تقول «الكلُّ مغطًّى» بلا مقامٍ
 *   مطبوعٍ دعوى لا قياس.
 *
 * ⛔ **ولا يُصلح هذا الفاحصُ شيئًا**: يقرأ ويُحصي. والإصلاحُ في الملفِّ نفسِه.
 *
 * ⚠ **والمرساةُ تُختار مميِّزةً**: لفظٌ عامٌّ («الشاشة») يمرُّ بلا حملٍ حقيقيّ،
 *   فتُختار عبارةٌ لا ترد إلّا إن حُمل المطلبُ فعلًا.
 *
 * التشغيل: php tools/rpr_prompt_reverse_review.php [--md]
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED);
mb_internal_encoding('UTF-8');
$ROOT = dirname(__DIR__);
$MD   = in_array('--md', $argv, true);

$PROMPT = $ROOT . '/docs/REPAIR01_20260823/plan/RPR02_RPR03.prompt.md';
$SRC    = array(
    'RPR-02' => $ROOT . '/docs/REPAIR01_20260823/orders/RPR-02_v2.3.txt',
    'RPR-03' => $ROOT . '/docs/REPAIR01_20260823/orders/RPR-03.txt',
);
foreach (array_merge(array($PROMPT), array_values($SRC)) as $p) {
    if (!is_file($p)) { exit("✘ مفقود: $p\n"); }
}
$doc = file_get_contents($PROMPT);

/* ── التطبيع: التشكيلُ والتطويلُ يُزالان من الطرفَين معًا ─────────────────────
   ⚠ وإلّا رسَبَ مطلبٌ محمولٌ لأنَّ البرومتَ يكتب «المقامُ» والأمرَ «المقام».
   ⚠ **وعطبان وقعا فعلًا في النسخةِ الأولى** — ومرساةٌ سليمةٌ رسَبَت على نصٍّ
     يحملها، **فأُصلحت الأداةُ لا مخرَجُها**:
     ① **ترميزُ ماركداون يقطع العبارة**: «الخمسةَ عشرَ **كاملةً**» فيها نجمتان
        بين الكلمتَين، و«دورةِ العملِ** في الملفِّ» كذلك — فتُجرَّد `*` و`` ` ``
        و`_` قبل المطابقة.
     ② **علامةُ الاقتباسِ في أوّلِ السطرِ تبقى بعد طيِّ الأسطر**: عبارةٌ تنكسر
        على سطرَين داخلَ `>` تصير «… يعرف > نفسَه …» — فتُنزع `>` أوّلَ السطر.
   ⛔ **ولا تُضعَّف مرساةٌ لتمرَّ**: تُجرَّد الزينةُ ويبقى النصُّ كما هو. */
$norm = function ($s) {
    $s = preg_replace('~^\s*>+\s?~mu', '', (string) $s);   /* ② اقتباسُ ماركداون */
    $s = preg_replace('~[*`_]~u', '', $s);                 /* ① زينةُ ماركداون  */
    $s = preg_replace('~[\x{064B}-\x{0652}\x{0653}-\x{0655}\x{0670}\x{0640}]~u', '', $s);
    $s = preg_replace('~[\x{0622}\x{0623}\x{0625}]~u', "\u{0627}", $s);
    $s = preg_replace('~\x{0649}~u', "\u{064A}", $s);
    $s = preg_replace('~\x{0629}~u', "\u{0647}", $s);
    $s = preg_replace('~\s+~u', ' ', $s);
    return $s;
};
$N = $norm($doc);

/* ═══ المقام: كلُّ مطلبٍ في الأمرَين بمرساتِه ══════════════════════════════
   الحقول: [الأمر, القسم, المطلب, مرساةٌ أو أكثر (كلُّها تُشترط)]              */
$REQ = array(
/* ── RPR-02 ─────────────────────────────────────────────────────────────── */
array('RPR-02','§ترويسة','نطاقُ الأمرِ وما ليس منه', array('ولا شيء غيرها','التكامل والانفاذ')),
array('RPR-02','§ترويسة','المرجعُ الحاكم: اثنا عشرَ ملفًّا و١٢٥ قرارًا', array('١٢٥ قرارا')),
array('RPR-02','§١','ثلاثةُ أرقامٍ لا اثنان — ٨٩٪ و٥٩٫٤٪ و٨٢٫٦٪', array('٥٩٫٤')),
array('RPR-02','§١','معيارُ الهجراتِ المصحَّح: الإجماليُّ لا الجديد', array('اجمالي الهجرات غير المصالحه')),
array('RPR-02','§٢','جدولُ فصلِ النطاقِ بين الأمرَين', array('في RPR-02','في RPR-03')),
array('RPR-02','§٣','المقامُ على ٢٢ رمزًا — ١٧ إدارةً و٤ خارجَها و`PLATFORM`', array('EX-CEO','EX-DVP','WS-MY','IAF','PLATFORM')),
array('RPR-02','§٣','جدولُ الرموزِ بأعمدتِه: مستهدَف · مبنيّ · شبح · مغطًّى · فائض', array('شبح','مغطي','فائض','غير مبني')),
array('RPR-02','§٣','أسوأُ فجوة: `EX-DVP` ٨٫٣٪', array('٨٫٣')),
array('RPR-02','§٣','ثلاثةُ قيودٍ على الرقم: اسميّةٌ · كونان · ملفّا المكتبة', array('ملفي المكتبه')),
array('RPR-02','§٣','إعادةُ إصدارِ القسمِ الثالثِ إلزاميّةٌ بأربعةٍ تحملها', array('اعاده اصدار القسم الثالث','مؤقت بتصميم')),
array('RPR-02','§٤·١','توحيدُ كونِ الأهدافِ قبل أيِّ حكم', array('كونين','بمعرف واحد لكل هدف')),
array('RPR-02','§٤·٢','الأحكامُ السبعةُ كاملةً', array('مطابق','مدموج في غيره','تبويب','اسقاط','غير مبني','غير منطبق','هدف متقاعد')),
array('RPR-02','§٤·٢','المقياسان منفصلَين ولا يُخلطان', array('اغلاق قرارات الاهداف','تحقق الاهداف','المتحقق ÷ المنطبق')),
array('RPR-02','§٤·٢','شاهدٌ لكلِّ حكم: مرجعٌ · مصدرُ قرار · معرِّفُ لقطة', array('بلا شاهد لا يقبل')),
array('RPR-02','§٤·٣','حارسا الحكمَين المُخرجَين — اثنان لا واحد', array('مرجع قرار مالك موثق','يعرض عددهما منفصلا')),
array('RPR-02','§٤·٤','معايير `PLATFORM` الأربعةُ معًا', array('ثلاث ادارات فاكثر','لا تملك حقيقه اعمال','مالك تقني مسمي','سجل المنصه')),
array('RPR-02','§٥','الأسئلةُ الخمسةَ عشرَ — بنيويّةٌ وإنفاذ', array('الوجود','الموضع','الحبه','الدوره المستنديه','حكم الحقل','السابق واللاحق','فصل الواجبات','البيانات التاريخيه','الاختبار السالب')),
array('RPR-02','§٥·١','قاعدةُ الجهدِ بثلاثِ درجاتِها', array('الخمسه عشر كامله','السبعه الاول')),
array('RPR-02','§٥·١','الإسقاطُ الحسّاس: السبعةُ زائدَ أربعة', array('زائد اربعه','ليست اعفاء امنيا')),
array('RPR-02','§٦','السايدبارُ بسبعِ خطواتِه', array('عطل ما ليس في الملف','اسما وعددا','دوره العمل في الملف','ولا ترتيب يدوي')),
array('RPR-02','§٦','دليلا السايدبار: بالوحداتِ وبالأدوار — و١٩ إدارةً فيها أدوار', array('اداره الصلاحيات','ادوار لا ادارات')),
array('RPR-02','§٧','خطواتُ الشاشةِ الثمانيَ عشرةَ كاملةً', array('تصحيح الحبه','فصل الام عن البنود','نزع الملكيه الغريبه','تحويل المستورد','تحويل المشتق','مرجع المصدر','ربط المستند','ربط الاعتماد','كتله التدقيق','اله الحاله','جسر دوره العمل','القوائم المحكومه','نقاء اللغه')),
array('RPR-02','§٧','تدرّجُ التحقّقِ البشريِّ بثلاثِ درجات', array('تحقق وظيفي كامل','اختبار دور واجراء اساسي','تحقق عرض ووصول')),
array('RPR-02','§٧','التمييزُ عن رحلةِ `RPR-03` الكاملة', array('ولا يعد احدهما عن الاخر')),
array('RPR-02','§٨','فرزُ الفائضِ ٢٦٧ بصنفَيه', array('٢٦٧','فائض حرج','فائض حميد')),
array('RPR-02','§٨','ابدأ بأحكامِهم: ١٥٩ متقاعدًا و٥٧ تبويبًا', array('١٥٩','٥٧')),
array('RPR-02','§٩','دفترُ الهجرات: بوّابةٌ فوريّةٌ ولا نافذةَ ظلّ', array('ولا نافذه ظل','ليس لها حاله مشروعه واحده')),
array('RPR-02','§٩','والقاعدةُ سليمةٌ والعطبُ أنَّ استدعاءَها عُرفٌ لا بوّابة', array('عرف لا بوابه')),
array('RPR-02','§١٠','لقطتان لا لقطة — ثلاثُ نوافذَ بشروطِ دخولِها', array('اللقطه الاولي','نافذه الاصلاح','اللقطه الثانيه','شرط دخول')),
array('RPR-02','§١٠','قاعدتان: أدواتُ القياسِ موثَّقةٌ · والترتيبُ بالبصمةِ لا بساعةِ الحائط', array('ساعه الحائط')),
array('RPR-02','§١١','محظور: لا اعتمادَ لرقمِ عجزٍ ناتجٍ عن طرحِ عددَين', array('طرح عددين')),
array('RPR-02','§١١','محظور: لا إصدارَ أساسٍ معتمدٍ بمالكٍ مجهولٍ أو مصدرٍ مزدوجٍ أو تايم شيت غير محسوم', array('تايم شيت غير محسوم')),
array('RPR-02','§١١','محظور: قاعدةٌ مانعةٌ دائمةٌ لمسارِ المكتبات', array('vendor')),
array('RPR-02','§١٢','معيارُ القبول: ستّةَ عشرَ مقياسًا', array('لا معيار اكتمال انجاز','ليس معيارا بل امنيه')),
array('RPR-02','§١٣','ترتيبُ التنفيذِ سبعَ عشرةَ خطوة', array('مراجعه عابره للموجات','حكم الفائض الحميد','قفل معيار قبول')),
array('RPR-02','§١٤','المطلوبُ في الردِّ — أحدَ عشرَ بندًا', array('بسبعه عشر عمودا','لكل رمز قانوني','سمه ولا تخمنه')),
array('RPR-02','§خاتمة','القاعدةُ الختاميّةُ الخماسيّة', array('نسبه تخفي نقصا اسوا من نقص معلن')),
/* ── RPR-03 ─────────────────────────────────────────────────────────────── */
array('RPR-03','§ترويسة','نطاقُه وما ليس منه والعلاقةُ بـ`RPR-02`', array('بالتوازي','بنداه الاولان')),
array('RPR-03','§١','الهيكلُ العظميُّ كاملٌ ولم يجرِ فيه الدم', array('لم يجر فيه الدم')),
array('RPR-03','§٢','محاورُ الجاهزيّةِ التسعةُ بأحكامِها', array('الوظيفيه','سلامه البيانات','تجربه المستخدم','النشر','النسخ والاستعاده','المراقبه','اعاده البناء')),
array('RPR-03','§٢','ثلاثٌ حمراءُ وخمسٌ جزئيّةٌ وصفرٌ أخضر — ولا نسبةَ إجماليّة', array('ولا نسبه اجماليه','يقرا محورا محورا')),
/* ⚠ **والمقامُ نفسُه كان ناقصًا صفًّا**: `RPR-03` §٣ و§٣·١ لم يكن لهما صفٌّ،
     **فأعطت القائمةُ ١٠٠٪ وهي لا تسأل عنهما** — وهذا بعينُه «صفرٌ من مفردةٍ
     لا وجودَ لها». ⇒ **قائمةٌ ناقصةٌ تُعطي أخضرَ كاذبًا، فتُدقَّق أقسامُ
     المصدرِ كلُّها قبل الحكم.** */
array('RPR-03','§٣','استعادةُ ما انهار قبل بناءِ ما ينقص', array('يضاعف الانهيار')),
array('RPR-03','§٣·١','دفترُ الهجرات: بوّابةٌ فوريّةٌ وتسويةُ الثمانِ والخمسين', array('مطبقه ومتحقق منها بالمخطط','ولا يعاد تنفيذها')),
array('RPR-03','§٣·٢','الاستعادةُ بثلاثةِ أدلّة', array('زمن الاستعاده الفعلي','نقطه الاستعاده الفعليه','مطابقه عينه')),
array('RPR-03','§٣·٢','التثبيتُ من الصفرِ بأربعِ خطواتِه', array('بيئه نظيفه','بذر ادني','اقلاع ناجح')),
/* ⚠ **وترويسةٌ محضةٌ لا تحتاج صفًّا**: `RPR-02` §٤ و`RPR-03` §٨ عنوانان يحمل
     أبناؤهما كلَّ مضمونِهما. **أمّا `RPR-03` §٤ فترويسةٌ ذاتُ دعوى** — «الناقلُ
     سليم» بـ٩٩٫٨٨٪ وصفرِ يتيم — **فلها صفُّها**. */
array('RPR-03','§٤','الناقلُ سليمٌ والمشكلةُ أنَّ أحدًا لا يستمع', array('٩٩٫٨٨','لا يستمع')),
array('RPR-03','§٤·١','المقامُ الصحيح: ١١ حدثَ أعمالٍ و٤٧ تدقيقيًّا', array('١١','٤٧','هدف لا يبلغ ولا ينبغي')),
array('RPR-03','§٤·٢','الترتيبُ الملزِمُ بستِّ خطواتِه', array('من طرفيه','عقد اثر','منع التكرار','سياسه الاعاده','صف الفاشل')),
array('RPR-03','§٤·٢','بندٌ مستقلٌّ: صحّةُ المستهلكين لا «حدثٌ بلا مستهلك»', array('عطب تشغيل في مستهلك حي')),
array('RPR-03','§٥','مراحلُ الاعتمادِ الخمسُ بشروطِ الانتقال', array('نافذه الظل','مده معلنه لا مفتوحه','يقاس اثرها في الايام الاولي')),
array('RPR-03','§٥','مقاييسُ النافذةِ الأربعة', array('منع خاطئ','سماح خاطئ','الدور المعتمد','محاولات التفاف')),
array('RPR-03','§٥','التنبيه: القيمُ عند المالكِ لا المنفِّذ', array('ليست عندك بل عند')),
array('RPR-03','§٦','الصلاحيّات: مصدرٌ واحدٌ والقائمةُ تُشتقُّ منه', array('٨٧','تشتق منه','الرابط المباشر','المنع تجميلي')),
array('RPR-03','§٧','الرحلاتُ الستُّ بمساراتِها', array('الايراد','المورد','المشتريات','العطل','العامل','القرار','مطابقه ثلاثيه','حدث لا نسخه معامله')),
array('RPR-03','§٧','ثلاثةُ أشخاصٍ لا ثلاثةُ أدوارٍ في حسابٍ واحد', array('ثلاثه اشخاص لا ثلاثه ادوار')),
array('RPR-03','§٧','مسارٌ سالبٌ بشريٌّ مع كلِّ رحلة', array('سجل المحاولات الممنوعه')),
array('RPR-03','§٨·١','القاعدةُ الصحيحةُ للقيدِ اليدويِّ بثلاثِ شُعَبِها', array('مرجع حدثه','معده ومعتمده','بلا مصدر ولا مبرر ولا اعتماد')),
array('RPR-03','§٨·١','لا تعارضَ مع «لا كاتبَ بشريَّ للأستاذ»', array('المحاسب يعد والمحرك يرحل')),
array('RPR-03','§٨·٢','المحطّاتُ الصامتةُ تُمرَّر ضمنَ رحلتِها', array('ضمن رحلتها لا منفرده')),
array('RPR-03','§٩','المراقبة: الرسائلُ الميتةُ والذهبيّةُ والاستجابة', array('الرسائل الميته','تعاد او تعوض','انذار الناقل','عينه معلنه')),
array('RPR-03','§١٠','معيارُ القبول: خمسةَ عشرَ مقياسًا كلُّها حمراء', array('نظام يعرف نفسه ونظام يعمل')),
array('RPR-03','§١١','ترتيبُ التنفيذِ خمسَ عشرةَ خطوة', array('يكشف المتوقف قبل ان يبني فوقه','بعد رحلاتها لا قبلها')),
array('RPR-03','§١٢','محظور: لا يصدر أساسٌ معتمدٌ وفيه محورُ جاهزيّةٍ أحمرُ بلا قرارِ مالك', array('محور جاهزيه احمر')),
array('RPR-03','§١٣','المطلوبُ في الردِّ — تسعةُ بنود', array('نتيجه اختبار التكرار','محضرا','١٦٤٤')),
array('RPR-03','§خاتمة','القاعدةُ الختاميّةُ الخماسيّة', array('لا يقرا حاضرا','مهما اكتملت خرائطه')),
);

/* ── القياس ──────────────────────────────────────────────────────────────── */
$rows = array(); $ok = 0; $bad = 0;
foreach ($REQ as $r) {
    list($order, $sec, $what, $anchors) = $r;
    $missing = array();
    foreach ($anchors as $a) {
        if (mb_strpos($N, $norm($a)) === false) { $missing[] = $a; }
    }
    $pass = empty($missing);
    if ($pass) { $ok++; } else { $bad++; }
    $rows[] = array($order, $sec, $what, $pass, $missing);
}
$tot = count($rows);

echo "\n═══ المراجعةُ العكسيّةُ لبرومتِ RPR-02/03 ═══\n";
printf("  المُنتَج : %s (%d حرفًا)\n", 'docs/REPAIR01_20260823/plan/RPR02_RPR03.prompt.md', mb_strlen($doc));
printf("  المقام  : %d مطلبًا من الأمرَين — كلٌّ بمرساتِه النصّيّة\n\n", $tot);

$cur = '';
foreach ($rows as $x) {
    if ($x[0] !== $cur) { $cur = $x[0]; echo "── $cur ──\n"; }
    printf("  %s %-9s %s\n", $x[3] ? '✔' : '✘', $x[1], $x[2]);
    if (!$x[3]) { echo "        ⚠ مرساةٌ غائبة: «" . implode('» · «', $x[4]) . "»\n"; }
}

echo "\n────────────────────────────────────────────────────────────────────\n";
printf("**محمولٌ %d · غائبٌ %d · المقام %d — التغطيةُ %.1f%%**\n",
    $ok, $bad, $tot, $tot ? ($ok * 100 / $tot) : 0);
echo $bad === 0
    ? "🟢 **البرومتُ يحمل كلَّ مطلبٍ في الأمرَين — بمرساةٍ لكلٍّ**\n"
    : "🔴 **$bad مطلبًا لم يُحمَل — ولا يُدَّعى الاكتمال**\n";

if ($MD) {
    $o  = "# المراجعةُ العكسيّةُ لبرومتِ `RPR-02`/`RPR-03`\n\n";
    $o .= "> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr_prompt_reverse_review.php --md`\n\n";
    $o .= sprintf("**محمولٌ %d · غائبٌ %d · المقام %d — التغطيةُ %.1f%%**\n\n",
        $ok, $bad, $tot, $tot ? ($ok * 100 / $tot) : 0);
    $cur = '';
    foreach ($rows as $x) {
        if ($x[0] !== $cur) { $cur = $x[0]; $o .= "\n## $cur\n\n| القسم | المطلب | الحكم | المرساةُ الغائبة |\n|---|---|---|---|\n"; }
        $o .= sprintf("| %s | %s | %s | %s |\n", $x[1], $x[2],
            $x[3] ? '✔' : '✘ غائب', $x[3] ? '—' : '`' . implode('` · `', $x[4]) . '`');
    }
    file_put_contents($ROOT . '/docs/REPAIR01_20260823/RPR_PROMPT_REVERSE_REVIEW.md', $o);
    echo "✔ كُتب docs/REPAIR01_20260823/RPR_PROMPT_REVERSE_REVIEW.md\n";
}
exit($bad === 0 ? 0 : 1);
