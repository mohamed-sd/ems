<?php
/**
 * tools/injfrd01_package_fix.php
 *   تصحيحاتُ ضبطِ الحزمةِ الخمسة — INJ-FRD-REM-01 §أولًا · قبلَ أوّلِ تغييرٍ برمجيّ
 * ═══════════════════════════════════════════════════════════════════════════
 * ◆ **الدفترُ أوّلًا ثم الوثيقةُ تُصالَح عليه** — بنصِّ الأمر: «والـWord لا يُعدل
 *   يدويًا بصورة مستقلة؛ يُعاد اشتقاقه/مصالحة محتواه مع الدفتر بعد التحديث».
 *
 * ◆ **ولا تُخترع قاعدةُ عمل**: حقولُ المتطلبَين الجديدَين تُنقل من نصِّ الوثيقةِ
 *   حيث نصّت، وتُورَث من شقيقِ المجالِ حيث كان الحقلُ اصطلاحًا مشتركًا
 *   (الحزمةُ · الصنفُ · انطباقُ الاختبار)، و**ما لا مصدرَ له يُوسَم**
 *   `NEEDS_GOVERNING_SOURCE` ولا يُملأ بالتخمين.
 *
 * ◆ **والمعرِّفُ الجديدُ من التسلسلِ الحرِّ لا بإعادةِ ترقيم**: الاعتمادُ بلغ
 *   FR-APP-011 فالحرُّ 012 · والأحداثُ بلغت FR-EVT-007 فالحرُّ 008. والقيمةُ
 *   اللغويةُ القديمةُ تُحفظ في `Legacy_Alias` فلا تنكسر إحالةٌ تاريخية.
 *
 * التشغيل:  php tools/injfrd01_package_fix.php [--apply]
 *   بلا --apply: عرضٌ فقط ولا يُمَسُّ ملفّ.
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED & ~E_WARNING);
mb_internal_encoding('UTF-8');

$ROOT  = dirname(__DIR__);
$XLSX  = $ROOT . '/docs/sources/INJ-FRD-REM-01/workbook.xlsx';
$DOCX  = $ROOT . '/docs/sources/INJ-FRD-REM-01/document.docx';
$apply = in_array('--apply', $argv, true);

echo "════ تصحيحاتُ ضبطِ حزمةِ INJ-FRD-REM-01 ════\n";
echo $apply ? "  الوضع: **تطبيق**\n\n" : "  الوضع: عرضٌ فقط (مرِّر --apply للتنفيذ)\n\n";

/* ── القيمُ الحقليةُ للمتطلبَين الجديدَين ─────────────────────────────────
 * المصدر: جدولا الفصلَين ٨ و١١ في الوثيقةِ الحاكمة — السلوكُ ومعيارُ القبولِ
 * منصوصان فيهما حرفًا. وما ليس فيهما يُورَث اصطلاحًا أو يُوسَم. */
$NGS = 'NEEDS_GOVERNING_SOURCE';
$TESTS = 'موجب=YES · سالب=YES · حدّيّ=YES · ارتدادِيّ=YES · بالدور=YES · فقدُ بيانات=NO';
$NAREASON = 'فقدُ بيانات: لا يمسُّ هجرةً ولا بياناتٍ قائمة';

$NEW = array(
    /* المعرِّفُ الجديدُ ⇐ [الصف، القيم] */
    'FR-APP-012' => array(
        'row' => 66, 'num' => 60, 'legacy' => 'FR-APP-007ب',
        'v' => array(
            'الفجوة' => 'GAP-03',
            'المجال' => 'السلطة',
            'المصدرُ الحاكم' => 'برنامج الإصلاح §GAP-03 · الوثيقة الفصل ٨',
            'الحالُ الآن' => 'جدولُ تفويضٍ بصفرِ صفٍّ وقارئِ إنتاجٍ واحد',
            'السلوكُ المطلوب' => 'التفويضُ مبنيٌّ غيرُ موصول: يُقرأ من مستوى السلطةِ الواحدِ أو يُوسَم متقاعدًا بقرارٍ مكتوب — ولا يبقى جدولًا بصفرِ صفٍّ وقارئِ إنتاجٍ واحد',
            'المُطلِق' => 'قراءةُ صاحبِ اليدِ في خطوةِ سلّم',
            'الفاعل' => 'النظام',
            'الشروطُ السابقة' => 'مستوى السلطةِ الواحدُ مبنيٌّ ومقروء',
            'المُدخَل' => 'الفاعلُ والكيانُ والخطوة',
            'المعالجة' => 'قراءةُ التفويضِ النافذِ من مستوى السلطةِ الواحد',
            'المُخرَج' => 'صاحبُ اليدِ الفعليُّ بعدَ التفويض',
            'انتقالُ الحالة' => '—',
            'قاعدةُ الصلاحية' => 'التفويضُ النافذُ وحدَه يُغيّر صاحبَ اليد',
            'سلوكُ الفشل' => $NGS . ' — سلوكُ الفشلِ عندَ تعذُّرِ قراءةِ التفويضِ غيرُ منصوصٍ في الوثيقة',
            'واقعةُ السجل' => 'سجلُّ قراءةِ التفويض',
            'الجداولُ والحقول' => 'جدولُ التفويضِ ومستوى السلطة',
            'الخدماتُ والأحداث' => 'خدمةُ السلطة',
            'اختبارٌ موجب' => 'تفويضٌ نافذٌ ← يُغيّر صاحبَ اليدِ فعلًا',
            'اختبارٌ سالب' => 'تفويضٌ منتهٍ ← لا يُغيّر صاحبَ اليد',
            'معيارُ القبول' => 'تفويضٌ نافذٌ يُغيّر صاحبَ اليدِ فعلًا · وتفويضٌ منتهٍ ← لا يُغيّرها · أو إعلانُ تقاعدٍ رسميّ',
            'الدليل' => 'EV-APP-012',
            'التبعيات' => 'FR-APP-007',
            'الأولوية' => 'P1',
            'الحالة' => 'مخطَّط',
            'Requirement_Type' => 'FUNCTIONAL',
            'Atomicity_Level' => 'ATOMIC',
            'Parent_Requirement_ID' => '—',
            'Change_Set_ID' => 'CHG-APP-LADDER-01',
            'Test_Applicability' => $TESTS,
            'N/A_Reason' => $NAREASON,
            'Threshold_Source' => $NGS . ' — مهلةُ سريانِ التفويضِ غيرُ منصوصة',
        ),
    ),
    'FR-EVT-008' => array(
        'row' => 67, 'num' => 61, 'legacy' => 'FR-EVT-001ب',
        'v' => array(
            'الفجوة' => 'GAP-06',
            'المجال' => 'الأحداث',
            'المصدرُ الحاكم' => 'برنامج الإصلاح §GAP-06 · الوثيقة الفصل ١١',
            'الحالُ الآن' => 'القراءةُ من الإسقاطِ بلا قرارٍ معلَن',
            'السلوكُ المطلوب' => 'القراءةُ من الإسقاطِ لا الجذر: تتبع قرارَ مجالِ الأحداثِ المعلَن — فإن أُعلن الجذرُ ناقلًا هاجرت القراءةُ إليه، وإن أُعلن سجلًّا بقي الإسقاطُ مسارًا قانونيًّا موثَّقًا',
            'المُطلِق' => 'قراءةُ واقعةِ عملٍ من مستهلك',
            'الفاعل' => 'النظام',
            'الشروطُ السابقة' => 'قرارُ مجالِ الأحداثِ معلَنٌ مكتوب',
            'المُدخَل' => 'نوعُ الواقعةِ وموضعُ قراءتِها',
            'المعالجة' => 'توجيهُ القراءةِ إلى الموضعِ الذي أعلنه القرار',
            'المُخرَج' => 'قراءةٌ من موضعٍ واحدٍ معلَن',
            'انتقالُ الحالة' => '—',
            'قاعدةُ الصلاحية' => '—',
            'سلوكُ الفشل' => 'قارئٌ يخالف القرارَ المعلَن يُرصد ويُسمّى — ولا يُبتلع صامتًا',
            'واقعةُ السجل' => 'سجلُّ قرارِ مجالِ الأحداث',
            'الجداولُ والحقول' => 'جذرُ الوقائعِ وإسقاطُه',
            'الخدماتُ والأحداث' => 'ناقلُ الأحداث',
            'اختبارٌ موجب' => 'قراءةٌ من الموضعِ المعلَن ← تعمل',
            'اختبارٌ سالب' => 'قارئٌ يخالف القرارَ المعلَن ← يُرصد ويُسمّى',
            'معيارُ القبول' => 'قرارٌ معلَنٌ مكتوب · وصفرُ قارئٍ يخالفه بعدَ الإعلان',
            'الدليل' => 'EV-EVT-008',
            'التبعيات' => 'FR-EVT-001',
            'الأولوية' => 'P1',
            'الحالة' => 'مخطَّط',
            'Requirement_Type' => 'ARCHITECTURE_CONTROL',
            'Atomicity_Level' => 'ATOMIC',
            'Parent_Requirement_ID' => '—',
            'Change_Set_ID' => 'CHG-EVT-BUS-01',
            'Test_Applicability' => $TESTS,
            'N/A_Reason' => $NAREASON,
            'Threshold_Source' => 'لا عتبةَ رقميةً في هذا المتطلب',
        ),
    ),
);

/* ─────────────────────── ① الدفتر ───────────────────────────────────── */
$z = new ZipArchive();
if ($z->open($XLSX) !== true) { exit("⛔ تعذّر فتحُ الدفتر\n"); }
$entries = array();
for ($i = 0; $i < $z->numFiles; $i++) { $entries[$z->getNameIndex($i)] = $z->getFromIndex($i); }
$z->close();

$s1 = $entries['xl/worksheets/sheet1.xml'];
$s4 = $entries['xl/worksheets/sheet4.xml'];

/* رأسُ العمودِ الجديد AH4 — يُضاف داخلَ صفِّ الرأسِ بعدَ AG4 */
$hdrCell = '<c r="AH4" t="inlineStr"><is><t xml:space="preserve">Legacy_Alias</t></is></c>';
if (strpos($s1, 'r="AH4"') === false) {
    $s1 = preg_replace('~(<c r="AG4"(?:\s[^>]*)?(?:/>|>.*?</c>))~su', '$1' . $hdrCell, $s1, 1);
}

/* ترتيبُ الأعمدةِ يُقرأ من صفِّ الرأسِ نفسِه — ولا يُفترَض */
$HEAD = array();
if (preg_match('~<row r="4"[^>]*>(.*?)</row>~su', $s1, $hm)) {
    preg_match_all('~<c r="([A-Z]+)4"[^>]*>.*?<t[^>]*>(.*?)</t>~su', $hm[1], $cm, PREG_SET_ORDER);
    foreach ($cm as $c) { $HEAD[trim(str_replace('◆ ', '', html_entity_decode($c[2], ENT_QUOTES | ENT_XML1, 'UTF-8')))] = $c[1]; }
}
if (count($HEAD) < 30) { exit("⛔ تعذّر قراءةُ صفِّ الرأس (" . count($HEAD) . " عمودًا)\n"); }

$added = 0;
foreach ($NEW as $id => $spec) {
    if (strpos($s1, '>' . $id . '<') !== false) { echo "  = {$id} مسجَّلٌ سلفًا\n"; continue; }
    $r = $spec['row'];
    $cells = '';
    $put = function ($colName, $val) use (&$cells, $HEAD, $r) {
        if (!isset($HEAD[$colName])) { return; }
        $cells .= '<c r="' . $HEAD[$colName] . $r . '" t="inlineStr"><is><t xml:space="preserve">'
                . htmlspecialchars((string) $val, ENT_QUOTES | ENT_XML1, 'UTF-8') . '</t></is></c>';
    };
    $put('#', $spec['num']);
    $put('المعرِّف', $id);
    foreach ($spec['v'] as $k => $v) { $put($k, $v); }
    $put('Legacy_Alias', $spec['legacy']);
    $s1 = str_replace('</sheetData>', '<row r="' . $r . '">' . $cells . '</row></sheetData>', $s1);
    $added++;
    echo "  ✔ {$id} — صفٌّ جديدٌ رقم {$r} · Legacy_Alias = {$spec['legacy']}\n";
}
$s1 = preg_replace('~<dimension ref="A1:[A-Z]+\d+"/>~', '<dimension ref="A1:AH67"/>', $s1);

/* عنوانُ ورقةِ الثوابت — العددُ يُشتقُّ لا يُكتب
 * ◆ **والاشتقاقُ بالقارئِ لا بالتعبيرِ النمطيّ**: أوّلُ كتابةٍ عدَّت خلايا
 *   العمودِ A بـ`<t>` داخلَ الخلية، وورقةُ الثوابتِ بنصوصٍ **مشتركةٍ** لا
 *   سطرية — فعاد العددُ **صفرًا** وكاد يُكتب في العنوان. والصفرُ المشتقُّ
 *   خطأً أسوأُ من العددِ المكتوبِ يدًا، فهو يبدو مشتقًّا. */
require_once __DIR__ . '/lib/xlsx_io.php';
$accWb = xlsx_read($XLSX);
$accN = 0;
foreach ((isset($accWb['ثوابت قبول الوثيقة']) ? $accWb['ثوابت قبول الوثيقة'] : array()) as $ri => $rr) {
    if ($ri < 4) { continue; }
    if (isset($rr[0]) && ctype_digit(trim((string) $rr[0]))) { $accN++; }
}
if ($accN < 1) { exit("⛔ تعذّر اشتقاقُ عددِ الثوابت — لا يُكتب عددٌ مشكوكٌ فيه
"); }
/* ◆ **الاستبدالُ النصيُّ لا يطابق نصًّا ليس نصًّا**: عربيةُ هذا المصنَّفِ مخزَّنةٌ
 *   **بمراجعَ رقميةٍ** (`&#1571;`) لا بحروفِ UTF-8. فأوّلُ كتابةٍ استبدلت الحروفَ
 *   فلم تطابق شيئًا — **وطبعت نجاحًا**. وهذا أخطرُ من الفشلِ الصريح، لأن التقريرَ
 *   يخضرُّ والملفُّ لم يُمَسّ. ⇒ تُستبدَل الخليةُ **بمرجعِها** لا بنصِّها. */
$cellRepl = function ($xml, $ref, $val) {
    $rep = '<c r="' . $ref . '" t="inlineStr"><is><t xml:space="preserve">'
         . htmlspecialchars($val, ENT_QUOTES | ENT_XML1, 'UTF-8') . '</t></is></c>';
    $pat = '~<c r="' . preg_quote($ref, '~') . '"(?:\s[^>]*)?(?:/>|>.*?</c>)~su';
    if (!preg_match($pat, $xml)) { throw new RuntimeException("خليةٌ لم تُوجد: {$ref}"); }
    return preg_replace($pat, $rep, $xml, 1);
};
$s4 = $cellRepl($s4, 'A1', "{$accN} ثابتًا تُقاس آليًّا من هذا الدفتر");
$s4 = $cellRepl($s4, 'A2', '◆ لا تُعتمد الوثيقةُ إلا إذا مرَّت الثوابتُ كلُّها — والعددُ مشتقٌّ من صفوفِ هذه الورقةِ لا مكتوبًا يدًا');
echo "  ✔ ورقةُ الثوابت: العنوانُ يقول الآن **{$accN}** — والعددُ مشتقٌّ من صفوفِها\n";

$entries['xl/worksheets/sheet1.xml'] = $s1;
$entries['xl/worksheets/sheet4.xml'] = $s4;

/* ─────────────────────── ② الوثيقةُ تُصالَح على الدفتر ────────────────── */
$z2 = new ZipArchive();
if ($z2->open($DOCX) !== true) { exit("⛔ تعذّر فتحُ الوثيقة\n"); }
$dEntries = array();
for ($i = 0; $i < $z2->numFiles; $i++) { $dEntries[$z2->getNameIndex($i)] = $z2->getFromIndex($i); }
$z2->close();
$dx = $dEntries['word/document.xml'];

/* المعرِّفاتُ اللغويةُ ⇐ الآلية — والقديمُ محفوظٌ في الدفتر */
$before = $dx;
foreach ($NEW as $id => $spec) { $dx = str_replace($spec['legacy'], $id, $dx); }
$idFix = ($dx !== $before);

/* الأعدادُ — تُصالَح على ما اشتُقّ من الدفتر */
$rep = array(
    'المتطلباتُ الأحدَ عشر' => 'المتطلباتُ الاثنا عشرَ',
    'المتطلباتُ السبعة'    => 'المتطلباتُ الثمانية',
    /* ◆ **الفهرسُ يصف فصلَه لا الدفتر**: فهرسُ الوثيقةِ كان يقول «أحدَ عشرَ»
     *   وفصلُها التاسعَ عشرَ يسرد **أربعةَ عشرَ** شرطًا مرقَّمةً ١..١٤ (مقيسةً
     *   بالعدِّ لا بالنقل). فيُصالَح الفهرسُ على فصلِه.
     * ◆ **ولا يُرقَّم الفصلُ إلى خمسةَ عشرَ**: الدفترُ فيه خمسةَ عشرَ ثابتًا
     *   **بمحتوًى مختلفٍ** لا بزيادةِ واحدٍ فقط — فترقيمُ العنوانِ وحدَه يجعله
     *   يكذب على جسدِه. والفرقُ يُسجَّل كشفًا (NF-32) ويُصالَح بالمحتوى لا
     *   بالعدد — وهذا نصُّ §14: الكشفُ يُسجَّل ولا يُخفى ولا يُوسَّع صمتًا. */
    'أحدَ عشرَ شرطًا'      => 'أربعةَ عشرَ شرطًا',
);
$hits = array();
foreach ($rep as $a => $b) {
    $n = substr_count($dx, $a);
    if ($n) { $dx = str_replace($a, $b, $dx); $hits[] = "«{$a}» ×{$n}"; }
}
$dEntries['word/document.xml'] = $dx;
echo "  ✔ الوثيقة: المعرِّفانِ " . ($idFix ? 'صُحِّحا' : 'لا حاجة')
   . ($hits ? ' · الأعداد: ' . implode(' · ', $hits) : ' · لا عددَ تغيَّر') . "\n";

/* ─────────────────────── ③ الكتابة ─────────────────────────────────── */
if (!$apply) { echo "\n◆ عرضٌ فقط — لم يُمَسَّ ملفّ. مرِّر --apply.\n"; exit(0); }

foreach (array(array($XLSX, $entries), array($DOCX, $dEntries)) as $pair) {
    list($path, $ent) = $pair;
    $tmp = $path . '.tmp';
    $zz = new ZipArchive();
    if ($zz->open($tmp, ZipArchive::CREATE | ZipArchive::OVERWRITE) !== true) { exit("⛔ تعذّر الكتابة\n"); }
    foreach ($ent as $n => $d) { $zz->addFromString($n, $d); }
    $zz->close();
    if (!@rename($tmp, $path)) { @copy($tmp, $path); @unlink($tmp); }
}
/* ◆ **الكتابةُ التي لا تُقرأ بعدَها كتابةٌ مزعومة** — ولا يُطبع نجاحٌ بلا
 *   قراءةٍ ثانيةٍ من القرص. */
$chk = xlsx_read($XLSX);
$t1  = trim((string) ($chk['ثوابت قبول الوثيقة'][0][0] ?? ''));
$okT = (mb_strpos($t1, 'أحدَ عشرَ') === false && mb_strpos($t1, (string) $accN) !== false);
$okR = 0;
foreach ((isset($chk['سجل المتطلبات']) ? $chk['سجل المتطلبات'] : array()) as $rr) {
    $v = trim((string) ($rr[1] ?? ''));
    if ($v === 'FR-APP-012' || $v === 'FR-EVT-008') { $okR++; }
}
printf("\n① المتطلبانِ في القراءةِ الثانية: %d/2 · أُضيف هذه الجولة: %d\n", $okR, $added);
printf("② عنوانُ ورقةِ الثوابتِ بعدَ الكتابة: %s — «%s»\n",
       $okT ? '✔ صُحِّح' : '✘ **لم يُكتب**', mb_substr($t1, 0, 44));
if (!$okT || $okR < 2) { exit("⛔ كتابةٌ مزعومة — أُوقِف قبلَ إعلانِ نجاح\n"); }
echo "② البصمةُ بعدَ التصحيح: دفتر=" . substr(hash_file('sha256', $XLSX), 0, 12)
   . " · وثيقة=" . substr(hash_file('sha256', $DOCX), 0, 12) . "\n";
