<?php
/**
 * tools/amd01_governing_source_index.php — `AMD-01` المرحلة ١ · فهرسُ المصادرِ الحاكمة
 * ═══════════════════════════════════════════════════════════════════════════
 * ◆ **المخرَجُ بنصِّه** — `AMD-01` المرحلة ١: *«فهرسُ مصادرٍ حاكمٌ بترتيبِ سلطتِها ·
 *   وقائمةُ النقاطِ التي لها أكثرُ من نصٍّ حاكم»*. **والقبول**: صفرُ نقطةٍ لها
 *   قراران حاكمان بلا حسم — `UNRESOLVED_GOVERNING_CONFLICT = 0`.
 *
 * ◆ **وسلطتان لا سلطةٌ واحدة** (`AMD-01` §٢·١ و`MASTER_EXEC` §٠·٣): قراراتُ
 *   الأعمالِ والملكيّةِ والسياسةِ مصدرُها **سجلُّ قراراتِ المالك**، وترتيبُ
 *   التنفيذِ والمراجعةِ مصدرُه **الأمر**. ⛔ **ولا يُخلطان**.
 *
 * ◆ **والوجودُ يُقاس لا يُدَّعى**: كلُّ مصدرٍ يُفحص على القرصِ ببصمتِه وحجمِه.
 *   فمصدرٌ حاكمٌ مفقودٌ **يُرسِّب** — وفهرسٌ يعدّ ما لا يجده دعوى لا قياس.
 *
 * ◆ **والتعارضُ لا يُخترَع ولا يُخبَّأ**: كلُّ نقطةٍ هنا **مقيسةٌ أو منصوصة**،
 *   ولكلٍّ حكمُها ومرجعُ حكمِها. ⛔ **وما لا حكمَ له يُرسِّب ويُرفع تعارضًا**
 *   — ولا يُختار أحدُ طرفَيه بالاجتهاد.
 *
 * التشغيل: php tools/amd01_governing_source_index.php [--md] [--selftest]
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED);
mb_internal_encoding('UTF-8');
define('EMS_CLI', true);
$ROOT = str_replace(DIRECTORY_SEPARATOR, '/', dirname(__DIR__));
require_once $ROOT . '/includes/session_bootstrap.php';
require_once $ROOT . '/config.php';
while (ob_get_level()) { ob_end_clean(); }
$conn = $GLOBALS['conn'];
$conn->set_charset('utf8mb4');

$MD   = in_array('--md', $argv, true);
$SELF = in_array('--selftest', $argv, true);
$D    = 'docs/REPAIR01_20260823/';

/* ═══ ① المصادرُ بترتيبِ سلطتِها — `AMD-01` §٢ ═══════════════════════════
   الرتبةُ ٠ لأمرِ التشغيلِ الشامل بنصِّ §٠·١: «قائدُ الأوامرِ الثلاثة». */
$SOURCES = array(
    array(0, 'أمرُ التشغيلِ الشامل', $D . 'orders/MASTER_EXEC.txt',
          'الترتيبُ والتوازي وعدمُ التوقّفِ ومعيارُ الإغلاقِ النهائيّ', 'نافذ'),
    array(1, '`AMD-01` التنفيذيّ', $D . 'orders/AMD-01.txt',
          'إدارةُ التنفيذِ واستعادةُ الاستمراريّةِ والمراجعةُ العكسيّة', 'نافذ'),
    array(1, '`RPR-02` v2.3', $D . 'orders/RPR-02_v2.3.txt',
          'إغلاقُ مطابقةِ المعماريّةِ والسايدبارِ والشاشات', 'نافذ'),
    array(1, '`RPR-03`', $D . 'orders/RPR-03.txt',
          'التكاملُ والإنفاذُ والجاهزيّةُ التشغيليّة', 'نافذ'),
    array(1, '`AMD-01` وثيقةُ التعديلِ v1', $D . 'orders/AMD-01_v1_amendment.txt',
          'سبعُ نقاطٍ وملحق', 'منسوخ — مستوعَبٌ في `AMD-01` النافذِ وملحقِه (`MASTER_EXEC` §٠·٢)'),
    array(1, '`AMD-01` السابق', $D . 'orders/superseded/AMD-01.txt', '—', 'منسوخ'),
    array(1, '`RPR-02` السابق', $D . 'orders/superseded/RPR-02_v2.3.txt', '—', 'منسوخ'),
    array(1, '`RPR-03` السابق', $D . 'orders/superseded/RPR-03.txt', '—', 'منسوخ'),
    array(2, 'سجلُّ قراراتِ المالك — جدولًا', 'DB:repair01_decisions',
          'المصدرُ الحاكمُ لكلِّ قرارِ أعمالٍ وملكيّةٍ وسياسة', 'نافذ'),
    array(2, 'قراراتُ المالكِ G1–G7', $D . 'owner/OWNER_DECISIONS_20260827_G1_G7.md',
          'قرارُ المالكِ 2026-08-27', 'نافذ'),
    array(3, 'الدستور — الملفّ ٠٣', $D . '03 · الدستور.xlsx',
          'المبادئُ والقواعدُ العامّة', 'نافذ'),
    array(4, 'الدليلُ المعماريُّ الموحّد — ٠١', $D . '01 · الدليل المعماري.xlsx',
          'الأسطحُ والحبّةُ والحقولُ والملكيّة', 'نافذ'),
    array(5, 'الموجة أ — التشغيلُ والأصول', $D . '04 · الموجة أ — التشغيل والأصول.xlsx',
          'متطلباتُ الموجةِ وآلاتُ حالتِها', 'نافذ'),
    array(5, 'الموجة ب — التعاقدُ والتوريد', $D . '05 · الموجة ب — التعاقد والتوريد.xlsx',
          'متطلباتُ الموجةِ وآلاتُ حالتِها', 'نافذ'),
    array(5, 'الموجة ج — المال', $D . '06 · الموجة ج — المال.xlsx',
          'متطلباتُ الموجةِ وآلاتُ حالتِها', 'نافذ'),
    array(5, 'الموجة د — الأشخاصُ والرقابة', $D . '07 · الموجة د — الأشخاص والرقابة.xlsx',
          'متطلباتُ الموجةِ وآلاتُ حالتِها', 'نافذ'),
    array(5, 'الموجة هـ — المساحاتُ والتقارير', $D . '08 · الموجة هـ — المساحات والتقارير.xlsx',
          'متطلباتُ الموجةِ وآلاتُ حالتِها', 'نافذ'),
    array(6, 'ملفُّ القيادة — ٠٢', $D . '02 · القيادة.xlsx',
          'أسطحُ الرئيسِ والنوابِ ومصفوفةُ السلطة', 'نافذ'),
    array(6, 'السجلاتُ المؤسسيّةُ والقرارات — ٠٩', $D . '09 · السجلات المؤسسية والقرارات.xlsx',
          'السجلاتُ المؤسسيّة', 'نافذ'),
    array(7, 'المصالحةُ مع النظام — ١٠', $D . '10 · المصالحة مع النظام.xlsx',
          '**أدلّةُ واقعٍ لا مصدرُ تصميم**', 'نافذ'),
    array(7, 'المراجعةُ الثانية — ١١', $D . '11 · المراجعة الثانية.xlsx',
          '**أدلّةُ واقعٍ لا مصدرُ تصميم**', 'نافذ'),
    array(7, 'مراجعةُ القيادة — ١٢', $D . '12 · مراجعة القيادة.xlsx',
          '**أدلّةُ واقعٍ لا مصدرُ تصميم**', 'نافذ'),
    array(8, 'السجلُّ الرسميُّ للشاشات', 'DB:repair01_screen_registry',
          '**يُقاس عليه ولا يُقاس به**', 'نافذ'),
);

/* ═══ ② نقاطٌ لها أكثرُ من نصٍّ — ولكلٍّ حكمُها ومرجعُه ══════════════════
   ⛔ **ولا يُكتب هنا تعارضٌ مخترَع**: كلُّ نقطةٍ إمّا **منصوصةٌ** في أمرٍ
      نافذٍ يعلن النسخَ، وإمّا **مقيسةٌ** في هذه الجولة. */
$CONFLICTS = array(
    array('C-01', '`PLATFORM` رمزٌ ثانٍ وعشرون في `repair01_departments` (بأمرِ مالكٍ في جولةٍ سابقة)'
        . ' × `MASTER_EXEC` §٤·٧ «⛔ ولا تُعدَّ `PLATFORM` إدارةً ٢٢»',
        'محسومٌ **بقاعدةِ الأمرِ نفسِه** — §٥·٢: «فاعدُدْ كم تجميعًا يَعُدُّ ذلك الجدولَ مقامًا:'
        . ' فإن عدَّ أحدُها ٢٢ فالصفُّ يُفسد مقامًا ⇒ يُنقل إلى سجلٍّ مستقلّ». **والعدُّ تمَّ**:'
        . ' مقامان فاسدان (`repair01_w135_gate` G1 يشترط ٤ ويقيس ٥ · و`baseline_xlsx_build.php:154`'
        . ' يطبع ٢٢ تحت عنوانٍ يُعدِّد ٢١) ⇒ **يُنقل**.',
        'MASTER_EXEC §٥·٢ · قياسُ `master_exec_phase0_measure.php`', true),
    array('C-02', 'وثيقةُ التعديلِ `AMD-01_v1` × `AMD-01` التنفيذيّ',
        'v1 **مستوعَبةٌ ومنسوخة** — والنافذُ `AMD-01` وملحقُه.',
        'MASTER_EXEC §٠·٢', true),
    array('C-03', '`RPR-02` v2.1 و v2.2 FINAL × v2.3',
        '**v2.3 وحدَه نافذ** — ولا يُعمل بنسختَين تحمل كلٌّ منهما وصفَ «نهائي».',
        'AMD-01 §١·١ · MASTER_EXEC §٠·٢', true),
    array('C-04', '«يبدأ الإصلاحُ من المبيعاتِ ولا ينتقل حتى اكتمالِها»'
        . ' × «العملُ على عدّةِ مساراتٍ معًا حيث لا تبعيّةَ تمنع»',
        '**الأوّلُ منسوخٌ نصًّا** — والعملُ متوازٍ.',
        'MASTER_EXEC §٠·٢', true),
    array('C-05', '«حاجزٌ في إدارةٍ واحدةٍ يوقف البرنامجَ كلَّه»'
        . ' × «الحاجزُ يوقف المتطلبَ أو النطاقَ أو المسارَ الذي ينطبق عليه فقط»',
        '**الأوّلُ منسوخٌ نصًّا**.',
        'MASTER_EXEC §٠·٢ · §١ · §٩ المحظور ④', true),
    array('C-06', 'ثلاثُ مفرداتِ الحجبِ (`حاجز إنفاذ` · `قيمة تضبط` · `محسوم آلية`) × ستُّ درجات',
        '**الستُّ تنسخ كلَّ تصنيفٍ سابق** — والترحيلُ ليس تبديلَ اسمٍ بل حكمًا جديدًا على كلٍّ.',
        'MASTER_EXEC §٣', true),
    array('C-07', 'أرقامُ `BL-20260828` حالةً راهنة × خطُّ أساسٍ تاريخيّ',
        '**خطُّ أساسٍ تاريخيٌّ يُعاد قياسُه** — وقد أُعيد: ١٣ من ١٥ رقمًا ما زالت قائمة.',
        'MASTER_EXEC §٠·٢ · §٦·٠ · §٩ المحظور ⑩', true),
    array('C-08', 'شرطُ «انحدارٍ أخضرَ كامل» في `repair01_freeze.php`'
        . ' × `MASTER_EXEC` §٢ «جمِّدِ اللقطةَ — ولا قياسَ قبلها»',
        '**قاعدةٌ لا تنطبق أصلًا** فصُحِّح انطباقُها لا استُثني منها: الخضرةُ شرطُ **إصدارِ'
        . ' أساسٍ** لا شرطُ **قياسٍ تشخيصيّ**. ⇒ نافذتان، والتشخيصيّةُ **تختم الإحصاءَ'
        . ' بأسماءِ الساقطين**.',
        'AMD-01 المرحلة ٥ · النصُّ الدستوريُّ البند ⑬ · مُنفَّذٌ في `e5a4543c`', true),
);

/* ⛔ **السالبُ يكسر مفردةً فريدة**: نقطةٌ بلا حكم ⇒ يجب أن يرسُب الفهرس */
if ($SELF) {
    $CONFLICTS[] = array('C-XX', 'نقطةُ اختبارٍ سالبٍ بلا حكم', '', '', false);
}

echo "\n═══ `AMD-01` المرحلة ١ — فهرسُ المصادرِ الحاكمة ═══\n\n";
printf("  %-4s %-38s %-9s %s\n", 'رتبة', 'المصدر', 'الحال', 'الوجود');
echo "  " . str_repeat('─', 92) . "\n";
$missing = 0; $present = 0; $rows = array();
foreach ($SOURCES as $s) {
    list($rank, $name, $path, $scope, $state) = $s;
    if (strpos($path, 'DB:') === 0) {
        $t = substr($path, 3);
        $n = @$conn->query("SELECT COUNT(*) FROM `" . $t . "`");
        $ok = (bool) $n;
        $ev = $ok ? ($n->fetch_row()[0] . ' صفًّا') : '**مفقود**';
    } else {
        $f = $ROOT . '/' . $path;
        $ok = is_file($f);
        $ev = $ok ? (number_format(filesize($f)) . ' بايت · ' . substr(sha1_file($f), 0, 8)) : '**مفقود**';
    }
    if ($ok) { $present++; } else { $missing++; }
    $rows[] = array($rank, $name, $path, $scope, $state, $ev, $ok);
    printf("  %-4d %-38s %-9s %s %s\n", $rank, mb_substr(strip_tags($name), 0, 36),
           $state === 'نافذ' ? 'نافذ' : 'منسوخ', $ok ? '✔' : '✘', $ev);
}

echo "\n  ── نقاطٌ لها أكثرُ من نصٍّ حاكم ──\n";
$unresolved = 0;
foreach ($CONFLICTS as $c) {
    $ok = $c[4] && trim($c[2]) !== '' && trim($c[3]) !== '';
    if (!$ok) { $unresolved++; }
    printf("  %s %s · %s\n", $ok ? '✔' : '⛔', $c[0], mb_substr($c[1], 0, 84));
    printf("       ↳ %s\n", $ok ? mb_substr($c[2], 0, 96) : '**بلا حكمٍ — يُرفع تعارضًا ولا يُختار أحدُ طرفَيه**');
}

echo "\n────────────────────────────────────────────────────────────\n";
printf("**مصادرُ %d · حاضرٌ %d · مفقودٌ %d · نقاطُ تعارضٍ %d · بلا حسمٍ %d**\n",
       count($SOURCES), $present, $missing, count($CONFLICTS), $unresolved);
$pass = ($missing === 0 && $unresolved === 0);
echo $pass
    ? "🟢 **`UNRESOLVED_GOVERNING_CONFLICT = 0`** — والمرحلةُ الأولى مستوفيةٌ لقبولِها\n"
    : "✘ **لم تُستوفَ**: مصدرٌ مفقودٌ أو نقطةٌ بلا حسم\n";

if ($SELF) {
    echo "\n═══ الاختبارُ السالب ═══\n";
    echo $unresolved >= 1
        ? "🟢 **العدّادُ تحرَّك بنقطةٍ بلا حكم — فالفاحصُ يَحمَرُّ فعلًا**\n"
        : "✘ **العدّادُ لم يتحرّك**\n";
    exit($unresolved >= 1 ? 0 : 1);
}

if ($MD) {
    $o  = "# `AMD-01` المرحلة ١ — `Governing Source Index`\n\n";
    $o .= "> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/" . basename(__FILE__) . " --md`\n";
    $o .= "> **القبول** — `AMD-01` المرحلة ١: *«صفرُ نقطةٍ لها قراران حاكمان بلا حسم»*.\n\n";
    $o .= "## سلطتان لا سلطةٌ واحدة\n\n| المجال | المصدرُ الأعلى |\n|---|---|\n";
    $o .= "| قراراتُ الأعمالِ والملكيّةِ والسياسة | **سجلُّ قراراتِ المالك** — ولا ينسخه أمرٌ تنفيذيٌّ إلّا بمرجعِ قرارِ مالكٍ صريح |\n";
    $o .= "| ترتيبُ التنفيذِ والمراجعةِ وإدارةِ الحواجز | **هذا الأمرُ والتعديلاتُ اللاحقةُ له** |\n\n";
    $o .= "## الفهرس\n\n| الرتبة | المصدر | نطاقُه | الحال | الوجودُ مقيسًا |\n|---|---|---|---|---|\n";
    foreach ($rows as $x) {
        $o .= '| ' . $x[0] . ' | ' . $x[1] . '<br>`' . $x[2] . '` | ' . $x[3] . ' | '
            . ($x[4] === 'نافذ' ? '✔ نافذ' : '⛔ ' . $x[4]) . ' | ' . ($x[6] ? '✔ ' : '✘ ') . $x[5] . " |\n";
    }
    $o .= "\n## نقاطٌ لها أكثرُ من نصٍّ حاكم — ولكلٍّ حكمُها ومرجعُه\n\n";
    $o .= "| # | النقطة | الحكم | مرجعُ الحكم |\n|---|---|---|---|\n";
    foreach ($CONFLICTS as $c) {
        $o .= '| `' . $c[0] . '` | ' . $c[1] . ' | ' . ($c[2] !== '' ? $c[2] : '⛔ **بلا حكم**')
            . ' | ' . ($c[3] !== '' ? $c[3] : '—') . " |\n";
    }
    $o .= "\n**مصادرُ " . count($SOURCES) . " · حاضرٌ " . $present . " · مفقودٌ " . $missing
        . " · نقاطُ تعارضٍ " . count($CONFLICTS) . " · بلا حسمٍ " . $unresolved . "**\n\n";
    $o .= $pass
        ? "🟢 **`UNRESOLVED_GOVERNING_CONFLICT = 0`**\n"
        : "✘ **لم تُستوفَ**\n";
    file_put_contents($ROOT . '/docs/REPAIR01_20260823/AMD01_GOVERNING_SOURCE_INDEX.md', $o);
    echo "\n✔ كُتب: docs/REPAIR01_20260823/AMD01_GOVERNING_SOURCE_INDEX.md\n";
}
exit($pass ? 0 : 1);
