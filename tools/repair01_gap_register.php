<?php
/**
 * tools/repair01_gap_register.php — كتابةُ فجواتِ الجولةِ في الدفترِ الرسميّ
 * ═══════════════════════════════════════════════════════════════════════════
 * **الدرسُ المحروق `gap-codes-register-not-prose`**: «**فجوةٌ في السردِ لم تُكتب
 * في الدفترِ تُيتِّم كشوفَها**». وقد وقع مرّةً أخرى: `FINDINGS.md` أضافت سبعةَ
 * عشرَ كشفًا، **خمسةٌ منها تستشهد بـ`GAP-67..71` ولم تُكتب في الدفتر** — فرسَب
 * `FR-GOV-006` («صفرُ كشفٍ مُهمَلٍ لغيابِ رمزٍ يقابله مطلب») **وحُجب كلُّ التزام**.
 *
 * ◆ **والقياسُ حسَم السببَ لا الظنّ**: حزامُ الشواهدِ **أخضرُ** بنسخةِ `HEAD`
 *   من `FINDINGS.md` **وأحمرُ** بنسخةِ العمل — فالعطبُ في غيابِ التسجيلِ لا في
 *   الكشوفِ ولا في الفاحص.
 *
 * ⛔ **ولا يُصلَح هذا بتحريرِ الكشوفِ لتمرَّ البوّابة**: الكشفُ دليلٌ مقيسٌ
 *   والدليلُ لا يُعدَّل ليُرضي فاحصًا. **بل يُكتب المطلبُ الذي تطالب به الفجوة.**
 *
 * ◆ **والمضمونُ منقولٌ من أمرَي المالكِ لا مؤلَّفٌ**: لكلِّ فجوةٍ **سلوكُها
 *   المطلوبُ ومعيارُ قبولِها واختبارُها السالبُ** منصوصةً في `RPR-02` و`RPR-03`
 *   — ويُذكر موضعُها في `المصدرُ الحاكم`. **وحالتُها `OPEN`** ⛔ فلا يُعلَن
 *   إغلاقٌ بلا شاهدٍ في الحزام.
 *
 * التشغيل:
 *   php tools/repair01_gap_register.php           ← يعرض ما سيُكتب ولا يكتب
 *   php tools/repair01_gap_register.php --apply   ← يُلحق الصفوفَ بالدفتر
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED);
mb_internal_encoding('UTF-8');
$ROOT  = str_replace(DIRECTORY_SEPARATOR, '/', dirname(__DIR__));
require_once $ROOT . '/tools/lib/xlsx_io.php';
$XLSX  = $ROOT . '/docs/sources/INJ-FRD-REM-01/workbook.xlsx';
$APPLY = in_array('--apply', $argv, true);
$SHEET = 'سجل المتطلبات';
$SRC   = 'أمرا المالك RPR-02 v2.3 · RPR-03 — 2026-08-28';

if (!is_file($XLSX)) { exit("⛔ الدفترُ الرسميُّ مفقود\n"); }

/* ═══ ما يُكتب — منقولًا من نصِّ الأمرَين ═════════════════════════════════ */
$SPEC = array(
    'FR-GOV-007' => array(
        'gap'     => 'GAP-67',
        'now'     => 'ملفَّا مكتبةٍ خارجيّةٍ مسجَّلان شاشتَين حيَّتَين',
        'want'    => 'لا يُسجَّل سطحًا أيُّ مسارٍ تحت مجلَّدِ المكتباتِ الخارجيّة — '
                   . '**قاعدةٌ مانعةٌ دائمةٌ لا معالجةُ حالتَين**: فالحالةُ تُعالَج مرّةً والقاعدةُ تمنعها دائمًا',
        'trigger' => 'تسجيلُ سطحٍ في السجلِّ الرسميّ',
        'input'   => 'مسارُ الملفِّ المرشَّحِ سطحًا',
        'proc'    => 'رفضُ كلِّ مسارٍ تحت `vendor/` قبلَ التسجيل',
        'out'     => 'سجلٌّ بلا ملفِّ مكتبة',
        'fail'    => 'مسارُ مكتبةٍ يُرصد ويُرفض ويُسمّى — ولا يُبتلع صامتًا',
        'tables'  => 'repair01_screen_registry · repair01_surfaces',
        'pos'     => 'مسحُ الأسطحِ لا يُدخل `vendor/` ⇒ صفرُ ملفِّ مكتبةٍ في السجل',
        'neg'     => 'حقنُ مسارٍ تحت `vendor/` في المسح ← يُرفض ولا يُسجَّل',
        'accept'  => 'ملفُّ مكتبةٍ مسجَّلٌ شاشةً = صفر · **والقاعدةُ المانعةُ مفعَّلة** '
                   . '(‏`RPR-02` §١٢ · المقيسُ اليوم: ٢ · لا قاعدة)',
        'ev'      => 'RPR-02 §١١ · قاعدةٌ مانعةٌ دائمة',
        'prio'    => 'P1',
    ),
    'FR-GOV-008' => array(
        'gap'     => 'GAP-68',
        'now'     => 'اثنتا عشرةَ شاشةً مالكُها رمزٌ لا إدارةٌ وبلا تبريرٍ معتمَد',
        'want'    => '`PLATFORM` ملكيّةٌ مشروعةٌ **لا تُقبل بلا تبرير**: يُقبل السطحُ منصّيًّا '
                   . 'بأربعةٍ **معًا** — يخدم ثلاثَ إداراتٍ فأكثرَ خدمةً فعليّةً · ولا يملك حقيقةَ أعمال · '
                   . 'وله مالكٌ تقنيٌّ مسمًّى شخصًا · ومسجَّلٌ في سجلِّ المنصّةِ بمعرِّفِه وقاعدةِ ظهورِه. '
                   . '**وما لم يستوفِ الأربعةَ يعود إلى إدارتِه من السبعَ عشرة**',
        'trigger' => 'إسنادُ سطحٍ إلى رمزِ `PLATFORM`',
        'input'   => 'معرِّفُ السطحِ ومعاييرُ الانطباقِ الأربعة',
        'proc'    => 'فحصُ الأربعةِ مجتمعةً — ولا يكفي واحدٌ منها',
        'out'     => 'سطحٌ منصّيٌّ مبرَّرٌ أو مردودٌ إلى إدارتِه',
        'fail'    => 'سطحٌ لا يستوفي الأربعةَ يُسمّى ويُعاد — وكتابةُ كلمةِ «منصّي» ليست تبريرًا',
        'tables'  => 'repair01_screen_registry.owner_code · repair01_departments',
        'pos'     => 'سطحٌ يستوفي الأربعةَ ← يبقى `PLATFORM` بتبريرِه المسجَّل',
        'neg'     => 'سطحٌ يستوفي ثلاثةً فقط ← يُردُّ إلى إدارتِه ولا يبقى منصّيًّا',
        'accept'  => 'شاشةُ `PLATFORM` بلا تبريرٍ منصّيٍّ معتمَد = صفر '
                   . '(‏`RPR-02` §٥·٤ و§١٢ · `RPR-03` §٦ · المقيسُ اليوم: ١٢)',
        'ev'      => 'RPR-02 §٥·٤ · معايير التبرير المنصّي الأربعة',
        'prio'    => 'P1',
    ),
    'FR-GOV-009' => array(
        'gap'     => 'GAP-69',
        'now'     => 'دفترُ الهجراتِ انقطع: القاعدةُ تُستدعى بالعُرفِ ولا بوّابةَ ترسُب على من لا يستدعيها',
        'want'    => '**رسوبٌ صلبٌ فوريٌّ — ولا نافذةَ ظلّ**: أيُّ هجرةٍ لا تسجّل نفسَها في '
                   . '`schema_migrations` ترسُب على بوّابةِ التكامل. **والمعيارُ: إجماليُّ الهجراتِ '
                   . 'غيرِ المصالَحةِ = صفر** — لا «هجرةٌ *جديدةٌ* خارجَ الدفتر = صفر»، فذاك يسمح '
                   . 'للقديمةِ أن تبقى خارجَه أبدًا. **ولا تبقى هجرةٌ واحدةٌ بلا حالةٍ محكومة**',
        'trigger' => 'إضافةُ ملفِّ هجرةٍ أو تشغيلُه',
        'input'   => 'ملفّاتُ `database/migrations/` ودفترُ `schema_migrations`',
        'proc'    => 'مصالحةُ القرصِ بالدفترِ · وفحصُ استدعاءِ `ems_migration_recorded()` في غيرِ المقيَّد',
        'out'     => 'بوّابةٌ خضراءُ بمقاماتٍ مطبوعة · ودفترٌ بلا هجرةٍ غيرِ محكومة',
        'fail'    => 'هجرةٌ لا تسجّل نفسَها ⇒ رسوبٌ برمزِ خروجٍ غيرِ صفريّ — ولا تمرُّ بتحذير',
        'tables'  => 'schema_migrations · gov_migration_settlement',
        'pos'     => 'هجرةٌ تستدعي الدفترَ وتُشغَّل ← تُقيَّد وتمرُّ البوّابة',
        'neg'     => 'هجرةٌ صوريّةٌ لا تستدعي الدفترَ ← **البوّابةُ ترسُب فعلًا** وعدَدُ الحاجبِ يتحرّك بواحد',
        'accept'  => 'هجراتٌ غيرُ مصالَحةٍ = صفر · **وبوّابةٌ ترسُب مفعَّلةٌ ومُثبَتةٌ بالكسر** '
                   . '(‏`RPR-02` §٩ و§١٢ · `RPR-03` §٣·١ و§١٠ · المقيسُ عندَ الإصدار: ٥٨ · لا بوّابة)',
        'ev'      => 'RPR-02 §٩ · RPR-03 §٣·١',
        'prio'    => 'P0',
    ),
    'FR-GOV-010' => array(
        'gap'     => 'GAP-70',
        'now'     => 'الشجرةُ العاملةُ غيرُ نظيفةٍ أثناءَ القياس — فجزءٌ من السطحِ المقيسِ خارجَ أيِّ التزام',
        'want'    => '**لا قياسَ أثناءَ بناءٍ ولا بناءَ أثناءَ قياس**: نافذةُ القياسِ الرسميّةُ '
                   . 'تشترط **شجرةً مُلزَمةً نظيفةً وبصمةَ التزامٍ دقيقة**، ⛔ ولا يُجمَّد إلّا بعدَ '
                   . 'اكتمالِ أدواتِ القياسِ والتزامِها — وإلّا دارت دورةُ تجميدٍ وفكٍّ بلا طائل',
        'trigger' => 'فتحُ نافذةِ قياسٍ رسميّةٍ أو إصدارُ تقريرِ أساس',
        'input'   => 'حالُ الشجرةِ وبصمةُ الالتزامِ وحالُ اللقطة',
        'proc'    => 'رفضُ التجميدِ والقياسِ على شجرةٍ متّسخة',
        'out'     => 'تقريرٌ يمثّل نسخةً فعليّةً واحدةً من النظام',
        'fail'    => 'قياسٌ على شجرةٍ متغيّرةٍ يُرسَّب ولا يُنشَر — فهو لا يمثّل أيَّ نسخة',
        'tables'  => 'repair01_freeze_snapshot',
        'pos'     => 'شجرةٌ نظيفةٌ ⇒ يُجمَّد وتُفتح النافذة',
        'neg'     => 'شجرةٌ متّسخةٌ ⇒ **لا يُجمَّد** ويُطبع السبب',
        'accept'  => 'كلُّ تقريرِ قياسٍ يحمل البصمةَ السداسيّةَ وصادرٌ عن شجرةٍ نظيفة '
                   . '(‏`RPR-02` §١٠ و§١١ · المقيسُ عندَ الإصدار: ١٣ بندًا غيرَ ملتزم)',
        'ev'      => 'RPR-02 §١٠ · لقطتان لا لقطة',
        'prio'    => 'P1',
    ),
    'FR-GOV-011' => array(
        'gap'     => 'GAP-71',
        'now'     => 'تواريخُ الالتزامِ غيرُ رتيبة: رأسٌ أحدثُ طوبولوجيًّا بساعةٍ أقدم',
        'want'    => '**ترتيبُ اللقطاتِ بالبصمةِ الدقيقةِ لا بساعةِ الحائط**: فتواريخُ الالتزامِ '
                   . 'غيرُ رتيبةٍ **وقد ثبت ذلك بالقياس**، وأيُّ اختيارِ لقطةٍ أو ترتيبٍ زمنيٍّ '
                   . 'بساعةِ الحائطِ يكذب',
        'trigger' => 'اختيارُ لقطةٍ أو ترتيبُ حقائقَ زمنيًّا',
        'input'   => 'بصماتُ الالتزامِ وتواريخُه',
        'proc'    => 'الترتيبُ بترتيبِ الالتزامِ الطوبولوجيِّ لا بـ`frozen_at`',
        'out'     => 'ترتيبٌ يطابق تسلسلَ الحقائق',
        'fail'    => 'ترتيبٌ بساعةِ الحائطِ يُرصد ويُصحَّح — ولا يُعتمَد',
        'tables'  => 'repair01_freeze_snapshot.frozen_at · بصمةُ الالتزام',
        'pos'     => 'اللقطةُ تُختار بتاريخِ الالتزامِ ⇒ الترتيبُ يطابق الحقائق',
        'neg'     => 'اختيارٌ بساعةِ الحائطِ ⇒ يُرصد الاختلافُ ويُرسَّب',
        'accept'  => 'صفرُ تقريرٍ يرتّب لقطاتٍ بساعةِ الحائط (‏`RPR-02` §١٠ · '
                   . 'وقد أُصلح موضعان في `5c186484` و`05a0010b`)',
        'ev'      => 'RPR-02 §١٠ · الترتيبُ بالبصمةِ لا بالساعة',
        'prio'    => 'P2',
    ),
);

/* ═══ قراءةُ الدفترِ وبناءُ الصفوف ════════════════════════════════════════ */
$wb  = xlsx_read($XLSX);
$wr  = $wb[$SHEET];
$hdr = $wr[3];
$ix  = array();
foreach ($hdr as $i => $h) { $ix[trim(str_replace('◆ ', '', (string) $h))] = $i; }

$haveIds = array(); $haveGaps = array(); $maxNum = 0;
foreach ($wr as $i => $r) {
    if ($i < 4) { continue; }
    $id = trim((string) ($r[$ix['المعرِّف']] ?? ''));
    if (!preg_match('~^[A-Z]{2,4}-[A-Z]{2,4}-\d{3}$~', $id)) { continue; }
    $haveIds[$id] = 1;
    $maxNum = max($maxNum, (int) trim((string) ($r[$ix['#']] ?? 0)));
    foreach (preg_split('~[\s·]+~u', trim((string) ($r[$ix['الفجوة']] ?? ''))) as $g) {
        $g = trim($g);
        if (preg_match('~^GAP-?(\d+)$~u', $g, $gm)) { $haveGaps['GAP-' . $gm[1]] = $id; }
    }
}
printf("الدفترُ الآن: **%d** معرِّفًا · أقصى ترقيمٍ **%d** · **%d** رمزَ فجوةٍ مربوطًا\n",
    count($haveIds), $maxNum, count($haveGaps));

$rows = array(); $skip = array(); $num = $maxNum;
foreach ($SPEC as $id => $s) {
    if (isset($haveIds[$id]))       { $skip[] = "$id — المعرِّفُ مسجَّلٌ سلفًا"; continue; }
    if (isset($haveGaps[$s['gap']])) { $skip[] = "{$s['gap']} — مربوطٌ سلفًا بـ{$haveGaps[$s['gap']]}"; continue; }
    $num++;
    $c = array();
    $c[$ix['#']]                    = (string) $num;
    $c[$ix['المعرِّف']]              = $id;
    $c[$ix['الفجوة']]               = $s['gap'];
    $c[$ix['المجال']]               = 'الحوكمة';
    $c[$ix['المصدرُ الحاكم']]        = $SRC . ' · ' . $s['ev'];
    $c[$ix['الحالُ الآن']]           = $s['now'];
    $c[$ix['السلوكُ المطلوب']]       = $s['want'];
    $c[$ix['المُطلِق']]              = $s['trigger'];
    $c[$ix['الفاعل']]               = 'النظام';
    $c[$ix['الشروطُ السابقة']]       = 'أمرُ المالكِ صادرٌ ومقروءٌ من مصدرِه';
    $c[$ix['المُدخَل']]              = $s['input'];
    $c[$ix['المعالجة']]             = $s['proc'];
    $c[$ix['المُخرَج']]              = $s['out'];
    $c[$ix['انتقالُ الحالة']]        = '—';
    $c[$ix['قاعدةُ الصلاحية']]       = '—';
    $c[$ix['سلوكُ الفشل']]           = $s['fail'];
    $c[$ix['واقعةُ السجل']]          = 'سجلُّ الكشوفِ `docs/baseline_20260821/FINDINGS.md`';
    $c[$ix['الجداولُ والحقول']]      = $s['tables'];
    $c[$ix['الخدماتُ والأحداث']]     = '—';
    $c[$ix['اختبارٌ موجب']]          = $s['pos'];
    $c[$ix['اختبارٌ سالب']]          = $s['neg'];
    $c[$ix['معيارُ القبول']]         = $s['accept'];
    $c[$ix['الدليل']]               = 'FINDINGS.md · ' . $s['ev'];
    $c[$ix['التبعيات']]             = '—';
    $c[$ix['الأولوية']]             = $s['prio'];
    $c[$ix['الحالة']]               = 'مُعلَن';
    $c[$ix['Requirement_Type']]     = 'ARCHITECTURE_CONTROL';
    $c[$ix['Atomicity_Level']]      = 'ATOMIC';
    $c[$ix['Parent_Requirement_ID']] = '—';
    $c[$ix['Change_Set_ID']]        = 'CHG-RPR-0203';
    $c[$ix['Test_Applicability']]   = 'موجب=YES · سالب=YES';
    $c[$ix['N/A_Reason']]           = '—';
    $c[$ix['Threshold_Source']]     = 'لا عتبةَ رقميةً في هذا المتطلب — والقيمُ من أمرِ المالك';
    $c[$ix['Legacy_Alias']]         = '—';
    /* ⛔ **ولا يُعلَن إغلاقٌ هنا**: الإغلاقُ بشاهدٍ في الحزامِ لا بصفٍّ يُكتب */
    $c[$ix['Closure_State']]        = 'OPEN';
    $rows[] = $c;
    printf("  + %s ⇐ %s · %s\n", $id, $s['gap'], mb_substr($s['now'], 0, 58));
}
foreach ($skip as $x) { echo "  ⟳ تُخطّى: $x\n"; }

if (!$rows) { echo "\nلا جديدَ يُكتب.\n"; exit(0); }
if (!$APPLY) { echo "\nقياسٌ فقط — أعِد التشغيلَ بـ`--apply` للكتابة.\n"; exit(0); }

$n = xlsx_append_rows($XLSX, $SHEET, $rows);
printf("\n✔ أُلحق **%d** صفًّا بـ`%s`\n", $n, $SHEET);

/* ═══ التحقّقُ بإعادةِ القراءةِ — ⛔ ولا يُصدَّق الكاتبُ على كلمتِه ════════ */
$wb2 = xlsx_read($XLSX);
$wr2 = $wb2[$SHEET];
$ok = 0;
foreach ($wr2 as $i => $r) {
    if ($i < 4) { continue; }
    $id = trim((string) ($r[$ix['المعرِّف']] ?? ''));
    if (isset($SPEC[$id])) {
        $g = trim((string) ($r[$ix['الفجوة']] ?? ''));
        if ($g === $SPEC[$id]['gap']) { $ok++; }
    }
}
printf("✔ أُعيدت القراءةُ: **%d/%d** مطابقًا بمعرِّفِه ورمزِ فجوتِه\n", $ok, count($SPEC));
exit($ok === count($SPEC) ? 0 : 1);
