<?php
/**
 * tools/rpr02a_prompt_reverse_review.php — المراجعةُ العكسيّةُ لبرومتِ RPR-02-A
 * ═══════════════════════════════════════════════════════════════════════════
 * **مراجعةٌ عكسيّةٌ**: تبدأ من **المُنتَجِ** (ملفُّ البرومت) وتسأل: هل يحمل كلَّ
 * ما نصَّ عليه **المُصمَّم** (أمرُ `RPR-02-A`)؟ — لا العكس.
 *
 * ◆ **والمقامُ يُعلَن**: كلُّ مطلبٍ في الأمرِ صفٌّ هنا بمرساةٍ نصّيّة، **ومن لا
 *   مرساةَ له لا يُدَّعى حملُه**. فقائمةٌ تقول «الكلُّ مغطًّى» بلا مقامٍ مطبوعٍ
 *   دعوى لا قياس.
 *
 * ⛔ **ولا يُصلح هذا الفاحصُ شيئًا**: يقرأ ويُحصي. والإصلاحُ في الملفِّ نفسِه.
 * ⚠ **والمرساةُ تُختار مميِّزةً**: لفظٌ عامٌّ («الشاشة») يمرُّ بلا حملٍ حقيقيّ.
 * ⛔ **ولا تُضعَّف مرساةٌ لتمرَّ** — إن رسبت فالعلاجُ في البرومتِ لا في المرساة،
 *   إلّا أن يكون العطبُ في الفاحصِ نفسِه فيُصلَح الفاحصُ لا مخرَجُه.
 *
 * التشغيل: php tools/rpr02a_prompt_reverse_review.php [--md]
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED);
mb_internal_encoding('UTF-8');
$ROOT = dirname(__DIR__);
$MD   = in_array('--md', $argv, true);

$PROMPT = $ROOT . '/docs/REPAIR01_20260823/plan/RPR-02-A.prompt.md';
$ORDER  = $ROOT . '/docs/REPAIR01_20260823/orders/RPR-02-A.txt';
foreach (array($PROMPT, $ORDER) as $p) {
    if (!is_file($p)) { exit("✘ مفقود: $p\n"); }
}
$doc = file_get_contents($PROMPT);

/* ── التطبيع: التشكيلُ والتطويلُ وزينةُ ماركداون تُزال من الطرفَين معًا ──────
   (منقولٌ حرفًا من `rpr_prompt_reverse_review.php` — فالعطبان اللذان أُصلحا
    هناك يقعان هنا حتمًا: ترميزُ ماركداون يقطع العبارة · وعلامةُ الاقتباسِ
    تبقى بعد طيِّ الأسطر.) */
$norm = function ($s) {
    $s = preg_replace('~^\s*>+\s?~mu', '', (string) $s);   /* اقتباسُ ماركداون */
    $s = preg_replace('~[*`_]~u', '', $s);                 /* زينةُ ماركداون  */
    $s = preg_replace('~[\x{064B}-\x{0652}\x{0653}-\x{0655}\x{0670}\x{0640}]~u', '', $s);
    $s = preg_replace('~[\x{0622}\x{0623}\x{0625}]~u', "\u{0627}", $s);
    $s = preg_replace('~\x{0649}~u', "\u{064A}", $s);
    $s = preg_replace('~\x{0629}~u', "\u{0647}", $s);
    /* ③ **عطبٌ ثالثٌ وقع فعلًا في هذه الجولة**: الرقمُ الهنديُّ والعربيُّ تمثيلان
       لعددٍ واحد — فمرساةُ «٢١» رسَبَت على نصٍّ يكتبها «21»، **وهو عينُ عائلةِ
       «المقارنةِ بين تمثيلَين لشيءٍ واحد»**. فتُطوى الأرقامُ إلى صورةٍ واحدة.
       ⛔ **وهذا لا يُضعِّف مرساةً**: توحيدُ تمثيلِ العددِ لا يُمرِّر عددًا آخر. */
    $s = strtr($s, array('٠'=>'0','١'=>'1','٢'=>'2','٣'=>'3','٤'=>'4',
                         '٥'=>'5','٦'=>'6','٧'=>'7','٨'=>'8','٩'=>'9','٫'=>'.'));
    $s = preg_replace('~\s+~u', ' ', $s);
    return $s;
};
$N = $norm($doc);

/* ═══ المقام: كلُّ مطلبٍ في `RPR-02-A` بمرساتِه ════════════════════════════
   الحقول: [القسم, المطلب, مرساةٌ أو أكثر (كلُّها تُشترط)]                     */
$REQ = array(

/* ── §ترويسة ───────────────────────────────────────────────────────────── */
array('§ترويسة', 'الموضوع: الحزمةُ المحدَّثةُ وتصحيحُ السايدبارِ وسدُّ العجز',
    array('الحزمة المحدثة', 'وتصحيح السايدبار وسد العجز')),
array('§ترويسة', 'المرفقات: اثنا عشرَ ملفًّا وأربعةٌ مفردةٌ منها',
    array('اثنا عشر مصنفا', 'الملفات المفرده الاربعه')),
array('§ترويسة', 'المرجعُ الحاكم: `RPR-02` v2.3 و`RPR-03` وما فيهما يبقى النافذ',
    array('RPR-02_v2.3', 'RPR-03', 'وما فيهما يبقي النافذ')),
array('§ترويسة', 'الحالة: إلزاميٌّ ولا يُبدَأ بناءٌ قبل الخطوةِ الأولى',
    array('لا تبن سطرا واحدا قبل جدول الفرز')),

/* ── §١ اعتمد الحزمةَ وحدَها ──────────────────────────────────────────── */
array('§١', 'افتح الملفَّ المضغوطَ واستبدلْ به كلَّ نسخةٍ عندك',
    array('افتح الملف المضغوط واستبدل به كل نسخه عندك')),
array('§١', 'الملفّاتُ المفردةُ نسخٌ من داخلِه لا إضافات',
    array('نسخ من داخل الحزمه لا اضافات')),
array('§١', 'خمسةُ ملفّاتٍ تحرّكت — بأسمائها الخمسة',
    array('الدليل المعماري', 'الدستور', 'الموجه ا', 'الموجه د', 'السجلات المؤسسيه')),
array('§١', 'ولا تعمل على أيٍّ منها من نسخةٍ أقدم',
    array('من نسخه اقدم')),

/* ── §٢ ما استُجدّ ────────────────────────────────────────────────────── */
array('§٢', 'سطحان في إدارةِ البلاغات',
    array('سطحان في اداره البلاغات')),
array('§٢', 'الإبلاغُ السياقيُّ — زرٌّ يحمل سياقَ الشاشةِ آليًّا فيصل مصنَّفًا ومربوطًا بمحلِّه',
    array('الابلاغ السياقي من داخل الشاشه', 'يحمل سياق الشاشه اليا', 'مصنفا ومربوطا بمحله')),
array('§٢', 'صندوقُ بلاغاتِ الإدارة — إسقاطٌ موحّدٌ معلَّمٌ بالنطاقِ لا نسخةٌ لكلِّ إدارة',
    array('صندوق بلاغات الاداره', 'اسقاطا موحدا معلما بالنطاق', 'لا نسخه لكل اداره')),
array('§٢', 'واحدٌ وعشرون سطحًا وُسمت `Documentation Artifact — خارج السايدبار`',
    array('Documentation Artifact — خارج السايدبار', 'واحد وعشرون سطح', '٢١')),
array('§٢', 'في المبيعاتِ والمورِدين والتمويلِ والأسطول — بأعدادِها',
    array('المبيعات 7', 'الموردون 6', 'الاسطول 5', 'التمويل 3')),
array('§٢', 'أصنافُها الستّة: قاموسُ البيانات · خريطةُ الترحيل · قواعدُ الاستنتاج · مصفوفةُ التكامل · القوائمُ المرجعيّة · تقاريرُ المراجعة',
    array('قاموس البيانات', 'خريطه الترحيل', 'قواعد الاستنتاج', 'مصفوفه التكامل', 'القوائم المرجعيه', 'تقرير المراجعه')),
array('§٢', 'تُقرأ عند البناءِ ولا تُبنى شاشةً ولا تظهر بندًا',
    array('تقرا عند البناء ولا تبني شاشه ولا تظهر بندا')),
array('§٢', 'م 111 — الصفرُ المقيسُ يُكتب صفرًا · والفراغُ يعني «غير مقيس»',
    array('م 111', 'الصفر المقيس يكتب صفرا ولا يترك فراغا', 'الفراغ يعني «غير مقيس»')),
array('§٢', 'م 112 — كلُّ حقلٍ يحمل جدولَه المصدرَ · وبلا مفتاحِ المصدرِ يُفرِط في الوسم',
    array('م 112', 'يحمل مصدره', 'يفرط في الوسم')),

/* ── §٣ التصحيح ──────────────────────────────────────────────────────── */
array('§٣', 'العجزُ في السايدبارِ ثلاثةٌ وستّون لا ثمانون',
    array('63 لا 80')),
array('§٣', 'سبعةَ عشرَ ممّا عُدَّ غائبًا هي أسطحُ التوثيق',
    array('سبعه عشر مما عد غائبا هي اسطح التوثيق')),
array('§٣', 'لا تظهر أصلًا · ولا تدخل طابورَ البناء · ولا تُحسب فجوةً في أيِّ قياسٍ قادم',
    array('لا ينبغي ان تظهر اصلا', 'ولا تدخل طابور البناء', 'ولا تحسب فجوه في اي قياس قادم')),

/* ── §٤ ترويسةُ الخطوات ─────────────────────────────────────────────── */
array('§٤', 'الخطواتُ بترتيبِها — ولا يُقدَّم لاحقٌ على سابق',
    array('بترتيبها ولا يقدم لاحق علي سابق')),

/* ── §٤·١ الفرز ──────────────────────────────────────────────────────── */
array('§٤·١', 'لكلِّ سطحٍ من الثلاثةِ والستّين سؤالان فقط',
    array('الثلاثه والستين', 'سؤالان فقط')),
array('§٤·١', 'السؤالان: هل له ملفٌّ على القرص؟ وهل له صفٌّ في السجلِّ الرسميّ؟',
    array('هل له ملف علي القرص؟', 'هل له صف في السجل الرسمي؟')),
array('§٤·١', 'السلّةُ (أ): مبنيٌّ ومسجَّلٌ وينقصه رابط — عملُ دقائق',
    array('مبني ومسجل وينقصه رابط', 'عمل دقائق')),
array('§٤·١', 'السلّةُ (ب): مبنيٌّ وغيرُ مسجَّلٍ ← يُسجَّل ثمَّ يُربط',
    array('مبني وغير مسجل', 'يسجل ثم يربط')),
array('§٤·١', 'السلّةُ (ج): غيرُ مبنيٍّ ← يدخل طابورَ البناء',
    array('غير مبني', 'يدخل طابور البناء')),
array('§٤·١', 'ولا تبنِ سطرًا واحدًا قبل الفرز — فقد تبني ما هو مبنيٌّ وينقصه رابطٌ فقط',
    array('فقد تبني ما هو مبني وينقصه رابط فقط')),
array('§٤·١', 'المخرَج: جدولٌ بثلاثةِ أعمدةٍ — السطح · السلّة · الدليل (مسارُ الملفِّ أو معرِّفُ الصفّ)',
    array('السطح | السله | الدليل', 'مسار الملف او معرف الصف')),

/* ── §٤·٢ دليلُ السايدبار ────────────────────────────────────────────── */
array('§٤·٢', 'سبعَ عشرةَ إدارةً وأربعةُ رموزٍ خارجَها',
    array('سبع عشره اداره واربعه رموز خارجها')),
array('§٤·٢', 'تُنزع الأدوارُ الأربعةُ المدسوسة — بأسمائها',
    array('اداره الصلاحيات', 'امين المستودع', 'المدير المالي', 'اداره الموقع')),
array('§٤·٢', 'وتُضاف الإداراتُ الثلاثُ الغائبة — بأسمائها',
    array('الخزينه', 'الحوكمه والالتزام', 'البلاغات')),
array('§٤·٢', 'ولكلِّ إدارةٍ مجموعاتُها وترتيبُ شاشاتِها كما في ملفِّها لا كما تراكمت',
    array('كما في ملفها', 'لا كما تراكمت')),
array('§٤·٢', 'الملفُّ يبقى باسمِه «دليل العرض بالأدوار» ويُنزع عنه «الشامل النهائي»',
    array('دليل العرض بالادوار', 'مرجعا نافعا لا مقاما للمصالحه', 'ينزع عنه وصف «الشامل النهائي»')),

/* ── §٤·٣ الأسطحُ المنصّيّة ──────────────────────────────────────────── */
array('§٤·٣', 'الأسطحُ المنصّيّةُ الثمانيةُ بأسمائها',
    array('المراسلات', 'الرئيسيه', 'مهامي', 'بلاغاتي', 'بوابتي', 'مساحه عملي', 'انجازي', 'طلباتي')),
array('§٤·٣', 'كلٌّ يُعرَّف مرّةً واحدةً بسطحٍ معلَّمٍ بنطاقِ المستخدمِ ويُستدعى بلا نسخ',
    array('كل يعرف مره واحده', 'معلم بنطاق المستخدم', 'يستدعي في القوائم بلا نسخ')),
array('§٤·٣', 'والمكسبُ مقيس: مئةٌ وسبعون بندًا تختفي من القوائم',
    array('١٧٠')),

/* ── §٤·٤ الأربعةُ والثلاثون ─────────────────────────────────────────── */
array('§٤·٤', 'الأربعةُ والثلاثون خارجَ بيتِها — ولكلٍّ سؤالٌ واحد: تكتب أم تقرأ؟',
    array('الاربعه والثلاثون خارج بيتها', 'تكتب ام تقرا؟')),
array('§٤·٤', 'تقرأ ← تبقى وتُوسَم إسقاطًا بمرجعِ مصدرِه',
    array('توسم اسقاطا بمرجع مصدره')),
array('§٤·٤', 'تكتب ← تُنقل ملكيتُها إلى بيتِها ويبقى عرضٌ للقراءةِ فقط',
    array('تنقل ملكيتها الي بيتها', 'عرض للقراءه فقط')),
array('§٤·٤', 'وابدأ بالأربعةِ في المراجعِ الداخليّ — يخرق استقلالَ التأكيدِ وهو مبدأٌ لا تفصيل',
    array('ابدا بالاربعه في المراجع الداخلي', 'يخرق استقلال التاكيد', 'مبدا لا تفصيل')),

/* ── §٤·٥ البناءُ بترتيبِه ───────────────────────────────────────────── */
array('§٤·٥', 'المبيعاتُ أوّلًا — بنودُ العقدِ ومصفوفةُ الالتزاماتِ والتغطيةُ التعاقديّةُ والمطالبات',
    array('المبيعات اولا', 'بنود العقد', 'مصفوفه الالتزامات', 'التغطيه التعاقديه', 'المطالبات')),
array('§٤·٥', 'ثمَّ الموردون — سجلُّ تسليمِ الخاناتِ وتخصيصُ الدفعِ على الإقفالات',
    array('ثم الموردون', 'سجل تسليم الخانات', 'تخصيص الدفع علي الاقفالات')),
array('§٤·٥', 'ثمَّ الأسطول — الخروجُ المؤقّتُ والدائمُ وحركةُ الموقعِ والمصالحات',
    array('ثم الاسطول', 'الخروج المؤقت والدائم', 'حركه الموقع والمصالحات')),
array('§٤·٥', 'ثمَّ الباقي',
    array('ثم الباقي')),

/* ── §٥ المحظورات ───────────────────────────────────────────────────── */
array('§٥', 'لا بناءَ قبل الفرز',
    array('لا بناء قبل الفرز')),
array('§٥', 'ولا حذفَ صامتٍ — فكلُّ تعطيلٍ بعذرٍ ومرجعٍ وتاريخ',
    array('ولا حذف صامت', 'بعذر ومرجع وتاريخ')),
array('§٥', 'ولا نسخةَ لسطحٍ منصّيٍّ في كلِّ إدارة',
    array('ولا نسخه لسطح منصي في كل اداره')),
array('§٥', 'ولا نقلَ سطحٍ من بيتِه بلا قرارٍ موثَّق',
    array('ولا نقل سطح من بيته بلا قرار موثق')),
array('§٥', 'ولا قياسَ أثناءَ بناء',
    array('ولا قياس اثناء بناء')),
array('§٥', 'ولا يُحسب سطحُ توثيقٍ عجزًا',
    array('ولا يحسب سطح توثيق عجزا')),

/* ── §٦ المطلوبُ في الردّ ────────────────────────────────────────────── */
array('§٦', 'أوّلًا وقبلَ غيرِه: جدولُ الفرزِ — كم في كلِّ سلّةٍ بدليلِ كلٍّ',
    array('اولا وقبل غيره', 'جدول الفرز', 'في كل سله')),
array('§٦', 'ثمَّ دليلُ السايدبارِ المعياريُّ ومصالحتُه بفروقٍ مسمّاة',
    array('بفروق مسماه')),
array('§٦', 'وعددُ البنودِ التي اختفت بعد توحيدِ المنصّيّةِ الثمانية',
    array('البنود التي اختفت')),
array('§٦', 'والأربعةُ والثلاثون بحكمٍ لكلٍّ',
    array('بحكم لكل')),
array('§٦', 'ولا ترسل خطّةَ بناءٍ قبل جدولِ الفرز — فالخطّةُ قبله تخمينٌ لا خطّة',
    array('ولا ترسل خطه بناء قبل جدول الفرز', 'تخمين لا خطه')),

/* ── §٧ الملاحظاتُ الثلاث ───────────────────────────────────────────── */
array('§٧', 'ثلاثُ ملاحظاتٍ تخصُّ المالكَ لا المنفِّذَ — والفصلُ معلَن',
    array('ملاحظات المالك الثلاث')),
array('§٧', 'الأولى — بوّابةٌ ترسُب لا إدخالٌ يدويّ',
    array('بوابه ترسب لا ادخال يدوي')),
array('§٧', 'ومعيارُ الإثباتِ أدقُّ ممّا كُتب في الأمرِ نفسِه',
    array('ادق مما كتبته في الامر')),
array('§٧', 'وبحصرِ المقامِ في غيرِ المقيَّدِ كي لا يوجب كسرَ كاشفِ التحرير',
    array('كاشف التحرير', 'حصر المقام في غير المقيد')),
array('§٧', 'وبإثباتِها بحركةِ عدّادٍ بواحدٍ ثمَّ عودةٍ لا بمجرّدِ رسوب',
    array('بحركه عداد بواحد ثم عوده', 'لا برسوب مجرد')),
array('§٧', 'فاعتمدْه لكلِّ حاجبٍ تبنيه بعده',
    array('لكل حاجب تبنيه')),
array('§٧', 'الثانية — العطبُ في عُدَّتِك أنتج قاعدةً مؤسَّسيّة (م 111)',
    array('صار الماده 111', 'العطب في عدتك انتج قاعده مؤسسيه')),
array('§٧', 'الثالثة — نافذةُ الظلِّ صفرٌ لأنَّ حدودَ الاعتمادِ العدديّةَ قرارُ مالكٍ لم يصدر',
    array('نافذه الظل صفر', 'حدود الاعتماد العدديه قرار مالك لم يصدر بعد')),
array('§٧', 'أربعَ عشرةَ رحلةً تُسجَّل ولا تحجب مهما عملت',
    array('اربع عشره رحله تسجل ولا تحجب')),
array('§٧', 'ولا يُحسب ذلك تقصيرًا منك',
    array('ولا يحسب ذلك تقصيرا منك')),
);

/* ── وضعُ التردّد `--freq`: كم مرّةً ترد كلُّ مرساةٍ في المُنتَج؟ ────────────
   ⚠ **ولِمَ يلزم؟** الاختبارُ السالبُ يكسر مرساةً ليرى العدّادَ يتحرّك —
   **ومرساةٌ ترد مرّتَين لا تنكسر بتحريرٍ واحد**، فيبقى العدّادُ ثابتًا
   ويُظنُّ الفاحصُ أعمى وهو سليم. **فالكسرُ يقع على مرساةٍ فريدةٍ أو على كلِّ
   مواضعِها.** وهذا الوضعُ يُخرج القائمةَ الصالحةَ للكسر. */
if (in_array('--freq', $argv, true)) {
    $u = 0; $m = 0; $z = 0; $t = 0; $uniqList = array();
    foreach ($REQ as $r) {
        foreach ($r[2] as $a) {
            $t++;
            $n = mb_substr_count($N, $norm($a));
            if ($n === 0)      { $z++; printf("  ✘ %-2d %s\n", $n, $a); }
            elseif ($n === 1)  { $u++; $uniqList[] = $a; }
            else               { $m++; printf("  ⚠ %-2d %s\n", $n, $a); }
        }
    }
    printf("\nمراسٍ: %d · فريدةٌ: %d · متكرِّرةٌ: %d · معدومةٌ: %d\n", $t, $u, $m, $z);
    echo "\nصالحٌ للكسرِ في الاختبارِ السالب (فريدٌ — أوّلُ 5):\n";
    foreach (array_slice($uniqList, 0, 5) as $a) { echo "  · $a\n"; }
    exit(0);
}

/* ── الاختبارُ السالب `--selftest` — **الحاجبُ يُثبِت أنّه يَحمَرّ** ─────────
   المعيارُ الذي أقرَّه المالكُ في `RPR-02-A` §٧·١: **«بإثباتِها بحركةِ عدّادٍ
   بواحدٍ ثمَّ عودةٍ لا بمجرّدِ رسوب»**. فيُكسَر هنا مرساةٌ **فريدةٌ** واحدةٌ في
   نسخةٍ **في الذاكرة** (‏لا في الملفّ) ويُشترط: ‏N ⇒ N−1 ⇒ N بالضبط.
   ⚠ **والمرساةُ تُختار فريدةً** — ومرساةٌ ترد مرّتَين لا تنكسر بتحريرٍ واحد
     فيبقى العدّادُ ثابتًا ويُظنُّ الفاحصُ أعمى وهو سليم. */
$score = function ($text) use ($REQ, $norm) {
    $t = $norm($text); $c = 0;
    foreach ($REQ as $r) {
        $all = true;
        foreach ($r[2] as $a) { if (mb_strpos($t, $norm($a)) === false) { $all = false; break; } }
        if ($all) { $c++; }
    }
    return $c;
};
if (in_array('--selftest', $argv, true)) {
    $base = $score($doc);
    $victim = null;
    foreach ($REQ as $r) {
        foreach ($r[2] as $a) {
            if (mb_substr_count($N, $norm($a)) === 1) { $victim = $a; break 2; }
        }
    }
    if ($victim === null) { exit("✘ لا مرساةَ فريدةً — الاختبارُ السالبُ غيرُ ممكن\n"); }
    /* الكسرُ في الذاكرة: تُحذف المرساةُ الفريدةُ من نصِّ المستند */
    $pos = mb_strpos($N, $norm($victim));
    $broken = mb_substr($N, 0, $pos) . '‡KSR‡' . mb_substr($N, $pos + mb_strlen($norm($victim)));
    $after  = $score($broken);
    $back   = $score($doc);
    echo "\n═══ الاختبارُ السالبُ — حركةُ عدّادٍ ثمَّ عودة ═══\n";
    echo "  المرساةُ المكسورةُ (فريدة): «{$victim}»\n";
    printf("  ① قبل الكسر  : %d\n  ② بعد الكسر  : %d\n  ③ بعد العودة : %d\n", $base, $after, $back);
    $pass = ($after === $base - 1) && ($back === $base);
    echo $pass
        ? "\n🟢 **العدّادُ تحرَّك بواحدٍ ثمَّ عاد — فالفاحصُ يَحمَرُّ فعلًا**\n"
        : "\n🔴 **العدّادُ لم يتحرَّك بواحدٍ — الفاحصُ لا يُوثَق به**\n";
    exit($pass ? 0 : 1);
}

/* ── القياس ──────────────────────────────────────────────────────────────── */
$rows = array(); $ok = 0; $bad = 0;
foreach ($REQ as $r) {
    list($sec, $what, $anchors) = $r;
    $missing = array();
    foreach ($anchors as $a) {
        if (mb_strpos($N, $norm($a)) === false) { $missing[] = $a; }
    }
    $pass = empty($missing);
    if ($pass) { $ok++; } else { $bad++; }
    $rows[] = array($sec, $what, $pass, $missing);
}
$tot = count($rows);

echo "\n═══ المراجعةُ العكسيّةُ لبرومتِ RPR-02-A ═══\n";
printf("  المُنتَج : %s (%d حرفًا)\n", 'docs/REPAIR01_20260823/plan/RPR-02-A.prompt.md', mb_strlen($doc));
printf("  المصمَّم : %s\n", 'docs/REPAIR01_20260823/orders/RPR-02-A.txt');
printf("  المقام  : %d مطلبًا من الأمرِ — كلٌّ بمرساتِه النصّيّة\n\n", $tot);

$cur = '';
foreach ($rows as $x) {
    if ($x[0] !== $cur) { $cur = $x[0]; echo "── $cur ──\n"; }
    printf("  %s %s\n", $x[2] ? '✔' : '✘', $x[1]);
    if (!$x[2]) { echo "      ⚠ مرساةٌ غائبة: «" . implode('» · «', $x[3]) . "»\n"; }
}

echo "\n────────────────────────────────────────────────────────────────────\n";
printf("**محمولٌ %d · غائبٌ %d · المقام %d — التغطيةُ %.1f%%**\n",
    $ok, $bad, $tot, $tot ? ($ok * 100 / $tot) : 0);
echo $bad === 0
    ? "🟢 **البرومتُ يحمل كلَّ مطلبٍ في الأمرِ — بمرساةٍ لكلٍّ**\n"
    : "🔴 **$bad مطلبًا لم يُحمَل — ولا يُدَّعى الاكتمال**\n";

if ($MD) {
    $o  = "# المراجعةُ العكسيّةُ لبرومتِ `RPR-02-A`\n\n";
    $o .= "> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02a_prompt_reverse_review.php --md`\n";
    $o .= "> **المصمَّم**: `docs/REPAIR01_20260823/orders/RPR-02-A.txt` · ";
    $o .= "**المُنتَج**: `docs/REPAIR01_20260823/plan/RPR-02-A.prompt.md`\n\n";
    $o .= sprintf("**محمولٌ %d · غائبٌ %d · المقام %d — التغطيةُ %.1f%%**\n",
        $ok, $bad, $tot, $tot ? ($ok * 100 / $tot) : 0);
    $o .= "\n> **ومن لا مرساةَ له لا يُدَّعى حملُه** — فالمقامُ مطبوعٌ لا مُدَّعًى.\n";
    $cur = '';
    foreach ($rows as $x) {
        if ($x[0] !== $cur) { $cur = $x[0]; $o .= "\n## $cur\n\n| المطلب | الحكم | المرساةُ الغائبة |\n|---|---|---|\n"; }
        $o .= sprintf("| %s | %s | %s |\n", $x[1],
            $x[2] ? '✔' : '✘ غائب', $x[2] ? '—' : '`' . implode('` · `', $x[3]) . '`');
    }
    file_put_contents($ROOT . '/docs/REPAIR01_20260823/RPR02A_PROMPT_REVERSE_REVIEW.md', $o);
    echo "✔ كُتب docs/REPAIR01_20260823/RPR02A_PROMPT_REVERSE_REVIEW.md\n";
}
exit($bad === 0 ? 0 : 1);
