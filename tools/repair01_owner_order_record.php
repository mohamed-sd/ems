<?php
/**
 * tools/repair01_owner_order_record.php — تثبيتُ أجوبةِ المالكِ من أمرِه المكتوب
 * ═══════════════════════════════════════════════════════════════════════════
 * **الحالُ قبلَه**: المراجعةُ العكسيّةُ (‏أمرُ المالك · البند ١٢) وجدت أربعةَ
 * قراراتٍ بنيويّةٍ حالتُها `APPROVED` **وخانةُ قرارِ المالكِ في مصنَّفِه خالية**،
 * ومرجعُها يشير إلى **موضعِ السؤالِ** لا إلى جواب. وأُثبت من ورقتَين في المصنَّف
 * (`OWNER_DECISIONS_MASTER` و`05_سجل_القرارات`) أنَّ كلتَيهما تقولان
 * «بانتظار قرار المالك».
 *
 * **والحالُ الآن**: أمرُ المالكِ المكتوبُ 2026-08-26 يجيب اثنَين منها نصًّا —
 *   · `DEC-OPEN-16` بالبنود 36–45 والقاعدةِ الحاكمةِ في ختامِه
 *   · `DEC-OPEN-18` بالبند 2
 * فيُستبدَل النصُّ بنصِّ المالكِ ويُستبدَل المرجعُ بمرجعٍ **يسمّي الجواب**.
 *
 * ⛔ **ولا يُثبَّت هنا ما لم يُجب عنه الأمر**: `DEC-OPEN-03` (‏تعدّدُ الكيانات)
 *   و`DEC-OPEN-12` (‏تعريفُ العطلِ الحرِج) يبقيان مرفوعَين للمالكِ كما هما —
 *   ولا يُصحَّح مرجعُ ما لا جوابَ له.
 *
 * ⛔ **ولا يُبنى شيءٌ هنا** — تصحيحُ قيدِ القرارِ ومرجعِه لا الشيفرة. وأثرُ
 *   الجوابِ على البناءِ نطاقُ W13.5 وW14.
 *
 * التشغيل: php tools/repair01_owner_order_record.php [--report]
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED);
mb_internal_encoding('UTF-8');
define('EMS_CLI', true);

$ROOT = dirname(__DIR__);
require_once $ROOT . '/includes/session_bootstrap.php';
require_once $ROOT . '/config.php';
while (ob_get_level()) { ob_end_clean(); }
$conn = $GLOBALS['conn'];
$conn->set_charset('utf8mb4');
$REPORT = in_array('--report', $argv, true);
$e = function ($s) use ($conn) { return $conn->real_escape_string((string) $s); };

/* ═══ ① الدليلُ شرطُ الكتابة ═══════════════════════════════════════════════
     ⛔ ولا يُثبَّت قرارُ مالكٍ بلا ملفِّ أمرِه على القرص — وهذا هو الحارسُ الذي
     غيابُه سمح بأن يُكتب قرارٌ نيابةً عنه. */
$docx = $ROOT . '/docs/REPAIR01_20260823/owner/OWNER_ORDER_20260826_W13_5.docx';
$md   = $ROOT . '/docs/REPAIR01_20260823/owner/OWNER_ORDER_20260826_W13_5.md';
if (!is_file($docx) || !is_file($md)) { exit("✘ لا ملفَّ أمرٍ على القرص — ولا يُثبَّت قرارٌ بلا دليلِه.\n"); }
$sha = substr(hash_file('sha256', $docx), 0, 16);
$BASE = 'امر المالك المكتوب 2026-08-26 (الاجابة عن الحوكمة.docx) '
      . '> docs/REPAIR01_20260823/owner/OWNER_ORDER_20260826_W13_5.md > sha256:' . $sha . ' > ';

/* ═══ ② النصوصُ — منقولةٌ من بنودِ الأمرِ لا مصوغةٌ عنه ══════════════════════ */
$R16 =
  'التحقيق ثلاثة انواع بثلاثة ملاك لا يخلطون. '
. 'اولا التحقيق التاديبي: تملكه الموارد البشرية DEP-07 في الغياب والسلوك الوظيفي '
. 'والتقصير ومخالفة واجبات العمل والانضباط، ثم قرار الجزاء حسب Policy و AAM و Law. '
. 'ثانيا تحقيق النزاهة والامتثال: تملكه الحوكمة والالتزام DEP-08 في الاحتيال والسرقة '
. 'والاختلاس والرشوة والتزوير والتلاعب وتضارب المصالح وتجاوز السلطة وخرق AAM وخرق '
. 'فصل الواجبات وخرق الامتثال. واذا كان موظف طرفا فالموارد البشرية تستقبل نتيجة '
. 'التحقيق للاثر التاديبي ولا تعيد التحقيق نفسه. '
. 'ثالثا تقصي الوقائع التشغيلي ليس تحقيق حوكمة: لماذا تعطلت المعدة او تاخر النقل او '
. 'نقص الانتاج او تلفت القطعة تملكه الادارة التشغيلية المختصة، فان كشف سوء سلوك احال '
. 'الى الموارد البشرية، وان كشف خرق نزاهة او ضبط احال الى الحوكمة، وان كشف مؤشر خطر '
. 'احال الى المخاطر. '
. 'والمراجعة الداخلية لا يكون لها طابور تحقيق يومي كجزء اصيل من عملها. يجوز تكليفها '
. 'بتحقيق مستقل خاص بتكليف مكتوب Written Mandate فقط عند وجود تضارب داخل الحوكمة او '
. 'قضية تخص تنفيذيا كبيرا او مسالة محجوزة او عدم وجود جهة داخلية مستقلة او قرار صريح '
. 'من المالك او السلطة المحجوزة. '
. 'وفحص التضارب قبل اي تحقيق: لا يحقق المتهم ولا صاحب المصلحة ولا صاحب القرار محل '
. 'الشبهة ولا الجهة التي بينها وبين النتيجة تضارب جوهري، في نفسها. ويجب ان يدعم '
. 'النظام التنحي Recusal. '
. 'والمحاولة الممنوعة ليست تحقيقا تلقائيا: المحاولة تسجل Log، والتكرار او المحاولة '
. 'الحرجة ترفع Governance Alert، ثم Triage، ثم تحقيق ان استدعى الامر. '
. 'والمراجعة الداخلية ليست تحت ادارة الحوكمة: هي خط ثالث مستقل، والحوكمة لا تعطي '
. 'المراجع نطاق المراجعة ولا تغير Finding ولا تغلقه نيابة عنه ولا تمنع تقريرا ولا '
. 'تعيد Rating. والحوكمة والالتزام وظيفة خط ثان تملك الاطار والسياسات والتفويضات '
. 'والاستثناءات ومراقبة الالتزام وحالات خرق الضوابط وتحقيقات النزاهة والامتثال، '
. 'بينما المراجعة الداخلية تقدم تاكيدا مستقلا للمالك على كفاية وفعالية الحوكمة وادارة '
. 'المخاطر والرقابة بما في ذلك فعالية ادارة الحوكمة والالتزام نفسها.';

$R18 =
  'المقام الحاكم سبع عشرة ادارة مرقمة DEP-01..DEP-17. وخارج هذا التسلسل: القيادة '
. 'التنفيذية Executive Workspace ومساحة عملي Personal Workspace والمراجعة الداخلية '
. 'Independent Assurance Function وفريق المنجم Operational Team تحت ادارة الموقع. '
. 'ولا تعتبر اي واحدة منها ادارة رقم 18 او 19. وتفصل المالية والخزينة الى نطاقين '
. 'مستقلين DEP-05 و DEP-06. ويحتفظ بالمعرفات التقنية القديمة عبر Crosswalk، ولا '
. 'اعادة ترقيم مدمرة للمفاتيح التاريخية. واي Registry او تقرير او Rule ما زال مبنيا '
. 'على 19 او 20 وحدة يجب ان يصالح مع هذا الهيكل.';

$PAIRS = array(
    array('DEC-OPEN-16', $R16, $BASE . 'البنود 36-45 والقاعدة الحاكمة'),
    array('DEC-OPEN-18', $R18, $BASE . 'البند 2'),
);

echo "══ تثبيتُ أجوبةِ المالكِ من أمرِه المكتوبِ 2026-08-26 ══\n";
echo ($REPORT ? "  وضعُ التقرير: يقرأ ولا يكتب\n\n" : "\n");

/* ═══ ③ الفرقُ يُطبَع قبلَ الكتابةِ لا بعدَها ═══════════════════════════════ */
foreach ($PAIRS as $p) {
    $r = $conn->query("SELECT owner_decision, src_ref FROM repair01_decisions WHERE decision_id = '" . $e($p[0]) . "'");
    if (!$r || !($old = $r->fetch_assoc())) { echo "✘ لا صفَّ لـ" . $p[0] . "\n"; exit(1); }
    printf("── %s ──\n", $p[0]);
    printf("   قبل : %4d حرفًا · %s\n", mb_strlen((string) $old['owner_decision']), $old['src_ref']);
    printf("   بعد : %4d حرفًا · %s\n\n", mb_strlen($p[1]), $p[2]);
}
if ($REPORT) { echo "لم يُكتب شيء.\n"; exit(0); }

/* ═══ ④ الكتابة ═══════════════════════════════════════════════════════════ */
foreach ($PAIRS as $p) {
    $ok = $conn->query("UPDATE repair01_decisions
                           SET owner_decision = '" . $e($p[1]) . "',
                               src_ref        = '" . $e($p[2]) . "',
                               approved_by    = 'المالك — امر مكتوب',
                               approved_at    = '2026-08-26',
                               status         = 'APPROVED',
                               blocking_level = 'NONE'
                         WHERE decision_id = '" . $e($p[0]) . "'");
    if (!$ok) { echo '✘ ' . $p[0] . ': ' . $conn->error . "\n"; exit(1); }
}

/* ═══ ⑤ الإثباتُ بإعادةِ القراءةِ لا بافتراضِ النجاح ═══════════════════════ */
foreach ($PAIRS as $p) {
    $r = $conn->query("SELECT LENGTH(owner_decision) L, approved_by, approved_at, src_ref
                         FROM repair01_decisions WHERE decision_id = '" . $e($p[0]) . "'");
    $n = $r->fetch_assoc();
    $okRef = (strpos((string) $n['src_ref'], 'sha256:') !== false);
    printf("%s %-14s %s بايت · اعتمده %s · %s · مرجعٌ بدليلٍ %s\n",
        ($okRef ? '✔' : '✘'), $p[0], $n['L'], $n['approved_by'], $n['approved_at'], ($okRef ? 'نعم' : 'لا'));
}
echo "\nالخطوةُ التالية: php tools/repair01_owner_decision_audit.php\n";
