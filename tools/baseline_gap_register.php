<?php
/**
 * tools/baseline_gap_register.php — كتابةُ فجواتِ لقطةِ الأساسِ في الدفترِ الرسميّ
 * ═══════════════════════════════════════════════════════════════════════════
 * **الدرسُ المحروقُ نفسُه للمرّةِ الثالثة** (`gap-codes-register-not-prose`):
 * «**فجوةٌ في السردِ لم تُكتب في الدفترِ تُيتِّم كشوفَها**». وقد وقع الآن على
 * لقطةِ `BL-20260902`: جدولُ الفجواتِ في `INJ-ARCH-ASBUILT_ar.md` سكَّ
 * `GAP-76..81` **ولم يُكتب منها واحدٌ في الدفتر** (الدفترُ يقف عند `GAP-75`)
 * ⇒ رسَب `FR-GOV-006` («صفرُ كشفٍ مُهمَلٍ لغيابِ رمزٍ يقابله مطلب») **وحُجب
 * كلُّ التزام**.
 *
 * ◆ **والقياسُ سمّى المدى ولم يظنَّه**: خمسةُ كشوفٍ يتيمةٌ لا واحد —
 *   `F-H16⇐GAP-77` (ظاهرٌ للفاحص) و`F-H05⇐GAP-78` · `F-H08⇐GAP-79` ·
 *   `F-H14/F-H15⇐GAP-80` · `F-H07⇐GAP-81` (**كانت تمرُّ صامتةً** لأنّ خانتَها
 *   مكتوبةٌ بخطٍّ عريضٍ `**GAP-80**` والفاحصُ يطابق الرمزَ حرفًا بلا تجريد).
 *   **فالخضرةُ كانت عمى قارئٍ لا سلامةَ سجلّ.**
 *
 * ◆ **وسادسٌ بلا رمزٍ أصلًا**: `F-H03` خانتُه كلمةُ «**جديد**» — يُعلن فجوةً
 *   ولا يسكُّ لها رمزًا، فتسقط من العدِّ صامتةً في الحالتَين. سُكَّ لها
 *   `GAP-82` في جدولِ الفجواتِ **بنصِّ الكشفِ نفسِه** ووُصلت خانتُه.
 *
 * ⛔ **ولا يُصلَح هذا بتحريرِ الكشوفِ لتمرَّ البوّابة**: الكشفُ دليلٌ مقيسٌ
 *   والدليلُ لا يُعدَّل ليُرضيَ فاحصًا. **بل يُكتب المطلبُ الذي تطالب به الفجوة.**
 *
 * ◆ **والمضمونُ منقولٌ من جدولِ الفجواتِ ومن `FINDINGS.md` لا مؤلَّفٌ هنا** —
 *   ويُذكر موضعُه في `المصدرُ الحاكم`. **وحالتُها `OPEN`** ⛔ فلا يُعلَن إغلاقٌ
 *   بلا شاهدٍ في الحزام.
 *
 * التشغيل:
 *   php tools/baseline_gap_register.php           ← يعرض ما سيُكتب ولا يكتب
 *   php tools/baseline_gap_register.php --apply   ← يُلحق الصفوفَ بالدفتر
 * ═══════════════════════════════════════════════════════════════════════════
 */
if (php_sapi_name() !== 'cli') { exit("CLI فقط\n"); }
error_reporting(E_ALL & ~E_DEPRECATED);
mb_internal_encoding('UTF-8');
$ROOT  = str_replace(DIRECTORY_SEPARATOR, '/', dirname(__DIR__));
require_once $ROOT . '/tools/lib/xlsx_io.php';
$XLSX  = $ROOT . '/docs/sources/INJ-FRD-REM-01/workbook.xlsx';
$APPLY = in_array('--apply', $argv, true);
$SHEET = 'سجل المتطلبات';
$SRC   = 'لقطةُ الأساسِ BL-20260902 · INJ-ARCH-ASBUILT_ar.md §جدولُ الفجوات';

if (!is_file($XLSX)) { exit("⛔ الدفترُ الرسميُّ مفقود\n"); }

/* ═══ ما يُكتب — منقولًا من جدولِ الفجواتِ وسجلِّ الكشوف ══════════════════ */
$SPEC = array(
    'FR-DAT-007' => array(
        'gap'     => 'GAP-77',
        'dom'     => 'البيانات',
        'type'    => 'DATA_INTEGRITY',
        'now'     => 'عمودُ مفتاحِ المستهلكِ ملوَّثٌ بنصٍّ بشريّ: `ems_event_deliveries.consumer_key` '
                   . 'فيه **٨ مفاتيحَ من ١٢ متمايزًا** نصوصٌ عربيّةٌ من بذرِ `UAT-2026` في **١٠ صفوفٍ من ٢١٬٤٢٨**',
        'want'    => 'مفتاحُ المستهلكِ **معرِّفٌ تقنيٌّ لا نصٌّ بشريّ** — والقاعدةُ تشمل **كلَّ عمودِ مفتاحٍ '
                   . 'في الناقل** لا الأعمدةَ التي فحصها `GAP-09` وحدَها. **وإغلاقُ `GAP-09` صادقٌ على مقامِه '
                   . 'وأعمى عن هذا العمود** — فالمقامُ يُوسَّع ولا يُنقض الإغلاق',
        'trigger' => 'كتابةُ صفِّ تسليمٍ في `ems_event_deliveries`',
        'input'   => 'قيمةُ `consumer_key` المرشَّحة',
        'proc'    => 'رفضُ القيمةِ التي تحمل حروفًا عربيّةً أو مسافاتٍ أو وسمَ بذرٍ قبلَ الكتابة',
        'out'     => 'عمودُ مفتاحٍ بمعرِّفاتٍ تقنيّةٍ وحدَها',
        'fail'    => 'مفتاحٌ بنصٍّ بشريٍّ يُرصد ويُسمّى ويُرفض — ولا يُبتلع صامتًا',
        'tables'  => 'ems_event_deliveries.consumer_key · event_consumers',
        'pos'     => 'عدٌّ على المفاتيحِ المتمايزة ⇒ صفرُ مفتاحٍ بحرفٍ عربيٍّ أو وسمِ `UAT-`',
        'neg'     => 'حقنُ صفِّ تسليمٍ بمفتاحٍ عربيٍّ ← يُرفض ولا يُكتب',
        'accept'  => 'صفٌّ بنصٍّ بشريٍّ في `ems_event_deliveries.consumer_key` = صفر '
                   . '(‏المقيسُ عندَ الكشف: **٨ من ١٢ مفتاحًا · ١٠ صفوف**)',
        'ev'      => 'FINDINGS.md `F-H16` · جدولُ الفجوات `GAP-77`',
        'prio'    => 'P2',
    ),
    'FR-GOV-017' => array(
        'gap'     => 'GAP-78',
        'dom'     => 'الحوكمة',
        'type'    => 'ARCHITECTURE_CONTROL',
        'now'     => 'إصدارُ مكوّناتِ الواجهةِ متقادمٌ اثنَي عشرَ يومًا: صفُّ `DRAFT` هو `ux-1.6.0` '
                   . 'بتاريخِ **2026-08-21 17:15** بخمسةِ ملفاتٍ، وبصمتُه المسجَّلةُ `4e73fa5d…` لا تطابق '
                   . 'المحسوبةَ `4099c942…` — **والبصمتان ثابتتان عبر ثلاثِ لقطات**',
        'want'    => 'بصمةُ الإصدارِ المسجَّلةُ تطابق المحسوبةَ على ملفاتِه المُعلَنة — **وإلّا أُعيد إصدارُه**. '
                   . '**والعطبُ تقادمُ تسجيلٍ لا اتّساخُ شجرة**: الفاحصُ يحسب على قائمةِ '
                   . '`gov_component_versions` لا على شجرةِ Git، فالشجرةُ النظيفةُ لا تُبرّئ',
        'trigger' => 'تغيُّرُ ملفٍّ من ملفاتِ إصدارٍ مُعلَن',
        'input'   => 'قائمةُ ملفاتِ الإصدارِ وبصمتُه المسجَّلة',
        'proc'    => 'إعادةُ حسابِ البصمةِ ومقارنتُها بالمسجَّلة',
        'out'     => 'إصدارٌ ببصمةٍ مطابقةٍ أو مطالبةٌ بإعادةِ إصدار',
        'fail'    => 'فرقُ بصمةٍ يُسمّى بملفِّه وبتاريخِ الإصدارِ — ولا يُنسب إلى اتّساخِ الشجرةِ ظنًّا',
        'tables'  => 'gov_component_versions',
        'pos'     => 'البصمةُ المسجَّلةُ = المحسوبةُ على الملفاتِ الخمسة',
        'neg'     => 'تغييرُ ملفٍّ من الخمسةِ بلا إعادةِ إصدارٍ ← يُرصد ويُسمّى',
        'accept'  => 'إصدارٌ مُعلَنٌ ببصمةٍ لا تطابق ملفاتِه = صفر '
                   . '(‏المقيسُ عندَ الكشف: `ux-1.6.0` · فرقٌ ثابتٌ عبر ثلاثِ لقطات)',
        'ev'      => 'FINDINGS.md `F-H05` · جدولُ الفجوات `GAP-78`',
        'prio'    => 'P2',
    ),
    'FR-GOV-018' => array(
        'gap'     => 'GAP-79',
        'dom'     => 'الحوكمة',
        'type'    => 'ARCHITECTURE_CONTROL',
        'now'     => 'المعلَّقُ في شاهدِ نهائيةِ الإصدارِ والأفعالِ تضاعف: **٨٢ ⇒ ١٦٣** بندًا '
                   . '— **ازديادٌ حقيقيٌّ لا عطبَ مقياس**',
        'want'    => 'بندٌ معلَّقٌ له **مالكٌ وتاريخُ مراجعةٍ**، والمعلَّقُ **لا ينمو بلا سقّاطةٍ تقيسه** '
                   . '— فالنموُّ المقيسُ قرارٌ مؤجَّلٌ مرارًا لا حالةٌ محايدة',
        'trigger' => 'قياسُ نهائيةِ الإصدارِ والأفعال',
        'input'   => 'عددُ البنودِ المعلَّقةِ في الشاهد',
        'proc'    => 'مقارنةُ العددِ بخطِّ أساسٍ مسجَّلٍ ورفضُ الازدياد',
        'out'     => 'عددٌ معلَّقٌ ثابتٌ أو نازلٌ بسقّاطة',
        'fail'    => 'ازديادُ المعلَّقِ يُسمّى برقمِه وخطِّ أساسِه — ولا يمرُّ بوصفِه نموًّا طبيعيًّا',
        'tables'  => 'tests/injfix01_release_and_action_finality_proof.php',
        'pos'     => 'المعلَّقُ ≤ خطِّ الأساسِ المسجَّل',
        'neg'     => 'إضافةُ بندٍ معلَّقٍ جديدٍ ← ترفعُ العدَّ فوقَ الأساسِ فيُرصد',
        'accept'  => 'معلَّقٌ يتجاوز خطَّ أساسِه = صفر (‏المقيسُ عندَ الكشف: **٨٢ ⇒ ١٦٣**)',
        'ev'      => 'FINDINGS.md `F-H08` · جدولُ الفجوات `GAP-79`',
        'prio'    => 'P2',
    ),
    'FR-GOV-019' => array(
        'gap'     => 'GAP-80',
        'dom'     => 'الحوكمة',
        'type'    => 'ARCHITECTURE_CONTROL',
        'now'     => 'الأرشفةُ المُعلَنةُ تجاوزت الأرشيفَ الفعليّ: اللقطةُ السابقةُ أعلنت **تسعَ لقطاتٍ** '
                   . 'آخرُها `BL-20260831d-aba54573` **والمجلَّدُ يحوي ثمانيًا بلا أثرٍ لـ`b/c/d`** '
                   . '⇒ **ثلاثُ لقطاتٍ أُعلنت ولا تُستعاد**. **ورأسُ اللقطةِ نفسِه بروايتَين**: '
                   . '`SNAPSHOT.md` يقول 2026-09-01 · 02:35→02:45 و`INJ-ARCH-ASBUILT`/`INJ-CURRENT-STATUS` '
                   . 'يقولان 2026-08-31 · 20:40→20:50 لنفسِ النافذة',
        'want'    => 'كلُّ رقمٍ يُعلَن مؤرشَفًا **يُستعاد من مجلَّدِه** — والعدُّ المُعلَنُ يُقاس على القرصِ '
                   . 'لا يُكتب سردًا. **ونافذةُ القياسِ روايةٌ واحدةٌ في الرؤوسِ الثلاثةِ جميعًا**',
        'trigger' => 'إصدارُ لقطةِ أساسٍ وأرشفةُ سابقتِها',
        'input'   => 'عددُ اللقطاتِ المُعلَنِ ومحتوى `historical/`',
        'proc'    => 'مطابقةُ المُعلَنِ بالمقيسِ على القرصِ ومطابقةُ نافذةِ القياسِ في الرؤوسِ الثلاثة',
        'out'     => 'عدٌّ مُعلَنٌ = عدٌّ مؤرشَفٌ · ونافذةٌ واحدةٌ في الرؤوسِ الثلاثة',
        'fail'    => 'فرقٌ بين المُعلَنِ والمؤرشَفِ يُسمّى بمعرِّفاتِ اللقطاتِ الناقصة',
        'tables'  => 'docs/baseline_20260821/historical/ · SNAPSHOT.md · INJ-ARCH-ASBUILT_ar.md · INJ-CURRENT-STATUS_ar.md',
        'pos'     => '`ls historical/` = العددُ المُعلَنُ في نصِّ الوثيقة · والرؤوسُ الثلاثةُ بنافذةٍ واحدة',
        'neg'     => 'إعلانُ لقطةٍ بلا مجلَّدٍ لها ← يُرصد ويُسمّى',
        'accept'  => 'لقطةٌ مُعلَنةٌ بلا مجلَّدٍ يُستعاد منه = صفر · ونافذةُ قياسٍ بروايتَين = صفر '
                   . '(‏المقيسُ عندَ الكشف: **ثلاثُ لقطاتٍ `b/c/d`** · **رأسان بتاريخَين**)',
        'ev'      => 'FINDINGS.md `F-H14` · `F-H15` · جدولُ الفجوات `GAP-80`',
        'prio'    => 'P2',
    ),
    'FR-GOV-020' => array(
        'gap'     => 'GAP-81',
        'dom'     => 'الحوكمة',
        'type'    => 'ARCHITECTURE_CONTROL',
        'now'     => 'مستخرِجُ الحقولِ تخلّف عن النموّ: **٨٥ شاشةً جديدةً بلا حقولٍ مستخرَجة** '
                   . 'ومحاذاةُ الجداولِ **٤٢٧ ⇒ ٣٤٧** بينما `needs_review` **٣٥٣ ⇒ ٤٧٩**، '
                   . 'والاسمُ التقنيُّ محلولٌ في **٨٬٧٥٧ من ١٦٬١٣٦ (٥٤٫٣٪)**',
        'want'    => 'مقامُ الاستخراجِ **يتبع نموَّ الأسطح**: شاشةٌ تُسجَّل ⇒ حقولُها تُستخرَج. '
                   . '**والنقصُ عجزُ مقياسٍ لا عجزُ منتَج** — فيُقرأ نقصًا في القياسِ ولا يُنسب إلى الشاشات',
        'trigger' => 'تسجيلُ سطحٍ جديدٍ في السجلِّ الرسميّ',
        'input'   => 'قائمةُ الأسطحِ المسجَّلةِ ومخرَجُ المستخرِج',
        'proc'    => 'مقارنةُ مقامِ الأسطحِ بمقامِ الشاشاتِ ذاتِ الحقولِ المستخرَجة',
        'out'     => 'صفرُ شاشةٍ مسجَّلةٍ بلا حقولٍ مستخرَجة',
        'fail'    => 'شاشةٌ بلا حقولٍ تُسمّى بمسارِها — ولا يُخصم النقصُ من نسبةِ المنتَج',
        'tables'  => 'repair01_screen_registry · سجلُّ الحقول · gov_field_class',
        'pos'     => 'شاشاتٌ مسجَّلةٌ بلا حقولٍ مستخرَجة = صفر',
        'neg'     => 'تسجيلُ سطحٍ بلا تشغيلِ المستخرِجِ ← يُرصد ويُسمّى',
        'accept'  => 'شاشةٌ مسجَّلةٌ بلا حقولٍ مستخرَجة = صفر '
                   . '(‏المقيسُ عندَ الكشف: **٨٥ شاشةً** · محاذاةٌ ٣٤٧ · `needs_review` ٤٧٩)',
        'ev'      => 'FINDINGS.md `F-H07` · جدولُ الفجوات `GAP-81`',
        'prio'    => 'P3',
    ),
    'FR-EVT-009' => array(
        'gap'     => 'GAP-82',
        'dom'     => 'الأحداث',
        'type'    => 'ARCHITECTURE_CONTROL',
        'now'     => 'مصدران للـDLQ يتناقضان: `ems_event_dead_letter` **صفرُ صفٍّ** بينما '
                   . '**٣٧ صفَّ تسليمٍ حالتُه `dlq`** (٢٥ `effectfanout` · ١١ `effectlink` · '
                   . '١ `balancecalculator`) ⇒ **«DLQ صفر» صادقةٌ على الجدولِ كاذبةٌ على الحالة**',
        'want'    => 'سؤالُ «كم رسالةً ماتت؟» **مصدرُ حقيقةٍ واحد**: إمّا يُملأ جدولُ الرسائلِ الميّتةِ '
                   . 'من حالةِ التسليم، وإمّا يُعلَن الجدولُ مهجورًا ويُقرأ العدُّ من `ems_event_deliveries.status`. '
                   . '**والقارئُ الذي يعُدُّ من الجدولِ وحدَه يظنُّ الناقلَ نظيفًا**',
        'trigger' => 'انتقالُ صفِّ تسليمٍ إلى الحالةِ `dlq`',
        'input'   => 'صفُّ التسليمِ وحالتُه',
        'proc'    => 'كتابةُ الرسالةِ الميّتةِ في مصدرِ الحقيقةِ المُعلَنِ وحدَه',
        'out'     => 'عدٌّ واحدٌ للرسائلِ الميّتةِ من مصدرٍ واحد',
        'fail'    => 'فرقُ العدَّين يُسمّى برقمَيه ومصدرَيه — ولا يُقرأ أحدُهما وحدَه',
        'tables'  => 'ems_event_dead_letter · ems_event_deliveries.status',
        'pos'     => 'عدُّ `dlq` من الجدولِ = عدُّه من حالةِ التسليم · أو الجدولُ مُعلَنٌ مهجورًا بقرارٍ مكتوب',
        'neg'     => 'صفُّ تسليمٍ يصير `dlq` بلا نظيرٍ في مصدرِ الحقيقةِ ← يُرصد',
        'accept'  => 'فرقُ عدِّ الرسائلِ الميّتةِ بين المصدرَين = صفر — أو **قرارُ هجرٍ مكتوبٌ لأحدِهما** '
                   . '(‏المقيسُ عندَ الكشف: **٠ مقابل ٣٧**)',
        'ev'      => 'FINDINGS.md `F-H03` · جدولُ الفجوات `GAP-82`',
        'prio'    => 'P2',
    ),
);

/* ═══ القراءةُ قبلَ الكتابة — ⛔ ولا يُكتب على مجهول ══════════════════════ */
$wb  = xlsx_read($XLSX);
if (!isset($wb[$SHEET])) { exit("⛔ ورقةُ «{$SHEET}» مفقودة\n"); }
$wr  = $wb[$SHEET];
$hdr = $wr[3];
$ix  = array();
foreach ($hdr as $i => $h) { $ix[trim(str_replace('◆ ', '', (string) $h))] = $i; }

$haveIds = array(); $haveGaps = array(); $maxNum = 0;
foreach ($wr as $i => $r) {
    if ($i < 4) { continue; }
    $id = trim((string) ($r[$ix['المعرِّف']] ?? ''));
    if (!preg_match('~^[A-Z]{2,4}-[A-Z]{2,4}-\d{3}$~', $id)) { continue; }
    $haveIds[$id] = 1;
    $maxNum = max($maxNum, (int) trim((string) ($r[$ix['#']] ?? 0)));
    foreach (preg_split('~[\s·]+~u', trim((string) ($r[$ix['الفجوة']] ?? ''))) as $g) {
        $g = trim($g, "* `\t\n\r");
        if (preg_match('~^GAP-?(\d+)$~u', $g, $gm)) { $haveGaps['GAP-' . $gm[1]] = $id; }
    }
}
printf("الدفترُ الآن: **%d** معرِّفًا · أقصى ترقيمٍ **%d** · **%d** رمزَ فجوةٍ مربوطًا\n",
    count($haveIds), $maxNum, count($haveGaps));

$rows = array(); $skip = array(); $num = $maxNum;
foreach ($SPEC as $id => $s) {
    if (isset($haveIds[$id]))        { $skip[] = "$id — المعرِّفُ مسجَّلٌ سلفًا"; continue; }
    if (isset($haveGaps[$s['gap']])) { $skip[] = "{$s['gap']} — مربوطٌ سلفًا بـ{$haveGaps[$s['gap']]}"; continue; }
    $num++;
    $c = array();
    $c[$ix['#']]                     = (string) $num;
    $c[$ix['المعرِّف']]               = $id;
    $c[$ix['الفجوة']]                = $s['gap'];
    $c[$ix['المجال']]                = $s['dom'];
    $c[$ix['المصدرُ الحاكم']]         = $SRC . ' · ' . $s['ev'];
    $c[$ix['الحالُ الآن']]            = $s['now'];
    $c[$ix['السلوكُ المطلوب']]        = $s['want'];
    $c[$ix['المُطلِق']]               = $s['trigger'];
    $c[$ix['الفاعل']]                = 'النظام';
    $c[$ix['الشروطُ السابقة']]        = 'الكشفُ مسجَّلٌ في `FINDINGS.md` والفجوةُ مسكوكةٌ في جدولِ الفجوات';
    $c[$ix['المُدخَل']]               = $s['input'];
    $c[$ix['المعالجة']]              = $s['proc'];
    $c[$ix['المُخرَج']]               = $s['out'];
    $c[$ix['انتقالُ الحالة']]         = '—';
    $c[$ix['قاعدةُ الصلاحية']]        = '—';
    $c[$ix['سلوكُ الفشل']]            = $s['fail'];
    $c[$ix['واقعةُ السجل']]           = 'سجلُّ الكشوفِ `docs/baseline_20260821/FINDINGS.md`';
    $c[$ix['الجداولُ والحقول']]       = $s['tables'];
    $c[$ix['الخدماتُ والأحداث']]      = '—';
    $c[$ix['اختبارٌ موجب']]           = $s['pos'];
    $c[$ix['اختبارٌ سالب']]           = $s['neg'];
    $c[$ix['معيارُ القبول']]          = $s['accept'];
    $c[$ix['الدليل']]                = 'FINDINGS.md · ' . $s['ev'];
    $c[$ix['التبعيات']]              = '—';
    $c[$ix['الأولوية']]              = $s['prio'];
    $c[$ix['الحالة']]                = 'مُعلَن';
    $c[$ix['Requirement_Type']]      = $s['type'];
    $c[$ix['Atomicity_Level']]       = 'ATOMIC';
    $c[$ix['Parent_Requirement_ID']] = '—';
    $c[$ix['Change_Set_ID']]         = 'CHG-BL-20260902';
    $c[$ix['Test_Applicability']]    = 'موجب=YES · سالب=YES';
    $c[$ix['N/A_Reason']]            = '—';
    $c[$ix['Threshold_Source']]      = 'المقيسُ عندَ الكشفِ في `FINDINGS.md` — ولا عتبةَ مؤلَّفةً هنا';
    $c[$ix['Legacy_Alias']]          = '—';
    /* ⛔ **ولا يُعلَن إغلاقٌ هنا**: الإغلاقُ بشاهدٍ في الحزامِ لا بصفٍّ يُكتب */
    $c[$ix['Closure_State']]         = 'OPEN';
    $rows[] = $c;
    printf("  + %s ⇐ %s · %s\n", $id, $s['gap'], mb_substr($s['now'], 0, 58));
}
foreach ($skip as $x) { echo "  ⟳ تُخطّى: $x\n"; }

if (!$rows) { echo "\nلا جديدَ يُكتب.\n"; exit(0); }
if (!$APPLY) { echo "\nقياسٌ فقط — أعِد التشغيلَ بـ`--apply` للكتابة.\n"; exit(0); }

$n = xlsx_append_rows($XLSX, $SHEET, $rows);
printf("\n✔ أُلحق **%d** صفًّا بـ`%s`\n", $n, $SHEET);

/* ═══ التحقّقُ بإعادةِ القراءةِ — ⛔ ولا يُصدَّق الكاتبُ على كلمتِه ════════ */
$wb2 = xlsx_read($XLSX);
$wr2 = $wb2[$SHEET];
$ok = 0;
foreach ($wr2 as $i => $r) {
    if ($i < 4) { continue; }
    $id = trim((string) ($r[$ix['المعرِّف']] ?? ''));
    if (isset($SPEC[$id])) {
        $g = trim((string) ($r[$ix['الفجوة']] ?? ''));
        if ($g === $SPEC[$id]['gap']) { $ok++; }
    }
}
printf("✔ أُعيدت القراءةُ: **%d من %d** مطلبًا حاضرٌ برمزِ فجوتِه\n", $ok, count($SPEC));
exit($ok === count($SPEC) ? 0 : 1);
