# تقريرُ مطابقةِ التنفيذ للوثائق الستَّ عشرة — 2026-07-27

**المنهج:** قراءةُ المستندات الستةَ عشر من `docs/update0001` بندًا بندًا، ومقابلتُها
بالحالة الحيّة **بالقياس لا بالذاكرة** (استعلامٌ مباشرٌ على القاعدة + قراءةُ الكود
+ تشغيلُ حزم الاختبار). كلُّ «✅» في هذا التقرير له برهانٌ مذكورُ الموضع.

**المقيس لحظةَ الكتابة:** 21 حزمة · **577 تأكيدًا · صفر فاشل** · 135 وحدة ·
419 صفَّ سايدبار لـ23 دورًا · شجرةُ عملٍ نظيفة · 28 التزامًا غيرَ مدفوع.

---

## 0 · الخلاصة التنفيذية

| المحور | الحال |
|---|---|
| **الشرط المعماري (UX-02 §9) — البوابات الأربع** | ✅ **مغلقةٌ كلُّها** |
| البنية التحتية (الناشر · العزل · السايدبار · اللوحات) | ✅ مكتملة |
| **مصدرُ حقيقة الوحدات (§9-③)** | ✅ منفَّذ حتى **قلب الكاتب** |
| شاشاتُ التشغيل (UX-03 §5) | 🟡 واحدةٌ من ثلاث |
| المالية (UX-02 التصحيحان) | ✅ منفَّذان · 🟡 مع انحرافٍ بنيويٍّ واحدٍ معلَن |
| الإداراتُ السبع (UX-04…UX-10) | ⬜ لم تبدأ — كانت محجوزةً بسجل الزمن الموحّد، **وقد قام** |

> **الحكم الحاكم:** العمودُ الفقري الذي كانت كلُّ الوثائق تحيل إليه — «سجلُّ الزمن
> الموحّد» و«الحقول الثلاثة منفصلة» و«أحكامُ الأطراف» و«لا مالَ إلا بالناشر» —
> **قائمٌ وحيٌّ ومختبَر**. والمتبقي في معظمه **واجهاتٌ وأطرافُ إداراتٍ تُبنى فوقه**،
> لا إعادةَ هندسةٍ للأساس.

---

## 1 · الشرط المعماري — UX-02 §9 (بوابات لا يبدأ عملُ واجهةٍ قبلها)

| # | نصُّ الوثيقة | الحال | البرهان |
|---|---|---|---|
| ① | سدُّ مسار الكتابة المباشرة — كلُّ معاملةٍ عبر الناشر | ✅ | `import_channel_test` 9/9 · الدفتر صفر بلا جذر |
| ② | معالجةُ الصفوف العشرة اليتيمة رجعيًّا بمراجعها | ✅ | رُدمت 10/10 بأوامر شرائها الحقيقية |
| ③ | **مصدرُ حقيقة الوحدات** | ✅ | §2 أدناه |
| ④ | توحيدُ مصدر السايدبار | ✅ | `unified_nav_test` 14/14 · 419 صفًّا لـ23 دورًا |

---

## 2 · UX-03 التشغيل — الوثيقة الأثقل

### 2.1 · §8 المواصفة التنفيذية — ✅ منفَّذةٌ كاملةً

| البند | النصّ | الحال | البرهان |
|---|---|---|---|
| §8.1 | «جداولُ السجل القانوني تصير **مصدرَ الكتابة الوحيد** عبر خدمة الإدخال» | ✅ | `EMS_TS_WRITER=service` حيّ · `writer_flip_test` 18/18 |
| §8.1 | «محرّكُ التحويل **يُعاد استخدامه كما هو**» | ✅ | `EffectFanout` لم يُعَد بناؤه — وُصل بعلَم `EMS_UNIT_FANOUT_SOURCE=legal` |
| §8.2 | آلةُ الحالات بمالكِ كل انتقالٍ وشرطِه | ✅ | `TimesheetEntryService::approve` · `state_machine_test` 17/17 |
| §8.2 | **مفتاحُ العطالة** (معدة × تاريخ × وردية) | ✅ | يرفض التكرارَ بمرجعه — برهانُ «صفر صفٍّ كُتب» |
| §8.2 | الإعادة **بالرقم نفسه** | ✅ | `round_no` (ترحيل) + `returnToSite` + `resubmit` |
| §8.2 | اعتمادُ الموقع = **قفلُ نسخة** | ✅ | 423 في الشاشة **وفي طلب التعديل القديم** معًا |
| §8.3 | `TimesheetEntryService` بمدخلاتِه الستة و`UnitDTO` وتحققِه | ✅ | `timesheet_entry_service_test` 50/50 |
| §8.3 | الأخطاء 422 · 409 · 423 | ✅ | مقيسةٌ في الحزمتين |
| §8.4 | خمسُ حالات اختبار (T1…T5) | ✅ | الخمسُ في الحزمة |
| §8.4 | الترحيل بموقعٍ رائدٍ ومطابقةِ صفرِ فرقٍ وعلَمِ رجوع | ✅ | ثلاثُ نوافذَ مطابقةٍ (2027-02/03/04) · صفر فرق |

### 2.2 · §5.1 التايم شيت — 🟡 الجوهرُ منفَّذ والجوّالُ باقٍ

| البند | الحال | ملاحظة |
|---|---|---|
| خريطةُ الاشتقاق الثلاثية | ✅ **كانت قائمةً سلفًا** | الشاشة تشتق الخمسةَ عبر AJAX (نوع · مورد · **وحدةُ عقد** · مشغّل · موقع) |
| الحقولُ الثلاثة منفصلةٌ أبدًا | ✅ | في القاعدة (`unit_type`/`unit_time_log`/التحليلي) **وفي الشاشة** (`applyBillingUnit`) |
| توزيعُ الزمن بحالته ومسؤوله ومرجعه | ✅ | `EMS_TS_TIME_LINES=on` · `timesheet_time_lines_test` 17/17 |
| تجاوزُ الطاقة «يُحفظ ويُعلَّم» ويُعلَن | ✅ | شارةٌ فوريةٌ عند الحفظ |
| عدّادُ المتبقي | ✅ | «سُجّل N من M» بشريط تقدم |
| **الحالاتُ الخمس · البطاقاتُ للجوال · دون اتصال** | ⬜ | آخرُ بنود §5.1 |
| ستةُ سيناريوهاتٍ ميدانية | 🟡 4 من 6 | «اليوم مقفل» و«تعديلٌ بعد الاعتماد» ✅ · «نسي معدة» جزئيًّا (العدّاد بلا تذكير) · «انقطاعٌ ودمج» ⬜ |

### 2.3 · §5.2 و§5.3 و§2.2 — ⬜ لم تبدأ

- **§5.2 صندوقُ الاعتماد الواحد**: «صفُّ تجاوزٍ **لا يُعتمد** قبل السبب وفحصِ التداخل وتحديدِ المشغّل الثاني» — **ثغرةٌ مفتوحةٌ اليوم**: الحارسُ يعلّم والمعتمِدون لا يرون العلامة.
- **§5.3 غرفةُ العمليات**: أربعةُ تبويبات؛ ④ يجرّ محرّكَ الالتزام.
- **§2.2 لوحةُ التوزيع**: جدول `shift_assignments` **غير موجود** — ولذلك يبقى اشتقاقُ المشغّل الآلي والوردية الافتراضية معلَّقَين (موثَّقٌ في الخدمة لا ملفَّق).

### 2.4 · §6 لوحاتُ التشغيل — ✅ منفَّذة (12 لوحةً · 23 دورًا بالوراثة)

---

## 3 · UX-02 المالية — التصحيحان الوظيفيان

### 3.1 · §8.1 «وحداتُ الأطراف» بدل «مطابقة الوحدات» — ✅

| البند | الحال |
|---|---|
| إبطالُ اشتراط التطابق | ✅ التساوي يُعلَّم «طبيعيًّا» ولا يُشترط |
| رأسُ الواقعة + ثلاثُ بطاقاتِ حكم | ✅ عميل · مورد · **مشغّل** (الثالثة أُضيفت لمّا قام محرّكُ السياسات) |
| «تقرأ من السجل القانوني **دون إعادة إدخال**» | ✅ الإدخالُ اليدوي أُغلق · العشرةُ القديمة قسمٌ مجمَّد |
| زرُّ «اعتماد الأحكام» لا «اعتماد التطابق» | ✅ |
| «لا يظهر مصطلح التطابق الثلاثي» | ✅ |
| البرهان | `party_units_screen_test` 24/24 |

### 3.2 · §8.2 سياساتُ المشغّلين — ✅ **والانحرافُ صُحّح** (commit 8861259)

**المنفَّذ:** النماذجُ الثلاثة (ساعة/طن/متر + نقلة) والأسسُ السبعة · المعدّل والحدود ·
النطاق بالأخصّية · السريان · العملة · محرّك `OperatorDue` · شاشةُ إدارة (وحدة 140) ·
وصلُ المروحة (`via=policy`). البرهان: `operator_due_policy_test` 21/21 + دخنة 17/17.

> ### الانحرافُ الذي كان — وتصحيحُه (2026-07-27 · commit `8861259`)
>
> **النصّان:** UX-02 §15.2-ج «القائمُ تُعاد استخدامًا وتوسيعًا **بدل جدولٍ
> جديد**» · UX-06 §8.1 «**لا جدولَ سياساتٍ جديدًا**». وكان المنفّذ قد أنشأ
> `operator_pay_policies` جديدًا (اكتُشف في هذه المراجعة بالقياس العكسي).
>
> **التصحيح المنفَّذ** (بقرارات المالك الثلاثة): توسعةُ `contract_hour_policies`
> بأعمدة §15.2-ج بأسمائها — جدولٌ واحدٌ بوضعَين يميّزهما `party_scope` ·
> ترحيلُ الـ25 صفًّا · قيدُ `UQ(company,operator,work_model,pay_basis,valid_from)`
> بمقصد النص · **توحيدُ القاموسين** (التعارضُ المرفوع منذ تموز حُسم: `unlogged`
> أُضيفت بحكمٍ صريحٍ `case_by_case` فلا ساعةَ تمرّ بلا حكم) · إسقاطُ الزائد.
>
> **برهانُ صفر الفرق:** `operator_due_policy_test` 21/21 بالأرقام اليدوية
> نفسها · دخنة HTTP 21/21 والمستحق **940 SDG نفسُه** قبل التوحيد وبعده.
>
> **مرفوعان لكاتب الوثيقة:** ① §15.2-ج تُغفل عمودَ المعدّل و§8.2 تطلبه —
> والتجربةُ المقيسة: `pct(5,2)` يبتر 3500 (قيمةٌ حقيقيةٌ في العقود) إلى
> 999.99 صامتًا ⇒ أُضيف `rate(14,4)`. ② حرفُ `UQ(operator_id, valid_from)`
> يمنع أساسين لمشغّلٍ واحدٍ في الفترة نفسها — طُبّق مقصدُه لا حرفُه.

### 3.3 · §15.2 بقيةُ الـSchemas — ⬜

| البند | الحال |
|---|---|
| أ — أعمدةُ يومية القيود (`txn_date` · `request_no` · `currency`/`fx_rate`/`base_amount` · `cost_center_id`/`account_id`) | ⬜ |
| ب — كشوفُ البنك والمضاهاة (3 جداول) | ⬜ غائبةٌ كلُّها |

---

## 4 · FES-01 معيار الحدث المالي

| البند | الحال | ملاحظة |
|---|---|---|
| §3 هيكلُ الحدث الموحّد + الرأس | ✅ | `ems_business_events` (293 صفًّا) |
| §6 «الحدثُ يولّد القيد» | ✅ | `EventPublisher` — 54/54 · لا كتابةَ إلا بالناشر |
| §7 منعُ التكرار والتتبع | ✅ | `fin_event_links` عطالةً |
| §10.4 خطةُ الترحيل والرجوع (12 خطوة) | ✅ | منهجُ الأعلام المتبَع حرفيًّا |
| §10.7 صفُّ «وحدة الأطراف» في مصفوفة التتبع | ✅ | منفَّذٌ بأحكامه الثلاثة |
| §10.8 قالبُ حالات الاختبار | ✅ | متَّبعٌ في الحزم الجديدة |
| **دفترُ FES الكامل بمجموعاته الثماني على كل الجداول** | 🟡 | النواةُ قائمة؛ التعميمُ على كل جدولٍ ذي أثرٍ لم يكتمل |

---

## 5 · الإداراتُ السبع — ⬜ لم تبدأ (وكانت محجوزة)

**اكتشافٌ حاكمٌ في هذه المراجعة:** كلُّ وثيقةِ إدارةٍ تحيل صراحةً إلى «سجل الزمن
الموحّد (UX-03)» بوصفه شرطًا مسبقًا. UX-04: «زمنُ التعطل يُقرأ من سجل الزمن
الموحّد — **لا حقلَ محليًّا**». UX-10: «زمنُ المعدة قراءةً من سجل الزمن الموحّد».
**هذا الشرطُ تحقق اليوم** — فالسبعُ صارت قابلةً للبدء.

| الوثيقة | المطلوبُ الجديد | الحال |
|---|---|---|
| **UX-04 الصيانة** | `inspection_templates` + `inspection_results` · توحيدُ تصنيفَي الأعطال (36) والبلاغات (113) · تبويباتُ أمر الصيانة الخمسة | ⬜ الجدولان غائبان |
| **UX-05 الموردون** | جداولُ الاستقطاب/RFQ والحاويات · بطاقةُ المورد الأم · تسويةٌ بجلبٍ آلي | ⬜ |
| **UX-06 القوى** | `PayrollBasisService` · تسويةُ العامل عبر `SettlementService` بـ`partyType=employee` | ⬜ (وسياساتُه تعتمد تصحيحَ §3.2) |
| **UX-07 البلاغات** | محرّكُ المهل والتصعيد | 🟡 وحدةُ البلاغات مبنيةٌ سلفًا (S12) — تحتاج مطابقةً بالنصّ |
| **UX-08 المبيعات** | `claims` + `claim_lines` · الفاتورةُ الضريبية · محرّكُ الالتزام | ⬜ الجدولان غائبان · الالتزاماتُ والجاهزيةُ مبنيتان |
| **UX-09 المشتريات** | المطابقةُ الثلاثية · صرفُ المخزون والعهدة | 🟡 وحدةُ المشتريات مبنية — المطابقةُ الثلاثية ناقصة |
| **UX-10 الأسطول** | `meter_readings` + `equipment_documents` · Viewٌ موحّدٌ للمعدات | ⬜ الجدولان غائبان |

---

## 6 · UX-00 الدستور — قائمةُ الالتزام §14

| البند | الحال |
|---|---|
| المبادئ — اللوحاتُ تفتتح بأسئلة الدور | ✅ 12 لوحة |
| اللغة — بلا محظورٍ معماريٍّ في الواجهات | ✅ حارسٌ في شاشة `nav_items` يمنع المصطلحات |
| البنية — الأبوابُ ستةٌ فاصلةٌ بلا قوائمَ وسيطة | ✅ (باستثناءٌ معلَنٌ واحد: «سجل النشاط» في SET بدل REP — موثَّقٌ بحجته) |
| الرحلة — شريطُ حالةٍ وخطوةٌ تاليةٌ ورابطُ الأصل | 🟡 في الطلب المالي والوحدة ✅ · بقيةُ المعاملات جزئيًّا |
| الجداول — 19 بندًا · الحقولُ الثلاثة · أحكامٌ بلا شرط تطابق | ✅ للوحدات · 🟡 للبقية |
| التنفيذُ البرمجي — مواصفةٌ لكل شاشة | 🟡 للجديدة ✅ |
| القبول — اختبارٌ مع مستخدمين فعليين وبلوغُ العتبات | ⬜ **لم يجرِ** (يشمل عتبةَ «80٪ من مديري المواقع») |

---

## 7 · المتبقي مرتَّبًا بالأولوية

### أ · تصحيحٌ واجب (دَينٌ أحدثتُه أنا)
1. **توحيدُ جدول السياسات** على §15.2-ج (توسعةُ القائم · ترحيلُ 25 صفًّا · القيد الفريد · إسقاطُ الزائد).

### ب · إغلاقُ ثغرةٍ مفتوحة
2. **§5.2 صندوقُ الاعتماد الواحد** — الحارسُ يعلّم واليومَ لا أحدَ يُلزَم به.

### ج · إكمالُ التشغيل
3. لوحةُ التوزيع `shift_assignments` (تفكّ اشتقاقَ المشغّل والوردية وثلاثَ فجواتٍ معلنة).
4. غرفةُ العمليات §5.3.
5. الجوّالُ ودون اتصال (§5.1 الباقي).
6. القديمُ Views/Redirects (المرحلةُ الثالثة الأخيرة).

### د · أطرافُ الإدارات (كلُّها مفتوحةٌ الآن)
7. UX-10 العدّاداتُ والوثائق (وفيها بيتُ قراءات العدّاد الباقية في الشاشة القديمة).
8. UX-04 الفحصُ الفني اليومي.
9. UX-08 المستخلصُ والفاتورة.
10. UX-05 RFQ والحاويات · UX-09 المطابقة الثلاثية · UX-06 خدمةُ أساس الأجر.

### هـ · المالية العميقة
11. أعمدةُ يومية القيود (§15.2-أ) · كشوفُ البنك والمضاهاة (§15.2-ب) · تعميمُ دفتر FES.

### و · محجوزٌ بالمالك
دفعُ 28 التزامًا · **استبدالُ أرقام السياسات التجريبية** · كلمةُ مرور `admin@enjaz.com` ·
زرُّ أحداث الصيانة · الستُّ المعطَّلة للدور 1 · **اختبارُ القبول الميداني بعتباته**.

---

## 8 · ملاحظاتُ أمانةٍ على هذا التقرير

1. **الانحرافُ في §3.2 اكتشفتُه في هذه المراجعة لا قبلها** — بنيتُ الجدول الجديد
   دون أن أقرأ §15.2-ج و§8.1 من UX-06. القياسُ العكسيُّ هو ما كشفه.
2. «✅» تعني **مقيسًا ببرهانٍ مذكور**، لا «كُتب كودُه».
3. الأرقامُ المالية كلُّها من بياناتٍ تجريبيةٍ بقرار المالك — وسياساتُ المشغّلين
   المبذورة **تجريبيةٌ موسومةٌ محصورةُ السريان**، تُستبدل قبل أي استعمالٍ حقيقي.
4. `metric_update` معطَّلٌ بسببٍ موثَّقٍ (مخصَّصُ الصيانة قرارٌ إداريٌّ لم يُقَرّ) —
   غيابٌ معلَنٌ لا نقصُ تنفيذ.
