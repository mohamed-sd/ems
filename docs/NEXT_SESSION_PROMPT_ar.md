# برومبت الجلسة القادمة — تنفيذ الـ72 المتبقية من update0008
> انسخ ما تحت السطر والصقه رسالةً أولى في جلسة جديدة.

---

أنت شركة برمجيات محترفة تتولى نظام EMS. مهمتك: **تنفيذ الـ72 مهمة المتبقية من حزمة update0008 حتى آخرها بلا توقف ولا رجوع إليّ في أي قرار**.

## ⓪ اقرأ قبل أي فعل (بهذا الترتيب)
1. ذاكرتك المحمَّلة تلقائيًّا — گوتشاتها قوانين لا اقتراحات.
2. `docs/UPDATE0008_TASK_LEDGER_ar.md` — الرصيد 95/167 والمنفَّذ بشاهده (لا تعِد ما أُنجز).
3. `docs/UPDATE0008_REMAINING_BLOCKERS_ar.md` — خريطة المتبقي مختومةً بما نُفِّذ يوم 2026-08-06.
4. `docs/UPDATE0008_DECISIONS_LOG_ar.md` — القرارات ق-01..ق-16 **نافذة ولا تُعاد مناقشتها**.
5. `docs/M00_AUDIT_ar.md` و`docs/UPDATE0008_AUDIT_ar.md` — القياس الحي (90.3٪ · 80.1٪): أعد تشغيلهما `--md` بعد كل موجة.
6. نصوص الوثائق التسع عند الحاجة: `docs/update0008_extracts/*.txt`.

## ① التفويض والمرجعية (سارٍ من المالك)
- كامل الصلاحية في كل قرار. مراجع القرار بالترتيب: **الوثائق ← النظام القائم ← اجتهادك بالمعمارية العالمية**. وعند تعارض وثيقتين: **الأحدث يغلب** (سابقة ق-16).
- **كل بيانات النظام تجريبية** — لك تعديلها وإضافتها بما يخدم المهام، موسومةً `[… — ق-15]`.
- لا تتوقف إلا لانسدادٍ بيد المالك حصرًا (وثّقه في الدفتر وواصل غيره).

## ② الموجات بالترتيب (كل موجة: نفّذ ← اختبر ← أحزمة ← دفتر ← التزام)

### الموجة ١ — أوراق الإدارات الـ17 فوق المحرك (الأثقل قيمةً)
شاشة واحدة معيَّرة `Portal/dept_board.php` (لا 17 نسخة): تقرأ دور الجلسة ← إدارته من خريطة الـ17 (WFM-MAP في `docs/update0008_extracts/WFM-01.txt` وورقات الخريطة) ← تعرض (مهام أعضائها · طلبات بيدها · إنجازات 30ي · متأخرات/تصعيدات · مؤشرات) من `work_items/requests/achievement_records` عبر نطاق المدير (`ems_manager_scope_user_ids`). سجّلها في `modules` وامنح الأدوار، واختبرها HTTP بدورين مختلفين على الأقل.

### الموجة ٢ — لحاق CMP-03: بقية الشاشات البينية (~189)
**النمط المرجعي منجز**: انظر كيف لُحقت الخماسية (`Portal/ceo_risk.php` → `exec_decisions` بهجرة `2026_11_14*`): جدول أصلي بأعمدة مشتقة من `$FIELDS` + `NULLIF` + `is_seed` + ترحيل صفوف المخزن ثم تحرير الشاشة من `cmp03_screen_rows`. اكشف القائمة: `SELECT canonical_file, COUNT(*) FROM cmp03_screen_rows GROUP BY 1`. رتّب بالأكثر صفوفًا حقيقية. **DDL إضافي فقط + `php database/dump-schema` بعد كل هجرة** (وإلا تعفّن المثبّت). التزم كل شاشة فور خضرتها.

### الموجة ٣ — توثيق الرموز: 586 معرفًا عاريًا
شغّل `php tools/trace_matrix.php --md` واقرأ `docs/TRACE_MATRIX_ar.csv`. لكل معرفٍ عارٍ: جد شاهده الفعلي (اختبار/حزام/خدمة تنفّذه سلوكًا) و**اقتبس رمزه في تعليقٍ بالشاهد** — لا تلفّق شاهدًا لغير المنفَّذ؛ غير المنفَّذ فعلًا اتركه عاريًا يقيسه التقرير. الهدف: رفع الاستشهاد الحرفي من 15.9٪ إلى ما يطابق الواقع.

### الموجة ٤ — بقية E-03 التجربي
- الأعمدة السبعة الحاكمة: 120 شاشة متبقية (`tools/e03_checks.php` يعدّها) — عبر `includes/gov_columns.php` المركزي لا يدويًّا.
- الحالات الخمس (`ems_state_empty/error/...` من `includes/screen_contract.php`) على الشاشات الحرجة أولًا (طوابير العمل والمال).
- سؤال الشاشة: 262/285 — أكمل الـ23 الباقية يدويًّا (لها خصوصية منعت الكانس).

### الموجة ٥ — مشروطة بالتاريخ: نافذة الظل (لا تنفّذها قبل 2026-08-19)
افحص تاريخ اليوم أولًا. إن حلّ الموعد وعدّاد الظل صفر فرق:
1. قلب `EMS_PERM_SOURCE` إلى SEC-01 (خطة رجوع جاهزة + مسح `tools/trial_readiness_sweep.php --per-role=2` قبل وبعد فورًا).
2. SEC-013: البعدان scope/field/time والأفعال الـ16 في `template_permissions` (القائم: visibility+action).
3. المراجعة الدورية بالسحب الآلي (M-14) نبضةً.
4. عمود درجة الأثر AC-E01-05 (DDL التعديلي ينفك بانقضاء النافذة).
إن لم يحلّ الموعد: وثّق وتجاوزها.

### الموجة ٦ — متفرقات الإقفال
- جزئيات M-00 الست الباقية (من `M00_AUDIT`): الأثر الخماسي ④-٤ والرباعي ④-٣ (مراكز تكلفة/حاويات H-01 ②③ إن بُنيت بنيتها وإلا وثّق التبعية) · نماذج العمل ④-١ · اختبار «التعمّق».
- M-14: الحملات الدورية آلية (بعد الموجة ٥).
- حسّن ما كسر من اختبارات بسبب لحاق الجلسة الموازية للجداول الأصلية (مثل `tools/m00_approve_actions_test.php` — كانت بيدهم لحظة الإقفال).

## ③ قواعد التشغيل الصارمة (مخالفتها = إعادة عمل)
- **PHP**: `/c/wamp64/bin/php/php8.2.30/php.exe` بالمسار الكامل. `config.php` يبتلع مخرج CLI: `ob_start()` قبل require ثم `ob_end_clean()` و`fwrite(STDOUT,…)`.
- **العربي في CLI**: لا `php -r` بمعطيات عربية عبر Git Bash (يفسد argv) — اختبر POST العربي بـPHP cURL. و`schtasks` عبر PowerShell لا Git Bash.
- **MariaDB 3307 — عائلة خلط الترتيبات القاتلة الصامتة**: لا `CASE WHEN ? = 'نص'` ولا `CONCAT('%',?,'%')` في مقارنات — الفروع والأنماط تُبنى في PHP، ومع `or die/error_log` دائمًا. `bind_param` بأنواعٍ مبنية بالضم لا بالعد اليدوي.
- **الجلسة الموازية** قد تعمل معك: حارتها `Clients/ Suppliers/ Opportunities/` وشاشات M-00 الخمس — لا تلمسها. **كل تعديلٍ ناجحٍ يُلتزم في نفَسه فورًا** (استرجاعاتها تمحو غير الملتزَم). حدّثت ملفًا تعمل عليه؟ زامن أدواتك معه ولا تسترجعه.
- **الأحزمة العشرة تبقى خضراء بعد كل موجة**: `act_checks --enforce · e02_checks --enforce · e05_checks · nav09_verify · nav_seven_guard · wfm_checks · e03_checks · sec_perm_checks` + `tests/wfm_engine_test.php` (44/44) + `tests/leg01_entities_test.php` (16/16). حزام يحمرّ = أصلح قبل المواصلة.
- **كل شاشة جديدة تُسجَّل في `modules` وتُمنح** وإلا حجبها الحارس fail-closed. المحلّل يفضّل المسار التام ثم ذيل `/الاسم.php`.
- گوتشات مثبتة: `canonical_file` بالاسم المجرد · `users.status` نص ENUM · `decide($conn,$id,$decision,$actor)` بهذا الترتيب · التنبيهات في `personal_notifications` · مفاتيح الخماسية الجديدة `EXDC-/EXAP-` · `contracts` بلا عمود رقم (هويته طرفاه ومشروعه) · مرساة الكانسات تتجنب التعليقات (درس inheader).
- نهاية الأسطر LF (خطاف الترميز يرفض المختلط) · لا `git push` (مفتاح SSH بعبارة مرور — حضّر وسلّم).

## ④ تعريف الإنجاز
كل موجة تُختم بـ: اختباراتها خضراء + الأحزمة العشرة خضراء + تحديث `UPDATE0008_TASK_LEDGER_ar.md` (دفعة مسماة بشاهدها) + إعادة `m00_audit --md` و`update0008_audit --md` + التزامات ذرية بالعربية + تحديث الذاكرة بالجوهر والگوتشات الجديدة. الختام: `php tools/release_gate.php` والبوابات الخمس تعبر، وتقرير نهائي بالأرقام: الرصيد من 95/167، والتغطيتان من 90.3٪/80.1٪، وما بقي معلَّلًا.

انطلق.
