# برومتُ التنفيذ الذاتي — update0005 · CAP-01

> **انسخ هذا الملفَّ كاملًا في أول رسالةٍ لجلسةٍ جديدة، ثم لا تتدخل حتى يصلك التقريرُ الختامي.**

---

## 0 · التكليف

أنت مهندسُ البرمجيات المسؤولُ عن تنفيذ **`update0005`** في `C:\wamp64\www\ems`.

**المهمة: سبعُ موجاتٍ وسبعٌ وثلاثون مهمةً — من الموجة ① إلى الموجة ⑦ — بلا توقفٍ وبلا عودةٍ إلى المالك.**

**اقرأ قبل أي فعل، بهذا الترتيب:**
1. **[`docs/UPDATE0005_PLAN_ar.md`](UPDATE0005_PLAN_ar.md)** — خارطتُك الملزمة: ماذا تنفّذ.
2. **[`docs/UPDATE0004_AUTONOMOUS_PROMPT_ar.md`](UPDATE0004_AUTONOMOUS_PROMPT_ar.md) §5** — **أعرافُ هذا المستودع الملزمة**. اقرأها كاملةً وطبّقها هنا حرفيًّا: الهجراتُ · `scopedQuery` · `EventPublisher` · العطالةُ · ساعةُ القاعدة · `TenantRegistry` · `action_guard` · الإخفاءُ في الخادم · اللغةُ العربية. **لا تُعِد اكتشافَها.**
3. الوثيقةُ المصدر: **`CAP-01` التوزيعُ التعاقديُّ للوحدات والقدرات** — نصُّها في `docs/sources/` إن وُجد، وإلا فاطلب استخراجَها من `C:\Users\User\Downloads\CAP-01 التوزيع التعاقدي للوحدات والقدرات-3.docx` بأداة PHP (**Python غيرُ متاحٍ في هذه البيئة**).

---

## 1 · قانونُ الاستقلال

**لا تتوقف. ولا تسأل. ولا تنتظر جوابًا.**

كلُّ سؤالٍ يُحسم بهذا السلّم بالترتيب:

| # | المصدر |
|---|---|
| **①** | **`CAP-01`** أولًا · ثم `OPM-01` · `PLAN-04 §2` · `CON-02`/`CON-03` · `GOV-01 §4` · `POL-01` · `FES-01` |
| **②** | **قراراتُ §3 من هذا البرومت** — قرارٌ مسجَّلٌ لا يُراجَع ولا يُجتهد فيه |
| **③** | **الكودُ والمشروعُ القائم** — نمطٌ مستقرٌّ يسبق ذوقَك. اقرأ نظيرًا قائمًا واحذُ حذوَه |
| **④** | **المعياريةُ العالمية** — ACID · idempotency · fail-closed · append-only audit · قيدٌ في القاعدة لا في التطبيق |

**وكلُّ نزولٍ إلى ③ أو ④ يُسجَّل صفًّا في `docs/UPDATE0005_JUDGMENT_LOG.md`** بالسؤال والدرجة والقرار والسبب **وأثرِ خطئه**. **والاجتهادُ غيرُ المسجَّل خطأٌ ولو كان صائبًا.**

**وما تؤجّله وتسجّله ولا تتوقف عنده** — نفسُ ستِّ حالات §1.3 من برومت `update0004`: الدفعُ إلى بعيدٍ · قلبُ علَمٍ إلى `enforce` · حذفُ ذي أثر · بياناتُ إنتاج · الأسرارُ · الإرسالُ خارج الجهاز.

---

## 2 · بيئةُ العمل — مقيسةٌ لا مفترضة

| البند | القيمة |
|---|---|
| الفرع | **افتح فورًا `git checkout -b feature/update0005`** من `main` بعد دمج/تثبيت `feature/update0004` |
| القاعدة | `equipation_manage` · **MySQL 8.4.7** · **٤٦٥ عمودًا مولَّدًا قائمًا** — فالأعمدةُ المولَّدةُ والفهارسُ المشروطةُ عُرفٌ لا اختراع |
| PHP | `C:\wamp64\bin\php\php8.2.30\php.exe` |
| الهجرات | `php database/migrate.php status\|up` — **كلُّ DDL هجرة** |

**أرقامٌ حيةٌ قِيست — ابنِ عليها:**
`op_containers`=**94** · `seat_assignments`=**0** · `container_consumption`=**1** · `container_swaps`=**4** · `contract_commitments`=**12** · `supplier_contract_lines`=**6** · `suppliers`=**16** · `unit_entries`=**146** · `unit_party_awards`=**15** · `unit_approvals`=**17** · `timesheet`=**366** · `ems_business_events`=**3626** · `units_of_measure`=**7**

**وجداولُ `CAP-01` العشرةُ كلُّها غائبةٌ بأسمائها** — والأربعةُ التي تُبنى بالاسم محدَّدةٌ في §3 أدناه.

---

## 3 · القراراتُ النافذة — **لا تُراجَع ولا يُجتهد فيها**

| # | القرار |
|---|---|
| **DEC-CAP-A** | **العقدان لا يحجبان.** تُبنى حقولُ §8.1 و§8.2 الآن بأسمائها وأنواعها · **والمالُ fail-closed**: احتسابُ مقابلِ احتياطيٍّ بلا قيمةٍ عقديةٍ صريحة → **422 مسجَّلة**. ويطابق §4③: «بلا نصٍّ فهي التزامُ موردٍ لا بندُ إيراد» |
| **DEC-CAP-B** | **الصادرُ لمجال القدرات وحدَه** — **ولا تمسّ `EffectFanout` ولا ADR-15 ولا `ems_business_events`**. سببُه: نمطُ الصادر يحلّ تباعدَ نظامين، ونشرُنا كتابةٌ في القاعدة نفسِها. والغرضُ الوحيدُ هنا إمرارُ `C28` |
| **DEC-CAP-C** | **الهجين — انحرافٌ موقَّعٌ عن §16**: `op_containers` **يبقى** شجرةَ التوزيع بدرجاتها الخمس · **ولا يُفكَّك** · **وتُبنى بأسمائها حرفيًّا أربعةٌ فقط**: `capacity_consumption_ledger` · `capacity_financial_event_links` · `substitute_coverages` · `coverage_settlement_lines` · **وقيودُ الدرجات تُنفَّذ في القاعدة بفهارسَ فريدةٍ مشروطةٍ على أعمدةٍ مولَّدة لا بفحوص تطبيق** |
| **DEC-CAP-D** | **من §15 يدخل `UX-03` و`FES-01` وحدَهما** · و**`ENT-01`/`ENT-02`/`ENT-03` و`POL-01` و`UAT-01` تُعلَن وتُؤجَّل** — تُسلَّم بشأنها ورقةُ تكليفٍ في التقرير لا تنفيذ |

### 3.1 · حقيقتان مقيستان تختصران عليك عملًا

**① سجلُّ الوحدة مرقَّمُ النسخ موجودٌ فلا تبنِه.** §13.2 يوجب `unit_record_id + unit_record_version` — و**`unit_entries` يحمله**: `revision_no` · `current_round` · `revises_entry_id` · `revision_kind` · `superseded_by_id` · `event_id` · ومعه `unit_approvals.round_no`.
**فمفتاحُ الدفتر:** `UQ(entry_id, revision_no, effect_type, effect_target_type, effect_target_ref)`.

**② `N-11` و`N-12` مبنيتان** (`n11_contractual_seats` · `n12_monthly_performance`) — و§17 يشترط الترتيبَ معهما، **والشرطُ مستوفًى**.

---

## 4 · بروتوكولُ الموجة — سبعُ خطواتٍ لا تُختصر

**① اقرأ** بندَ الموجة في الخطة وقسمَها في `CAP-01` وحالاتِها من `C1`→`C33`.
**② قِس** الحالةَ القائمةَ **قبل** أي تعديل. لا تفترض. واكتب ما قِسته.
**③ نفّذ** مهمةً مهمةً بترتيبها — وابحث عن نظيرٍ قائمٍ قبل كل بناء.
**④ اختبر** بأسماء الحالات (`C1`→`C21` في الموجات · و`C22`→`C33` تُسجَّل لـ`UAT-01`). **وإن فشل اختبارٌ فأصلح السببَ لا الاختبار.**
**⑤ سجّل** في `docs/UPDATE0005_EXECUTION_LOG.md` صفًّا لكل مهمة — **قبل الالتزام لا بعده**.
**⑥ التزم** التزامًا **واحدًا لكل موجة**:
```
feat(CAP·<الموجة>): <العنوان> — <عدد> مهام

<سطرٌ لكل مهمة>

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
**⑦ انتقل** فورًا. **بلا تقريرٍ وسيطٍ وبلا سؤال.**

---

## 5 · الموجاتُ السبع — **سلسلةٌ خطيةٌ بلا توازٍ**

| # | الموجة | المهام | لماذا هنا |
|---|---|---|---|
| **①** | حقولُ الالتزام والاحتياطي | `CAP-01`→`06` · 6 | الدفترُ يسجّل `role_snapshot` — ولا معنى للدور قبل الحقول |
| **②** | الدفترُ والتغطيةُ البديلة | `CAP-07`→`12` · 6 | **الأربعةُ التي تُبنى بالاسم** |
| **③** | قلبُ الأرصدة والقيود | `CAP-13`→`17` · 5 | لا يُقلب المصدرُ إلى دفترٍ لا يوجد |
| **④** | الخدماتُ الثماني | `CAP-18`→`25` · 8 | القيدُ في القاعدة يسبق الخدمةَ التي تشرحه |
| **⑤** | الصادرُ والذريةُ والأحداث | `CAP-26`→`30` · 5 | ينشر ما تُنتجه الخدمات |
| **⑥** | اللقطةُ ومسارُ الوحدة | `CAP-31`→`34` · 4 | `C22`→`C29` كلُّها عن اللقطة والمسودة والعكس |
| **⑦** | الشاشاتُ والحزام | `CAP-35`→`37` · 3 | |

**كلُّ موجةٍ تبني على مخرَج سابقتها — فلا تُقدِّم ولا تدمج.**
**وتفصيلُ كل مهمةٍ وقبولُها في الخطة §3.**

---

## 6 · ثلاثةُ حرّاسٍ لا تُخرق في هذه الحزمة

**① لا يُعدَّل رصيدٌ مباشرةً أبدًا.** `BalanceCalculator` يحسب من الدفتر والإعكاسات · **ولا يقرأ عمودَ رصيدٍ مخزَّن** · وتعديلُ رصيدٍ مباشرةً → **403 بنيويًّا**. والأعمدةُ الأربعةُ في `op_containers` تصير **مخبأً يُعاد بناؤه** لا مصدرًا.

**② لا تُخفي التغطيةُ العجز.** عجزُ المتعطل **يبقى مسجَّلًا وجزاؤه يُحتسب** · وحصةُ المغطِّي **لا ترتفع** · والتغطيةُ **بندٌ استثنائيٌّ مستقل**. ومحاولةُ تصفير عجز المتعطل → **403** · وإدراجُ ساعات التغطية في تنفيذ الحصة → **403**.

**③ لا استهلاكَ قبل اكتمال الاعتماد.** المسودةُ والمرفوضةُ **صفرُ سطرٍ في الدفتر** · والاستدعاءُ عند اكتمال السلسلة لا قبله · وإعادةُ الإرسال → **409 بمرجع السطر القائم**.

---

## 7 · المخرَجات

| المخرَج | المسار |
|---|---|
| سجلُّ التنفيذ | `docs/UPDATE0005_EXECUTION_LOG.md` |
| سجلُّ الاجتهادات | `docs/UPDATE0005_JUDGMENT_LOG.md` |
| الهجرات | `database/migrations/` |
| بطاقاتُ المواصفة | `docs/specs/` |
| **ورقةُ التكليف المؤجَّل** (`DEC-CAP-D`) | `docs/UPDATE0005_DEFERRED_ASSIGNMENTS_ar.md` |
| **التقريرُ الختامي** | `docs/UPDATE0005_FINAL_REPORT_ar.md` |

**والتقريرُ الختاميُّ يحوي:** جدولَ الموجات السبع بأحزمتها · **كلَّ اجتهاد** · كلَّ مؤجَّلٍ بنصِّ ما أعددتَه · الأعلامَ الثلاثةَ وشرطَ قلب كلٍّ منها · **وما يحتاج توقيعَ المالك** · والأرقامَ قبل وبعد.

---

## 8 · تعريفُ الإنجاز

**التكليفُ منجَز** حين: الموجاتُ السبعُ منجَزة · وحزامُ `C1`→`C21` أخضر · و`C22`→`C33` مسجَّلةٌ لـ`UAT-01` · وورقةُ التكليف المؤجَّل مكتوبة · والتقريرُ الختاميُّ تام · والفرعُ جاهزٌ للدفع **بلا دفع**.

**ولا تُعلن الإنجازَ وأنت لم تُتمّ** — **والصدقُ في التقرير أهمُّ من اكتمالٍ مدَّعًى.**

---

## 9 · ابدأ الآن

```
① git checkout -b feature/update0005
② اقرأ UPDATE0005_PLAN_ar.md كاملةً · ثم §5 من برومت update0004
③ أنشئ سجلَّي التنفيذ والاجتهادات
④ ابدأ الموجة ① · المهمة CAP-01
⑤ امضِ إلى الموجة ⑦ بلا توقف
⑥ اكتب ورقةَ التكليف المؤجَّل والتقريرَ الختامي
```

**ولا ترجع إليَّ قبل ⑥.**
