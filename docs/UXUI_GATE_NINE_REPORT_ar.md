# تقريرُ بوابةِ الترقيةِ التسع — الحالُ المقيس

· الالتزامات `2f8b3d1` (الوصولُ والاستجابةُ والمركزية) و`e91a7c4` (البوابةُ المجمَّعة).
· وكلُّ رقمٍ مخرَجُ أمرٍ يعيد إنتاجَه.

---

## ① البند ٣ — الوصولُ الرقميّ: **136 مخالفةً ⇐ 1**

قِيس في **DOM حيٍّ بعد تشغيلِ JS** لا في النصّ. وثلاثةٌ من العيوبِ كانت **في مولِّدي أنا** — أصلحتُها في مصدرِها:

| العيب | كان | صار | العلاج |
|---|---|---|---|
| أزرارُ المجموعاتِ بلا اسمٍ لقارئِ الشاشة | **37** | **0** | الاسمُ يعيش في `span` يُخفيه طيُّ الشريط و`innerText` لمخفيٍّ فراغ — فثُبِّت على الزرِّ نفسِه في **أربعةِ مواضعِ تصيير**؛ 39/39 مسمّاةٌ الآن |
| معالمُ الصفحة (`main`/`nav`) | **غائبة** | **موجودة** | الشريطُ `role=navigation` في غلافِه القائم · والمحتوى `role=main` مركزيًّا في طبقةِ التوحيد |
| مؤشرُ التركيزِ المرئيّ | **منزوع** | **قاعدةٌ مركزية** | `:focus-visible` بلكنةِ العلامة — 23 قاعدةً محمَّلةً مُثبَتة |

**والمخالفةُ الباقيةُ واحدة** (زرٌّ أيقونيٌّ في القشرةِ العلوية).

**وعيبٌ في مسباري رصدتُه وأصلحتُه**: قياسُ التركيزِ بـ`.focus()` برمجيًّا **لا يُفعّل `:focus-visible` بحكمِ المواصفة** — فكان يُبلّغ 40/40 «بلا حلقة» وهي موجودةٌ للوحةِ المفاتيح. صار يُثبت وجودَ القاعدةِ لا حالةً لحظيةً لا تقع إلا بالمفتاح.

---

## ② البند ٤ — الاستجابة: ثلاثُ دقاتٍ نظيفة

| الدقة | تمريرٌ أفقيٌّ للصفحة | الترويسة | زرٌّ رئيسيّ |
|---|---|---|---|
| 1920×1080 | **لا** ✔ | 59px | 1 ✔ |
| 1366×768 | **لا** ✔ | 59px | 1 ✔ |
| **768×1024 (لوحيّ)** | **لا** ✔ | **96px — عند الحدِّ تمامًا** | 1 ✔ |

وصفرُ عنصرٍ فائضٍ عن عرضِ الصفحةِ في الدقاتِ الثلاث.

---

## ③ البند ٦ — مركزيةُ المكوّنات: خطُّ الأساسِ الصادق **1/10**

بنيتُ بوابةً تقيس النمطَ المحليَّ في ملفِّ الشاشةِ نفسِه (لا في المكتبة):

| الشاشة | مخالفات | أبرزُها |
|---|---|---|
| `Maintenance/orders.php` | **58** | كتلتا `style` · 27 قياسًا خارجَ السلّم |
| `Timesheet/timesheet.php` | 11 | كتلتا `style` · 6 ألوانٍ مثبَّتة |
| `FinRequests/request_form.php` | 8 | كتلةُ `style` · لونٌ مثبَّت |
| `Portal/my_tasks.php` | 5 | كتلةُ `style` |
| `Portal/ceo_board.php` · `Operations/sites_board.php` | 4 لكلٍّ | كتلةُ `style` |
| `Suppliers/supplier_profile.php` · `Risk/risk_register.php` · `Finance/approvals_inbox.php` | 3 لكلٍّ | كتلةُ `style` |
| ◆ `Contracts/contracts.php` | **0** ✔ | نظيفةٌ تمامًا |

**الإجمالي 99 مخالفةً** — وهذا هو الحاجزُ الأولُ أمام ترقيةِ تسعِ شاشات.

---

## ④ البوابةُ التسعُ مجمَّعةً — والحكمُ الصادقُ **0/10**

| الشاشة | ① | ②نص | ②متصفح | ③ | ④ | ⑤ | ⑥ | ⑦ | ⑧ | ⑨ | النتيجة |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **`Portal/my_tasks.php`** | ✔ | ✔ | **✔** | ◐ | **✔** | ✔ | ✗(5) | ✔ | ⛔ | ⛔ | **6/10** |
| `Contracts/contracts.php` | ✔ | ✗(1) | — | ◐ | — | ✗ | **✔** | — | ⛔ | ⛔ | 2/7 |
| الثمانيةُ الباقية | ✔ | ✔ | — | ◐ | — | ✔ | ✗ | — | ⛔ | ⛔ | 3/7 |

**و«—» خارجَ المقامِ بنصِّ ف١٦-٢**: «ما لا يُقاس بالمتصفحِ لا يُحسب أخضر» — فلا يُعدُّ مارًّا ولا راسبًا.

### وربطُ القياسِ بإصدارِ المكوّنِ أثبت نفسَه عمليًّا

غيّرتُ CSS المكتبةِ (حلقةُ التركيز) فبطلت **كلُّ** قياساتِ `ux-1.0.0` تلقائيًّا وصارت `NOT_MEASURED`. سُجِّل `ux-1.1.0` وأُعيد القياس. **«لا يُقرأ رقمٌ قديمٌ على بناءٍ جديد» صار سلوكًا لا شعارًا.**

### والبندان ⑧ و⑨ لا يُشغَّلان في أداتي بحال

أنشأتُ `gov_independent_reviews` بيتًا لنتيجتِهما: المنفِّذُ والمشاركونَ والمهامُّ **ومجموعُ المحاولاتِ مقامًا للنسبةِ** ومرجعُ المحضر. تُقرأ ولا تُكتب من أدواتي — وتبقى `BLOCKED_EXTERNAL_INPUT` حتى يسجّلها مستقلٌّ عن البانِي.

---

## مصفوفةُ جاهزيةِ القدرات

| القدرة | الحال | المقياس ومقامُه |
|---|---|---|
| البند ① صفرُ فقد | 🟢 | 10/10 بقياسٍ مستقرٍّ (قدرةٌ بنيويةٌ + قالبُ صف) |
| البند ② النصيّ | 🟡 | 9/10 خضراء |
| البند ② المتصفح | 🟡 | **1/10 مقيسٌ كاملًا** بالإصدارِ الحالي · والبقيةُ خارجَ المقام |
| البند ③ الوصول | 🟡 | التضادُّ 29/29 آليًّا · والـ11 الحيّةُ 136⇐1 على شاشةٍ واحدةٍ مقيسة |
| البند ④ الاستجابة | 🟡 | **1/10** بثلاثِ دقاتٍ كاملة |
| البند ⑤ اللغة | 🟢 | 9/10 |
| البند ⑥ المكوّنات | 🔴 | **1/10** · 99 مخالفةً محدَّدةً بالاسمِ والعدد |
| البند ⑦ خطُّ الأساس | 🟡 | مربوطٌ بالإصدارِ ويعمل · 1/10 له قياسٌ حديث |
| البندان ⑧ ⑨ | ⛔ | `BLOCKED_EXTERNAL_INPUT` — بيتُهما جاهزٌ ومنفِّذُهما مستقلّ |
| الصمتُ لا يُرقِّي | 🟢 | 281 معتمَد / **0 مطبَّقٌ مؤقَّتًا** |
| سجلُّ الأسطح | 🟢 | **المقام 466** قابلًا للتصيير |
| مسحةُ التلوث | 🟢 | 560 موضعًا · صفرُ كتابةٍ في بياناتِ العمل |
| بواباتُ التنقلِ ومصفوفةُ الحفظ | 🟢 | خضراء |

---

## قراراتٌ مفوَّضةٌ حُسمت — بسببها

| القرار | السبب في سطر |
|---|---|
| إصلاحُ الوصولِ في المولِّدِ لا في الشاشات | 37 زرًّا بلا اسمٍ مصدرُها أربعةُ مواضعِ تصييرٍ — إصلاحُها يعالج كلَّ شاشةٍ في النظام |
| `role=main` في طبقةِ JS لا في الشاشات | الغلافُ مكتوبٌ في مئاتِ الملفات — وكنسُها خطرٌ لا يوازي عائدَه، والطبقةُ تعمل في كلِّ صفحة |
| البوابةُ تجمع ولا تُعيد اختراعَ الفحوص | مصدرُ كلِّ رقمٍ واحدٌ — ورقمانِ لمقياسٍ واحدٍ يتفرَّقان حتمًا |
| «NOT_MEASURED» خارجَ المقامِ لا راسب | نصُّك: «ما لم يُقَس لا يُحسب مارًّا» — واحتسابُه راسبًا كذبٌ بالاتجاهِ الآخر |
| تسجيلُ `ux-1.1.0` بدل تعديلِ `ux-1.0.0` | «المرقَّى لا يُعدَّل في صمت» — والتغييرُ الجوهريُّ يُنشئ إصدارًا يلزمه إعادةُ تحقُّق |

---

## `Blocked` — معزولٌ ولم يوقف شيئًا

| البند | الحال | السبب |
|---|---|---|
| البوابتان ⑧ ⑨ | `BLOCKED_EXTERNAL_INPUT` | منفِّذٌ مستقلٌّ عن البانِي — بيتُ النتيجةِ جاهزٌ (`gov_independent_reviews`) |
| السقوفُ الستة | `BLOCKED_OWNER_INPUT` | قادحُ القاعدةِ يشترط الدورَ 9 |
| `timesheet.time_notes` | `BLOCKED_OWNER_INPUT` | 48,379 إصابةً في حقلٍ مشروع — سياسةُ بياناتٍ لا قرارٌ تقنيّ |
| الدفعُ إلى الأصل | `BLOCKED_OWNER_INPUT` | 217 التزامًا · بلا نقلِ مادةٍ سرية |

```bash
git push origin fix/remediation-2026-08
```

---

## التالي

تنظيفُ الأنماطِ المحليةِ التسعِ (البند ⑥ — الحاجزُ الأول) · ثم قياسُ المتصفحِ لبقيةِ العشرِ بحساباتِها · ثم `INJAZ DataGrid` وترحيلُ العقودِ عليه.
