# انزياحُ الحزمةِ الحاكمة — 2026-08-31 (النسخ -3 / -3 / -2)

> ⛔ مقيسٌ ورقةً ورقةً وصفًّا صفًّا قبل النصب (`scratchpad/gov3/xdiff.php`) —
> والمنصوبُ القديمُ محفوظٌ حرفًا في `_pkg_backup_20260831/`.

## ⓪ البصمات

| الملف | المنصوبُ القديم | الجديدُ المنصوبُ الآن | الحكم |
|---|---|---|---|
| `01 · الدليل المعماري` | `58b9cb58` (954,815) | **`0fd7c901`** (956,950) | **تغيّر** |
| `02 · القيادة` | `c262f8f8` | `c262f8f8` | **مطابقٌ حرفًا — لم يُمسّ** |
| `03 · الدستور` | `b4c18ab4` (79,545) | **`cef3b4e6`** (84,064) | **تغيّر** |
| `09 · السجلات والقرارات` | `c7cd6644` (584,367) | **`4257b3f7`** (585,388) | **تغيّر** |

## ① الدليل المعماري -2 ⇒ -3 — **شاشةٌ جديدةٌ واحدة**

| الورقة | الفرق |
|---|---|
| `17_إدارة_المخازن` | **شاشةٌ جديدةٌ برقم 3: «إسناد أمناء المخازن»** — مخزن × شخص × فترة إسناد (`Child Register`) · [التأسيس المرجعي] · **17 حقلًا** · «أساسُ تصفيةِ كلِّ حركةٍ بنطاقِ أمينِها» — والشاشاتُ 18⇒**19** وترقيمُ ما بعدها انزاح +1 |
| `00_الفهرس_المؤسسي` | الإجمالي 395⇒**396** شاشة · 6,914⇒**6,931** حقلًا |
| `99_المراجعة_المعمارية` | صفُّ الشاشةِ الجديدةِ + إعادةُ ترقيمِ صفوفِ المخازن |

⚠ **انزياحُ معرِّفات**: سجلُّ المتطلّباتِ الجديدُ يعيد ترقيمَ `WH-03..WH-19`
(«دليل الأصناف» كان `WH-03` فصار `WH-04`…) — **والدفترُ الحيُّ على الترقيمِ
القديمِ و8 من المنزاحاتِ `EVIDENCE_CLOSED` منسوبةً لمعرِّفِها القديم**.
⛔ فالمصالحةُ **بنسبٍ** (م 121) لا باستيرادٍ يدهس الأحكام
[[registry-rule-vs-value]] — ودرسُ حجبِ 157 متطلّبًا على المصالحةِ واقعٌ سلفًا.

## ② الدستور -2 ⇒ -3 — **عشرُ موادَّ جديدةٍ (م 113–122) وورقةُ خريطةِ المصادر**

| المادة | المضمون (تقنينُ دروسِ `NAVR` نصًّا حاكمًا) |
|---|---|
| م 113 | بوابةُ `G0` تمنع بناءَ وظيفةٍ جديدةٍ **ولا تمنع مصالحةً معماريّة** |
| م 114 | الإدارةُ تملك العمليّةَ · الدورُ ينفّذ · **النطاقُ يصفّي** — ولا تُنسخ شاشةٌ لكلِّ نطاق |
| م 115 | اتجاهُ الحقيقةِ واحد: مصدرٌ ← هدفٌ ← تنفيذ — **ويُمنع اشتقاقُ الهدفِ من الوضعِ القائم** |
| م 116 | كلُّ سجلٍّ حاكمٍ يجيب أربعة: كاتبُه · قارئُه زمنَ التشغيل · أين يُنفَّذ · كيف يُكشف انحرافُه |
| م 117 | الحقيقةُ السياقيّةُ لا تُخزَّن علاقةً عالميّة |
| م 118 | لا سقوطَ صامتًا إلى مسارٍ قديم — البديلُ معلَنٌ محدَّدُ النطاقِ مقيس |
| م 119 | كلُّ مقياسٍ يحمل ثمانيًا (السؤال · البسط · المقام · الاستبعادات …) |
| م 120 | حالاتُ الإغلاقِ ستٌّ لا تُقفَز: محسوم ← مبنيّ ← موصول ← مُمارَس ← مُثبَت ← مغلقٌ بالدليل |
| م 121 | نسبُ الهدفِ لا ينقطع عند البناء — معرِّفٌ ثابتٌ منذ تعريفِه |
| م 122 | لكلِّ كونٍ مصدرٌ حاكمٌ معلَنٌ في **خريطةِ المصادر** |

**وورقةٌ جديدة** `J_خريطة_المصادر_الحاكمة` (14 صفًّا): `DEP-01..17`⇒الملف ٠١ ·
`EX-CEO`/`EX-DVP`⇒الملف ٠٢ · `WS-MY`/`IAF`⇒أوراقُهما · `PLATFORM`⇒المعاييرُ
الأربعة · والقرارات⇒الملف ٠٩ — **وواقعُ النظامِ «مرجعٌ يُقاس عليه» لا هدف
(م 115)** والخريطةُ نفسُها «مرجعٌ لا يُقرأ زمنَ التشغيل» (م 116).

وتعديلاتٌ طفيفةٌ في `A`/`B`/`F` (صفّان–ثلاثة لكلٍّ — تحديثُ إشاراتٍ للموادِّ الجديدة).

## ③ السجلات والقرارات -1 ⇒ -2 — **إسقاطُ الشاشةِ الجديدةِ على السجلّات**

| الورقة | الفرق |
|---|---|
| `01_سجل_المتطلبات` | 435⇒**436** صفًّا: `WH-03` الجديد «إسناد أمناء المخازن» + إعادةُ ترقيمِ `WH-04..WH-19` |
| `02_تتبع_الحقول` | 7,594⇒**7,611**: **17 حقلَ** الشاشةِ الجديدةِ + إعادةُ نسبِ حقولِ المخازنِ لمعرِّفاتِها المنزاحة |
| `OWNER_DECISIONS_MASTER` | سطرُ الرأسِ فقط: «المصدرُ الحاكمُ لكلِّ قرار **(م 122)**» — **لا قرارَ جديدًا ولا تغييرَ قرار** (125: 114 معتمدًا · 11 بانتظارِ قيم) |
| `00_الفهرس` · `04_مصفوفة_القبول` | تحديثُ عدّاداتٍ وإشارات |

## ④ الخلاصةُ التنفيذيّة

الانزياحُ **متّسقٌ حول محورَين لا غير**:
1. **شاشةُ «إسناد أمناء المخازن»** — دليلًا ومتطلّبًا (`WH-03`) وحقولًا (17) — تفتح: مصالحةَ نسبِ `WH-*` ثمَّ استيرادَ الهدفِ وموضعَه وبناءَه.
2. **تقنينُ م 113–122** — نصٌّ دستوريٌّ لما أثبتته حملةُ `NAVR` عمليًّا — يفتح: ربطَ فحوصِ الامتثالِ القائمةِ بموادِّها وسدَّ ما لا فحصَ له.

و**ملفُّ القيادةِ لم يتغيّر** — فأعمالُ `NAVR ⑤` (استيرادُ `NT-EX-*`) باقيةٌ بحجّيتها.
