# الطورُ صفر · ① — تصنيفُ بنودِ الشجرةِ وإقفالُها إلى صفر

> **الأمرُ الحاكم**: `orders/MASTER_EXEC.txt` §٢① — *«راجعْ كلَّ `Modified`
> و`Untracked` · ⛔ ولا تحذفْ شيئًا بلا مراجعة · وصنِّفْ كلًّا إلى: عملٍ صحيحٍ
> يُلتزَم · أثرِ قياسٍ يُوثَّق · عملٍ غيرِ صالحٍ يُرفع للمراجعةِ قبل الحذف»*.
>
> **الحالُ عند التسليم**: `HEAD` = `7b41969a` · الفرع `repair01/w01-ownership` ·
> `git status --porcelain` = **23 سطرًا** (‏٢٠ بندًا: ثلاثةٌ منها إعاداتُ تسميةٍ
> مرحَّلةٌ تُطبع سطرَين لكلٍّ في بعضِ القراءات).

## ١ · الحكمُ على كلِّ بند

### أ · عملٌ صحيحٌ يُلتزَم — ١٦ بندًا

| # | البند | الحكمُ ودليلُه |
|---|---|---|
| ١ | `orders/MASTER_EXEC.txt` · `.docx` ⟨جديد⟩ | **أمرُ التشغيلِ الشاملُ النافذ** — المرجعُ الحاكمُ ٠ في §٠·١. مطابقٌ بايتًا لأصلِ المالك (`md5 3f34c75c…`) |
| ٢ | `orders/AMD-01.txt` · `.docx` ⟨جديد⟩ | **`AMD-01` التنفيذيُّ النافذ** — المرجعُ ١ (`md5 adf80e55…`) |
| ٣ | `orders/AMD-01_v1_amendment.{txt,docx}` ⟨إعادةُ تسمية⟩ | **وثيقةُ التعديلِ v1 مستوعَبةٌ ومنسوخةٌ** — §٠·٢. التسميةُ تمنع قراءتَها أمرًا نافذًا |
| ٤ | `orders/RPR-02_v2.3.{txt,docx}` ⟨معدَّل⟩ | **v2.3 النافذُ وحدَه** — v2.1 وv2.2 FINAL منسوختان (`AMD-01` §١·١) |
| ٥ | `orders/RPR-03.{txt,docx}` ⟨معدَّل⟩ | **`RPR-03` النافذ** — المرجعُ ٣ |
| ٦ | `orders/superseded/` ⟨جديد⟩ | **النسخُ المنسوخةُ محفوظةٌ لا محذوفة** — §٠·٢: «منسوخٌ لا يُعمل به»، والحفظُ يُبقي أثرَ النسخِ مرئيًّا |
| ٧ | `plan/MASTER_EXEC.prompt.md` ⟨جديد⟩ | **برومتُ الأمرِ الشامل** — تغطيتُه ٨٠/٨٠ بمرساةٍ لكلِّ مطلب |
| ٨ | `plan/AMD-01.prompt.md` ⟨جديد⟩ | **برومتُ `AMD-01` النافذ** — ٦٦/٦٦ |
| ٩ | `plan/AMD-01_v1_amendment.prompt.md` ⟨إعادةُ تسمية⟩ | برومتُ وثيقةِ التعديلِ v1 — ٦٣/٦٣ · مسمًّى بمنسوخِه |
| ١٠ | `tools/master_exec_prompt_reverse_review.php` ⟨جديد⟩ | **فاحصُ حملِ البرومتِ لأمرِ التشغيل** — سالبُه يحرِّك العدّادَ ٨٠→٧٩→٨٠ |
| ١١ | `tools/amd01v1_prompt_reverse_review.php` ⟨جديد⟩ | فاحصُ برومتِ وثيقةِ التعديلِ v1 — سالبُه ٦٣→٦٢→٦٣ |
| ١٢ | `tools/amd01_prompt_reverse_review.php` ⟨معدَّل⟩ | **أُعيد توجيهُه** من وثيقةِ التعديلِ إلى `AMD-01` التنفيذيِّ النافذ — سالبُه ٦٦→٦٥→٦٦ |
| ١٣ | `AMD01_PROMPT_REVERSE_REVIEW.md` ⟨معدَّل⟩ | مخرَجُ ①٢ — مولَّدٌ من تشغيلٍ حيّ |
| ١٤ | `AMD01V1_PROMPT_REVERSE_REVIEW.md` ⟨جديد⟩ | مخرَجُ ①١ |
| ١٥ | `MASTER_EXEC_PROMPT_REVERSE_REVIEW.md` ⟨جديد⟩ | مخرَجُ ①٠ |
| ١٦ | `tools/repair01_w2_apply.php` ⟨معدَّل⟩ | **«الفراغُ لا يدهس قيمة»** — صفُّ الكونِ القادمُ من مسحِ القرصِ يحمل `owner_code=''` فكانت `W02` تُفرِغ مالكَ كلِّ سطحٍ كتبته موجةٌ لاحقة، فيقرأ دفترُ كلِّ موجةٍ نطاقًا أضيقَ من نطاقِ حاجبِها (`W3-09`: ٦٩ في الدفتر و٨٣ في الاشتقاق) |

### ب · أثرُ قياسٍ يُوثَّق — بندٌ واحد

| # | البند | ما قيس |
|---|---|---|
| ١٧ | `docs/uxui_matrix_20260818.csv` ⟨معدَّل⟩ | **أثرُ ترقيمٍ لا أثرُ مضمون.** الفرقُ ١٠١ سطرًا، **وعمودُ `n` وحدَه** تغيّر: `2714..2814` ⇐ `2916..3016` (‏إزاحةُ ‎+٢٠٢). والدليلُ قاطعٌ بالقياس: `cut -d, -f2- \| sort \| md5sum` **متطابقٌ في الطرفَين** — `437cc3d58aa8c520e44f73f9ac5ba35f`. وعددُ الأسطرِ ٥٠٩ = ٥٠٩ |

### ج · فائضٌ مكرَّرٌ يُرفع حكمُه ثمَّ يُحذف — بندٌ واحد

| # | البند | الحكمُ ودليلُه |
|---|---|---|
| ١٨ | `docs/New folder (2)/` ⟨جديد · ٤ ملفّات⟩ | **مجلَّدُ إنزالٍ مكرَّرٌ بايتًا — لا عملٌ مفقود.** كلُّ ملفٍّ من الأربعةِ له توأمٌ مطابقُ البصمةِ مسجَّلٌ سلفًا في `orders/`: `أمر تنفيذ شامل.docx` = `MASTER_EXEC.docx` (`3f34c75c…`) · `AMD-01 …-4.docx` = `AMD-01.docx` (`adf80e55…`) · `RPR-02 v2.3 …-4.docx` = `RPR-02_v2.3.docx` (`d1e86754…`) · `RPR-03 …-4.docx` = `RPR-03.docx` (`fd22f2f6…`). ⇒ **الحذفُ لا يُفقِد بايتًا واحدًا**، والمضمونُ محفوظٌ في `orders/` تحت إدارةِ الإصدار |

⛔ **ولا بندَ في الصنفِ «عملٌ غيرُ صالح»** — فلم يُحذف شيءٌ فيه عملٌ لا مثيلَ له.

## ٢ · تصحيحٌ رافقَ الإقفال — التقريرُ يسمّي مصدرَه لا سلفَه

**العطب**: عنوانُ تقريرِ المراجعةِ العكسيّةِ والأمرُ المولِّدُ له كانا **منسوخَين
نصًّا** من أوّلِ نسخةٍ في هذه العائلة. فصدر `MASTER_EXEC_PROMPT_REVERSE_REVIEW.md`
معنونًا **«لبرومتِ `AMD-01`»** ومنسوبًا إلى **`amd01_prompt_reverse_review.php`** —
أداةٍ لم تُشغَّل لإنتاجِه. وهذا **تناقضٌ داخلَ التقريرِ نفسِه**: صدرُه يكذّب متنَه.

**والعلاجُ في الأداةِ لا في المخرَج**: `REV_LABEL` تُشتقّ من `basename($ORDER)`
والأمرُ المولِّدُ من `basename(__FILE__)` — **فلا يستطيع التقريرُ أن ينتحل اسمَ
غيرِه بعدَ اليوم**. وأُعيد توليدُ الثلاثةِ من تشغيلٍ حيّ.

## ٣ · الحصيلة

| المقياس | القيمة |
|---|---|
| بنودٌ صُنِّفت | **٢٠ / ٢٠** |
| عملٌ صحيحٌ التُزم | ١٦ |
| أثرُ قياسٍ وُثِّق | ١ |
| فائضٌ مكرَّرٌ حُذف بحكمٍ مسجَّل | ١ (‏٤ ملفّات · صفرُ بايتٍ مفقود) |
| عملٌ غيرُ صالحٍ رُفع | ٠ |
| `git status --porcelain` بعد الإقفال | **صفر** |

## ٤ · حاجزٌ حقيقيٌّ اعترض الإقفال — ورُفع بعلاجٍ لا بتجاوز

**ما وقع**: أوّلُ محاولةِ التزامٍ رسبت على حزامِ الشواهد:

```
✘ ارتداد — شاهدُ مطلبٍ مُغلَقٍ بالدليلِ أحمرُ الآن:
   · tests/injfrd01_gov006_finding_intake.php
```

**والسببُ مقيسٌ لا مظنون**: `FR-GOV-006` معيارُه *«صفرُ كشفٍ مُهمَلٍ لغيابِ رمزٍ
يقابله مطلب»*. و`docs/baseline_20260821/FINDINGS.md` أضافت في الالتزام
`56203bfe` **أربعةَ كشوفٍ** تستشهد بـ`GAP-73` و`GAP-74` و`GAP-18` و`GAP-10`،
**ولم يُكتب لأيٍّ منها مطلبٌ في الدفترِ الرسميّ** — فصار الكشفُ مكتوبًا ولا
مالكَ له. ⇒ **المعيارُ مخروقٌ حقًّا، والحزامُ صادق.**

**⛔ والحاجزُ سابقٌ لهذه الجلسة**: الكشوفُ دخلت في `56203bfe` — ثلاثةَ التزاماتٍ
قبلَ `HEAD` — ولم يمسَّها هذا العمل. فما قِستُه ارتدادٌ **مُعلَنٌ متأخِّرًا** لا
ارتدادٌ أحدثتُه.

**والعلاجُ بالقانونِ المحروقِ نفسِه** — `gap-codes-register-not-prose`:
*«فجوةٌ في السردِ لم تُكتب في الدفترِ تُيتِّم كشوفَها»*. والعُدّةُ موجودةٌ سلفًا
(`tools/repair01_gap_register.php`، بُنيت لوقوعِ الدرسِ مرّةً ثانيةً على
`GAP-67..71`) ⇒ **وُسِّع مواصفُها بأربعةِ مطالبَ منقولةٍ من الكشفِ ومن نصِّ
الأمرَين** لا مؤلَّفةٍ:

| المطلب | الفجوة | الأولوية | مصدرُه الحاكم |
|---|---|---|---|
| `FR-GOV-012` | `GAP-73` | P1 | `F-G05` · `RPR-02` §٥·٩ — قيمةُ الملكيّةِ تتبع قاعدةَ حكمِها |
| `FR-GOV-013` | `GAP-74` | P2 | `F-G06` · `RPR-02` §١١ — مقامُ تقريرِ الفرقِ يشمل ما مسَّه الفعل |
| `FR-GOV-014` | `GAP-18` | P1 | `F-G08` · `RPR-03` §٣·٢ — حكمٌ ذو شقَّين لا يُغلق بشقٍّ واحد |
| `FR-GOV-015` | `GAP-10` | P0 | `F-G09` · `RPR-03` §٦ — كلُّ قناةٍ حسّاسةٍ تمرُّ بالمقرِّر |

⛔ **وحالةُ الأربعةِ `OPEN`** — فالتسجيلُ يعطي الكشفَ مالكًا ولا يُغلق فجوة.
⛔ **ولم يُحرَّر كشفٌ ليمرَّ فاحص**: الكشفُ دليلٌ مقيسٌ لا يُعدَّل ليُرضيَ بوّابة.

**والقياسُ بعدَ العلاج**: `FR-GOV-006` **٥ نجاح · ٠ رسوب** (‏كان ٤ و١) ·
والفجواتُ المربوطةُ بمطلبٍ ٣٥ ⇐ **٤٤** · وحزامُ الشواهدِ **٢٠ خضراء · ٨ حمراءَ
مُعلَنةً · صفرُ خلل** (‏كان ١٩ و٩ وخللًا واحدًا).

> **⚠ وأثرٌ على مقامٍ مجاورٍ يُعلَن ولا يُخبَّأ**: دفترُ `INJ-FRD-REM-01` صار
> **٧٠ مطلبًا لا ٦٦**. فكلُّ نسبةٍ منسوبةٍ إلى ٦٦ تُعاد قسمتُها على ٧٠ عند
> اقتباسِها بعدَ هذا الالتزام — **ولا يُنقل رقمٌ قديمٌ حالةً راهنة**.
