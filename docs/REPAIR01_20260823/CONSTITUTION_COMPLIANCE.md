# CONSTITUTION_COMPLIANCE — امتثالُ النظامِ لموادِّ الدستور (الملف ٠٣ · النسخة -3)

> **Baseline_ID:** `BL-20260901-13010626` · أمرُ `GOV_EXEC` §7: «حوِّلْ موادَّه إلى اختباراتِ امتثال».
> **بنيةُ هذا السجلِّ بنيةُ مصدرِه**: ورقةُ `00_الدستور_والمقامات` تسوق الموادَّ
> **مفردةً** (م101–م122) **وعناقيدَ** تجمع بقيّةَ الموادِّ المرقَّمةِ بموضوعِها —
> فكلُّ صفٍّ هنا يقابل صفَّ المصدرِ حرفًا، ولا مادّةَ مخترَعةً خارجَه.
> **الحكمُ ثلاثيّ**: ✔ فحصٌ حيٌّ يمثِّله (بوّابة/شاهد/قيدُ مخطَّط) · ◐ مثبَتٌ
> بنيويًّا وفحصُه الحيُّ بندُ سجلٍّ مسمًّى · Ⓝ حكمُ N/A بسندِه.

## ⓐ الموادُّ المفردة (م101–م122)

| المادّة | نصُّها المختصر | الحكم | الفحصُ/الدليلُ الحيّ |
|---|---|---|---|
| م101 | السطحُ مصدرُ حقيقةٍ يحمل أربعًا (الحبّة·المالك·الكيان·المعرِّف) وبوّابةٌ تمنع سطحَين لكيانٍ وحبّة | ✔ | عقودُ CTL2 الخمسة (206/206 مصنَّفةً بحبّتها) · قيدا `chk_grain_witness`/`chk_sot_witness` في المخطَّط · شاهدُ م114 ② يمنع الملفَّ الثاني للحبّةِ الواحدة |
| م102 | تقريرُ الجمعِ إسقاطٌ لا مالك | ✔ | صنفا `DOMAIN_PROJECTION`/`EXECUTIVE_PROJECTION` في السجلِّ + لوحة rpr03 (لا تقريرَ مصدرَ حقيقة = 0) |
| م103 | سطحُ الجميعِ إسقاطٌ موحَّدٌ معلَّمٌ بالنطاق | ✔ | `PLATFORM_SHARED` بمعاييرِه الأربعة (T13 §٥·٤) — 12 سطحًا مسجَّلًا |
| م104 | لا دمجَ تبويبًا إلا بإثباتِ الأبِ والحبّةِ والصلاحية — ولا تُضحّى الصلاحيةُ لجمالِ السايدبار | ✔ | تصنيفُ `TAB_CHILD` بقاعدةٍ مكتوبةٍ في `source_ref` كلِّ موضعٍ + `ROLE_VISIBILITY_CONFORMANCE` 187/187 |
| م105 | إغلاقُ النقصِ بتغطيةِ المؤهَّلِ لا بالعدد | ✔ | منهجُ بوّاباتِ UXUI التسعِ (المقامُ المؤهَّلُ في كلِّ بوّابة) وشاهدُ حارسِ المفردات [[measure-token-must-exist]] |
| م106–107 | موانعُ تشغيلِ المصالحةِ الثلاثة وفرقُ تشغيلِها عن إغلاقِها | ✔ | `baseline_reconcile` (يعمل بالاستثناءاتِ المعلنة) + `UNCONTRACTED_CROSS_DOMAIN_WRITE=0` (rpr03 #9) + `WRONG_OWNER` أُغلق في NAVR |
| م108 | لا لقطةَ قياسٍ والبناءُ جارٍ | ✔ | عقدُ GATE-00: شجرةٌ نظيفةٌ و`HEAD` ثابتٌ قبل/بعد التشغيلةِ الحاكمة (هذه اللقطةُ نفسُها كذلك قُصَّت) |
| م109 | لا اسمَ معروضًا غيرَ معتمدٍ والحارسُ يُختبر سلبًا | ✔ | `nav_canonical` (APPROVED يغلب — [[nav-label-four-source-precedence]]) وبوّابةُ الأسماءِ في UXUI بشاهدِها السالب |
| م110 | لا يحقّق أحدٌ في نفسِه — والمراجعةُ بتكليفٍ مكتوب | ◐ | بنيةُ IAF المستقلّةُ (ورقتُها ومساحتُها ودورُها 33) قائمةٌ — وفحصُ التكليفِ بالحالةِ بندُ حملةِ IAF عند تشغيلِها الحيّ |
| م111 | الصفرُ المقيسُ يُكتب صفرًا لا فراغًا | ✔ | عطبُ `fromArray` أُصلح في عُدّةِ XLSX [[xlsx-zero-written-blank]] — والتوليدُ يُفحص بصفرٍ واحدٍ مقيس |
| م112 | كلُّ حقلٍ يحمل مفتاحَ مصدرِه | ✔ | `repair01_fields.src_ref` لكلِّ حقلٍ (سُوّي على مواضعِ الحاكمِ -2 · 7,611) — وسياسةُ الحساسيةِ على المفتاحِ لا الاسم (القنواتُ التسع 15/15) |
| م113 | `G0` تمنع البناءَ الجديدَ لا المصالحةَ المعماريّة | ✔ | حكمُ `G0-04` (صفةُ قرارٍ لا حالة — [[repair01-w13-people]]) والمصالحاتُ كلُّها جرت تحته |
| م114 | الإدارةُ تملك · الدورُ ينفّذ · النطاقُ يصفّي — ولا نسخَ شاشةٍ لنطاق | ✔ | **شاهدٌ جديدٌ** `tests/gov_m114_scope_filter_proof.php` (7/7: 366 مسارًا مشتركًا · صفرُ نسخةِ حبّةٍ · مِجَسٌّ سالب) — وشاشةُ الإسنادِ الجديدةُ نفسُها تجسيدُ المادّة |
| م115 | اتجاهُ الحقيقةِ واحدٌ ولا هدفَ من الوضعِ القائم | ✔ | `TARGET_DERIVED_FROM_CURRENT_WITHOUT_RULING = 0` (المقياسُ الرسميُّ يتولّد كلَّ تشغيلة) ومصالحةُ الإرثِ 725/725 «يقترح ولا يعتمد نفسَه» |
| م116 | السجلُّ الحاكمُ يجيب أربعةً أو يُخفَّض | ✔ | `GOVERNING_REGISTRY_WITHOUT_PRODUCTION_READER = 0` — وحكمُ تخفيضِ `EVT-SUBS` سابقةُ التطبيق |
| م117 | لا علاقةَ عالميّةً لحقيقةٍ سياقيّة | ✔ | نموذجُ `nav_placements` (بدل `route→group` العالمي) و`uq_one_primary` — و`CONTEXT_CARDINALITY`: كشفا النمطِ بندان (`CL-PAT-USERROLE` مفتوحًا بفعلِه) |
| م118 | لا سقوطَ صامتًا لمسارٍ قديم | ✔ | `GLOBAL_FALLBACK=0` + `gov_nav_findings` + `EMS_NAV_STRICT` + بديلُ الإرثِ معلَنٌ مقيسٌ (`LEGACY_TARGET_RUNTIME_READ_COUNT=0`) وmonitor السلّمِ بمفتاحِه |
| م119 | كلُّ مقياسٍ يحمل ثمانيَه | ✔ | لوحةُ `NAVR_METRICS` (سؤال/بسط/مقام/استبعاد/مصدر/لقطة/اتجاه/معيار في كلِّ صفّ) — ولوحاتُ rpr02/rpr03 بمقاماتِها المنفصلة · `UNEXPLAINED_METRIC_EXCLUSION=0` |
| م120 | حالاتُ الإغلاقِ الستُّ لا تُقفَز | ✔ | مستوياتُ السجلِّ الجامعِ الستةُ لكلِّ بندٍ (Decision→…→Evidence_Closed) — و`IMPLEMENTED_NOT_VERIFIED` حالةٌ محترمةٌ لا تُقفز (23 بندًا واقفةً على نوافذِها) |
| م121 | نسبُ الهدفِ لا ينقطع — معرِّفٌ ثابت | ✔ | `nav_targets` (NT-*) و`gov_req_id_recon` (**19 حكمَ مصالحةٍ بالاسمِ المطبَّعِ لانزياحِ الحزمةِ -3**) وشاهدُ `gov_exec_wh_recon` ⑨ 5/5 |
| م122 | لكلِّ كونٍ مصدرٌ في خريطةِ المصادر | ✔ | `GOVERNING_SOURCE_MAP.md` **طوبقت بورقةِ `J` حرفًا** — صفوفُها العشرةُ كلُّها ممثَّلةٌ (أُكملت DECISIONS · CONSTITUTION · CURRENT-STATE في هذه الجولة) و`TARGET_WITHOUT_GOVERNING_SOURCE=0` |

## ⓑ العناقيدُ كما ساقها المصدر

| صفُّ المصدر | الحكم | الفحصُ/الدليلُ الحيّ |
|---|---|---|
| م2·90 — مالكٌ واحدٌ ومستهلكون بلا نسخٍ يدويّ | ✔ | بوّابةُ `TenantDb`/`TenantRegistry` (573 إعلانَ جدولٍ) والمراجعُ FK — وسقّاطةُ الاستعلامِ الخامِّ تمنع مسارًا ثانيًا جديدًا |
| م7·5 — Grain ذرّيّ وكلُّ 1:N في Child | ✔ | عقودُ الحبّةِ (CTL2 206/206) — وشاشةُ الإسنادِ الجديدةُ Child Register بنصِّ ورقتِها |
| م8·83 — كلُّ دورةٍ آلةُ حالةٍ ولا Status حرّ | ✔ | `RPR-02 #4 = 100%` (`BUILT_TRANSACTION_WITHOUT_REQUIRED_STATE_MODEL=0`) والمفرداتُ المغلقةُ (`chk_cyc_bridge` وأخواتُها) — والجديدُ أخذ حكمَه (حالاتُه قائمةٌ محكومةٌ بقيدَي مخطَّط) |
| م9·10·53 — البوّابةُ في الخدمةِ والاختبارُ السالبُ إلزاميّ | ✔ | `ems_post_contract` (CSRF+فعلٌ مسجَّلٌ+صلاحية+Idempotency) وسوالبُ الحزامِ في كلِّ شاهدٍ (نمطُ «يكسر فيُرى») |
| م11 — سجلُّ رفضٍ موحَّد | ✔ | `log_security_event` بأسطحِه الأربعة [[security-log-screen]] — `UNREGISTERED_ACTION_DENY` وأخواتُها |
| م12·49 — التكاملُ ثلاثةُ أنواعٍ لا تختلط | ✔ | خريطةُ المصادر (Reference/Event/Command بصفوفِها) وعقودُ `EffectLinkConsumer` ومصفوفةُ `D` — و`EFFECT_MISSING=0` نمطًا |
| م16·85·86 — لا تعديلَ مباشرًا لحقيقةِ غيرِك والإلغاءُ بتعويض | ✔ | `rpr03 #9 UNCONTRACTED_CROSS_DOMAIN_WRITE=0` + `immutable_key` في البوّابة (الحصانةُ حتى على السوبر) + نمطُ التعويضِ في الناقل (`Compensated`) |
| م17·39 — الاعتمادُ محرّكٌ واحدٌ بلا Hardcode | ◐ | المحرّكُ (`gov_ladders`) مبنيٌّ والحلُّ وقتَ التنفيذِ بالمفاتيح — **والقيمُ محجوبةٌ على قرارِ المالك OA-06** (نافذةُ الظلِّ قبل الإنفاذِ بنصِّ م) |
| م18·19·21 — منعُ التجزئةِ وفصلُ الميزانية/الالتزام/الدفع | ◐ | كياناتُ الدورةِ الماليّةِ قائمةٌ (موجاتُ H/M) — وفحوصُ التجزئةِ بمفتاحٍ ونافذةٍ ضمن حملةِ الماليّةِ P2 بترتيبِ السجلّ |
| م20 — حدودُ الماليّةِ والخزينة | ✔ | دفاترُ W11 بفصلِها (`fin_project_scope` fail-closed) وفصلُ الواجبات 92/92 |
| م23·24·25 — الإقفالُ كيانٌ · Current≠History · لا Overwrite بعد اعتماد | ✔ | كياناتُ الإقفالِ (شهر×مخزن وأخواتُها) · إسقاطُ التعديلاتِ الحتميُّ [[contract-amendments-projection]] · وترتيبُ الحقائقِ بتاريخِ التزامٍ [[repair01-w16-baseline]] |
| م26·27·84 — التحكّمُ المستنديُّ الموحَّد | ◐ | حارسُ انتهاءِ المستند (E-08) والمرفقاتُ بمراجعِها — وتعميمُ `Document_ID` كتالوجًا موحَّدًا بندُ P2 |
| م32·42·72·73 — المشتقُّ صفرُ إدخالٍ والتقاريرُ من المصادر | ✔ | صنفُ `DERIVED` محكومٌ (المحظورُ DERIVED+Editable في رأسِ ورقةِ الحقول) — وشاشةُ الإسنادِ تجسّده (النافذُ اليومَ يُشتقّ ولا يُكتب) |
| م37·38·81·82 — قواميسُ معياريّةٌ لكلِّ قيمةٍ بيتٌ واحد | ✔ | `nav_canonical` · قاموسُ الأفعال `nav09_action_map` · `gov_columns` المركزيّة · والقوائمُ المحكومةُ تُقرأ من مخطَّطِها (نمطُ الشاشةِ الجديدة) |
| م45·47·48·88 — الأثرُ الماليُّ Idempotent ولا تكاملَ صامتًا | ✔ | مفاتيحُ منعِ التكرارِ (`ems_pc_idem`/'idempotency_key') وحالاتُ التسليمِ الكاملةُ في `ems_event_deliveries` بنبضِ `cron_events` المعلَن |
| م74·75·76 — أمنُ الحقلِ والأثرُ الحسّاسُ Append-Only | ✔ | `SensitiveFieldGuard`/`FieldGovernor` (القنواتُ التسع 15/15 · `ACTIVE_SENSITIVE_FIELD_BYPASS=0`) وكتلُ AUDIT في كلِّ حبّة |
| م78·79·80 — لكلِّ قاعدةٍ سريانٌ وإصدار | ◐ | السلالمُ والسياساتُ تحمل إصدارَها — وفحصُ «يُطبَّق النافذُ لحظةَ الحدث» عبرَ الأسطحِ الماليّةِ بندُ حملةِ P2 |
| م54·56·95 — الإغلاقُ خمسُ مراحلَ والنسبُ منفصلة | ✔ | السجلُّ الجامعُ بمستوياتِه — **ولا تُجمع المقاماتُ في نسبةٍ واحدة** (قاعدةُ كلِّ لوحاتِنا المنشورة) |

## ⓒ الحصيلة

- **المفردةُ م101–م122**: 21 ✔ · 1 ◐ (م110 — فحصُ التكليفِ الحيُّ ببند حملةِ IAF).
- **العناقيد**: 13 ✔ · 4 ◐ — وكلُّ ◐ بندُ سجلٍّ مسمًّى لا فراغَ (OA-06 · حملةُ الماليّة P2 · كتالوجُ المستندات · سريانُ القواعد).
- **صفرُ مخالفةٍ تنفيذيّةٍ مقيسةٍ بلا حكم** — وكشفُ تصادمِ التسميةِ الستّةُ قُيّد `CL-PAT-DUPLABEL`.
