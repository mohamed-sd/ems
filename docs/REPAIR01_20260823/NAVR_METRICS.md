# NAVR — المقاييسُ العشرةُ المنفصلة

> ⛔ مولَّدٌ من التشغيلةِ نفسِها التي ولّدت `SIDEBAR_GUIDE_COMPARE.md` @ `f592cdf9` · 2026-08-31 20:56
> ⛔ **ولا تُجمع في نسبةٍ واحدة** — كلُّ مقياسٍ بمقامِه.

| المقياس | القيمة | المقام والقراءة |
|---|---|---|
| `TARGET_BUILD_COVERAGE` | **240/374** | مواضعُ الدليلِ المربوطةُ بشاشةٍ مبنيّة — والباقي `NOT_BUILT` بموضعِه المستهدَفِ المسجَّل |
| `RENDERED_TARGET_COVERAGE` | **182/202** | المُصيَّرُ من أهدافِ بنودِ القائمةِ المبنيّة (بعد استبعادِ التصنيف) |
| `BUILT_NOT_RENDERED` | **20** | مبنيٌّ بدفترِه ولا بندَ في سايدبارِ دورِه — عطبٌ يُعالَج |
| `GROUP_CONFORMANCE` | **143/182** | المُصيَّرُ في مجموعةِ دليلِه |
| `ORDER_CONFORMANCE` | **150/182** | المُصيَّرُ في ترتيبِ دليلِه (LCS) |
| `ROLE_VISIBILITY_CONFORMANCE` | **163/163** | موضعُ قائمةٍ مبنيٌّ لدورِ مساحتِه صلاحيةُ عرضِه قائمة |
| `GROUP_HEADER_COVERAGE` | **81/88** | رؤوسُ مجموعاتِ الدليلِ الظاهرةُ (المقامُ بعد استبعادِ ما كلُّ شاشاتِه غيرُ مبنيّة) |
| `EXACT_DEPARTMENT_NAV_CONFORMANCE` | **2/17** | إداراتٌ مطابقةٌ تمامًا فيما بُني (الهدفُ النهائيُّ 100٪) |
| `GLOBAL_FALLBACK_COUNT` | **0** | سقوطُ مساحةِ أعمالٍ للتصنيفِ العامّ — الهدف 0 (من `gov_nav_findings`) |
| `TARGET_DERIVED_FROM_CURRENT_COUNT` | **682** | صفوفُ `gov_target_nav` المؤلَّفةُ من التصيير (`RENDER-ALIGN`) — طبقةٌ ساقطةُ الصلاحيّةِ كهدفٍ، والحاكمُ `nav_placements` من الورقة |
| `TARGET_DERIVED_FROM_CURRENT_WITHOUT_RULING` | **0** | صفوفُ الإرثِ **بلا حكمِ مصالحةٍ** في `gov_legacy_nav_recon` — الهدف 0 (§٤ من أمرِ الحوكمة) |
| `UNIQUE_TARGET_SCREEN_BUILD_COVERAGE` | **222** شاشةً فريدة | الشاشاتُ المتمايزةُ خلف المواضعِ المبنيّة (§٢٤: Screens ≠ Placements) |
| `TARGET_PLACEMENT_BUILD_COVERAGE` | **240/374** | مواضعُ الأهدافِ المبنيّة — الشاشةُ الواحدةُ قد تحمل أكثرَ من موضع |
| `ROLE_VISIBILITY_EXPLICIT_NA` | **0** | مستبعَدو الرؤيةِ المسمَّون بسببِهم (المعادلة: RENDERED_APPLICABLE = TESTED + EXPLICIT_NA) |
| `STRUCTURAL_NAV_PASS` | **2/17** | إداراتٌ بلغت المطابقةَ البنيويّةَ — **وكلُّ بالغةٍ تدخل HUMAN_NAV_VERIFICATION فورًا** (§٢٦: لا انتظارَ 17/17) |
| `HUMAN_NAV_PASS` | **0/2** من المؤهَّلات | التحقُّقُ البشريُّ بدورٍ حقيقيٍّ — وDEP-08 عند أهليّتِها `BLOCKED_ROLE_BINDING` لا PASS مزوَّر (§٢٧) |
| `LEGACY_TARGET_RUNTIME_READ_COUNT` | **0** | مساحةُ أعمالٍ مهاجرةٌ ما زال تصييرُها يقرأ سلطةَ الإرث — الهدف 0 (§٢١) |
| `BUILT_SCREEN_WITHOUT_TARGET_LINEAGE` | **284** (المقام 480 · بنسبٍ 196) | Baseline §١٠: بنودُ قوائمِ المساحاتِ المهاجرةِ بلا نسبِ هدفٍ (`target_id`) — **قياسُ أساسٍ لا هدفُ إغلاقٍ بعد**؛ وغيرُ المنسوبِ أدواتٌ ومراسٍ وبنودٌ خارجَ الورقةِ تُصالَح تباعًا |
| `UNEXPLAINED_METRIC_EXCLUSION` | **0** | كلُّ استبعادٍ في هذه اللوحةِ مكتوبُ السببِ في خانتِه (§٨ من أمرِ الحوكمة) |

مصنَّفٌ خارجَ مقامِ السايدبار (تبويب/إسقاط — المطلوب ١٠): **12** هدفًا.

> **معادلةُ النسب (§١٠)**: Applicable 480 = With 196 + Without 284 — وكلُّ Without مفسَّرٌ صنفًا (أداة/مرساة/خارج الورقة) في سجلِّ المصالحة.
