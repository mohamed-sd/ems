# NAVR — المقاييسُ العشرةُ المنفصلة

> ⛔ مولَّدٌ من التشغيلةِ نفسِها التي ولّدت `SIDEBAR_GUIDE_COMPARE.md` @ `b0d2c3e9` · 2026-08-31 18:59
> ⛔ **ولا تُجمع في نسبةٍ واحدة** — كلُّ مقياسٍ بمقامِه.

| المقياس | القيمة | المقام والقراءة |
|---|---|---|
| `TARGET_BUILD_COVERAGE` | **240/374** | مواضعُ الدليلِ المربوطةُ بشاشةٍ مبنيّة — والباقي `NOT_BUILT` بموضعِه المستهدَفِ المسجَّل |
| `RENDERED_TARGET_COVERAGE` | **171/201** | المُصيَّرُ من أهدافِ بنودِ القائمةِ المبنيّة (بعد استبعادِ التصنيف) |
| `BUILT_NOT_RENDERED` | **30** | مبنيٌّ بدفترِه ولا بندَ في سايدبارِ دورِه — عطبٌ يُعالَج |
| `GROUP_CONFORMANCE` | **131/171** | المُصيَّرُ في مجموعةِ دليلِه |
| `ORDER_CONFORMANCE` | **140/171** | المُصيَّرُ في ترتيبِ دليلِه (LCS) |
| `ROLE_VISIBILITY_CONFORMANCE` | **161/163** | موضعُ قائمةٍ مبنيٌّ لدورِ مساحتِه صلاحيةُ عرضِه قائمة |
| `GROUP_HEADER_COVERAGE` | **77/88** | رؤوسُ مجموعاتِ الدليلِ الظاهرةُ (المقامُ بعد استبعادِ ما كلُّ شاشاتِه غيرُ مبنيّة) |
| `EXACT_DEPARTMENT_NAV_CONFORMANCE` | **0/17** | إداراتٌ مطابقةٌ تمامًا فيما بُني (الهدفُ النهائيُّ 100٪) |
| `GLOBAL_FALLBACK_COUNT` | **0** | سقوطُ مساحةِ أعمالٍ للتصنيفِ العامّ — الهدف 0 (من `gov_nav_findings`) |
| `TARGET_DERIVED_FROM_CURRENT_COUNT` | **682** | صفوفُ `gov_target_nav` المؤلَّفةُ من التصيير (`RENDER-ALIGN`) — طبقةٌ ساقطةُ الصلاحيّةِ كهدفٍ، والحاكمُ `nav_placements` من الورقة |

مصنَّفٌ خارجَ مقامِ السايدبار (تبويب/إسقاط — المطلوب ١٠): **12** هدفًا.
