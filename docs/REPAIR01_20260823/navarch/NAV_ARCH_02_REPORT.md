# `NAV-ARCH-02` — الردُّ المطلوبُ بنصِّ §43

> ⛔ **لا «تمَّ تنظيفُ السايدبار»**. وكلُّ رقمٍ أدناه يُنتجه مقياسٌ يُعاد
> تشغيلُه، ومصدرُه مكتوبٌ بجانبِه.

## ① معرِّفُ الأساس

| | |
|---|---|
| `Baseline ID` | **`NAB-8B35EC2C05`** |
| `Commit Hash` | `73b9aaa1152456a2c349e92745ff4d949e60125e` |
| `DB Snapshot` | `e505111dd084c6a2…` · 711 هجرة |
| `Navigation Registry Version` | `7fec35930978b5f3…` |
| `Role Permission Version` | `805e0bd8c6130b35…` · 4112 منحة |
| `Target Architecture Version` | `nav_placements@7fec35930978` |
| `Timestamp` | `2026-09-01T10:33:02Z` |
| المولِّد | `tools/navarch/baseline.php` — لقطةٌ واحدةٌ (§5) |

## ② سجلُّ المساحات

`nav_workspaces` = 22 مساحةً بأعمدةِ §6 الستّةِ المضافةِ بهجرةِ `2028_04_14_navarch02_registers` (‏ولها `_down.php` مُثبَتُ العكس). والأنواعُ: `DEPARTMENT` 17 · `EXECUTIVE` 2 · `PERSONAL` 1 · `PLATFORM_UTILITY` 1 · `INDEPENDENT_ASSURANCE` 1. و`IAF` نُقلت من `DEPARTMENT` إلى `INDEPENDENT_ASSURANCE` بنصِّ §6.

## ③ مخطَّطُ الموضعِ الفعليّ

**`nav_workspace_placements`** — سجلٌّ **جديدٌ** بنصِّ §8، فيه 623 موضعًا بأصنافِ §9 التسعة:

| الصنف | العدد |
|---|---:|
| `PRIMARY` | 313 |
| `PERSONAL` | 214 |
| `TAB_CHILD` | 43 |
| `GLOBAL_SHELL` | 36 |
| `CONTEXTUAL_ACTION` | 13 |
| `EXECUTIVE_PROJECTION` | 4 |

⛔ **ولم يُمَسَّ `nav_placements` القائم** (413 صفًّا · `MENU_ITEM` 329). ولو
فُكِّكت مفرداتُ عمودِه في مكانِها **لسقطت صفوفٌ صامتةً عند سبعةَ عشرَ قارئًا**
يعُدُّ `MENU_ITEM` بالاسم — وهو عطبٌ مقيسٌ حرفًا في هذه الشجرةِ من قبل
(‏17/17 ⇒ 0/17 **بلا تغيُّرِ صفٍّ واحد**). فالقديمُ يبقى لقرّائه، والتقاعدُ
بمراحلِ §33 لا بهجرةٍ واحدة.

## ④ وصفُ المُصيِّرِ القديمِ — والجذرُ المؤكَّد

`includes/unified_nav.php` · `getUnifiedNavItems()` تُرجِع **كلَّ صفوفِ الدورِ في**
`nav_items` **بشرطِ `can_view`** ثمَّ تُطبَع. أي حرفًا:

```
Rendered Sidebar = All Authorized Screens        ⛔ الممنوعةُ بنصِّ §20
```

**فالصلاحيّةُ كانت تُنشئ موضعًا.** والمقيس: `nav_items` النشطةُ 2476 صفًّا، وسايدبارُ التشغيلِ 90 رابطًا مقابلَ 12 في ورقةِ الدليل.

والسقوطُ الثاني: `emsNavTaxonomy()`/`nav_route_group` — **مفتاحُه المسارُ وحدَه**
فمسارٌ واحدٌ يأخذ مجموعةً عالميّةً واحدةً مهما اختلف سياقُه.

## ⑤ الـ89 في `DEP-11` — قبلُ، بتصنيفِ كلٍّ

| الطبقة | العدد | الحكمُ الآليّ |
|---|---:|---|
| ① الدليل | 12 | `PRIMARY` · `KEEP_PRIMARY` |
| ② المرساة | 2 | `GLOBAL_SHELL` (§10) |
| ③ الشخصيّة | 6 | `PERSONAL` ⇒ `WS-MY` (§11) |
| ④ المستعارة | 43 | على قواعدِ §12 الخمس |
| ⑤ الإرث | 26 | `LEGACY_NAVIGATION_DISPOSITION` (§15) |
| **المجموع** | **89** | — مطابقٌ لرقمِ §1 حرفًا |

## ⑥ `DEP-11` بعدُ — تصييرُ الظلِّ بأعدادِه

| | العدد |
|---|---:|
| Primary | 16 |
| Secondary Approved | 0 |
| Global Shell | 2 |
| Personal | 14 |
| تبويبٌ تابع | 1 |
| مُسيَّق (‏إسقاطُ قراءةٍ/فعلٌ سياقيّ) | 8 |
| مُحوَّل (`REDIRECT`/`REPLACE`) | 6 |
| مُتقاعَدٌ فعلًا | **0** — ⛔ ولا مسارَ أُوقف (§33) |
| `TARGET_GAP_CANDIDATE` | **0** — ولا هدفَ ناقصٌ في `DEP-11` |
| مُصعَّدٌ إلى `L2` مالكِ المجال | 6 |
| مُصعَّدٌ إلى `L4` المالك | **0** لِـ`DEP-11` |

⛔ **والعددُ النهائيُّ لم يُحدَّد مسبقًا** (§29): بلغ 16 بندًا في الدورة — لا 12 ولا 89 — والمعيارُ `UNEXPLAINED_EXTRA = 0`.

## ⑦ الاختباراتُ السالبةُ الثمانية (§39)

`tests/navarch02_negative_tests.php` — **9 نجاح · 0 رسوب**. ولكلِّ اختبارٍ ضِلعان:
يرفض الممنوعَ **ويقبل المسموحَ** (‏فحارسٌ يرفض كلَّ شيءٍ أخضرُ كاذب). والعطبُ
مدسوسٌ بمفردةٍ فريدةٍ في مساحةٍ صندوقيّةٍ `ZZ-NT` تُمحى — ⛔ **ولم يُمَسَّ صفٌّ حيٌّ**
**ولا مُنِعت صلاحيّةٌ واحدة.**

## ⑧ التحقّقُ البشريّ (§31)

`HUMAN_UAT_PASS` = **PENDING**. ورقةُ 10 في المصنَّفِ تحمل نقاطَ §31 العشرَ
بخطوةٍ ومتوقَّعٍ لكلٍّ، ومعها الحالةُ الآليّةُ المُثبَتةُ سلفًا. ⛔ **ولا يُكتب**
**«نجح» عن إنسانٍ لم يجرِّب** — وهو البندُ الوحيدُ الباقي في §40.

## ⑨ المقاييسُ قبل/بعد

| | قبل | بعد |
|---|---:|---:|
| ظهوراتُ السايدبارِ في 18 مساحة | **1192** | **542** |
| `DEP-11` | 90 | 16 في الدورة |
| `TARGET_NAV_RECALL` (‏DEP-11) | 100٪ | 100٪ — **ولا يُعَدُّ مطابقةً (§24·§42)** |
| `PLACEMENT_PRECISION` (‏DEP-11) | — لم يكن يُقاس | 100٪ |
| `EXACT_WORKSPACE_NAV_CONFORMANCE` | — لم يكن موجودًا | **18/18** مساحةً `PASS` |
| `GLOBAL_FALLBACK_COUNT` | غيرُ مقيسٍ (‏السقوطُ قائم) | **0** |
| `LEGACY_FALLBACK_RENDER_COUNT` | غيرُ مقيسٍ (‏السقوطُ قائم) | **0** |

## ⑩ الظلُّ — الأعدادُ الستّةُ (§30)

| مُبقًى | منقول | مُسيَّق | مُحوَّل | مُزال | يحتاج قرارًا | المجموع |
|---:|---:|---:|---:|---:|---:|---:|
| 301 | 241 | 189 | 71 | 315 | 63 | **1180 = 1180** |

**والمقامُ مغلق**: لكلِّ ظهورٍ حكمٌ واحدٌ — لا صفرَ ولا اثنان.

## ⑪ خطّةُ تعميمِ الـ17 (§36)

**والشرطُ الحاكمُ قبلَ أيِّ خطوة** (‏§36 حرفًا): «لا يبدأ التعميم قبل إثبات أنَّ
`Renderer` الجديد **لا يحتاج `Legacy fallback`**» — وهو **مُثبَتٌ الآن**:
`GLOBAL_FALLBACK_COUNT = 0` و`LEGACY_FALLBACK_RENDER_COUNT = 0` في 18 مساحةً، 
و`NT-03` يُثبت أنَّ إرثًا فعّالًا بلا موضعٍ **لا يُصيَّر**.

| الموجة | المدى | شرطُ الدخول | شرطُ الخروج |
|---|---|---|---|
| **0 — الطيّار** | `DEP-11` | مُنجَزٌ بنيويًّا 11/11 | `HUMAN_UAT_PASS = YES` |
| **1** | 17 مساحةً خضراءَ بنيويًّا سلفًا: `DEP-01` · `DEP-02` · `DEP-03` · `DEP-04` · `DEP-05` · `DEP-06` · `DEP-07` · `DEP-09` · `DEP-10` · `DEP-12` · `DEP-13` · `DEP-14` · `DEP-15` · `DEP-16` · `DEP-17` · `EX-CEO` · `IAF` | إغلاقُ الطيّارِ بشريًّا | تسعةُ أصفارِ §25 لكلٍّ |
| **2** | 0 بـ`WRONG_ORDER` وحدَه: — لا واحدة | موجة 1 | ضبطُ ترتيبِ الدليلِ ثمَّ صفرٌ |
| **3** | 0 بنقصِ هدفٍ وحدَه: — لا واحدة | قرارُ المالكِ في سجلِّ 12 | بناءُ الهدفِ ثمَّ 100٪ |
| **4** | 0 بالعطبَين معًا: — لا واحدة | موجتا 2 و3 | تسعةُ أصفار |
| **5** | 2 مساحةً **بلا دورٍ ممثِّل**: `DEP-08` · `EX-DVP` | ⛔ **قرارُ المالكِ أوّلًا** (§6) | ربطٌ ثمَّ قياسٌ ثمَّ تسعةُ أصفار |
| **6** | `WS-MY` ثمَّ أدواتُ المنصّة | الموجاتُ السابقة | فصلُ القشرةِ مُثبَتًا (‏NT-06 · NT-07) |
| **7 — القلب** | استبدالُ `getUnifiedNavItems` بـ`navarch_render` | كلُّ ما سبق | صفرُ فقدِ وصولٍ في UAT |

## ⑫ ⛔ ما لم يُحسَم — باسمِه وسببِ عدمِ حسمِه

| # | البند | لماذا لم يُحسَم | مَن يحسمه |
|---|---|---|---|
| 1 | `HUMAN_UAT_PASS` | **قرارٌ بشريٌّ لا يُنتحَل** — والبنودُ الآليّةُ الأحدَ عشرَ خضراء | مستخدمُ تشغيلٍ حقيقيّ (§31) |
| 2 | 63 ظهورًا إرثيًّا | لا استبدالَ ولا حكمَ مصالحةٍ ولا مالكَ مسجَّل. **والاستعمالُ غيرُ مقيسٍ في هذه الشجرة** فلا يجوز `RETIRE_CANDIDATE` (§32) | `L2` مالكُ المجال |
| 3 | قياسُ الاستعمالِ نفسُه | `nav_redirects.hits` صفرٌ كلُّها و`workspace_navigation_log` ليس مفتاحُه المسار — **فلا تليمتري ملاحةٍ في هذه الشجرة** | `L3` الحوكمة: أتُبنى؟ |
| 4 | أربعٌ من تبعيّاتِ §33 الستّ | المفضّلاتُ والمهامُّ والإشعاراتُ والتكاملاتُ **لم تُقَس** — والروابطُ الداخليّةُ وحدَها قِيست | `L3` قبلَ أيِّ إيقافِ مسار |
| 5 | `DEP-08` و`EX-DVP` | لهما أهدافٌ في الدليلِ **ولا دورَ يمثّلهما** فلا تُصيَّران ولا تُقاسان. ⛔ و§6 يمنع إنشاءَ إدارةٍ لوجودِ دورٍ — والعكسُ كذلك | `L4` المالك |
| 6 | 12 ظهورًا كرَّر مسارَه في مساحتِه | مسارٌ واحدٌ باسمَين في مساحةٍ واحدة — **اسمٌ حاكمٌ واحدٌ لكلِّ مسارٍ في مساحةٍ (§7)**. والسجلُّ الحاكمُ مفتاحُه `(مساحة, مسار)` فيطويه — ⛔ **فقُيِّد باسمِه في `UNEXPLAINED_EXTRA_REGISTER` ولم يُبتلَع** | `L2` مالكُ المجال |
| 7 | قلبُ المُصيِّرِ حيًّا | **الظلُّ لا يغيّر تجربةَ المستخدم (§30)** — ولا يُقلَب قبلَ UAT | بعدَ البند 1 |

---

**والمخرجاتُ الاثنا عشرَ** في `NAV_ARCH_02_OUTPUTS.xlsx` — ورقةٌ لكلِّ مخرَجٍ بنصِّ §37.

