# RPR-02-A · الأربعةُ والثلاثون خارجَ بيتِها — بحكمٍ لكلٍّ

> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02a_outofhouse.php`
> **مولَّدٌ**: 2026-08-28 19:30:13
> **المقام**: `repair01_screen_registry.ownership_verdict = 'EXECUTIVE_PROJECTION'` · **الحكم**: مسحُ المصدرِ على القرص

## ⓪ أيُّ أربعةٍ وثلاثين؟ — مقامان يتشابهان عددًا ويختلفان هويّة

| المقام | العدد | التقاطعُ مع الآخر |
|---|---:|---:|
| `repair01_screen_registry.ownership_verdict = 'EXECUTIVE_PROJECTION'` | **34** | 2 |
| `10 › 08_مؤشرات_المشاكل` — «مرشَّحةٌ للدمج» **فئةً خالصة** (وهو رقمُ رأسِ الورقة) | **34** | 2 |
| — وبضمِّ المشكلاتِ المركَّبةِ التي تذكر الدمجَ أيضًا | 39 | 4 |

**الحكم:** تقاطعُهما 2 سطحًا — فليسا المقامَ نفسَه.

## ① الحكمُ سطحًا سطحًا

| # | الرمز | البيتُ الحالي | السطح | البيتُ المعياريُّ (مصدرُ الحقيقة) | يكتب/يقرأ | الدليل | الحكم |
|---:|---|---|---|---|---|---|---|
| 1 | `SCR-0002` | `EX-CEO` | مركز تقارير الحوكمة | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `admin/index.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 2 | `SCR-0004` | `EX-CEO` | التكليفات التنظيمية | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `admin/org_assignments.php` — صفرُ عبارةِ كتابةٍ في المصدر · لكنّه يستقبل `$_POST` | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 3 | `SCR-0006` | `EX-CEO` | الهيكل التنظيمي والمسميات | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `admin/org_structure.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 4 | `SCR-0037` | `EX-CEO` | المخاطر التجارية | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Clients/commercial_risks.php` — صفرُ عبارةِ كتابةٍ في المصدر · لكنّه يستقبل `$_POST` | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 5 | `SCR-0453` | `EX-CEO` | اعتمادات الرئيس التنفيذي | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يكتب** | `Portal/ceo_approvals.php` — كتابةٌ في المصدر: INSERT INTO ×1 · UPDATE EXEC_APPROVALS SET ×2 · يستقبل `$_POST` · **بلا حارسِ فعلٍ مرصود** | ⛔ **تُنقل ملكيتُه إلى بيتِه** ويبقى هنا عرضٌ للقراءةِ فقط — **بقرارٍ موثَّق** |
| 6 | `SCR-0456` | `EX-CEO` | لوحة الرئيس التنفيذي | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Portal/ceo_board.php` — صفرُ عبارةِ كتابةٍ في المصدر · لكنّه يستقبل `$_POST` | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 7 | `SCR-0457` | `EX-CEO` | العقود والالتزامات المحجوزة أو المصعدة | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يكتب** | `Portal/ceo_contracts.php` — كتابةٌ في المصدر: INSERT INTO ×2 · UPDATE EXEC_CONTRACT_SIGNINGS SET ×1 · يستقبل `$_POST` · **بلا حارسِ فعلٍ مرصود** | ⛔ **تُنقل ملكيتُه إلى بيتِه** ويبقى هنا عرضٌ للقراءةِ فقط — **بقرارٍ موثَّق** |
| 8 | `SCR-0459` | `EX-CEO` | المخاطر والقرارات العليا | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يكتب** | `Portal/ceo_risk.php` — كتابةٌ في المصدر: INSERT INTO ×1 · يستقبل `$_POST` · **بلا حارسِ فعلٍ مرصود** | ⛔ **تُنقل ملكيتُه إلى بيتِه** ويبقى هنا عرضٌ للقراءةِ فقط — **بقرارٍ موثَّق** |
| 9 | `SCR-0460` | `EX-CEO` | مراجعة العقود وملاحظاتها | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Portal/contract_review.php` — صفرُ عبارةِ كتابةٍ في المصدر · لكنّه يستقبل `$_POST` | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 10 | `SCR-0461` | `EX-CEO` | إنجاز الإدارة | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Portal/dept_achievement.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 11 | `SCR-0463` | `EX-CEO` | وضع التأسيس وإغلاقه | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Portal/founding_mode.php` — صفرُ عبارةِ كتابةٍ في المصدر · لكنّه يستقبل `$_POST` | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 12 | `SCR-0464` | `EX-CEO` | حوكمة مكتبُ الرئيس التنفيذي والنواب | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Portal/gov_dept_ceo.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 13 | `SCR-0474` | `EX-CEO` | قرار فتح مشروع جديد | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يكتب** | `Portal/project_charter.php` — كتابةٌ في المصدر: INSERT INTO ×1 · UPDATE EXEC_PROJECT_CHARTERS SET ×1 · يستقبل `$_POST` · **بلا حارسِ فعلٍ مرصود** | ⛔ **تُنقل ملكيتُه إلى بيتِه** ويبقى هنا عرضٌ للقراءةِ فقط — **بقرارٍ موثَّق** |
| 14 | `SCR-0475` | `EX-CEO` | بصمة الإصدار وتقرير النشر | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Portal/release_stamp.php` — صفرُ عبارةِ كتابةٍ في المصدر · لكنّه يستقبل `$_POST` | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 15 | `SCR-0518` | `EX-CEO` | مركز التقارير التنفيذية | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Reports/reports.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 16 | `SCR-0628` | `EX-CEO` | سجل العقود الموحد | ⛔ **غيرُ مسجَّل** — يُحسم بقرارٍ موثَّق | **يقرأ** | `Workforce/contract_registry.php` — صفرُ عبارةِ كتابةٍ في المصدر · لكنّه يستقبل `$_POST` | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 17 | `SCR-0767` | `EX-CEO` | التقرير اليومي التنفيذي | DEP-12 | **يقرأ** | `Portal/exec_daily_report.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 18 | `SCR-0768` | `EX-CEO` | تفصيل توقفات اليوم | DEP-11 | **يقرأ** | `Portal/exec_daily_stops.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 19 | `SCR-0769` | `EX-CEO` | انحرافات وقرارات اليوم | DEP-11 | **يقرأ** | `Portal/exec_daily_deviations.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 20 | `SCR-0770` | `EX-CEO` | التقرير الأسبوعي التنفيذي | DEP-11 | **يقرأ** | `Portal/exec_weekly_report.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 21 | `SCR-0771` | `EX-CEO` | التقرير الشهري التنفيذي | DEP-05 | **يقرأ** | `Portal/exec_monthly_pack.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 22 | `SCR-0772` | `EX-CEO` | الطلبات المرفوعة إلى القيادة | DEP-05 | **يقرأ** | `Portal/exec_raised_requests.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 23 | `SCR-0773` | `EX-CEO` | سجل العقود الموحد | DEP-01 | **يقرأ** | `Portal/exec_contract_registry.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 24 | `SCR-0774` | `EX-CEO` | تجاوزات الخطوط الحمراء | DEP-08 | **يقرأ** | `Portal/exec_redline_breaches.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 25 | `SCR-0775` | `EX-CEO` | الاستثناءات الحرجة | DEP-08 | **يقرأ** | `Portal/exec_critical_exceptions.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 26 | `SCR-0776` | `EX-CEO` | المسائل المحجوزة | DEP-08 | **يقرأ** | `Portal/exec_reserved_matters.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 27 | `SCR-0777` | `EX-CEO` | التصعيدات العليا | DEP-10 | **يقرأ** | `Portal/exec_escalations.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 28 | `SCR-0778` | `EX-CEO` | قيادة الأزمات والطوارئ | EX-CEO | **يقرأ** | `Portal/exec_crisis_command.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 29 | `SCR-0779` | `EX-CEO` | القرارات الاستراتيجية | EX-CEO | **يقرأ** | `Portal/exec_strategic_decisions.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 30 | `SCR-0780` | `EX-CEO` | موافقات التعيين في المسميات القيادية | DEP-07 | **يقرأ** | `Portal/exec_leadership_appointments.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 31 | `SCR-0781` | `EX-CEO` | قرارات الاجتماعات | EX-CEO | **يقرأ** | `Portal/exec_meeting_decisions.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 32 | `SCR-0782` | `EX-CEO` | متابعة القرارات التنفيذية | EX-CEO | **يقرأ** | `Portal/exec_actions_followup.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 33 | `SCR-0783` | `EX-DVP` | الإنابات والتفويضات | DEP-08 | **يقرأ** | `Portal/exec_delegations.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |
| 34 | `SCR-0784` | `WS-MY` | بلاغاتي | DEP-10 | **يقرأ** | `Portal/my_reports.php` — صفرُ عبارةِ كتابةٍ في المصدر | **يبقى** · يُوسَم `IMPORTED_READONLY` بمرجعِ مصدرِه |

## ② الحصيلة

| الحكم | العدد |
|---|---:|
| **يقرأ** ⇒ يبقى ويُوسَم إسقاطًا | **30** |
| **يكتب** ⇒ تُنقل ملكيتُه بقرارٍ موثَّق | **4** |
| **المجموع** | **34** |

> ⛔ **وسمٌ يقول «إسقاط» وملفٌّ يكتب**: الأربعةُ والثلاثون كلُّها موسومةٌ `PROJECTION` في السجل،
> **والمقيسُ أنّ 4 منها تكتب فعلًا في قاعدةِ البيانات** — فالوسمُ لا يُصدَّق، والمصدرُ يُقاس.

## ③ المراجعُ الداخليُّ المستقلّ — استقلالُ التأكيدِ مقيسًا

| المقياس | العدد |
|---|---:|
| أسطحٌ حيّةٌ يملكها `IAF` | 21 |
| منها **ليست** `AUDIT_ASSURANCE` — أي خارجَ وظيفةِ التأكيد | **0** |

**ظهورُه في مساحاتِ غيرِه** — وهو **قراءةٌ رقابيّةٌ لا ملكيّة**:

| بيتُ السطح | التصنيف | عدد |
|---|---|---:|
| المالية والخزينة | `CONTROL_OVERSIGHT` | 22 |
| مساحة العمل الشخصية | `PERSONAL_SPACE` | 6 |
| مساحة العمل الشخصية | `CONTROL_OVERSIGHT` | 3 |
| مكتب الرئيس التنفيذي والنواب | `PERSONAL_SPACE` | 2 |
| المالية والخزينة | `SHARED_WORK_ITEM` | 1 |
| مركز البلاغات | `PERSONAL_SPACE` | 1 |

**الحكم:** ✔ **المراجعُ الداخليُّ لا يملك سطحًا خارجَ وظيفةِ التأكيد** — فكلُّ ما يملكه `AUDIT_ASSURANCE` و`iaf_*`. وحضورُه على أسطحِ المالية والخزينة والقيادة **قراءةٌ مصنَّفةٌ `CONTROL_OVERSIGHT`** لا ملكيّة. ⇒ **م 110 غيرُ مخروقةٍ في الملكية**، وما وصفه الأمرُ خرقًا هو الحضورُ الرقابيُّ المشروع.
