# `RPR-02` §١٢ — معيارُ القبولِ بستّةَ عشرَ مقياسًا

> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02_acceptance_scorecard.php --md` · اللقطة `SNAP-9871003c-20260830-021857`

**مقيسٌ 16 · محجوبٌ 0** — وكان في الأمرِ أحدَ عشرَ «غيرَ مقيس».

| # | المقياس | المستهدف | الحال | المقيس | الشاهد |
|---:|---|---|---|---|---|
| 1 | إغلاق قرارات الأهداف — كل هدف بحكم من السبعة | 100٪ | `MEASURED` | **97.5٪** | 547 من 561 · `repair01_target_universe.verdict IS NOT NULL` |
| 2 | تحقّق الأهداف — المتحقِّق ÷ المنطبق | يُعلَن بلا سقف | `MEASURED` | **37.1٪** | 203 ÷ 547 · وخرج من المقامِ بقرارٍ أو ببديلٍ **0** — يُعرض منفصلًا (§٤·٣) |
| 3 | مطابقة حقول كل سطح مطابَق لملفه | 100٪ | `MEASURED` | **23.3٪** | 670 من 2879 حقلٍ منطبقٍ على **200** سطحًا مطابَقًا — مقيسًا من الأثرِ بـ`rpr02_field_measure.php` · و`AUDIT` **595** خارجَ المقامِ بنصِّ §٧ الخطوة ١١ (إلحاقية) · و**5** سطحًا خلا أثرُه من مفردةٍ فحمل شاهدَ عجزِه (`NO_VOCAB`/`REDIRECTOR`) ولم يُكتب صفرًا. ⛔ **وهذا قياسُ حضورٍ لا قياسُ نوعٍ ولا ترتيب** |
| 4 | مطابقة آلة الحالة لكل معاملة | 100٪ | `MEASURED` | **8.7٪** | 18 من 208 سطحَ معاملةٍ (حبّةٌ `ROW`/`LINE`) يحمل `state_model_ref` — ⛔ **وحملُ المرجعِ ليس مطابقةَ الآلة**: هذا قياسُ وجودٍ لا قياسُ تطابق |
| 5 | اختبار سالب لفصل الواجبات الحرج | 100٪ | `MEASURED` | **8.7٪** | 8 من **92** فصلَ واجبٍ حرجٍ يذكره فاحصٌ سالبٌ أو حاجبٌ بمعرِّفِه — والفواحصُ على القرص **20** وكانت **بسطًا موهومًا لمقامٍ غيرِ موجود**. و`enforced_by` **لا يصلح مفتاحًا**: `PROSE` 27 · `ABSENT` 13 (W6·W15 بلا عمودٍ أصلًا) ⇒ فالمفتاحُ `process_key`. ⛔ **وهذا ادّعاءُ حراسةٍ لا إثباتُ حمرة** |
| 6 | مرجع مصدر صريح لكل إسقاط منطبق | 100٪ | `MEASURED` | **23.2٪** | 43 من 185 سطحَ إسقاطٍ يحمل `source_of_truth` غيرَ فارغ |
| 7 | جسر معرّف الشاشة بمرحلة دورة العمل | 100٪ من المنطبق | `MEASURED` | **55.6٪** | 345 من 621 سطحًا له صفٌّ في `gov_screen_cycle` — ⛔ **والوصلُ بالمسارِ لا بالمعرِّف**، و§٧ الخطوة ١٣ تشترط الوصلَ **بمعرِّفِ الشاشةِ صراحةً لا بالمسارِ ولا بالاسم** |
| 8 | تطابق ترتيب السايدبار ومجموعاته مع الملف | 100٪ | `MEASURED` | **68.1٪** | **المُصيَّرُ** مقابلَ **الملفِّ التصميميّ** (`repair01_requirements` بجسرِ المعرِّف): مطابقٌ **522** من **767** · ومحجوبٌ على المصالحة `NO_BRIDGE` **1329** — لا سطحَ مطابَقًا فلا متطلبَ يُقاس عليه. ⛔ **وكان يُقاس على المخزن** (`link_groups`) فأعطى **61.7%** بمقامٍ 3975 — **و`link_groups` لا تُصيَّر أصلًا لمن له معتمَدٌ أو إعلان**، ففرقُ المخزنِ لا يراه مستخدم. والتفصيلُ في `rpr02_s6_sidebar.php` |
| 9 | أسطح تكتب ومالك حقيقتها مجهول | صفر | `MEASURED` | **22** | أسطحٌ تكتب **حقيقةَ أعمالٍ تملكها** (`grain_fact_scope='OWN_FACT'`) و`source_of_truth` فارغ — والمقامُ **131** من 208 سطحَ كتابة؛ و77 مستبعَدٌ **كيانُه من كِيتٍ مشتركٍ أو بنيةٍ صِرفة** (‏`ems_post_idempotency` · `guard_denials` …) — ⛔ **ونسبتُه إلى السطحِ خرقٌ كاذب**. **فُتح بقياسِ الحبّة** |
| 10 | حقيقة واحدة لها مصدران | صفر | `MEASURED` | **6** | كيانٌ **يملكه** أكثرُ من واجهةٍ حيّةٍ بحبّةِ سجلٍّ — أعلاها: exec_approvals · iaf_findings · tickets · sensitive_read_log · unit_entries · والمقامُ `OWN_FACT` وحدَه ⇒ **الكيانُ المكتوبُ من كِيتٍ مشتركٍ لا يُعدُّ مصدرًا ثانيًا** · وحُسم **2** بمصدرٍ قانونيٍّ مسمًّى (`N1` هويّةُ اسمِ الأثر · `N2` المُعلِنُ يغلب المُستنتَج) · والباقي **5** عبورُ إداراتٍ **يحتاج عقدًا لا ترجيحًا** (‏وهو بعينِه مقامُ **#١١**) و**2** تعادلٌ داخلَ إدارةٍ واحدة. ⛔ **وترجيحُ أحدِ الكاتبَين هنا يُخفي #١١ ولا يحلُّها**. **فُتح بقياسِ الحبّة** |
| 11 | كتابة تعبر حدود إدارة بلا عقد | صفر | `MEASURED` | **4** | كيانٌ تكتبه إدارتان فأكثرُ — **والعقدُ صار مقيسًا** بـ`rpr02_cross_contract.php`: كاتبٌ بكتابةٍ خامّةٍ **15** · بخدمةٍ أو ناشرٍ بلا خامٍّ **0** · مُصرِّحٌ بلا كتابةٍ **2** (‏ليس كاتبًا). وكياناتٌ **بعقدٍ مقيس 0** ⇒ ⛔ **صفرُ عبورٍ متعاقَدٍ عليه في الشجرةِ كلِّها**، وكلُّ عبورٍ حقيقيٍّ يكتب خامًّا. **فالرقمُ صار خرقًا مقيسًا لا سقفَ خرق** |
| 12 | هجرات غير مصالَحة مع الدفتر | صفر | `MEASURED` | **0** | غيرُ مصالَحةٍ **0** من 319 في `gov_migration_settlement` · وخطُّ الأمرِ كان ٥٨ |
| 13 | شاشة PLATFORM بلا تبرير منصّي معتمد | صفر | `MEASURED` | **30** | أسطحٌ `PLATFORM_SHARED` حيّةٌ **30** — **ومسجَّلةٌ الآن بمعرِّفِها وقاعدةِ ظهورِها 25 من 30** بـ`rpr02_platform_register.php`: منها **18** مالكُها **نطاقٌ مُعلَنٌ** (‏فـ`PLATFORM_SHARED` فيها **صفةُ ظهورٍ عابرٍ للأدوارِ لا فراغُ ملكيّة**) والباقي مربوطٌ بـ**5** قدرةً من ثمانِ `AMD-01` §٤·٧. ⛔ **والتسجيلُ ليس اعتمادًا**: معتمَدٌ **0** — و§٤·٤ يشترط `معتمَدًا`. ⇒ **فالباقي فعلُ اعتمادٍ لا عملُ تحليل** |
| 14 | ملف مكتبة مسجَّل شاشةً — وقاعدة مانعة مفعَّلة | صفر · مفعَّلة | `MEASURED` | **0 · حاجبٌ قائم** | ملفّاتُ `vendor/` **بالمسارِ لا باسمِ الملفّ**: 2 · وغيرُ الموسومةِ `RETIRE`: **0** (‏والموسومةُ حكمٌ مسجَّلٌ في §١١ لا عطبٌ مفتوح) · قيدُ مخطَّطٍ باسمِ المكتبة: 0 · حاجبٌ في العُدّة: نعم |
| 15 | أهداف خرجت من المقام بلا مرجع قرار مالك | صفر | `MEASURED` | **0** | الخارجون من المقامِ **0** · ومنهم بلا ذِكرِ قرارٍ في شاهدِه **0** (§٤·٣ الحارس الأول: لا يصدر أيٌّ منهما إلّا بمرجعِ قرارِ مالكٍ موثَّقٍ بتاريخه) |
| 16 | اسم معروض غير معتمد | صفر | `MEASURED` | **4** | غيرُ `APPROVED` **4** من 524 في `nav_canonical` — ⛔ **وهذا قياسُ السجلِّ لا قياسُ المعروض**: الاسمُ يُصيَّر من أربعةِ مصادرَ بترجيح |

⛔ **ولا طبقةَ ثالثةً اسمُها «صفر» بلا قياس** — فصفرٌ من مفردةٍ لا وجودَ لها أخضرُ كاذب،
والمحجوبُ يُعلَن محجوبًا بمرحلتِه ولا يُكتب صفرًا.
