# `RPR-02` §١٢ — معيارُ القبولِ بستّةَ عشرَ مقياسًا

> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02_acceptance_scorecard.php --md` · اللقطة `SNAP-b98bd884-20260829-211211`

**مقيسٌ 14 · محجوبٌ 2** — وكان في الأمرِ أحدَ عشرَ «غيرَ مقيس».

| # | المقياس | المستهدف | الحال | المقيس | الشاهد |
|---:|---|---|---|---|---|
| 1 | إغلاق قرارات الأهداف — كل هدف بحكم من السبعة | 100٪ | `MEASURED` | **87.5٪** | 491 من 561 · `repair01_target_universe.verdict IS NOT NULL` |
| 2 | تحقّق الأهداف — المتحقِّق ÷ المنطبق | يُعلَن بلا سقف | `MEASURED` | **41.3٪** | 203 ÷ 491 · وخرج من المقامِ بقرارٍ أو ببديلٍ **0** — يُعرض منفصلًا (§٤·٣) |
| 3 | مطابقة حقول كل سطح مطابَق لملفه | 100٪ | `BLOCKED` | **—** | دفترُ الحقولِ التصميميُّ **7591** صفًّا و`gov_field_trace` المبنيُّ **1417** — **ولا مفتاحَ يجمعهما**: الأوّلُ باسمِ السطحِ العربيِّ والثاني بمسارِ الملفّ. ⇒ `blocked at stage: مصالحةُ مفتاحِ الحقلِ بين الدفترَين (§٧ الخطوة ٥)` |
| 4 | مطابقة آلة الحالة لكل معاملة | 100٪ | `MEASURED` | **8.7٪** | 18 من 208 سطحَ معاملةٍ (حبّةٌ `ROW`/`LINE`) يحمل `state_model_ref` — ⛔ **وحملُ المرجعِ ليس مطابقةَ الآلة**: هذا قياسُ وجودٍ لا قياسُ تطابق |
| 5 | اختبار سالب لفصل الواجبات الحرج | 100٪ | `BLOCKED` | **—** | فواحصُ سالبةٌ على القرص **20** · **ولا سجلَّ يربط فاحصًا بفصلِ واجباتٍ بعينِه** ⇒ العددُ لا يصلح بسطًا ولا مقامًا. `blocked at stage: سجلُّ فصلِ الواجباتِ الحرجِ بمعرِّفاتِه` |
| 6 | مرجع مصدر صريح لكل إسقاط منطبق | 100٪ | `MEASURED` | **12.4٪** | 23 من 185 سطحَ إسقاطٍ يحمل `source_of_truth` غيرَ فارغ |
| 7 | جسر معرّف الشاشة بمرحلة دورة العمل | 100٪ من المنطبق | `MEASURED` | **55.6٪** | 345 من 621 سطحًا له صفٌّ في `gov_screen_cycle` — ⛔ **والوصلُ بالمسارِ لا بالمعرِّف**، و§٧ الخطوة ١٣ تشترط الوصلَ **بمعرِّفِ الشاشةِ صراحةً لا بالمسارِ ولا بالاسم** |
| 8 | تطابق ترتيب السايدبار ومجموعاته مع الملف | 100٪ | `MEASURED` | **61.7٪** | المجموعةُ تطابق المعتمَدَ في **860 من 1991** · والترتيبُ **1591 من 1984** (‏مقارنةً **ترتيبيّةً داخلَ المجموعةِ لا قيمةً لقيمة**) · المقامُ بنودٌ حيّةٌ مُصيَّرةٌ 2096 — والتفصيلُ في `rpr02_s6_sidebar.php` |
| 9 | أسطح تكتب ومالك حقيقتها مجهول | صفر | `MEASURED` | **190** | أسطحٌ حبّتُها `ROW`/`LINE` (‏تكتب) وكيانُها مقيسٌ **و`source_of_truth` فارغ** — وهذا المقياسُ **فُتح بقياسِ الحبّة**، فقبلَه لم يكن يُعرف مَن يكتب |
| 10 | حقيقة واحدة لها مصدران | صفر | `MEASURED` | **18** | كيانٌ تكتبه أكثرُ من واجهةٍ حيّةٍ بحبّةِ سجلٍّ — أعلاها: op_containers · guard_denials · ems_post_idempotency · tickets · unit_state_changes · **فُتح بقياسِ الحبّة** |
| 11 | كتابة تعبر حدود إدارة بلا عقد | صفر | `MEASURED` | **10** | كيانٌ تكتبه إدارتان فأكثرُ بحبّةِ سجلٍّ — ⛔ **والعقدُ غيرُ مقيسٍ بعدُ**، فالرقمُ **سقفُ الخرقِ لا الخرقُ**: منه ما له عقدٌ مُعلَنٌ لم يُسجَّل. **فُتح بقياسِ الحبّة** |
| 12 | هجرات غير مصالَحة مع الدفتر | صفر | `MEASURED` | **0** | غيرُ مصالَحةٍ **0** من 316 في `gov_migration_settlement` · وخطُّ الأمرِ كان ٥٨ |
| 13 | شاشة PLATFORM بلا تبرير منصّي معتمد | صفر | `MEASURED` | **30** | أسطحٌ `PLATFORM_SHARED` حيّةٌ **30** · وسجلُّ المنصّةِ فيه **1** قدرةً ⇒ **لا واحدةَ منها مسجَّلةٌ بمعرِّفِها وقاعدةِ ظهورِها** (§٤·٤ الشرط الرابع)، فالأربعةُ مجتمعةً غيرُ مستوفاةٍ ولا واحدةٌ تُعفى |
| 14 | ملف مكتبة مسجَّل شاشةً — وقاعدة مانعة مفعَّلة | صفر · مفعَّلة | `MEASURED` | **0 · حاجبٌ قائم** | ملفّاتُ `vendor/` غيرُ الموسومةِ `RETIRE`: **0** (‏والمسجَّلةُ `RETIRE` حكمٌ مسجَّلٌ لا عطب) · قيدُ مخطَّطٍ باسمِ المكتبة: 0 · حاجبٌ في العُدّة: نعم |
| 15 | أهداف خرجت من المقام بلا مرجع قرار مالك | صفر | `MEASURED` | **0** | الخارجون من المقامِ **0** · ومنهم بلا ذِكرِ قرارٍ في شاهدِه **0** (§٤·٣ الحارس الأول: لا يصدر أيٌّ منهما إلّا بمرجعِ قرارِ مالكٍ موثَّقٍ بتاريخه) |
| 16 | اسم معروض غير معتمد | صفر | `MEASURED` | **4** | غيرُ `APPROVED` **4** من 524 في `nav_canonical` — ⛔ **وهذا قياسُ السجلِّ لا قياسُ المعروض**: الاسمُ يُصيَّر من أربعةِ مصادرَ بترجيح |

⛔ **ولا طبقةَ ثالثةً اسمُها «صفر» بلا قياس** — فصفرٌ من مفردةٍ لا وجودَ لها أخضرُ كاذب،
والمحجوبُ يُعلَن محجوبًا بمرحلتِه ولا يُكتب صفرًا.
