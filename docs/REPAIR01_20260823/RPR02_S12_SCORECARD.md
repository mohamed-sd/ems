# `RPR-02` §١٢ — معيارُ القبولِ بستّةَ عشرَ مقياسًا

> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02_acceptance_scorecard.php --md` · اللقطة `SNAP-d6727ae1-20260831-081015`

**مقيسٌ 16 · محجوبٌ 0** — وكان في الأمرِ أحدَ عشرَ «غيرَ مقيس».

| # | المقياس | المستهدف | الحال | المقيس | الشاهد |
|---:|---|---|---|---|---|
| 1 | إغلاق قرارات الأهداف — كل هدف بحكم من السبعة | 100٪ | `MEASURED` | **100٪** | 561 من 561 · `repair01_target_universe.verdict IS NOT NULL` |
| 2 | تحقّق الأهداف — المتحقِّق ÷ المنطبق | يُعلَن بلا سقف | `MEASURED` | **48٪** | 269 ÷ 561 · وخرج من المقامِ بقرارٍ أو ببديلٍ **0** — يُعرض منفصلًا (§٤·٣) |
| 3 | مطابقة حقول كل سطح مطابَق لملفه | 100٪ | `MEASURED` | **24٪** | 690 من 2879 حقلٍ منطبقٍ على **200** سطحًا مطابَقًا — مقيسًا من الأثرِ بـ`rpr02_field_measure.php` · و`AUDIT` **595** خارجَ المقامِ بنصِّ §٧ الخطوة ١١ (إلحاقية) · و**2** سطحًا خلا أثرُه من مفردةٍ فحمل شاهدَ عجزِه (`NO_VOCAB`/`REDIRECTOR`) ولم يُكتب صفرًا. ⛔ **وهذا قياسُ حضورٍ لا قياسُ نوعٍ ولا ترتيب** |
| 4 | مطابقة آلة الحالة لكل معاملة | 100٪ | `MEASURED` | **100٪** | **127 من 127** سطحَ معاملةٍ حقيقيّةٍ (`OWN_FACT` بحبّةِ `ROW`/`LINE`) يحمل `state_model_ref`. ⛔ **و0 سطحًا تحمله وكيانُها من كِيتٍ مشتركٍ** (‏`guard_denials`) ⇒ **ليست معاملاتٍ بحبّتِها**، وكانت تُقرأ بسطًا على مقامٍ 204 فتُعطي ٨٫٧٪. ◆ **وحكمُ الحقلِ مقيسٌ من المخطَّط** (§٧ الخطوة ١٢ «لا حقلَ حالةٍ حرّ»): كياناتُ معاملاتٍ **118** — محكومٌ `ENUM` **30** · **حرٌّ 51** (‏المستهدَفُ صفر) · وبلا حقلِ حالةٍ 37 ⇒ المحكومُ 37٪ من 81. ⛔ **وحملُ المرجعِ ليس مطابقةَ الآلة** |
| 5 | اختبار سالب لفصل الواجبات الحرج | 100٪ | `MEASURED` | **100٪** | 92 من **92** فصلَ واجبٍ حرجٍ يذكره فاحصٌ سالبٌ أو حاجبٌ بمعرِّفِه — والفواحصُ على القرص **21** وكانت **بسطًا موهومًا لمقامٍ غيرِ موجود**. و`enforced_by` **لا يصلح مفتاحًا**: `PROSE` 27 · `ABSENT` 13 (W6·W15 بلا عمودٍ أصلًا) ⇒ فالمفتاحُ `process_key`. ⛔ **وهذا ادّعاءُ حراسةٍ لا إثباتُ حمرة** |
| 6 | مرجع مصدر صريح لكل إسقاط منطبق | 100٪ | `MEASURED` | **100٪** | 159 من **159** سطحَ إسقاطٍ **منطبقٍ** يحمل `source_of_truth` غيرَ فارغ — والمقامُ الخامُّ 185، **ومستبعَدٌ 26** كيانُه من **كِيتٍ مشتركٍ أو بنيةٍ صِرفة** (`SHARED_KIT` · `INFRA_ONLY` · بلا كيانٍ مقيس) ⇒ **لا مصدرَ خاصًّا له يُسمّى**، و`rpr02_sot_assign` يرفض كتابتَه بنصِّه: «مرجعٌ كاذبٌ أسوأُ من غيابِه». ⛔ **والمستبعَدُ معروضٌ بعددِه وسببِه ولا يُطوى** (§٤·٣) — والمفتوحُ الحقيقيُّ **0** |
| 7 | جسر معرّف الشاشة بمرحلة دورة العمل | 100٪ من المنطبق | `MEASURED` | **100٪** | **127 من 127** سطحَ **معاملةٍ منطبقة** (`OWN_FACT` بحبّةِ `ROW`/`LINE`) موصولٌ **بمعرِّفِ الشاشة** لا باسمِ ملفّ. و§٥·١٠ تقول «كلُّ شاشةٍ **ينطبق عليها سيرُ عمل**» — **والقراءةُ الصِّرفةُ لا تُنشئ حالةً** (‏ولها `stage_kind='contextual'` وليست في المقام). ⛔ **و0 صفَّ دورةٍ بقي بلا معرِّف**: اسمُ ملفِّه يطابق أكثرَ من سطحٍ حيٍّ (`index.php` ثلاثةَ أسطح) — **واختيارُ أحدِها يربط مرحلةً بشاشةٍ ليست هي**. و**266** صفًّا لسطحٍ غيرِ قائمٍ يُعلَن ولا يُحذف. ⚠ **والتضييقُ لا يُقرأ تحسينًا**: الكلُّ الحيُّ 435 من 651 — **فالنقصُ موزَّعٌ حقيقيّ** |
| 8 | تطابق ترتيب السايدبار ومجموعاته مع الملف | 100٪ | `MEASURED` | **100٪** | **المُصيَّرُ** مقابلَ **الملفِّ التصميميّ** (`repair01_requirements` بجسرِ المعرِّف): مطابقٌ **1154** من **1154** · ومحجوبٌ على المصالحة `NO_BRIDGE` **1173** — لا سطحَ مطابَقًا فلا متطلبَ يُقاس عليه. ⛔ **وكان يُقاس على المخزن** (`link_groups`) فأعطى **99.5%** بمقامٍ 4443 — **و`link_groups` لا تُصيَّر أصلًا لمن له معتمَدٌ أو إعلان**، ففرقُ المخزنِ لا يراه مستخدم. والتفصيلُ في `rpr02_s6_sidebar.php` |
| 9 | أسطح تكتب ومالك حقيقتها مجهول | صفر | `MEASURED` | **0** | أسطحٌ تكتب **حقيقةَ أعمالٍ تملكها** (`grain_fact_scope='OWN_FACT'`) و`source_of_truth` فارغ — والمقامُ **127** من 204 سطحَ كتابة؛ و77 مستبعَدٌ **كيانُه من كِيتٍ مشتركٍ أو بنيةٍ صِرفة** (‏`ems_post_idempotency` · `guard_denials` …) — ⛔ **ونسبتُه إلى السطحِ خرقٌ كاذب**. **فُتح بقياسِ الحبّة** |
| 10 | حقيقة واحدة لها مصدران | صفر | `MEASURED` | **0** | كيانٌ **يملكه** أكثرُ من واجهةٍ حيّةٍ بحبّةِ سجلٍّ — أعلاها: exec_approvals · iaf_findings · unit_entries · work_items · fin_notifications · والمقامُ `OWN_FACT` وحدَه ⇒ **الكيانُ المكتوبُ من كِيتٍ مشتركٍ لا يُعدُّ مصدرًا ثانيًا** · وحُسم **5** بمصدرٍ قانونيٍّ مسمًّى (`N1` هويّةُ اسمِ الأثر · `N2` المُعلِنُ يغلب المُستنتَج) · والباقي **1** عبورُ إداراتٍ **يحتاج عقدًا لا ترجيحًا** (‏وهو بعينِه مقامُ **#١١**) و**0** تعادلٌ داخلَ إدارةٍ واحدة. ⛔ **وترجيحُ أحدِ الكاتبَين هنا يُخفي #١١ ولا يحلُّها**. **فُتح بقياسِ الحبّة** |
| 11 | كتابة تعبر حدود إدارة بلا عقد | صفر | `MEASURED` | **0** | كيانٌ تكتبه إدارتان فأكثرُ — **والعقدُ صار مقيسًا** بـ`rpr02_cross_contract.php`: كاتبٌ بكتابةٍ خامّةٍ **4** · بخدمةٍ أو ناشرٍ بلا خامٍّ **2** · مُصرِّحٌ بلا كتابةٍ **9** (‏ليس كاتبًا). وكياناتٌ **بعقدٍ مقيس 2** ⇒ ⛔ **صفرُ عبورٍ متعاقَدٍ عليه في الشجرةِ كلِّها**، وكلُّ عبورٍ حقيقيٍّ يكتب خامًّا. **فالرقمُ صار خرقًا مقيسًا لا سقفَ خرق** |
| 12 | هجرات غير مصالَحة مع الدفتر | صفر | `MEASURED` | **0** | غيرُ مصالَحةٍ **0** من 341 في `gov_migration_settlement` · وخطُّ الأمرِ كان ٥٨ |
| 13 | شاشة PLATFORM بلا تبرير منصّي معتمد | صفر | `MEASURED` | **12** | أسطحُ `PLATFORM_SHARED` الحيّةُ **12** — وكانت **30** فحُكم عليها بأربعةِ §٥·٤ بـ`rpr02_platform_justify.php`: **18** أخلَّ بواحدٍ **وله نطاقٌ مُعلَنٌ فعاد إليه** (‏«وما لم يستوفِ الأربعةَ يعود إلى إدارتِه» — وظهورُه العابرُ محفوظٌ في `visibility_class` وهو موضعُه) · و**0** استوفى الأربعةَ. ⛔ **والباقي 12 سطحًا حاجزُه المعيارُ ③ وحدَه**: «مالكٌ تقنيٌّ مسمًّى شخصًا» — ومستوفوه **0** من  فـ`repair01_platform_capabilities.tech_owner` **فارغٌ في صفِّه الوحيد** ولا جدولَ آخرَ يحمله. ⇒ `OWNER_BLOCKER · NEEDS_GOVERNING_SOURCE` — **وتسميةُ شخصٍ من عندنا تلفيقٌ يمنعه §٥ المحظور ④**. ◆ **والاثنا عشرَ هم بعينِهم «الأسطحُ المملوكةُ لرمزٍ لا إدارة» في `RPR-03` §نصِّه** |
| 14 | ملف مكتبة مسجَّل شاشةً — وقاعدة مانعة مفعَّلة | صفر · مفعَّلة | `MEASURED` | **0 · حاجبٌ قائم** | ملفّاتُ `vendor/` **بالمسارِ لا باسمِ الملفّ**: 2 · وغيرُ الموسومةِ `RETIRE`: **0** (‏والموسومةُ حكمٌ مسجَّلٌ في §١١ لا عطبٌ مفتوح) · قيدُ مخطَّطٍ باسمِ المكتبة: 0 · حاجبٌ في العُدّة: نعم |
| 15 | أهداف خرجت من المقام بلا مرجع قرار مالك | صفر | `MEASURED` | **0** | الخارجون من المقامِ **0** · ومنهم بلا ذِكرِ قرارٍ في شاهدِه **0** (§٤·٣ الحارس الأول: لا يصدر أيٌّ منهما إلّا بمرجعِ قرارِ مالكٍ موثَّقٍ بتاريخه) |
| 16 | اسم معروض غير معتمد | صفر | `MEASURED` | **3** | **بنودٌ مُصيَّرةٌ** باسمٍ غيرِ معتمَد: **3** على 2 مسارًا — منها **2** تسميتُها `PENDING_OWNER` (‏**فعلُ اعتمادٍ**) و**1** **بلا صفِّ تسميةٍ ألبتّة** (‏فجوةُ سجلّ). و104 صيغةَ عرضٍ (`?view=`) تحتفظ بوسمِها **بقرارِ المُصيِّرِ نفسِه** فلا تُحاسَب. ⛔ **وكان يُقاس على السجلِّ فيعطي 3 من 554** — والفرقُ في التركيبةِ لا العدد: صفُّ `MERGED` **لا يُصيَّر** ومسارٌ **بلا صفٍّ** لا يظهر في عدِّ السجلِّ وهو معروضٌ بلا اعتماد |

⛔ **ولا طبقةَ ثالثةً اسمُها «صفر» بلا قياس** — فصفرٌ من مفردةٍ لا وجودَ لها أخضرُ كاذب،
والمحجوبُ يُعلَن محجوبًا بمرحلتِه ولا يُكتب صفرًا.
