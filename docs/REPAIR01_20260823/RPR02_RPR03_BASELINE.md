# `RPR02_RPR03_BASELINE` — قياسُ الأساسِ قبل الطورِ صفر

> **مولَّدٌ من تشغيلٍ حيٍّ** بالسطر: `php tools/repair01_rpr0203_baseline.php --md`
> ⛔ **ولا يُحرَّر يدويًّا** — التعديلُ في الأداةِ ثمَّ يُعاد التوليد.

## ٠ · البصمةُ السداسيّة

| المفردة | القيمة |
|---|---|
| `Snapshot ID` | `UNFROZEN-b5a2cc7f-20260828-134307` **⛔ لا نافذةَ مجمَّدة — قياسٌ خارجَ التجميد** |
| `Commit Hash` | `b5a2cc7f14c61abcef4790fa6343d716b04500fd` (`repair01/w01-ownership`) |
| `Schema Version` | 953T/15267C · و28 مَشهدًا |
| `Registry Version` | 783 صفًّا في `repair01_screen_registry` |
| `Measured At` | 2026-08-28 13:43:07 |
| `Tool Version` | `repair01_rpr0203_baseline.php v1.0` |
| حالُ الشجرة | **متّسخةٌ — 72 ملفًّا** |

> ⛔ **والشجرةُ متّسخةٌ فهذا القياسُ لا يمثّل التزامًا** — يُقرأ استكشافًا لا
> لقطةً، **ويُعاد بعد التنظيفِ والتجميد** (البند ⑤ · و§٤·٣ من أمرِ التشغيل).

## ١ · التعارضاتُ الأربعةُ — مقيسةً ومفسَّرة

| التعارض | رقمُ الأمر | قياسي | الحكم | السببُ المقيس |
|---|---|---|---|---|
| المبنيّ | ٦٢٣ | **623** مبنيًّا · و**664** سطحًا | **لا تعارض** | سجلّان لا سجلّ: `repair01_screen_registry` فيه 783 = **623 مبنيًّا** (`lifecycle<>GHOST_TARGET`) + 160 شبحًا · و`repair01_surfaces` (664) **سجلُّ استخراجٍ بحبّةٍ أدقَّ** (408 صفًّا حكمُها «N:1 تجزئة أدق»). **ومقارنةُ ٦٦٤ بـ٦٢٣ مقارنةُ تمثيلَين لشيءٍ واحد** |
| المستهدَف | ٤٣١ | **429** | **رقمُ الأمرِ زائدٌ اثنين** | `repair01_requirements` = 429 وهو **مطابقٌ للدليلِ المعماريِّ ① حرفًا**: 367 شاشةً للإداراتِ السبعَ عشرة + 62 للأربعةِ خارجَ التعداد = 429. وجدولُ الأمرِ يعلن `DEP-08`=٣٢ و`DEP-14`=١٧ **والدليلُ يقول 31 و16** ⇒ فرقُ اثنين. **والدليلُ هو المرجعُ الحاكم** |
| وقائعُ التسليمِ اليتيمة | صفر | **82** إجمالًا · و**0** حيًّا | **لا تعارض** | الـ82 **كلُّها مؤرشفةٌ** (`archived_at IS NOT NULL`) — واليتيمُ الحيُّ **0**. ورقمُ الأمرِ يقصد الحيَّ ورقمي كان يقصد الإجمالي |
| الرسائلُ الميتة | ٢٦ | **0** · **17** · **26** | **لا تعارض — ثلاثةُ جداولَ لا رقمٌ واحد** | `ems_event_dead_letter`=0 (جدولٌ لم يُملأ قطُّ) · `gov_dead_letter_rulings`=17 (أحكامٌ على `ems_job_queue` لا على الأحداث) · **و`SUM(delivered_failed)` في `ems_event_outbox` = 26** ⇒ **وهذا هو رقمُ الأمرِ بعينِه** |

## ٢ · الهجرات — والمقامُ كان ناقصًا في قياسي السابق

| المقياس | القيمة | المقام |
|---|---|---|
| ملفّاتُ الهجرةِ المُدارةِ على القرص | **462** | بعُرفِ `YYYY_MM_DD_` نفسِه الذي في `migrate.php` — **379 `.php` + 83 `.sql`** · وخارجَ العُرفِ 1 |
| صفوفُ الدفتر | **703** | `schema_migrations` · {"baseline":126,"applied":577} |
| **قرصٌ خارجَ الدفتر** | **0** | **صفرٌ — والأمرُ قاسَ ٥٨ قبلَ التسوية** |
| **دفترٌ بلا ملفٍّ على القرص** | **241** | {"baseline":34,"applied":207} · وبالامتداد {"sql":235,"php":6} — **محكومةٌ في `gov_migration_settlement`** |
| أحكامُ التسوية | **299/299** متحقَّقٌ منها | `gov_migration_settlement` |

> ⚠ **وتصحيحُ قياسٍ سابقٍ لي**: كنتُ عددتُ القرصَ `*.php` وحدَه (379)
> **والدفترُ يحمل أسماءً بامتدادِ `.sql`** — فظهر «324 صفًّا بلا ملفّ»، والصوابُ **241**: والفرقُ 83 هي ملفّاتُ `.sql` القائمةُ فعلًا على القرصِ ولم تُعَدّ.
> **وهو عينُ «المقارنةِ بين تمثيلَين لشيءٍ واحد» — وقع مرّةً أخرى، وأُصلح في الأداةِ لا في المخرَج.**

## ٣ · السجلُّ الرسميُّ والملكيّة

| المقياس | القيمة | المقام / المفردة |
|---|---|---|
| `repair01_screen_registry` | **783** | {"LIVE_REGISTERED":235,"LIVE_UNREGISTERED":388,"GHOST_TARGET":160} |
| `repair01_surfaces` | **664** | سجلُّ استخراجٍ بحبّةٍ أدقَّ — **لا يُقارَن بالسجلِّ الرسميّ** |
| `repair01_requirements` | **429** | 367 للإدارات + 62 خارجَ التعداد |
| `repair01_target_gaps` | **334** | {"BUILD":139,"MERGE":195} |
| `repair01_ownership` | **985** | أحكامُ الملكيّة |
| `repair01_fields` | **7491** | سجلُّ الحقول |
| `repair01_departments` | **21** | ⛔ **و`PLATFORM` غيرُ مسجَّل** — والرموزُ الحيّةُ في السجلِّ **22** |
| أسطحُ `PLATFORM` | **12** | `owner_code=PLATFORM` · **مفردةٌ حيّةٌ مؤكَّدة** |
| أسطحُ `EX-DVP` | **1** | من 12 مستهدَفًا ⇒ **أسوأُ فجوةٍ في النظام** |
| ملفُّ مكتبةٍ مسجَّلٌ سطحًا | **2** في السجلِّ الرسميّ · **9** في `repair01_surfaces` | مقامان — **والقاعدةُ المانعةُ غيرُ مفعَّلة** |
| جسرُ الشاشةِ بدورةِ العمل | **لا يوجد** | `gov_nav_stage_bridge` (111 صفًّا) **يربط طبقةً بطبقةٍ لا شاشةً بمرحلة** — فلا عمودَ `screen_id` فيه |

## ٤ · الأحداثُ والآثار

| المقياس | القيمة | المقام / المفردة |
|---|---|---|
| أنواعُ الأحداثِ المتميّزة | **58** | `COUNT(DISTINCT event_key)` |
| الوقائع | **21546** | `ems_business_events` · أقصى معرِّفٍ 45829 والصندوقُ 21546/45829 — **مجالُ معرِّفٍ واحدٌ لا مجالان** |
| التسليم | **21520/21546 = 99.88٪** | `delivered_ok ÷ consumers_declared` — **ويطابق ٩٩٫٨٨٪ في الأمر** |
| الفاشل | **26** · وفي الصفِّ الميت **1** | `SUM(delivered_failed)` · `SUM(in_dlq)` |
| **تصنيفُ الأنواع** | **11 أعمالٍ · 47 تدقيقيًّا** | `gov_event_rulings` = 58 · **مُنجَزٌ سلفًا ومطابقٌ للأمر** · 0 بلا قرار · 0 محكومٌ غيرُ حيٍّ · 0 حيٌّ غيرُ محكوم |
| **سجلّاتُ المستهلكين** | **4 · 38 · 13 · 9** | `ems_event_consumers` · `ems_event_subscriptions` (33 نشطًا) · `ems_event_deliveries` · `..._orphans` — **أربعةُ مقاماتٍ لا واحد** |
| اليتامى | **82** إجمالًا · **0** حيًّا · **66** بحالةِ `dlq` | `archived_at` يفصل |

### ٤·١ مؤشّراتُ المستهلكين — أربعتُهم متأخّرون، واثنان محكومان

| المستهلك | المؤشّر | التأخّرُ عن 45829 | آخرُ تقدّم | أيام | الحكمُ القائم |
|---|---|---|---|---|---|
| `fx` | 6613 | **21002** واقعةً | 2026-08-12 05:31:59 | **16** | `NO_HANDLER_ON_DISK` — محكومٌ في `gov_orphan_consumer_rulings` |
| `finance_routing_replay` | 17980 | **14384** واقعةً | 2026-08-15 21:41:18 | **12** | `NO_HANDLER_ON_DISK` — محكومٌ في `gov_orphan_consumer_rulings` |
| `finance` | 17985 | **14379** واقعةً | 2026-08-16 12:23:02 | **12** | ⛔ **بلا حكمٍ ولا إنذار** |
| `finance_routing` | 17985 | **14379** واقعةً | 2026-08-16 12:23:02 | **12** | ⛔ **بلا حكمٍ ولا إنذار** |

## ٥ · الاعتمادُ والتجميد

| المقياس | القيمة | المقام |
|---|---|---|
| الشاشاتُ الذهبيّة | **10** معلّقةً من **10** | `gov_golden_approvals.state=pending` · **مفردةٌ حيّةٌ مؤكَّدة** |
| لقطاتُ التجميد | **6** · المفتوحُ **0** | `repair01_freeze_snapshot` |

## ٦ · ما لم أستطع قياسَه — مسمًّى لا مخمَّنًا

- **نافذةُ ظلِّ الاعتماد**: لا تُقاس قبلَ صدورِ القيمِ العدديّةِ من المالك (`RPR-03` §٥ ①).
- **الرحلاتُ البشريّةُ الستّ**: تحتاج مستخدمين حقيقيّين بصلاحيّاتِهم — لا تُقاس آليًّا.
- **الاستجابةُ وإتاحةُ الوصول**: تحتاج مسحًا على عيّنةٍ معلَنةٍ لم يُشغَّل بعد.
- **مطابقةُ حقولِ كلِّ سطحٍ لملفِّه**: تحتاج توحيدَ الكونِ ومصالحةَ المعرّفاتِ أوّلًا (§٥·١ و§٥·٢).

