# تصحيحُ رسالةِ الالتزام `4f143bfb` — الرقمان الصادقان بمقامَيهما

> **FINAL_CLOSE §١·١ البند ①** — رسالةُ `4f143bfb` أعلنت رقمَين يخالفان
> مُخرَجَ الأداة، والتاريخُ لا يُعاد كتابتُه فيُصحَّح بالتزامِ تصحيحٍ مُعلَن.

## ما قالته الرسالةُ وما يُخرجه المقياس

| الادّعاءُ في `4f143bfb` | المقيسُ حرفًا (`rpr02_acceptance_scorecard.php` · لقطة `295c628e`) |
|---|---|
| «`#8` الاتجاهيُّ **100٪**» | **97.3٪** — مطابقٌ **1071 من 1101** · **و30 بندًا مخالفٌ باقٍ** |
| «`NO_BRIDGE` **صفرٌ** بلا حكم» — تُقرأ «الرقمُ صفر» | **1٬226 مصنَّفةٌ بشاهدٍ لا مجسورة · وصفرٌ منها بلا حكم** — والرقمُ 1226 كما هو |

## الصياغةُ التي لا تُقرأ خطأً — وتَنسخ ما سواها

- **`#8` تطابقُ السايدبار: 97.3٪ (1071 من 1101) — والهدفُ 100٪ لم يُبلَغ بعدُ.**
- **`NO_BRIDGE`: 1٬226 بندًا محجوبًا على المصالحةِ، كلُّها مصنَّفةٌ بشاهدٍ،
  وصفرٌ منها بلا حكم** — ولا يُكتب «`NO_BRIDGE` صفر».

⚠ وهي **رابعُ مرّةٍ في ملفِّ السايدبارِ وحدَه** يُعلَن بلوغُ 100٪ قبل بلوغِه —
والعلاجُ حاجبٌ لا تذكير: **`G-CLAIM-01`** (`tools/repair01_claim_gate.php`
بخطّافِ `commit-msg`) يردُّ أيَّ رسالةِ التزامٍ تحمل نسبةً لا يُخرجها المقياسُ
حرفًا — **ومُثبَتٌ بحركةِ عدّادِ `guard_denials` بواحدٍ ثمَّ عودة**
(`tools/repair01_claim_gate_negative.php`).
