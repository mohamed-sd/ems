# `GOV_EXEC` — إغلاقُ جولةِ المتبقّي · ما نُفِّذ وما بقي بحاجزِه

> **الأمرُ**: [`orders/GOV_EXEC.txt`](orders/GOV_EXEC.txt) — 25 قسمًا · **والبرومت**:
> [`plan/GOV_EXEC_REMAINDER.prompt.md`](plan/GOV_EXEC_REMAINDER.prompt.md) — ستةُ بنود.
> **القياس**: تشغيلٌ حيٌّ على `cba0d2db` (2026-09-01) · الشجرةُ **صفر**.
> ⛔ **كلُّ رقمٍ هنا شُغِّلت أداتُه في هذه الجلسة** — ولا رقمَ منقولٌ من تقريرٍ سابق.
> ⛔ **والمقاماتُ لا تُجمع في نسبةٍ واحدة** (م 119 · §20).

---

## ⓪ الحكمُ في سطر

**البند ① أُقفل**: كلُّ مساحةٍ لها **دورٌ حيّ** بلغت `CURRENT_RELEASE_TARGET_NOT_BUILT = 0`
— ثمانيَ عشرةَ إدارةً بلا استثناء. **والباقي ستةُ مواضعَ في `EX-DVP` وحدَها**،
وحاجزُها مسمًّى: لا دورَ نوّابٍ حيٌّ يحملها (`OA-09`).

| البند | نقطةُ البدء | الآن | الحال |
|---|---|---|---|
| ① بناءُ مواضعِ الدليل | **314/413** | **407/413** | ✅ كلُّ ذي دورٍ حيٍّ · والستّةُ الباقيةُ `BLOCKED_OWNER` |
| ② جبهةُ الحقول | **891/4039** | **2320/5420** | ⏳ صاعدةٌ — والمقامُ صعد بدخولِ 90 سطحًا جديدًا الجسرَ |
| ③ إثباتُ `IMPLEMENTED_NOT_VERIFIED` | 25 بندًا | **10** | ✅ 15 نُقلت لحاجزِها المكتوب · و`FR-NAV-001` أُغلق بالدليل |
| ④ البنودُ المفتوحة (`OPEN`) | 13 | **13** | ⏳ ثمانيةُ `GAP-*` بشواهدِها الحمراءِ المُعلَنة |
| ⑤ التحقّقُ البشريّ | 0/17 | **0/17** · و**17 بطاقةً جاهزة** | ✅ إعدادُها تمَّ — والتوقيعُ بشريّ |
| ⑥ المحجوبُ بحاجزِه | 11 قرارًا + 8 بوّابات | **+ `OA-09` و`OA-10`** | ✅ مسمًّى ومُتجاوَز |

---

## ① البند ① — تسعُ مساحاتٍ بُنيت في هذه الجولة

| المساحة | مواضعُ بُنيت | الجداولُ الجديدة | حبّاتٌ قائمةٌ استُكملت | برهانُ التصيير |
|---|---|---|---|---|
| `DEP-04` الأسطول | **23** | 16 | 7 (`equipments` · `equipment_documents` · `fleet_equipment_component` · `fleet_equipment_history` · `fleet_depreciation_profile` · `equipments_types` · `mnt_breakdown`) | 25/0 |
| `DEP-12` الموقع | **18** | 13 | 3 (`sites` · `site_day` · `site_day_shift`) + 2 تُقرأ ولا تُكتب | 20/0 |
| `DEP-13` القوى | **14** | 5 | 6 + `equipment_operators` (لا سجلَّ ثالثًا للإنسان) | 16/0 |
| `DEP-02` الموردون | **10** | 0 | 10 (تصحيحُ تسميةِ 100 عمودٍ مبهم) | 12/0 |
| `DEP-01` المبيعات | **9** | 1 | 8 (منها `contracts` نفسُه لمصفوفةِ الالتزامات) | 11/0 |
| `DEP-11` التشغيل | **7** | 4 | 3 | 9/0 |
| `DEP-14` الصيانة | **7** | 2 | 5 (منها `failure_codes` شجرةً مرجعيّة) | 9/0 |
| `EX-CEO` القيادة | **5** | 2 | 3 | 7/0 |
| **المجموع** | **93** | **43** | **47** | **109/0** |

**ولا جدولَ موازٍ** (الدستور §7 · §17): سبعةٌ وأربعون سطحًا بُني فوقَ **مالكِ حبّتِه
القائم**، وأربعةُ أسطحٍ حبّتُها عند إدارةٍ أخرى بُنيت `read` **بلا فعلِ كتابةٍ واحد**
فبقي `UNCONTRACTED_CROSS_DOMAIN_WRITE = 0`.

---

## ② المقاييسُ العشرةُ — حيّةً على `cba0d2db`

| المقياس | البدء | الآن | الحكم |
|---|---|---|---|
| `TARGET_BUILD_COVERAGE` | 314/413 | **407/413** | ⏳ الستّةُ `BLOCKED_OWNER` |
| `RENDERED_TARGET_COVERAGE` | 201/201 | **294/294** | ✅ |
| `BUILT_NOT_RENDERED` | 0 | **0** | ✅ |
| `GROUP_CONFORMANCE` | 201/201 | **294/294** | ✅ |
| `ORDER_CONFORMANCE` | 201/201 | **294/294** | ✅ |
| `ROLE_VISIBILITY_CONFORMANCE` | 215/215 | **308/308** | ✅ |
| `GROUP_HEADER_COVERAGE` | 89/89 | **106/106** | ✅ |
| `EXACT_DEPARTMENT_NAV_CONFORMANCE` | 17/17 | **17/17** | ✅ |
| `GLOBAL_FALLBACK_COUNT` | 0 | **0** | ✅ |
| `TARGET_DERIVED_FROM_CURRENT_WITHOUT_RULING` | 0 | **0** | ✅ |

**ولوحةُ الإدارات**: مطابقٌ تامًّا **7 ⇒ 15** · غيرُ مطابق **0** · بلا دورٍ حيٍّ **2**.

---

## ③ سبعةُ أعطابٍ كُشفت بالقياسِ وأُصلحت في **العُدّةِ** لا في مخرَجِها

| # | ما كُشف | أين أُصلح |
|---|---|---|
| ① | التوصيلُ الخماسيُّ وربطُ الموضعِ **لا يُصيّران**: القفلُ الثالثُ (بندُ `nav_items`) يبقى مغلقًا | `gov_exec_dept_build` تفتحه بنفسِها |
| ② | الاختبارُ السالبُ يقيس **منحةً قائمةً** ويسمّيها عطبًا — والصلاحيةُ مساران | برهانُ التصيير: مرشَّحُ السالبِ من لا يملكها بأيِّهما |
| ③ | سقّاطةُ `GAP-29`: الفعلُ المولَّدُ يستكمل مفتاحَه **باستعلامٍ خام** | مرَّ ببوّابةِ المستأجرِ فعاد المقيسُ إلى أساسِه 607 |
| ④ | هجرةٌ جديدةٌ **تُقادِم** آخرَ تمرينِ تثبيتٍ فيرتدُّ `FR-GOV-014` | عُدّةٌ تُجريه وتقيسه وتقيّده وتنظّف بعدَه (`DR-2026-0015..0022`) |
| ⑤ | `GLOBAL_FALLBACK` قفز 0 ⇒ 1 على `DEP-03` **بلا عطبٍ فيها** — أثرُ مسبارٍ في برهانِ `fin26` | البرهانُ يلتقط حالَ الصفِّ قبلَ التصييرِ ويعيده بعدَه |
| ⑥ | مفتاحُ دفترِ الحقولِ على `screen_id` وحدَه يخالف §24 (`Screens ≠ Placements`) · والجسرُ يعدُّ الزوجَ مرّتَين | المفتاحُ `(screen_id, requirement_id)` · والجسرُ يجمع بالزوج |
| ⑦ | **ستّةٌ وسبعون فعلًا مُعلَنًا ولا واحدٌ مسجَّلٌ** في قاموسِ الأفعال · و**55 جدولًا خارجَ بوّابةِ المستأجر** | العُدّةُ تسجّل الرمزَ مع الشاشة · والجداولُ سُجِّلت `T_TENANT` بعقدِها |

**وبرهانٌ جديدٌ يفصل التصييرَ عن الممارسة**: `tests/gov_exec_action_exercise_proof.php`
— لكلِّ مواصفةٍ سطحٌ يعلن فعلًا: الرمزُ مسجَّل · `POST` بلا رمزِ حمايةٍ يُردّ ·
`POST` صحيحٌ **يكتب صفًّا فعلًا** · ثمَّ يُكنس ما زُرع. **29/0**.

---

## ④ ما بقي — بحاجزِه المسمّى وبمقامِه

| البند | المقيس | الحاجز |
|---|---|---|
| ستّةُ مواضعِ `EX-DVP` | 407/413 | `OA-09` — لا دورَ نوّابٍ حيّ |
| جبهةُ الحقول | **2320/5420** | حجمُ عملٍ — لا عطب |
| `HUMAN_NAV_PASS` | 0/17 · **17 بطاقةً جاهزة** | بشريٌّ بدورٍ حقيقيٍّ لكلِّ مساحة |
| `IMPLEMENTED_NOT_VERIFIED` | **10** | ستّةُ مطالبَ بحواجزَ زمنيّةٍ/قياسيّةٍ مكتوبةٍ + 4 بنودِ جولات |
| `OPEN` | 13 | ثمانيةُ `GAP-*` بشواهدِها الحمراءِ المُعلَنة + 5 قراراتِ ملكية |
| قراراتُ المالك | 11 قيمةً + 10 بوّابات (`OA-01..OA-10`) | `BLOCKED_OWNER` |
| دفعُ الفرعِ للبعيد | محليٌّ متقدِّمٌ بـ12 التزامًا | `BLOCKED_ENVIRONMENT` — بمفتاحِ المالك |

```bash
git push origin repair01/w01-ownership && git push origin --tags
```

---

## ⑤ بطاريّةُ التثبيتِ — خضراءُ في القياسِ الطازج

`الحزام 26/7/0` · `بوّاباتُ UXUI التسع` مجتازة · `سقّاطةُ GAP-29` على أساسِها **607** ·
`بوّابةُ دفترِ الهجرات` **3/0** · `تمرينُ التثبيت` `DR-2026-0022` **pass** (صفرُ كائنٍ
حيٍّ لا يُستنسَخ) · `EFFECT_MISSING = 0` · `GLOBAL_FALLBACK = 0` ·
`UNCONTRACTED_CROSS_DOMAIN_WRITE = 0`.

> **القاعدةُ الختاميّةُ بنصِّ الأمر**: لم يبقَ فرقٌ **غيرُ مفسَّرٍ** بين الملفّاتِ
> الحاكمةِ والنظامِ الفعليّ — وكلُّ ما بقي **مقيسٌ بمقامِه أو محجوبٌ بحاجزٍ مسمًّى**.
