# `RPR-02` #١٣ — تسجيلُ أسطحِ المنصّةِ بقدرتِها أو نطاقِها

> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02_platform_register.php --md` · اللقطة `SNAP-274f2217-20260830-093943`

## ما كان ناقصًا ليس البياناتِ بل الربط

الثلاثون تحمل **كلُّها** قاعدةَ ظهورٍ وصنفَ ظهورٍ وسياسةَ صلاحيّةٍ وصنفَ حارسٍ ودورَ
مالكٍ **مقيسةً** (٣٠/٣٠)، و**١٨** منها تحمل رمزَ مالكٍ أيضًا. ⇒ فالمقياسُ **يربط ما
هو مقيسٌ ولا يخترع بيانًا**.

## القواعدُ الثلاث

| القاعدة | أسطح | الحكم |
|---|---:|---|
| `P1` مالكُه نطاقٌ مُعلَن | **0** | `PLATFORM_SHARED` فيه **صفةُ ظهورٍ عابرٍ للأدوارِ لا فراغُ ملكيّة** |
| `P2` قدرةٌ واحدةٌ من الثمان | **7** | من قدراتِ `AMD-01` §٤·٧ — ⛔ **ولا تاسعةَ تُخترع** |
| `P3` بلا ربطٍ مميِّز | **5** | ⛔ **يُعلَن مفتوحًا بمرشَّحيه** |

⇒ **مسجَّلٌ بمعرِّفِه وقاعدةِ ظهورِه: 7 من 12**.

## ⛔ والتسجيلُ ليس اعتمادًا

و#١٣ يشترط تبريرًا **معتمَدًا**. فكلُّ صفٍّ يولد `AWAITING_OWNER`، **والمقياسُ لا
ينخفض بالتسجيل**: ما ينخفض هو **مجهولُ السبب**. ⇒ فالباقي **فعلُ اعتمادٍ واحدٌ لا
عملُ تحليل**. وقاعدةٌ صلبةٌ في القاعدةِ نفسِها: `APPROVED` **يوجب مرجعَ قرارِ مالك**.

## الأسطحُ بشواهدِها

| السطح | الوسم | القاعدة | النطاق/القدرة |
|---|---|---|---|
| `SCR-0397` | الرئيسية | `P2_CAPABILITY_BOUND` | `ENTERPRISE_SEARCH` |
| `SCR-0403` | قريبا | `P2_CAPABILITY_BOUND` | `SCREEN_REGISTRY` |
| `SCR-0404` | بطاقة المستخدم | `P2_CAPABILITY_BOUND` | `PERMISSION_CAPABILITY` |
| `SCR-0462` | ورقة الإدارة | `P3_UNBOUND_DECLARED` | ⛔ مفتوح |
| `SCR-0468` | لوحتي | `P3_UNBOUND_DECLARED` | ⛔ مفتوح |
| `SCR-0470` | طلباتي | `P2_CAPABILITY_BOUND` | `REQUEST_ROUTING` |
| `SCR-0472` | التنبيهات | `P2_CAPABILITY_BOUND` | `EVENT_INFRA` |
| `SCR-0473` | مكوّنات البوابة | `P2_CAPABILITY_BOUND` | `SCREEN_REGISTRY` |
| `SCR-0476` | سجل تدقيق الظهور | `P3_UNBOUND_DECLARED` | ⛔ مفتوح |
| `SCR-0478` | من يرى ماذا (محاكاة) | `P2_CAPABILITY_BOUND` | `PERMISSION_CAPABILITY` |
| `SCR-0479` | مساحة العمل | `P3_UNBOUND_DECLARED` | ⛔ مفتوح |
| `SCR-0514` | تقرير المنع | `P3_UNBOUND_DECLARED` | ⛔ مفتوح |

### الشواهدُ كاملةً

- **`SCR-0397`** — P2 · بلا رمزِ مالكٍ، ومسارُه `main/global_search.php` أو وسمُه «الرئيسية» يطابق **قدرةً واحدةً لا غير**: `ENTERPRISE_SEARCH` («البحثُ المؤسسيّ») من قدراتِ `AMD-01` §٤·٧ الثمان · ظهورُه `MENU_ITEM` بقاعدةِ `NAV_ITEMS_ACTIVE` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SELF_EARLY` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0403`** — P2 · بلا رمزِ مالكٍ، ومسارُه `main/soon.php` أو وسمُه «قريبا» يطابق **قدرةً واحدةً لا غير**: `SCREEN_REGISTRY` («سجلُّ الشاشات») من قدراتِ `AMD-01` §٤·٧ الثمان · ظهورُه `MENU_ITEM` بقاعدةِ `NAV_ITEMS_ACTIVE` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SHELL` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0404`** — P2 · بلا رمزِ مالكٍ، ومسارُه `main/user_profile.php` أو وسمُه «بطاقة المستخدم» يطابق **قدرةً واحدةً لا غير**: `PERMISSION_CAPABILITY` («قدرةُ الصلاحيات») من قدراتِ `AMD-01` §٤·٧ الثمان · ظهورُه `MENU_ITEM` بقاعدةِ `NAV_ITEMS_ACTIVE` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SHELL` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0462`** — P3 · **لا رمزَ مالكٍ ولا قدرةَ يطابقها** مسارُه ولا وسمُه ⇒ **يُعلَن مفتوحًا ولا يُلحق بأوّلِ مصادفة** · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SELF_EARLY` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0468`** — P3 · **لا رمزَ مالكٍ ولا قدرةَ يطابقها** مسارُه ولا وسمُه ⇒ **يُعلَن مفتوحًا ولا يُلحق بأوّلِ مصادفة** · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SELF_EARLY` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0470`** — P2 · بلا رمزِ مالكٍ، ومسارُه `Portal/my_requests.php` أو وسمُه «طلباتي» يطابق **قدرةً واحدةً لا غير**: `REQUEST_ROUTING` («توجيهُ الطلبات») من قدراتِ `AMD-01` §٤·٧ الثمان · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SELF_EARLY` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0472`** — P2 · بلا رمزِ مالكٍ، ومسارُه `Portal/notifications.php` أو وسمُه «التنبيهات» يطابق **قدرةً واحدةً لا غير**: `EVENT_INFRA` («بنيةُ الأحداث») من قدراتِ `AMD-01` §٤·٧ الثمان · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SELF_EARLY` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0473`** — P2 · بلا رمزِ مالكٍ، ومسارُه `Portal/portal_elements.php` أو وسمُه «مكوّنات البوابة» يطابق **قدرةً واحدةً لا غير**: `SCREEN_REGISTRY` («سجلُّ الشاشات») من قدراتِ `AMD-01` §٤·٧ الثمان · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SELF_EARLY` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0476`** — P3 · **يطابق 2 قدراتٍ** (PERMISSION_CAPABILITY · SECURITY_CAPABILITY) ⇒ **المطابقةُ لا تميّز** ⇒ **يُعلَن مفتوحًا ولا يُلحق بأوّلِ مصادفة** · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SHELL` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0478`** — P2 · بلا رمزِ مالكٍ، ومسارُه `Portal/visibility_simulator.php` أو وسمُه «من يرى ماذا (محاكاة)» يطابق **قدرةً واحدةً لا غير**: `PERMISSION_CAPABILITY` («قدرةُ الصلاحيات») من قدراتِ `AMD-01` §٤·٧ الثمان · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SHELL` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0479`** — P3 · **لا رمزَ مالكٍ ولا قدرةَ يطابقها** مسارُه ولا وسمُه ⇒ **يُعلَن مفتوحًا ولا يُلحق بأوّلِ مصادفة** · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `PLATFORM_ALL_AUTHENTICATED` · حارسُه `SHELL` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
- **`SCR-0514`** — P3 · **يطابق 2 قدراتٍ** (SECURITY_CAPABILITY · SHARED_DOCS_NOTIF) ⇒ **المطابقةُ لا تميّز** ⇒ **يُعلَن مفتوحًا ولا يُلحق بأوّلِ مصادفة** · ظهورُه `DIRECT_ONLY` بقاعدةِ `NO_NAV_NO_PARENT` · سياسةُ صلاحيّتِه `DOMAIN_OWNER_PLUS_GRANTED` · حارسُه `SHELL` · دورُ مالكِه «W2_DECISION:W2-D-01» — **كلُّها مقيسةٌ لا مُعلَنة** · لقطة SNAP-274f2217-20260830-093943
