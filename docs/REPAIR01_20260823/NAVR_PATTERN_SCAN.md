# NAVR — مسحُ النمطِ على بقيّةِ النظام (المطلوب ١١)

> **اللقطة:** `BL-20260831c-f592cdf9` (حزمة GATE-00 الذرّيّة).
> كشوفٌ مقيسةٌ تُسجَّل ولا تُفتح لها إصلاحاتٌ جانبيّةٌ داخل هذا الـSprint —
> كلُّ بندٍ بمقياسِه الحيِّ عند لقطتِه، ويدخل السجلَّ الجامعَ بندًا مرشَّحًا.

## ① Target مولَّدٌ من Current

| الموضع | المقيس | الحكم |
|---|---|---|
| `gov_target_nav` | **682/725 (94٪)** بوثيقة `RENDER-ALIGN*` | ⛔ العطبُ الأمُّ — عولج بطبقةِ `nav_placements` من الورقة، والجدولُ يبقى إرثًا لغيرِ المغطَّى ويُقاس بـ`TARGET_DERIVED_FROM_CURRENT_COUNT` |
| `nav_canonical_current` | لقطةُ حاضرٍ **مسمّاةٌ حاضرًا** | ✔ سليم — لا يدّعي أنه Target |
| `docs/uxui_matrix_20260818.csv` عمود `current_*` | «من التصييرِ الحيِّ — يملؤه المبرمج» **مُعلَنًا** | ✔ سليم — التصريحُ مكتوبٌ في الخانة |

## ② Registry حاكمٌ بلا Production Reader

| السجل | المقيس | الحكم |
|---|---|---|
| `ems_event_subscriptions` | **125 اشتراكًا مُعلَنًا غيرَ مسجَّلِ معالجٍ** (نبضُ `cron_events.log` كلَّ تشغيلة: `bus-unwired: 125`) | ⚠ كشفٌ قائمٌ مُنذَرٌ به — قرارُ التفعيلِ «قلبٌ مُدار» مؤجَّلٌ بنصِّ `cron_events.php` |
| `gov_target_nav` بعد NAVR | قارئُه التصييريُّ يسقط عنه متى وُجدت مواضعُ — يبقى قارئُ الإرثِ لغيرِ المغطَّى | ◆ انتقاليٌّ مقصود — يُقاس حتى تغطيةِ الأدوارِ كلِّها |
| `nav_group_taxonomy` | قارئُه حيٌّ (غيرُ المغطَّى + بقايا خارجَ الدورة) | ✔ حيٌّ — لكنه **أداةٌ خارجَ الدورةِ** لا دورةَ إدارة (حُكم في `NAVR_ROOT_AUDIT` §④) |

## ③ علاقةُ 1:1 والحقيقةُ 1:N

| الموضع | المقيس | الحكم |
|---|---|---|
| `nav_route_group` (PK=`route`) | 557 مسارًا برأسٍ واحدٍ عالميّ | ⛔ العطبُ الأمُّ — عولج بـ`nav_placements` (الشاشةُ بموضعٍ لكلِّ مساحة)؛ الجدولُ يبقى احتياطَ غيرِ المغطَّى |
| `repair01_screen_registry.state_model_ref` | مرجعُ آلةٍ واحدٌ للسطح — و63 سطحًا كيانُه من **كِيتٍ مشترك** | ◆ مقيسٌ ومحكومٌ سلفًا (RPR-02 هدف 4: ليست معاملاتٍ بحبّتِها) — لا فعلَ الآن |
| `users.role` vs `users.role_id` | `role` مملوءٌ و`role_id` فارغ (ذاكرة الحملة) | ⚠ ازدواجُ عمودٍ قائمٌ موثَّق — بندٌ مرشَّحٌ للسجلِّ لا إصلاحَ جانبيًّا |
| `nav_ws_roles` الجديد | `uq(role_id, binding)` يسمح بـPRIMARY واحدة | ✔ بالتصميم — وSECONDARY بابُها مفتوحٌ للشاشةِ المشتركةِ بموضعين |

## ④ Fallback صامتٌ إلى Legacy

| الموضع | المقيس | الحكم |
|---|---|---|
| مُصيِّرُ السايدبار قبل NAVR | إعلانُ 17 مجموعةً للدور 26 يُعرض 8 رؤوسِ تصنيفٍ **بلا واقعةٍ مسجَّلة** | ⛔ عولج: `gov_nav_findings` + `EMS_NAV_STRICT` — و`GLOBAL_FALLBACK_COUNT=0` مقيسًا الآن |
| `get_module_permissions` | القالبُ النافذُ يحكم حصرًا وإلا `role_permissions` | ✔ **مُعلَنٌ لا صامت** — دلالةُ الدالةِ منصوصةٌ ومختبرةٌ (فصل ٩٢/٩٢) |
| بوّابةُ السلّم `EMS_UNIT_LADDER` | وضعُ `monitor` يسجّل ويمرّر | ✔ مُعلَنٌ بمفتاحِ بيئةٍ ويُقاس (RPR-03 M6) — انتظارُ وقائعَ لا صمت |

## ⑤ Metric خضراءُ بمقامٍ لا يجيب عن السؤال

| المقياس | المقيس | الحكم |
|---|---|---|
| RPR-02 هدف 8 «تطابق السايدبار مع الملف» = 100٪ | مقامُه **المجسورُ بمعرِّفِ شاشةٍ فقط** (1155) ويحجب `NO_BRIDGE`=1172 — وسؤالُ المالكِ على **342 شاشةَ ورقة** | ⚠ **مقيسٌ وموثَّقٌ** (`SIDEBAR_GUIDE_WHY` §⑦): مقامان بسؤالين — والمرجعُ لسؤالِ الدليلِ صار `NAVR_METRICS.md` بمقاماتِه العشرة |
| بوّابةُ NF-24 | تمنع الازديادَ ولا تدّعي الإغلاق (نصُّها) | ✔ صادقةُ العنوان |

## ⑥ Built ≠ Wired ≠ Verified

| الموضع | المقيس | الحكم |
|---|---|---|
| نافذةُ ظلِّ الاعتماد | مبنيّةٌ وموصولةٌ وشاهدُها 11/11 — **والمرصودُ الحقيقيُّ 0 قرار** | ✔ حالتُها الصادقةُ `IMPLEMENTED_NOT_VERIFIED` في السجلِّ الجامع (CL-FR-APP-001) |
| أحداثُ الأعمال | العقودُ كاملةٌ والمؤشّراتُ عند الرأس — والأثرُ Verified لمسارِ التسويةِ وحدَه نمطًا | ◆ محكومٌ بلوحةِ RPR-03 بمقاماتٍ منفصلة |
| مواضعُ NAVR الجديدة | Built=240/374 · Rendered=182/202 · **وHuman Render Verification لم يقع** | ⛔ **بوّابةُ الإغلاقِ النهائيِّ بشريّةٌ** (المطلوب الختاميّ): لا يُعلَن Sidebar PASS قبل تحقّقٍ بشريٍّ بدورٍ حقيقيٍّ لكلِّ إدارة — تُقيَّد `BLOCKED_UAT` |
