# RPR-W11 — عقودُ الأثرِ لأحداثِ الماليّةِ والخزينة

> ⛔ **مولَّدٌ من المخزن — لا تحرّره يدويًّا**: `php tools/repair01_w11_docs.php` يعيد كتابتَه.

⛔ **حدثٌ بلا عقدِ أثرٍ مسجَّلٍ لا يُنفَّذ** — والعقدُ يُكتب في `repair01_events` قبل أوّلِ إطلاق. و**النشرُ بلا مستهلكٍ نشطٍ مرفوضٌ في الجذرِ نفسِه** (`BUS_NO_CONSUMER`)، فالعقدُ شرطُ نشرٍ منفَّذٌ لا وثيقة.


## `acc.account.reconciled` — اقفال مطابقة حساب رقابي

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/acc_reconciliations.php` |
| المحفِّز | اقفال جلسة مطابقة بصفر فرق مفتوح |
| الحمولةُ الدنيا | `recon_id · period_id · account_code` |
| المستهلكُ بالاسم | `AccountingCycleService::onAccountReconciled` |
| أثرُه المقيس | **يغلق بند مطابقة الذمم المدينة او الدائنة في قائمة الاقفال بحسب طبيعة الحساب** |
| الشروطُ المسبقة | صفر بند فرق مفتوح ومن اعد لا يقفل |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.account.reconciled` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | جلسة مطابقة جديدة تحمل مرجع السابقة |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.adjustment.posted` — ترحيل تسوية نهاية الفترة

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/acc_adjustments.php` |
| المحفِّز | اعتماد تسوية بمستند اساسها |
| الحمولةُ الدنيا | `adjustment_id · period_id · adj_kind` |
| المستهلكُ بالاسم | `AccountingCycleService::onAdjustmentPosted` |
| أثرُه المقيس | **يغلق بند ترحيل التسويات في قائمة اقفال الفترة فيتقدم شرط الاقفال** |
| الشروطُ المسبقة | مستند اساس مكتوب وفترة مفتوحة ومن اعد لا يعتمد |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.adjustment.posted` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | العكس في الفترة التالية لا تعديل المرحلة |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.entry.posted` — ترحيل قيد على طلب مقبول

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/journal_form_fin.php` |
| المحفِّز | تثبيت قيد متوازن في فترة مفتوحة على طلب اعتراف مقبول |
| الحمولةُ الدنيا | `entry_id · request_id · total_debit · total_credit` |
| المستهلكُ بالاسم | `AccountingCycleService::onEntryPosted` |
| أثرُه المقيس | **يعيد احتساب التعرض الائتماني لكل عميل بحد فعال فتقرا الاتاحة من الذمم القائمة** |
| الشروطُ المسبقة | طلب مقبول وفترة مفتوحة ومدين يساوي دائن |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.entry.posted` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | التصحيح بقيد عكسي يحمل مرجع الاصل |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.period.closed` — اقفال الفترة المحاسبية

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/periods_fin.php` |
| المحفِّز | اقفال فترة بميزان متوازن وصفر بند حاجب |
| الحمولةُ الدنيا | `period_id · run_id` |
| المستهلكُ بالاسم | `AccountingCycleService::onPeriodClosed` |
| أثرُه المقيس | **يمنع الترحيل على الفترة فيصير الاقفال اثباتا لا اعلانا** |
| الشروطُ المسبقة | ميزان متوازن وصفر بند حاجب ومن اجرى الميزان لا يقفل |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.period.closed` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | اعادة الفتح بطلب معتمد بقاعدته |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.period.reopened` — اعادة فتح فترة مقفلة

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/acc_reopen_governance.php` |
| المحفِّز | اعتماد طلب اعادة فتح بمبرره ونطاقه |
| الحمولةُ الدنيا | `reopen_id · period_id` |
| المستهلكُ بالاسم | `AccountingCycleService::onPeriodReopened` |
| أثرُه المقيس | **يعيد السماح بالترحيل في نطاق الطلب المعتمد وحده** |
| الشروطُ المسبقة | مبرر وقاعدة صلاحية ونطاق ووحدات ومن طلب لا يعتمد |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.period.reopened` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | اعادة الاقفال بعد التصحيح |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.recognition.decided` — قرار المالية في طلب الاعتراف

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/journal_form_fin.php` |
| المحفِّز | قبول المالية للطلب او رده بسبب مكتوب |
| الحمولةُ الدنيا | `request_id · decision · decided_by` |
| المستهلكُ بالاسم | `AccountingCycleService::onRecognitionDecided` |
| أثرُه المقيس | **ينشئ واقعة الاعتراف المالية عند القبول ويختمها في الطلب — والرد لا ينشئ شيئا** |
| الشروطُ المسبقة | الطلب معلق ومن طلب لا يقرر |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.recognition.decided` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | الرد يقيد بسببه ولا واقعة تنشا |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.recognition.requested` — طلب اعتراف من نطاق مصدري

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/journal_form_fin.php` |
| المحفِّز | اصدار نطاق مصدري طلب اعتراف بواقعة ذات اثر مالي |
| الحمولةُ الدنيا | `source_module · source_ref · amount · currency` |
| المستهلكُ بالاسم | `AccountingCycleService::onRecognitionRequested` |
| أثرُه المقيس | **يحل فترة الطلب من تقويم الفترات ويكتبها في الطلب فلا يرحل طلب بلا فترة** |
| الشروطُ المسبقة | المصدر ليس المالية والمرجع مكتوب والمبلغ موجب |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.recognition.requested` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | الطلب يبقى معلقا ويعاد اصداره من مصدره |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.statements.issued` — اصدار القوائم المالية

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/financial_statements_fin.php` |
| المحفِّز | اشتقاق القوائم بعد الاقفال من الميزان المقفل |
| الحمولةُ الدنيا | `period_id · run_id` |
| المستهلكُ بالاسم | `AccountingCycleService::onStatementsIssued` |
| أثرُه المقيس | **يغلق بند اصدار التقارير في قائمة الاقفال فتكتمل دورة الفترة** |
| الشروطُ المسبقة | الفترة مقفلة وميزانها متوازن |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:acc.statements.issued` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | اصدار نسخة جديدة بعد اعادة الفتح والاقفال |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `acc.trial.balanced` — جولة ميزان مراجعة

| البند | القيمة |
|---|---|
| المصدر | 05 الإدارة المالية · `Finance/acc_trial_balance.php` |
| المحفِّز | اشتقاق ميزان الفترة من القيود المنشورة |
| الحمولةُ الدنيا | `run_id · period_id · balanced` |
| المستهلكُ بالاسم | `AccountingCycleService::onTrialBalanced` |
| أثرُه المقيس | **يغلق بند مراجعة الفروق في قائمة الاقفال عند التوازن وحده — وغير المتوازن لا يغلق شيئا** |
| الشروطُ المسبقة | الفترة معرفة والقيود منشورة |
| سياسةُ الإعادة | إعادة تنشئ جولة جديدة بمرجعها |
| مفتاحُ منعِ التكرار | `w11:acc.trial.balanced` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | الجولة لقطة بزمنها ولا تعدل |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `tre.bank.reconciled` — اقفال جلسة مطابقة بنكية

| البند | القيمة |
|---|---|
| المصدر | 06 إدارة الخزينة · `Finance/bank_reconciliation_fin.php` |
| المحفِّز | اقفال كشف بصفر فرق مفتوح |
| الحمولةُ الدنيا | `statement_id · period_to` |
| المستهلكُ بالاسم | `TreasuryCycleService::onBankReconciled` |
| أثرُه المقيس | **يغلق بند مطابقة البنك في قائمة اقفال الفترة التي يقع فيها الكشف** |
| الشروطُ المسبقة | صفر بند فرق مفتوح ومن اعد لا يراجع |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:tre.bank.reconciled` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | جلسة مطابقة جديدة تحمل مرجع السابقة |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `tre.count.approved` — اعتماد جرد نقدي

| البند | القيمة |
|---|---|
| المصدر | 06 إدارة الخزينة · `Finance/tre_cash_count.php` |
| المحفِّز | اعتماد جلسة جرد بلجنتها ومعالجة فرقها |
| الحمولةُ الدنيا | `count_id · box_id · difference` |
| المستهلكُ بالاسم | `TreasuryCycleService::onCashCountApproved` |
| أثرُه المقيس | **يكتب حركة تسوية بفرق الجرد فيتطابق الرصيد الدفتري مع المعدود** |
| الشروطُ المسبقة | لجنة من عضوين فاكثر ومن عد لا يعتمد والفرق بمعالجة مكتوبة |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:tre.count.approved` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | جلسة جرد جديدة تحمل مرجع السابقة |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `tre.payment.executed` — تنفيذ امر دفع

| البند | القيمة |
|---|---|
| المصدر | 06 إدارة الخزينة · `Finance/tre_pay_batch.php` |
| المحفِّز | تنفيذ امر دفع لمستفيد محقق من طلب معتمد |
| الحمولةُ الدنيا | `payment_id · vessel_id · beneficiary_id` |
| المستهلكُ بالاسم | `TreasuryCycleService::onPaymentExecuted` |
| أثرُه المقيس | **يكتب حركة خروج نقد من الوعاء فيتغير رصيده المشتق** |
| الشروطُ المسبقة | الطلب معتمد والمستفيد محقق ومن اعد لا ينفذ |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:tre.payment.executed` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | الاسترداد بسند مستقل لا بمحو الحركة |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

## `tre.receipt.allocated` — تخصيص تحصيل على فواتير

| البند | القيمة |
|---|---|
| المصدر | 06 إدارة الخزينة · `Finance/tre_allocations.php` |
| المحفِّز | تخصيص سند قبض على فاتورة او اكثر |
| الحمولةُ الدنيا | `receipt_id · lines` |
| المستهلكُ بالاسم | `TreasuryCycleService::onReceiptAllocated` |
| أثرُه المقيس | **يعيد احتساب المحصل والمتبقي وحالة كل ذمة مخصص عليها فتقفل الفاتورة بتغطيتها** |
| الشروطُ المسبقة | السند قبض ومجموع التخصيص لا يتجاوز مبلغه |
| سياسةُ الإعادة | إعادة بلا أثر |
| مفتاحُ منعِ التكرار | `w11:tre.receipt.allocated` |
| سلوكُ الفشل | يرفع ولا يبتلع |
| التعويض | عكس التخصيص بسطر مقابل لا بمحو السطر |
| حالةُ العقد | `RECORDED` بقاعدة `W11_EVENT_CONTRACT` |

---

**المقيس:** عقودُ أثرٍ مسجَّلة **13** · كلٌّ بمستهلكٍ نشطٍ في `event_consumers` وناشرٍ مُثبَتٍ من القرص (`W11-20`).
