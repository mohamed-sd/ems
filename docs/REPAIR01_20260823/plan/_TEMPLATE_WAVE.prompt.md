# RPR-W03 — الجداول  ·  المرحلة ٣ من ٩
> ملفُّ تنفيذٍ مستقلّ. الصقْه في جلسةِ Claude Code نظيفةٍ داخل `C:\wamp64\www\ems`.

---
## ٠ · اقرأْ قبل أيِّ فعل
1. `docs/REPAIR01_20260823/plan/_CONTEXT.md`      ← الأعرافُ والمحظورات
2. `tools/repair01_tasks.json`                     ← الدفتر · مصدرُ الحقيقة
3. `docs/REPAIR01_20260823/plan/W02_CLOSURE.md`    ← ماذا أُغلق فعلًا قبلك
4. `docs/REPAIR01_20260823/plan/W03_FEEDBACK.md`   ← ملاحظاتُ التشغيلِ في الأيّام التسعة

## ١ · إعادةُ الأساس  (إلزاميّة — لا تتخطَّها)
شغّلْ بوّاباتِ المراحل السابقة **قبل** لمسِ أيِّ ملف:
```
php tools/repair01_w1_gate.php && php tools/repair01_w2_gate.php
```
- خضراءُ كلُّها ← تابعْ إلى §٢.
- سقطت واحدة ← **قفْ**. أبلغْ بالرقم الساقط ولا تبدأ المرحلةَ الجديدة.
  (تسعةُ أيّامِ استخدامٍ قد تكون أرجعت ما أُغلق — والبناءُ فوق تراجعٍ صامتٍ يضاعف العمل.)
وشغّلْ `git status` — الشجرةُ المُفسَدةُ تُشخَّص قبل الشيفرة لا بعدها.

## ٢ · النطاق  (٢١٤ بندًا — مُدرَجةٌ حرفيًّا لا بالإحالة)
| الرمز | السطح | الجذر | المكوِّن | المعيار |
|---|---|---|---|---|
| RPR-0147 | `Operations/movement_orders.php` | العُدَّة | مفتاح الجداول | عمودُ الإجراءات أوّلًا |
| … | … | … | … | … |

**رتّبْ هكذا:** العُدَّةُ أوّلًا (١٩١ بندًا ترثُ من ٣ ملفّات) ← ثمّ ٢٣ شاشةً يدويّة.
عكسُ الترتيب = إصلاحُ الشاشةِ مرّتين.

## ٣ · العقدُ التصميميّ
المصادرُ الوحيدةُ المسموحة: `ems-tables` · `ems-forms.css` · `ems-filters.css` ·
`ems-statcards.css` · `EmsDetailsModal` · زرُّ Excel المركزيّ.
⛔ لا CSS جديدًا في الصفحة. البندُ الذي يحتاج نمطًا خارجَ العُدَّةِ يُسجَّل استثناءً في §٧ ولا يُنفَّذ.

## ٤ · المحظورات
- ⛔ `php migrate.php up` — ٣٥ هجرةً «معلَّقةً» أثرُها قائمٌ في المخطَّط.
- ⛔ الدفعُ إلى `main` — الفرعُ من `main`، والدفعُ للمستخدم (SSH بعبارة مرور).
- ⛔ حذفُ صفٍّ أو جدول. التعديلُ على العرضِ لا على البيانات.

## ٥ · البوّابة
```
php tools/repair01_w3_gate.php
```
المتوقَّع: `W3 tables: 214/214 · exceptions 0`

## ٦ · الفحصُ السلبيّ  (شرطُ إغلاقٍ لا زينة)
اكسرْ بندًا واحدًا عمدًا ← أعِدْ تشغيلَ البوّابة ← يجب أن تسقط ← أرجِعْه.
لم تسقط؟ البوّابةُ عمياءُ والمرحلةُ **غيرُ مُغلقة** مهما كان الرقمُ أخضر.
ارسُ على أسماء الأصناف لا على العبارات — نصُّ حالةِ الخطأ يطابق العبارةَ فيُخضِرُّ كذبًا.

## ٧ · المخرَجات  (اكتبها قبل أن تُنهي)
- [ ] `tools/repair01_tasks.json` — حقلا `state` و`measured` محدَّثان لكلِّ بندٍ لُمس
- [ ] `docs/REPAIR01_20260823/plan/W03_CLOSURE.md` — المُنجَز · المؤجَّل بسببه · الاستثناءات
- [ ] `docs/REPAIR01_20260823/plan/W04_FEEDBACK.md` — ملفٌّ فارغٌ يملؤه المستخدم في التسعة أيّام
- [ ] التزامٌ واحدٌ للمرحلة كلِّها (تراجعٌ بأمرٍ واحد)

## ٨ · تعريفُ الإنجاز
§١ خضراء · §٥ خضراء · §٦ يسقط ويرجع · §٧ مكتملة.
ناقصٌ واحدٌ = المرحلةُ مفتوحة. لا تُعلنْ إغلاقًا جزئيًّا.

## ٩ · سلِّمْ في آخر ردٍّ
الرقمَ المقيس · قائمةَ ما لم يُنفَّذ ولماذا · الاستثناءاتِ المسجَّلة · أمرَ التراجع.
