# `NAV-ARCH-02` — طورُ القلبِ والتعميم · **ما تبقّى**

> **اقرأ هذا الملفَّ كاملًا قبل أن تكتب سطرًا.**
> **الأمرُ الحاكم:** [`orders/NAV_ARCH_02.txt`](../orders/NAV_ARCH_02.txt) — 43 قسمًا · **ما زال نافذًا**.
> **والمراجعةُ التي حدّدت المتبقّي:** [`NAV_ARCH_02_REVERSE_AUDIT.md`](../NAV_ARCH_02_REVERSE_AUDIT.md)
> ⛔ **اقرأها أوّلًا** — فيها الأربعةُ الأدلّةُ التي تُثبت أنَّ المُصيِّرَ غيرُ موصول.

---

# ⛔⛔⛔ ٠ · قواعدُ لا تُخالَف

1. **لا توقّف (§35)**: `Block the affected Placement, not the program`.
   ⛔ «هل أكمل؟» · ⛔ «أنتظر توجيهك» · **والبديلُ**: سطرُ تقدُّمٍ ثمَّ البندُ
   التاليَ **في الرسالةِ نفسِها**.
2. **⛔ لا تُعِدْ ما بُني** — **28 قسمًا من 43 مُنفَّذٌ بالدليل**، وهذه بواباتُ
   ارتدادٍ تُشغَّل لا تُبنى:
   `nav_workspaces` **8/8 عمودًا** · `nav_workspace_placements` **15/15 عمودًا**
   و**625 موضعًا** · الاختباراتُ السالبةُ **9/0** · المخرجاتُ **13 ورقة** ·
   `TARGET_GAP_CANDIDATE` **0** · تصعيدٌ إلى المالك **0**.
   ⛔ **وأيُّ هبوطٍ في واحدةٍ يوقف البندَ ويُعالَج فورًا.**
3. **⛔ ولا تُخفى شاشةٌ مستعمَلةٌ بلا** تصنيفٍ وبديلِ وصولٍ ودليلٍ ومصير (§4).
   **و«اختفى من السايدبار» ≠ «سُحبت صلاحيّتُه»** (§42).
4. **`G-CLAIM-01` نافذ**: الكسرُ المقيسُ لا نسبةٌ مخترَعة.
   **وكلُّ تغييرِ قاعدةٍ بهجرةٍ لها عكسٌ `_down.php`** · ⛔ ولا تعديلَ يدويّ ·
   ⛔ **ولا تتخطَّ خطّافًا بـ`--no-verify`**.
5. **⚠ جلسةٌ أخرى قد تعمل** — تحقَّقْ من نظافةِ الشجرةِ قبل كلِّ قياس.
   ⚠ **وساعةُ الجهازِ ترجع** — افرزِ الالتزاماتِ بالسلفِ لا بالختم.
6. **⚠ وقع عطبُ مستودعٍ سابقًا** (مؤشِّرُ الفرعِ صار أصفارًا) — **التزمْ دفعةً
   دفعةً ولا تُطِلْ عملًا غيرَ ملتزَم**.

---

# ١ · نقطةُ البدء — مقيسةٌ حيًّا على `5dd382f8`

| المفردة | القيمة |
|---|---|
| الجذر · PHP | `C:\wamp64\www\ems` · `/c/wamp64/bin/php/php8.3.28/php.exe` |
| الفرع | `repair01/w01-ownership` |

## ⭐ العطبُ الجوهريُّ — مُثبَتٌ بأربعةِ قياسات

**المُصيِّرُ الجديدُ مبنيٌّ ولا يعمل في الإنتاج:**

| الفحص | النتيجة |
|---|---|
| `nav_workspace_placements` في `includes/unified_nav.php` | **صفرُ ذكر** |
| `includes/navarch_renderer.php` (184 سطرًا · `navarch_render()`) | **صفرُ مستهلكٍ إنتاجيّ** |
| ما يقرأه الإنتاجُ فعلًا | `nav_items:105` · `nav_group_taxonomy:980` · `nav_route_group:991` · `emsNavTaxonomy():974` تُستدعى في `1056` و`1575` |
| **بنودُ `DEP-11`** | **الحيُّ 90** مقابل **الظلِّ 19** ⇒ **فرقُ 71 بندًا** |

⇒ **سايدبارُ المستخدمِ لم يتغيّر** · **واللوحةُ خضراءُ لأنَّ
`tools/navarch/metrics.php:30` يستدعي المُصيِّرَ الجديدَ فيقيس الظلَّ لا الإنتاج.**

⛔ **وهذا ليس خطأً في العملِ السابق** — §30 يوجب الظلَّ **قبل** القلب،
والظلُّ ثبت. **فهذا طورُه التالي.**

---

# ٢ · بنودُ العملِ الستّة

## ⭐⭐ ① قلبُ المُصيِّر (§20·§21·§22) — **البندُ الحاسم**

**المعادلةُ الجديدةُ حرفًا:**
```
Rendered Sidebar = Active Workspace Placements ∩ User Authorized Screens
```

**والواجهةُ جاهزة**: `navarch_render($conn, $wsId, $roleId, array $opts = array())`
في `includes/navarch_renderer.php`.

**العمل — ⛔ بمفتاحٍ لا بقطعٍ مباشر:**
1. **أضِفْ مفتاحَ بيئةٍ** (‏على نمطِ `EMS_NAV_WORKSPACE` القائم) يحوّل
   `renderUnifiedNavigationV2` إلى `navarch_render` — **افتراضُه القديمُ أوّلًا**.
2. **صيِّرْ كلَّ مساحةٍ بالمفتاحَين** وقارنْ — ⛔ ولا تقلبِ الافتراضَ قبل أن
   يخضرَّ الفرق.
3. **اقلبِ الافتراضَ لـ`DEP-11` وحدَها أوّلًا** ثمَّ عمِّمْ.
4. **وأثبتْ بالتصييرِ الحيِّ** أنَّ `DEP-11` صار **19 موضعًا لا 90**.

⛔ **ولا يدخل السايدبارَ إلّا `PRIMARY` و`SECONDARY_APPROVED`** (§9).
**و`No Placement = No Sidebar Render` حتى لو `can_view = 1`** (§22).

## ② نزعُ السقوطاتِ الخمس (§23) — **بمواضعِها المقيسة**

| السقوط | الموضع | الحكم |
|---|---|---|
| `nav_items` بالدور | `unified_nav.php:105` | **يُنزع** — لا يُنشئ ظهورًا |
| `nav_group_taxonomy` | `:980` | **يُنزع** |
| `nav_route_group` | `:991` | **يُنزع** |
| `emsNavTaxonomy()` | `:974` · تُستدعى `:1056` و`:1575` | **تُنزع باستدعاءَيها** |
| «الدورُ يراها فيُعرَض» | `can_view` كسببِ ظهور | **يُنزع** — يبقى كحارسِ وصول |

**الهدف**: `GLOBAL_FALLBACK_COUNT = 0` · `LEGACY_FALLBACK_RENDER_COUNT = 0`
**مقيسَين على الإنتاجِ لا على الظلّ**.

⛔ **ولا تُنزع دفعةً واحدة** — واحدًا واحدًا مع تصييرٍ وقياسٍ بعد كلٍّ.

## ③ إعادةُ قصِّ الأساس (§5)

`NAV_ARCH_BASELINE` الحاليُّ (`NAB-8CD4A8954F`) **سجّل رسالةَ خطأٍ مكانَ
`Commit Hash`** — لأنّه قُصَّ أثناءَ عطبِ `git`.
**أعِدْ قصَّه على مستودعٍ سليمٍ** بالستّةِ كاملةً — ⛔ ولا تُصحَّحْ قيمةٌ يدويًّا.

## ④ إتمامُ أنواعِ الموضعِ التسعة (§9)

**`CONTEXTUAL_ACTION` و`UTILITY` معرَّفتان بصفرِ صفّ.** راجعِ المواضعَ
الـ625 — **فالحالةُ ج في §12 توجب `CONTEXTUAL_ACTION`** لكلِّ إجراءٍ سياقيٍّ
عابر. ⛔ **ولا تملأْ نوعًا لتكتمل الصورة** — إن لم ينطبق فاكتبْ حكمَ عدمِ
انطباقِه.

## ⑤ التعميمُ على المساحاتِ الباقية (§36)

**`EXACT_WORKSPACE_NAV_CONFORMANCE` = 6/18** ⇒ **18/18**.

⛔ **ولا تعميمَ قبل إثباتِ أنَّ المُصيِّرَ الجديدَ لا يحتاج سقوطًا إرثيًّا**
(§36 حرفًا). والترتيبُ: 16 إدارةً ← `EX-CEO` ← `EX-DVP` ← `WS-MY` ← `IAF` ←
أدواتُ المنصّة.

**ولكلِّ مساحةٍ**: صيِّرْ ⇒ قِسْ ⇒ أغلقْ بتسعةِ أصفارِ §25 ⇒ التزمْ ⇒ **انتقلْ
فورًا**.

## ⑥ التحقّقُ البشريُّ (§31) — **يُعَدُّ ويُسلَّم ولا يُنتظَر**

`HUMAN_UAT_PASS` = `PENDING` · و`HUMAN_DEPARTMENT_PASS` = **0/17**.

**ورقةُ `10 DEP11_PILOT_UAT` جاهزةٌ بنقاطِ §31 العشر.** جهِّزْ مثلَها لكلِّ
مساحةٍ تبلغ الأصفار، **وسلِّمْها بسطرٍ واحدٍ وامضِ**.

⛔ **ولا تنتحلْ نتيجةً بشريّة** — والبندُ يبقى `PENDING` بحاجزِه.

## ⑦ التقاعدُ المرحليُّ (§33) — **بعد القلبِ لا قبلَه**

خمسُ مراحل: `A` تعايشٌ بوسم ← `B` تحويل ← `C` إزالةٌ من السايدبار ←
`D` إيقافُ المسار ← `E` دليلُ التقاعد.

⛔ **ولا يُحذف مسارٌ تاريخيٌّ قبل فحصِ**: المفضّلات · الروابطِ الداخليّة ·
المهامّ · الإشعارات · التقارير · التكاملات (§33).

---

# ٣ · معاييرُ الإغلاق

## الطيّار (§40) — **11/12 بلغت · والناقصُ واحد**

`TARGET_NAV_RECALL` 100٪ · `WRONG_GROUP`/`ORDER`/`LABEL` 0 ·
`PERMISSION_ONLY_RENDERED_ITEM` 0 · `UNAPPROVED_SECONDARY_PLACEMENT` 0 ·
`ACTIVE_LEGACY_WITHOUT_DISPOSITION` 0 · `UNEXPLAINED_EXTRA_MENU_ITEM` 0 ·
`GLOBAL_FALLBACK_COUNT` 0 · `LEGACY_FALLBACK_RENDER_COUNT` 0 ·
`TARGET_LINEAGE_BROKEN` 0 ⇒ **⛔ والناقصُ `HUMAN_UAT_PASS = YES`**.

⭐ **وأعِدْ قياسَ الأحدَ عشرَ على الإنتاجِ بعد القلب** — فقيمتُها الحاليّةُ
عن الظلِّ وحدَه.

## البرنامجُ كلُّه (§41)

17/17 إدارةً · القيادة · `WS-MY` · `IAF` · والقشرةُ منفصلة ·
**ولا صلاحيّةَ تُنشئ موضعًا** · ولا إرثَ يظهر بلا حكم · ولا سقوطَ عامّ ·
وكلُّ موضعٍ له مصدرٌ حاكم.

---

# ٤ · ترتيبُ التنفيذ

```
①  تحقَّقْ أنَّ الشجرةَ نظيفةٌ · واقرأ NAV_ARCH_02_REVERSE_AUDIT.md
②  أعِدْ قصَّ NAV_ARCH_BASELINE على مستودعٍ سليمٍ (البند ③)
③  ⭐ البند ①: اقلبِ المُصيِّرَ بمفتاحٍ — DEP-11 أوّلًا
    · قارنِ الظلَّ بالحيِّ وأثبتْ 90 ⇒ 19
④  البند ②: انزعِ السقوطاتِ الخمسَ واحدًا واحدًا بمواضعِها المقيسة
    · وأعِدْ قياسَ FALLBACK على الإنتاجِ بعد كلِّ نزع
⑤  أعِدْ قياسَ الأحدَ عشرَ على الإنتاجِ وأثبتْ بقاءَها صفرًا
⑥  البند ④: أتمِمْ نوعَي الموضعِ أو اكتبْ حكمَ عدمِ انطباقِهما
⑦  البند ⑤: عمِّمْ على 17 مساحةً — واحدةً واحدةً بأصفارِ §25
⑧  البند ⑥: بطاقاتُ التحقّقِ البشريِّ تُسلَّم تباعًا ولا تُنتظَر
⑨  البند ⑦: التقاعدُ بمراحلِه الخمسِ بعد القلب
⑩  أعِدْ توليدَ المخرجاتِ الاثني عشرَ من لقطةٍ واحدةٍ بختمِها
⑪  حدِّثِ السجلَّ الجامعَ · والتزمْ
⑫  ⛔ ولا تتوقّفْ بين أيِّ خطوتَين — والمحجوبُ يُسمّى حاجزُه ويُتجاوَز
```

---

# ٥ · سطرُ التقدّم

```
[<BL>] <البند/المساحة> · <المقياس> <من> ⇒ <إلى> · دليل: <الأداة> · التالي: <البند>
```

⛔ **ثمَّ ابدأِ التاليَ في الرسالةِ نفسِها.**

---

> **بنصِّ الأمر §22**: *«`No Placement = No Sidebar Render` — حتى لو
> `can_view = true`. الصلاحيّةُ تبقى صحيحة، لكنَّ الشاشةَ لا تظهر في سايدبارِ
> هذه المساحة.»*
