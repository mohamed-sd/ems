# RPR-W09 — آلاتُ الحالةِ لكيانات المشتريات والمخازن

> ⛔ **مولَّدةٌ من `repair01_w9_states` — لا تحرّرها يدويًّا**: `php tools/_w9_docs.php` يعيد كتابتَها من السجلّ.

**القاعدةُ الحاكمة:** لا نصَّ حالةٍ حرّ. ولكلِّ كيانٍ **ممنوعٌ صريحٌ بسببٍ مكتوب** — والممنوعُ بلا سببٍ ليس ممنوعًا بل نسيانًا.


## `proc_award`

**مسموحٌ 1 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `draft` | `approved` | صاحب صلاحية الاعتماد | الفائز له عرض والمعايير معلنة والفائز غير الأدنى بسبب مكتوب | محضر ترسية معتمد | بوابة فصل الواجبات |

**ممنوعٌ صراحةً:**

- ⛔ `approved` ⇐ `draft` — المحضر المعتمد صدر عنه أمر شراء فإرجاعه للمسودة يفك سند الأمر

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `draft` ⇐ `approved`: يفتح بمحضر إلغاء ترسية · التصحيح: التصحيح بمحضر جديد يحمل مرجع السابق

## `proc_count_session`

**مسموحٌ 2 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `draft` | `reviewed` | مراجع الجرد | بند واحد على الأقل | كشف جرد | توقيع المراجع |
| `reviewed` | `approved` | مدير المخازن | كل فرق بقرار تسوية مسبب ومعتمد غير العاد | محضر جرد معتمد | بوابة فصل الواجبات |

**ممنوعٌ صراحةً:**

- ⛔ `draft` ⇐ `approved` — اعتماد جرد بلا مراجعة يفقد الطبقة التي تكشف خطأ العد

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `draft` ⇐ `reviewed`: يفتح بإرجاع للعد · التصحيح: التصحيح بإعادة عد بند لا بكتابة فوق الكشف
- `reviewed` ⇐ `approved`: يفتح بقرار مكتوب قبل الإقفال · التصحيح: التصحيح بجلسة جرد جديدة تحمل مرجع السابقة

## `proc_issue_request`

**مسموحٌ 2 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `approved` | `issued` | أمين المخزن | المصروف لا يتجاوز المعتمد والفترة غير مقفلة | سند صرف | توقيع المستلم |
| `draft` | `approved` | أمين المخزن | بند واحد على الأقل وخفض المعتمد بسبب مكتوب | طلب صرف معتمد | بوابة فصل الواجبات |

**ممنوعٌ صراحةً:**

- ⛔ `draft` ⇐ `issued` — الصرف على طلب غير معتمد يتخطى بوابة الاعتماد التي هي غاية الطلب

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `approved` ⇐ `issued`: لا يفتح — المرتجع بمستند مرتجع · التصحيح: التصحيح بسند مرتجع لا بتعديل السند
- `draft` ⇐ `approved`: يفتح بإرجاع للتعديل قبل الصرف · التصحيح: التصحيح بتعديل الكميات ما دام لم يصرف

## `proc_offer`

**مسموحٌ 1 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `received` | `evaluated` | لجنة الترسية | فتح المظاريف وقع | ورقة تقييم العرض | توقيع اللجنة |

**ممنوعٌ صراحةً:**

- ⛔ `received` ⇐ `awarded` — الترسية على عرض لم يقيم تتخطى المقارنة التي هي غاية طلب العروض

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `received` ⇐ `evaluated`: يعاد التقييم بقرار مكتوب · التصحيح: التصحيح بورقة تقييم جديدة تحمل المرجع

## `proc_order`

**مسموحٌ 2 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `draft` | `issued` | مسؤول المشتريات | محضر ترسية معتمد أو سبب شراء مباشر دون الحد | أمر شراء | اعتماد صاحب الصلاحية |
| `issued` | `closed` | مسؤول المشتريات | مطابقة ثلاثية مقبولة لكل فاتورة | محضر إقفال أمر | اعتماد مدير المشتريات |

**ممنوعٌ صراحةً:**

- ⛔ `closed` ⇐ `issued` — الأمر المقفل رحلت مطابقته للمالية فإرجاعه يفك قيدا مرحلا

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `draft` ⇐ `issued`: يفتح بتعديل بمساره الحوكمي · التصحيح: التصحيح بسطر تعديل لا بكتابة فوق الأمر
- `issued` ⇐ `closed`: يفتح بقرار مكتوب لتعديل لاحق · التصحيح: التصحيح بإشعار مدين أو دائن لا بتعديل المقفل

## `proc_package`

**مسموحٌ 2 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `draft` | `cancelled` | مسؤول المشتريات | لا طلب عروض صادر عنها | محضر إلغاء حزمة | اعتماد مدير المشتريات |
| `draft` | `closed` | مسؤول المشتريات | عضو واحد على الأقل بسبب ضم مكتوب | خطة شراء الفترة | اعتماد مدير المشتريات |

**ممنوعٌ صراحةً:**

- ⛔ `closed` ⇐ `draft` — الحزمة المقفلة صدر عنها طلب عروض فإرجاعها للتحرير يفك سند الطلب

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `draft` ⇐ `cancelled`: لا تفتح — تنشأ حزمة جديدة · التصحيح: التصحيح بإلغاء وإنشاء لا بتعديل ملغاة
- `draft` ⇐ `closed`: تفتح بقرار مكتوب قبل إصدار طلب العروض · التصحيح: التصحيح بضم أو فك عضو ما دامت في التحرير

## `proc_rfq`

**مسموحٌ 3 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `draft` | `issued` | مسؤول المشتريات | حزمة مقفلة وموعد فتح معلن ودعوة واحدة على الأقل | طلب عروض مرسل | اعتماد مدير المشتريات |
| `issued` | `opened` | لجنة فتح المظاريف | بلوغ موعد الفتح المعلن | محضر فتح مظاريف | توقيع اللجنة |
| `opened` | `awarded` | لجنة الترسية | محضر ترسية بمعايير معلنة قبل الفتح | محضر ترسية | اعتماد صاحب الصلاحية |

**ممنوعٌ صراحةً:**

- ⛔ `issued` ⇐ `awarded` — الترسية قبل فتح المظاريف تجعل المعايير وصفا للفائز لا حكما عليه

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `draft` ⇐ `issued`: يفتح بتمديد الموعد بقرار مكتوب · التصحيح: التصحيح بتمديد الموعد لا بتعديل الشروط بعد الإرسال
- `issued` ⇐ `opened`: لا يعاد الفتح — المحضر واحد · التصحيح: التصحيح بإثبات ملاحظة في المحضر لا بإعادة الفتح
- `opened` ⇐ `awarded`: يفتح بإلغاء الترسية بقرار مكتوب · التصحيح: التصحيح بمحضر تعديل ترسية لا بكتابة فوق المحضر

## `proc_transfer`

**مسموحٌ 2 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `draft` | `in_transit` | أمين المخزن المرسل | بند واحد على الأقل ومخزنان مختلفان | أمر تحويل مرسل | اعتماد أمين المخزن |
| `in_transit` | `received` | أمين المخزن المستلم | مستلم غير المرسل وفرق الاستلام بسبب مكتوب | محضر استلام تحويل | توقيع المستلم |

**ممنوعٌ صراحةً:**

- ⛔ `draft` ⇐ `received` — الاستلام قبل الإرسال يخلق رصيدا في الوجهة بلا خصم من المصدر

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `draft` ⇐ `in_transit`: يفتح بإلغاء الإرسال قبل الاستلام · التصحيح: التصحيح بإلغاء وإنشاء أمر جديد
- `in_transit` ⇐ `received`: لا يفتح — الفرق بمحضر · التصحيح: التصحيح بمحضر فرق لا بتعديل الكميات

## `proc_wh_close`

**مسموحٌ 1 · ممنوعٌ صراحةً 1**

| من | إلى | مالكُ الانتقال | الشرطُ المسبق | المستندُ الرسميّ | بوّابةُ الاعتماد |
|---|---|---|---|---|---|
| `open` | `closed` | مدير المخازن | جرد الفترة معتمد والمعادلة تنطبق | كشف إقفال شهري | اعتماد المالية |

**ممنوعٌ صراحةً:**

- ⛔ `closed` ⇐ `open` — إعادة فتح فترة مقفلة تغير رقما وقع عليه في القوائم — الفتح بقرار مالية لا بفعل مخزن

**قاعدةُ إعادةِ الفتحِ والتصحيح:**

- `open` ⇐ `closed`: يفتح بقرار إعادة فتح فترة من المالية · التصحيح: التصحيح بقيد تسوية في الفترة التالية لا بتعديل المقفلة
