# `GOV_UI_FINISH` — إتمامُ ما تبقّى من `GOV_UI_EXEC` · **بلا توقّف**

> **اقرأ هذا الملفَّ كاملًا قبل أن تكتب سطرًا.**
> **الأمرُ الحاكم:** [`orders/GOV_UI_EXEC.txt`](../orders/GOV_UI_EXEC.txt) — 23 قسمًا · **ما زال نافذًا**.
> **والمراجعةُ التي حدّدت المتبقّي:** [`GOV_UI_RELABEL_AUDIT.md`](../GOV_UI_RELABEL_AUDIT.md)
> ⛔ **الحزمةُ الحاكمةُ منصوبةٌ وثابتة** — الدليل `60a8e3b2` · القيادة `51a703f0`.

---

# ⛔⛔⛔ ٠ · لا تتوقّفْ حتى تُنهي

1. **الدورةُ الملزِمة** (§22): `READ TARGET → COMPARE → FIND ROOT CAUSE → CORRECT → RENDER → VERIFY → CLOSE → CONTINUE`.
2. **عباراتٌ محظورةٌ حرفيًّا:**
   ```
   ⛔ «هل أكمل؟»                  ⛔ «أنتظر توجيهك»
   ⛔ «أنهيتُ الإدارةَ — أتابع؟»   ⛔ «أصدرتُ التقرير — أبدأ التالي؟»
   ⛔ «الوقتُ طال — أكمل في جلسةٍ أخرى؟»
   ```
   **والبديلُ الوحيد**: سطرُ تقدُّمٍ ثمَّ **البندُ التاليَ في الرسالةِ نفسِها**.
3. **الحاجزُ يمنع بندَه وحدَه** ولا يوقف البرنامج. **وثلاثةٌ فقط تُحجَب**:
   قرارُ أعمالٍ غائبٌ · مصدرٌ حاكمٌ مفقودٌ فعلًا · صلاحيةُ بيئة.
   ⛔ **وما عداها يُصحَّح في مسارِه.**
4. **وإن نفد السياق**: حدِّثِ السجلَّ الجامعَ والتزمْ **واستأنفْ منه** — ⛔ لا
   من ذاكرةِ المحادثةِ ولا من آخرِ رسالة.
5. **⛔ لا تُعِدْ ما ثبت** — عشرُ بوّاباتِ ارتدادٍ تُشغَّل لا تُبنى:
   `TARGET_BUILD` **413/413** · `MENU_TARGET` **327/327** ·
   `GROUP_CONFORMANCE`/`ORDER_CONFORMANCE`/`LABEL_CONFORMANCE` **307/307** لكلٍّ ·
   `ROLE_VISIBILITY_CONFORMANCE` **301/301** ·
   `STRUCTURAL_DEPARTMENT_PASS` **17/17** · `BUILT_NOT_RENDERED` ·
   `GLOBAL_FALLBACK` · `WITHOUT_LINEAGE` = **0**.
   ⛔ **وأيُّ هبوطٍ في واحدةٍ منها يوقف البند ويُعالَج فورًا** قبل المضيّ.
6. **`G-CLAIM-01` نافذ**: الكسرُ المقيسُ (`2570/5460`) لا نسبةٌ مخترَعة.
7. **هجرةٌ لكلِّ تغييرِ قاعدةٍ ولها عكسٌ `_down.php`** · ⛔ ولا تعديلَ يدويّ ·
   **والعطبُ في الأداةِ يُصلَح في الأداةِ لا في بياناتِها**.
8. **بوّابةُ `UXW` تفحص الشجرةَ كلَّها** — أبقِها نظيفةً والتزمْ دفعةً دفعة.
   ⛔ **ولا تتخطَّ خطّافًا بـ`--no-verify` أبدًا.**

---

# ١ · نقطةُ البدء — مقيسةٌ حيًّا على `581d8ea7`

| المفردة | القيمة |
|---|---|
| الجذر · PHP | `C:\wamp64\www\ems` · `/c/wamp64/bin/php/php8.3.28/php.exe` |
| الفرع | `repair01/w01-ownership` |
| الدليل · القيادة | `60a8e3b2` · `51a703f0` |

⚠ **وساعةُ الجهازِ ترجع** — ⛔ **لا تفرزِ الالتزاماتِ بالختمِ الزمنيّ**، افرزها
بالسلفِ (`git merge-base --is-ancestor`). *(بدا الرأسُ «راجعًا» وهو سليم.)*

## اللوحةُ الاثنا عشرَ الآن (§19)

| المقياس | القيمة | الحالة |
|---|---|---|
| `TARGET_BUILD_COVERAGE` | **413/413** | ✔ بوّابةُ ارتداد |
| `MENU_TARGET_COVERAGE` | **327/327** | ✔ |
| `BUILT_NOT_RENDERED` | **0/0** | ✔ |
| `GROUP_CONFORMANCE` · `ORDER_CONFORMANCE` · `LABEL_CONFORMANCE` | **307/307** لكلٍّ | ✔ |
| `ROLE_VISIBILITY_CONFORMANCE` | **301/301** | ✔ |
| `STRUCTURAL_DEPARTMENT_PASS` | **17/17** | ✔ |
| ⛔ **`FIELD_CONFORMANCE`** | **2570/5460** | **الجبهةُ الكبرى** |
| ⛔ **`GRAIN_CONFORMANCE`** | **375/413** | 38 سطحًا |
| ⛔ **`STATE_MODEL_CONFORMANCE`** | **149/210** | 61 سطحَ معاملة |
| ⛔ **`HUMAN_DEPARTMENT_PASS`** | **0/17** | بشريٌّ — يُعَدُّ ويُسلَّم |

## ومصفوفةُ §18 — 413 هدفًا بأحكامِها

| الحكم | العدد | النسبة |
|---|---|---|
| ✅ `EXACT_MATCH` | **97** | 23.5٪ |
| `N/A` (بحكمٍ مكتوب) | 71 | 17.2٪ |
| ⛔ **`FIELD_MISMATCH`** | **242** | **58.6٪** |
| ⛔ `GRAIN_MISMATCH` | **3** | 0.7٪ |

⭐ **صفرٌ من**: `WRONG_GROUP` · `WRONG_ORDER` · `WRONG_LABEL` ·
`WRONG_ROLE_VISIBILITY` · `WRONG_SURFACE_TYPE` · `NOT_BUILT` ⇒ **البنيةُ
والتسميةُ مُقفَلتان، والعملُ كلُّه داخلَ الشاشة.**

---

# ٢ · ⭐ البندُ الأوّل — الحقول (§11 · 242 سطحًا · 2,890 حقلًا)

**هذا 58.6٪ من العمل.** والترتيبُ **من أثقلِ مساحةٍ دَينًا**:

| الرتبة | المساحة | أسطحٌ غيرُ مطابقة |
|---|---|---|
| ① | `DEP-08` الحوكمة والالتزام | **32** |
| ② | `DEP-05` المالية | **25** |
| ③ | `DEP-03` التمويل · `DEP-07` الموارد البشرية | **24** لكلٍّ |
| ⑤ | `EX-CEO` الرئيس التنفيذي | **21** |
| ⑥ | `DEP-02` الموردون | **20** |
| ⑦ | `DEP-17` المخازن | **19** |
| ⑧ | `DEP-06` الخزينة | **18** |
| ⑨ | `IAF` المراجعة الداخلية | **17** |
| ⑩ | `DEP-16` المشتريات | **16** |
| ⑪–㉑ | `DEP-09` 13 · `DEP-10` 13 · `DEP-15` 13 · `DEP-01` 12 · `EX-DVP` 12 · `DEP-14` 11 · `DEP-04` 9 · `WS-MY` 7 · `DEP-11` 5 · `DEP-13` 3 · `DEP-12` 2 | |

**ولكلِّ سطحٍ — أحدَ عشرَ فحصًا بنصِّ §11:**
`Grain` · `Fields` · `Field Order` على دورةِ المستند · `Inputs` قابلةٌ
للتحرير · `Imported/Reference` قراءةً فقط · `Derived` محسوبةً ·
`1:N ⇒ Child Register` · `Current` مفصولٌ عن `History` · `Audit` ·
`Approval` بالمحرّكِ المركزيّ · `State Machine` بنموذجِه.

**والمصدرُ الحاكمُ للحقول**: `09/02_تتبع_الحقول` — **و`05` للتفاصيلِ الذرّيّةِ
في نطاقِه** (المبيعاتُ · الموردون · التعاقدُ والتوريد).

⚠ **وفخُّ المقام**: كلُّ إضافةٍ ترفع المقامَ فتهبط النسبةُ والعملُ صاعد.
**أعلِنِ الكسرَ لا النسبةَ** (§19).

---

# ٣ · البندُ الثاني — الحبّة (§11 · `GRAIN_CONFORMANCE` 375/413)

**38 سطحًا**، منها **6 تجمع حبّتَين** و**3 بحكمِ `GRAIN_MISMATCH` صريح**.

**والقاعدةُ**: الصفُّ الواحدُ يمثّل الكيانَ الصحيحَ وحدَه — **و`1:N` تتحوّل
إلى `Child Register`** لا خلايا متعدّدة. ⛔ **ولا تُنشئْ شاشةً جديدةً لحبّةٍ
ثانية** إن كان الحكمُ دمجًا موثَّقًا — اقرأْ حكمَ الدليلِ أوّلًا.

---

# ٤ · البندُ الثالث — آلاتُ الحالة (§14 · 149/210)

**61 سطحَ معاملةٍ** بلا نموذجِ حالةٍ مطابق. **ولكلٍّ**: النموذجُ الحاكم ·
الإصدارُ · الحالاتُ · الانتقالاتُ المسموحة · الحُرّاسُ · الاعتمادُ ·
الأحداثُ · التدقيق. ⛔ **ولا تربطِ النموذجَ بالاسمِ وحدَه.**

**والهدف**: `BUILT_TRANSACTION_WITHOUT_REQUIRED_STATE_MODEL = 0`.
**و`05 · الموجة ب`** هو المصدرُ الذرّيُّ لآلاتِ حالةِ التعاقدِ والتوريد.

---

# ٥ · البندُ الرابع — التحقّقُ البشريّ (§17 · 0/17)

⛔ **لا تنجزه أنت — لكنّك تُعِدُّه**. لكلِّ إدارةٍ بلغت أصفارَ §20:
**بطاقةُ تحقُّقٍ** فيها الدورُ ومستخدمُه · الشجرةُ المتوقَّعةُ بمجموعاتِها
وترتيبِها · نقاطُ الفحصِ الثمانيةُ (`Login` · `Workspace` · `Group sequence` ·
`Screen sequence` · `Labels` · `hidden/visible` · `direct URL` ·
`actual task navigation`) · وموضعُ التوقيع.

**ثمَّ سلِّمْها بسطرٍ واحدٍ وامضِ** — ⛔ **ولا تنتظرْها**.

---

# ٦ · البندُ الخامس — المحجوبُ بحاجزٍ مسمًّى

⛔ **لا تُنفِّذْه ولا تنتظرْه — سمِّ حاجزَه وامضِ.**

| البند | الحاجز |
|---|---|
| `DEP-08` الحوكمة · `EX-DVP` النوّاب — بلا دورٍ حيٍّ يحملهما | `BLOCKED_OWNER` |
| قراراتُ القيمِ العدديّةِ المفتوحة | `BLOCKED_OWNER` |
| بنودُ `UAT` | `BLOCKED_UAT` |
| دفعُ الفرعِ إلى البعيد | `BLOCKED_ENVIRONMENT` |

⚠ **و`DEP-08` أثقلُ مساحةٍ في دَينِ الحقول (32 سطحًا)** — **وحجبُها في
الرؤيةِ لا يمنع تصحيحَ حقولِها**. أنجزِ الحقولَ وسمِّ حجبَ الرؤيةِ وحدَه.

---

# ٧ · معياراتُ الإغلاق

**لكلِّ إدارةٍ (§20) — ثلاثةَ عشرَ صفرًا**: `WRONG_GROUP` · `WRONG_ORDER` ·
`WRONG_LABEL` · `BUILT_NOT_RENDERED` · `UNEXPLAINED_EXTRA_MENU_ITEM` ·
`ROLE_VISIBILITY_MISMATCH` · `CURRENT_RELEASE_TARGET_NOT_BUILT` ·
`TARGET_WITHOUT_LINEAGE` · **`GRAIN_MISMATCH`** · **`REQUIRED_FIELD_MISSING`** ·
**`UNRULED_EXTRA_FIELD`** · **`WRONG_FIELD_ORDER`** ·
**`REQUIRED_STATE_MODEL_MISSING`** ⇒ `DEPARTMENT_ARCHITECTURE_CONFORMANCE = 100%`.

**والعامّ**: `FIELD_CONFORMANCE` **5460/5460** · `GRAIN` **413/413** ·
`STATE_MODEL` **210/210** · `EXACT_MATCH` **342/342** من المنطبق
(‏413 − 71 `N/A`) · ومخرجاتُ §23 العشرةُ من **لقطةٍ واحدة**.

**وللقيادةِ (§21)**: `CEO` 100٪ · `DVP` 100٪ · نسبُ الإسقاطِ صحيحٌ ·
**ولا حقيقةَ معاملاتٍ مكرّرة**.

⛔ **ولا تُجمع هذه المقاماتُ في نسبةٍ واحدة** (§19).

---

# ٨ · دورةُ الإدارةِ — كرِّرْها إحدى وعشرين مرّةً بلا استئذان

```
① اقرأ ورقةَ المساحةِ في «01 · الدليل المعماري.xlsx» ودفترَ حقولِها في «09»
   (و«05» إن كانت في نطاقِه)
② لكلِّ سطحٍ غيرِ مطابقٍ: صحِّحِ الحبّةَ ثمَّ الحقولَ ثمَّ ترتيبَها
   ثمَّ الأصنافَ (مستوردٌ قراءةً · مشتقٌّ محسوبًا) ثمَّ الأبَ والابنَ
   ثمَّ التدقيقَ والاعتمادَ وآلةَ الحالة
③ صيِّرْ بعمليّةٍ نقيّةٍ وأثبتْ — ⛔ لا يكفي الجدولُ (§16)
④ أعِدْ قياسَ FIELD/GRAIN/STATE_MODEL للمساحة
⑤ تحقَّقْ من أصفارِ §20 الثلاثةَ عشرَ
⑥ تحقَّقْ أنَّ بوّاباتِ الارتدادِ العشرَ لم تتحرّك
⑦ حدِّثِ السجلَّ الجامعَ بدليلِ اللقطةِ · والتزمْ (والشجرةُ نظيفة)
⑧ جهِّزْ بطاقةَ التحقّقِ البشريِّ وسلِّمْها بسطرٍ
⑨ ⛔ وانتقلْ فورًا إلى المساحةِ التاليةِ — بلا سؤال
```

---

# ٩ · ترتيبُ التنفيذِ الكلّيّ

```
①  تحقَّقْ أنَّ الشجرةَ نظيفةٌ · واقصُصْ أساسًا واحدًا (BL جديد)
②  ⭐ البند ①: الحقولُ — 21 مساحةً بترتيبِ الدَّين (DEP-08 أوّلًا · 32 سطحًا)
    وكلُّ مساحةٍ تُغلَق وتُلتزَم قبل التالية
③  البند ②: الحبّة — 38 سطحًا (‏3 بحكمٍ صريحٍ و6 تجمع حبّتَين)
④  البند ③: آلاتُ الحالة — 61 سطحَ معاملةٍ · و«05» مصدرُها الذرّيّ
⑤  البند ④: 17 بطاقةَ تحقُّقٍ بشريٍّ تُسلَّم تباعًا فورَ بلوغِ كلِّ إدارة
⑥  أعِدْ توليدَ مخرجاتِ §23 العشرةِ من لقطةٍ واحدةٍ بختمِها
⑦  حدِّثِ السجلَّ الجامعَ · والتزمْ · وأصدِرْ لوحةَ §19 الاثنتي عشرةَ نهائيّةً
⑧  ⛔ ولا تتوقّفْ بين أيِّ خطوتَين — والمحجوبُ يُسمّى حاجزُه ويُتجاوَز
```

---

# ١٠ · سطرُ التقدّم

```
[<BL>] <المساحة/البند> · <المقياس> <من> ⇒ <إلى> · دليل: <الأداة> · التالي: <البند>
```

⛔ **ثمَّ ابدأِ التاليَ في الرسالةِ نفسِها.**

---

> **بنصِّ الأمر**: *«الهدفُ أن تصبح الشاشةُ صورةً مباشرةً للمستندِ أو الكيانِ
> الذي تديره»* · و*«لا تعتبرِ التقريرَ نقطةَ توقّف»* · و*«لا ترفعْ سؤالَ
> مالكٍ إلّا إذا كانت المصادرُ متعارضةً حقيقةً أو القرارُ غيرَ موجود»*.
