# `RPR-02` §٥·٩ — مصدرُ الحقيقةِ بقاعدةِ الكاتبِ الوحيد

> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02_sot_assign.php --md` · اللقطة `SNAP-aa23387a-20260829-232409`

## الحاجزُ ارتفع بقياسٍ سابقٍ لا بقرارٍ جديد

`grain_entity` قيست في §٧ الخطوة ١ على الأسطحِ الحيّة، **فصار معروفًا أيُّ كيانٍ
يكتبه أيُّ سطح** — وهذا بعينِه ما كان ينقص `source_of_truth`.

## القاعدةُ الواحدة

| القاعدة | الأسطح | الحكم |
|---|---:|---|
| `SOLE_WRITER` | **113** | كيانٌ يكتبه **سطحٌ حيٌّ واحدٌ لا غير** ⇒ هو مصدرُ حقيقتِه **بتعريفِ §٥·٩ لا بترجيح** |
| `DUPLICATE_SOURCE` | **77** | على **17** كيانًا — ⛔ **لا يُعيَّن ويبقى فارغًا** |
| قائمٌ سلفًا | 18 | قيمةٌ من موجةٍ سابقةٍ — لا تُدهَس |

⇒ **#٩ من 190 إلى 77**.

## والباقي ليس عطبًا جديدًا

الـ**77** الباقيةُ تكتب **17** كيانًا لكلٍّ كاتبان فأكثر —
**وهذا بعينِه مقامُ المقياسِ #١٠** («حقيقةٌ لها مصدران») معدودًا بالأسطحِ لا بالكيانات.
⇒ **فبقيّةُ #٩ و#١٠ عطبٌ واحدٌ يُعدُّ مرّتَين**، وإغلاقُ #١٠ يُغلقهما معًا.
⛔ **ولا يُعيَّن أحدُ الكاتبَين مصدرًا لتنخفض #٩** — فذلك يُخفي #١٠ ولا يحلُّها.

## الكياناتُ متعدّدةُ الكتّاب

| الكيان | كتّابُه |
|---|---:|
| `op_containers` | 21 |
| `ems_post_idempotency` | 11 |
| `tickets` | 8 |
| `fin_event_effects` | 5 |
| `unit_state_changes` | 5 |
| `exec_approvals` | 4 |
| `iaf_findings` | 3 |
| `activity_logs` | 2 |
| `asset_hour_reconciliations` | 2 |
| `equipments` | 2 |
| `fin_notifications` | 2 |
| `modules` | 2 |
| `permit_requests` | 2 |
| `roles` | 2 |
| `sensitive_read_log` | 2 |
| `unit_entries` | 2 |
| `work_items` | 2 |
