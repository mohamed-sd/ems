# RPR-02-A · فحصُ المادّة 111 على كلِّ بانِ إكسل

> ⛔ **مولَّدٌ من تشغيلٍ حيّ**: `php tools/rpr02a_m111_audit.php --md`
> **مولَّدٌ**: 2026-08-28 16:31:32
> **الفحص**: يُكتب صفٌّ فيه **صفرٌ مقيس** و**قيمةٌ غيرُ مقيسة (`null`)**، ثمَّ يُعاد قراءةُ الملفِّ من القرص.
> **القبول**: الصفرُ يُقرأ `0` · وغيرُ المقيسِ يُقرأ خانةً خالية. ⛔ والفرقُ بينهما هو المطلوب، لا أحدُهما.

| بانِ الإكسل | من يستعمله | «صفرٌ مقيس» يُقرأ | «غيرُ مقيس» يُقرأ | شاهدٌ غيرُ صفريّ | الحكم |
|---|---|---|---|---|---|
| `tools/lib/xlsx_out.php` › `xlsx_create()` | يستعمله `repair01_w16_baseline_pack.php` | `0` | `(خالية)` | `7` | ✔ **مطابق** |
| `PhpSpreadsheet::fromArray(..., strictNullComparison: true)` | نمطُ `tools/baseline_xlsx_build.php` (‏10 نداءات) | `0` | `(خالية)` | `7` | ✔ **مطابق** |
| ⚠ **الاختبارُ السالب** — النمطُ نفسُه **بلا** `strictNullComparison` | وهو ما كان عليه بانِ الأساسِ قبلَ الإصلاح | `(خالية)` | `(خالية)` | `7` | ✔ **سقط كما يجب** — فالفحصُ يرصد فعلًا |

**الحصيلة: 2/2 بانيًا مطابقًا للمادّة 111** — والاختبارُ السالبُ يُثبت أنّ الفحصَ يرصد الخرقَ ولا يمرّ عليه.
