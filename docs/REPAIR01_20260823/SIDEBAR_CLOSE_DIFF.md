# فرقُ الإغلاقِ — SIDEBAR_CLOSE §٤ · مقيسًا من الشجرةِ حرفًا

> الأساسُ المقارَنُ: ختمُ `f761e273` (ما بعدَ تصحيحِ الاتجاه) · والحيُّ بعد الإغلاق.
> ⛔ حركةُ هذه الجولةِ **فرعيّاتٌ داخلَ الأبوابِ** ودوران دخلا مدى القياسِ لأوّلِ
> مرّةٍ — فالختمُ القديمُ لا يحمل فرعيّاتٍ ولا الدورَين، والفرقُ أدناه مقيسٌ
> بمجسّاتِ تصييرٍ حيّةٍ قبلَ كلِّ كتابةٍ وبعدَها (محاضرُها في سجلِّ الجولة).
> والختمُ الجديدُ صار يحمل الفرعيّاتِ والأدوارَ الأربعةَ والثلاثين كلَّها
> (١٬٩٥٢ موضعًا) فلا يعمى فرقٌ قادم.

## البند ① — الدوران غيرُ المُعلَنَين (كانا خارجَ الملفِّ قطًّا)

```
الدور 11 «مشغل اسطول» · الأبواب: 8 ⇒ 8 · البنود: 65 ⇒ 65
  قبل : صفرُ إعلانٍ — لا عنوانَ ملفٍّ فرعيًّا واحدًا (بقي على التصنيف العام)
  بعد : 37 بندًا تحت عناوينِ الملفِّ التصميميِّ الفرعيّةِ داخل أبوابِه
  بنودٌ انتقلت (فرعيًّا): 37

الدور 14 «مشرف صيانة» · الأبواب: 7 ⇒ 7 · البنود: 61 ⇒ 61
  قبل : صفرُ إعلانٍ — لا عنوانَ ملفٍّ فرعيًّا واحدًا
  بعد : 34 بندًا تحت عناوينِ الملفِّ الفرعيّةِ داخل أبوابِه
  بنودٌ انتقلت (فرعيًّا): 34
```

القبول: **34/34 دورًا محاذًى** ✔ · وانتقالُ كلٍّ من الدورَين > صفر ✔.

## البند ② — `NO_BRIDGE` ⇐ صفرٌ بلا حكم

كان **1,226** مبهمًا (أكبرَ من المقامِ نفسِه) — صار كلٌّ بحكمِه المقيس:

| الحكم | العدد | الشاهد |
|---|---:|---|
| جُسر بالاسمِ المعياريِّ R2 (دخل المقامَ) | 8 | اسمٌ معتمدٌ يطابق متطلبًا وحيدًا |
| تبويبُ ابنٍ | 132 | سجلُّ تبويباتِ الكياناتِ `entity_tabs` |
| مسارٌ متقاعدٌ بوارثٍ | 285 | `nav_redirects` النشط |
| سطحٌ منصّيٌّ مشترك | 7 | `ownership_verdict='PLATFORM_SHARED'` |
| فائضٌ حميدٌ (سطحٌ مسجَّلٌ لا متطلبَ له بعد الثلاث) | 730 | حكمٌ صادقٌ يُعلَن — لا جسرَ يُلفَّق |
| **⛔ بلا حكم** | **0** | — |

## البند ③ — المخالفون ⇐ صفرٌ أو بحاجزٍ مسمًّى

`#٨` الاتجاهيُّ (المصدرُ الملفُّ والمُصحَّحُ الشجرةُ): **934 من 934 = 100٪**.

وغيرُ المُصيَّرِ المنطبقُ كلُّه **بحاجزٍ مسجَّلٍ باسمِه** (خارجَ المقامِ لا مُسقَطًا صامتًا):
عزلُ مساحةٍ مصرَّحٌ بسجلِّه 99 · بوّابةُ المشرفِ الخارجيِّ الضيّقةُ عمدًا (UX-05 §4) 52 ·
مُنِع بقالبٍ نافذٍ (GOV-AUTH-01) 10 · الظهورُ بالصلاحيةِ (س٦) 2 · بوّابةُ التمويلِ fail-closed 1.

حركاتُ الفرعيّاتِ السبعُ المقيسةُ (مجسُّ قبل⇒بعد لكلٍّ):
`workforce/contract_registry.php` (دوران 9·15) ⇒ «صناديق القرار» ·
`tickets/my_tickets.php` (أدوار 32·34·35) ⇒ «عملي اليومي» ·
`financing/financing_board.php` (دور 26) ⇒ «التعاقد» ·
`admin/sec_governance.php` (دور 15) ⇒ «الادوار والصلاحيات» — وفخٌّ قيس بثلاثِ
حركاتٍ: **صيغةُ الهمزةِ للاسمِ تلتهمها خريطةُ تسميةٍ قديمةٌ فتُصيَّر
«الإعدادات»، والألفُ العاريةُ تُصيَّر نفسَها وتطابق الملفَّ بالتطبيعِ المعتمد**.

## §٣ — الأربعةُ المحجوبةُ بقرارِ المالكِ: لم تُمَسَّ ولم يُعَدْ رفعُها
(س٢ تسميتا `daily_units_report`/`view_timesheet` · س٧ صفُّ `view_timesheet` · س٦ منحُ دورَي 28·29 على `my_tasks`).
