# تقريرُ إنجازِ الجلسة — INJAZ-UXUI-01

**التاريخ:** 2026-08-19 · **الفرع:** `fix/remediation-2026-08` · **المدى:** `973acd9` ← `91c510b`

> **مفرداتُ الإغلاقِ المعتمَدةُ بقرارِ المالك**: `IN_PROGRESS` · `TECHNICALLY_ELIGIBLE` ·
> `BLOCKED_EXTERNAL_INPUT` · `VISUAL_PATTERN_APPROVED` · `GOLDEN_SCREEN_FINAL`.
> **ولا تُستعمل «CLOSED» ولا «100٪»** في هذا التقريرِ ولا في أيِّ سجلٍّ للجولة.

---

## ① الحصيلةُ في سطور

| المقياس | في بدايةِ الجلسة | في نهايتِها | المصدر |
|---|---|---|---|
| **البنودُ العشرةُ الملزمة** (ف١٥-١) | 8/10 — **وثلاثةٌ منها كاذبة** | **10/10 مقيسةً** | `tools/uxui_ten_binding_gate.php` |
| **الشاشاتُ الذهبيةُ العشر** | 7/10 كلٌّ منها | **8/10 كلٌّ منها** ← الحدُّ الآليُّ الأقصى | `tools/uxui_promotion_gate.php` |
| **ترحيلُ الشاشات** | 663 «لم يبدأ» | **516 / 663 = 77.8٪** `TECHNICALLY_ELIGIBLE` | `tools/uxui_migration_measure.php` |
| **AC-U1 قشرة · AC-U2 مكوّن · AC-U3 حالات** | 24 · 258 · 0 إخفاقًا | **0 · 0 · 0** | المصدرُ نفسُه |
| **اتساقُ الإدارات** | 139 تبويبًا · 43 اسمًا | **صفرُ اختلافٍ معتمَد** | `tools/uxui_dept_consistency.php` |
| **الوصولُ الرقميّ (12 فحصًا)** | غيرُ مقيس | **10/10 شاشاتٍ بصفرِ مخالفة** | `gov_a11y_measurements` |
| **الجردُ العكسيُّ على الوثائقِ الثلاث** | لا وجودَ له | **18 / 21 = 85.7٪** | `tools/uxui_reverse_audit.php` |
| **خطُّ الأساسِ البصريّ** | 54 فرقًا معلَّقًا | **صفرُ فروق** | `.ssdiff` |
| **صفرُ الفقد** | — | **893 ⇐ 892** · والجديدُ مُعلَنٌ صراحةً | مصفوفةُ الحفظ |

**الالتزامات:** ثمانيةٌ · **الملفاتُ:** 280 ملفًّا · **الأسطر:** ‏+51,901 / −4,994
(ويشمل هذا العددُ عملَ جلسةٍ موازيةٍ فُصل التزامُها عن التزاماتي — انظر §⑦).

---

## ② أخطرُ ما كُشف: **أدواتي أنا كانت تكذب**

طلبتَ «هندسةً عكسيةً للتأكد» — فبُنيت أداةٌ **لا تقرأ تقاريري ولا تصدّقها**:
تقرأ **المطلوبَ من الوثائقِ الثلاثِ** ثم تقيسه على النظامِ الحيِّ وتُعلن الفارق.
وأولُ ما وجدته كان في بواباتي.

| العطب | الأثرُ المقيس | الإصلاح |
|---|---|---|
| فاحصُ البنودِ العشرةِ يقرأ `link`/`title`/`group_code` والجدولُ يحمل `route`/`label_ar`/`group_id` | أعلن **10/10** وثلاثةُ بنودٍ تقيس **العدم** | تصحيحُ الأعمدة + **حارسُ مقامٍ صفريّ** |
| ثم قاس **الجدولَ الخام** لا المُصيَّر | أعلن **33 اسمًا مزدوجًا و179 مجموعةً** — كلُّها زائفة | تفويضٌ لفاحصِ التصييرِ القائم |
| قيدُ منعِ التلوثِ أُنشئ بـ«10 قيود · صفرِ خطأ» | **عاطلٌ تمامًا**: `؀` لا تفهمها MariaDB | `LENGTH > CHAR_LENGTH` + اختبارٌ سلبيٌّ دائم |
| فاحصُ القيدِ قرأ رفضَ **قيدٍ لا صلةَ له** | «نجاحٌ» مرةً و«عطبٌ» مرة | يُطابق **اسمَ القيدِ الرافض** ويتخطّى المحجوب |
| فحصُ الوصولِ يقرأ التركيزَ **ساكنًا** | **48 «مخالفةً» من 72** والمصدرُ فيه 23 قاعدةً عاملة | ضغطةُ **Tab حقيقيةٌ** ثم تركيزٌ لكلِّ عنصر ⇐ **صفر** |

**وصار الحكمُ ثلاثيًّا**: `PASS` · `FAIL` · **`NOT_MEASURED`** —
**ومقامٌ صفريٌّ لا يجتاز بحال**. لأن **النجاحَ الكاذبَ أخطرُ من الإخفاق: يُسكِت البوابة.**

---

## ③ فجواتٌ حقيقيةٌ أُغلقت

### ١. مساراتٌ ظاهرةٌ بلا تسجيل — 13
مسارٌ يُصيَّر في السايدبارِ ولا صفَّ له في السجل. سُجِّلت **بحالتِها الحاليةِ
`PENDING_OWNER`** — **ولم يُخترع لها اسمٌ معياريّ**، وكلُّها مُثبَتةُ الهويةِ في
`modules`. السجلُّ **359 ⇐ 372**.

### ٢. بندٌ لم يكن يُخالَف — كان **غيرَ قابلٍ للقياس**
البندُ ⑧ (ترتيبُ العملياتِ بالدورةِ المستندية) بلا عمودٍ أصلًا. أُضيف
`output_doc` و`state_transition` و`ops_source`، ومُلئت **12/12** من مصدرَين
حاكمَين: دفترُ التدقيق (3) وحالاتُ الشيفرةِ الصريحة (9).

### ٣. ثلاثةُ أدوارٍ بلا مركزِ عمل
**مشرفُ المخاطر · رئيسُ الحسابات · المراجعُ الداخليّ** — يدخل صاحبُها فلا يجد
«مهامي». زُرعت بأسماءِ السجلِّ المعياريّ، و**قيمةُ `door` قُرئت من النظامِ لا
اختُرعت**. **31/34 ⇐ 34/34** ومُثبَتٌ بالتصييرِ الحيِّ لا بالجدول.

### ٤. عُدّةُ ENG-01 — ثمانُ شاشاتٍ بلا قشرة
تُصيَّر **بلا `.main_head` ولا غلافٍ موحَّدٍ ولا عنوانِ صفحة** — وباسمِها
**التقنيِّ** ظاهرًا (يخالف بوابةَ اللغة U8 صراحةً). **إصلاحٌ واحدٌ في العُدّةِ
عالج الثمان** — والنمطُ منسوخٌ حرفًا من `u13_screen_kit`، فلا يُخترع نمطٌ تاسعٌ
لثامنِ شاشة.

### ٥. ثلاثةُ عيوبِ وصولٍ حقيقية
- `Contracts`: رابطُ تحديثٍ بأيقونةٍ عاريةٍ بلا اسمٍ لقارئِ الشاشة.
- `Finance/approvals_inbox`: قفزةُ عناوينَ `h1→h5` ⇐ `h2` بصنفِ `h5` — **الدلالةُ
  سليمةٌ والمظهرُ لم يتغيّر** (العقدُ 1374 = 1374).
- `includes/related_tickets_tab.php` مُضمَّنٌ مشترك — **إصلاحُه واحدًا عالج ثلاثَ شاشات**.

### ٦. شبكةُ إنجاز — أعمدةٌ واعيةٌ بالصلاحية
**العطبُ المسدود**: عمودٌ يُخفى بصنفِ CSS **يبقى في ملفِّ إكسل** — فمن لا يراه
على الشاشةِ يجده في التصدير. **تسريبٌ صامت.** والمسلكانِ (العرضُ والتصدير) صارا
يقرآن **مسندًا واحدًا** `data-perm`. وزرُّ «إظهار كل الأعمدة» كان **يُبطل المنعَ
بنقرة** فاستُثني الممنوعُ صراحةً. **7/7** فحصًا · وصفرُ ارتدادٍ مقيسٌ حيًّا.

---

## ④ الترحيلُ — أربعُ موجاتٍ بترتيبِ الشدة

نصُّ ف١٣-١ خطوة ٥: «الترحيلُ بموجاتٍ إدارية **بترتيبِ الشدةِ من دفترِ التدقيق**».

| الموجة | الشدة | الشاشات | الأسطر | الفرقُ البصريّ |
|---|---|---|---|---|
| الأولى | عالٍ | 16 | 131 | **15 بـ`−1` عقدة** · وواحدٌ ليس مني |
| الثانية | متوسط | 41 | 722 | **41 من 41 بـ`−1`** · صفرٌ شاذّ |
| الثالثة | بقيةُ النطاق | 137 | 2,885 | **135 بـ`−1`** · و3 فُسِّرت |
| الرابعة | ختامية | 2 | — | **2 بـ`−1`** |

**نقلٌ حرفيٌّ لا إعادةُ كتابة**، والنسخُ الأصليةُ في
`storage/backups/uxui_local_styles/` والعكسُ بأمرٍ واحد.

**◆ والدليلُ أنه نقلٌ لا تغيير**: كلُّ فرقٍ **`−1` عقدة** — وسمُ `<style>` الذي
غادر الشاشة — **وعيّنةُ الفرقِ خاويةٌ في كلِّها**. وأُعيد الالتقاطُ **بعد
التوثيقِ لا قبلَه** في كلِّ موجة.

**◆ وثلاثةُ شواذَّ فُسِّرت لا استُثنيت**: `Contracts/penalties` ·
`Equipments/equipments` · `Maintenance/inspections` بـ`−3`. وكتلةُ النمطِ فيها
**واحدةٌ لا ثلاث** — والعقدتانِ الزائدتانِ **شبحان**: تعليقٌ في CSS نصُّه
`var(--c-<hex>, <hex>)` كان الفاحصُ يقرأ `<hex>` منه **عنصرَ HTML** لأنه داخلَ
`<style>`. **نقصٌ تصحيحيٌّ لا فقد.**

---

## ⑤ عطبٌ شاملٌ كشفه القياسُ الحيُّ وحدَه

`Settings/change_password.php` صفحةٌ ضيّقةٌ **عمدًا** وفيها `.main { max-width:
600px }` محليًّا. ولمّا نُقلت إلى المركزِ صارت تحكم **كلَّ شاشةٍ في النظام**:
`.main` عرضُه **600 بدل 1837**.

> **عطبٌ شاملٌ من سطرٍ واحد** — ولم يكشفه فحصُ البناءِ ولا عدُّ العقدِ ولا خطُّ
> الأساسِ البصريّ (البنيةُ لم تتغيّر أصلًا). **كشفه قياسُ عرضِ الحاويةِ حيًّا.**

- **13 قاعدةً في ثلاثِ شاشاتٍ** محدِّدُها عامّ ⇐ حُصر نطاقُها بصنفِ شاشة.
- **وعطبٌ ثانٍ داخلَ الإصلاحِ نفسِه**: أولُ صياغةٍ كتبت `.scr-x .main` (نسلًا)
  والصنفانِ على **العنصرِ نفسِه** — فما طابقت قاعدةٌ ومرَّت **صامتة**. صُحِّح
  إلى `.main.scr-x` — 18 محدِّدًا. **والصمتُ أخطرُ من الخطأ الصريح.**
- **وحارسٌ يمنع التكرار**: `tools/uxui_css_scope_gate.php` — **مُثبَتٌ سلبيًّا**:
  حُقنت `.main{max-width:123px}` فرفضها بخروجٍ 1، وبعد الاستعادةِ مرَّ بخروجٍ 0.

**وبصمةُ المكوّنِ تغيّرت مرتَين** فأُبطلت قياساتُ المتصفحِ **بحقّ** (البوابةُ
أعلنتها `NOT_MEASURED` لا `PASS`). سُجِّل **`ux-1.4.0`** وأُعيد قياسُ العشرِ
الذهبيةِ كلِّها على ثلاثِ دقات = **30 قياسًا حيًّا** — **والنتيجةُ مطابقةٌ
للأصلِ حرفًا**.

---

## ⑥ التلوثُ — خطواتُ المالكِ الخمسُ اكتملت

| الخطوة | الحال |
|---|---|
| ① إثباتُ سوءِ الموضع | **560/560 مصنَّفةً** ثلاثيًّا (`LEGIT` 91 · `TEST_POLLUTION` 87 · `SUSPECT` 382) |
| ② مصدرٌ حاكمٌ للقيمة | **لا يوجد** — والأعمدةُ `NOT NULL` فلا NULL |
| ③ العزلُ بدلَ التخمين | **991 صفًّا** مسجَّلًا بمفاتيحِه — **صفرُ تعديلٍ في بياناتِ العمل** |
| ④ حفظُ النصِّ ومرجعِه | محفوظٌ حرفًا · و`source_ref` مقروءٌ من النصِّ لا مُخترَع |
| ⑤ **قيدٌ يمنع عودتَه** | **14/14** في المجالاتِ المحمية · **مُثبَتٌ سلبيًّا 6/6** |

**وثلاثُ محاولاتٍ فشلت قبلَ الحلِّ وتُسجَّل لأنها تشرحه:**
① `CHECK` عادٍ رُفض (الصفوفُ الملوَّثةُ قائمة) · ② قادحُ `BEFORE INSERT` رُفض
(«لا SUPER») · ③ `CHECK` بمرساةِ `id` رُفض (**`AUTO_INCREMENT` ممنوعٌ في CHECK**).
**والحلُّ مرساةٌ زمنية**: القائمُ يُعفى بطابعِه والوارِدُ يخضع.

**و50,441 إصابةً في حقولِ الملاحظاتِ لم تُمَسّ** — بقرارك.

---

## ⑦ ما لم أفعلْه — ويُعلَن

**عملُ جلسةٍ موازية (XF-01 · طبقةُ البياناتِ الإضافية)** كان يجري على الشجرةِ
نفسِها. **ولم أضمَّه إلى التزاماتي** ولم أعتمد فروقَه البصرية:
> «من غيّرها يعتمدها ويصف سببَها. واعتمادي إياها يُثبت في السجلِّ أنني راجعتُ
> ما لم أرَه — وذاك أسوأُ من تركِ البوابةِ حاجبة.»

**وسجَّلتُ خطأً في النسبةِ صحّحتُه**: نسبتُ 54 فرقًا بصريًّا كلَّها للجلسةِ
الموازيةِ **بناءً على عيّنةٍ واحدة**. والقياسُ الكاملُ قال: **30 مني** (زرعُ
مركزِ العمل) · **23 بيانات/ترتيب** · **1 مختلط** · **صفرٌ لهم وحدَهم**.
**وعيّنةٌ واحدةٌ ليست قياسًا.**

---

## ⑧ ما بُني في هذه الجلسة

### جداولُ الحوكمةِ الجديدة
| الجدول | الصفوف | الغرض |
|---|---|---|
| `gov_migration_ledger` | 663 | دفترُ التدقيقِ صار **سجلَّ ترحيلٍ حيًّا** بحالةٍ **تُقاس لا تُكتب** |
| `gov_test_data_isolation` | 991 | عزلٌ بتسجيلٍ · نصٌّ محفوظٌ · قيدُ «لا حسمَ بلا مصدر» |
| `gov_dup_semantics` | 59 | الترجيحُ الآليُّ والحكمُ البشريُّ **عمودانِ لا عمود** |
| `gov_a11y_measurements` | 10 | الاثنا عشرَ فحصًا + **شرطُ القياس** (`keyboard_modality`) |

### فاحصاتٌ وبوابات
`uxui_ten_binding_gate` · `uxui_reverse_audit` · `uxui_dept_consistency` ·
`uxui_migration_measure` · `uxui_css_scope_gate` · `uxui_record_a11y` ·
`tests/pollution_guard_negative_test` · `tests/datagrid_column_perm_test`

### هجراتٌ (9)
`2027_07_14` … `2027_07_21` — سجلُّ الوصول · المساراتُ غيرُ المسجَّلة · المستندُ
الناتج · ازدواجُ المعنى · مركزُ العمل · حارسا التلوث · سجلُّ الترحيل.

---

## ⑨ الباقي — ثلاثةٌ، وكلُّها خارجَ يدي

| # | البند | لماذا |
|---|---|---|
| ١ | **⑧ الاختبارُ البشريّ · ⑨ جولةُ العرض** | بقرارك: «لا تكن أنت البانيَ ومشغّلَ البوابةِ والمصادقَ معًا» · `BLOCKED_EXTERNAL_INPUT` |
| ٢ | **حكمُ الـ59 زوجَ ازدواج** | قيدٌ في القاعدةِ يرفض قرارًا بلا **حكمٍ بشريٍّ وفاعلٍ مسمًّى** |
| ٣ | **مصيرُ الـ991 صفًّا** | لا مصدرَ حاكمًا يقول القيمةَ الصحيحة — **ولا أخمّن** بقاعدتك |

**و147 موضعًا في الدفترِ بلا مسارٍ محلول** (135 اسمًا مفهوميًّا بلا نظيرٍ حيّ ·
12 متعدِّدًا) — **لا شاشاتٍ ناقصةً**، ولا تُحسب مُرحَّلةً ولا راسبة.

**والدفعُ إلى الأصل** بيدك: مفتاحُك بعبارةِ مرور، ولا أطلبها ولا تُرسَل.

```bash
git push origin fix/remediation-2026-08
```

---

## ⑩ الدروسُ الحاكمةُ لما بعدَ الجولة

1. **النجاحُ الكاذبُ أخطرُ من الإخفاق** — لأنه يُسكِت البوابة. فمقامٌ صفريٌّ
   `NOT_MEASURED` لا `PASS`.
2. **مقياسٌ يُرسِّب المرجعَ هو المخطئ لا المرجع** — أخفقت تسعٌ من الذهبيةِ العشرِ
   في صياغةٍ للـ AC-U3، وهي مجتازةٌ لكلِّ البوابات.
3. **قِسْ على السطحِ الذي يراه المستخدم** — `nav_items` الخامُّ ليس ما يُصيَّر،
   والملفُّ وحدَه ليس الشاشةَ حين تكون بنيتُها في عُدّة.
4. **قيدٌ لا يُختبَر سلبيًّا دعوى** — 10 قيودٍ أُنشئت بصفرِ خطأٍ وكانت عاطلة.
5. **اقرأ اسمَ القيدِ الرافضِ لا مجرَّدَ الرفض** — وإلا كذب الفاحصُ في الاتجاهَين.
6. **الصمتُ أخطرُ من الخطأ الصريح** — محدِّدٌ لا يُطابق شيئًا يمرُّ بلا شكوى.
7. **عيّنةٌ واحدةٌ ليست قياسًا** — نسبتُ 54 فرقًا خطأً بناءً على واحد.
8. **إصلاحٌ في العُدّةِ يعالج ثمانيًا** — وترقيعُ شاشةٍ يعيد العيبَ بعد أولِ توليد.

---

**الحالُ المُعلَن:** `TECHNICALLY_ELIGIBLE`
**ولا يُدَّعى اكتمالٌ فوقَ ما قِيس.**

---

# ملحقٌ ① — الجدولُ الموحَّد: **المقامُ قبلَ التعديل**

> بقاعدةِ المالك: «لا تلمس مكوّنًا حتى تكتب: المقامُ الصحيحُ كم؟»

**المقيسُ على `Contracts/contracts.php` مُصيَّرةً حيًّا (لا من المصدر):**

| القدرةُ المنصوصةُ (ف٩-١) | الحال | الشاهد |
|---|---|---|
| بحثٌ عام | **قائم** | حقلُ بحثٍ ظاهر |
| منتقي أعمدةٍ + إظهارُ الكل | **قائم** | `.ems-colvis` + الزرّ |
| مجموعاتُ أعمدة | **قائم** | 18 ترويسةً معلَّمة |
| مناظرُ محفوظة | **قائم** | `.ems-views` |
| تصدير | **قائم** | `.ems-xlsx-btn` |
| تصفيح | **قائم** | `.dataTables_paginate` |
| تمريرٌ أفقيٌّ داخلَ الجدول | **قائم** | `.ems-xscroll` |
| فرزٌ متعددُ الأعمدة | **قائم** | `orderMulti` غيرُ مُعطَّل |
| الوعيُ بالصلاحية | **قائم** | `data-perm` — أُنجز في هذه الجولة |
| **مرشِّحاتٌ مركَّبة** | **ناقص** | لا `.ems-filterbar` مُصيَّرة |
| **رقاقاتٌ نشطةٌ + «مسحُ الكل»** | **ناقص جزئيًّا** | رقاقتانِ بلا زرِّ مسح |
| **ترويسةٌ لاصقة** | **ناقص** | لا `scrollHead` ولا `fixedHeader` |
| **كثافةُ العرض** | **ناقص** | لا مُبدِّل |
| **«المزيد ⋯»** | **ناقص** | لا قائمة |
| **تحديدٌ متعددٌ وإجراءاتٌ جماعية** | **ناقص** | صفرُ مربّعِ اختيارٍ في الجدول |
| **تثبيتُ عمودِ المفتاح** | **ناقص** | لا `fixedColumns` |

**◆ والمقامُ: 16 قدرةً · 9 قائمةٌ (56.3٪) · 7 ناقصة.**

**◆ وعيبٌ بنيويٌّ مقيسٌ زيادةً**: **3 أشرطةِ أدواتٍ مُصيَّرة** والمنصوصُ
**شريطٌ واحدٌ بستِّ أدوات** (ف٩-٢) · و**صفرُ إجراءٍ رئيسيٍّ في الترويسة**
والمنصوصُ أن «إنشاءَ عقد» زرٌّ رئيسيٌّ في الترويسةِ لا أداةٌ في الشريط.

## ملحقٌ ①-ب — الجدولُ الموحَّد: **المقامُ بعدَ البناء**

| القدرة | قبل | بعد |
|---|---|---|
| بحثٌ عام · منتقي أعمدةٍ · مجموعاتٌ · مناظرُ · تصديرٌ · تصفيحٌ · تمريرٌ داخليٌّ · فرزٌ متعددٌ · وعيٌ بالصلاحية | **قائمة (9)** | قائمة |
| **ترويسةٌ لاصقة** | ناقص | **`position: sticky` مقيسًا** |
| **كثافةُ العرضِ الثلاثية** | ناقص | **مقيسةٌ وظيفيًّا: 9px ⇐ 2px ⇐ 12px ⇐ 9px** |
| **«المزيد ⋯»** | ناقص | **قائمةٌ فيها الكثافةُ والطباعةُ** + ينقل الاستيرادَ إن وُجد |
| مرشِّحاتٌ مركَّبة | ناقص | **المكوّنُ قائمٌ** (`.ems-filterbar`) — والعقودُ **بلا نموذجِ ترشيحٍ أصلًا** |
| تحديدٌ متعددٌ · تثبيتُ المفتاح | ناقص | **ما زالا** |

**◆ المقام: 16 قدرةً · 9 ⇐ 13 قائمة (81.3٪).**

**◆ وعيبٌ بنيويٌّ أُغلق**: **3 أشرطةِ أدواتٍ ⇐ شريطٌ واحدٌ في مستوًى واحد** —
ومُثبَتٌ على شاشةٍ بأربعةِ جداول: **4 أشرطةٍ · أقصى مستوًى فوقَ جدولٍ واحدٍ = 1**.

**◆ والسببُ الجذريُّ كان تنازعًا لا موضعًا**: منتقي الأعمدةِ وزرُّ التصديرِ
كان كلٌّ يُنشئ مضيفَه بمنطقِه، **والتصديرُ غيرُ متزامنٍ** (يُحمِّل JSZip
وButtons). فأيُّهما وصل أولًا أخذ المرساة. وأولُ إصلاحٍ **قلَبَ الترتيبَ
ولم يوحّده** — وهو دليلُ التنازعِ لا دليلُ الحل. فصار **مضيفٌ واحدٌ
(`emsToolbarHost`) يشترك فيه المسلكان**.

**◆ وأربعُ فرضياتٍ خاطئةٍ في الكثافةِ قبل أن أقرأ**:
① ترتيبُ الملفات ② الخصوصيةُ ③ المختصرةُ مقابل المحور ④ اسمُ متغيّرِ السلّم
(اخترعتُ `--ems-space-N` والقائمُ `--space-N`، **والقيمةُ الاحتياطيةُ ساوت
الحالَ فبدا الصنفُ عاملًا وهو لا يفعل شيئًا**).
**والسببُ الحقيقيُّ `!important` على القاعدةِ العامة** — قرأتُه من المتصفحِ
بعد أربعِ محاولات. **والقاعدة: اقرأ الإعلانَ الفائزَ قبل أن تغيّر شيئًا.**

---

# ملحقٌ ② — بطاقةُ الإحصاء: **المقامُ قبلَ التعديل**

> قرارُ المالك (رابعًا): «لا تلمس مكوّنًا حتى تكتب: **المقامُ الصحيحُ كم؟ وعددُ
> البطاقاتِ الصحيحُ كم؟ وأين يوجد كلُّ واحد؟** — ولا نعيد خطأَ المقاماتِ في
> أولِ مكوّنٍ نوحّده».

## المقيسُ الآن — والفارقُ عمّا كتبتُ سابقًا يُعلَن

**كتبتُ في تقريرٍ سابق**: «الـ21 كلُّها عائلةُ بطاقةِ الإحصاء»، ووزّعتُها
على ثلاثِ شاشاتٍ (الصيانة 18 · العقود 2 · ملفُّ المورد 2).

**والمقيسُ بالجردِ الكاملِ على 46 شاشةً في 20 مجلدًا:**

| العائلة | قواعدُ CSS | أين | مواضعُ الاستعمال | الشاشات |
|---|---|---|---|---|
| `.stat-card` | **3** | `ems-screens.css` (2) · `main_admin_style.css` (1) | **90** | **17** |
| `.stats-card` | **0** | — *(لا قاعدةَ CSS لها إطلاقًا)* | **88** | **19** |
| `.kpi-card` | **1** | `design-tokens.css` | **26** | **10** |
| `.ux-statcard` | **3** | `uxui-components.css` | **0** | **0** |

**◆ المقامُ الصحيح: 204 موضعًا في 46 شاشةً · و3 عائلاتٍ مستعمَلةٍ + هدفٌ بلا مستهلك.**

## وثلاثةُ فوارقَ جوهريةٍ عن تقريري السابق — أُسجّلها

1. **«21» كان خطأً بمقدارِ عشرةِ أضعاف**: المقيسُ **204**. والرقمُ القديمُ كان
   على نطاقٍ ضيّقٍ (ثلاثُ شاشاتٍ) قُرئ كأنه الكلّ.
2. **الشاشاتُ 46 لا 3**: والصيانةُ والعقودُ وملفُّ المورد **ليست أكبرَ
   المستعملين** — بل `Oprators/oprators.php` (20 موضعًا) و`Clients/clients.php`
   (10) و`FinRequests/cycle_time_board.php` (8).
3. **`.stats-card` أكثرُ العائلاتِ استعمالًا (88 موضعًا) وليس لها قاعدةُ CSS
   واحدة**. فهي إمّا تعتمد على قواعدَ أعمَّ أو **بلا تصميمٍ أصلًا** — وهذا
   وحدَه يغيّر معنى «التوحيد»: ليس دمجَ ثلاثةِ تصاميم، بل **إعطاءُ تصميمٍ لمن
   لا تصميمَ له وتوحيدُ اثنَين**.

## وقرارُك (ب) — التوحيدُ بمتغيّراتٍ مصرَّحة

**والشرطان مُثبَتان قبل التنفيذ**: ثلاثةُ متغيّراتٍ مسمّاةٍ لا رابعَ لها ·
ونصفُ القطرِ 35px في شاشةِ العملاءِ **يُجدوَل مع موجةِ التعميمِ بخطِّ أساسِه
الخاصِّ** ولا يُوحَّد الآن.

**◆ ولا يُنفَّذ التوحيدُ في هذه الجلسة**: تحريكُ 204 موضعٍ في 46 شاشةً موجةُ
تعميمٍ بذاتِها ولها موضعُها في ترتيبِك (بعدَ أولِ ترقيةِ نمط). **والمقامُ
مكتوبٌ الآن** فلا يُبنى على رقمٍ كاذبٍ حين تأتي.
