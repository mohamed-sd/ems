# إدارةُ المبيعات — تقريرُ التدقيق

**الدور:** «مشرف المبيعات» (#888 · دور 12) · **الأسطح:** 46 · **الشركة:** 4
**التاريخ:** 2026-09-04 · **اللقطة:** `016c394d`

---

## ملاحظةٌ على الطريقة

**لم أُدخِل كلمةَ مرورٍ في نموذجِ دخول** — هذا حدٌّ لا أتجاوزه.
استعملتُ **محرِّكَ الفحصِ القائمَ في مستودعك** (`tests/dept_suite/engine.php`) الذي يحمل اعتمادَه في مانيفستِه، وأضفتُ إليه ما ينقصه.

وأصلحتُ مُنقّي الإشاراتِ **ثلاثَ مرّات** قبل أن أثق برقم: النسخةُ الأولى أخرجت **195 إشارةً** جُلُّها وهمٌ — عدّت `$can_delete = false;` جملةَ SQL، وقاست الموضعَ بجدولٍ خطأ. **والعطبُ كان في المُنقّي لا المُنقَّى.**

---

# ① الحالةُ العامّة — سليمة

| المقياس | النتيجة |
|---|---|
| أسطحٌ تُعرَض بلا خطأ | **41 / 41** المتاحة |
| `Fatal error` · `Parse error` | **0** |
| `Warning` · `Notice` · `Deprecated` | **0** |
| عمليّاتُ CRUD مؤكَّدةٌ من القاعدة | **112 ناجحة · 0 فاشلة** |
| حارسُ الرمزِ المكسور | **19 / 19 حيّ** |
| بقايا في القاعدةِ بعد الكنس | **0** |

**صفرُ تحذيرِ PHP في 41 سطحًا** نتيجةٌ لا يبلغها كثيرٌ من الأنظمة. القاعدةُ متينة.

---

# ② المشاكلُ المعماريّة

## ⓵ ثلاثةُ أسطحٍ في جردِ المبيعاتِ **مملوكةٌ لإداراتٍ أخرى** 🔴

الحارسُ يردُّها إلى `dashboard.php` — **وهو مُحِقٌّ. الخطأُ في الجرد.**

| السطح | الوحدة | مالكُها الحقيقيّ |
|---|---|---|
| `Operations/equipment_quota.php` | «توزيع وحدات المورد على معداته» | التشغيل · **الموردون** · مشرف موردين |
| `Operations/sites_board.php` | «لوحة المواقع» | التشغيل · **الموقع** |
| `Operations/swap_request.php` | «طلب تبديل» | **التشغيل** |

**الأثر:** جردُ أسطحِ المبيعاتِ خاطئٌ بثلاثة. وعُدّةُ الفحصِ تعدُّها «محجوبة» فتمرُّ خضراءَ — **فالخطأُ مستورٌ بحكمٍ صحيح**.

**العلاج:** تُنقَل الثلاثةُ إلى مانيفستاتِ مالكيها (DEP-11 · DEP-02 · DEP-12).

## ⓶ خمسةَ عشرَ سجلًّا قائمًا بذاته **بلا موضعٍ في السايدبار** 🟡

يُفتَح بالمسارِ ويعمل — **ولا يُبلَّغ في الملاحة**. وهذه ليست شاشاتِ تفصيلٍ تُفتَح بسياقٍ (تلك واحدةٌ فقط: `client_profile.php`).

```
Clients/activities.php            Clients/pricelists.php
Clients/contract_amendments.php   Clients/rate_books.php
Clients/contract_commitments.php  Clients/readiness_lines.php
Clients/contract_events.php       Clients/tenders.php
Clients/units_of_measure.php      Contracts/contracts_details.php
Contracts/unit_client_match.php   Contracts/unit_statement_client.php
Reports/contract_report.php       Reports/contractall.php
movement/client_tree.php
```

**الأثر:** «الصلاحيةُ تمنح الوصولَ والموضعُ يمنح الملاحة» — فالموظّفُ **لا يبلغ خمسةَ عشرَ سجلًّا إلا بمسارٍ يحفظه**. ومنها مرجعيّاتٌ أساسيّةٌ: قوائمُ التسعير · وحداتُ القياس · كتبُ الأسعار · المناقصات.

**العلاج:** موضعٌ في `nav_placements` لكلِّ سجلٍّ قائمٍ بذاته.

---

# ③ المشاكلُ البرمجيّة

## ⓷ عُدّةُ الفحصِ نفسُها **عمياءُ عن التحذيرات** 🟡

`tests/dept_suite/engine.php:392` يرصد `Fatal error` و`Parse error` **وحدَهما**.

فصفحةٌ تطبع عشرينَ `Warning: Undefined array key` تمرُّ **«ناجحةً · HTTP 200»**.

**الأثر:** ثغرةُ قياسٍ لا عطبُ منتَج. وقد سددتُها في `tools/injint01/sales_runtime.php` والنتيجةُ **صفر** — لكنّ الثغرةَ تبقى مفتوحةً لما يأتي.

**العلاج:** تُضمُّ العائلاتُ الثلاثُ إلى حاجبِ المحرِّك.

## ⓸ `Operations/swap_request.php` — ثلاثةُ استعلاماتٍ بلا فحصِ نتيجة 🟢

الاستعلامُ الفاشلُ يُرجِع `false` فيصير الجدولُ فارغًا صامتًا — **فراغٌ يُقرأ «لا بيانات»**.

**وخارجُ نطاقِ المبيعاتِ فعليًّا** (السطحُ مملوكٌ للتشغيل · بند ⓵) — فيُعالَج مع مالكِه.

## ⓹ حقنُ SQL — **لم أجد شاهدًا**

فحصتُ السلاسلَ المُستبدِلةَ في الأسطحِ الستّةِ والأربعين وتتبّعتُ إسنادَ كلِّ متغيّر. العيّناتُ التي فحصتُها يدويًّا **كلُّها مُهذَّبة**:

- `Clients/activities.php:69` → `$company_id = intval($_SESSION[...])` (سطر 45)
- `Clients/clients.php:143` → `$project_active_status_sql` جزءُ جملةٍ حرفيٌّ ثابت

> ⚠ **وهذه ليست شهادةَ خلوّ**: المُنقّي النصّيُّ لا يُثبت الأمان. ما أقوله: **لم أجد شاهدًا على حقنٍ فيما فحصتُ**، والحكمُ القاطعُ يحتاج مراجعةً يدويّةً للأسطحِ كلِّها.

---

# ④ المشاكلُ المنطقيّة

## ⓺ ثلاثُ بطاقاتٍ **فارغةٍ أبدًا** في ثلاثِ شاشات 🟡

بطاقةُ «سجل حقول الورقة» تقرأ جدولًا **أُنشئ ولم يُملأ قطُّ**:

| الشاشة | تكتب في | والبطاقةُ تقرأ | النتيجة |
|---|---|---|---|
| `Clients/quotations.php` | `quotations` **141** | `sal_quotations` **0** | «لا سطر مسجل بعد» |
| `Projects/projects.php` | `project` **125** | `sal_projects` **0** | «لا سطر مسجل بعد» |
| `Opportunities/client_need_rfq.php` | — | `sal_client_needs` **0** | فارغة |

**والبطاقةُ مقصودةٌ بالتصميم** — تعليقُها يقول: «يُضاف بجانبِ ما بُني لا بدلًا منه». فليست خطأً في التوجيه.

**لكنَّ الأثرَ حقيقيّ:** المستخدمُ يرى بطاقةً تقول «لا سطر مسجل بعد في سجل العروض» **وأمامه 141 عرضًا**. رسالةٌ تُقرأ نفيًا وهي عن سجلٍّ آخر.

**العلاج:** تُملأ `sal_*` من مصدرِها، أو تُخفى البطاقةُ حتى تُملأ، أو يُصرَّح في عنوانِها أنَّها سجلُّ الورقةِ لا سجلُّ العروض.

## ⓻ الاعتمادُ الذاتيّ — **لا شاهد**

أشار مُنقّي إلى ثلاثةِ أسطحٍ ثمَّ **سقطت كلُّها بالفحص**:

- `plan_actual_link.php` — لفظُ «اعتماد» في **رؤوسِ جدولٍ** (`<th>`)
- `unit_client_match.php` — في **نصِّ إرشادٍ** وأسماءِ حالاتٍ (`sales_approved`)
- `sites_board.php` — خارجُ نطاقِ المبيعاتِ أصلًا

> **الماسحُ يقرأ نصَّ الواجهةِ كما يقرأ المنطق.** ولا يُكتب «عطبٌ» على مطابقةِ لفظ.

---

# ⑤ الخلاصة

| المحور | مؤكَّد | الخطورة |
|---|---|---|
| **معماريّ** | 3 أسطحٍ في جردِ إدارةٍ ليست مالكتَها | 🔴 عالية |
| **معماريّ** | 15 سجلًّا بلا موضعٍ في الملاحة | 🟡 متوسطة |
| **برمجيّ** | عُدّةُ الفحصِ عمياءُ عن التحذيرات | 🟡 متوسطة |
| **برمجيّ** | 3 استعلاماتٍ بلا فحصِ نتيجة (خارجَ النطاق) | 🟢 منخفضة |
| **منطقيّ** | 3 بطاقاتٍ فارغةٍ أبدًا برسالةٍ مضلِّلة | 🟡 متوسطة |
| **برمجيّ** | حقنُ SQL | **لا شاهد** |
| **منطقيّ** | اعتمادٌ ذاتيّ | **لا شاهد** |

## الحكم

**إدارةُ المبيعاتِ سليمةٌ تشغيليًّا.** لا خطأَ برمجيًّا واحدًا في 41 سطحًا، والقاعدةُ متينةٌ والحرّاسُ أحياء.

والمشاكلُ الثلاثُ المؤكَّدةُ **مشاكلُ جردٍ وموضعٍ وعرضٍ** لا مشاكلُ شيفرة:

1. **ثلاثةُ أسطحٍ في الجردِ الخطأ** — تُنقَل إلى مالكيها.
2. **خمسةَ عشرَ سجلًّا لا يُبلَغ إلا بمسارٍ محفوظ** — يُوضَع لها موضع.
3. **ثلاثُ بطاقاتٍ تنفي وجودَ ما هو موجود** — تُملأ أو تُخفى أو يُصحَّح عنوانُها.

---

## العُدّةُ المستعملة

| الأداة | الوظيفة |
|---|---|
| `tests/dept_suite/run.php` | **قائمةٌ عندك** — 46 سطحًا × 5 عمليّات |
| `tools/injint01/sales_runtime.php` | **جديدة** — تسدُّ عمى المحرِّكِ عن التحذيرات |
| `tools/injint01/sales_audit.php` | **جديدة** — مرشَّحاتٌ معماريّةٌ وبرمجيّةٌ ومنطقيّة |
| `docs/injint01/sales_runtime.json` · `sales_audit.json` | التفصيلُ الخام |
