# INJ-INT-01 — تقريرُ الدفعةِ الأولى

**اللقطة:** `016c394d` · فرع `freeze/single-company` · الشركة 4 · 2026-09-04 UTC
**النسخةُ الاحتياطيّة:** `C:/wamp64/backups/ems/ems_snapshot_20260904T014106Z.sql` (193 م.ب · 1,236 جدولًا)
**البصمة:** `5b628f7aa4c122cc5ae4e8c3d7c50f88747befe8079dc901baefef7f7a8c0928`

---

# القسمُ الأوّل — السبعةُ الآمنةُ: منفَّذة

| # | البند | الحالة | الدليل |
|---|---|---|---|
| 0.1 | دفعُ 455 التزامًا | 🔴 **بيدك** | SSH بعبارةِ مرور — الأمرُ جاهزٌ أدناه |
| 0.2 | لقطةٌ ذرّيّة | ✅ | 193 م.ب · مختومةٌ بـSHA-256 · **خارجَ شجرةِ المستودع** |
| 0.3 | تجميدُ الإعادةِ التاريخيّة | ✅ | `HISTORICAL_AUTO_REPLAY = OFF` مُثبَتًا |
| 2 | حاجبُ الساعة | ✅ | `CLOCK_INTEGRITY = PASS` |
| 7 | سجلُّ التصرُّفِ في التسليم | ✅ | 37 صفًّا محكومًا |
| 9 | تدقيقُ تغطيةِ اللاتكرار | ✅ | 34 مستهلكًا · المعيارُ = 0 |
| 10 | إعادةُ القياسِ الكاملة | ✅ | `docs/injint01/REMEASURE.txt` |

**ولم يُمَسَّ جدولٌ حيٌّ ولا صفٌّ قائم.** كلُّ ما كُتب: جدولانِ جديدانِ بهجرةٍ لها عكسُها.

## أربعةُ اكتشافاتٍ لم تكن في الدراسة

### ⓵ `HISTORICAL_AUTO_REPLAY` مطفأةٌ — لكنَّ المخزنَ ليس ساكنًا

مهمّةُ `event_retry` **حيّةٌ كلَّ خمسِ دقائق**. وفحصُ شيفرتِها حسم الفرق:

| القياس | القيمة |
|---|---|
| قراءةُ `ems_event_deliveries` لإعادةِ الدفع | **0 موضعًا** |
| استهدافُ `state='processed'` | **0 موضعًا** |
| حرّاسُ `NOT EXISTS` قبل الإنتاج | 4 مواضع |
| تسليماتٌ أُعيدت وصارت `processed` | **0** |

> فما تفعله **ملءٌ أماميٌّ محروسٌ باللاتكرار** لا إعادةُ تسليم: تُنشئ حدثًا لصفِّ مصدرٍ **لم يُنتَج له حدثٌ قطُّ**. فالقياسُ قد يزيد بين تشغيلَين — وهذا **نموٌّ لا ازدواج**.

### ⓶ الأحدَ عشرَ: صفرُ خسارةٍ ماليّة

| الحكم | العدد | بأثرٍ محقَّق |
|---|---|---|
| `NON_RETRYABLE` | 26 | **25** |
| `SOURCE_UNRESOLVABLE` | 11 | **0** |
| `SAFE_RETRY` | **0** | — |

قراءتان تُغلِقان البابَ:

- الـ25 المتقاعدةُ **أثرُها محقَّقٌ سلفًا** — فالقرارُ المكتوبُ («مغطًّى بمستهلكٍ عاملٍ آخر») **صحيحٌ بالقياسِ لا بالثقة**.
- الـ11 اليتيمةُ **أثرُها صفرٌ** — فلا قيدَ ضاع ولا مبلغَ سقط. آباؤها محذوفون فحسب.

### ⓷ 27 من 28 مستهلكًا ذا أثرٍ **لم تمرَّ به معاملةٌ واحدة**

| المستهلك | مفاتيح | أحداثٌ منتَجة |
|---|---|---|
| `governance_watch` | 70 | **21,627** |
| **كلُّ الباقين** (محاسبة · خزينة · تمويل · رواتب · تحصيل) | 1 لكلٍّ | **صفر** |

> `BUILT_NOT_EXERCISED_CONSUMERS = 27 / 28`

وهذا يفسّر ما بدا لغزًا: الأساسُ الماليُّ حقيقيٌّ (6,713 قيدًا متوازنًا بفرق 0.00) **لكنّه لا يُغذَّى من الناقل**. الدورةُ الماليّةُ تعمل بمسارٍ مباشرٍ، ومستهلكو الناقلِ مسجَّلون على مفاتيحَ لا تُنتَج.

### ⓸ `EffectLinkConsumer` **مُتحقِّقٌ لا مُنتِج**

لا يكتب سطرًا واحدًا. يفحص ثلاثةَ شواهدَ ويرمي `EFFECT_MISSING` إن غابت جميعًا. وخريطتُه للكيانات **سليمة** (`fin_asset → fin_assets`).

> ⇐ فالأحدَ عشرَ **ليست عطبًا فيه؛ هي الحارسُ يعمل كما صُمِّم**: يرفض أن يمرَّ بصمتٍ على حدثٍ فقد أباه.

---

# القسمُ الثاني — الثلاثةُ المتبقّية

## ⓵ GAP-11 — **الخطرُ انهار من 2,202 صفحةٍ إلى رابطَين**

قِستُ الفرقَ بظلٍّ لا يمسُّ شيئًا (`tools/injint01/gap11_shadow.php`): نصّبتُ جلسةَ مستخدمٍ حقيقيٍّ لكلِّ دورٍ، واستدعيتُ **الدالّتَين اللتين تستدعيهما الشاشةُ نفسُها**.

| القياس | القيمة |
|---|---|
| أدوارٌ مقيسة | **32** (31 مغطًّى بقالبٍ نافذ) |
| بنودُ ملاحةٍ مفحوصة | **2,354** |
| `WOULD_APPEAR` | **0** |
| `WOULD_DISAPPEAR` | **2** |

والاثنانِ **رابطٌ واحدٌ في دورَين**:

```
Portal/my_tasks.php  →  دور 28 «إدارة المخاطر» · دور 29 «محلل المخاطر»
```

### والجذرُ مقيسٌ لا مُخمَّن

`navarch_authorized_routes()` تُصرِّح ببندٍ إذا `permission_code IS NULL` — **فرعٌ مفتوحٌ افتراضًا** لا نظيرَ له في الحارسِ المركزيّ (وهو fail-closed عبر القالب). والمستخدمانِ (‏#897 · #898) **لا بندَ لهما في قالبَيهما** (21 · 25).

> **وبنودُ الملاحةِ النشِطةُ بلا `permission_code` = 2 من 2,485.**
> فإغلاقُ GAP-11 مسُّ **صفَّين**، لا قلبُ سايدبار.

**والقرارُ الوحيدُ المتبقّي قرارُ أعمالٍ لا هندسة:** أَيَستحقُّ دورا المخاطرِ شاشةَ «مهامي»؟

- **نعم** ⇒ يُضاف البندُ إلى القالبَين، ثم تُغلق الفجوةُ **بصفرِ اختفاء**.
- **لا** ⇒ تُغلق الفجوةُ ويختفي الرابطان بحقّ.

## ⓶ توحيدُ مصدرِ الـDLQ — **أخطرُ ممّا قدَّرتُ، والسببُ ليس الذي ذكرتُه**

قلتُ لك إنَّ الخطرَ كاتبٌ حيّ. القراءةُ الكاملةُ أظهرت ما هو أدقّ: **مسارانِ بدلالتَين متضادّتَين**.

| | المسار (أ) — `EventDispatcher:136` | المسار (ب) — `EventDeliveryWorker` |
|---|---|---|
| عند استنفادِ المحاولات | `INSERT` في `ems_event_dead_letter` | `state='dlq'` على الصفِّ نفسِه |
| ثمَّ | **`DELETE FROM ems_event_deliveries`** | يُبقي الصفَّ شاهدًا |
| صفوفُه اليوم | 0 | **37** |

> **المسارُ (أ) يحذف صفَّ التسليم.** فتحويلُ `ems_event_dead_letter` إلى رؤيةٍ — كما ينصُّ الأمرُ حرفًا — لا يكسر إدراجًا فحسب، بل **يُبقي الحذفَ ويُلغي التسجيل**: يختفي التسليمُ بلا أثرٍ في أيِّ مكان.

والجدولُ فارغٌ لأنَّ المسارَ (أ) **لم يُطلق قطُّ**، لا لأنَّه مهجور.

**والعلاجُ إعلانٌ لا هندسة:** يُعلَن `ems_event_deliveries.state` حاكمًا، وتُوجَّه المراقبةُ إليه، **ولا يُمَسُّ الجدولُ ولا كاتبُه**. `DLQ_SOURCE_OF_TRUTH_COUNT = 1` بالإعلانِ والقياس.

*(وأمّا `ems_event_delivery_orphans` = 82 فمحسومةٌ سلفًا: بذرُ UAT-2026 للشركة 1 المعلَّقة، مؤرشفٌ بسببٍ مكتوب.)*

## ⓷ إعادةُ مصالحةِ الأحكام — الخطرُ قائمٌ كما وصفتُه

الأحكامُ الثمانيةُ والخمسون **كلُّها** موسومة:

```
decided_by = "INJ-FIX-01 · تفويضُ المالك 2026-08-21"
```

والكتّابُ القائمون (`2027_08_26` · `2027_08_28`) يستعملون `INSERT` و`UPDATE` مباشرَين على الجدولِ نفسِه. فإعادةُ الاستيعابِ **تدهس تفويضَك** بلا خطأٍ ولا إنذار.

**والعلاج:** يُقاس في **عمودٍ جديدٍ بجانبِ** الحكمِ لا فوقَه، فيظهر الانحرافُ ويبقى القرار.

**والمقامُ متقادمٌ 14 يومًا** (آخرُ قياس 2026-08-21)، وقد تغيّر تحته شيءٌ جوهريّ: أحكامُ `business` الأحدَ عشرَ كلُّها معلَّقةٌ على `GovernanceWatchConsumer` — **مراقبٌ لا مُنتِجُ أثر**.

---

# الخلاصة

| البند | الخطرُ المُقاس | القرارُ المطلوب |
|---|---|---|
| **GAP-11** | 🟢 **رابطانِ · صفّان** | أَيَستحقُّ دورا المخاطرِ «مهامي»؟ |
| **DLQ** | 🟡 حُلَّ بالإعلانِ بلا مساسٍ بالمخطَّط | إقرارُ `deliveries.state` حاكمًا |
| **الأحكام** | 🟡 دهسٌ صامتٌ — يُتجنَّب بعمودٍ مجاور | إذنٌ بالقياسِ المجاور |

والحاجزُ الحقيقيُّ الوحيدُ ليس أيًّا من الثلاثة — بل **455 التزامًا لا نسخةَ لها إلا هذا القرص**:

```bash
git push -u origin freeze/single-company
```
