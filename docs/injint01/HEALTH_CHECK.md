# دراسةُ سلامةِ النظامِ بعد التعديلاتِ الأخيرة

**قراءةٌ واختبارٌ فقط — بلا تعديلِ حرف.**
**اللقطة:** `7d2f6ac1` · **الشركة:** 4 · **2026-09-04**

---

# الحكمُ المختصر

## ✅ **النظامُ يعمل. ولم يُعطَّل شيءٌ بتعديلاتي.**

**وثلاثةُ أعطابٍ قائمةٌ أقدمُ من جلستي** — تُسمَّى أدناه بأدلّتِها.

---

# ① نطاقُ ما فُحص

بين الأمسِ واليومَ وقع تغييرانِ لا واحد:

| المصدر | ما مسَّه |
|---|---|
| **تعديلاتي** | `EventDispatcher` (6 مواضع) · محرِّكُ الفحصِ وعارضُه · **4 جداولَ جديدة** |
| **تعديلاتٌ ليست لي** | خمسةُ التزامات — أربعةٌ منها **PERM-01** تمسُّ الصلاحيّات (13:42 · 14:07 · 14:17) |

⇒ **فالدراسةُ تشمل الاثنين.**

---

# ② الأثرُ الماليّ — سليمٌ تمامًا

| المقياس | قبلَ التعديل | الآن |
|---|---|---|
| قيودُ اليوميّة | 6,713 | **6,713** |
| أسطرُ القيد | 13,426 | **13,426** |
| مدين · دائن | — | 37,694,775.13 · 37,694,775.13 |
| **الفرق** | 0.00 | **0.00** |
| **قيدٌ غيرُ متوازنٍ منفردًا** | — | **0** |
| `fin_event_effects` | 5,210 | **5,210** |
| `fin_event_links` | 10,519 | **10,519** |

**صفرُ صفٍّ ماليٍّ تغيّر.**

---

# ③ الناقلُ — يعمل، والجديدُ ليس منّي

## الاختبارات

```
tests/event_dispatcher_test.php   →   16 ناجح · 0 فاشل
cron_jobs.php (دورةٌ حيّة)         →   0 فشل · 0 إنذار
```

## والمؤشراتُ لاحقةٌ مكتملة

```
finance · finance_routing · fx · replay   ⇒  المؤشر = 17985
MAX(fin_financial_events.id)              =  17985   ✔
```

## ⚠ لكنَّ الـdlq زاد أربعةً وظهر `failed=1`

**وهذا لم يأتِ من تغييري — والدليلُ قاطع:**

| القياس | القيمة |
|---|---|
| صفوفُ **الجيلِ الأولِ** (الذي عدّلتُه) في الدفتر | **0** |
| `ems_dispatcher_attempts` (جدولي الجديد) | **0** |
| كلُّ الصفوفِ الجديدة | **الجيلُ الثاني** (`outbox_id > 0`) — **لم أمسَّه** |
| صفٌّ ميتٌ أُعيد التقاطُه | **0** |

---

# ④ 🔴 عطبٌ حيٌّ — أخطرُ ما وجدته، وليس منّي

## `finance.recon_adjustment.posted` تُنتِج أيتامًا باستمرار

| القياس | القيمة |
|---|---|
| أحداثٌ منتَجة | **226** |
| **منها يتيمٌ** | **226 / 226** |
| جدولُ الكيان `bank_recon_matches` | **0 صفّ** |
| `AUTO_INCREMENT` للجدول | **1398** |
| أُنتج في آخرِ ست ساعات | **5** |
| أوّلُ ظهور | **2026-07-31** |

## ⇒ **عدّادٌ عند 1398 وجدولٌ فارغ = الصفوفُ أُدرجت ثمَّ زالت**

فالمُنتِج (`BankReconService`) يُدرِج مطابقةً · يُطلق حدثَها · **ثمَّ يزول الصفّ** — فيبقى الحدثُ بلا أبٍ ويعزله `EffectLinkConsumer` بحقّ.

## والتوقيتُ يُبرِّئُ تعديلاتي ويشير إلى نافذةِ PERM-01

```
مسحي انتهى            10:03
الأحداثُ اليتيمةُ الخمس  13:55 – 14:01     ← بعده بأربعِ ساعات
التزامُ PERM-01 «كاشفُ فصلِ الواجبات»  13:42
التزامُ PERM-01 التالي                14:07
```

⇒ **تقع بين التزامَي PERM-01.** وأداةُ كشفِ فصلِ الواجباتِ تُمارِس دورةَ الدفعِ والمطابقةِ البنكيّة — **فهذا أرجحُ مصدرٍ، ويحتاج تأكيدَك.**

> ⚠ **والعطبُ نفسُه أقدمُ بكثير** — 226 حدثًا منذ 2026-07-31. فما جرى اليومَ **زيادةٌ على قائمٍ لا حَدَثٌ جديد.**

---

# ⑤ الصلاحيّاتُ بعد PERM-01 — لم يفقد دورٌ وصولَه

سألتُ **الحارسَ المركزيَّ نفسَه** عن كلِّ وحدةٍ لكلِّ دورٍ له حسابٌ حيّ:

| القياس | القيمة |
|---|---|
| أدوارٌ مقيسة | **32** |
| وحداتٌ لكلٍّ | 773 |
| **أدوارٌ بصفرِ سماح** | **0** ✅ |
| المجموع: يسمح · يمنع | 4,913 · 19,823 |

**والفصلُ البنكيُّ ما زال مفتوحًا:** الدوران 34 و35 **171 سماحًا لكلٍّ** — متطابقانِ تمامًا، و**166 شاشةً مشتركة**. ⇒ PERM-01 عند §3/§4 ولم يغلقه بعد.

---

# ⑥ الشاشاتُ تُصيَّر — قياسٌ حيّ

## المبيعات — الفحصُ الكامل

| العملية | ناجح | بتحذير | فاشل |
|---|---|---|---|
| عرض | 43 | **0** | 0 |
| إضافة · تعديل · حذف · حارس | 19 · 17 · 14 · 19 | **0** | **0** |
| **الإجمالي** | **112** | **0** | **0** |

**والفرقُ عن خطِّ الأساس: لا شيءَ تغيّر (230 عمليّةً مطابقة) · وصفرُ بقايا.**

## والمسحُ الكاملُ — ستَّ عشرةَ إدارةً قبلَ PERM-01 وبعده

**1,446 سطحًا · صفرُ تحذيرٍ في المسحَين · وأربعةَ عشرَ إدارةً مطابقةً حرفًا.**

| المقياس | قبل | بعد |
|---|---|---|
| أسطحٌ مُصيَّرة | 1,446 | **1,446** |
| تحذيراتُ PHP | 0 | **0** |
| جردٌ خطأ · بلا موضع · بطاقات | 72 · 424 · 630 | **72 · 424 · 630** |

### وإدارتانِ نقصت كلٌّ منهما سطحًا نظيفًا — **عابرٌ لا ثابت**

| الإدارة | نظيف (قبل ⇒ بعد) | السبب |
|---|---|---|
| DEP-02 | 105 ⇒ 104 | `HTTP=1` |
| DEP-04 | 99 ⇒ 98 | `HTTP=1` |

**أعدتُ فحصَ DEP-04 مباشرةً:**

```
نظيف = 99   ·   HTTP = 0
```

⇒ **رمزٌ عابرٌ في تشغيلةٍ واحدة** (تزاحمٌ على الخادمِ أثناء مسحِ 1,446 سطحًا) **لا انحدارٌ في سطح**. ولو كان ثابتًا لتكرّر.

# ⑦ سجلُّ الأخطاء — 81 خطأً اليوم، **لا واحدَ منها من النظامِ بسببي**

| العائلة | العدد | الحكم |
|---|---|---|
| `settlement collect: Table 'proc_issue' doesn't exist` | 14 | 🔴 **عطبٌ قائم** |
| `role_board: Unknown column 'q.is_deleted'` | 14 | 🔴 **عطبٌ قائم** |
| `role_board: scopedQuery: manual company_id refused` | 14 | 🔴 **عطبٌ قائم** |
| `role_board: يلزم WHERE علوية واحدة بالضبط` | 14 | 🔴 **عطبٌ قائم** |
| أخطاءُ **أدواتي أثناء بنائها** | 3 | ✅ أُصلحت في حينها · `tools/` لا الإنتاج |

**وأوّلُ ظهورٍ للأربعِ الأُوَل: `2026-09-03 21:13` — قبلَ بدءِ جلستي.**

⇒ **شاشةُ `main/role_board.php` تُخرج أربعةَ أخطاءٍ مختلفةٍ 14 مرّةً في اليوم.** وهي بعينِها الشاشةُ التي رصدها مسحي «يردُّها الحارس» في المبيعاتِ والموردين.

---

# ⑧ الجداولُ الجديدةُ — معزولةٌ بنيويًّا

| الجدول | صفوف | مفاتيحُ خارجة | يشير إليه |
|---|---|---|---|
| `injint01_retry_disposition` | 77 | **0** | **0** |
| `injint01_idempotency_audit` | 34 | **0** | **0** |
| `gov_event_observations` | 58 | **0** | **0** |
| `ems_dispatcher_attempts` | 0 | **0** | **0** |

**صفرُ مفتاحٍ أجنبيٍّ في الاتجاهَين** ⇒ إسقاطُها لا يُيتِّم صفًّا، ووجودُها لا يقيّد كتابةً.

---

# ⑨ الخلاصة

## ما لم يُعطَّل

| | |
|---|---|
| الأثرُ الماليّ | ✅ صفرُ صفٍّ تغيّر · الميزان 0.00 |
| الناقل | ✅ 16/16 · مؤشراتٌ لاحقة · صفرُ إعادةِ التقاط |
| الصلاحيّات | ✅ صفرُ دورٍ فقد وصولَه |
| الشاشات | ✅ 112/0/0 · والمالية مطابقةٌ حرفًا |
| صياغةُ الشيفرة | ✅ 21 ملفًّا سليمًا |

## وثلاثةٌ قائمةٌ تحتاج قرارَك — **كلُّها أقدمُ من جلستي**

| # | العطب | الدليل |
|---|---|---|
| ① | **`bank_recon_matches` يُنتِج أيتامًا حيًّا** | 226/226 يتيم · عدّاد 1398 وجدول فارغ · 5 في آخرِ 6 ساعات |
| ② | **`role_board` أربعةُ أخطاءٍ × 14 يوميًّا** | `php_error.log` منذ 2026-09-03 21:13 |
| ③ | **`proc_issue` جدولٌ مفقود** | يكسر `settlement collect` 14 مرّةً يوميًّا |

## وواحدٌ يحتاج تأكيدَك

**الأحداثُ اليتيمةُ الخمسُ اليومَ تقع بين التزامَي PERM-01 (13:42 · 14:07).** أَشغَّلت أداةَ كشفِ فصلِ الواجباتِ حينها؟ فإن نعم، فهي تُمارِس دورةَ الدفعِ والمطابقةِ **وتترك أيتامًا** — وذلك يحتاج حارسًا في الأداةِ نفسِها.
