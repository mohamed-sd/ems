# INJ-INT-01 + RJ-01 — دراسةُ ما قبلَ التنفيذ

**المِسبار:** `tools/injint01_recon.php` (قراءةٌ محضة · قابلٌ للإعادة)
**الرأس:** `016c394d` · فرع `freeze/single-company` · الشركة 4 · القياس 2026-09-04 01:16 UTC

---

## ① خلاصةُ الحكم

الأمرانِ سليمانِ منهجًا (**أصلِح القائمَ قبل أن تبني**)، لكنّ **ستةً من أرقامِهما مأخوذةٌ عن
لقطةٍ متقادمة**. لو نُفِّذت الدفعةُ الأولى حرفيًّا لصرفتُ العملَ على أعطابٍ مُصلَحةٍ سلفًا،
ولوقفتُ عند بنودٍ **مُدخَلُها فارغٌ أصلًا**.

المنهجُ يُعتمَد. والمقامُ يُعاد قياسُه — وقد أُعيد أدناه.

---

## ② مصالحةُ ادّعاءاتِ الأمرِ بالمقيسِ الحيّ

| # | ادّعاءُ الأمر | المقيسُ الآن | الحكم |
|---|---|---|---|
| 1 | «Clock Skew مثبت» — تُصلَح الساعةُ أوّلًا | `OS = PHP = DB` في UTC · **الفرق 0 ثانية** | **مُصلَحٌ سلفًا** — البند 2 من الدفعةِ ساقط |
| 2 | «11 تسليمًا كلُّها dlq و**صفر** processed» | **21,381 processed** · 37 dlq | **المقامُ خطأ** — الناقلُ يعمل، لا يتعطّل |
| 3 | «تعارضٌ: `ems_event_dead_letter=0` مقابل dlq» | الجدولُ مهجورٌ فعلًا؛ الحاكمُ `ems_event_deliveries.state` | تعارضٌ حقيقيّ — **يُحسم بإعلانٍ لا بهندسة** |
| 4 | «EffectFanout عطبُ FQCN — الصنفُ `App\Core` والمسارُ `App\Services`» | صحيحٌ حرفيًّا · **لكنَّ `inactive_reason` مكتوب**: «الصنفُ الحقيقيُّ `App\Services\EffectFanout` — محرّكُ دوالَّ ساكنةٍ يُنادى داخلَ المعاملةِ نفسِها، **ولا مدخلَ `handle()` فيه**. فليس مستهلكَ ناقلٍ أصلًا، وأثرُه الماليُّ باقٍ حيث كان» | **`NOT_CAUSAL`** — §7 يُجاب بـ`STALE_REGISTRY_ONLY`. البند 8 ساقط |
| 5 | «23 من 58 لها Business Consumer» | **58/58** لها صفٌّ نشِطٌ في السجلِّ الحاكم. الـ23 كانت تقيس `EffectLinkConsumer` **وحدَه** | مقامٌ جزئيٌّ قُرِئ كلّيًّا |
| 6 | «6,713 قيدًا و13,426 سطرًا متوازنة بفرق 0.00» | 6,713 · 13,426 · مدين=دائن=37,694,775.13 · **فرق 0.00** | ✔ **مطابقٌ حرفًا** |
| 7 | GAP-11 مفتوحة | ✔ مؤكَّدٌ بحاجبِ `injfix01_gap_coverage` (12 مفتوحةً من 33) | ✔ صحيح |

---

## ③ الأعطابُ الحقيقيّةُ المقيسة

### ⓵ خطرٌ داهم — 455 التزامًا خارجَ المستودعِ البعيد

```
فرع freeze/single-company · لا upstream مضبوط
git rev-list origin/main..HEAD  =  455
```

هذا **أوّلُ معاييرِ الأمرِ حرفيًّا** (`LOCAL_ONLY_CRITICAL_COMMIT = 0`) وأخطرُ ما في القياس:
عملُ الحملاتِ كلِّها (REPAIR01 · NAV-ARCH-02 · GOV-UI · PERM-SCR · GOV-AUTH) قائمٌ على
قرصٍ واحدٍ بلا نسخةٍ بعيدة.

**والدفعُ ليس لي**: المستودعُ بـSSH بعبارةِ مرور — التنفيذُ بيدك.

### ⓶ الـDLQ — سبعةٌ وثلاثون بثلاثِ عائلاتٍ لا رقمٌ واحد

| العائلة | العدد | الحكم |
|---|---|---|
| `effectfanout` · `CONSUMER_RETIRED` | **25** | **قرارٌ مكتوبٌ لا عطب**: «أُغلق ولا يُعاد — ونوعُ الحدثِ مغطًّى بمستهلكٍ عاملٍ آخر» |
| `effectlink` · `HANDLER_ERROR` (EFFECT_MISSING) | **11** | عطبٌ حقيقيّ — أدناه |
| `balancecalculator` · `NO_SUB` | 1 | اشتراكٌ معطَّل |

### ⓷ الأحدَ عشرَ — الجذرُ **كنسٌ حذف الآباءَ**، والمجموعةُ **غيرُ قابلةٍ للإعادة**

كلُّ صفٍّ من الأحدَ عشرَ يشير إلى **كيانِ مصدرٍ لا وجودَ له**:

| الحدث | الكيان | الجدولُ الحقيقيّ | الواقع |
|---|---|---|---|
| `expense.depreciation.recorded` ×9 | `fin_asset#873,874,875` | `fin_assets` | **26 صفًّا · أقصى id = 191** |
| `payable.finance_installment.accrued` ×1 | `fin_funding_schedule#991` | `fin_funding_schedules` | **57 صفًّا · المدى 58–114** |
| `finance.recon_adjustment.posted` ×1 | `bank_recon_match#1361` | `bank_recon_matches` | **فارغٌ تمامًا (0)** |

وجميعها `links=0 · effects=0` — فالأثرُ **لم يتحقّق** ولا ازدواجَ يُخشى.

> **الحكم: `SOURCE_UNRESOLVABLE` / `NON_RETRYABLE`.**
> مجموعةُ `SAFE_RETRY` **فارغة**، فالبندان 9 و10 من الدفعةِ الأولى **بلا مُدخَل**.
> والإعادةُ تفشل بالخطأِ نفسِه حتمًا.

الجذرُ متّسقٌ مع دفترِ الحملاتِ: كنسُ تصغيرِ القاعدةِ حذف صفوفَ الآباءِ وترك الأحداثَ يتيمة.

### ⓸ سجلّانِ للمستهلكين — وأحدُهما لا يقرؤه الإنتاج

| السجل | صفوف | من يقرؤه |
|---|---|---|
| `event_consumers` | 142 | **الحاكم** — `EventOutboxFanout:45` · `EventDeliveryWorker:346,357` |
| `ems_event_subscriptions` | 142 | **لا سطرَ إنتاجٍ واحد** — هجرتانِ وثلاثُ أدواتِ قياسٍ فقط |

مكتوبٌ في `App/Core/EventDispatcher.php:331` بالحرف: «فالسجلُّ **إعلانُ نيّةٍ لا عقدُ تنفيذ**».

### ⓹ المقامُ الصادقُ لتغطيةِ الأثر

|  | العدد |
|---|---|
| مفاتيحُ منتَجة | 58 |
| محكومةٌ `business` | 11 |
| منها لها مستهلكُ أثرٍ نطاقيّ (`EffectLinkConsumer`) | **11 / 11** |
| مغطّاةٌ بمراقبٍ فقط (`GovernanceWatchConsumer`) | 70 مفتاحًا |

**لكنَّ آخرَ قياسٍ للأحكامِ 2026-08-21** — أي **14 يومًا** قبل اليوم. المقامُ يُعاد اشتقاقُه.

### ⓺ 2,224 حدثًا بختمٍ مستقبليّ — وليست انزياحَ ساعة

`occurred_at` في 2087 (220) و2088 (1,999) — والعددانِ **يطابقان تمامًا**
`payable.finance_installment.accrued` (220) و`expense.depreciation.recorded` (1,999).

فهي **جداولُ إهلاكٍ وأقساطٍ مُسقَطةٌ عمدًا**، لا خللَ ساعة. و`created_at` المستقبليُّ = **صفر**.

---

## ④ حالةُ سلسلةِ RJ-01 — أين تنقطع فعلًا

| المقطع | الحيُّ | الحكم |
|---|---|---|
| **DEP-01** تجاري | clients 91 · project 125 · opportunities 23 · quotations 141 · contracts 121 · commitments 147 | ✅ **ممارَس** |
| **DEP-02** موردون | suppliers 74 · supplier_contracts 20 · rfqs 3 · quotes 1 · awards 1 | ⚠️ **رقيق** |
| **DEP-02→04** الحصة→الوحدة | `sup_quota_supplier_unit` **0** · `sup_allocation_unit_equipment` **0** | 🔴 **انقطاعٌ بنيويٌّ أوّل** |
| **DEP-04** أسطول | equipments 219 · asset_use_right 70 · asset_readiness 320 · equipment_operators 377 | ✅ ممارَس |
| **DEP-11** تشغيل | timesheet 654 · unit_entries 542 · unit_approvals 976 · `site_day` **0** | ⚠️ يعمل بمسارٍ لا بمسارَين |
| **DEP-05** مطالبة→فاتورة | claims 300 · claim_lines 345 · `ar_claim_invoices` **0** · tax_invoices 285 | 🔴 **انقطاعٌ بنيويٌّ ثانٍ** — الطرفانِ مملوءانِ والجسرُ فارغ |
| **DEP-05/06** ذمة→نقد | fin_receivables 286 · fin_payments 1,118 · allocations 214 | ✅ ممارَس |

### وأربعةُ مخطَّطاتٍ متوازيةٍ راكدة (`LEGACY_PARALLEL`)

| الحيّ | المتوازي الراكد |
|---|---|
| `quotations` 141 | `sal_quotations` **0** |
| `claims` 300 | `sal_claims` **0** |
| `timesheet` 654 | `ops_timesheet` **0** |
| `supplier_contracts` 20 | `supplierscontracts` 20 |

(و`client_contracts` **رؤيةٌ** لا جدولٌ متوازٍ — لا تُحسب.)

---

## ⑤ خطّةُ التنفيذِ المصحَّحة

### الدفعةُ صفر — السلامة
| # | البند | الحال |
|---|---|---|
| 0.1 | دفعُ 455 التزامًا + وسمٌ + استعادةٌ من نسخةٍ جديدة | 🔴 **بيدك — SSH بعبارةِ مرور** |
| 0.2 | لقطةُ قاعدةٍ ذرّيّة | لي |
| 0.3 | تثبيتُ `HISTORICAL_AUTO_REPLAY = OFF` بدليل | لي |

### الدفعةُ الأولى — إغلاقُ العمودِ الفقريّ *(ما يبقى قائمًا بعدَ التصحيح)*
| # | البند | ملاحظة |
|---|---|---|
| 1 | ~~إصلاحُ الساعة~~ | **ساقط** — الفرق 0 |
| 2 | تثبيتُ الساعةِ بحاجبٍ يقيسها كلَّ تشغيل | يبقى — منعُ الارتداد |
| 3 | حسمُ `DLQ_SOURCE_OF_TRUTH = 1` | إعلانُ `ems_event_deliveries.state` حاكمًا · إحالةُ `ems_event_dead_letter` رؤيةً أو تقاعدًا |
| 4 | إغلاقُ **GAP-11** بتمريرِ `navarch_renderer` عبرَ نقطةِ القرارِ المركزيّة | **بلا استثناءِ ملفٍّ من الحاجب** |
| 5 | إعادةُ مصالحةِ `EVENT_PURPOSE_RECONCILIATION` (58) | الأحكامُ متقادمةٌ 14 يومًا |
| 6 | ~~تتبّعُ EffectFanout~~ | **ساقط** — `NOT_CAUSAL` بسببٍ مكتوب · يُوثَّق ويُغلق |
| 7 | حكمُ الأحدَ عشرَ `SOURCE_UNRESOLVABLE` في `DELIVERY_RETRY_DISPOSITION` | ⚠️ **قرارُ مالكٍ** — أدناه |
| 8 | ~~Canary + Safe Retry Batch~~ | **ساقط** — `SAFE_RETRY` فارغةٌ بالقياس |
| 9 | `IDEMPOTENCY_COVERAGE_AUDIT` للمستهلكين النشِطين | يبقى |
| 10 | إعادةُ القياسِ كاملًا بالمِسبارِ نفسِه | يبقى |

### الدفعةُ الثانية — عقودُ RJ-01
تُبنى على الانقطاعَين المقيسَين وحدَهما:
**INT-03** الحصة→الوحدة→المعدة · **INT-08** المطالبة→الفاتورة.
وباقي الحلقاتِ **قائمةٌ وممارَسة** — تُوثَّق ولا تُبنى.

---

## ⑥ ثلاثةُ قراراتٍ لا أحسمُها وحدي

1. **الدفعُ إلى `origin`** — SSH بعبارةِ مرور. بيدك، وهو حاجبُ الدفعةِ صفر.
2. **مصيرُ الأحدَ عشرَ يتيمةِ المصدر** — ثلاثةُ خيارات: حكمٌ مكتوبٌ وإبقاءٌ في الـDLQ (الأسلم) · أو كنسٌ محكومٌ بأثرٍ · أو إعادةُ بناءِ الآباءِ المحذوفين. **قرارُ بياناتٍ تاريخيّةٍ = مالك.**
3. **شاهدُ RJ-01** — معاملةٌ حقيقيّةٌ محكومةٌ أم معاملةُ UAT معزولة؟

---

*تمّت الدراسة. لا كتابةَ على المخزنِ حتى الآن — القياسُ قراءةٌ محضة.*
