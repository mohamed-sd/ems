# سجلُّ الاجتهادات — update0005 · CAP-01

| # | الموجة | المهمة | السؤال | الدرجة | ما قررتَه | لماذا | ما يترتب لو كان القرارُ خطأً |
|---|---|---|---|---|---|---|---|
| J-01 | ① | CAP-01 | `measure_code` — أيربط بـ`units_of_measure` أم يكون ENUM؟ | ③ | `ENUM('hour','ton','trip','meter')` نصًّا — مرآةُ `supplier_contract_lines.work_model` القائم | `units_of_measure` قاموسُ شركةٍ بأكواد محلية (`UOM-1001`…) لا مقياسُ نظامٍ عابرٌ للشركات؛ و§16 يسمّي الأربعة نصًّا؛ والنمطُ المستقرُّ (work_model · unit_type) ENUM | لو أراد المالكُ مقاييسَ تُدار صفوفًا لزم توسيعُ ENUM بهجرةٍ لكل مقياسٍ جديد — كلفةُ هجرةٍ لا كلفةُ بنية |
| J-02 | ① | CAP-01 | «على contract_commitments وop_containers بحسب الدرجة» — أيُّ الحقول أين؟ | ③ | السياسةُ السبعةُ كلُّها على `contract_commitments` (بيتُ العقد) · وعلى درجة «نوع» في `op_containers` الأعدادُ الثلاثةُ وحدَها (أساسيٌّ · مطلوبٌ · مسموح) | قاعدةُ §5-⑦ «المصدرُ الواحد»: تكرارُ السياسة (المقابلُ · قاعدةُ التفعيل · المعالجة) في الشجرة يفتح مصدرين للحقيقة؛ والشجرةُ تحتاج الأعدادَ لفرض Σ وسقفِ الاحتياطي لا السياسة | لو احتاجت الشجرةُ السياسةَ محليًّا لزم JOIN إلى الالتزام — كلفةُ قراءةٍ لا خطرُ تفارُق |
| J-03 | ① | CAP-03/04 | ما «تسجيلُ الاحتياطي» الذي يُعدُّ على السقف؟ | ③ | صفوفُ `seat_assignments` الحيّةُ (state=active) بدور «احتياطي» على البند — والمجموعُ عبر بنود الالتزام الواحد | خطةُ §8.3 بيتُها `seat_assignments` (DEC-CAP-C: لا `supplier_equipment_plan` باسمها)؛ والدورُ فيها ENUM قائم؛ والعدُّ من السجل الحي لا من عدادٍ مخزَّن (عقيدةُ §13) | لو قصد المالكُ عدَّ التسجيل من جدولٍ آخر (خطة موردٍ مستقلة) لعدَّ السقفُ صفوفًا أقل — الحارسُ يبقى fail-closed فلا بابَ يُفتح |
| J-04 | ① | CAP-06 | أين تُسجَّل 422 حارسِ DEC-CAP-A؟ | ③ | `ActivityLogService::log` بـ`action_type=blocked · module_name=capacity` — والتسجيلُ لا يُسقط الحكم إن فشل | خدمةُ التسجيل المركزيةُ القائمةُ للجميع؛ ولا جدولَ رفضاتٍ مخصصًا في CAP-01؛ والحكمُ (422) أسبقُ من أثره الرقابي | لو أراد المالكُ سجلًّا محاسبيًّا مستقلًّا للرفضات نُقلت الأسطرُ منه — البياناتُ محفوظةٌ بطابعها الزمني |
| J-05 | ① | البروتوكول | قالبُ الالتزام يذكر «Claude Opus 5» والموديلُ المنفِّذ Fable 5 | ③ | التوقيعُ باسم الموديل الفعلي `Claude Fable 5` | الصدقُ في النسبة أولى من حرفية قالبٍ كُتب لموديلٍ سابق (نمطُ update0004) | لا أثرَ وظيفيًّا — سطرُ توثيقٍ في رسائل git |
| J-06 | ② | CAP-07 | كيف يُنفَّذ «Insert-only لا تعديلَ ولا حذف» بنيويًّا؟ | ③ | نمطُ `immutable_key` القائمُ في TenantRegistry (كحصانة `ems_business_events`) على عمودٍ لا يفرغ (`effect_type`) — لا triggers | القاعدةُ صفرُ triggers اليوم (مقيسة)؛ وimmutable_key هو عُرفُ الحصانة المستقر؛ والكاتبُ الخام خارج البوابة محكومٌ بعُرف «الحارسُ في الخدمة» الساري على كل الجداول | كاتبٌ خامٌ يلتفّ على البوابة يستطيع UPDATE — وهي ثغرةٌ عامةٌ في كل دفاتر النظام لا خاصةٌ بهذا الدفتر؛ إغلاقُها الكامل يستلزم صلاحياتِ مستخدمِ قاعدةٍ (مؤجَّلُ REV-02: app-DB-user) |
| J-07 | ② | CAP-09 | «القائمةُ المحكومة» للأسباب — ما قيمُها؟ | ① نصًّا + ③ ترميزًا | الخمسةُ من §6.1-① حرفيًّا: عطل=breakdown · صيانةٌ مجدولة=scheduled_maintenance · خروجٌ للترحيل=relocation_exit · وثيقةٌ منتهية=document_expired · نقصُ مشغّل=operator_shortage | النصُّ يسمّي الخمسة؛ والترميزُ الإنجليزيُّ للقيم عُرفُ ENUMات النظام الجديدة (state·level·effect_type) والعرضُ عربيٌّ في الشاشة | لو أراد المالكُ سببًا سادسًا فتوسيعُ ENUM هجرةٌ صغيرة — والقائمةُ «محكومةٌ» بالتصميم فضيقُها مقصود |
| J-08 | ③ | CAP-14 | استهلاكٌ بلا سجلِّ وحدةٍ مرقَّم (source_kind=manual أو مقياسٌ خارج الأربعة) — أيُلفَّق له سطرُ دفتر؟ | ④ | لا يُلفَّق — يُخصم العمودُ (السلوكُ القائم) ويُترك للظل يرصده فرقًا معلَنًا | مفتاحُ الدفتر يوجب سجلَّ وحدةٍ ونسخة (§13.2)، واختراعُ سجلٍّ وهميٍّ نقضٌ لقاعدة عدم التلفيق؛ والظلُّ وُجد ليكشف هذا بالضبط | استهلاكاتٌ يدويةٌ كثيرةٌ تؤخر أهليةَ القلب إلى ledger — وهذا صحيحٌ بالتصميم: القلبُ لا يصحّ ودفترٌ ناقص |
| J-09 | ③ | CAP-16 | «منعُ تداخل فترتين فعّالتين لمقعد» — فهرسُ مدًى مستحيلٌ في MySQL | ④ | القيدُ البنيويُّ على الشائع: تخصيصٌ مفتوحٌ فعّالٌ واحدٌ لكل مقعد (عمودٌ مولَّدٌ فريد) · والتداخلُ المدَّدُ يبقى بحارس `ContractSeatService` القائم (409) | MySQL بلا exclusion constraints؛ والفريدُ المشروطُ يقفل الحالةَ الغالبة (تخصيصان مفتوحان) بنيويًّا؛ والخدمةُ تحرس البقية — «قيدٌ في القاعدة» بأقصى الممكن لا بأكثر منه | تداخلٌ مدَّدٌ يكتبه كاتبٌ خامٌ يفلت من الفهرس — يلتقطه حزامُ C4 وحارسُ الخدمة على كل مسارٍ رسمي |
| J-10 | ④ | CAP-20 | أين تُخزَّن موافقاتُ درجات التغطية؟ | ③ | `approvals_json` على الصف ({role:{by,at}}) بمصفوفة أدوارٍ ثابتةٍ في الخدمة — لا جدولُ موافقاتٍ جديد | صندوقُ الاعتماد القائم (EMS_APPROVAL_BOX) مبنيٌّ لسلاسل الوحدات لا للتغطيات؛ وjson مرجعيٌّ يكفي ثلاثةَ أدوارٍ بطابعها الزمني؛ وDEC-CAP-C يضيّق الجداولَ الجديدةَ على الأربعة المسماة | لو أراد المالكُ إدماجَ التغطيات في صندوق الاعتماد الجامع (§15.2) رُحّلت الأدوارُ من json إلى سلسلةٍ — البياناتُ كاملةٌ بطابعها |
| J-11 | ④ | CAP-21 | «مهلتُها المعلنة» للتصعيد — كم؟ | ④ | عمودٌ `escalate_after_days` افتراضُه 3 أيامٍ قابلٌ للضبط لكل فجوةٍ — والتصعيدُ الثاني بضعفها | CAP-01 لا يسمّي مدةً؛ والعمودُ يجعلها «معلنةً» قابلةً للحكم لا ثابتةً مدفونة؛ و3 أيامٍ دون أسبوع العمل فلا فجوةَ تعبر أسبوعًا صامتة | لو أراد المالكُ مهلةً أخرى فتحديثُ عمودٍ لا كود — وتُعرض للتوقيع في التقرير |
| J-12 | ④ | CAP-25 | ما «إقفالُ الشهر» الذي يحجب تعديلَ الحصة؟ | ③ | إقفالُ n12 القائم: وجودُ `monthly_performance.state='closed'` لفترةٍ استُهلك فيها من البند → 423 على تعديل كمياته | n12 هو سجلُّ الأداء الشهري المعتمدُ في النظام (مقعدٌ × شهر) وإقفالُه هو الإقفالُ التشغيليُّ الوحيدُ القائم؛ ولا جدولَ إقفالٍ آخر | لو قصد المالكُ إقفالًا ماليًّا (فترات FES) وسّعنا الشرطَ بOR — والحارسُ الحاليُّ أضيقُ فأسلم (fail-closed) |
| J-13 | ⑤ | CAP-29 | «تسجيلُ الأحداث في FES-01» — لا قاموسَ أحداثٍ جدوليًّا في النظام | ③ | التسجيلُ ثلاثيًّا: ثوابتُ `CapacityEvents` (القاموسُ البرمجي) + `source_module=capacity` في قائمة عقد الناشر §9 (إضافةُ قيمةٍ لقائمةٍ لا مساسَ بالبنية) + بطاقةُ مواصفةٍ في docs/specs | FES-01 يسجّل الأحداثَ في «القاموس» ولا جدولَ باسمه؛ وfin_effect_map خريطةُ المروحة المحرَّمةُ بDEC-CAP-B؛ والعرفُ القائم: مفاتيحُ الأحداث ثوابتُ خدماتٍ وقوائمُ الناشر | لو أنشأ المالكُ جدولَ قاموسٍ لاحقًا (FES-01 v2) بُذرت الستةُ منه صفوفًا — المفاتيحُ مستقرةٌ لن تتغير |
| J-14 | ⑤ | CAP-27 | قلبُ `EMS_CAP_OUTBOX` إلى on — أهو من المؤجَّلات؟ | ② نصًّا | قُلب on فورَ خضرة C28 | حالاتُ التأجيل الستُّ تخص `enforce` والخطةُ §4 تنصّ حرفيًّا: «EMS_CAP_OUTBOX: بعد CAP-27 وخضرةِ C28» — والشرطُ تحقق في الحزام | لو أراده المالكُ مطفأً فسطرٌ في .env — والعاملُ لا-عمليةَ موثَّقةً عندها |
| J-15 | ⑥ | CAP-34 | تعارضٌ: H-01 المنفَّذ «الاستهلاكُ عند اعتماد الموقع» × CAP-01 §12 «لا يُستهلك إلا باكتمال سلسلة الاعتماد» | ① | CAP-01 تحكم — نُقل الاستدعاءُ إلى `sales_approved` وحُدِّث شاهدُ الحزام القديم | سلّمُ §1: CAP-01 أولُ المصادر وأخصُّها وأحدثُها، وتسمّي حكمَها «التصحيحَ الحاكم» صراحةً — فالقاعدةُ القديمةُ منسوخةٌ نصًّا لا اجتهادًا؛ وتحديثُ الاختبار من عُرف «إن كان الاختبارُ نفسُه خاطئًا فأصلحه وسجّل» | لو قصد المالكُ إبقاءَ خصم الموقع لبعض المواقع فالفرعُ شرطُ حالةٍ واحد — والعطالةُ بمفتاح الجولة تحمي الحالتين من الازدواج |
| J-16 | ⑥ | CAP-32 | أيُّ علَمٍ يحكم منعَ فتح التايم شيت بالتخصيصات الناقصة؟ | ③ | قائمةُ مواقع `EMS_CONTAINER_GATE` نفسُها بوضعها (enforce/monitor) — لا علَمَ جديدًا | الحارسان أخوان على البوابة نفسِها (اكتمالُ التوزيع شرطُ التسجيل)؛ والعُرفُ المقيس: قائمةُ مواقعَ لا قلبةً على الجميع (فخُّ E-08: 138/138)؛ وعلمٌ جديدٌ لكل حارسٍ تضخمٌ إعداديّ | لو أراد المالكُ فصلَهما أُضيف `EMS_CAP_CONTEXT_GATE` وسطرُ قراءةٍ واحد |
| J-17 | ⑦ | CAP-35 | «تبويبٌ داخل ملف العقد» وملفُّ العقد «شارةٌ مفردةٌ بلا تبويبات» (قرارُ مالكٍ 2026-07-28) | ③ | شاشةٌ فرعيةٌ `contract_coverage.php` تُفتح برابطٍ بارزٍ من ملف العقد — لا هيكلةَ تبويباتٍ داخل الشاشة الأم | عُرفُ ملف العقد القائم: كلُّ تفصيلةٍ شاشةٌ فرعية (contract_lines · contract_sites · …)؛ وإقحامُ تبويباتٍ ينقض قرارَ الشارة المفردة؛ والرحلةُ «التغطيةُ ← الموردون ← المعدات» كاملةٌ داخل الشاشة الفرعية | لو أراد المالكُ تبويبًا حرفيًّا داخل الشاشة الأم فالنقلُ include وتفعيلُ حاوية تبويب — والمحتوى جاهزٌ كما هو |
