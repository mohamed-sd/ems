# FINDINGS — ما اكتُشف أثناء استخراج `BL-20260828c-96a640e1+WT` (سُجّل ولم يُصلَح)

> **اللقطة:** `BL-20260828c-96a640e1+WT` · القياس: 2026-08-28 · 21:25→22:10 · التفصيل في [SNAPSHOT.md](SNAPSHOT.md)
> **قاعدة الجولة محفوظة:** «استخراج الحقيقة أولًا — لا إصلاح أثناء القياس». **صفرُ تعديلٍ على كودِ النظامِ أو بياناته** — ولا حتى على الأربعةِ الصفوفِ المرتدَّة، **وهي أوّلُ ما تُغري باليد**: تصحيحُها بالاستعلامِ يمحو أثرَ العطبِ ويُبقي سببَه.
> سجلّات اللقطات الستِّ السابقة في [`historical/`](historical/).

## أولًا — اكتشافات هذه الجولة

| # | الاكتشاف | Evidence | GAP |
|---|---|---|---|
| **F-G01** | **جولةٌ كاملةٌ وقعت خارجَ أيِّ التزام**: `HEAD` هو `96a640e1` نفسُه واللقطةُ السابقةُ قِيست على شجرةٍ نظيفة — واليومَ **٥٦ بندًا** — **٣٩ منها من الجولةِ و١٧ من مخرجاتي، والقسمةُ تُذكر** (تسعُ أدواتٍ `rpr02a_*` · ستُّ وثائقَ مولَّدة · **١١ ملفَّ حزمةٍ مصدريّةٍ مستبدَل** · و`repair01_ingest.php` معدَّل). ⇒ **ما قِسته لا يُعاد إنتاجُه من الالتزام** — ولهذا حملت اللقطةُ اللاحقةَ `+WT` | `git status --porcelain` = 56 | GAP-70 |
| **F-G02** | **العلاجُ بلا حاجبٍ يرتدُّ — والدليلُ مقارنةٌ داخلَ الحزمةِ نفسِها**: نظافةُ الشجرةِ عولجت في اللقطةِ السابقةِ **بفعلٍ** (التزامٍ واحد) فارتدَّت في **أوّلِ** جولةٍ تالية؛ ودفترُ الهجراتِ عولج **بحاجبٍ** فصمَد وهو أخضرُ اليوم **4/4** بمقاماتٍ مطبوعة. ⇒ **الفرقُ ليس في جودةِ الفعلِ بل في وجودِ الحارس** | `git status` × `repair01_migration_gate.php` | GAP-70 |
| **F-G03** | **حكمٌ واحدٌ راسبٌ أسقط ثلاثَ فجواتٍ دفعةً واحدة**: البصمةُ المحسوبةُ لا تطابق المسجَّلةَ (`4099c94…` مقابل `4e73fa5d…`) — والفاحصُ الذي يحملها **يصرّح بـ`GAP-14` و`GAP-26` و`GAP-32`** معًا. ⇒ **التراجعُ ليس ثلاثَ انتكاساتٍ بل واحدةً تُحسَب ثلاثًا** | `injfix01_release_and_action_finality_proof.php` | GAP-14/26/32 |
| **F-G04** | **السجلُّ الرسميُّ يناقض قاعدتَه المكتوبة — والمقياسُ سؤالٌ واحد**: أربعةُ صفوفٍ `verdict_rule` فيها «RPR-W15 §٤·٢ · **إعادةُ ملكيةٍ من مكتبِ الرئيسِ إلى إدارتِها**» و`owner_code` فيها **`EX-CEO`** (`SCR-0505` DEP-05⇒EX-CEO · `SCR-0508` DEP-11⇒EX-CEO · `SCR-0515` DEP-05⇒EX-CEO · `SCR-0532` DEP-09⇒EX-CEO). **القاعدةُ في 11 صفًّا · 7 تُطيعها · 4 تناقضها.** وفي `SCR-0532` دُهس `owner_rule` من `W01_SURFACE_REGISTER` إلى `W10_SPLIT:W10_PARENT_DEFAULT` — **وهو العطبُ المسجَّلُ سلفًا: الاستيعابُ يمحو أحكامَ W01** | مقارنةُ السجلِّ بلقطتِه السابقة صفًّا بصفّ | **GAP-73** |
| **F-G05** | **وقواعدُ الحكمِ بُترت مع الارتداد**: `verdict_rule` في الأربعةِ فقد ذيولَه (مرجعَ التسميةِ وسياسةَ المنحِ الحيِّ وحكمَ المالك) فبقي صدرُه وحده. ⇒ **الارتدادُ لم يُبدِّل القيمةَ فقط بل قلَّص أثرَ الإثبات** | مقارنة | GAP-73 |
| **F-G06** | **الطوابعُ تسمّي النافذة**: الصفوفُ الاثنان والخمسون تحمل `verdict_at`=**19:07:29** و`updated_at`=**19:09:04**، و`tools/repair01_ingest.php` حُفظ **19:08:55**، و`rpr02a_reingest.php` شُغِّل **19:33**. ⇒ **التقريرُ الذي أعلن Δ=0 كُتب بعد وقوعِ التغيير بأربعٍ وعشرين دقيقة** | طوابعُ الجدولِ وأزمنةُ الملفات | GAP-74 |
| **F-G07** | **«صفرُ فرقٍ» يصدُق على المقامِ ويكذب على السؤال**: `RPR02A_REINGEST.md` يعرض **تسعةَ عشرَ مقياسًا كلُّها Δ=0** — وهو **صادقٌ فيما عدَّ** (`source_files` · `decisions` · `departments` · `crosswalk` · `surfaces` · `target_gaps` · `requirements` · `fields`) — **ولا يعُدُّ `repair01_screen_registry` أصلًا**، وهو الجدولُ الذي تغيّر فيه 52 صفًّا | مقارنةٌ حيّة | **GAP-74** |
| **F-G08** | **`GAP-18` لم يكن مُغلقًا — وهذا تصحيحٌ على حكمي أنا**: حكمُه ذو شقَّين «الدفترُ يطابق القرصَ **وبصمةُ المخطَّطِ تُعاد إنتاجُها من استنساخٍ نظيف**». الشقُّ الأوّلُ ✔ (البوابةُ 4/4) · **والثاني ✘**: ثمانيةُ جداولَ حيّةٍ غائبةٌ عن الاستنساخ (`acc_account_recon` · `acc_credit_limit` · `acc_invoice_line` …) و**فرقُ أعمدةٍ 3,434**. ⇒ **أغلقتُه على شقٍّ واحدٍ من شقَّين** | `injfix01_clean_clone_fingerprint_proof.php` | GAP-18 |
| **F-G09** | **`GAP-10` (P0) عاد مفتوحًا بمسربٍ واحدٍ مسمّى**: القناةُ ⑤ (المناظرُ المحفوظة) — **8 ملفاتِ قناةٍ · 1 يمسُّ حقلًا حسّاسًا · 0 يمرُّ بالمقرِّر · إعفاءٌ مُعلَنٌ 0 · مفتوحةٌ 1**. والفاحصُ يرفض إعلانَ «صفرِ تسريب» ما دام رقمٌ غيرَ صفر — **وهي فضيلةُ أداةٍ لا عطبُها** | `injfix01_sensitive_fields_nine_channels_proof.php` | GAP-10 |
| **F-G10** | **التراجعُ 27/33 ⇒ 22/33 سببُه ثلاثةٌ لا خمسة**: الأداةُ تُشغِّل كلَّ شاهدٍ و**رمزُ خروجٍ غيرُ صفريٍّ يقلب التصريحَ إلى OPEN**. والشواهدُ الحمراءُ **4 من 34** تفسّر الخمسةَ كلَّها. **والحاسمُ**: `git status` يُظهر أنّ **لا ملفَّ في `tests/` أو `app/` أو `includes/` أو `database/` تغيّر** ⇒ **الشواهدُ نفسُها لم تتبدّل، فالتغيُّرُ في المقيسِ لا في المقياس** | تشغيلُ الشواهدِ الأربعة + `git status` | GAP-10/14/18/26/32 |
| **F-G11** | ✔ **`GAP-68` أُغلق**: `PLATFORM` صار صفًّا في `repair01_departments` (**21 ⇒ 22**) فصار لمالكِ الاثنتَي عشرةَ شاشةً **اسمٌ لا رمزُ قرار** | استعلام حي | GAP-68 |
| **F-G12** | **المستهلكون: الطرفان يكبران ولا يلتقيان**: **58/58** مفتاحًا منتَجًا `handler_class` فيه `GovernanceWatchConsumer` — **حارسٌ لا مستهلكُ أعمال**. وفي المقابل **الاشتراكاتُ الفعّالةُ غيرُ الحارسِ نمت 4 ⇒ 32** (`FinancingCycleService` 10 · `AccountingCycleService` 9 · `SplitProjectionConsumer` 5 · `TreasuryCycleService` 4 · وأربعٌ) — **وكلُّ الاثنتين والثلاثين على مفاتيحَ لا تُنتَج ولا مرّةً**. ⇒ **58 مفتاحًا يُنتَج · 102 مشترَكٌ عليه · التقاطعُ صفر** | `ems_event_subscriptions` × `ems_business_events` × `gov_event_rulings` | GAP-05 |
| **F-G13** | **`in_projection=1` في 11 صفًّا ليس مستهلكًا**: `gov_event_rulings` يسم أحدَ عشرَ حدثًا `business` ومعها `in_projection=1` — **وهي صفةُ حكمٍ لا اشتراكٌ حيّ**؛ ولو قُرئت مقياسًا لأعطت «11/58 موصولة» وهي صفر. **وسّمتُها هنا كي لا تُقرأ خطأً** | مقارنةٌ بالحقلَين | GAP-05 |
| **F-G14** | **سجلُّ المتطلباتِ اتّسع فنزلت النسبةُ بلا تراجعِ عمل**: **61 ⇒ 66** بخمسةِ مطالبَ جديدةٍ (`FR-GOV-007..011` في `CHG-RPR-0203`، منها **P0 واحد**) **كلُّها `OPEN`**. البسطُ ثابتٌ عند 26 ⇒ **42.6٪ ⇒ 39.4٪**. و«صفرُ مفتوحٍ» في اللقطةِ السابقةِ **لم يعد صادقًا** | `tools/injfrd01_status.php` | — |
| **F-G15** | **حاجبٌ مُثبَتٌ ≠ مخزنٌ نظيف — وهو فصلٌ أحسنته هذه الجولة**: `rpr02a_gates.php` يفصل الحكمَين صراحةً: `G-DOC-01` **مُثبَتٌ** (17→18→17) **ومخزنُه مخروقٌ بـ17** · `G-PLT-01` **مُثبَتٌ** (2→3→2) **ومخزنُه مخروقٌ بـ2** · `G-UNI-01` مُثبَتٌ ونظيف. ⇒ **وخلطُهما يجعل حاجبًا سليمًا يبدو راسبًا** | `RPR02A_GATES.md` + منطقُ الأداة | — |
| **F-G16** | **عجزُ السايدبارِ صار بمقامٍ مُعلَنٍ لأوّلِ مرّة**: **395 بطاقةً − 21 سطحَ توثيقٍ = 374 مؤهَّلًا** · حاضرٌ **111** · **غائبٌ 263 (70.3٪)** بثلاثِ سلال: (أ) **11** مبنيٌّ ومسجَّلٌ ينقصه رابطٌ — عملُ دقائق · (ب) **4** مبنيٌّ غيرُ مسجَّل · (ج؟) **60 يُحسَم بيد** · (ج) **188** غيرُ مبنيّ. **واستبعادُ أسطحِ التوثيقِ من المقامِ قبلَ القسمةِ هو الصوابُ بعينِه** | `RPR02A_TRIAGE.md` | — |
| **F-G17** | **مقامان لسؤالٍ واحدٍ — مرّتان في لقطةٍ واحدة**: ① جداولُ `company_id`: **657 من 953** بعدِّ الجداولِ الأساسيةِ وحدَها · **671** بعدِّ المناظرِ معها (فرقُ **14 منظرًا**) ② الذهبية: **10/10** تجتاز التسعَ الموضوعية (`injfix01_golden_promotion_gate`) بينما `uxui_golden_gates` يرصد **مخالفتَين** في `FinRequests/request_form.php`. **كلاهما صادقٌ على مقامِه** — وسمّيتُ المقامَين في الوثائق | استعلامان × أداتان | — |
| **F-G18** | **ما لم يتغيّر — وهو أكثرُ الحزمة**: الجداول **953** · CHECK **831** · التنقّل **2096/1768/637** · الصلاحيات **3757** · الأحداث **21,546** بـ**58** مفتاحًا · القيود **6,713/13,426** بفرق **0.00** · الظهورات **1200** · التسويات **469 بصفرٍ مدفوع** · `EMS_LADDER_GATE` غيرُ معلَن · `gov_ladder_shadow` **صفر** · `fx` عند **6,613** منذ **2026-08-12**. **والطابور 15,848 ⇒ 16,208** وحدَه تحرّك | قياس حي | — |
| **F-G19** | **`PHP` يكتب طوابعَه بـUTC وساعةُ النظامِ UTC+03** — فرقُ **ثلاثِ ساعات**: مخرجُ أداةِ التفريغِ قال «تم 18:30» والساعةُ 21:30. ⇒ **أيُّ ترتيبٍ زمنيٍّ يخلط مصدرَي الطوابعِ يكذب** | مقارنةُ `date` بمخرجِ الأداة | GAP-71 |
| **F-G20** | **`GAP-67` باقٍ**: `SCR-0626`/`SCR-0627` ما زالتا `LIVE_REGISTERED` وهما `vendor/phpoffice/.../Payments.php` و`Depreciation.php` | استعلام | GAP-67 |

## ثانيًا — تصحيحات على قياسِ الجولةِ السابقة

| # | التصحيح | لماذا |
|---|---|---|
| **C-01** | ⛔ **`GAP-18` لم يكن مُغلقًا** — أغلقتُه على شقِّ الدفترِ وحدَه وحكمُه ذو شقَّين. **والبوابةُ التي مدحتُها ما زالت خضراءَ 4/4 وصادقةً فيما تفحص** — الخطأُ في مدى ما استنتجتُه منها، لا فيها | خطأُ قراءةٍ عندي |
| **C-02** | **«Deployment readiness ✔» و«Remote reproducibility ◐» سقطتا** — لا لتراجعِ الدفترةِ بل لأنّ الشجرةَ خرجت عن الالتزامِ والاستنساخَ النظيفَ راسبٌ قياسًا | تغيّرُ الحالة |
| **C-03** | **«صفرُ مطلبٍ مفتوح» لم يعد صادقًا** — خمسةُ مطالبَ جديدةٍ `OPEN` ومقامُ السجلِّ 61 ⇒ **66** | نموُّ المقام |
| **C-04** | **«27/33» صارت «22/33»** — وليست خمسَ انتكاساتٍ بل **ثلاثةَ أسبابٍ** يفسّرها **أربعةُ شواهدَ حمراءَ من 34** | مقامٌ ثابتٌ وبسطٌ نزل |
| **C-05** | **صفُّ «سجل المتطلبات» كان قد سقط من §١ في اللقطةِ السابقة** بفعلِ تحريرٍ منّي أزاح صفًّا وكرّر آخرَ — **أُعيد الصفُّ وحُذف المكرَّر**؛ وكذلك صفٌّ مكرَّرٌ في §٩ وصفَّان مفقودان في §٣ (بوابةُ المستأجرِ وبوابةُ السلّم) | عطبُ تحريرٍ عندي |
| **C-06** | **«ستةَ عشرَ يومًا» لـ`fx` تبقى** — القياسُ في اليومِ نفسِه، فلا تُقرأ سبعةَ عشرَ | ثباتُ التاريخ |

## ثالثًا — إصلاحات P0 أثناء القياس

**لا شيء في النظام — ولا في عُدّتي هذه المرّة.**

والمرشَّحُ الأوّلُ للإصلاحِ الفوريِّ كان `GAP-73` (ارتدادُ الملكيةِ الرباعيّ)، **وتركتُه عمدًا**: تصحيحُ أربعِ قيمٍ باستعلامٍ **يمحو أثرَ العطبِ ويُبقي سببَه** — وهو أنّ الاستيعابَ يكتب فوقَ أحكامِ الموجاتِ اللاحقةِ بلا حاجبٍ يمنعه. **والعلاجُ الصحيحُ حاجبٌ لا استعلام**، وهو قرارُ مالكٍ لا قرارُ قائس.

والمانعُ الأوّلُ اليومَ **فعلُ مالكٍ أيضًا**: التزِمْ ما في الشجرةِ أو انزعْه — فيعود Deployment وتُصلَح البصمةُ وتُغلَق ثلاثُ فجواتٍ بحكمٍ واحد.
