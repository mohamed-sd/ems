# FINDINGS — ما اكتُشف أثناء استخراج BL-20260823 (سُجّل ولم يُصلَح)

> **قاعدة الجولة محفوظة:** «استخراج الحقيقة أولًا — لا إصلاح أثناء القياس». **صفر تعديل على كود النظام أو بياناته أو عُدّة القياس** في هذه الجولة.
> سجلّات اللقطات الثلاث السابقة في [`historical/`](historical/) — **ما وُسم هناك «مفتوحًا» لا يُقرأ اليوم دون مراجعة §4 من INJ-CURRENT-STATUS**.

## أولًا — اكتشافات هذه الجولة

| # | الاكتشاف | Evidence | GAP/FR |
|---|---|---|---|
| **F-D01** | **ثلاث فجوات كانت تُعلَن مغلقةً وهي مفتوحة — أُغلقت فعليًّا هذه المرة.** `GAP-14`: `$verb` صار يُعرَّف في `action_guard.php:218` **قبل** استعماله في `:240`، وأُضيف عمودا **`verb`** و**`verb_state`** إلى `guard_denials` بقيد `chk_denial_verb_present` يميّز `PRE_VERB` (رفض يسبق حلّ الفعل مشروعًا) من `REQUIRED` — **5,354 مقابل 351**. `GAP-22`: `fin_party_scope` صار يقرأ `fin_party_scope_registry` ويعيد `PARTY_SCOPE_NONE` للدور ≤ 0 بدل `null`. `GAP-60`: حظر الصيغ نُفِّذ في المصدر بهجرة ذات revert مُجرَّب | `SHOW COLUMNS` · `CHECK_CONSTRAINTS` · قراءة الكود | GAP-14/22/60 |
| **F-D02** | **المانع الأول لم يُرفع — بل قِيس ثم أُزيل سببه، والإعلان بقي محجوزًا بنصاب معلن.** إنفاذ السلّم كان سيمنع **31 من 76 مستخدمًا** من كل اعتماد بعطب قراءة (`users.role_id` فارغ) لا بقاعدة حوكمة — ومنهم **الحساب الوحيد لإدارة الموردين** الذي يقوم عليه معيار `XC-05` نفسه، «فكان الإنفاذُ سيُغلق الإدارةَ التي فتحناها». أُصلح بدالّة `ems_resolve_actor_role` في ثلاثة مواضع حيّة بشاهد صفر انحراف. **و`EMS_LADDER_GATE` ما زال غير معلَن لأن `gov_ladder_shadow` صفر صف** (0 من 500 قرار · 0 من 7 أيام) | `96e36541` · `.env` · استعلام | GAP-59/63 · `FR-APP-002/004` |
| **F-D03** | **سجل متطلبات بمقام خارجيّ لأول مرة**: 61 مطلبًا في ثماني حزم، **صفر مفتوح في كل حزمة**، وأربع حالات إغلاق منفصلة: **26 `EVIDENCE_CLOSED` · 21 `IMPLEMENTED_NOT_CLOSED` · 13 `BLOCKED_OWNER_DECISION` · 1 `BLOCKED_GOVERNING_SOURCE`**. والتقرير **مشتقّ حيًّا لا مكتوب بيد** | `tools/injfrd01_status.php` · `CLOSURE_REPORT` | CAP-31 |
| **F-D04** | **المحطات المبنيّة ما زالت صفر صف عبر لقطتين**: `ar_claim_invoices` · `ar_completion_certs` · `ar_accruals` · `tre_pay_batches` · `sal_quotation_lines` · `sup_violations` · `unit_final_approvals` = **صفر**. و`gov_ladder_decisions` تحرّك من 0 إلى **2** فقط. والسبب مسمّى: `FR-JRN-001` **يلزمه منفِّذ بشريّ بحساب حقيقي، والبذرة لا تُقبل ممارسةً بنصّ المطلب** | عدّ الصفوف | GAP-61 · `FR-JRN-001` |
| **F-D05** | **قرار مالك P0 معلَّق في بيانات التايم شيت**: **654 مقابل 48,380** — رقمان متجاوران لم يُحسم أيّهما المقام، **ولا تُمَسّ البيانات التاريخية حتى الحسم**. وهذا يجعل «مصدر 98.89٪ من روابط الأثر» رقمًا صحيحًا بمقامٍ غير محسوم | تقرير الإغلاق `FR-DAT-001` | GAP-65 |
| **F-D06** | **92 ملفًّا يستعلم عن السلطة مباشرةً** بدل مستوى موحَّد — وهي تبعية تحجب **أربعة مطالب** (`FR-APP-008/009/010` + `FR-APP-012`) | `8a1856c8` · `FR-APP-007` | GAP-64 |
| **F-D07** | **`fx` متوقف أحد عشر يومًا** — المؤشر عند 6,613 من 17,985 و`updated_at=2026-08-12 05:31:59` **بلا تغيير عبر أربع لقطات متتالية** | `ems_event_consumers` | GAP-07 |
| **F-D08** | **الأحداث: 0/58 له مستهلك أعمال نشط عبر أربع لقطات** — رغم أن حزمة `CHG-EVT-BUS-01` أُغلقت بصفر مفتوح (7 مُغلقة بالدليل + 1 منفَّذة). **الحزمة أغلقت الحوكمة لا الاستهلاك** | استعلام التقاطع | GAP-05 |
| **F-D09** | **تسع تصحيحات ذاتية داخل الجولة نفسها** — بنصوص التزاماتها: «أقحمتُ شريطًا ثانيًا في ثمانية أسطح ثم نزعتُه» · «قيمةٌ ابتلعها ENUM في هجرتي أنا» · «مخططي تعفّن بتسعةِ جداولَ صنعتُها — والفاحصُ كشفني» · «الحزامُ أمسك عطبًا في شاهدي في أوّلِ دورةٍ بعدَ وصلِه» · «بوابةٌ تعدُّ قيمةً بصفرِ صفٍّ يحملها لا تقدر أن ترسُب» · «استثناءٌ يُطرح من طرفَي الكسرِ يُخفي ما يُظهره» · «أخضرُ عقدٍ مستنِدٌ إلى مئةٍ وعشرين عرضًا ردميًّا» · «صفرُ حساب كان عدًّا في العمودِ الخطأ» · «رقمٌ منقولٌ في ملفِّ ماركداون كان يُخضِّر بوابةَ الجسر». **يُقرأ صحّةً في الحوكمة لا ضعفًا** | `git log` | — |
| **F-D10** | **أرقام كانت أعلى مما يحتمله القياس، صُحِّحت بالخفض**: تغطية الاشتقاق **5 من 100 لا 30** · و**94 من 100 تُسمَّى في السايدبار غير ما تُسمَّى في اللوحة** · و**119 رابطًا مُطفأً بقرار لا بإهمال — والعذر دعوى حتى يُقاس** | `61d9596d` · `d7b16d65` · `3d4e9e9c` | — |
| **F-D11** | **251 مخالفة كلها أقدم من حارسها** — فالحارس لم يُخترق، والمخالفات إرث. تمييزٌ يمنع قراءة الرقم اتهامًا للحارس | `cdaed900` | — |
| **F-D12** | **13 معيارًا كانت خضراء ولم تُقَس** — والمؤشر صار بابًا يرسُب. و**25 معيار نفي في بيانات حيّة**، و**ثلاثة أنواع من الصفر تُفصَل** (صفر لا بيانات · صفر لا مخالفة · صفر لم يُقَس) | `729111d8` · `c63e91cb` | — |
| **F-D13** | **دفتر الهجرات صمد**: 30 هجرة جديدة (380 → 410 ملفًّا) و**صفر خارج الدفتر** — القاعدة الذرّية (`_ledger.php`) أثبتت أنها تمنع تكرار النمط لا تعالجه مرةً | مقارنة glob×ledger | GAP-18 مُغلق |
| **F-D14** | **المالك المجهول انخفض 110 → 83** (25٪) و`gov_ownership_rulings` نمت 259 → **286** — والتعارض ثابت عند 13 (12 منها مرادفات اسم) | reconcile_stats | GAP-20 |
| **F-D15** | **حقل حساس واحد أُلغي بقرار**: `scr_sensitive_fields` صارت **33 «معتمد» + 1 «ملغاة»** بدل 34 معتمد — والإلغاء حالة مُعلَنة لا حذف صامت | استعلام | — |
| **F-D16** | **آلة اثنتي عشرة حالة مبنيّة ولم تُمارَس** — والالتزام يقولها بعنوانه. مثالٌ ثالث على «مبنيّ غير مُمارَس» بعد المحطات وقرارات السلّم | `a52254eb` | GAP-61 |
| **F-D17** | **«القدرة الغائبة الوحيدة» موجودة وتعمل — ولها توأم**؛ و**ثلاث من أربع «قدرات تُبنى أول مرة» مبنيّة سلفًا**. أي أن **وثيقة المطالب نفسها كانت تصف غيابًا حيث القياس يجد بناءً** | `eab9e69d` · `ad1b3e06` | — |

## ثانيًا — تصحيحات على قياس الجولة السابقة

| # | التصحيح | لماذا |
|---|---|---|
| **C-01** | **GAP-59 لم يعد «عَلَمًا منسيًّا»** — القياس السابق وصفه «مُعطَّل بالعَلَم» وهو وصفٌ صحيحٌ حينها؛ واليوم صار **قرارًا محجوزًا بنصاب معلن** بعد أن قِيس أثره وأُزيل مانعه | تغيّر النظام |
| **C-02** | **GAP-14 و GAP-22 و GAP-60 أُغلقت** — وكان رصدي لها مفتوحةً رغم ادعاء الإغلاق صحيحًا في حينه | تغيّر النظام |
| **C-03** | **GAP-62 استُوعب** — التسعة صارت مطالب مسمّاة بحالات في السجل الرسمي بدل «دَين وثائقي» | تغيّر النظام |
| **C-04** | **«Zero duplicate current baselines» صار أخضر** — كان أحمر بسبب 24 وثيقة باسم CURRENT؛ و`FR-GOV-003` أغلقها بوثيقة حاضرة واحدة | تغيّر النظام |
| **C-05** | **مقام الحقول**: 13,175 → **13,185** · و**1,417 حقلًا صارت مقيسةً من الشجرة** بدل تقدير | مقياس أدق |

## ثالثًا — إصلاحات P0 أثناء القياس

**لا شيء.** والمرشّح الوحيد (`EMS_LADDER_GATE=enforce`) **محجوزٌ بقرار مالك موثَّق النصاب** — لا بسهوٍ يُصلَح: الوثيقة تشترط نافذة ظلّ (500 قرار أو 7 أيام) و`gov_ladder_shadow` صفر صف. **والإعلان قبلها «قلبٌ بلا مقارن»** بنصّ الجولة نفسها.
