# BL-20260828b — نقطة القياس المثبَّتة (Snapshot)

**معرّف اللقطة:** `BL-20260828b-96a640e1` — كل رقم في مخرجات هذه الحزمة الثلاثة مقيسٌ على هذه اللقطة وحدها.
**اللقطات الخمس السابقة** في [`historical/`](historical/) — **أرقامها تاريخ ولا تُقرأ حاضرًا**.

> **لماذا لقطة سادسة:** **4 التزامات فقط** (`RPR-0` → `RPR-PRM`) — لكنها **أغلقت أخطر فجوة في اللقطة السابقة** (الدفترة) و**نظّفت الشجرة**. فالدلتا صغيرة والأثر حاكم.

## نقطة القياس

| البند | القيمة | الدليل |
|---|---|---|
| تاريخ/وقت القياس | **2026-08-28 · 16:35 → 17:15** (UTC+03) · **ونافذةُ تصحيحٍ ذاتيٍّ 17:20 → 17:40** لا قياسَ جديدًا فيها (F-F16..F-F19) | `date` + طوابع الاستخراج |
| Environment | تطوير محلي — WAMP على Windows 11 Pro، `C:\wamp64\www\ems` | بيئة الجلسة |
| PHP | **8.2.30** | `php -v` |
| قاعدة البيانات | MariaDB **11.4.9-log** · `equipation_manage` | `SELECT VERSION()` |
| Branch | **`repair01/w01-ownership`** (بلا upstream) | `git branch --show-current` |
| commit_hash | **`96a640e150b2eac84904f5d1fc1616fde08cdf0e`** — «RPR-PRM: المراجعةُ العكسيّةُ للبرومت — ٦٥/٦٥» (2026-08-28 15:44 +03) | `git rev-parse HEAD` |
| آخر Tag | `inj-exec-01-start` — الوصف `inj-exec-01-start-177-g96a640e1` | `git describe --tags` |
| التزامات غير مدفوعة | **70 تجاه `origin/main`** — ولا upstream للفرع | `git rev-list origin/main..HEAD --count` |
| **الشجرة العاملة** | **نظيفة تمامًا — صفر بند** (كانت 13 في اللقطة السابقة) ⇒ **كل السطح المقيس داخل التزام** | `git status --porcelain \| wc -l` = 0 |
| ثبات اللقطة | `HEAD` لم يتغيّر بين بدء القياس ونهايته **ولا أثناء نافذةِ التصحيح** ⇒ لا جزءَ `STALE` | `git rev-parse HEAD` قبل وبعد |
| **تصحيحٌ ذاتيٌّ داخل النافذة** | **صفرُ تعديلٍ على النظام** · وثلاثةُ أعطابٍ في عُدّتي أنا: `$SNAP$SNAP` · **الصفرُ المقيسُ يُكتب خليّةً فارغة** · **صفَّان متقادمان داخل تقريري** | [FINDINGS.md](FINDINGS.md) F-F16..F-F17 |
| إصدار الـSchema | **953 جدولًا أساسيًّا** (+1) + **28 منظرًا** · CHECK **831** · UNIQUE **564** · FK **311** | قياس حي |
| **دفتر الهجرات** | **703 صفًّا** (577 applied + 126 baseline) — كان 645 · و**463 ملفًّا على القرص** | قياس حي |
| **هجرات خارج الدفتر** | **صفر** (كانت **58**) — والمعلَّق الوحيد `_ledger.php` وهو **مكتبةٌ لا هجرة** | مقارنة glob×ledger |
| إصدار سجل التنقّل | `nav_items` نشط **2096** · `link_groups` نشط **1768** · `modules` **637** — بلا تغيير | قياس حي |
| إصدار محرك الاعتماد | `gov_ladders` 17 · `gov_journey_ladders` 14/14 `wired=1` · **`EMS_LADDER_GATE` ما زال غير معلَن ⇒ `monitor`** · `gov_ladder_shadow` **صفر صف** | قياس حي + `.env` |
| إصدار سجل الشاشات | `repair01_screen_registry` **783** (235 مسجَّلة + 388 حية غير مسجَّلة + **160 هدفًا**) · `repair01_departments` **21** · **صفر بلا مالك** | قياس حي |

## التحوّل الحاكم: الدفترة أُغلقت **بحاجزٍ مُثبَتٍ بالكسر** لا بإدخال

الفجوة التي أعدتُ فتحها في اللقطة السابقة (58 هجرة خارج الدفتر) **أُغلقت بالعلاج الذي يوقف تكرارها**:

| الحاجب | ما يفحص | المقام المقيس | النتيجة |
|---|---|---|---|
| `G-MIG-01` | كل هجرة **غير مقيَّدة** تستدعي `ems_migration_recorded()` | **0** غير مقيَّدة من **379** ملفًّا | ✔ |
| `G-MIG-02` | لا هجرة على القرص خارج الدفتر | **462** هجرة مُدارة · خارج الدفتر **0** | ✔ |
| `G-MIG-03` | لا صفَّ دفترٍ بلا ملفٍّ ولا حكم | **703** صفًّا · بلا ملف **241** · **بلا حكم 0** | ✔ |
| `G-MIG-04` | كل اسمٍ خارج عُرف التسمية مُعلَنٌ بحكمه | **1** ملفًّا خارج العُرف · بلا حكم **0** | ✔ |

**وثلاثة أمور تجعل هذا إغلاقًا لا ادّعاءً** — تحقّقتُ منها بتشغيل الحاجب بنفسي:
1. **الحاجب يطبع مقامه** لكل حكم، ولقطته الذاتية تحمل `Commit Hash` و`Schema Version` و`Registry Version`.
2. **مُثبَتٌ بالكسر بحركةِ عدّاد**: `[0,379,0,0]` ⇒ `[1,380,1,1]` ⇒ `[0,379,0,0]` — فرسوبُ الحاجب وحده لم يُقبل دليلًا لأنه كان راسبًا سلفًا بـ57.
3. **القيدُ فعلٌ لا نيّة**: هجرةٌ تستدعي الدفتر ولم تُشغَّل ⇒ `G-MIG-02` يرسُب.
4. **وحاجبٌ يوجب كسرَ حاجبٍ آخر ليس حاجبًا**: الصياغة الأولى طالبت 326 ملفًّا بالاستدعاء، منها 269 مقيَّدة سلفًا — وتحريرها يغيّر بصمتها فيُشعل كاشف التحرير في `migrate.php`. **فحُصر المقام في غير المقيَّد وحده.**

⇒ وهذا يطابق تشخيصي في اللقطة السابقة حرفًا: **القاعدة كانت سليمة وتعمل حين تُستدعى** (قياسهم: 52 تستدعي وكلها مقيَّدة · 57 لا تستدعي وكلها خارج الدفتر) — **والعطب أن الاستدعاء عُرفٌ، فصار شرطًا**.

## أرقام اللقطة المرجعية (كلها بمقامها — كلها من استعلام حي)

المصدر: [`snapshot_measures_f.json`](snapshot_measures_f.json).

- جداول أساسية **953** · بعمود `company_id` **657** · مناظر 28 · قيود CHECK **831** · UNIQUE **564** · FK **311**.
- أدوار **35** · مستخدمون نشطون **77** · `role_permissions` **3757**.
- الشاشات — **المقامات تُسمّى ولا تُخلط**: السجل الرسمي **783** (**623 As-Built** + 160 هدفًا) · أسطح PHP ممسوحة **778** (590 شاشة) · `nav_items` نشط **2096** · سجل الحزمة **758** · ظهورات المساحات **1200**.
- الحقول: **14,609** مستخرَجة · **14,608 مرتبطة بمعرّف شاشة رسميّ**.
- الأحداث **21,546** · القيود **6,713** بسطور **13,426** (فرق **0.00**) · الطابور منجز **15,848**.

## قاعدة عدم الخلط

لا يُخلط قياس من commit آخر أو schema آخر. أي إعادة قياس تُوسم بمعرّف لقطة جديد.
