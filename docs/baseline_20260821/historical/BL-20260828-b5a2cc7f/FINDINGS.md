# FINDINGS — ما اكتُشف أثناء استخراج BL-20260828 (سُجّل ولم يُصلَح)

> **قاعدة الجولة محفوظة:** «استخراج الحقيقة أولًا — لا إصلاح أثناء القياس». **صفر تعديل على كود النظام أو بياناته**. ما أُضيف: أداتا قراءة (`tools/baseline_db_dump5.php` · `tools/baseline_reconcile_rp01.php`) وتوسيع بانٍ للإكسل — كلها عُدّة قياس لا نظام.
> سجلّات اللقطات الأربع السابقة في [`historical/`](historical/).

## أولًا — اكتشافات هذه الجولة

| # | الاكتشاف | Evidence | GAP |
|---|---|---|---|
| **F-E01** | **تغيّر بنيويّ في المستودع**: الفرع `fix/remediation-2026-08` **دُمج في `main` وحُذف** (`git branch -a` يعطي `main` + `repair01/w01-ownership` فقط)، و`origin/main` صار `eacb41ef` (2026-08-24). ⇒ **مقام «الالتزامات غير المدفوعة» تغيّر**: 66 تجاه `origin/main` ولا upstream للفرع — **ولا يُقارَن بـ410 السابقة** | `git branch -a` · `git rev-list` | — |
| **F-E02** | **النظام صار يملك سجل الشاشات والإدارات بنفسه**: **141 جدول `repair01_*`**، أهمها `repair01_screen_registry` (**783**) بمعرّفات `SCR-nnnn` و`repair01_departments` (**21**) برموز `DEP-nn`. **وكل شاشة تحمل الحكم وقاعدته نصًّا**: `owner_rule` · `lifecycle_rule` · `visibility_rule` · `guard_kind`+`guard_evidence` · `permission_policy` · `verdict_rule` ⇒ **أسئلة «لماذا يرى هذا الدور هذه الشاشة» صار لها جواب مكتوب لا مستنتَج** | استعلام حي | — |
| **F-E03** | **الفحص الأحمر التاريخي صار أخضر**: **783/783 شاشة لها مالك** (`rp01_owner_missing = 0`) بعد أن كان 83 مالكًا مجهولًا. **أول مرة يخضرّ فحصا «صفر Route بلا مالك» و«100٪ لها Owner»** في خمس لقطات | مصالحة `rp01` | GAP-20 مُغلق |
| **F-E04** | **⚠ الدفترة انهارت — والانكسار عند ملفٍ واحدٍ بعينه.** التطابق **تامٌّ بلا استثناء**: كلُّ هجرةٍ تستدعي `ems_migration_recorded` **في الدفتر**، وكلُّ هجرةٍ لا تستدعيها **خارجه**. والقياس: `2027_10_*` **31/31 تستدعي** · `2027_11_01..05` **5/5 تستدعي وكلها مقيَّدة** (آخرها 2026-08-23 03:25) · ثم **من `2027_11_06_injfrd66_w5_derived_views.php` فصاعدًا: 0 من 42 تستدعي و0 من 42 مقيَّدة**. ⇒ **القاعدة سليمةٌ وتعمل حين تُستدعى — والعطب أنها تُستدعى بالعُرف، فانقطع العُرفُ دفعةً واحدةً عند أول هجرةٍ في جولةٍ جديدة**، ولا بوابةَ ترسُب على من لا يستدعيها | grep على 78 ملفًّا × الدفتر | GAP-18 · GAP-69 |
| **F-E05** | **`GHOST_TARGET` ليس As-Built**: السجل الرسمي يضمّ **160 صفًّا** دورةُ حياتها `GHOST_TARGET` و`visibility_class = NOT_BUILT` — **هدفٌ لم يُبنَ**. فمقام هذه الحزمة **623 لا 783**، وأي قراءة للـ783 حكمًا As-Built **تضخّم المقام 25.7٪** | `GROUP BY lifecycle` | — |
| **F-E06** | **السجل الرسمي يحمل عيبَين**: `SCR-0626` و`SCR-0627` مسجَّلتان `LIVE_REGISTERED` وهما **ملفَّا مكتبة**: `vendor/phpoffice/.../Periodic/Payments.php` و`.../Depreciation.php` — دخلتا على الأرجح بمسح اسمٍ (`Payments`/`Depreciation`) لا بمسح سطح | مصالحة القرص | GAP-67 |
| **F-E07** | **12 شاشة مالكها `PLATFORM` واسم إدارتها رمزُ قرار** (`W2_DECISION:W2-D-01`) لا اسم إدارة — رمز مالكٍ خارج جدول الإدارات القانونية | توزيع الإدارات | GAP-68 |
| **F-E08** | **103 سطح قرص خارج السجل الرسمي** — لكن تفصيلها يقلب القراءة: **71 معالجًا + 24 كرون + 7 دخول + شاشة واحدة**. ⇒ **فرقُ نطاقٍ لا فجوة**: السجل يغطي الشاشات لا المعالجات. **والشاشة الواحدة وحدها تستحق تسجيلًا** | `rp01_orphans` | — |
| **F-E09** | **الشجرة العاملة غير نظيفة أثناء القياس**: 13 بندًا غير ملتزم (5 معدَّلة منها `includes/unified_nav.php` + 8 غير متتبَّعة، عُدّة `repair01_ops_*` وأدلّتها) ⇒ **جزءٌ من السطح المقيس خارج أي التزام** — عملُ موجةٍ جارٍ لا نظافةَ شجرة | `git status` | GAP-70 |
| **F-E10** | **سكيو في تواريخ الالتزام**: الرأس `b5a2cc7f` بتاريخ **06:15** بينما التزامٌ **أقدمُ منه طوبولوجيًّا** (`c8dc29a7`) بتاريخ **14:58** ⇒ **أي ترتيب أو اختيار لقطة بساعة الحائط يكذب**. والجولة نفسها اكتشفت هذا وأصلحته في موضعَين (`5c186484` · `05a0010b`) | `git log --format='%h %ci'` | GAP-71 |
| **F-E11** | **نموّ ضخم في القاعدة والتنقّل**: جداول 655 → **952** (+297، منها **141 `repair01_`**) · CHECK 355 → **829** (+474) · `nav_items` نشط 1574 → **2096** · `link_groups` نشط 936 → **1768** · `modules` 505 → **637**. **والعزل لم يواكب**: `company_id` في 657 من 952 = **69.0٪** (كان 75.9٪) — لأن طبقة القياس الجديدة لا تحمل العمود | قياس حي | — |
| **F-E12** | **دليلٌ صحيحٌ صار متقادمًا**: التثبيت من الصفر (664 كائنًا) والنسخ/الاستعادة (RTO 58.5ث · PITR 6ث · 34 قادحًا) أُثبتا على **639 جدولًا** — والنظام اليوم **952**. **الدليل صحيح بتاريخه ولا يُقرأ حاضرًا** ⇒ محورا Backup و Reproducibility يعودان ◐/✘ | مقارنة المقامات | — |
| **F-E13** | **`fx` متوقف ستة عشر يومًا** — المؤشر عند 6,613 و`updated_at=2026-08-12` **بلا تغيير عبر خمس لقطات متتالية** | `ems_event_consumers` | GAP-07 |
| **F-E14** | **0/58 نوع حدث له مستهلك أعمال — عبر خمس لقطات** رغم أن حزمة الأحداث أُغلقت بصفر مفتوح | استعلام التقاطع | GAP-05 |
| **F-E15** | **المحطات المبنيّة ما زالت صفر صف عبر ثلاث لقطات**: الفوترة والخزينة والعروض ومخالفات الموردين. و**التسويات 469 بصفر مدفوع** | عدّ الصفوف | GAP-61 |
| **F-E16** | **انخفاض غير مفسَّر في عدّ ملفات PHP**: 6,197 → **4,889** — و`git diff` بين اللقطتين يُظهر **صفر حذف** و580 إضافة. ⇒ الفارق في **ملفات غير متتبَّعة زالت من القرص**، و**السبب غير محدَّد بالقياس** ⇒ **NEEDS_REVIEW ولا يُخمَّن** | `git diff --diff-filter=D` | — |
| **F-E17** | **مصالحة الحقول نجحت بامتياز**: **14,608 من 14,609 حقلًا ارتبطت بمعرّف شاشة رسميّ** — فسؤال «سعر الوحدة يظهر أين؟» صار يُجاب بمعرّفٍ واحد عبر الحزمة كلها | `rp01_stats` | — |

## ثانيًا — تصحيحات على قياس الجولات السابقة

| # | التصحيح | لماذا |
|---|---|---|
| **C-01** | **`Screen_ID`/`Department_ID` لم يعودا اشتقاقًا** — كانا يُولَّدان في مُوفِّق الحزمة، وصارا من `repair01_*`. **والحزمة تُصالح ولا تستبدل** | تغيّر النظام |
| **C-02** | **GAP-20 أُغلق** — والمقام تغيّر معه: من «626 صفًّا بسبعة مصادر» إلى «623 As-Built بسجل واحد» | تغيّر النظام |
| **C-03** | **GAP-18 أُعيد فتحه** — وكنتُ أعلنتُه مُغلقًا في اللقطة السابقة بشاهد «صفر خارج الدفتر». الشاهد كان صحيحًا حينها، **والقاعدة لم تصمد لجولة واحدة** | تغيّر النظام |
| **C-04** | **مقام «غير المدفوع» انقطع** — لا يُقارَن 66 اليوم بـ410 أمس لأن الفرع دُمج وحُذف | تغيّر المقام |
| **C-05** | **Backup/PITR والتثبيت**: كانا ✔ وصارا ◐/✘ — **لا لأن شيئًا انكسر بل لأن الدليل صار على مخطَّط أصغر بـ297 جدولًا** | تقادم دليل |

## ثالثًا — إصلاحات P0 أثناء القياس

**لا شيء في النظام.** والمرشّح الأول (`GAP-69` — بوابة ترسُب على هجرة لا تستدعي القاعدة) **لم يُلمس عمدًا**: إضافة بوابة تُرسِّب على 42 هجرة قائمة **قرارُ حوكمةٍ بأثر رجعيّ**، لا سطرًا يُكتب أثناء القياس. وقد سُجِّل بشاهده ومقامه.
