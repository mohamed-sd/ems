# BL-20260902 — نقطة القياس المثبَّتة (Snapshot)

**معرّف اللقطة:** `BL-20260902-2676dea5` — كل رقم في مخرجات هذه الحزمة الثلاثة مقيسٌ على هذه اللقطة وحدها.
**اللقطات التسع المؤرشفة** في [`historical/`](historical/) — **أرقامها تاريخ ولا تُقرأ حاضرًا**؛ آخرها `BL-20260901-13010626`.

> ⚠ **تصحيحٌ على سجلِّ الأرشفة نفسِه:** اللقطةُ السابقةُ أعلنت «التسعَ السابقة … آخرها `BL-20260831d-aba54573`» — **والمجلَّدُ كان يحوي ثمانيًا، ولا أثرَ فيه لـ`BL-20260831b/c/d`**. ⇒ **ثلاثُ لقطاتٍ أُعلنت ولم تُؤرشَف**، فأرقامُها موجودةٌ في النثرِ لا في أساسٍ يُستعاد. وهذه اللقطةُ تُصحّح العدَّ: **تسعٌ مؤرشفةٌ فعلًا بعد أرشفةِ `BL-20260901`**.

## نقطة القياس

| البند | القيمة | الدليل |
|---|---|---|
| تاريخ/وقت القياس | **2026-09-02 · 01:23 → 02:15** (UTC+03) | `date` + طوابع الاستخراج |
| Environment | تطوير محلي — WAMP على Windows 11 Pro، `C:\wamp64\www\ems` | بيئة الجلسة |
| PHP | **8.2.30** (أدوات القياس) · **وساعتُه UTC** بفارقِ ثلاثِ ساعاتٍ عن ساعةِ النظام | `php -v` · مقارنةُ الطوابع |
| قاعدة البيانات | MariaDB **11.4.9-log** · `equipation_manage` · المنفَذ **3307** | `SELECT VERSION()` |
| Branch | **`repair01/w01-ownership`** (بلا upstream) | `git branch --show-current` |
| commit_hash | **`2676dea5bd7e0f4c3c55d99c6500ef2bbab03f72`** — «GOV_UI_FINISH — بوابات الارتداد العشر تعاد قياسها» (2026-09-02 00:53) | `git rev-parse HEAD` |
| آخر Tag | `pre-build-20260830` — الوصف `pre-build-20260830-175-g2676dea5` | `git describe --tags` |
| التزامات غير مدفوعة | **406 تجاه `origin/main`** (كانت 326) · **80 التزامًا منذ اللقطة السابقة** | `git rev-list --count` |
| **الشجرة العاملة** | ✔ **نظيفةٌ تمامًا — صفر بند عند تثبيتِ اللقطة** ⇒ **كلُّ السطحِ المقيسِ داخلَ التزام**؛ والمتغيّرُ بعدها **مخرجاتُ القياسِ نفسِها** (37 بندًا: استخراجاتٌ ووثائقُ وأرشيف) لا كودُ النظام | `git status --porcelain` = 0 قبل · 37 بعد |
| ثبات اللقطة | `HEAD` لم يتغيّر بين بدء القياس ونهايته ⇒ لا جزءَ `STALE` | `git rev-parse HEAD` قبل وبعد |
| إصدار الـSchema | **1228 جدولًا أساسيًّا** (كان 998 · **+230**) + **28 منظرًا** · CHECK **926** · UNIQUE **590** · FK **320** | قياس حي |
| **دفتر الهجرات** | `schema_migrations` **952 صفًّا** (كان 822) · خارج الدفتر **صفر** · البوابة **4/4 خضراء** شغّلتُها بنفسي | `tools/repair01_migration_gate.php` |
| إصدار سجل التنقّل | `nav_items` نشط **2476** (كان 2366) · `link_groups` نشط **2635** · `modules` **763** | قياس حي |
| إصدار محرك الاعتماد | `gov_ladders` **17** · **`EMS_LADDER_GATE` ما زال غيرَ معلَن ⇒ `monitor`** · `gov_ladder_shadow` **صفر صف** | قياس حي + `.env` |
| إصدار سجل الشاشات | `repair01_screen_registry` **909** (كان 814) · **749 As-Built** + 160 هدفًا · **صفر بلا مالك** | قياس حي |
| الناقل | `ems_business_events` **21,627** · `ems_event_deliveries` **21,428** · **`EffectLinkConsumer` 11 تسليمًا كلُّها `dlq`** | قياس حي |

## ثلاثةُ تحوُّلاتٍ حاكمةٍ في هذه اللقطة

### ① الصفرُ الذي دام ستَّ لقطاتٍ انكسر — عند الوصلِ لا عند النتيجة

**«0/58 حدثًا له مستهلكُ أعمال» صارت 23/58.** والفاعلُ صنفٌ واحدٌ جديد: `EffectLinkConsumer` يشترك على ثلاثةٍ وعشرين مفتاحًا **مِمّا يُنتَج فعلًا** (لا على مفاتيحَ ميتةٍ كما كان حالُ الاثنتين والثلاثين اشتراكًا السابقة). والباقي **35 مفتاحًا بحارسِ الحوكمةِ وحدَه**.

**ولا يُقرأ هذا نجاحًا كاملًا — والمقامُ الثاني يقوله:** التسليماتُ الفعليةُ لهذا المستهلك **11**، **وكلُّها `dlq`**، **وصفرٌ منها `processed`**.
والسببُ مكتوبٌ بنصِّه في كلِّ صفّ:

> `EFFECT_MISSING: expense.depreciation.recorded — لا رابط مروحة ولا مراة مالية ولا سجل كيان قائم — يعزل باسمه ولا يمر بصمت`

⇒ **Built ✔ · Wired ✔ · Exercised ✔ (11 محاولة) · Verified ✘ (صفر نجاح).** وهذا **تقدُّمٌ حقيقيٌّ لا يُبالَغ فيه**: انتقل العطبُ من «لا أحدَ يستمع» إلى «يستمع ويرسُب باسمِه» — **والرسوبُ المُسمّى أفضلُ من الصمت**، لكنه ليس تسليمًا.

### ② تسعُ شواهدَ حمراءَ صارت ثلاثًا — واثنتان منها ليستا انتكاسًا

مقياسُ الإغلاق **16/33 ⇒ 23/33**، والشواهدُ **25/34 ⇒ 32/35 خضراء**. والحُمرُ الثلاثُ **تُقرأ بأسبابِها لا بلونِها**:

| الشاهد | ما رسَب | القراءةُ الصحيحة |
|---|---|---|
| `injfix02_ownership_ruling_proof` | «**انخفض إلى 88 من 96** — تُشدُّ السقّاطة» | ⚠ **تحسُّنٌ يُقرأ رسوبًا**: غيرُ المحسومِ نزل ثمانيةً، **والسقّاطةُ ترسُب على النقصانِ كما ترسُب على الازدياد** حتى تُشَدّ |
| `injfix02_field_extraction_limit_proof` | «85 شاشةً جديدةً بلا حقولٍ مستخرَجة» · «لا ازديادَ — 121 ≤ 36» | ⚠ **نموٌّ يسبق أداةَ القياس**: 96 شاشةً بُنيت (620 ⇒ **716**) ومستخرِجي لا يقرأ حقولَها — **عجزُ المقياسِ لا عجزُ المنتَج** |
| `injfix01_release_and_action_finality_proof` | «البصمةُ لا تطابق» · «**المعلَّقُ لا يزداد — 163 ≤ 82**» | ⛔ **رسوبٌ حقيقيٌّ بشقَّين**: بصمةٌ متقادمةٌ (أدناه) · و**المعلَّقُ تضاعف 82 ⇒ 163** |

### ③ تصحيحٌ على حكمي أنا: البصمةُ لم تسقط بسببِ الشجرة قطُّ

في `BL-20260828c` كتبتُ أنّ «الشجرةَ اتّسخت فسقطت البصمة». **وهذا خطأ.** الشجرةُ اليومَ **نظيفةٌ تمامًا (صفر بند)** والبصمتان **لم تتغيّرا حرفًا** عبر ثلاثِ لقطات: المحسوبةُ `4099c942…` والمسجَّلةُ `4e73fa5d…`.

والقراءةُ الصحيحة من الشيفرة: الفاحصُ يجمع بصمةَ **قائمةِ ملفاتٍ مُعلَنةٍ** في `gov_component_versions.files_json` — **لا شجرةَ git**. والصفُّ `DRAFT` هو `ux-1.6.0` **بخمسةِ ملفاتٍ وتاريخُه 2026-08-21 17:15** ⇒ **إصدارُ مكوّناتِ الواجهةِ لم يُعَد إصدارُه منذ اثنَي عشرَ يومًا** بينما ملفاتُه تغيّرت. **فالعطبُ إصدارٌ لم يُجدَّد، لا شجرةٌ متّسخة.**

## أرقام اللقطة المرجعية (كلها بمقامها — كلها من استعلام حي)

المصدر: [`snapshot_measures_i.json`](snapshot_measures_i.json).

- جداول أساسية **1228** · مناظر 28 · CHECK **926** · UNIQUE **590** · FK **320** · دفتر الهجرات **952**.
- أدوار **35** · مستخدمون غير محذوفين **77** · `role_permissions` **4111**.
- الشاشات — **المقامات تُسمّى ولا تُخلط**: السجل الرسمي **909** (**749 As-Built** + 160 هدفًا) · أسطح PHP ممسوحة **905** (716 شاشة) · `nav_items` نشط **2476** · سجل الحزمة **885** · ظهورات المساحات **1388** · `gov_screen_cycle` **1949**.
- الحقول: **16,136** مستخرَجة · **16,135** مرتبطة بمعرّف شاشة رسميّ · **8,757 بالاسم التقنيّ (54٫3٪)** · و**7,379 موسومةٌ `NEEDS_REVIEW`** وكلُّها أعمدةُ جداول (**75٪** من 9,835).
- الأحداث **21,627** · **58 مفتاحًا منتَجًا** · **23 له مستهلكُ أعمال** · التسليمات **21,428** · القيود **6,713/13,426** بفرق **0.00** · الطابور منجز **22,766**.
- الحوكمة: `gov_field_class` **2394** (كان 630) · الحقول الحساسة بسياسة **34** · التسويات **469 بصفرٍ مدفوع**.

## قاعدة عدم الخلط

لا يُخلط قياس من commit آخر أو schema آخر. أي إعادة قياس تُوسم بمعرّف لقطة جديد.
**وقاعدةُ الأرشفةِ تُشدَّد بعد ما وقع:** لا يُذكر معرّفُ لقطةٍ في وثيقةٍ حاضرةٍ **إلّا وله مجلَّدٌ في `historical/`** — فالرقمُ الذي لا يُستعاد ليس تاريخًا بل ذكرى.
