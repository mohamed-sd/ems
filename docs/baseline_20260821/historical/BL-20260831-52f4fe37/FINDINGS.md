# FINDINGS — ما اكتُشف أثناء استخراج `BL-20260831-52f4fe37` (سُجّل ولم يُصلَح)

> **اللقطة:** `BL-20260831-52f4fe37` · القياس: 2026-08-31 · 10:32→11:30 · التفصيل في [SNAPSHOT.md](SNAPSHOT.md)
> **قاعدة الجولة محفوظة:** «استخراج الحقيقة أولًا — لا إصلاح أثناء القياس». **صفرُ تعديلٍ على كودِ النظامِ أو بياناته** — ولا على `cron_events.php` وهو أرخصُ إصلاحٍ بأكبرِ أثر، **لأنّ جدولتَه فعلُ بيئةٍ لا فعلُ قائس**.
> سجلّات اللقطات السبعِ السابقة في [`historical/`](historical/).

## أولًا — اكتشافات هذه الجولة

| # | الاكتشاف | Evidence | GAP |
|---|---|---|---|
| **F-H01** | ✔ **الصفرُ الذي دام ستَّ لقطاتٍ انكسر**: استهلاكُ الأحداثِ **0/58 ⇒ 23/58** مفتاحًا منتَجًا له **مستهلكُ أعمالٍ فعّال** — كلُّها عبر `App\Services\Bus\Consumers\EffectLinkConsumer` (ملفٌّ جديدٌ في الدلتا)، و**35 بحارسِ الحوكمةِ وحدَه** بعد أن كانت 58 | `ems_event_subscriptions` × `ems_business_events` × `gov_event_rulings` | GAP-05 |
| **F-H02** | ⛔ **ولا يُقرأ «الناقلُ يعمل» — والمقياسُ الثاني يمنع ذلك**: تسليماتُ `effectlink` **صفٌّ واحدٌ** حالتُه **`failed`** بـ`HANDLER_ERROR`، ونصُّه: `EFFECT_MISSING: settlement.approved — لا رابط مروحة ولا مراة مالية ولا سجل كيان قائم (كيان settlement#5502) — **يعزل باسمه ولا يمر بصمت**`. وزمنُه **10:40** أي **داخلَ نافذةِ قياسي**. ⇒ **`Wired` ✔ · `Exercised` مرّةً · `Verified` ✘** | `ems_event_deliveries` | GAP-05 |
| **F-H03** | **والسببُ مقيسٌ ومسمّى — لا مجهول**: `scheduler_parity` يرسُب بـ«غيرُ مجدول: **`cron_events.php`**». ⇒ **مستهلكٌ بلا مُشغِّلٍ سجلٌّ بلا قارئ** — وجدولةُ ملفٍّ واحدٍ تحرّك 23 مفتاحًا من «موصول» إلى «مُثبَت»: **أرخصُ فعلٍ بأكبرِ أثرٍ في هذه اللقطة** | `injfix01_scheduler_parity_proof.php` | **GAP-76** |
| **F-H04** | ✔ **`fx` لحق بعد ستةَ عشرَ يومًا**: 6,613 ⇒ **17,985** بتاريخ **2026-08-31 07:18** فصار مساويًا للمستهلكَين الآخرَين · و**DLQ 26 ⇒ صفر** | `ems_event_consumers` · `ems_event_dead_letter` | GAP-07 |
| **F-H05** | ⛔ **والعطبُ انتقل ولم يختفِ**: `bus_stall_alarm` يرسُب الآن بـ«الملتحقون: finance · finance_routing · **fx** · **أُنذر خطأً: fx**». ⇒ **`GAP-07` كان أحمرَ لأنّ `fx` متوقّف، وهو اليومَ أحمرُ لأنّ الإنذارَ يُنذر عنه وقد لحق** — **فجوةٌ واحدةٌ حمراءُ بسببَين متضادَّين**. ⇒ **يُقرأ نصُّ الرسوبِ لا لونُه** | `injfix01_bus_stall_alarm_proof.php` | GAP-07 |
| **F-H06** | ✔ **الملكيةُ المرتدّةُ عادت — بنفسِ الاستعلامِ الذي كشفها**: الأربعةُ عادت إلى `DEP-05`/`DEP-11`/`DEP-05`/`DEP-09`، و`SCR-0532` **استعاد `W01_SURFACE_REGISTER`**. **القاعدةُ في 11 صفًّا · صفرٌ يناقضها** (كان 4) | استعلامُ القاعدةِ مقابلَ القيمة | GAP-73 |
| **F-H07** | ✔ **الشجرةُ نُظِّفت وحزمةُ الأساسِ صارت داخلَ التزام**: ٥٦ ⇒ **3 بنود**، وكلُّها **أدواتُ فحص** (`repair01_w13_scan` · `repair01_w14_scan` · `repair01_w14_gate`) | `git status --porcelain` | GAP-70 |
| **F-H08** | ⚠ **«السقّاطةُ تحرس الدَّينَ ولا تحرس التقدُّم» — أوضحُ مثالٍ في السلسلة**: `permission_single_point_gate` **يرسُب لأنّ العددَ تحسَّن**: «◆ **انخفض إلى 20 من 72** — تُشدُّ السقّاطة بـ`--retighten`». ⇒ **قرّاءُ القرارِ نزلوا 52 قارئًا** وهو أكبرُ مكسبٍ صلاحياتيٍّ قِسته، **ويُقرأ رسوبًا** | تشغيلٌ حيّ | **GAP-75** |
| **F-H09** | ⚠ **وثلاثُ سقّاطاتٍ أخرى ترسُب على النموِّ الطبيعيّ**: `field_extraction_limit` (**79 ≤ 77**) · `ownership_ruling` (**97 ≤ 96**) · `path_rulings` (**3 ≤ 2**). ⇒ **ستٌّ من تسعِ شواهدَ حمراءَ ليست عطبًا في النظام**، والمقياسُ يخصم بها **6 من 33** | تشغيلُ الشواهد | GAP-75 |
| **F-H10** | **⇒ القراءةُ الصحيحةُ للتراجع 22/33 ⇒ 16/33**: **راسبٌ بحقٍّ اثنان** (`GAP-10` مسربُ القناةِ ⑤ · و`cron_events` بلا جدولة) · **وواحدٌ تقادمُ تسجيل** · **وستٌّ سقّاطاتٌ تحتاج شدًّا**. **والمقياسُ سليمٌ في آليّتِه**: يُرسِّب الفجوةَ إذا رسَب شاهدُها — **ومضلِّلٌ إذا قُرئ رقمًا مجرَّدًا** | `tools/injfix01_gap_coverage.php` | GAP-75 |
| **F-H11** | ⛔ **تصحيحٌ على قراءتي في اللقطةِ السابقة**: قلتُ إنّ «الشجرةَ اتّسخت فسقطت البصمةُ». **وهذا خطأ.** الفاحصُ يحسب البصمةَ على **قائمةِ ملفاتٍ مُعلَنةٍ في `gov_component_versions`** (خمسةُ ملفاتٍ للإصدارِ `ux-1.6.0`) **لا على شجرةِ Git** (`tests/injfix01_release_and_action_finality_proof.php:52-72`). **والدليلُ قاطع**: الشجرةُ اليومَ نظيفةٌ **والبصمتان لم تتغيّرا حرفًا** — نفسُ `4099c94…` ونفسُ `4e73fa5d…`. **السببُ الحقيقيّ**: الإصدارُ سُجِّل **2026-08-21 17:15** ولم يُعَد إصدارُه بعد تغيُّرِ ملفاتِه ⇒ **تقادمُ تسجيلٍ لا اتّساخُ شجرة** | قراءةُ الفاحصِ + `gov_component_versions` | GAP-14/26/32 |
| **F-H12** | ◐ **الاستنساخُ النظيفُ انكمشت فجوتُه انكماشًا كبيرًا**: الجداولُ الحيّةُ الغائبةُ عنه **8 ⇒ 3** (وكلُّها `repair01_*` **أحدثُ من المرجع**: `repair01_fc_states` · `repair01_owner_close` · `repair01_render_align`) و**فرقُ الأعمدةِ 3,434 ⇒ 40**. ⇒ **`GAP-18` ما زال مفتوحًا وقد قارب** — و`database/schema/{schema.sql,seed_reference.sql,MANIFEST.json}` أُعيد توليدُها في الدلتا | `injfix01_clean_clone_fingerprint_proof.php` | GAP-18 |
| **F-H13** | ⛔ **`GAP-10` (P0) باقٍ بحرفِه**: القناةُ ⑤ (المناظرُ المحفوظة) — **8 ملفاتِ قناةٍ · 1 يمسُّ حقلًا حسّاسًا · 0 يمرُّ بالمقرِّر · إعفاءٌ مُعلَنٌ 0 · مفتوحةٌ 1** — **بنفسِ الأرقامِ منذ لقطتَين** رغم تعديلِ `app/Services/Security/SensitiveFieldGuard.php` في الدلتا. **وثمانِ قنواتٍ من تسعٍ نظيفة** | `injfix01_sensitive_fields_nine_channels_proof.php` | GAP-10 |
| **F-H14** | ⛔ **تلوُّثُ عمودِ مفتاحِ المستهلك**: `ems_event_deliveries.consumer_key` فيه **8 مفاتيحَ من 12 متمايزًا** نصوصٌ بشريّةٌ من بذرِ UAT (`مطابقٌ للمستند المرفق · UAT-2026` · `بانتظار اعتماد الإدارة المختصة · UAT-2026` …) في **10 صفوف** من 21,351. و`GAP-09` («صفرُ نصٍّ بشريٍّ في حقلِ مفتاح») **مُغلقٌ وصادقٌ على مقامِه** — **وهذا العمودُ خارجَ ذلك المقام** ⇒ **إغلاقٌ صحيحٌ على ما فُحِص، وعمودٌ لم يُفحَص** | `SELECT DISTINCT consumer_key` | **GAP-77** |
| **F-H15** | ◐ **`PLATFORM` صار `PLTF` وحُذف صفُّه من `repair01_departments`** (22 ⇒ 21) — و**الفحصُ المطلوبُ في البرومت يقبله صراحةً** («Owner **أو PLATFORM_SHARED**»)، فالاثنتا عشرةَ شاشةً (`global_search` · `soon` · `user_profile` · `dept_board` …) **منصّيّةٌ بحقٍّ**. **والباقي أنّه لا يُترجَم إلى اسمٍ** من جدولِ الإدارات | استعلامُ الرموزِ بلا صفٍّ | GAP-68 |
| **F-H16** | **نموُّ المقامات — وكلُّها تُذكر قبل أيِّ مقارنة**: جداول **953 ⇒ 987** · CHECK **831 ⇒ 919** · UNIQUE **564 ⇒ 581** · `repair01_*` **141 ⇒ 173** · دفتر الهجرات **703 ⇒ 808** · `nav_items` **2096 ⇒ 2327** · `link_groups` **1768 ⇒ 2615** · `modules` **637 ⇒ 667** · الصلاحيات **3757 ⇒ 4003** · السجل الرسمي **783 ⇒ 813** · الحقول **14,609 ⇒ 14,857** · الطابور **16,208 ⇒ 19,592** | قياس حي | — |
| **F-H17** | ✔ **الدفترةُ صمَدت عبر 105 هجراتٍ جديدة**: البوابةُ **4/4 خضراء** بمقاماتٍ **نمت** (`G-MIG-01` 0 من **484** · `G-MIG-02` **567** مُدارة وصفر خارج · `G-MIG-03` **808** صفًّا وصفر بلا حكم · `G-MIG-04` 1 وصفر بلا حكم). ⇒ **هذا هو الفرقُ بين علاجٍ بحاجبٍ وعلاجٍ بفعل** — الحاجبُ صمَد لجولةٍ من 226 التزامًا | تشغيلٌ حيّ | GAP-18/69 |
| **F-H18** | **سجلُّ المتطلباتِ 66 ⇒ 70** بأربعةِ مطالبَ جديدةٍ (`FR-GOV-012..015`، منها **P0**) **كلُّها `OPEN`** — فالمفتوحُ **5 ⇒ 9** والبسطُ ثابتٌ عند **26**: النسبةُ **39.4٪ ⇒ 37.1٪** **بنموِّ المقامِ لا بتراجعِ العمل** | `tools/injfrd01_status.php` | — |
| **F-H19** | **ما لم يتغيّر عبر ثمانِ لقطات**: الأحداث **21,546** بـ**58** مفتاحًا · القيود **6,713/13,426** بفرق **0.00** · التسويات **469 بصفرٍ مدفوع** · `EMS_LADDER_GATE` **غيرُ معلَن** · `gov_ladder_shadow` **صفر صف** · الذهبيةُ **0/10 اعتمادًا** · `gov_field_class` 630 · الحقولُ الحساسةُ بسياسة 34 | قياس حي | GAP-59/63 · GAP-23 |
| **F-H20** | **مقامان لسؤالٍ واحدٍ — مرّتان**: ① الذهبية: **10/10** تجتاز التسعَ الموضوعية (`injfix01_golden_promotion_gate`) بينما `uxui_golden_gates` يرصد **مخالفتَين** في `FinRequests/request_form.php` ② الاستهلاك: **23/58 موصولة** مقابل **0/23 مُثبَتة**. **كلاهما صادقٌ على مقامِه** — وسُمِّي المقامان في الوثائق | أداتان × استعلامان | — |

## ثانيًا — تصحيحات على قياسِ الجولةِ السابقة

| # | التصحيح | لماذا |
|---|---|---|
| **C-01** | ⛔ **«الشجرةُ اتّسخت فسقطت البصمةُ» كان تشخيصًا خاطئًا** — البصمةُ تُحسَب على **قائمةِ ملفاتٍ مُعلَنةٍ في `gov_component_versions`** لا على شجرةِ Git، والدليلُ أنّ الشجرةَ نظُفت **والبصمتان لم تتغيّرا حرفًا**. **والسببُ الحقيقيُّ تقادمُ تسجيلِ الإصدارِ `ux-1.6.0` منذ 2026-08-21** | خطأُ تشخيصٍ عندي — قرأتُ نتيجةً ولم أقرأ الفاحص |
| **C-02** | **`GAP-05` لم يعد «0/58»** — صار **23/58 موصولة** و**0/23 مُثبَتة**. وأيُّ اقتباسٍ للرقمِ القديمِ بعد اليومَ **تاريخٌ لا حاضر** | تغيّرُ النظام |
| **C-03** | **`GAP-07` لم يعد «`fx` متوقّف»** — لحق تمامًا، **والفجوةُ بقيت مفتوحةً لسببٍ معاكس** (إنذارٌ كاذب). ⇒ **لا يُقتبس سببُها القديمُ مع لونِها الجديد** | العطبُ انتقل |
| **C-04** | **`GAP-73` و`GAP-70` أُغلقتا** — وكان رصدي لهما مفتوحتَين صحيحًا في حينه؛ والعلاجُ جاء **بردِّ القيمِ لا بتبريرِها** | تغيّرُ النظام |
| **C-05** | **«22/33» صارت «16/33» — ولا انتكاسةَ فيها** | ستُّ سقّاطاتٍ رسَبت على النموِّ أو التحسُّن |
| **C-06** | **كلُّ المقاماتِ نمت** (الجداولُ والقيودُ والتنقّلُ والسجلُّ والحقول) ⇒ **أيُّ رقمٍ من `BL-20260828c` لا يُقارَن بلا ذكرِ مقامِه القديم** | نموُّ المقام |

## ثالثًا — إصلاحات P0 أثناء القياس

**لا شيء — لا في النظامِ ولا في عُدّتي.**

والمرشَّحُ الأوّلُ كان **جدولةَ `cron_events.php`** (`GAP-76`): فعلٌ واحدٌ يحرّك 23 مفتاحًا من «موصول» إلى «مُثبَت» ويُغلق `GAP-05` عمليًّا. **وتركتُه** لأنّه **فعلُ بيئةٍ لا فعلُ قائس** — جدولةُ مهمّةٍ في نظامِ التشغيلِ خارجَ مدى «استخراج الحقيقة»، وأثرُها يتجاوز القياسَ إلى التشغيل.

**والمانعان التاليان فعلُ مالكٍ أيضًا**: نافذةُ الظلِّ (`GAP-63`) وقرارُ مقامِ التايم شيت (`GAP-65`).

**وملاحظةٌ على الأداءِ لا على النظام:** لم أشدَّ أيَّ سقّاطةٍ من السقّاطاتِ الستّ (`--retighten`) رغم أنّ شدَّها **يرفع المقياسَ فورًا من 16/33** — لأنّ شدَّ سقّاطةٍ **تغييرُ خطِّ أساسٍ مُعلَن**، وفعلُه أثناءَ القياسِ يجعل الرقمَ الذي أسلّمه صناعتي لا قياسي.
