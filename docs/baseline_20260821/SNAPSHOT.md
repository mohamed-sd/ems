# BL-20260831 — نقطة القياس المثبَّتة (Snapshot)

**معرّف اللقطة:** `BL-20260831-52f4fe37` — كل رقم في مخرجات هذه الحزمة الثلاثة مقيسٌ على هذه اللقطة وحدها.
**اللقطات السبعُ السابقة** في [`historical/`](historical/) — **أرقامها تاريخ ولا تُقرأ حاضرًا**.

> **أكبرُ دلتا في السلسلة:** **226 التزامًا · 552 ملفًّا · 105 هجرةً** في ثلاثةِ أيام (`96a640e1` ⇒ `52f4fe37`). وثلاثُ نتائجَ تُقرأ قبل أيِّ رقم:
> ① **صفرُ مستهلكِ الأحداثِ انكسر** — 0/58 ⇒ **23/58 موصولة**، وتسليمُها الأوّلُ **رسَب باسمِه** لا بصمت.
> ② **الشجرةُ عادت نظيفةً** (٥٦ ⇒ **3 بنود**) و**الملكيةُ المرتدّةُ عادت** (٤ ⇒ **صفر**) ⇒ `GAP-70` و`GAP-73` **مُغلقتان**.
> ③ **ومقياسُ الإغلاق نزل 22/33 ⇒ 16/33** — **ولا انتكاسةَ واحدةً فيه**: تسعُ شواهدَ حمراءَ من 34، **أغلبُها سقّاطاتٌ رسَبت على النموِّ أو على التحسُّن**.

## نقطة القياس

| البند | القيمة | الدليل |
|---|---|---|
| تاريخ/وقت القياس | **2026-08-31 · 10:32 → 11:30** (UTC+03) | `date` + طوابع الاستخراج |
| Environment | تطوير محلي — WAMP على Windows 11 Pro، `C:\wamp64\www\ems` | بيئة الجلسة |
| PHP | **8.2.30** — وساعتُه UTC بفارقِ ثلاثِ ساعاتٍ عن ساعةِ النظام | `php -v` · مقارنةُ الطوابع |
| قاعدة البيانات | MariaDB **11.4.9-log** · `equipation_manage` · المنفَذ **3307** | `SELECT VERSION()` |
| Branch | **`repair01/w01-ownership`** (بلا upstream) | `git branch --show-current` |
| commit_hash | **`52f4fe3760f1963eff28f74659a04a9310f883f0`** — «FINAL_CLOSE ⑰⑱» (2026-08-31 09:20 +03) | `git rev-parse HEAD` |
| آخر Tag | **`pre-build-20260830`** — الوصف `pre-build-20260830-65-g52f4fe37` (**وسمٌ جديدٌ لم يكن في اللقطة السابقة**) | `git describe --tags` |
| التزامات غير مدفوعة | **296 تجاه `origin/main`** (كانت 70) — ولا upstream للفرع | `git rev-list origin/main..HEAD --count` |
| **الشجرة العاملة** | ✔ **3 بنود** (كانت ٥٦) — وكلُّها أدواتُ فحصٍ (`repair01_w13_scan` · `repair01_w14_scan` · `repair01_w14_gate`) · **وحزمتي كلُّها داخلَ التزام** | `git status --porcelain` |
| ثبات اللقطة | `HEAD` لم يتغيّر بين بدء القياس ونهايته ⇒ لا جزءَ `STALE` | `git rev-parse HEAD` قبل وبعد |
| **مدى الدلتا** | **226 التزامًا · 552 ملفًّا**: 130 في `docs/REPAIR01_20260823` · **105 هجرة** · 41 في حزمتي · **8 في `app/Services`** · 3 في `database/schema` | `git diff --name-only` |
| إصدار الـSchema | **987 جدولًا أساسيًّا** (+34) + 28 منظرًا · CHECK **919** (+88) · UNIQUE **581** · FK **311** | قياس حي |
| **دفتر الهجرات** | `schema_migrations` **808 صفًّا** (كان 703) · خارج الدفتر **صفر** · البوابة **4/4 خضراء** بمقاماتٍ نمت (484/567/808) | `tools/repair01_migration_gate.php` |
| إصدار سجل التنقّل | `nav_items` نشط **2327** (+231) · `link_groups` نشط **2615** (+847) · `modules` **667** (+30) | قياس حي |
| إصدار محرك الاعتماد | `gov_ladders` **17** · **`EMS_LADDER_GATE` ما زال غيرَ معلَن ⇒ `monitor`** · `gov_ladder_shadow` **صفر صف** | قياس حي + `.env` |
| إصدار سجل الشاشات | `repair01_screen_registry` **813** (653 As-Built + 160 هدفًا) · **صفر بلا مالك** · `repair01_departments` **21** · و**رمزٌ واحدٌ بلا صفِّ إدارة: `PLTF`** (12 شاشة) | قياس حي |

## ① الصفرُ الذي دام ستَّ لقطاتٍ انكسر — ثمَّ رسَب تسليمُه الأوّلُ باسمِه

**«0/58 حدثًا له مستهلكُ أعمال»** كان أثبتَ رقمٍ في هذه السلسلة. اليوم:

| المقياس | اللقطات الستّ | **الآن** |
|---|---|---|
| مفاتيحُ منتَجةٌ لها **مستهلكُ أعمالٍ فعّال** | **0 / 58** | **23 / 58** |
| مفاتيحُ بحارسِ الحوكمةِ وحدَه | 58 | **35** |
| المستهلكُ الجديد | — | `App\Services\Bus\Consumers\EffectLinkConsumer` |

**ولا يُقرأ هذا «الناقلُ يعمل».** المقياسُ الثاني يقول غيرَ ذلك:

| المقياس | القيمة |
|---|---|
| تسليماتُ `effectlink` | **1** |
| حالتُها | ⛔ **`failed`** · `HANDLER_ERROR` |
| نصُّ العطب | `EFFECT_MISSING: settlement.approved — لا رابط مروحة ولا مراة مالية ولا سجل كيان قائم (كيان settlement#5502) — **يعزل باسمه ولا يمر بصمت**` |
| زمنُها | 2026-08-31 **10:40** — أي **داخلَ نافذةِ قياسي** |

⇒ **الحكمُ الدقيق: `Wired` نعم · `Exercised` مرّةً واحدة · `Verified` لا.** والرسوبُ **فضيلةٌ في التصميم**: المستهلكُ يعزل الحدثَ باسمِه بدل أن يمرَّ صامتًا — وهو أوّلُ ما يجب أن يُطلب من مستهلكٍ جديد.

## ② ما أُغلق إغلاقًا حقيقيًّا

| الفجوة | كانت | **الآن** | الشاهد |
|---|---|---|---|
| **GAP-73** الملكيةُ المرتدّة | 4 صفوفٍ تناقض قاعدتَها | ✔ **صفر من 11** — و`SCR-0532` استعاد `W01_SURFACE_REGISTER` | استعلامُ القاعدةِ مقابلَ القيمة |
| **GAP-70** الشجرةُ العاملة | ٥٦ بندًا | ✔ **3 بنود** — وكلُّها أدواتُ فحص | `git status` |
| **GAP-07** توقُّفُ `fx` | 6,613 منذ 2026-08-12 | ✔ **17,985** · محدَّثٌ **2026-08-31 07:18** — **لحق بالبقيّة** | `ems_event_consumers` |
| **GAP-05** استهلاكُ الأحداث | 0/58 | ◐ **23/58 موصولة** · **1 تسليمٌ راسب** | `ems_event_subscriptions` × `ems_event_deliveries` |
| **DLQ** | 26 | ✔ **صفر** | `ems_event_dead_letter` |

## ③ ومقياسُ الإغلاق نزل — ولا انتكاسةَ واحدةً فيه

**22/33 ⇒ 16/33**، وتسعُ شواهدَ حمراءَ من 34. **والفحصُ لكلِّ واحدةٍ يقلب القراءة**:

| الشاهد | نصُّ الرسوب | التشخيص |
|---|---|---|
| `permission_single_point_gate` | «**انخفض إلى 20 من 72** — تُشدُّ السقّاطة بـ`--retighten`» | ⚠ **تحسُّنٌ يُقرأ رسوبًا**: قرّاءُ القرارِ نزلوا 72 ⇒ **20** والسقّاطةُ لم تُشَدّ |
| `injfix02_field_extraction_limit` | «لا ازديادَ — **79 ≤ 77**» | ⚠ **سقّاطةٌ على النموّ** |
| `injfix02_ownership_ruling` | «بلا حسمٍ: **97 ≤ 96**» | ⚠ **سقّاطةٌ على النموّ** |
| `path_rulings` | «لم يكتسب الوهميُّ قارئًا جديدًا — **3 ≤ 2**» | ⚠ **اختبارٌ سالبٌ لم يتحرّك** |
| `bus_stall_alarm` | «الملتحقون: finance · finance_routing · **fx** · **أُنذر خطأً: fx**» | ⚠ **`fx` لحق فصار الإنذارُ كاذبًا** — العطبُ انتقل من التوقُّفِ إلى الإنذار |
| `scheduler_parity` | «غيرُ مجدول: **`cron_events.php`**» | ⛔ **عاملٌ جديدٌ بلا جدولة** |
| `clean_clone_fingerprint` | 3 جداولَ غائبةٍ · **فرقُ أعمدةٍ 40** | ◐ **تحسُّنٌ كبير**: كان 8 جداولَ وفرقَ **3,434** عمودًا |
| `release_and_action_finality` | البصمةُ `4099c94…` ≠ `4e73fa5d…` | ⛔ **الإصدارُ `ux-1.6.0` مسجَّلٌ منذ 2026-08-21** بخمسةِ ملفاتٍ — **بصمتُه لم تُعَد بعد تغيُّرِها** |
| `sensitive_fields_nine_channels` | القناةُ ⑤ — **مسربٌ واحدٌ مسمّى** | ⛔ **باقٍ بحقّ** (`GAP-10` · P0) |

⇒ **الراسبُ بحقٍّ اثنان** (`GAP-10` و`cron_events`) · **وواحدٌ تقادمُ تسجيلٍ** · **وستٌّ سقّاطاتٌ تحتاج شدًّا لا إصلاحًا**.

## تصحيحٌ على قراءتي في اللقطةِ السابقة

قلتُ في `BL-20260828c` إنّ **«الشجرةَ اتّسخت فسقطت البصمةُ»**. **وهذا خطأ.** فحصتُ الفاحصَ اليوم: هو يحسب البصمةَ على **قائمةِ ملفاتٍ مُعلَنةٍ في `gov_component_versions`** (خمسةُ ملفاتٍ للإصدارِ `ux-1.6.0`) **لا على شجرةِ Git**. والدليلُ قاطع: **الشجرةُ اليومَ نظيفةٌ (3 بنود) والبصمتان لم تتغيّرا حرفًا** — نفسُ `4099c94…` ونفسُ `4e73fa5d…`.
⇒ **السببُ الحقيقيّ**: الإصدارُ سُجِّل **2026-08-21** ولم يُعَد إصدارُه بعد تغيُّرِ ملفاتِه — **تقادمُ تسجيلٍ لا اتّساخُ شجرة**. و`GAP-14`/`GAP-26`/`GAP-32` تُقرأ على هذا الأساسِ لا على ذاك.

## أرقام اللقطة المرجعية (كلها بمقامها — كلها من استعلام حي)

المصدر: [`snapshot_measures_h.json`](snapshot_measures_h.json).

- جداول أساسية **987** · مناظر 28 · CHECK **919** · UNIQUE **581** · FK **311** · جداول `repair01_*` **173**.
- أدوار **35** · مستخدمون غير محذوفين **77** · `role_permissions` **4003**.
- الشاشات — **المقامات تُسمّى ولا تُخلط**: السجل الرسمي **813** (**653 As-Built** + 160 هدفًا) · أسطح PHP ممسوحة **809** (620 شاشة) · `nav_items` نشط **2327** · سجل الحزمة **789** · ظهورات المساحات **1261** · `gov_screen_cycle` **1757**.
- الحقول: **14,857** مستخرَجة · **14,856** مرتبطة بمعرّف شاشة رسميّ · **وبالاسم التقنيّ 7,446 من 14,857 (50٫1٪)** — و7,411 موسومةٌ `NEEDS_REVIEW` صراحةً.
- الأحداث **21,546** · **58 مفتاحًا منتَجًا** · **23 لها مستهلكُ أعمال** · التسليمات **21,351** · **DLQ صفر**.
- القيود **6,713** بسطور **13,426** (فرق **0.00**) — **بلا تغيير عبر ثمانِ لقطات** · الطابور منجز **19,592**.
- التسويات **469** · **صفرٌ مدفوع** — بلا تغيير.

## قاعدة عدم الخلط

لا يُخلط قياس من commit آخر أو schema آخر. أي إعادة قياس تُوسم بمعرّف لقطة جديد.
**وفي هذه اللقطةِ تنبيهٌ خاصّ:** المخطَّطُ كبر **34 جدولًا** والقيودُ **88 قيدًا** في ثلاثةِ أيام — فأيُّ رقمٍ من `BL-20260828c` **لا يُقارَن بغيرِ ذكرِ مقامِه القديم**.
