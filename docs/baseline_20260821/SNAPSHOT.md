# BL-20260901b — نقطة القياس المثبَّتة (Snapshot)

**معرّف اللقطة:** `BL-20260901b-6a7a6429` — كل رقم في مخرجات هذه الحزمة الثلاثة مقيسٌ على هذه اللقطة وحدها.
**اللقطات العشر المؤرشفة** في [`historical/`](historical/) — **أرقامها تاريخ ولا تُقرأ حاضرًا**؛ آخرها `BL-20260902-2676dea5`.

> ⚠ **الساعةُ رجعت والرسمُ البيانيُّ تقدَّم — والترتيبُ بالرسمِ لا بالساعة.**
> اللقطةُ السابقةُ حملت طابعَ **2026-09-02 · 01:23**، وهذه تحمل **2026-09-01 · 17:48** — أي **أقدمَ بسبعِ ساعاتٍ ونصف**. ومع ذلك **`2676dea5` سلفٌ مُثبَتٌ لـ`6a7a6429`** (`git merge-base --is-ancestor` ⇒ صفر) و**بينهما ستةٌ وثلاثون التزامًا**. ⇒ **التقدُّمُ حقيقيٌّ والساعةُ كاذبة**، ولذلك حملت اللقطةُ اللاحقةَ `b` لا تاريخًا جديدًا.
> وأوضحُ شاهدٍ داخليّ: أحدثُ الالتزاماتِ طابعُه `09-01 17:42` وأقدمُها في الدفعةِ نفسِها `09-02 06:29` — **ترتيبٌ زمنيٌّ مقلوبٌ داخل دفعةٍ واحدة**.

## نقطة القياس

| البند | القيمة | الدليل |
|---|---|---|
| تاريخ/وقت القياس | **2026-09-01 · 17:48 → 18:40** (UTC+03 بساعةِ الجهاز — **وهي مشكوكٌ فيها**، والترتيبُ بالرسمِ البيانيّ) | `date` + `git merge-base` |
| Environment | تطوير محلي — WAMP على Windows 11 Pro، `C:\wamp64\www\ems` | بيئة الجلسة |
| PHP | **8.2.30** (أدوات القياس) · وساعتُه UTC بفارقِ ثلاثِ ساعات | `php -v` |
| قاعدة البيانات | MariaDB **11.4.9-log** · `equipation_manage` · المنفَذ **3307** | `SELECT VERSION()` |
| Branch | **`repair01/w01-ownership`** (بلا upstream) | `git branch --show-current` |
| commit_hash | **`6a7a6429a903fdfe7b6ce6bc2256ddd773b40088`** — «إعادةُ سبعةِ مصنَّفاتٍ حُذفت بلا قصدٍ في الالتزامِ السابق» | `git rev-parse HEAD` |
| آخر Tag | `pre-build-20260830` — الوصف `pre-build-20260830-211-g6a7a6429` | `git describe --tags` |
| التزامات غير مدفوعة | **442 تجاه `origin/main`** (كانت 406) · **36 التزامًا منذ اللقطة السابقة** | `git rev-list --count` |
| **الشجرة العاملة** | ✔ **نظيفةٌ تمامًا — صفر بند عند تثبيتِ اللقطة**؛ والمتغيّرُ بعدها **مخرجاتُ القياسِ نفسِها** لا كودُ النظام | `git status --porcelain` = 0 |
| ثبات اللقطة | `HEAD` لم يتغيّر بين بدء القياس ونهايته ⇒ لا جزءَ `STALE` | `git rev-parse HEAD` قبل وبعد |
| إصدار الـSchema | **1235 جدولًا أساسيًّا** (+7) + **28 منظرًا** · CHECK **926** · UNIQUE **594** · FK **320** | قياس حي |
| **دفتر الهجرات** | `schema_migrations` **968 صفًّا** (كان 952) · خارج الدفتر **صفر** · البوابة **4/4** بمقاماتٍ نمت (644 · 727 · 968 · 2) | `tools/repair01_migration_gate.php` |
| **إصدار سجل التنقّل** | ⭐ **`EMS_NAV_ARCH=on`** — التصييرُ الحيُّ عبر `includes/navarch_renderer.php` · `nav_placements` **413** · `nav_targets` **413** · `nav_items` نشط 2476 · `link_groups` 2635 | `.env:151` + قياس حي |
| إصدار محرك الاعتماد | `gov_ladders` **17** · **`EMS_LADDER_GATE` ما زال غيرَ معلَن ⇒ `monitor`** · `gov_ladder_shadow` **صفر صف** | `.env` + قياس حي |
| إصدار سجل الشاشات | `repair01_screen_registry` **910** · **750 As-Built** + 160 هدفًا · **صفر بلا مالك** | قياس حي |
| الناقل | `ems_business_events` **21,627** · التسليمات **21,418** · `EffectLinkConsumer` **11 كلُّها `dlq`** — **بلا تغيير** | قياس حي |

## ثلاثةُ تحوُّلاتٍ حاكمةٍ في هذه اللقطة

### ① القلبُ وقع: السايدبارُ صار يُصيَّر من معماريةٍ جديدةٍ حيّة

**`EMS_NAV_ARCH=on`** في `.env:151` — وهذا **ليس ادّعاءَ وثيقةٍ بل عَلَمٌ مقروءٌ من ملفَّين حيَّين** (`includes/navarch_renderer.php` و`includes/unified_nav.php`). وأثرُه مقيسٌ في تقريرِ الجولةِ نفسِها **قبل/بعد لكلِّ دور**:

| الدور | قبلُ | **بعدُ** | الفارق |
|---|---:|---:|---:|
| التشغيل | 90 | **31** | **−59** |
| المالية | 113 | **36** | **−77** |
| الأسطول | 77 | **49** | −28 |
| الموردون | 63 | **35** | −28 |

⇒ **الجوابُ عن «لماذا يرى هذا الدورُ هذه الشاشة؟» تغيّر مصدرُه**: صار من `nav_placements` (413) و`nav_targets` (413) بدل توليدِ الروابطِ من `nav_items` وحدَها. **والانخفاضُ ليس فقدانًا بل حذفُ تكرارٍ وضبطُ نطاق** — ومعناه أنّ أيَّ رقمٍ سابقٍ عن «عدد روابط الدور» **لا يُقرأ اليوم**.

### ② والقلبُ نفسُه فتح قارئَ قرارٍ خارجَ النقطةِ الواحدة

الحاجبُ `injfix01_permission_single_point_gate` رسَب بنصٍّ **يسمّي الملفَّ**:

> `✘ صفرُ قارئِ قرارٍ جديدٍ خارجَ النقطةِ الواحدة — 1 — includes/navarch_renderer.php` · `✘ لا ازديادَ — 21 ≤ 20`

والقراءةُ من الشيفرةِ نفسِها (`navarch_renderer.php:58-66`): الدالّةُ `navarch_authorized_routes()` تستعلم `role_permissions` **باستعلامٍ خام** عبر `perm_nav_view_exists_sql()` بدل المرورِ بنقطةِ القرارِ المركزيّة. **والملفُّ يعلن اختيارَه صراحةً في تعليقِه**: «`role_permissions` … **مصدرُ تفويضٍ لا مصدرُ ظهور**».

⇒ **مسارٌ مزدوجٌ مُعلَنٌ لا حادثٌ صامت** — لكنّه **يُعيد فتحَ `GAP-11`** بحكمِ المقياسِ نفسِه (قرّاءُ القرارِ 20 ⇒ **21**). **والقرارُ قرارُ مالكٍ**: إمّا يُمرَّر عبر النقطةِ الواحدة، أو يُعلَن استثناءً مكتوبًا في الحاجبِ فيُستثنى بالاسم.

### ③ المقاييسُ الثلاثةُ افترقت — وهذا أوّلُ افتراقٍ من نوعِه

| المقياس | اللقطةُ السابقة | **الآن** | القراءة |
|---|---|---|---|
| مقياسُ الإغلاق | 23/33 | **22/33** | ⛔ **انتكاسةٌ واحدةٌ حقيقية**: `GAP-11` — والشواهدُ 32/35 ⇒ **31/35** |
| المتطلباتُ المُغلقةُ بدليل | 33/71 = 46.5٪ | **33/77 = 42.9٪** | ⚠ **البسطُ ثابتٌ والمقامُ نما ستةً** (`OPEN` 3 ⇒ **9**) — **نزولٌ بلا تراجعِ عمل** |
| بوابةُ الهجرات | 4/4 على 628 | **4/4 على 644** | ✔ **صمدت على مقامٍ نما ثانيةً** |

⇒ **ولا يُقال «النظام تراجع»**: مقياسٌ نزل بانتكاسةٍ مسمّاة، وآخرُ نزل **لأنّ المطالبَ الجديدةَ أُعلنت** — وإعلانُ مطلبٍ جديدٍ تقدُّمٌ في الحوكمةِ لا تراجعٌ في البناء.

## أرقام اللقطة المرجعية (كلها بمقامها — كلها من استعلام حي)

المصدر: [`snapshot_measures_j.json`](snapshot_measures_j.json).

- جداول أساسية **1235** · مناظر 28 · CHECK **926** · UNIQUE **594** · FK **320** · دفتر الهجرات **968**.
- أدوار **35** · مستخدمون غير محذوفين **77** · `role_permissions` **4112** · **وقرّاءُ القرارِ خارجَ النقطةِ الواحدة 21**.
- الشاشات — **المقامات تُسمّى ولا تُخلط**: السجل الرسمي **910** (**750 As-Built** + 160 هدفًا) · أسطح PHP ممسوحة **906** (717 شاشة) · `nav_items` نشط **2476** · **`nav_placements` 413** · سجل الحزمة **886** · ظهورات المساحات **1389** · `gov_screen_cycle` **1950**.
- الحقول: **16,138** مستخرَجة · **16,137** مرتبطة بمعرّف شاشة رسميّ · **8,775 بالاسم التقنيّ (54٫4٪)** · و**7,363 موسومةٌ `NEEDS_REVIEW`** وكلُّها أعمدةُ جداول.
- الأحداث **21,627** · **58 مفتاحًا منتَجًا** · **23 له مستهلكُ أعمالٍ موصول** · التسليمات **21,418** · **37 صفًّا حالتُه `dlq`** بينما `ems_event_dead_letter` **صفر** · القيود **6,713/13,426** بفرق **0.00** · الطابور منجز **23,808**.
- الحوكمة: `gov_field_class` **2411** · الحقول الحساسة بسياسة **34** · التسويات **469 بصفرٍ مدفوع**.

## قاعدة عدم الخلط

لا يُخلط قياس من commit آخر أو schema آخر. أي إعادة قياس تُوسم بمعرّف لقطة جديد.
**وقاعدةٌ زائدةٌ فرضتها هذه اللقطة:** **لا يُرتَّب شيءٌ بساعةِ الجهاز** — لا الالتزاماتُ ولا اللقطات. **الترتيبُ بالرسمِ البيانيِّ (`merge-base`) وبالمعرِّفِ التسلسليّ**، وأيُّ حكمِ «أحدثُ/أقدم» مبنيٍّ على طابعِ وقتٍ يُوسَم `NEEDS_REVIEW`.
