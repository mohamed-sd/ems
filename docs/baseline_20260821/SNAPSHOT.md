# BL-20260828c — نقطة القياس المثبَّتة (Snapshot)

**معرّف اللقطة:** `BL-20260828c-96a640e1+WT` — كل رقم في مخرجات هذه الحزمة الثلاثة مقيسٌ على هذه اللقطة وحدها.
**اللقطات الستُّ السابقة** في [`historical/`](historical/) — **أرقامها تاريخ ولا تُقرأ حاضرًا**.

> **لماذا اللاحقة `+WT`:** `HEAD` **لم يتغيّر** عن اللقطة السابقة (`96a640e1` نفسه)، لكنّ الشجرةَ العاملةَ فيها **٥٦ بندًا غيرَ ملتزم** — **٣٩ منها جولةُ `RPR-02-A`** (18:20 → 19:53) التي وقعت **خارجَ أيِّ التزام** وكتبت في قاعدةِ البيانات، **و١٧ مخرجاتُ حزمتي أنا** من نافذةِ التصحيحِ السابقة. **والقسمةُ تُذكر كي لا يُنسَب إلى الجولةِ ما ليس منها.**
> ⇒ **السطحُ المقيسُ اليوم لا يُعاد إنتاجُه من `96a640e1` وحده.** فالمعرّفُ يحمل اللاحقةَ إعلانًا لا زينة، ومن يستنسخ الالتزامَ لا يحصل على ما قِسته.

## نقطة القياس

| البند | القيمة | الدليل |
|---|---|---|
| تاريخ/وقت القياس | **2026-08-28 · 21:25 → 22:10** (UTC+03) | `date` + طوابع الاستخراج |
| Environment | تطوير محلي — WAMP على Windows 11 Pro، `C:\wamp64\www\ems` | بيئة الجلسة |
| PHP | **8.2.30** — **وساعتُه UTC** بفارقِ ثلاثِ ساعاتٍ عن ساعةِ النظام | `php -v` · مقارنةُ الطوابع |
| قاعدة البيانات | MariaDB **11.4.9-log** · `equipation_manage` · **المنفَذ 3307** (لا 3306) · الخدمة `wampmariadb64` | `SELECT VERSION()` · `netstat` |
| Branch | **`repair01/w01-ownership`** (بلا upstream) | `git branch --show-current` |
| commit_hash | **`96a640e150b2eac84904f5d1fc1616fde08cdf0e`** — **نفسُه في اللقطة السابقة** | `git rev-parse HEAD` |
| آخر Tag | `inj-exec-01-start` — الوصف `inj-exec-01-start-177-g96a640e1` | `git describe --tags` |
| التزامات غير مدفوعة | **70 تجاه `origin/main`** — ولا upstream للفرع | `git rev-list origin/main..HEAD --count` |
| **الشجرة العاملة** | ⛔ **٥٦ بندًا غيرَ ملتزم** (كانت **صفرًا**) — **٣٩ من جولةِ `RPR-02-A` و١٧ من مخرجاتي** — والمَحاجُّ: `tools/` و`docs/` **وحدهما**؛ **صفرُ تعديلٍ** في `app/` أو `includes/` أو `database/` أو `tests/` | `git status --porcelain` |
| ثبات اللقطة | `HEAD` لم يتغيّر أثناء القياس ⇒ لا جزءَ `STALE` بسببِ التزام؛ **والخطرُ من الشجرةِ لا من الالتزام** | `git rev-parse HEAD` قبل وبعد |
| إصدار الـSchema | **953 جدولًا أساسيًّا** + **28 منظرًا** · CHECK **831** · UNIQUE **564** · FK **311** — **بلا تغيير** | قياس حي |
| **دفتر الهجرات** | `schema_migrations` **703 صفًّا** · خارج الدفتر **صفر** · البوابة **4/4 خضراء** شغّلتُها بنفسي | `tools/repair01_migration_gate.php` |
| إصدار سجل التنقّل | `nav_items` نشط **2096** · `link_groups` نشط **1768** · `modules` **637** — بلا تغيير | قياس حي |
| إصدار محرك الاعتماد | `gov_ladders` **17** · **`EMS_LADDER_GATE` ما زال غيرَ معلَن ⇒ `monitor`** · `gov_ladder_shadow` **صفر صف** | قياس حي + `.env` |
| إصدار سجل الشاشات | `repair01_screen_registry` **783** (623 As-Built + 160 هدفًا) · **صفر بلا مالك** · `repair01_departments` **21 ⇒ 22** | قياس حي |

## ثلاثةُ تحوُّلاتٍ حاكمةٍ في هذه اللقطة

### ① الشجرةُ اتّسخت ثانيةً — والبصمةُ سقطت معها، وأسقطت ثلاثَ فجواتٍ دفعةً واحدة

`RPR-0c` في اللقطةِ السابقةِ نظّف الشجرةَ بمبرَّرٍ صريح: «**بصمةٌ تصف حالةً منقضيةً لا تصف شيئًا**». والجولةُ التاليةُ تركتها **٥٦ بندًا**، فرسَب فاحصُ البصمةِ فورًا:

> `✘ البصمةُ المحسوبةُ تطابق المسجَّلة — 4099c94… ≠ 4e73fa5d…`

وهذا الفاحصُ وحدَه **يصرّح بثلاثِ فجوات** (`GAP-14` · `GAP-26` · `GAP-32`) ⇒ **حكمٌ واحدٌ راسبٌ أسقط ثلاثةَ أحكام**. ⇒ **العلاجُ عاد قبل أن يُحرَس**: نظافةُ الشجرةِ ليست عادةً بل شرطًا لم يُثبَّت بحاجب.

### ② أربعُ شاشاتٍ ارتدَّت ملكيتُها — والسجلُّ يناقض نفسَه بنصِّه

أربعةُ صفوفٍ في السجلِّ الرسميِّ **تحمل قاعدةَ حكمِها مكتوبةً**: «RPR-W15 §٤·٢ · **إعادةُ ملكيةٍ من مكتبِ الرئيسِ إلى إدارتِها**» — **ومالكُها اليوم `EX-CEO`**، أي **عادت إلى ما نصَّت القاعدةُ على إخراجِها منه**:

| الشاشة | المالك قبل | المالك الآن |
|---|---|---|
| `SCR-0505` `Reports/approval_lag_report.php` | DEP-05 | **EX-CEO** |
| `SCR-0508` `Reports/daily_units_report.php` | DEP-11 | **EX-CEO** |
| `SCR-0515` `Reports/margin_report.php` | DEP-05 | **EX-CEO** |
| `SCR-0532` `Risk/risk_dept_ceo.php` | DEP-09 | **EX-CEO** |

**والمقياسُ يُلتقط بسؤالٍ واحد:** القاعدةُ مكتوبةٌ في **11** صفًّا · **7 منها تُطيعها** · **4 تناقضها**.
وزيادةً: `SCR-0532` تغيّرت قاعدةُ ملكيتِه **`W01_SURFACE_REGISTER` ⇒ `W10_SPLIT:W10_PARENT_DEFAULT`** — **حكمُ W01 دُهس بافتراضِ W10**، وهو العطبُ المسجَّلُ سلفًا في ذاكرةِ المشروع. **وأثرُ الطوابع**: التغييرُ وقع **19:07 → 19:09**، ومِلَفُّ `tools/repair01_ingest.php` حُفظ **19:08:55**.

### ③ تقريرُ «قبل/بعد» أعلن صفرَ فرقٍ — لأنّ مقامَه لا يشمل الجدولَ الذي تغيّر

`RPR02A_REINGEST.md` يعرض **تسعةَ عشرَ مقياسًا كلُّها Δ=0**. وهو **صادقٌ فيما عدَّ**: `source_files` · `decisions` · `departments` · `crosswalk` · `surfaces` · `target_gaps` · `requirements` · `fields`.
**ولا يعُدُّ `repair01_screen_registry` أصلًا** — وهو الجدولُ الذي تغيّر فيه **52 صفًّا**. ⇒ **«صفرُ فرقٍ» يصدُق على المقامِ ويكذب على السؤال.**

## أرقام اللقطة المرجعية (كلها بمقامها — كلها من استعلام حي)

المصدر: [`snapshot_measures_g.json`](snapshot_measures_g.json).

- جداول أساسية **953** · مناظر 28 · **بعمود `company_id` 657 من 953** (وبعدِّ المناظرِ معها **671** — **مقامان لسؤالٍ واحدٍ فسُمِّيا**) · CHECK **831** · UNIQUE **564** · FK **311**.
- أدوار **35** · مستخدمون غير محذوفين **77** · `role_permissions` **3757**.
- الشاشات — **المقامات تُسمّى ولا تُخلط**: السجل الرسمي **783** (**623 As-Built** + 160 هدفًا) · أسطح PHP ممسوحة **778** (590 شاشة) · `nav_items` نشط **2096** · سجل الحزمة **758** · ظهورات المساحات **1200** · دفتر ترحيل الشاشات **663** · `gov_screen_cycle` **1224**.
- الحقول: **14,609** مستخرَجة · **14,608** مرتبطة بمعرّف شاشة رسميّ · **7,225 فقط بالاسم التقنيّ (49٫5٪)**.
- الأحداث **21,546** · **58 مفتاحًا منتَجًا** · القيود **6,713** بسطور **13,426** (فرق **0.00**) · الطابور منجز **16,208**.
- سجلّاتُ REPAIR01: متطلبات **433** · قرارات **125** · حقول **7,591** · فجوات هدف **335** · أسطح **664** · ملكيات **985**.

## قاعدة عدم الخلط

لا يُخلط قياس من commit آخر أو schema آخر. أي إعادة قياس تُوسم بمعرّف لقطة جديد.
**وفي هذه اللقطة قاعدةٌ زائدة:** ما دامت الشجرةُ متّسخةً، فأيُّ رقمٍ هنا **يصف شجرةً عاملةً لا التزامًا** — ومن أراد إعادةَ إنتاجِه يحتاج الشجرةَ نفسَها لا الالتزامَ وحده.
