# FINDINGS — ما اكتُشف أثناء استخراج BL-20260821b (سُجّل ولم يُصلَح)

> **قاعدة الجولة محفوظة:** «استخراج الحقيقة أولًا — لا إصلاح أثناء القياس». **صفر تعديل على كود النظام أو بياناته**. ما أُضيف: أدوات قراءة (`tools/baseline_db_dump3.php` · `baseline_db_dump4.php`) ومخرجات في `docs/baseline_20260821/`.
> سجل الجولة السابقة (F-01..F-37) محفوظ في [`historical/BL-20260821-f0bc3e4e/FINDINGS.md`](historical/BL-20260821-f0bc3e4e/FINDINGS.md) — **ما وُسم هناك «مفتوحًا» لا يُقرأ اليوم دون مراجعة عمود «الحالة الآن»** في INJ-CURRENT-STATUS §4.

## أولًا — اكتشافات جديدة في هذه الجولة

| # | الاكتشاف | Evidence | GAP |
|---|---|---|---|
| **F-B01** | **نمط «الهجرة خارج الدفتر» تكرّر**: الـ35 السابقة قُيِّدت (baseline 52→102)، و**15 هجرة جديدة** (`2027_08_31`→`2027_09_14`) مطبَّقة فعليًّا وخارج `schema_migrations`. الجسّ الوظيفي أثبت التطبيق: `gov_ownership_rulings`=259 · `gov_nav_reference_standard`=17 · `ems_event_delivery_orphans`=82 · `gov_path_rulings`=6 · `gov_finance_gate_policy`=9 · `gov_delegation_state`=5 · `gov_sensitive_policy_debt`=6 · `gov_topbar_exemptions`=2 | مقارنة glob×ledger + جسّ | GAP-18 |
| **F-B02** | **Baseline حالية مكرّرة**: `docs/ARCHITECTURE_CURRENT_SYSTEM_v26_ar.md` (غير متتبَّع) مقيسة على اللقطة السابقة `f0bc3e4e` وتصف نفسها «As-Built … أيُّ تعارض: v26 يغلب» — بينما هذه الحزمة مقيسة على `dbba1ef0`. **وثيقتان تدّعيان الحاضر بلقطتين مختلفتين** | `git status` + ترويسة v26 | GAP-55 |
| **F-B03** | **«الالتزامات غير المدفوعة» رقم بلا مقام في v26**: قالت 263؛ والصحيح **43** تجاه upstream الفرع و**306** تجاه `origin/main` — و263 كانت قياس `main` عند اللقطة الأقدم. ثلاثة أرقام صحيحة بثلاثة مقامات | `git rev-list @{u}..HEAD` · `origin/main..HEAD` | — |
| **F-B04** | **عيب في عُدّة القياس نفسها**: `tools/baseline_db_dump.php` يبني `db_tables` باستعلامين **مترابطين** لكل جدول — تعثّر **أكثر من نصف ساعة** عند 618 جدولًا وأُوقف. الصيغة التجميعية أنتجت الجدول نفسه في **5.9 ثانية**. (أُضيف `baseline_db_dump4.php` بديلًا؛ لم يُحذف الأصل) | قياس زمني | — |
| **F-B05** | **العطب استُنسخ في علاجه**: `gov_event_rulings` (58 حكمًا) أُنشئ لعلاج «سجل بلا قارئ» — و**هو نفسه بلا قارئ إنتاج**: قرّاؤه هجرتان وأداة قياس وفاحصان. و`handler_class` فيه = `GovernanceWatchConsumer` للـ58 جميعًا | مسح مراجع | GAP-51 |
| **F-B06** | **إنذار توقف المستهلكين مبنيٌّ ومبرهَنٌ وغيرُ موصول**: `EventDispatcher::alertStalledConsumers()` موصولة في `cron_events.php:144` — و`cron_events.php:16` متقاعد يدويًّا (`ems_manual_run_retired` يخرج بالرمز 3 ما لم يُعرَّف `EMS_JOB_WORKER`، **ولا ملف في المستودع يعرّفه**)، والمهمة المجدولة تشغّل `EventDeliveryWorker` الذي لا يذكر هذه الدوال. الشاهد الحاسم: **صفر إشعار `[BUS-UNWIRED:*]`** رغم **74 اشتراكًا نشطًا غير موصول** | استعلام `fin_notifications` + مسح | GAP-52 |
| **F-B07** | **`EMS_cron_events` يُرجع `Last Result = 3` باستمرار** — خروج غير صفري متكرر **خارج الطابور** فلا يظهر في `ems_job_queue` ولا في إنذاراته | `schtasks /query /v` | GAP-53 |
| **F-B08** | **13 موضع ابتلاع في بناء سطور التسوية**: `SettlementService.php` (الأسطر 178, 271, 285, 320, 352, 364, 388, 450, 476, 496, 674, 686, 896) موسومة `ems_catch_ignored()` في مسارات السُّلف وقطع الغيار والصيانة والنقل وجزاء الطاقة ⇒ **تسوية ناقصة السطور بصمت**. مسار مختلف عمّا سدّه GAP-16 | قراءة الكود | GAP-54 |
| **F-B09** | **تلوّث `consumer_key` نصف منظَّف**: 20 → **10**. الحجر حيَّد عمود `consumer` وحده وترك `consumer_key` يحمل جملًا عربية («مطابقٌ للمستند المرفق · UAT-2026») في صفوف `published` — لا يلتقطها العامل ولا صندوق الموتى | `WHERE consumer_key REGEXP '[^ -~]'` | GAP-09 |
| **F-B10** | **`fx` لم يتحرك**: المؤشر عند 6,613 من 17,985 و`updated_at=2026-08-12 05:31:59` — **تسعة أيام**. الجديد أنه صار مصنَّفًا `orphan` ومُنذَرًا عنه بالاسم — لكن الإنذار لا يعمل تلقائيًّا (F-B06) | `ems_event_consumers` | GAP-07 |
| **F-B11** | **كانس التصعيد في `tools/` وغير مجدول**: `tools/injfix01_approval_escalation_sweeper.php` مكتوب بإتقان (يقرأ `escalate_after_hours`، عاطل الأثر، يستثني بذر UAT) — و`ems_job_schedule` ثماني مهام **بلا واحدة للتصعيد**، و**44 خطوة معلَّقة أقدمها 2025-08-24** | استعلام + جدولة | GAP-04 |
| **F-B12** | **الإنذار صار يعمل بحق فكشف تأخُّرًا حقيقيًّا**: `statement_build` متأخرة ~44 ساعة و`settlement_recalc` ~42 ساعة، وكلتاهما فوق عتبة 108,000ث. (قبل توسيع العمود SMALLINT→INT كانتا صامتتين) | `ems_job_schedule` | GAP-17 نتيجةً |
| **F-B13** | **صفر حركة مالية منذ 2026-08-16**: أحدث حدث 12:17 وأحدث قيد 13:17 — خمسة أيام. و187 مسوَّدة عالقة أقدمها 2026-07-18 | `MAX(created_at)` | — |
| **F-B14** | **نمط «السقّاطة» يجمّد الدين ولا يغلقه**: أربعة فواحص ترسُب **عند النقصان أيضًا** (`72 ≤ 72` قارئ قرار · `608 ≤ 608` ملف SQL خام) بمبرّر معلن «سقّاطةٌ لا تُشدّ تصير سقفًا يُنسى» ⇒ تقدُّم في الحوكمة لا في الوصول | نصوص الفواحص | GAP-11/29 |
| **F-B15** | **تلوّث باقٍ في `approval_steps`**: أربعة صفوف تحمل **نصًّا عربيًّا في خانة `role_required`** («بانتظار اعتماد الإدارة المختصة · UAT-2026» …) — الحجر عالج `approval_workflow_rules` (20 صفًّا إلى `approval_rules_quarantine`) ولم يمسّ هذا الجدول | `GROUP BY role_required` | GAP-09 |
| **F-B16** | **`fin_party_scope` ما زال fail-open — والانفتاح مستهلَك حيًّا لا نائمًا**: `Finance/dues_fin.php:256` و`Finance/payments_fin.php:287` ⇒ الدور غير المذكور يرى **ذمم ومدفوعات كل الأطراف** (رواتب موظفين ومستحقات موردين معًا) وهو الخلط الذي يمنعه توثيق الدالة نفسها. والمقلوب فعلًا هو جاره `fin_project_scope` **في الملف نفسه** — فالإصلاح كان موضعيًّا لا مبدئيًّا. والالتزام الذي ظُن أنه يخصّه (`25528ddb`) **لا يمسّ أي ملف إنتاج** بل سقّاطة تصنيف مساحات، ونصّه يقول صراحةً «ولم تُقلَب إلى الإغلاق الافتراضي» | `fin_helpers.php:167` + مستهلكان + `git show --stat` | GAP-22 (رُفعت إلى P1) |
| **F-B17** | **`gov_data_classes id=61` حُجر لا حُذف** (`active=0`) — والحجر **نافذ فعليًّا** لأن `FieldGovernor::classOf()` تشترط `JOIN … c.active=1`؛ لكن النص الملوّث باقٍ في `create_roles/edit_roles/read_roles` | استعلام + `FieldGovernor.php:104` | GAP-09 |
| **F-B18** | **62 شاشة على القرص خارج كل السجلات** (كانت 63) — والمالك المجهول 112 (كان 213) والتعارض 13 (كان 126) بعد جولة أحكام الملكية | reconcile_stats | GAP-20 |
| **F-B19** | **`governance_watch` اتّسع 68 → 70 مفتاحًا** — فحارس «لا حدث بلا مستهلك» يزداد إشباعًا بالحارس نفسه، والمفاتيح المشترَك عليها ولا تُنتج ارتفعت **10 → 16** | استعلام `event_consumers` | GAP-05 |
| **F-B20** | **منحة نافذة واحدة تشير إلى قالب `draft`** فتسقط لمسار `role_permissions` ⇒ تغطية القوالب 75/78 لا 76/78 | استعلام المنح × القوالب | GAP-11 |
| **F-B21** | **أخطر اكتشاف الجولة: أداة الإغلاق تقيس الشيء الخطأ.** `tools/injfix01_gap_coverage.php:37` يعدّ الفجوة «مغطّاة» إن **ذكر رمزَها** فاحصٌ أخضر — فأنتجت «33/33» بينما فاحص GAP-21 **ينفي الإغلاق بنصّه** (`:116-117`: «وسدُّه عملٌ مجدوَلٌ لا يُدَّعى إنجازُه هنا»)، وGAP-14 لم يُمَسّ كودُه أصلًا | الكود `:37-40` + نصوص الفواحص | GAP-56 |
| **F-B22** | **`GAP-14` أُعلن مُغلقًا وهو لم يُمَسّ**: `git log f0bc3e4e..HEAD -- includes/action_guard.php` **فارغ**، والعيب في `:232`/`:246` حرفًا كما في الأساس. ما جرى ردمُ بيانات في `guard_denials` لا إصلاح كود | git log + grep | GAP-14 |
| **F-B23** | **تشخيص GAP-07 السابق مردود**: ليس «مستهلك توقّف» بل **لا معالج صرفٍ مسجَّلًا أصلًا** (`register()` يسجّل مستهلكَين والجدول أربعة) — والصفّان اليتيمان `enabled=1` يولّدان `[BUS-STALL]` كل ساعة إلى الأبد | `d487b37c` + `fin_notifications` | GAP-07/57 |
| **F-B24** | **الفوترة غائبة لا «غير مقيسة»**: أنماط `ar_*`/`ap_*`/`invoice*` = **صفر مطلق على القرص** ⇒ رحلة الإيراد تسقط عند الفوترة. و184 ملفًّا في دفتر الدورة لا وجود لها على القرص (يبقى 53 بعد المصالحة) | تدقيق الملحق NF-01 | GAP-34..50 |
| **F-B25** | **GAP-24 كان مُغلقًا ولم يُقرأ كذلك**: السلطة حُسمت **بعدد قرّاء الإنتاج** ووُسم الحكم في **تعليق الجدول نفسه** — `fin_financial_periods` «سلطة الإقفال الوحيدة» و`scr_monthly_close` «ليس سلطة الإقفال». (تصحيح على تقديري الأولي في هذه الجولة) | `TABLE_COMMENT` | GAP-24 |
| **F-B26** | **تصادم ترقيم الفجوات**: جولة الإصلاح تملك سجلًّا رسميًّا GAP-27..33 والملحق يحجز 34..50 — فترقيمي الأولي (27..32) كان يتصادم معها. **حُسم**: فجوات هذا القياس تبدأ من GAP-51 | `MANIFEST.v8.json` | — |

## ثانيًا — تصحيحات على قياس الجولة السابقة (لا تغيُّرات في النظام)

| # | التصحيح | لماذا وقع الخطأ |
|---|---|---|
| **C-01** | **`gov_field_class` (630) كان يحكم العرض فعلًا** — عبر `includes/u13_screen_kit.php:46-69` ثم `:66` «بلا صنفٍ = لا يُصيَّر»، مستهلَكة من ~32 شاشة إنتاج. القياس السابق قال «مسار التحرير فقط» لأنه أغفل هذه العُدّة. **العُدّة أقدم من جولة الإصلاح** (آخر مسّ `f07790f4`) — فهذا **تصحيح قياس لا مكسب جولة** | البحث اقتصر على `FieldGovernor` ولم يشمل عُدَّة التصيير |
| **C-02** | **مقام «ملفات SQL الخام» كان خاطئًا**: 1,257 كانت تعدّ `tests/` و`tools/` و`database/migrations/` و`seeds/` أسطحَ تسريب. المقام الصحيح **608 من 983 ملف إنتاج ممسوح** | مسح بلا استبعاد غير الإنتاج |
| **C-03** | **«التصعيد بلا كود» مردود جزئيًّا**: التصعيد حي في ثلاثة مسارات (`work_escalations` · `ticket_escalations` · `risk_escalations`) وأحدها يقرأ `escalate_after_hours` فعلًا (`Tickets/cron_tickets.php:112`) — لكنه **تصعيد تذاكر لا اعتماد** ولا يمسّ `approval_steps` | خلط بين عمود بنفس الاسم في جدولين |
| **C-04** | **الشريط العلوي عشرة مسارات لا تسعة** — العاشر كشفه فاحص الجولة | العدّ اليدوي للروابط |

## ثالثًا — إصلاحات P0 أثناء القياس

**لا شيء.** لم يُكتشف عطل يهدد سلامة البيانات لحظيًّا بدرجة توجب كسر قاعدة «لا إصلاح أثناء القياس». أقربها GAP-52 (إنذار مبنيّ غير موصول) وGAP-07 (توقف `fx` منذ تسعة أيام) — وكلاهما حالة مستقرة، وتشخيصهما أول بنود ما بعد الاعتماد.
