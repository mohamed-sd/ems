# FINDINGS — ما اكتُشف أثناء استخراج BL-20260822 (سُجّل ولم يُصلَح)

> **قاعدة الجولة محفوظة:** «استخراج الحقيقة أولًا — لا إصلاح أثناء القياس». **صفر تعديل على كود النظام أو بياناته**. الاستثناء الوحيد المعلَن: **عيب في عُدّة القياس نفسها** أُصلح (F-C04 أدناه) — وهو أداة قراءة لا نظام.
> سجلّا الجولتين السابقتين في [`historical/`](historical/) — **ما وُسم هناك «مفتوحًا» لا يُقرأ اليوم دون مراجعة §4 من INJ-CURRENT-STATUS**.

## أولًا — اكتشافات هذه الجولة

| # | الاكتشاف | Evidence | GAP |
|---|---|---|---|
| **F-C01** | **أخطر اكتشاف الجولة — «مُثبَت بالمقياس، مُعطَّل بالعَلَم»**: `gov_journey_ladders` يقول 14/14 `ladder_wired=1` بنصّ «موصولٌ فعلًا: سلّمُه يُقرأ من نقطةِ قرارٍ حيّةٍ قبلَ الكتابة» — **وهذا صحيح مقيسًا**. لكن `ems_ladder_mode()` تقرأ `EMS_LADDER_GATE` بافتراض `'monitor'`، و**المتغيّر غير معلَن في `.env`** (المعلَن `EMS_APPROVAL_RULES` و`EMS_SUBSTITUTE_LADDER` فقط) ⇒ `ems_ladder_guard()` **يُسجّل الخرق ويعيد `ok=true`** فتمضي الكتابة في **10 مواضع إنتاج**. الحجب 422 في `enforce` وحده. ⇒ **`ladder_wired` علمُ «يُقرأ» لا علمُ «يمنع»** | `unit_chain_helpers.php:290` · `:411-417` · `grep -iE "LADDER" .env` | GAP-59 |
| **F-C02** | **المحطات الغائبة بُنيت — وكلها صفر صف**: `ar_claim_invoices` · `ar_completion_certs` · `ar_accruals` (الفوترة التي كانت «صفرًا مطلقًا على القرص») · `tre_pay_batches`+`tre_pay_batch_lines`+`tre_beneficiaries` · `sal_quotation_lines`+`sal_quotation_revisions`+`sal_client_needs` · `sup_violations` · `unit_corrections` · `unit_final_approvals` · `gov_ladder_decisions`. **عشرة جداول جديدة بصفر صف** — الرحلة تحوّلت من FAIL (غائبة) إلى BLOCKED (مبنيّة غير مُمارَسة) | عدّ الصفوف + `CREATE_TIME` | GAP-61 |
| **F-C03** | **دَين الدفترة أُغلق بقاعدة لا بإدخال**: `database/migrations/_ledger.php` يوفّر `ems_migration_recorded()` **ذرّية** تكتب البصمة والزمن والمنفِّذ **وتُحدِّث الصفَّ إن تغيّر محتوى الملف بعد التطبيق** ⇒ **صفر هجرة خارج الدفتر** (كانت 35 ثم 15) و**518 `applied`** بدل 476. (`_ledger.php` نفسه عونٌ لا هجرة، فظهوره في «المعلَّق» ليس دَينًا) | مقارنة glob×ledger | GAP-18 مُغلق |
| **F-C04** | **عيب عُدّة القياس أُصلح**: `tools/baseline_db_dump.php` كان يبني `db_tables` باستعلامَين **مترابطَين** لكل جدول فتعثّر أكثر من نصف ساعة عند 618 جدولًا (F-B04 في الجولة السابقة). صيغة التجميع أنتجت الناتج نفسه في **ثوانٍ** — وأُدخلت في الأصل هذه المرة لا في بديل | قياس زمني | — |
| **F-C05** | **`GAP-14` مُعلَن مُغلقًا وهو مفتوح** — للمرة الثانية: نصّ الإغلاق «لا سجلَّ رفضٍ بفعلٍ فارغ — الفعلُ إلزاميٌّ بقيد»، والقيد الفعلي `chk_denial_ref_present` يحرس **`attempted_ref`** — و**لا عمود `verb` في `guard_denials` أصلًا** (أعمدته: `deny_id, company_id, guard_code, person_id, attempted_ref, reason_code, at`). والكود: `$verb` مستعمَل في `action_guard.php:232` ومعرَّف في `:246` بلا تغيير | `SHOW COLUMNS` + `CHECK_CONSTRAINTS` + grep | GAP-14 |
| **F-C06** | **دَين قرّاء القرار نما ولم ينحسر**: كان 72 وصار **87** — والفاحص **أخضر** لأن معياره ضُيِّق إلى «لا محرّك محليًّا يوازيها» بدل «تمرير الكل عبر النقطة الواحدة». والفاحص يعلن في متنه سبب عدم التحويل: «تحويلُ قارئِ صلاحيةٍ يغيّر مَن يرى ماذا على نظامٍ حيّ» | تشغيل `injfix01_permission_single_point_gate.php` | GAP-11 |
| **F-C07** | **حظر الصيغ نُفِّذ حيث تنظر البوابة ولم يُنفَّذ في المصدر**: 14 صيغة ممنوعة في التنقّل والسجل الكنسي بصفر إصابة — و**9 منها حيّة في `gov_screen_cycle` في 24 صفًّا**. وNF-27 ينصّ: «كنسها قبل تفعيل الاشتقاق وإلا وُلد السايدبار المشتق بالأسماء الممنوعة» ⇒ **الخضرة اليوم صحيحة وتنقلب حمراء لحظة التفعيل** | المراجعة العكسية `f330a975` | GAP-60 |
| **F-C08** | **مسار إعادة البناء بالهجرات غير قابل للسلوك**: `migrate.php status` يقول **`missing=241`** — 241 هجرةً مسجَّلةً مطبَّقةً بلا ملف على القرص ولا في الأرشيف ⇒ «انسخ المستودع ثم شغّل الهجرات» **مستحيل**، والمسار الوحيد هو المُثبِّت من دمب المخطَّط والبذرة. وقد **أُثبت حيًّا**: 664 كائنًا على قاعدة خِلْو والفارغ فارغ | `8aab3e30` | — |
| **F-C09** | **ثلاثة مقاييس رسبت على سلوك صحيح في يوم واحد** — لأن توقّعاتها أرقام مجمَّدة من عهد سابق (`roles=25` · `modules=208` · `nav_items=613`) والبذرة نمت مشروعًا (35 · 502 · 1802). **فكان شاهد التثبيت يرسُب على نجاح التثبيت.** الدرس بنصّ الالتزام: «العددُ المجمَّدُ يتعفَّن، والسقّاطةُ لا» | `8aab3e30` · `ee9b5819` · `c46d5691` | — |
| **F-C10** | **9 مطالب من نصّ الوثائق لم تُنفَّذ** — قياس عكسي يستخرج المطالب من نصّ الوثيقة **وقت التشغيل** فلا يتقادم: **4 منفَّذ · 9 مطلوب لم يُنفَّذ · 2 محجوز على المالك**. ومنها: **24 وثيقة معمارية باسم CURRENT (19 بلا وسم تاريخية)** · قاعدة سنة تسمية الهجرات · قلب نطاق الأطراف (12 مسارًا مفتوحًا) | `f330a975` | GAP-62 |
| **F-C11** | **`fx` متوقف عشرة أيام** — المؤشر عند 6,613 من 17,985 و`updated_at=2026-08-12 05:31:59` بلا تغيير عبر ثلاث لقطات. والإنذار المبنيّ ما زال غير موصول | `ems_event_consumers` | GAP-07/52 |
| **F-C12** | **`fin_party_scope` ما زال fail-open** عبر ثلاث لقطات — `return null` للدور غير المذكور، والانفتاح مستهلَك حيًّا في `Finance/dues_fin.php` و`payments_fin.php`. والمراجعة العكسية تعدّه من التسعة غير المنفَّذة (12 مسارًا مفتوحًا افتراضًا) | `fin_helpers.php` | GAP-22 |
| **F-C13** | **الأحداث بلا تغيير**: 58 نوعًا · **0/58** له مستهلك أعمال نشط · 21,285 واقعة · dlq 26 · **11 تسليمًا `published` معلَّقًا** | استعلامات | GAP-05 |
| **F-C14** | **`gov_ladder_decisions` مبنيٌّ بصفر صف** — سجل قرارات السلّم أُنشئ ولم يُكتب فيه، رغم أن `ems_ladder_record()` تُنادى عند القبول في البوابة. متسق مع النمط `monitor` (لا قرارات لأن لا خطوات تُحسم) | عدّ الصفوف | GAP-61 |
| **F-C15** | **التنقّل انكمش بصفر فقد مُثبَت**: النشط 1629 → **1574** بجولة دمج، والإجمالي 1742 → **1802**. والمخفيّ مسجَّل في `gov_nav_hidden_log`(115) والموحَّد في `gov_label_unified_log`(74) ⇒ **الإخفاء صار مُسجَّلًا لا صامتًا** | قياس حي | — |

## ثانيًا — تصحيحات على قياس الجولة السابقة

| # | التصحيح | لماذا وقع الخطأ |
|---|---|---|
| **C-01** | **GAP-01 لم يعد «غير موصول»** — القياس السابق قال «14/14 `ladder_wired=0` والشاشة تقود الخطوة ولا تقرأ سلّمها»، وهذا صار **متجاوَزًا**: البوابة تقرأ السلّم فعلًا في 10 مواضع. الحكم الصحيح اليوم: **موصول غير نافذ** | تغيّر النظام لا خطأ القياس |
| **C-02** | **GAP-18 لم يعد «نمطًا يتكرّر»** — عولج بقاعدة تمنع التكرار لا بإدخال يدوي | تغيّر النظام |
| **C-03** | **GAP-56 كان تشخيصه صحيحًا وأُغلق** — الأداة صارت تقرأ تصريحًا مقيسًا وتقدر على الرسوب. **وهذا يجعل «27/33» رقمًا قابلًا للقراءة بخلاف «33/33» السابقة** | تغيّر النظام |
| **C-04** | **«الفوترة غائبة» صارت «الفوترة مبنيّة غير مُمارَسة»** — الجداول أُنشئت والشاشتان تناديان LD-06 | تغيّر النظام |
| **C-05** | **مقام الحقول تغيّر**: 12,929 → **13,175**، وغير الموثَّق تقنيًّا **7,202** (بتصريح الأداة) — أدقّ من تقدير «336 جدولًا» السابق | مقياس أدق أُضيف |

## ثالثًا — إصلاحات P0 أثناء القياس

**لا شيء في النظام.** أقرب المرشَّحين GAP-59 (بوابة السلّم لا تحجب) — و**لم يُلمس عمدًا**: إعلان `EMS_LADDER_GATE=enforce` يحوّل عشرة مواضع إنتاج من التسجيل إلى المنع، وهو **تغيير سلوك حيّ يستوجب جولة قياس أثر وقرار مالك**، لا سطرًا يُكتب أثناء القياس.

**الاستثناء الوحيد المعلَن:** إصلاح عيب في **عُدّة القياس** (F-C04) — أداة قراءة لا نظام، والعيب كان موثَّقًا في سجل الجولة السابقة.
