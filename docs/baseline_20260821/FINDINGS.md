# FINDINGS — ما اكتُشف أثناء استخراج `BL-20260902-2676dea5` (سُجّل ولم يُصلَح)

> **اللقطة:** `BL-20260902-2676dea5` · القياس: 2026-09-02 · 01:23→02:15 · التفصيل في [SNAPSHOT.md](SNAPSHOT.md)
> **قاعدة الجولة محفوظة:** «استخراج الحقيقة أولًا — لا إصلاح أثناء القياس». **صفرُ تعديلٍ على كودِ النظامِ أو بياناته.**
> سجلّات اللقطات التسع المؤرشفة في [`historical/`](historical/).

## أولًا — اكتشافات هذه الجولة

| # | الاكتشاف | Evidence | GAP |
|---|---|---|---|
| **F-H01** | ✔ **الصفرُ الذي دام ستَّ لقطاتٍ انكسر — عند الوصل**: **0/58 ⇒ 23/58** مفتاحًا منتَجًا له مستهلكُ أعمالٍ فعّال، كلُّها عبر صنفٍ واحدٍ جديد `EffectLinkConsumer`. **والفرقُ عن الاشتراكاتِ الاثنتين والثلاثين السابقة أنّها كانت كلُّها على مفاتيحَ لا تُنتَج** — وهذه على مفاتيحَ تُنتَج فعلًا | `ems_event_subscriptions` × `ems_business_events` | GAP-05 |
| **F-H02** | ⛔ **ولم ينكسر عند النتيجة**: التسليماتُ الفعليةُ للمستهلكِ الجديد **11**، **وكلُّها `dlq`**، **وصفرٌ `processed`**. والسببُ مكتوبٌ بنصِّه: `EFFECT_MISSING: expense.depreciation.recorded — لا رابط مروحة ولا مراة مالية ولا سجل كيان قائم — **يعزل باسمه ولا يمر بصمت**`. ⇒ **Built ✔ · Wired ✔ · Exercised ✔ · Verified ✘** — **والرسوبُ المُسمّى خيرٌ من الصمتِ الأخضر** | `ems_event_deliveries` | GAP-05 |
| **F-H03** | ⛔ **مصدران للـDLQ يتناقضان**: `ems_event_dead_letter` **صفرُ صف** بينما **37 صفَّ تسليمٍ حالتُه `dlq`** (25 `effectfanout` · 11 `effectlink` · 1 `balancecalculator`). ⇒ **«DLQ صفر» صادقةٌ على الجدولِ كاذبةٌ على الحالة** — ومن قرأ الجدولَ وحدَه ظنَّ الناقلَ نظيفًا | استعلامان | GAP-82 |
| **F-H04** | ✔ **`GAP-18` أُغلق بشقَّيه أخيرًا**: الاستنساخُ النظيفُ **أخضرُ تمامًا** — صفرُ جدولٍ غائبٍ وصفرُ فرقِ أعمدة، بعد أن كان **8 جداولَ وفرقَ 3,434 عمودًا** ثمَّ **3 جداولَ وفرقَ 40**. ⇒ **مسارُ الإصلاحِ مقيسٌ في ثلاثِ لقطاتٍ لا مُدَّعى** | `injfix01_clean_clone_fingerprint_proof` | GAP-18 |
| **F-H05** | ⛔ **تصحيحٌ على حكمي أنا — البصمةُ لم تسقط بسببِ الشجرةِ قطُّ**: كتبتُ في `BL-20260828c` أنّ «الشجرةَ اتّسخت فسقطت البصمة». **الشجرةُ اليومَ نظيفةٌ تمامًا والبصمتان لم تتغيّرا حرفًا عبر ثلاثِ لقطات** (`4099c942…` مقابل `4e73fa5d…`). والقراءةُ من الشيفرة: الفاحصُ يبصم **قائمةَ ملفاتٍ مُعلَنةً** في `gov_component_versions.files_json` لا شجرةَ git؛ والصفُّ `DRAFT` هو **`ux-1.6.0` بخمسةِ ملفاتٍ وتاريخُه 2026-08-21** ⇒ **إصدارٌ لم يُجدَّد اثنَي عشرَ يومًا** | قراءةُ الفاحصِ + استعلام | **GAP-78** |
| **F-H06** | ⚠ **سقّاطةٌ ترسُب على التحسُّن**: `injfix02_ownership_ruling` يرسُب بنصِّه «**انخفض إلى 88 من 96** — تُشدُّ السقّاطة» — أي أنّ **غيرَ المحسومِ نزل ثمانيةَ صفوف** والمقياسُ يُرسِّب لأنّه لم يُشَدّ. ⇒ **لونُ الشاهدِ ليس حكمًا؛ نصُّه هو الحكم** | تشغيل حي | GAP-75 |
| **F-H07** | ⚠ **نموٌّ يسبق أداةَ القياس**: `injfix02_field_extraction_limit` يرسُب بـ«**85 شاشةً جديدةً بلا حقولٍ مستخرَجة**» و«لا ازديادَ — 121 ≤ 36». والسببُ أنّ **96 شاشةً بُنيت في يومين** (620 ⇒ **716**) ومستخرِجي لا يقرأ حقولَها: المحاذاةُ 427 ⇒ **347** و`needs_review` 353 ⇒ **479**. ⇒ **عجزُ المقياسِ لا عجزُ المنتَج** | `field_extract` + الشاهد | **GAP-81** |
| **F-H08** | ⛔ **رسوبٌ حقيقيٌّ واحدٌ بشقَّين**: `release_and_action_finality` — (أ) البصمةُ المتقادمة (F-H05) · (ب) «**المعلَّقُ لا يزداد — 163 ≤ 82**» ⇒ **المعلَّقُ تضاعف**. وهذا الشاهدُ وحدَه **يصرّح بثلاثِ فجوات** (`GAP-14` · `GAP-26` · `GAP-32`) ⇒ **حكمٌ واحدٌ راسبٌ يخصم ثلاثةً من مقياسِ الإغلاق** | تشغيل حي | **GAP-79** |
| **F-H09** | ✔ **الحاجبُ الصحيحُ يصمُد على مقامٍ ينمو**: بوابةُ الهجراتِ بُنيت على مقامِ **379** ملفًّا وهي خضراءُ اليومَ على **628** — **بلا تعديلٍ ولا استثناء**: `G-MIG-01` (0/628) · `G-MIG-02` (711 مُدارةً · صفر خارج) · `G-MIG-03` (952 صفًّا · صفر بلا حكم) · `G-MIG-04` (2 خارجَ العُرفِ · صفر بلا حكم). **والمخطَّطُ قفز +230 جدولًا في يومين ولم يفلت أحدُها** | تشغيل حي | GAP-18/69 |
| **F-H10** | ✔ **ثلاثُ فجواتٍ من موانعِ الأمنِ أُغلقت**: `GAP-10` (P0 — القنواتُ التسعُ خضراء و**34/34 داخلَ الإنفاذ**) · `GAP-11` (نقطةُ قرارٍ واحدة) · `GAP-17`/`GAP-76` (تكافؤُ المجدول). ⇒ **Security readiness ارتفع من ◐− إلى ◐+** | ثلاثةُ شواهدَ خضراء | — |
| **F-H11** | ✔ **`GAP-07` أُغلق بعد أن انتقل عطبُه**: `fx` كان متوقّفًا ستةَ عشرَ يومًا ⇒ لحق (**17,985**) ⇒ **فصار الإنذارُ يُنذر عنه خطأً** («أُنذر خطأً: fx») ⇒ ثمَّ ضُبط شرطُ الإنذارِ فصار أخضر. ⇒ **العطبُ ينتقل ولا يختفي حتى يُطارَد إلى آخرِ صورةٍ له** | `bus_stall_alarm_proof` | GAP-07 |
| **F-H12** | ✔ **الاتجاهُ صاعدٌ في المقاييسِ الثلاثةِ معًا لأوّلِ مرّة**: الإغلاق **16/33 ⇒ 23/33** · الشواهد **25/34 ⇒ 32/35** · المتطلبات `EVIDENCE_CLOSED` **26 ⇒ 33** و`OPEN` **9 ⇒ 3** ⇒ **37.1٪ ⇒ 46.5٪ ببسطٍ نما لا بمقامٍ نقص** | ثلاث أدوات | — |
| **F-H13** | ⛔ **سجلُّ أحكامِ الأحداثِ متقادمٌ عن الاشتراكات**: `gov_event_rulings.handler_class` = `GovernanceWatchConsumer` **للثمانيةِ والخمسين جميعًا**، بينما الاشتراكاتُ تحمل مستهلكَ أعمالٍ على **23**. ⇒ **من يقرأ سجلَّ الأحكامِ يرى صفرًا ومن يقرأ الاشتراكاتِ يرى 23** — مصدرا حقيقةٍ لسؤالٍ واحد | استعلامان | GAP-05 |
| **F-H14** | ⛔ **الأرشفةُ المُعلَنةُ تجاوزت الأرشيفَ الفعليّ**: اللقطةُ السابقةُ أعلنت «**التسعَ السابقة** … آخرها `BL-20260831d-aba54573`» **والمجلَّدُ كان يحوي ثمانيًا ولا أثرَ فيه لـ`b/c/d`**. ⇒ **ثلاثُ لقطاتٍ أُعلنت ولا تُستعاد** — وهو نقضٌ لقاعدةِ «أيُّ رقمٍ قديمٍ يُحفَظ في Historical Baseline». صُحِّح العدُّ في هذه اللقطةِ إلى **تسعٍ مؤرشفةٍ فعلًا** | `ls historical/` مقابل نصِّ الوثيقة | **GAP-80** |
| **F-H15** | ⛔ **تناقضٌ داخلَ رأسِ اللقطةِ السابقة**: `SNAPSHOT.md` قال إنّ القياسَ **2026-09-01 · 02:35→02:45** بينما رأسا `INJ-ARCH-ASBUILT` و`INJ-CURRENT-STATUS` قالا **2026-08-31 · 20:40→20:50** لنفسِ اللقطة. ⇒ **نافذةُ قياسٍ واحدةٌ بروايتَين** | مقارنةُ الرؤوسِ الثلاثة | **GAP-80** |
| **F-H16** | ⛔ **تلوُّثُ عمودِ مفتاحِ المستهلكِ باقٍ**: **8 مفاتيحَ من 12** متمايزةً في `ems_event_deliveries` نصٌّ بشريٌّ عربيٌّ موسومٌ `UAT-2026` (10 صفوف من 21,428). و`GAP-09` («صفرُ صفٍّ بنصٍّ بشريٍّ في حقلِ مفتاح») **مُغلقٌ بتصريحٍ مقيس** ⇒ **إغلاقُه صادقٌ على الأعمدةِ التي فحصها وأعمى عن هذا العمود** | عدٌّ على المفاتيحِ المتمايزة | GAP-77 |
| **F-H17** | ⚠ **سجلّان للشاشاتِ وفرقُهما اتّسع**: السجلُّ الرسميُّ **909 · صفر بلا مالك**، وسجلُّ حزمتي **885** فيه **21 تعارضَ ملكيةٍ** (كان 13) و**67 مالكًا مجهولًا** (كان 84). ⇒ **التعارضُ نما بالنموِّ والمجهولُ نقص** — وكلاهما في سجلِّ حزمتي لا في السجلِّ الرسميّ، **فالفرقُ نطاقٌ لا عطبُ ملكية** | مقارنةُ المصالحتَين | GAP-20 |
| **F-H18** | **ما لم يتغيّر رغم ثمانين التزامًا**: القيود **6,713/13,426** بفرق **0.00** · التسويات **469 بصفرٍ مدفوع** · `EMS_LADDER_GATE` **غيرُ معلَن** · نافذةُ الظلِّ **صفر صف** · الذهبيةُ **0/10 اعتمادًا** · الأدوار 35 · المستخدمون 77. ⇒ **الجولةُ حوكمةٌ وواجهةٌ، والتشغيلُ الماليُّ ساكن** | قياس حي | — |
| **F-H19** | **الحوكمةُ الحقليّةُ تضاعفت أربعَ مرّات**: `gov_field_class` **630 ⇒ 2394** صفًّا، و`u13_column` في سجلِّ حقولي **577 ⇒ 2362**. ⇒ **الحقولُ المصنَّفةُ صارت رابعَ أكبرِ مصدرٍ في سجلِّ الحقول** | قياس حي | — |

## ثانيًا — تصحيحات على قياسِ الجولاتِ السابقة

| # | التصحيح | لماذا |
|---|---|---|
| **C-01** | ⛔ **«الشجرةُ اتّسخت فسقطت البصمة» كانت قراءةً خاطئة** — الفاحصُ يبصم قائمةَ ملفاتٍ مُعلَنةً في `gov_component_versions` لا شجرةَ git؛ والدليلُ أنّ الشجرةَ نظيفةٌ اليومَ والبصمتان **لم تتغيّرا حرفًا** | خطأُ إسنادِ سببٍ عندي |
| **C-02** | **«DLQ صفر» التي أعلنتُها تحتاج تقييدًا** — صفرٌ في `ems_event_dead_letter` **و37 صفَّ تسليمٍ حالتُه `dlq`**؛ فتُقرأ «صفرٌ في الجدولِ المخصَّص» لا «صفرُ رسائلَ ميتة» | مقامٌ ناقص |
| **C-03** | **«صفر تصادم في معرّفات GAP: 51..77» صارت 51..81** | نموُّ السجل |
| **C-04** | **عددُ اللقطاتِ المؤرشفةِ صُحِّح**: «تسعٌ» كانت مُعلَنةً وثمانٍ موجودة؛ **وبعد أرشفةِ `BL-20260901` صارت تسعًا فعلًا** | تصحيحُ عدٍّ |
| **C-05** | **`GAP-14`/`GAP-26`/`GAP-32` تُقرأ معًا** — يصرّح بها شاهدٌ واحدٌ راسب، فلا تُعدُّ ثلاثَ انتكاساتٍ مستقلّة | قراءةُ آليةِ التصريح |
| **C-06** | **نسبةُ `company_id` (657/953) لا تُنقَل إلى مقامِ 1228** — وُسمت `NEEDS_REVIEW` بدل ترحيلِ رقمٍ قديمٍ على مقامٍ جديد | قاعدةُ عدمِ الخلط |

## ثالثًا — إصلاحات P0 أثناء القياس

**لا شيء في النظام — ولا في عُدّتي.**

والمرشَّحُ الوحيدُ الذي يستحقُّ الذكرَ هو **مطاردةُ `EFFECT_MISSING`**: أحدَ عشرَ تسليمًا في `dlq` **تسمّي كياناتِها بأسمائها** (`fin_asset…` · `bank_re…` · `settlement#5502`)، وسدُّ الأثرِ المفقودِ يحوّل ثلاثةً وعشرين مفتاحًا من **موصولٍ** إلى **مُثبَت**. **وتركتُه لأنّه إصلاحُ بياناتِ مصدرٍ لا إصلاحُ قياس** — وهو عملُ جولةٍ لا عملُ قائس.

والموانعُ الثلاثةُ الأولى للجاهزيةِ **كلُّها فعلُ مالك**: إعلانُ `EMS_LADDER_GATE` بعد نافذةِ ظلٍّ حقيقية · حسمُ مقامِ التايم شيت · اعتمادُ الشاشاتِ الذهبيةِ العشر باختبارٍ بشريّ.
