# INJAZ_REVERSE_AUDIT_SUMMARY — الحكمُ النهائيُّ للمراجعةِ العكسية

**خطُّ الأساس:** `fix/remediation-2026-08` · HEAD `a6d6a3ae6671` · 2026-08-15T17:37:05Z
**المصنَّف:** `INJAZ_REVERSE_AUDIT_MASTER.xlsx` (33 ورقةً · منها 19 ورقةَ إدارة)
**التحقُّق من عدمِ المساس:** `php audit_tools/ra09_fingerprint.php` ⇐ بصمتا المخططِ والقوادحِ **مطابقتان** لخطِّ الأساس بعد كلِّ القياسات. التدقيقُ قراءةٌ فقط، ولم يُعدَّل سطرُ منتجٍ واحد.

---

## ⓪ الجملةُ الواحدة

> النظامُ **مبنيٌّ أكثرَ مما هو موصول، وموصولٌ أكثرَ مما هو مُثبَت**.
> البنيةُ ناضجةٌ (555 جدولًا · 230 قيدَ CHECK · 466 وحدةَ صلاحيات)، لكنَّ
> **سلاسلَ المالِ الثلاثَ الكبرى مقطوعةٌ حيًّا**: الوقائعُ لا تصل الدفتر،
> والمستخلصاتُ بلا بنود، والعقودُ بلا عروض. وهذه لم يكشفها أيُّ تدقيقٍ سابق.

---

## ① نسبةُ التنفيذِ الحقيقية — والمقاماتُ الأربعةُ على المقامِ نفسِه (595)

| طريقةُ العدّ | العدد | النسبة | ما تعنيه فعلًا |
|---|---:|---:|---|
| ① «مذكورٌ في وثيقةِ تصحيح» | 272 | **45.7٪** | ← الطريقةُ التي أنتجت الادعاءَ السابق |
| ② «تنفيذٌ مُغلق» (سجلُّ الحالة) | 342 | 57.5٪ | ادعاءُ التنفيذِ بلا اشتراطِ دليل |
| ③ «شاهدٌ أخضرُ حيٌّ» (أُعيد تشغيلُه) | 267 | 44.9٪ | اختبارٌ شُغِّل فعلًا في هذه الجولة |
| ④ **«الثلاثةُ معًا» — الحكمُ المعتمد** | **248** | **41.7٪** | قرارٌ + تنفيذٌ + دليلٌ حيّ |

**الفارقُ بين ① و④ = 24 بندًا.** هذا مقدارُ التضخُّمِ الصافي في الادعاءِ السابق، لا أكثرَ ولا أقل.

## ② نسبةُ ما أُثبت لا ما بُرمج

**44.9٪** (267 شاهدًا أخضرَ من 595). و**94 بندًا مُعلَنٌ تنفيذُها وليس لها شاهدٌ أخضرُ مُشغَّل** — هذه هي «فجوةُ الدليل» الحقيقية. وخمسةُ شواهدَ **حمراءُ حيًّا**: `INJ-0013 · INJ-0032 · INJ-0036 · INJ-0219 · INJ-0502`.

## ③ اكتمالُ كلِّ إدارة (الحكمُ الثلاثي · ورقة 05 والأوراق D01–D19)

| الأعلى | ٪ | | الأدنى | ٪ |
|---|---:|---|---|---:|
| إدارة الموردين | 60.7 | | القوى التشغيلية | **16.0** |
| المالية والخزينة | 58.5 | | المشتريات التشغيلية | **21.4** |
| التمويل والملكية · الموارد البشرية | 51.9 | | المراجع الداخلي المستقل | 28.6 |
| مكتب الرئيس التنفيذي | 48.6 | | النقل والترحيل | 31.8 |
| إدارة الحوكمة والالتزام | 48.1 | | إدارة الموقع | 32.3 |

## ④ اكتمالُ كلِّ طبقةٍ معمارية

| الطبقة | المقيس | الحكم |
|---|---|---|
| القرار | 569 / 595 = **95.6٪** | شبهُ محسوم — 26 بندًا تنتظر المالك |
| التنفيذ | 342 / 595 = **57.5٪** | |
| الدليل | 267 / 595 = **44.9٪** | |
| **دوراتُ العملِ عبرَ الإدارات** | 29 / 48 وصلةً = **60.4٪** | ورقة 90 |
| ترحيلُ الوقائعِ للدفتر | 9 / 5,255 = **0.17٪** | ✖ منهار |
| تسليمُ أحداثِ الأعمال | 20 / 21,197 = **0.09٪** | ✖ ناقلٌ بلا مستهلكين (4 فقط) |
| الشاشاتُ الحيّة | 118 / 448 تُصيَّر للمخوَّل | الباقي محجوبٌ fail-closed ✔ |
| الصلاحيات | 0 كتابةٍ قبل الحارس ✔ · **7 شاشاتٍ تُسرَّب بلا منحة** ✖ | |
| CSRF | **293 نموذجَ POST بلا رمز** تحت مسارات الإنفاذ | ✖ |
| الأداء | p95 = 600 مل.ث ✔ · p99 = 2.8 ث · أبطأُ 5.9 ث | مقبولٌ بحدود |
| الاستمرارية | RPO الفعليُّ **10 أيام** · `log_bin=OFF` · نسخةُ بياناتٍ واحدةٌ من 442 ملفَّ نسخ | ✖ لا استعادةَ لنقطةِ زمن |
| UAT | **صفر** جولةٍ بالأدوارِ الحقيقية | ✖ |

## ⑤⑥⑦ أحدث تحسنٌ حقيقي؟ وما الذي تغيّر؟ وهل هو مقامٌ أم إصلاح؟

**نعم، التحسنُ حقيقيٌّ ومقيس — والادعاءُ متضخِّمٌ في آنٍ واحد.** كلاهما صحيح:

- **حقيقيٌّ:** تدقيقُ 2026-08-09 سجّل 595 عيبًا وصفرَ إغلاق. اليومَ **248 منها مُغلقٌ بشاهدٍ أخضرَ أُعيد تشغيلُه أمامي** (لا منقولٍ عن تقرير)، و**268 من 273 شاهدًا اجتازت** عند إعادةِ التشغيل، و**صفرُ كتابةٍ قبل الحارس** بعد أن كانت فئةَ P0. هذا إصلاحٌ لا محاسبة.
- **متضخِّمٌ:** الرقمُ المعلَن 45.7٪ محسوبٌ بـ«ذُكر في وثيقةِ تصحيح» — وهو معيارٌ يَعُدُّ النيّةَ لا الأثر. بالمعيارِ الصارمِ نفسِه على المقامِ نفسِه: **41.7٪**.
- **ولا تغييرَ في المقام:** المقامُ 595 ثابتٌ في الحسابين. الفارقُ كلُّه في البسط.

**لكنَّ الأهمَّ:** هذه الجولةُ كشفت **ستةَ حواجبَ لم تكن في أيِّ سجلٍّ سابق** (B8–B13 أدناه) — أي أنَّ الرقمَ 41.7٪ نفسَه **محسوبٌ على سجلٍّ ناقص**. العيوبُ الحقيقيةُ أكثرُ من 595.

## ⑧ حواجبُ الإطلاق (ورقة 04)

| # | الحاجب | النوع | الدليلُ المقيس |
|---|---|---|---|
| B2 | 7 شاشاتٍ تُصيَّر لقارئٍ بلا منحة | Security | `ra05c` — عقودُ مورّدين وموظفين وتقارير |
| B3 | 293 نموذجَ POST بلا CSRF | Security | `ra03c` تحت 27 مسارَ إنفاذ |
| B6 | INJ-0219 انحدارُ سلسلةِ الاعتماد | Correctness | 16/39 حيًّا |
| **B8** | **الوقائعُ الماليةُ لا تُرحَّل إلى الدفتر** | **Financial** | 9 من 5,255 · و**5,207 حالتُها `Published` تنتظر** — والانتقالُ إلى `UnderReview` **بلا موضعِ نداءٍ واحدٍ** في الشجرة (خطوةٌ لم تُبنَ لا عطلٌ) · والتسعةُ بُذرت مُرحَّلةً (`posted_by` و`posted_at` فارغان) · والدفترُ (1,653 قيدًا · 9 بمرجعِ واقعة) يُملأ من مسارٍ يدويٍّ موازٍ. الأداة: `tools/fin00_posting_gap.php`<br>◆ **تصحيحُ قراءة:** العمودُ `state` يبقى `draft` أبدًا لأن `LEGACY_MIRROR` بلا مقابلٍ لـ`Published` — فالحالةُ الحاكمةُ `fes_status` لا `state`. |
| **B9** | **298 مستخلصًا بصفرِ بند — و285 فاتورةً فوقها** | **Financial** | `claims=298` · `claim_lines=0` · `tax_invoices=285` |
| **B10** | **سلسلةُ «عرضٌ ← عقد» مقطوعةٌ حيًّا** | Correctness | 120 عقدًا كلُّها `quotation_id=NULL` مع وجودِ 20 عرضًا |
| **B11** | **الناقلُ ينشر ولا يُسلِّم** | Integration | 20 تسليمًا من 21,197 · 4 مستهلكين |
| **B12** | **17 مهمةً ميتةً من 20** | Operational | `job_type` يحمل نصَّ ملاحظةِ UAT لا رمزًا — بقايا جولةٍ ماتت |
| **B13** | **لا استعادةَ لنقطةِ زمن** | Continuity | `log_bin=OFF` · أحدثُ نسخةِ بياناتٍ عمرُها 10 أيامٍ · 434 لقطةَ **بنيةٍ بلا بيانات** ونسخةَ بياناتٍ **واحدة** · لا محضرَ استعادةٍ قط |

**و6 من 13 حاجبَ P0 غيرُ مُغلقٍ بالطبقاتِ الثلاث · و`release_gate.php` رمزُ خروجِه 1.**

## ⑨ حواجبُ العرضِ على المستثمرين والجهاتِ السيادية

1. **أرقامٌ ماليةٌ غيرُ مسنَدة** — أيُّ سؤالٍ من نوع «أرِني القيدَ خلف هذا الإيراد» ينتهي بلا إجابةٍ في 99.8٪ من الحالات (B8).
2. **فاتورةٌ بلا تفصيل** — 285 فاتورةً فوق مستخلصاتٍ بصفرِ بند (B9). لا تصمد أمامَ مدقِّق.
3. **تسريبُ عقودٍ لقارئٍ بلا منحة** (B2) — سؤالُ الحوكمةِ الأولُ في أيِّ عرض.
4. **صفحاتٌ بحجم 11.8 و10.2 ميجابايت** (`Reports/deliy.php` · `Reports/deriver.php`) وأبطأُ شاشةٍ 5.9 ثانية.
5. **RPO عشرةُ أيامٍ بلا محضرِ استعادة** (B13) — سؤالُ العنايةِ الواجبةِ القياسي.
6. **صفرُ جولةِ UAT بالأدوارِ الحقيقية** — لا توقيعَ قبولٍ يُعرَض.

## ⑩ المسارُ الحرج

```
B13 الاستمرارية ─┐
                 ├─► B8 ترحيلُ الوقائع ─► B9 بنودُ المستخلص ─► إقفالٌ ماليٌّ صادق
B10 عرض←عقد ─────┘                          │
                                            └─► B11 الناقل ─► تكاملُ الإداراتِ حقيقةً
B2 · B3 الأمن ──► بوابةُ الأمن ──► أيُّ عرضٍ خارجي
B12 كنسُ بقايا UAT ──► قياسٌ نظيفٌ للطابور
```

**الترتيبُ غيرُ قابلٍ للتبديل:** لا معنى لردمِ فجوةِ الدليلِ (94 بندًا) قبل B8، لأنَّ الشواهدَ ستُثبِّت سلوكًا ماليًّا مقطوعًا أصلًا.

## ⑪ المدةُ الواقعيةُ بأيامِ العملِ البشرية

| السيناريو | يوم-شخص | 3 مهندسين @70٪ | 5 مهندسين @70٪ |
|---|---:|---|---|
| ① رفعُ بوابةِ الإصدارِ بمعاييرها | **74** | ~7 أسابيع | ~4 أسابيع |
| ② جاهزيةٌ داخليةٌ حقيقية (①+كلُّ P1+ردمُ الدليل) | **312** | ~7 أشهر | ~4 أشهر |
| ③ الإغلاقُ الكاملُ (347 بندًا مفتوحًا) | **876** | ~20 شهرًا | ~12 شهرًا |

المفتوحُ: P0=6 (34 يومًا) · P1=66 (180) · P2=179 (477) · P3=92 (180) · P4=4 (5).
منها **39 بندَ «دليلٍ غائب» (58.5 يومًا) لا تمسُّ المنتج** — كتابةُ شاهدٍ فقط.

> تنبيهٌ على التقدير: الـ876 مجموعٌ خامٌّ بلا توازٍ ولا وفوراتِ تجميع، ومبنيٌّ على نموذجِ صعوبةٍ واحدٍ مشتركٍ (`ra_effort_model.php`) لا على تخمين. وهو **لا يشمل** الحواجبَ الستةَ المكتشفةَ حيًّا (B8–B13) لأنها خارجُ سجلِّ الـ595 — أضِف لها 25–40 يومًا.

## ⑫ حكمُ الجاهزية

| الحالة | الحكم |
|---|---|
| غيرُ جاهز | — |
| **جاهزٌ داخليًّا بشروط** | ✅ **هذا هو الحكم** |
| Release Candidate | ✖ |
| جاهزٌ للإطلاق | ✖ |
| جاهزٌ للعرضِ الخارجي | ✖ |
| جاهزٌ لـSaaS متعدد العملاء | ✖ |

**الشروطُ الثلاثةُ للاستخدامِ الداخلي:** ألّا يُعتمد أيُّ رقمٍ ماليٍّ من النظامِ مستندًا رسميًّا حتى إغلاقِ B8/B9 · وأن يُقيَّد الوصولُ بالأدوارِ المُختبَرةِ حتى إغلاقِ B2/B3 · وأن يُؤخذ تفريغٌ يوميٌّ يدويٌّ حتى إغلاقِ B13.

**حالةُ البوابة: `Release Gate: FAILED`** — والنسبةُ الإجماليةُ 41.7٪ **إعلاميةٌ فقط** ولا تتجاوز حالةَ البوابة.

---

## أدواتُ إعادةِ القياس (كلُّها قراءةٌ فقط)

```bash
cd C:/wamp64/www/ems/docs/reverse_audit_2026-08
php audit_tools/ra09_fingerprint.php    # أثبت أن البنية لم تتحرك
php audit_tools/ra07_journeys.php       # الرحلات الست
php audit_tools/ra08_nfr.php            # الأداء والناقل والاستعادة
php audit_tools/ra10_rollup.php         # الأرقام الختامية ومقارنة طرائق العد
php audit_tools/ra90_build_workbook.php # إعادة بناء المصنَّف كاملًا
```

## حدودُ هذه الجولةِ — مُعلَنةٌ لا صامتة

1. **لا جولةَ متصفحٍ بصريةً** على الدقاتِ الأربع — بنودُ UX موسومةٌ `Needs Live Browser Verification`، ولم تُحسب في أيِّ بسط.
2. **لا اختبارَ حملٍ ولا تزامن** — بيئةٌ محليةٌ بمستخدمٍ واحد. `NFR_Evidence_Percent` على مستوى الإدارةِ **غيرُ مقيس**.
3. **`UAT_Completion_Percent` = غيرُ مقيسٍ لكلِّ إدارة** — لا جولةَ قبولٍ بالأدوارِ الحقيقية.
4. **الاختباراتُ الحيّةُ عبر HTTP قراءةٌ فقط** (دخولٌ ثم GET) — لا POST على مساراتِ المنتج، فأفعالُ الكتابةِ مقيسةٌ بالكودِ والقاعدةِ لا بالنقر.
5. **سجلُّ الـ595 ناقصٌ** بدليلِ الحواجبِ الستةِ المكتشفةِ هنا — كلُّ نسبةٍ مبنيةٍ عليه هي **سقفٌ أعلى** لا حقيقةٌ نهائية.
