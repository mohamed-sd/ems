# AUDIT_BASELINE_MANIFEST — خطُّ أساسِ المراجعةِ العكسية

**لحظةُ التثبيت:** 2026-08-15T17:37:05Z (20:37 محليًّا) · **الجامع:** `audit_tools/ra00_baseline.php` · **التفصيل الكامل:** `evidence/baseline.json`

> كلُّ نتيجةٍ في هذه المراجعةِ تُنسب إلى هذا الخطِّ. وأيُّ تغييرٍ بنيويٍّ بعده
> لا يدخل النتائجَ إلا بخطِّ أساسٍ جديدٍ مُعلَن. المسابيرُ الشاهدةُ تكتب صفوفَ
> اختبارٍ **وتنظّفها** — وبصمةُ المخططِ تُعاد بعد كلِّ جولةٍ لإثباتِ أنَّ البياناتِ
> وحدَها تحرّكت لا البنية.

## ① Git

| البند | القيمة |
|---|---|
| الفرع المفحوص | `fix/remediation-2026-08` |
| HEAD | `a6d6a3ae667107cb8dd528c6e296cf38ba986dcc` |
| تاريخ HEAD | 2026-08-15 18:54:31 +0300 |
| أمام `origin/fix/remediation-2026-08` | **0** — كلُّ الالتزاماتِ مدفوعة |
| أمام `origin/main` | **121 التزامًا** |
| حالة الشجرة | نظيفة عدا `~$X-02…docx` (قفلُ Word مؤقتٌ غيرُ متتبَّع) |

◆ **حدثٌ مرصودٌ أثناء الجلسة:** `refs/heads/main` المحليُّ أُعيد ضبطُه إلى
`origin/main` في **2026-08-15 19:46:58** (من 553ad70 بتاريخ 08-13 إلى d0f7834
بتاريخ 08-11). الحدثُ خارجيٌّ (لا أداةَ في المستودعِ تمسُّ refs)، و**لا عملَ
يتيمًا**: 553ad70 سلفٌ مباشرٌ لِـHEAD الفرعِ المفحوص. أثرُه الوحيدُ أن «أمامَ
main» صار يُقرأ 121 بعد أن كان يُقرأ 35 قبل الضبط — **تغيّرَ المقامُ لا العمل**.

## ② البيئة

| البند | القيمة |
|---|---|
| PHP (CLI المعتمد للقياس) | 8.2.30 — `C:\wamp64\bin\php\php8.2.30\php.exe` |
| قاعدة البيانات | MariaDB **11.4.9** · `equipation_manage` على المنفذ **3307** |
| الويب | Apache على 80 (حي) · MySQL 3306 (معطَّل — القياس كلُّه على 3307) |
| نظام التشغيل | Windows 11 Pro (win32) |

## ③ بصمتا البنية

| البصمة | القيمة |
|---|---|
| المخطط (SHOW CREATE TABLE للجداول والمناظر، بلا AUTO_INCREMENT) | `d66f94e28c28107e34c3db71ed9a925f176328ce` |
| القوادح (الاسم·الجدول·التوقيت·الفعل·النص) | `601bca3ea9729271d7ea8986ee2d3b53db36fdde` |

## ④ أعدادُ البنيةِ لحظةَ التثبيت

| المقياس | العدد | | المقياس | العدد |
|---|---:|---|---|---:|
| جداول أساس | **555** | | مناظر (Views) | 6 |
| قوادح | 14 | | مفاتيح أجنبية | 255 |
| قيود CHECK | **230** | | قيود UNIQUE | 358 |
| جداول بعمود `company_id` | **462** | | ترحيلات مطبَّقة | 414 |
| وحدات صلاحيات (`modules`) | **466** | | منح أدوار | 2622 |
| أدوار | 35 | | مستخدمون | 77 |
| روابط تنقّل نشطة | 1250 | | قاموس الأفعال (`nav09_action_map`) | **362** |
| ملفات PHP في الشجرة | **1668** | | ملفات اختبار `tests/` | 289 |
| أدوات `tools/` | 308 | | ملفات ترحيل على القرص | 117 |
| وقائع أعمال (`ems_business_events`) | 21,190 | | سجل التدقيق (`activity_logs`) | 170,332 |

## ⑤ بصماتُ مدخلاتِ التدقيق (sha1 في baseline.json)

وثائقُ `docs/fix/` الأربع · مصنفاتُ `docs/audit_2026-08/` السبعة ·
`master_register.tsv` (595) · `INJ_findings_state.tsv` · `FIX_rules_register.tsv` (619) ·
`ARCHITECTURE_CURRENT_SYSTEM_v21_ar.md`.

## ⑥ حدودُ هذا الفحص (مُعلَنةٌ سلفًا — لا سقفَ صامتًا)

1. **أدواتُ المراجعةِ قراءةٌ فقط** ولا تكتب إلا تحت `docs/reverse_audit_2026-08/`.
2. إعادةُ تشغيلِ المسابيرِ الموجودةِ (التي تكتب وتنظّف صفوفَها) جزءٌ من القياس —
   وتُثبَت سلامةُ البنيةِ بإعادةِ البصمتين بعد كلِّ جولة.
3. الاختباراتُ السلوكيةُ الحيّةُ عبر HTTP **قراءةٌ فقط** (دخولٌ ثم GET) — لا POST
   على مساراتِ المنتجِ من أدواتِ هذه المراجعة.
4. ما لا يُقاس في هذه الجولةِ يُوسم صراحةً: `Needs Live Browser Verification` أو
   `Not Measured This Round` — ولا يُحسب في أيِّ بسطٍ أو مقام.
