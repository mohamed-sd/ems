# سجلُّ تنفيذ حزمة update0001 — (أمرُ التنفيذ الإصدار 2)

> **عند بداية أي جلسة: اقرأ هذا السجل واستأنف من «المهمة الجارية» بلا سؤالٍ ولا انتظار إذن.**
> المرجع الحاكم: `docs/EXECUTION_BRIEF_update0001_ar.md` ثم `docs/update0001/PLAN-01*.docx`.

## رأسُ الحالة (يُحدَّث دائمًا)

- **الموجةُ الجارية:** ③-ب **خطُّ الأساس التجاري** (`P-01`→`P-12`) — **والموجاتُ ①②③ أُغلقت** (`WAVE1_CLOSURE_20260730.md` · `WAVE2_CLOSURE_20260730.md` · **`WAVE3_CLOSURE_20260731.md`**)
- ~~**الموجةُ السابقة:** ③ الاحتسابُ والاستحقاق (24 مهمة) — **مكتملةٌ 24/24**~~ — **الموجتان ①② أُغلقتا** (`WAVE1_CLOSURE_20260730.md` · `WAVE2_CLOSURE_20260730.md` — مكتملتان تقنيًّا؛ وما ينتظر توقيعًا أو زمنًا لا يوقف العمل §11)
- **الموجةُ التالية:** ③-ب **خطُّ الأساس التجاري** (`P-01`→`P-12`) — أضافها `docs/EXECUTION_ADDENDUM_PLAN03_ar.md` (التوجيه الثالث · **ملحقٌ نافذ**)، وموضعُها **بعد** إتمام ما تبقّى من ③ و**قبل** ④. والإجمالي صار **116 مهمة**
- **آخرُ مهمةٍ أُقفلت:** P-07 أهدافُ التخصيص الخمسة — **وبها صار الرصيدُ غيرُ المخصَّص عمودًا لا رسالةً تختفي**
- **المهمةُ الجارية:** **`P-08` العملاتُ الثلاث** 🔴🔥 (`contract` · `settlement` · `functional` + **الفروقُ الأربعة التي لا تُخلط**: رصيدٌ غيرُ مسدد · فرقٌ محقق · فرقٌ غيرُ محقق · زيادةُ سداد) — **ومانعُها `P-07` مغلق**
- **المتبقي في الموجة ③:** **صفر** — و**M-20 · M-21 · M-23 أُغلقت ضمن شرائح H-09**
- **المتبقي في ③-ب:** 5 (`P-08`→`P-12` بترتيبها **الملزم**)
- **📄 وثيقةُ PLAN-03 صارت في المستودع:** `docs/update0001/PLAN-03 خط الأساس التجاري وتصحيح الهرم التعاقدي.docx` — كانت **خارجَه كلِّه** والملحقُ يحيل إليها بوصفها توجيهًا نافذًا؛ وهي المصدرُ الوحيدُ لأنماط الدفع الثمانية وحقولِ السطر واختباراتِ §9 الثمانية عشر
- **⚠ گوتشا بيئيةٌ عامةٌ مقيسة (P-01):** **`CURDATE()` في MySQL (توقيتٌ محلي) و`date()` في PHP (UTC) يختلفان يومًا كاملًا ثلاثَ ساعاتٍ يوميًّا** — فأيُّ حزمةٍ تبذر بساعةٍ وتقيس بأخرى **تسقط عشوائيًّا حول منتصف الليل**. المقياسُ الواحدُ في الحزمة شرطٌ لا تفضيل
- **⏳ ينتظر PLAN-02 (لا يوقف العمل):** `N-06` «حدودُ المعاملة مغلقة» **مُعرَّفةٌ في PLAN-02 غيرِ المتوفر** — و`N-06` في وثائقنا القديمة تعني شيئًا آخر (نتائجُ التفتيش). **مسجَّلةٌ ومتجاوَزة، ولا تُخترع** (الملحق §2-③)
- **⏳ معلَّقٌ على الزمن (M-19):** قلبُ `EMS_SUPPLIER_DOC_GATE` من `monitor` إلى `enforce` بعد أسبوعِ رصدٍ نظيف — القياسُ يظهر في `error_log`، والقلبُ قبل استكمال وثائق الموردين يجمّد اعتمادَ تسوياتهم
- **🔴 أحمرُ سابقٌ لهذه الجلسة (لا علاقةَ له بها):** `equipment_documents_test` 29/1 — تأكيدُه «63 وثيقةً موسومةً منتهيةً» صار 34 بعد **تجديد 29 وثيقةً في 2026-07-29** (مقيس: صفرُ صفٍّ تغيّر اليوم · و`expiry_date < CURDATE()` = 34 بالضبط). الرقمُ في التأكيد **خطُّ أساسٍ قديم** لا خللٌ في الشجرة — وتصحيحُه قرارُ صاحب E-08 لا قرارُ عابر
- **⏳ ينتظر قرارَ المالك (لا يوقف العمل):** ① تصنيفُ أنواع الغياب في `payroll_absence_types` — بُذرت كلُّها «لا خصم»، والغيابُ غيرُ المصنَّف يظهر **سطرًا معلَنًا بلا خصم** (خصمٌ بلا قرارٍ أسوأُ من خصمٍ مؤجَّل) · ② **حدُّ حمايةِ الصافي** `payroll_settings.protection_percent` — `NULL` = لم يُقرَّر، والمقاصّةُ **تُعلنه** ولا تفترض رقمًا
- **⏳ معلَّقٌ على الزمن (H-09):** وصلُ **القيد المحاسبي** بجهات التحمّل — الأسطرُ تحمل جهاتِها ونسبَها سلفًا، ووصلُها بالمروحة مشروطٌ بـ**مطابقةِ شهرٍ بصفر فرق** لفئةٍ رائدةٍ واحدة (ENT-01 §9-الرائدة)
- **ملفاتُ فحص الطبقات:** `layer_3_signoff.md` العقود · `layer_4_signoff.md` الالتزامات · `layer_5_signoff.md` الحصص — مكتملةٌ تقنيًّا تنتظر إعلانَ المالك
- **⏳ معلَّقٌ على الزمن:** قلبُ `EMS_CONTAINER_GATE_MODE=enforce` للرائد 10 بعد أسبوعِ رصدٍ نظيفٍ (تقريرُ `Operations/cron_container_gate_report.php` الأسبوعيُّ يحكم — §13-①) · وفتحُ `EMS_ROTATION_AUTOTRANSFER` للرائد نفسِه **بعد** استكمال دوراته من مقبض الشاشة (صفرُ دورةٍ = صفرُ نقلٍ ممكن — الفتحُ الفارغ ضوضاء)
- **مشغّلُ الانحدار:** `php tests/_regression.php [core|all]` — **عدّةٌ لا اختبار** (بُنيت مع M-22): تشغّل الحزمَ وتطبع رمزَ خروجٍ وعدّادَين لكلٍّ. آخرُ تشغيلٍ لـ`core` (57 حزمة): **56 خضراء · 1 حمراء** = `contract_state_machine_test` **45/1** وهي **انزياحُ التوقُّعات المفروزُ سلفًا** في خط الأساس §3 (يتوقع 9 عقودًا والقائم 10) — لا انحدارَ جديد
- **حالةُ الانحدار:** **30 حزمةً خضراء** عند إغلاق الموجة ② (منها الجديدة: `price_adjustment` 38/0 ×2 · `supplier_contract_lines` 38/0 ×2 · `rotation_autotransfer` 15/0 ×2) — والسابقة: `employee_contract_registry_test` 52/0 ×2 · datatable_server 34/0 · supplier_portal_guard 52/0 · tenant_leak 130/0 · audit 26/0 · publisher 54/0 · fes 45/0 · period 19/0 · journal 27/0 · base_equivalent 19/0 · fx 45/0 · unified_nav 16/0 · role_board 16/0 · ts-service 50/0 · doc-guard 41/0 · sites 24/0 · contract_state_machine 46/0 · resp_party 12/0 · attribution 42/0 · rollback_flag 11/0 · operator_due_policy 21/0 · employee_settlement 49/0 · idempotency 14/0 — الباقي على خط الأساس §3
- **مؤشرات PLAN-01 §14:** مهامٌّ بدأت ومانعُها مفتوح = **0** · مهامٌّ أُعيد فتحُها بعد الإنجاز = 0

## يحتاج قرارَ المالك (حذفٌ أو قرارٌ من الستة فقط)

- **DEC-M41 · متراكمُ الدوريات الثلاث** (`docs/reports/dec_M41_periodic_backlog.md`) — **57 قسطَ تمويلٍ مستحقًّا بلا اعترافٍ محاسبي** (جدولُ السداد قائمٌ منذ 2026-02 وصفرُ حدثٍ منشور) · و**صفرُ قاعدةِ مخصصِ صيانةٍ مكتوبة** (وبلا قاعدةٍ لا مخصص) · و**صفرُ إقرارٍ ضريبيٍّ مقدَّم**. **والاعترافُ الرجعيُّ قرارُ سياسةٍ محاسبية لا أثرُ زرّ** — والمحرّكُ صحيحٌ من اليوم بأي حال
- **DEC-M30 · استدراكُ الإهلاك المتراكم** (`docs/reports/dec_M30_depreciation_catchup.md`) — كشف بناءُ M-30 بالقياس أن **الإهلاك لم يُحتسب قط منذ اقتناء الأصول**: 9 أصولٍ بصفٍّ واحدٍ لشهرٍ واحد (`2026-07`) و**صفرُ قيدٍ في الدفتر**. والاستدراكُ الكاملُ يقع بـ**~144 قسطًا بـ~8.9 مليون** (مقيسٌ بـ`--dry` **بلا كتابةِ صفٍّ واحد**). ثلاثةُ خيارات: **أ** استدراكٌ رجعيٌّ كامل · **ب** قيدٌ تصحيحيٌّ واحدٌ في فترةٍ مسمّاة (**رأيُ المنفِّذ**) · **ج** من الآن فصاعدًا والماضي يبقى معلَنًا. **والمحرّكُ صحيحٌ من اليوم بأي حال** — القرارُ يخصّ الماضي وحدَه

## سجلُّ المهام

| الموجة | الرمز | الشريحة | الحالة | التاريخ | ما بُني | الاختبار | مطابقة | اجتهادات |
|---|---|---|---|---|---|---|---|---|
| ③-ب | P-07 | — | **منجزة** | 2026-08-01 | بطاقةُ جردٍ `docs/specs/P-07_allocation_targets.md` — و**§1 مُلزِمة: لا تُعِد بناءَ التخصيص (منفَّذٌ في M-05)** فهذه **توسعةٌ لا هدم** · والمقيس: `fin_collection_allocations` قائمةٌ و`receivable_id` فيها **NOT NULL بمفتاحٍ أجنبيّ** ⇒ **الفاتورةُ هدفُها الوحيد** فلا سبيلَ لتخصيص سندٍ على مقدَّمٍ أو معلَمٍ أو محتجَزٍ أو ختامية · و«الفائضُ» **يُعلَن في رسالةٍ ثم يختفي**: `unallocated` **قيمةٌ في مصفوفة الإرجاع ولا عمودَ يحملها** — وهو **عينُ ما تمنعه قاعدةُ M-05 نفسِها** «رقمٌ يختفي أسوأُ من رقمٍ معلَن» · و**لا قيدَ بنيويًّا يمنع Σ من تجاوز السند** (الحلقةُ تحدّه لكن **نداءً ثانيًا أو كتابةً مباشرةً تتجاوزه**) · هجرة `2026_09_30`: `fin_payments` **+`allocated_amount`** بـ`CHECK ≤ amount` — **قيدُ «Σ ≤ السند» محمولٌ على السند**، گوتشا «`CHECK` لا يرى صفوفًا أخرى» **للمرة الخامسة** · **+`unallocated_amount` عمودًا مولَّدًا** (رصيدٌ ظاهرٌ لا يُكتب بيدٍ ولا يختفي) · **+`target_kind` الخمسة و`target_ref`** و`receivable_id` صار **يقبل NULL** و`uq_alloc_target` (سند × نوع × مرجع) و**`ck_alloc_target`**: الفاتورةُ وحدَها تحمل ذمّة · **ولا وحدةَ جديدة** — «الذمم والتحصيل» (**166**) بيتُ التخصيص (درسُ `H-13`) · `CollectionService::allocate` بالتحقُّق **كلِّه قبل أيِّ كتابة** و**الهدفُ يطابق نوعَه** (والأربعةُ غيرُ الفاتورة **أسطرُ خطةِ الدفع P-05** لا جدولٌ ثالث) و**الهدفُ يتحدث في مستلمِه** و**الزيادةُ رصيدٌ دائنٌ للعميل لا إيراد** · و`record()` وُسّعت **بسطرين ومنطقُها لم يُمَسّ** | `allocation_targets_test` **24/0 ×2** — **وفيه معيارُ §4 حرفيًّا**: سندٌ **10,000** على **مقدمٍ 5,000 وفاتورتين 3,000 و2,000** ⇒ **Σ التخصيصات = السند بالضبط والباقي صفر** وثلاثةُ أسطرٍ في القاعدة مجموعُها 10,000 · و**سطرُ المقدَّم بلا ذمّةٍ أصلًا** · و14,000 على 10,000 ⇒ **409 بالفائض 4,000 وصفرُ سطرٍ كُتب** · و**رفعُ العدّاد بكتابةٍ مباشرة يرفضه `CHECK`** · ومقدَّمٌ على سطرِ معلَمٍ **422** وهدفٌ مكرَّرٌ **409** وغيرُ موجودٍ **404** ومبلغٌ يفوق المتبقي **409** («رصيدٌ دائنٌ للعميل لا إيراد») ومسدَّدٌ **409** وسندُ صرفٍ **422** · و**سندٌ 6,000 على معلَمٍ 4,000 ⇒ 2,000 باقيةً في العمود** يعلنها لوحُ السندات · **واختبارُ M-05 بقي 24/0 كما هو** + **حزمةُ انحدارٍ 56 خضراء** | — (بذرُ 2088 يُكنس · و**صفرُ مساسٍ بمنطق `record()`**) | ① **الرقمُ المحسوبُ ليس رقمًا محفوظًا**: «الفائضُ يُعلَن» تحققت في M-05 **رسالةً** — وبقيت ناقصةً حتى صار **عمودًا مولَّدًا** يبقى بعد إغلاق الشاشة ② **والتوسعةُ تُقاس بما لم تكسره**: `record()` بقيت بمنطقها واختبارُها أخضرَ — والتوسعةُ سطران فيها لا إعادةُ كتابة ③ **ولا جدولَ ثالثٌ للأهداف**: الأربعةُ غيرُ الفاتورة **أسطرُ `contract_payment_schedule`** التي بُنيت في `P-05` — فالتخصيصُ يُحدّث مستلمَ السطر نفسِه ④ **وبابان لفعلٍ واحدٍ يفترقان**: لم تُسجَّل وحدةٌ جديدة، والتوسعةُ في الشاشة 166 حيث يسكن الفعل |
| ③-ب | P-06 | — | **منجزة** | 2026-08-01 | بطاقةُ جردٍ `docs/specs/P-06_contract_guarantees.md` — والمقيس: **لا جدولَ للضمانات في القاعدة كلِّها**، والقائمُ عمودٌ واحدٌ `contracts.guarantees` **MEDIUMTEXT نثرًا حرًّا** وفيه حيًّا «رهن سيارة» (×5) و«تأمين المشروع» (×3) و**نصُّ لوريمَ لا معنى له** في **9 عقودٍ من 10** ⇒ رهنٌ وتأمينٌ وخطابُ ضمانٍ **كلامٌ واحدُ الشكل**: لا يُعرف أهو **أصلٌ أم التزامٌ محتمل** ولا متى ينتهي ولا أيُخصم من مستخلص · والمحتجَزُ **مبالغُه مسجَّلة** (`claims.retention_amount` + `claim_lines`) **بلا تاريخِ ردٍّ ولا شروطِه** — «متى يُرد؟» **سؤالٌ بلا موضعٍ يجيبه** · **والمفارقةُ المقيسة**: جانبُ الموردين **مُهيكَلٌ بالفعل** (`supplier_contracts.performance_guarantee` · `guarantee_retention_days` · `supplier_contract_closures.guarantee_due_date/released_at`) **وجانبُ العملاء نثر** · هجرة `2026_09_29`: **`contract_guarantees`** سجلَّ **أدواتٍ وشروطٍ وتواريخِ ردّ** — والحسمُ في القيود: **`ck_cg_nature`** (المحتجَزُ **أصلٌ حتمًا** وما عداه **خارجَ الميزانية حتمًا** · و«أخرى» خارجَها لأن **الافتراضَ الآمن ألّا يصير شيءٌ أصلًا إلا بإعلان**) · **`ck_cg_deduct`** (**لا يُخصم من مستخلصٍ إلا المحتجَزُ النقدي** — §9-⑯ حرفيًّا) · **`ck_cg_dates`** (**ولكلِّ أداةٍ أفقُها**: المحتجَزُ **تاريخُ ردٍّ أو شرطُه** وغيرُه **انتهاءُ سريان**) · **`ck_cg_state_reason`** (ولا أداةَ تخرج صامتة) · **ونقلُ النثر**: 9 صفوفٍ صُنّفت **بمطابقةٍ حرفيةٍ لنصٍّ قائم لا بتخمين**، و`source_text` يحمل النص و`needs_review=1` على كلٍّ — **والعمودُ الأصليُّ لم يُمَسّ** · `ContractGuaranteeService` بثلاث قواعد: **① الطبيعةُ محكومةٌ بالنوع** · **② ولا يُخصم إلا المحتجَز** · **③ والرصيدُ يُقرأ من مصدره** (`claims` + `claim_lines` — **ولا جدولَ ثالث**، درسُ `M-01` حرفيًّا؛ وقيمةُ سطر السجل **سقفٌ متعاقَدٌ عليه لا رصيد**) · الشاشةُ **177** تُري **عمودين لا مجموعًا** | `contract_guarantees_test` **34/0 ×2** — **وفيه البرهان**: خطابُ ضمانٍ 50,000 وتأمينٌ 20,000 ⇒ **الأصلُ صفرٌ** والالتزامُ المحتملُ **70,000**، وقائمةُ **ما يجوز خصمُه من مستخلص فيها المحتجَزُ وحدَه** · و**قلبُ الطبيعة أو قابليةِ الخصم بكتابةٍ مباشرة يرفضه `CHECK`** · ومستخلصٌ باحتجازِ 1,000 ⇒ **الأصلُ 1,000 لا سقفُ السطر 6,000**، وردٌّ 400 ⇒ **600**، **وسطرُ السجل لم يتغيّر** · وطبيعةٌ مختارةٌ **422 ×2** وخصمٌ لغير المحتجَز **422** وأداةٌ بلا أفقٍ **422 ×2** وخروجٌ بلا سببٍ **422** · و**9 أداةٍ نُقلت والنصُّ باقٍ في 9 عقود** و**صفرُ منقولٍ صار أصلًا** + **برهانُ HTTP للشاشة 177: 12/0** + **حزمةُ انحدارٍ 55 خضراء** | — (بذرُ 2086/2087 يُكنس · و**صفرُ مساسٍ بـ`contracts.guarantees`** · وصفوفُ النقل التسعةُ **تنتظر إقرارَ المالك** بـ`needs_review`) | ① **الطبيعةُ المحاسبية ليست خيارَ مستخدم**: الأصلُ والالتزامُ المحتملُ يختلفان في الميزانية اختلافًا لا يُصلحه تنبيه — فحُسم في `CHECK` ② **والافتراضُ الآمن «خارجَ الميزانية»**: الاتجاهُ الخطِرُ أن يصير شيءٌ **أصلًا** بالصدفة، فما لا يُعرف نوعُه لا يُحسَب أصلًا ③ **ولا جدولَ ثالثٌ للمبالغ**: مبالغُ المحتجَز مسجَّلةٌ في مصدرَين، وتكرارُها يفتح مصدرًا ثانيًا للرقم الواحد — فالسجلُّ **شروطٌ وتواريخُ ردٍّ** والرصيدُ يُقرأ من `claims` (نظيرُ `advance_balance` حرفيًّا) ④ **والنقلُ اقتراحٌ لا إقرار**: تصنيفٌ من مطابقةٍ حرفيةٍ + `source_text` + `needs_review` — **فالآلةُ تقترح والمالكُ يُقرّ**، ولا نثرَ يُمحى |
| ③-ب | P-05 | — | **منجزة** | 2026-08-01 | بطاقةُ جردٍ `docs/specs/P-05_contract_payment_schedule.md` — و**المقيسُ الأول**: وثيقةُ **PLAN-03 نفسُها كانت خارج المستودع** والملحقُ يحيل إليها بوصفها توجيهًا نافذًا ⇒ نُسخت إلى `docs/update0001/` وهي المصدرُ الوحيدُ للأنماط الثمانية وحقولِ السطر واختباراتِ §9 · **والمقيسُ الثاني**: النظامُ يعرف **ما قُبض** (`contract_advances`) و**ما فُوتر** (`claims` + `fin_receivables`) — و**لا موضعَ يعرف ما استُحقّ**، فلا توقُّعَ ولا «متأخر» **والتأخرُ يُكتشف بالمصادفة**؛ وشروطُ السداد في الرأس **نصٌّ حرّ** (`payment_time` VARCHAR(50)) و**تاريخٌ واحدٌ للعقد كلِّه** (`payment_date`)؛ و`contract_advances` **بلا نوع** فالمقدمُ المستهلَك ورسومُ التعبئة ورسومُ الحجز غيرِ المستردة ودفعةُ المعلَم **تسكن صفًّا واحدَ الشكل** — و§6: «**الخلطُ بينها يقلب التزامًا إلى إيرادٍ أو العكس**» · هجرة `2026_09_28`: **`contract_payment_schedule`** بالأنماط الثمانية ENUMًا وأنواعِ الدفعة الستة و**أنواعِ المقدم الأربعة** وحقولِ السطر السبعة والإصدارات (`version` + `effective_from/to` + `amendment_id`) · و**القيدُ الحاكمُ بنيويّ** `ck_cps_treatment`: المستهلَكُ ⇒ **التزامٌ حتمًا** · والحجزُ والمعلَمُ ⇒ **إيرادٌ حتمًا** · و**التعبئةُ وحدَها بنص العقد** ومعها `treatment_basis` **إلزاميّ** · و`ck_cps_advance_link`: **لا يُوصَل بدفتر السلف إلا الالتزام** · و`remaining_amount` **مولَّدًا** · و`ck_cps_due`: **لا سطرَ بلا تاريخِ استحقاقٍ أو شرطِه** · `ContractPaymentScheduleService` بأربع قواعد: **① الأنواعُ لا تُخلط** · **② ودفترُ السلف للالتزام وحدَه** (والالتزامُ يُقبض بـ`advance_record` **نفسِها** فلا مصدرَ ثانيًا للرقم) · **③ والزائدُ يُعلَن ولا يُبتلع** (409: «رصيدٌ دائنٌ للعميل لا إيراد» وبيتُه `P-07`) · **④ والملحقُ يفتح نسخةً والمقبوضُ يُرحَّل** · والتوليدُ **من الجدول لا من التخمين**: شهرُ التوقف **لا سطرَ دفعٍ له** وردُّ المحتجز **بشرطٍ لا بتاريخ** · الشاشةُ **176** | `contract_payment_schedule_test` **51/0 ×2** — **وفيه البرهان**: قبضُ **رسومِ تعبئةٍ إيرادًا** ⇒ **رصيدُ المقدم لم يزد بمليم** و`contract_advances` **صفٌّ واحدٌ لا اثنان**؛ وقبضُ **مستهلَكٍ** ⇒ الرصيدُ **12,000** بمرجعِ قبضه · و**قلبُ المعالجة بكتابةٍ مباشرة يرفضه `CHECK`** ومثلُه **وصلُ سطرٍ إيراديٍّ بدفتر السلف** · ومستهلَكٌ بإيرادٍ **422** وحجزٌ بالتزامٍ **422** وتعبئةٌ بلا إعلانٍ **422** وبلا نصِّ عقدٍ **422** ومقدمٌ بلا نوعٍ **422** — **وصفرُ سطرٍ يُكتب في الخمس** · و**11 تسويةً لا 12** وكانونُ الأول **10,000 من الجدول لا بالقسمة** · وتوليدٌ ثانٍ **409** وتسويةٌ يدويةٌ **422** وسطرٌ بلا استحقاقٍ **422** وقبضٌ زائدٌ **409** وعلى مختومةٍ **423** ونسخةٌ راجعةٌ **422** · و**الشرطيُّ لا يتأخر أبدًا** و**11 سطرًا متأخرًا أُعلنت لا اكتُشفت** + **برهانُ HTTP للشاشة 176: 21/0** + **حزمةُ انحدارٍ 54 خضراء** | — (بذرُ 2084/2085 يُكنس · و**صفرُ مساسٍ بـ`contract_advances` بنيةً**) | ① **ما يحسمه الحسابُ لا يُقلَب وما يحسمه العقدُ يُعلَن**: ثلاثةُ أنواعٍ معالجتُها حتميةٌ في `CHECK`، والتعبئةُ وحدَها اختيارٌ **مشروطٌ بنصٍّ مكتوب** — فلا «افتراضٌ» يقلب التزامًا إيرادًا ② **ولا مصدرَ ثانيًا للرقم**: القبضُ الالتزاميُّ يمرّ بـ`advance_record` القائمة، فرصيدُ الاستهلاك يبقى تعريفًا واحدًا (درسُ M-01 نفسُه) ③ **والامتناعُ عن قيدٍ قرارٌ مقيس**: «Σ الخطة = قيمةُ العقد» **يكذب** على كل عقدٍ فيه مقدَّم — فلم يُفرَض، **وأُعلن سببُ عدم فرضه** في الهجرة والشاشة معًا ④ **والوثيقةُ الحاكمةُ تُقاس كما يُقاس الكود**: أهمُّ ما في هذه المهمة أنها كشفت أن مصدرَها **لم يكن في المستودع** |
| ③-ب | P-04 | — | **منجزة** | 2026-08-01 | بطاقةُ جردٍ `docs/specs/P-04_contract_resource_plan.md` — و**أخطرُ ما قيس في الموجة**: `contractequipments` (12 صفًّا · 24 عمودًا · 9 عقود) تحمل خطةَ الموارد **والسعرَ معًا** (`equip_price` · `equip_price_currency` · `equip_total_contract` — و**12 من 12 صفًّا تحمل سعرًا**)، و**الحاوياتُ الجذرُ تُبذَر منها حرفيًّا**: قياسٌ على الحيّ يُظهر `op_containers.contract_item_id` = `contractequipments.id` و`cap_qty` = `equip_total_contract` **بالضبط** في الحاويات الست ⇒ **خطةُ المعدات مصدرُ المال ومصدرُ الطاقة معًا** وهو عينُ الازدواج الذي حسمته `P-02` في جانب القيمة · و`capacity_share_percent` **لا وجودَ له في أي جدولٍ من القاعدة** · هجرة `2026_09_27`: **`contract_resource_plan` بـ30 عمودًا ولا عمودَ مالٍ واحدًا فيها** (لا سعرَ ولا عملةَ ولا مبلغ — **فالفصلُ صار في الجدول لا في الاتفاق**) + `client_contract_lines` +`resource_share_total` عدّادًا بـ`CHECK ≤ 100` — **نمطُ «الأبُ يحمل العدّاد» للمرة الرابعة** + عمودٌ مولَّد `live_type_key` مع `UNIQUE` ⇒ **نوعٌ واحدٌ نافذٌ لكل بند** والمنتهيةُ تخرج من القيد (حيلةُ `P-01`: MySQL لا يقيّد NULL) + `op_containers` +`resource_plan_id` **يقبل NULL** (وصلٌ إضافيٌّ لا يغيّر سلوكًا و`contract_item_id` القديمُ **لا يُمَسّ**) · `ContractResourcePlanService` بأربع قواعد: **① الخطةُ لا تحمل مالًا** (ولا كتابةَ في `fin_*` بتاتًا) · **② وΣ الحصص ≤ 100 أبدًا** والمائةُ التامة **شرطُ الاكتمال لا شرطُ الإدراج** · **③ وصفرُ الحصة يُفسَّر باسمه** (منتجٌ بصفرٍ **يُصحَّح آليًّا** إلى احتياطي · ومساندٌ بحصةٍ **422**) · **④ والتعديلُ إنهاءٌ وإضافةٌ لا محو** · والطاقةُ = الكمية × الحصة و**آخرُ نوعٍ يبتلع باقيَ التقريب** (درسُ `M-30`) · وحدُّ اليوم **الورديّات × الساعات ≤ 24** ⇒ 422 · الشاشةُ **175** تُري الحصصَ والطاقةَ والفجوةَ — **ولا خانةَ سعرٍ واحدة** | `contract_resource_plan_test` **39/0 ×2** — **وفيه البرهان**: خطةُ مواردَ كاملةً (60% + 40%) ⇒ **قيمةُ العقد 60,000 USD قبلها وبعدها — لم تتغيّر بمقدار ذرة** وعددُ بنود القيمة كما هو · **وبنيةً**: مسحُ `information_schema` ⇒ **صفرُ عمودٍ** فيه `price\|amount\|currency\|cost\|value\|rate\|tax\|total` · **والطاقةُ 7,200 + 4,800 = 12,000 = المتعاقَد** لا نصفان متساويان · و**33.333 + 33.333 + 33.334 على 10,000 ⇒ 10,000 بالضبط لا 9,999.99** · 115% ⇒ **409 بالفائض 15% وصفرُ صفٍّ كُتب** · ورفعُ العدّاد بكتابةٍ مباشرة **يرفضه `CHECK`** · ومنتجٌ بصفرٍ **يُصحَّح** · ومساندٌ بحصةٍ **422** · و3×10 ساعاتٍ **422** · والتعديلُ **يزيد الصفوف 4 ⇒ 6** والمنتهيةُ باقيةٌ بأسبابها · وإنهاءٌ بلا سبب **422** وإنهاءُ المنتهي **409** · وبندٌ مقطوعٌ **422** وموقعٌ من عقدٍ آخرَ **422** وبندٌ منتهٍ **409** + **برهانُ HTTP للشاشة 175: 16/0** (وفيه: **«ولا خانةَ سعرٍ واحدة» فحصًا نصّيًّا على النموذج**، و**شاشةُ البنود ما زالت تقول 60,000 بعد خطةٍ كاملة**) + **حزمةُ انحدارٍ 53 خضراء** | — (بذرُ 2082/2083 يُكنس · و**صفرُ مساسٍ بـ`contractequipments`** · وحارسُ قائمة الدور 1 رُفع **26 ⇒ 27** بإعلانِ سببه وتاريخه كما يشترط نصُّه) | ① **الفصلُ بنيويٌّ لا اتفاقي**: «الخطةُ لا تدخل القيمة» تبقى شعارًا ما دام الجدولُ يقبل عمودَ سعر؛ فحُسم بألّا يوجد العمودُ أصلًا — **والاختبارُ يفحص البنيةَ لا السلوكَ فقط** ② **والقديمُ يبقى شاهدًا**: `contractequipments` لا تُمَسّ ولا تُجمَّد وتظل تغذّي الحاويات الست، لأن `EMS_BASELINE_GATE` مطفأٌ والملحق §2-② يمنع القلبَ على الجميع ③ **ولا بذرَ صامتًا**: بنودُ البيع الحية **صفر**، فالبذرُ اليوم **يخترع بنودًا لا وجودَ لها** — والخريطةُ محسوبةً في `docs/reports/p04_resource_backfill_plan_20260801.md` (**Σ الحصص = 100% في كل عقدٍ من التسعة**) ④ **وثلاثةُ صفوفٍ من القديم يرفضها الحارسُ الجديد** (30 و40 و40 ساعةً في يوم) — **كشفٌ لا عائق**: الرقمُ المستحيلُ كان يُكتب لأن القديمَ لا يفحص، ولو بُذر آليًّا لانتقل الخللُ ساكنًا ⑤ **وطلبُ العمالة ليس استحقاقًا**: الأعدادُ تُخطَّط ولا تُصرَف — ولا مسارَ من هذه الخدمة إلى `fin_*` |
| ③-ب | P-03 | — | **منجزة** | 2026-08-01 | بطاقةُ جردٍ `docs/specs/P-03_contract_monthly_plan.md` — والمقيس: القائمُ `contract_commitments` **كميةٌ واحدة + دورية** (`daily`/`monthly`/`contract`)، فـ«20,000 طنٍّ على مدى العقد» **تُقسَّم بالتساوي ضمنًا أو لا تُقسَّم** — **ولا موضعَ في النظام يقول «آذارُ تعبئةٌ بـ500 وتموزُ توقفٌ بصفر»** وهو **واقعُ كل عقدٍ موسمي** · هجرة `2026_09_26`: `client_contract_lines` **+2** (`qty_planned_total` عدّادًا · `plan_sealed_version`) و**`CHECK`: العدّادُ ≤ المتعاقَد** — **نمطُ «الأبُ يحمل العدّاد» للمرة الثالثة** (`op_containers` · `rfq_lines` · وهنا) لأن **`CHECK` لا يرى صفوفًا أخرى** + **`contract_monthly_plan`** بـUQ(بند × نسخة × شهر) و`month_kind` **بأسماء الحالات** (اعتيادي · **تعبئة** · تصاعد · **توقف** · صيانة) و**`CHECK`: الكميةُ ≥ 0 — والصفرُ مسموحٌ عمدًا** · `ContractMonthlyPlanService` بأربع قواعد: **① Σ لا يتجاوز المتعاقَد** (409 بالفائض · **والفحصُ قبل أي كتابة**) · **② والمساواةُ التامة شرطُ الختم لا شرطُ الإدراج** (الناقصةُ تُحفظ **بفجوةٍ معلَنة** و`seal` **422 بالفجوة**) · **③ وصفرٌ بنوعٍ `normal` يُصحَّح آليًّا إلى `shutdown`** فلا صفرَ بلا تفسير · **④ والملحقُ يفتح نسخةً لا يعدّل** (المختومةُ **423**) و`effectivePlan(تاريخ)` تعطي **النسخةَ التي حكمت ذلك اليوم** · الشاشةُ **174** تُري الفجوةَ والأشهرَ الغائبةَ بخلفيةٍ مميَّزة | `contract_monthly_plan_test` **21/0 ×2** — **وفيه البرهان**: **200 تعبئةً + صفرٌ توقفًا + 10×1180 = 12,000 = المتعاقَد** و**القيمةُ السنوية 60,000 صحيحة** · **وكانونُ الثاني 1,000 لا 5,000** و**القسمةُ المتساوية لم تقع** · 18,000 ⇒ **409 بالفائض 6,000 وصفرُ صفٍّ كُتب** · رفعُ العدّاد **يرفضه `CHECK`** · ختمُ الناقصة **422** · المختومةُ **423** · وشهرٌ خارج المدة **422** · **وآذارُ بالنسخة 1 وأيلولُ بالنسخة 2** + برهانُ HTTP للشاشة 174 + **حزمةُ انحدارٍ 52 خضراء** | — (بذرُ 2081 يُكنس · و**صفرُ مساسٍ بـ`contract_commitments`**) | ① **الكميةُ منحنًى لا رقم**: القسمةُ المتساوية **افتراضٌ خفيّ** يُنتج قيمةً شهريةً كاذبةً في كل عقدٍ موسمي — والاختبارُ يثبت أنها **لم تقع** ② **المساواةُ شرطُ الختم لا الإدراج**: لو اشتُرطت عند الإدراج لاستحال بناءُ خطةٍ على مراحل؛ ولو لم تُشترط أصلًا لصار «Σ = المتعاقَد» شعارًا ③ **الصفرُ يُفسَّر آليًّا**: كميةُ صفرٍ بنوعٍ اعتياديٍّ **تناقض**، فالخدمةُ تسمّيها `shutdown` بدل أن تقبل صفرًا صامتًا ④ **گوتشا مقيسة في القراءة**: `periodValue` كانت تفترض «اليوم» فأعطت **صفرًا** لخطةِ سنةٍ مستقبلية؛ والافتراضُ الصحيحُ **بدايةُ الفترة المسؤولِ عنها** — وإلا **تغيّر تقريرُ سنةٍ ماضيةٍ كلما مرّ يوم** ⑤ **P-05 لم تُستبق**: خطةُ الدفع تعتمد هذا الجدولَ وتأتي بعده |
| ③-ب | P-02 | — | **منجزة** | 2026-08-01 | بطاقةُ جردٍ `docs/specs/P-02_client_contract_lines.md` **قبل البناء** — والمقيسُ يطابق §1: **لا دالةَ تحسب «قيمة العقد» في الشجرة كلِّها** (بحثُ `contract_value`/`contractValue`/«قيمة العقد» أعطى ملفًّا واحدًا لا علاقةَ له) — **فالخطرُ لم يقع بعدُ لأن أحدًا لم يحسبها قط، ويتحقق لحظةَ بنائها** · و`commitment_type` **يخلط محورين في ENUM واحد**: كمياتٍ تُفوتَر (`total_qty` 2 · `period_qty` 2 · `min_guaranteed` 1) وطاقةً لا تُفوتَر (`equipment_count` 2 · `daily_availability_hours` 4 · `capacity_support` 1) · و**حاملُ السعر اليوم `contractequipments.equip_price` (12 صفًّا)** — فحسابُ القيمة منه **ومن الأطنان معًا** يضاعف الإيراد · هجرة `2026_09_25`: **`client_contract_lines`** بنموذجٍ وسعرٍ وسريانٍ وحالةٍ ضريبية · UQ(شركة × عقد × رقم) و**UQ(عقد × التزام × سريان)** («نسختان لا تكديس») · و**أربعةُ `CHECK`** أهمُّها **الخاضعُ يلزمه رمزٌ ضريبيّ** · `ContractLineService` بأربع قواعد: **① الطاقةُ لا تصير بندَ بيع** (**422 باسم النوع** ونصُّه يقول «يضاعف الإيراد») · **② ولا سعرَ بلا مرجعٍ ضريبيّ** (والرمزُ غيرُ المسجَّل 422) · **③ والسريانُ لا يتداخل** (409) **والتغييرُ نسخةٌ تُخلِف** (القديمةُ تُغلق عند **سريان الجديدة − يوم** **بسعرها الذي لم يُمسّ**) · **④ و`contractValue()` هي الدالةُ الوحيدة** — بكل عملةٍ على حدة **وتُعلن ما استبعدته باسمه** · **وقاعدةُ قراءةٍ جوهرية**: **بلا تاريخ = النافذُ وحدَه · وبتاريخٍ = النافذُ والمستبدَلُ داخل نافذته** (نظيرُ قاعدة E-24 حرفيًّا) · الشاشةُ **173** تُري الفصلَ نفسَه: التزاماتُ الطاقة **لا تظهر في قائمة الاشتقاق** وتُعلَن مستبعَدةً | `contract_lines_test` **23/0 ×2** — **وفيه البرهان**: **95,000 USD من الأطنان وحدَها** و**القيمةُ الساذجةُ 116,000 لم تقع** · عددُ المعدات وساعاتُ الإتاحة **422 باسميهما** · خاضعٌ بلا رمزٍ **422 ومحوُ الرمز يرفضه `CHECK`** · تداخلُ السريان **409** · إعادةُ تسعيرٍ رجعيةٌ **422** · **وآذار 95,000 وآب 110,000 بنسختين لا تُجمعان** · **وعملتان منفصلتان** · + برهانُ HTTP للشاشة 173 + **حزمةُ انحدارٍ 51 خضراء** | — (بذرُ 2082 يُكنس · و**صفرُ مساسٍ بـ`contract_commitments` أو `contractequipments`**: كلاهما يبقى مصدرَ الالتزام والطاقة، والجديدُ مصدرَ القيمة وحدَه) | ① **الفصلُ بجدولٍ لا بعمود**: لو حُمِّل `contract_commitments` عمودَ سعرٍ لبقي المحوران مختلطين وصار التمييزُ **اتفاقًا يُنسى**؛ فالجدولُ المستقلُّ يجعل «ما يحمل المال» **سؤالًا له جوابٌ واحد** ② **گوتشا وقعت في كودي أنا**: تعبيرٌ ثلاثيٌّ يفحص القيمةَ الافتراضية ثم **يقرأ `$a['state']` غيرَ الموجود** فمرّ `''` — و**ENUM ابتلعه صامتًا** (sql_mode فارغ) فكُتب بندٌ **بحالةٍ فارغةٍ لا تراه قيمةُ العقد**؛ والاختبارُ كشفه لأنه يقيس **الرقمَ لا نجاحَ الإدراج** ③ **المستبدَلُ يُقرأ بتاريخه**: إسقاطُه يمحو التاريخَ ويجعل «المقارنةَ التاريخية» دعوى — والدرسُ نفسُه تكرّر من E-24 ④ **`lump_sum` بكميةِ 1**: مقطوعٌ بلا كميةٍ يجعل «الكمية × السعر» بلا معنى، فالكميةُ تُضبط لا تُترك ⑤ **P-04 لم تُستبق**: `capacity_share_percent` بيتُها خطةُ الموارد — وهذه المهمةُ **تمنع دخولَها القيمة** ولا تبنيها |
| ③-ب | P-01 | — | **منجزة** | 2026-08-01 | بطاقةُ جردٍ `docs/specs/P-01_contract_operational_sites.md` **قبل البناء** — و**القياسُ يطابق §1 الملزمة حرفًا بحرف**: `project_id` قائمٌ بـ**صفر NULL** · و`site_id` بمفتاحٍ خارجي و**صفر NULL** و**كلُّ المواقع الـ19 `is_default=1` وواحدٌ لكل مشروع** (أي أن العمودَ **مرآةٌ للمشروع لا بُعدٌ مستقل**) · و**صفرُ استعمالٍ له** في `Contracts/` و`app/Services/Contract/` — **والخللُ الحقيقي**: «نطاقُ العقد» مفهومٌ **بأربعة أبعاد** (اسمٌ · مدةٌ · موقعٌ · بنود) اختُزل في عمودٍ يشير إلى موقعٍ واحد، **فعقدٌ يعمل في موقعين لا يمكن تمثيلُه أصلًا** · هجرتان `2026_09_22` و`2026_09_23`: **`contract_operational_sites`** بـUQ(شركة × عقد × موقع) و**`UQ(عقد × primary_flag)` بعمودٍ مولَّدٍ (1 أو NULL)** — «رئيسٌ واحدٌ على الأكثر» **بنيويًّا** (وحيلةُ NULL لأن UNIQUE على `is_primary` كان سيمنع نطاقين غيرَ رئيسيين) — وثلاثةُ `CHECK` · **وتعبئةٌ رجعيةٌ من الواقع**: 10 عقودٍ ⇒ **10 نطاقاتٍ رئيسية** كلٌّ بمرجعه المكتوب · و**`contracts.site_id` يبقى** موسومًا في تعليقه «مرآةٌ موروثة» · و**`project_id` NOT NULL + مفتاحٌ خارجيٌّ إلى `project`** · و**VIEW `client_contracts`** + `docs/specs/PLAN03_NAME_MAP.md` · `ContractSiteService` بأربع قواعد: **① النطاقُ داخل مدة عقده** (422 بتاريخيه) · **② والموقعُ مرةً واحدة** (409) · **③ ورئيسٌ واحدٌ** والنقلُ **ذرّي** · **④ والإقفالُ بسببٍ مكتوب** · الشاشةُ **172** بلوح «عقودٌ بلا نطاق» | `contract_sites_test` **24/0 ×2** (عقدٌ بلا مشروعٍ **ترفضه البنية** · **10 عقودٍ = 10 نطاقاتٍ رئيسية** · تكرارُ الموقع **409** · **وموقعٌ ثانٍ يُقبل — وهو ما كان مستحيلًا** · النقلُ يترك **رئيسًا واحدًا** و`UNIQUE` يرفض الثاني · **بدايةٌ قبل العقد ونهايةٌ بعده 422** · إقفالٌ بلا سببٍ **422 و`CHECK`** · **VIEW بالعدد نفسِه** ويُظهر النطاقَ الرئيسي · **والمستخلصاتُ 1 بـ106.20 قبلَ وبعد**) + برهانُ HTTP للشاشة 172 + **حزمةُ انحدارٍ 50 خضراء** | `docs/reports/p01_hierarchy_relink_20260801.md` — **صفرُ فرقٍ**: العقودُ 10=10 · المستخلصاتُ 1=1 بمجموع **106.20 = 106.20** · الأسطرُ 5=5 · **وصفرُ صفٍّ في `contracts` عُدِّلت قيمتُه** (التعبئةُ قراءةٌ وإدراجٌ في جدولٍ جديد) | ① **`NOT NULL` وحدَه لا يمنع «بلا مشروع»**: `@@sql_mode` **فارغٌ** في هذه البيئة فإدراجٌ يُغفل `INT NOT NULL` **يكتب صفرًا صامتًا** — **النظيرُ الحرفيُّ لگوتشا ENUM**؛ والحارسُ الحقيقيُّ **مفتاحٌ خارجي** ② **`CHECK` يقع لحظةَ الإدراج**: تعبئةُ `close_reason` بـ`UPDATE` لاحقٍ **رفضت الدفعةَ كلَّها** — فالسببُ في الإدراج نفسِه ③ **«رئيسٌ واحد» بعمودٍ مولَّد**: `UNIQUE(عقد، is_primary)` كان سيمنع نطاقين **غيرَ رئيسيين** — فالحلُّ `1` أو **`NULL`** وMySQL لا تقيّد الـNULLات ④ **گوتشا بيئيةٌ عامة**: `CURDATE()` (MySQL محلي) و`date()` (PHP UTC) **يختلفان يومًا كاملًا ثلاثَ ساعاتٍ يوميًّا** — كشفه عبورُ منتصف الليل، و`period_lock_test` كان **يبذر بساعةٍ ويقيس بأخرى** (خللٌ كامنٌ منذ كتابته) فصُحّح ⑤ **فجوةُ بياناتٍ لا انحدار**: `fin_financial_periods` بسبعة أشهرٍ لسنةٍ مفتوحة، فمع أغسطس **صفرُ فترةٍ تغطي اليوم**؛ أُكملت الخمسةُ بحالة **`planned`** — والحارسُ ينصّ أنها **ليست مقفلة** فصفرُ تغييرٍ سلوكي |
| ③ | H-21 | — | **منجزة** | 2026-07-31 | بطاقةُ جردٍ `docs/specs/H-21_supplier_rfq.md` — والمقيس: الجداولُ الثلاثةُ **معدومة** · و**`quotations` القائم عروضُ مبيعاتٍ للعملاء لا RFQ موردين** فلا يُعاد استعمالُه · ومصدرُ الكميات `contract_commitments` بـ(`client` × `company`) — **ما نلتزم به للعميل هو ما نطلب له موردًا** · ومصدرُ «السجل» `supplier_evaluations` (M-17) · هجرة `2026_09_21`: `supplier_rfqs` بحالاتِ §8.2 الست و`CHECK`ين + **`rfq_lines` بـ`UNIQUE`(طلب × التزام) و`qty_awarded` عدّادًا بـ`CHECK ≤ qty_required`** — **نمطُ `op_containers.allocated_qty` حرفيًّا** (گوتشا «`CHECK` لا يرى صفوفًا أخرى») + `rfq_quotes` بـUQ(بند × مورد) والمعاييرِ الثلاثة أعمدةً + `rfq_awards` **بحجّة الاختيار حين لا يكون الأرخص** · `RFQService` بخمس قواعد: **① اشتقاقًا لا إدخالًا** (وعقدٌ بلا التزاماتٍ **422** ولا حقلَ لكميةٍ في الشاشة) · **② والإقفالُ يحكم حالةً وتاريخًا** (**423**) · **③ والعزلُ متبادلٌ بـ403 تُسجَّل** لا بفراغٍ صامت · **④ والترسيةُ تُفحص قبل أي كتابةٍ فـ409 بقيمة المتاح** — **ولا تُرسى كميةٌ بلا عرضٍ مقدَّم** · **⑤ ولا حدثَ ماليًّا**: حقائقُ محايدةٌ وحدَها · الشاشةُ **171** بجدول مقارنةٍ يسم الأرخصَ والأسرعَ والأعلى سجلًّا | `rfq_cycle_test` **25/0 ×2** (سيناريو §8.3 حرفيًّا: عقدٌ بلا التزاماتٍ **422** · بندٌ **20000 طن بمرجع التزامه** · عرضٌ على مسودةٍ **423** وبعد الموعد **423** · **إعادةُ التقديم استبدالٌ لا تكديس** · **403 عند قراءة عرض الآخر** · الترسيةُ قبل الإقفال **423** · **12k+9k=21k ⇒ 409 بالمتاح 20000 وصفرُ ترسيةٍ كُتبت** · **و12k+8k=20k** · و`CHECK` يرفض تجاوزَ العدّاد · وموردٌ بلا عرضٍ يُرفض · **وصفرُ حدثٍ ماليّ و4 حقائقَ محايدة**) + برهانُ HTTP للشاشة 171 + **حزمةُ انحدارٍ 49 خضراء** | — (بذرٌ بمرجع عقدٍ معزولٍ يُكنس · و**صفرُ مساسٍ بـ`quotations`**) | ① **غرضان لا غرض**: إعادةُ استعمال `quotations` كانت تخلط **بابَ البيع ببابِ الشراء** — والاسمُ المشترك أغرى بها ② **العدّادُ على الأب** هو الحلُّ الوحيدُ لـ«Σ ≤ الالتزام» — والقاعدةُ مثبَتةٌ من H-01 وأُعيد استعمالُها لا اختراعُ غيرها ③ **الفحصُ قبل الكتابة**: 409 تُحسب على **مجموع** ما يُطلب في الدفعة الواحدة لا على كلِّ سطرٍ وحدَه — وإلا مرّ 12k+9k بسطرين ④ **403 تُسجَّل ولا تُفرَغ**: إرجاعُ قائمةٍ فارغةٍ للمورد الفضولي يخفي المحاولةَ ويوهمه أن لا عروضَ ⑤ **«السجل» من M-17**: المعيارُ الثالثُ له مصدرٌ حيٌّ — وغيابُه `NULL` معلَنٌ لا صفرٌ يظلم موردًا ⑥ **`ContainersAllocated` لم تُستبق**: وصلُ الترسية بالحاويات قرارُ تسلسلٍ، وبيتُه H-01/H-06 |
| ③ | H-13 | — | **منجزة** | 2026-07-31 | بطاقةُ جردٍ `docs/specs/H-13_bank_reconciliation.md` — والمقيس: الجداولُ الثلاثةُ معدومة، والقائمُ `fin_bank_statement_lines` (**11 عمودًا وصفّان**) **بلا رأسِ كشفٍ** (فلا مدى ولا رصيدا افتتاحٍ وإقفال) **وبلا مرجعٍ بنكيّ** (فإعادةُ الاستيراد **تُضاعف بلا مانع**) و`matched_payment_id` **مضاهاةٌ بعمودٍ واحد** بلا قاعدةٍ ولا فرقٍ ولا قرار · هجرة `2026_09_20`: **`bank_statements`** بـUQ(شركة × حساب × مرجع) و`CHECK` يوجب وقتَ الإقفال ومُقفِله + **`bank_statement_lines`** بـ**`line_key` بصمةِ (كشف × مرجع × تاريخ × اتجاه × مبلغ) و`UNIQUE` عليها** ⇒ «Idempotent بمفتاح السطر» **بنيويًّا** + **`bank_recon_matches`** بـUQ(سطر البنك) و**الفرقُ عمودٌ مولَّدٌ لا يُكتب** و**`CHECK`ان**: **فرقٌ مفتوحٌ يلزمه سبب** و**الحسمُ يلزمه حاسم** · `BankReconService` بخمس قواعد: **① الاستيرادُ عاطلٌ بمفتاح السطر** (وسطرٌ **بلا مرجعٍ يُرفض ويُعلَن** · والترقيمُ يتابع) · **② والمضاهاةُ بقاعدةٍ تُحفظ في الصف** (المرجعُ أولًا ثم المبلغُ والتاريخُ **± 3 أيام**) · **③ ولا سندٌ يُطابَق مرتين** · **④ والفرقُ يُفتح بسببٍ ويُحسم بقيدِ تسويةٍ بمرجع الفرق** (`recon_adj:‹مضاهاة›`) **والسندُ الأصليُّ لا يُمسّ** · **⑤ ولا إقفالَ وفرقٌ مفتوح** · **و«بلا نظير» ليس صمتًا**: صفُّ مضاهاةٍ بـ`payment_id=NULL` **يحجب الإقفال** حتى يُحسم بيد · **الشاشة 103 تتوسّع ولا تُستبدل** (استيرادٌ · مضاهاةٌ آلية · بطاقتا نسبةِ المطابقة والفروقِ المفتوحة · قرارٌ لكل سطر · إقفال) **والدورةُ القديمةُ تبقى معلَنةً أسفلَها** | `bank_reconciliation_test` **28/0 ×2** (**4 أسطرَ أُدرجت وسطرٌ بلا مرجعٍ رُفض** · **استيرادٌ ثانٍ: صفرُ سطرٍ جديد و4 متخطّاة** · طُوبق **بالمرجع** وطُوبق **بالمبلغ والتاريخ** رغم اختلاف المرجع · **فرقُ 200 محسوبًا** · **وسطرٌ ثانٍ لا يلتقط سندًا مرتبطًا** · فتحٌ بلا سببٍ **422 و`CHECK`** · **قيدُ تسويةٍ بفرقِ 200 والسندُ 3000 لم يُمسّ** · حسمٌ ثانٍ **409** · **الإقفال 423 بأسمائها** ثم يقع · واستيرادٌ على مقفلٍ **423**) + برهانُ HTTP للشاشة 103 + **حزمةُ انحدارٍ 48 خضراء** | — (بذرُ 2085 يُكنس · و**صفرُ مساسٍ بالقائم**: `fin_bank_statement_lines` وصفّاه كما هما) | ① **لا وحدةَ جديدة**: كِدتُ أسجّل «المطابقة البنكية» وحدةً **170** — والقياسُ كشف أنها **مسجَّلةٌ 103**؛ والدرسُ **مكتوبٌ في الهجرة نفسِها** لا في رأسي ② **گوتشا مقيسة تكرّرت**: `fin_payments.direction` قيمُه **`disbursement`/`collection`** لا `payment` — و**ENUM يبتلع الخطأ صامتًا**، فبذرٌ خاطئٌ جعل المضاهاةَ تفشل **بلا رسالة**؛ صُحّح في الخدمة والبذر معًا ③ **«بلا نظير» يحجب الإقفال**: البديلُ كشفٌ يُقفل وفيه إيداعٌ لا يعرف أحدٌ مصدرَه ④ **والقاعدةُ المطبَّقةُ تُحفظ**: مضاهاةٌ لا تقول **لماذا** طابقت لا تُراجَع ⑤ **القديمُ يبقى**: `fin_bank_statement_lines` وشاشتُه للقراءة والتاريخ — **والترحيلُ قرارُ مالك** لا أثرُ نشر |
| ③ | M-41 | — | **منجزة** | 2026-07-31 | بطاقةُ جردٍ `docs/specs/M-41_periodic_events.md` **قبل البناء** — والمقيس: **① مخصصُ الصيانة**: «جدولُ الإعداد **القائم**» الذي تفترضه الوثيقةُ **غيرُ موجودٍ إطلاقًا** (ثمانيةُ جداولِ قواعدَ في القاعدة **ليس فيها مخصصُ صيانة**) · **② قسطُ التمويل**: `fin_funding_schedules` **قائمٌ بـ57 قسطًا** لكن **صفرَ مفتاحٍ فريدٍ** على (الالتزام × القسط) — والفريدُ الوحيدُ `PRIMARY` — ولا عمودَ حدثٍ ولا حدثَ منشور · **③ الإقرار**: حركاتٌ (صفّان) **بلا كيانِ إقرارٍ** بمفتاح الفترة · هجرة `2026_09_19`: `fin_maint_provision_rules` (أعمدةُ #23 نصًّا · نطاقٌ بمحورين والأخصُّ يغلب) + `fin_maint_provisions` بـ**UQ(شركة × معدة × فترة)** و**`CHECK` يجعل مبلغًا بلا قاعدةٍ مستحيلًا** («لا كتابةَ يدويةً على الدفتر» بنيويًّا) + `fin_funding_schedules` **+2 و`UNIQUE`(شركة × التزام × قسط)** + `fin_tax_returns` بـ**UQ(شركة × فترة)** و**الصافي عمودٌ مولَّدٌ لا يُكتب** · `PeriodicEventService` **بنمط M-30 نفسِه للثلاث**: قفلُ الفترة أولًا (**423**) ← العطالةُ قبل الحساب ← معاملةٌ واحدةٌ (حدثٌ ثم صفّ) ← **مفتاحُ عطالةٍ حتميّ** (`mprov:معدة:فترة` · `fund:التزام:قسط` · `taxret:شركة:فترة`) · **وقاعدةُ عدم التلفيق نافذةٌ في الثلاث**: بلا قاعدةٍ **لا مخصص** · وقبل الاستحقاق **لا قسط** · وبلا حركاتٍ **إقرارٌ بأصفاره يُعلَن ولا يُخفى** · **والكميةُ من الوحدات المعتمدة وحدَها** · الكرون `cron_periodic_fin.php` بـ`--dry` والشاشة **169** | `periodic_events_test` **25/0 ×2** (بلا قاعدةٍ **صفرُ مخصصٍ وسببٌ مكتوب** · **الأخصُّ يغلب** · **المخصص 420 = (40+20 معتمدةً) × 7 و100 غيرِ المعتمدة لم تدخل** · صفٌّ وحدثٌ واحدٌ بعد تشغيلين · **قسطٌ 5500 والمؤجَّلُ إلى 2099 بلا حدث** · **وقسطٌ مكرَّرٌ يرفضه `UNIQUE`** · **الصافي 18000 = 30000 − 12000** والصافي **مولَّدٌ لا يُكتب** · إقرارٌ ثانٍ **409** والحركاتُ وُسمت `filed` · **و423 في الثلاث** بفترةٍ مقفلة) + **حزمةُ انحدارٍ 47 خضراء** | — (بذرُ 2087 يُكنس بالكامل · و**صفرُ تشغيلٍ على البيانات الحية**: القياسُ بـ`--dry` **57 قسطًا بلا اعتراف** وصفرُ قاعدةِ مخصصٍ وصفرُ إقرار) | ① **الوثيقةُ تُقاس هي الأخرى**: #23 تقول «جدولُ الإعداد **القائم**» — والقياسُ يقول لا وجودَ له؛ فبُني، **وسُجّل أن النصَّ افترض ما ليس قائمًا** ② **المفتاحُ الغائبُ أخطرُ من الميزة الغائبة**: `fin_funding_schedules` بـ57 صفًّا **بلا فريدٍ على (التزام × قسط)** — أي أن قسطًا مكرَّرًا كان ممكنًا في **مالٍ حقيقي**؛ ووضعُه أهمُّ ما في المهمة ③ **الصافي عمودٌ مولَّد**: البديلُ ثلاثةُ أرقامٍ ورابعٌ يُكتب فينحرف عنها — وقد وقع نظيرُه في M-02 ④ **وقفلُ الفترة يتخطّى ولا يكسر** في الأقساط: القسطُ في فترةٍ مقفلةٍ **يُعلَن ويُترك** والباقي يمضي — أما المخصصُ والإقرارُ فيرفضان الفترةَ كلَّها لأن أثرَهما فترةٌ كاملة ⑤ **گوتشا**: `admin_companies` **بلا `is_deleted`** — والشرطُ عليه كان يُرجع **صفرَ شركةٍ صامتًا** فيبدو الكرونُ ناجحًا وهو لم يعمل ⑥ **DEC-M41 معلَّقة**: 57 قسطًا واعترافُها الرجعيُّ **قرارُ سياسةٍ محاسبية لا أثرُ زرّ** |
| ③ | M-30 | — | **منجزة** | 2026-07-31 | بطاقةُ جردٍ `docs/specs/M-30_depreciation_event.md` **قبل البناء** — والمقيسُ: `fin_depreciation` قائمٌ (9 صفوف) و**مفتاحُ منع التكرار `uq_fin_dep` موجودٌ سلفًا**، فالناقصُ **أربعةٌ مقيسة**: ① `journal_entry_id` **NULL في 9 من 9** — **صفرُ قيدِ إهلاكٍ في تاريخ النظام كلِّه** ② المحرّكُ **داخل الشاشة** و**يتجاهل `acquisition_date` تجاهلًا تامًّا** (`FA-001` اقتُني 2025-01 وأولُ إهلاكٍ له **2026-07** — 18 شهرًا ضائعة) ③ الفترةُ **`date('Y-m')` جبرًا** فشهرٌ فات **لا يُدرَك أبدًا** ④ **ولا فحصَ لقفل الفترة ولا كرون** · هجرة `2026_09_18` (idempotent): `fin_depreciation` **+4** (`event_id` · `method` · `basis_json` · `source`) **وتصريحٌ رجعيٌّ** للتسعة القائمة `legacy` — **ولا يُختلق لها حدثٌ** لأن ذلك يخترع تاريخًا لم يقع · `DepreciationService` بخمس قواعد: **① لا إهلاكَ قبل الاقتناء** · **② والفترةُ مُدخَلٌ لا حاضر** (+`catchUp` يستدرك من شهر الاقتناء) · **③ وقفلُ الفترة يحكم** (`ems_period_check` ⇒ **423 بلا كتابة**) · **④ والمفتاحُ يمنع التكرار مرتين** (`UNIQUE` **ومفتاحُ عطالةٍ حتميٌّ `dep:أصل:فترة`** في الناشر) · **⑤ ولا تُهلَك خردةٌ ولا زيادة** · **وخللٌ ثالثٌ كشفه الاختبار وأُصلح**: القسطُ المدوَّر يترك **كسرًا يتيمًا لا يُهلَك أبدًا** (10000÷3 = 3333.33×3 = **9999.99**) — فصار **القسطُ الأخير هو المتبقّي كلَّه** بترتيبٍ يُحسب من شهر الاقتناء · الكرون `Finance/cron_depreciation_fin.php` بـ**`--dry` يقيس ولا يكتب** · والشاشة **101** تستدعي الخدمةَ **بفترةٍ مختارة** + زرُّ الاستدراك + **عمودُ «شهورٌ غير محتسَبة»** فالفجوةُ تُرى | `depreciation_event_test` **22/0 ×2** (فترةٌ قبل الاقتناء **422** · القسطُ **12000** يدويًّا · **صفّان وحدثان لا أربعة** بعد تشغيلين · `expense.depreciation.recorded` بمفتاح `dep:N:2088-03` · فترةٌ مقفلةٌ **423 وصفرُ صفٍّ كُتب** · الاستدراكُ يسدّ الفجوة و**ما بعد العمر الإنتاجي ليس فجوة** · **5 أقساطٍ = 60000 بالضبط** و`fully_depreciated` · قسطٌ بعد الاستيفاء **409** · **وآخرُ قسطٍ 3333.34 والمجموع 10000 بالضبط**) + **حزمةُ انحدارٍ 46 خضراء** | `docs/reports/m30_test_isolation_incident_20260731.md` — **تقريرُ مطابقةٍ لحادثةٍ وقعت واستُرجعت**: أولُ تشغيلٍ للحزمة استدعى الخدمةَ **بلا نطاق** فاستدرك الأصولَ التسعةَ الحية (961 صفًّا · 67.3 مليون · 952 حدثًا)؛ **واستُرجع الكلُّ اشتقاقًا من الصفوف الباقية** (`source` هو الحدُّ الفاصل) — و**المطابقةُ صفرُ فرقٍ حرفًا بحرف** (135000 · 90000 · 84375 · 37500 …) | ① **الاسترجاعُ أمكن لأن البنيةَ تحمل مصدرَها**: عمودُ `source` الذي أضافته الهجرةُ نفسُها هو ما فرّق المكتوبَ الآن عن القائم — **ولولاه لكان الاسترجاعُ تخمينًا** ② **والقاعدةُ تُوسَّع**: «حزمةٌ تستدعي خدمةً تعمل على كل ما في الشركة تُسيَّج بنطاقٍ مسمًّى — **والخدمةُ التي لا تقبل نطاقًا يجب أن تقبله**»؛ فصارت `runPeriod`/`catchUp` تقبلان `$only` وهو **بابٌ منتَجيٌّ حقيقي** لا حيلةَ اختبار ③ **آخرُ قسطٍ يستوعب التقريب**: البديلُ قرشٌ يتيمٌ في الدفاتر إلى الأبد — والترتيبُ يُحسب **من شهر الاقتناء لا من عدد الصفوف** فشهرٌ استُدرك لا يزيح الأخير ④ **الاستدراكُ الرجعيُّ لم يقع**: ~8.9 مليون قرارُ مالكٍ لا أثرُ كرون (DEC-M30) — و`--dry` وُجد لهذا ⑤ **گوتشا**: `config.php` يحمّل طبقةَ البوابة **كسولًا** داخل `ems_tenant_db()` — فمن يبني بوابتَه بنفسه يلزمه استدعاءُ الأربعة صراحةً، وإلا فشل بـ«Class TenantRegistry not found» **بلا رسالةٍ** في CLI ⑥ **گوتشا**: حذفُ `ems_business_events` قبل `fin_financial_events` يرفضه `fk_ffe_root` — **الإسقاطُ يُحذف قبل جذره** |
| ③ | M-52 | — | **منجزة** | 2026-07-31 | بطاقةُ جردٍ `docs/specs/M-52_transfer_tariff.md` **قبل البناء** — و**أولُ ما فيها تصحيحُ ادّعاء الكتالوج**: «لا كيانَ لأمر الترحيل — وحدةُ النقل جدولان فقط» **غيرُ صحيح**؛ الكتالوجُ بحث ببادئة `trs_` والوحدةُ مبنيةٌ ببادئة **`transfer_`**: `transfer_orders` (**11 صفًّا** · دورةُ حياةٍ من سبع مراحل) + سبعةُ جداولَ و**ستُّ شاشاتٍ حية** (108–113 · الدور 23) — **فالناقصُ شيئان بالضبط: التعرفةُ والوصلُ بتسوية المورد** (`transfer_cost_lines` يسجّل **تكاليفَ وقعت** و`transfer_cost_rules` يقرّر **المتحمِّلَ** — ولا جدولَ أسعارٍ في النظام كلِّه · و`collectLines` بلا سطر نقلٍ و`fin_dues.transport` **صفر**) · هجرة `2026_09_17`: `transfer_tariffs` (**أربعةُ نماذجَ تسعير** `per_trip`/`per_km`/`per_ton`/`per_equipment` · معدلٌ بدقّته · حدّان · **ونطاقٌ بثلاثة محاور** مورد·نوع·مسار كلُّها NULL = الأعمّ · وسريانٌ وحالةٌ · **وUQ** لمفتاحها في تاريخها · **وثلاثةُ `CHECK`**) + `transfer_orders` **+8**: **`charge_supplier_id`** (**نظير `proc_issue`/`mnt_order` حرفيًّا** — و`cost_bearer` القائمُ **يبقى ولا يُمسّ**: ذاك متحمِّلُ **تكلفتنا** وهذا **من نحمّله تعرفتَنا**) · `tariff_id/amount/currency/note` · `distance_km` · `priced_at/by` · و**`CHECK` يجعل مبلغًا مسعَّرًا بلا مرجع تعرفةٍ مستحيلًا** («المبلغُ يُقرأ من مصدره لا يُكتب» بنيويًّا) · `TransferTariffService` بأربع قواعد: **① لا تحميلَ بلا تعرفةٍ مكتوبة** (422 — و**`actual_cost_usd` لا تُستعمل بديلًا**: تلك تكلفتُنا لا تعرفتُه) · **② والمسلَّمُ وحدَه يُسعَّر** (423) · **③ والأخصُّ يغلب** (مورد 4 > مسار 2 > نوع 1 > الأعمّ) · **④ ولا يُسعَّر مرتين** (409 بمبلغه ومرجعه · وإعادةُ التسعير **بحجّةٍ مكتوبةٍ تُسجَّل في البيان**) · **الوصلُ الحي**: `SettlementService::collectLines` يضيف **المصدرَ السادس** ويمرّ بـ`SupplierRuleService::priceCharge('transport')` كما تمرّ القطعُ والصيانة · الشاشةُ **168** بلوح «المسلَّمُ بلا تسعير» بعدّاده | `transfer_tariff_test` **21/0 ×2** («planned» 423 · بلا تعرفةٍ **422 و`actual_cost_usd`=999.99 لم تُستعمل** · **بالنقلة 700** · **بالطن 12×25=300** · **بالمعدة 450** · **بالكيلومتر بلا مسافةٍ 422** وبـ150 كم **1200** · **أربعُ تعرفاتٍ وتعرفةُ المورد وحدَها تفوز** · **1200 مقصوصةٌ إلى 500** ومرفوعةٌ إلى **2000** معلَنًا · تسعيرٌ ثانٍ **409** · **وسطرُ النقل 1200 في التسوية المولَّدة برابط أمره** · و`CHECK` يرفض محوَ مرجع التعرفة) + **`transfer_tariff_http_proof` 12/0** (اللوحُ يعلن الفجوة · والتسعيرُ بلا تعرفةٍ يُرفض من الخادم · وتعرفةٌ تُكتب فيقع التسعيرُ بـ1350 ببيانه) + **حزمةُ انحدارٍ 45 خضراء** | — (بذرُ 2091 يُكنس · و**صفرُ أثرٍ على المال الحي**: صفرُ أمرٍ يحمل `charge_supplier_id` اليوم، فـ`chargeLines` تعيد صفرَ سطر) | ① **الكتالوجُ يُقاس لا يُصدَّق**: البحثُ ببادئةٍ خاطئةٍ كاد يجعلني أبني كيانًا **قائمًا بعشرة جداولَ وست شاشات** — والقاعدةُ «قِس الواقعَ بنفسك» (§8-③) هي التي منعت ازدواجًا كاملًا ② **`charge_supplier_id` لا توسيعُ `cost_bearer`**: ENUM المتحمِّل (`client`·`company`·`new_client`) يجيب سؤالًا آخر — **من يتحمّل تكلفتنا الداخلية**؛ وحشرُ `supplier` فيه كان يخلط سؤالين ويكسر كلَّ قارئٍ للعمود ③ **التعرفةُ ليست التكلفة**: `transfer_cost_lines` (وقودٌ وعمالةٌ ومقاول) **مصروفُنا**، وتحميلُ المورد به يجعل ربحَ النقل صفرًا حتمًا ويكشف هيكلَ تكلفتنا في كشفه — فالتعرفةُ سعرٌ مكتوبٌ مستقل ④ **بلا مسافةٍ لا تسعيرَ بالكيلومتر**: البديلُ تقديرُ مسافةٍ من اسم المسار — رقمٌ بلا مصدر ⑤ **إعادةُ التسعير بابٌ مفتوحٌ بحجّة**: منعُها مطلقًا يجمّد خطأً بلا مخرج، وفتحُها صامتةً يجعل المبلغَ رأيًا — فالحجّةُ تُكتب **وتبقى في البيان** ⑥ **گوتشا**: `SettlementService::generate` توقيعُها `($gate, $conn, …)` — **البوابةُ أولًا** خلافًا لبقية الخدمات |
| ③ | E-24 | — | **منجزة** | 2026-07-31 | بطاقةُ جردٍ `docs/specs/E-24_pay_policy_state.md` **قبل البناء** — والمقيسُ فيها: `contract_hour_policies` (49 صفًّا · `operator` 25) **بلا عمود حالةٍ إطلاقًا**، فصفٌّ يُكتب من الشاشة **يصير نافذًا في اللحظة نفسِها**، و«الاستبدال» **لا أثرَ له**: سياستان متداخلتان تُقرآن معًا والقديمةُ لا تُغلق ولا تشير إلى خَلَفها · هجرة `2026_09_16` (**idempotent** بنمط `SET @ddl`+`PREPARE` القائم في الشجرة): `policy_state` ENUM الأربع **بافتراض `draft`** — **fail-closed**: كاتبٌ ينسى الحالةَ يُنتج صفًّا **عاطلًا لا صفًّا يسعّر** · `superseded_by` · `state_changed_at/by` · `state_note` · و**`CHECK`ان**: **الاستبدالُ يلزمه خَلَفٌ مسمًّى** و**الإنهاءُ يلزمه سببٌ مكتوب** · وتعبئةٌ رجعيةٌ **تصريحُ حالةٍ لا تغييرُ سلوك** (القائمُ `active` ← فالمنقضي والموقوفُ `expired` بسببهما) · `PayPolicyStateMachine` **قائمةُ سماحٍ لا منع** (نمط H-02): **`activate`** بسريانٍ بتاريخٍ إلزاميٍّ ومعدلٍ وعملةٍ وmin≤max — **وبه يقع الإخلاف**: كلُّ نافذةٍ بالمفتاح نفسِه يتداخل سريانُها **تُغلق عند «سريان الجديدة − يوم»** وتصير `superseded` بخَلَفها · **`expire`** بسببٍ مكتوب (والمفتوحُ الطرفِ يُغلق باليوم **والماضي لا يُمسّ**) · **`guardEdit`** التحريرُ للمسودة وحدَها **423** · **`sweepExpired`** تصريحٌ بما وقع · **الوصلُ في موضعين**: `OperatorDue::applicablePolicies` و`EffectFanout::hourPolicy` كلاهما يستثني `draft` **ولا يستثني `superseded` ولا `expired`** · الشاشةُ `Finance/operator_pay_policies_fin.php`: الإضافةُ **تحفظ مسودةً** · «فعّل» · «أنهِ» بسببه · والمستبدَلةُ **بخَلَفها مسمًّى** | `pay_policy_state_test` **24/0 ×2** (مسودةٌ ⇒ **«لا حكم»** · بلا سريانٍ/معدلٍ/عملةٍ **422** · وبعد التفعيل **80** = 8×10 · **الإخلاف**: القديمةُ `superseded` بخَلَفها و`effective_to`=**2092-06-30** · **ويومٌ قبل السريان ما زال 80 وبعدَه 160** · تعديلُ نافذةٍ **423** · التطابقُ يرفضه **UQ** بنيويًّا · إنهاءٌ بلا سببٍ **422 وCHECK** · تفعيلُ منتهيةٍ **423** · استبدالٌ بلا خَلَفٍ **يرفضه CHECK** · والكنسُ يصرّح) + **`pay_policy_state_http_proof` 14/0** (من الشاشة: «لا تسعّر شيئًا حتى تُفعَّل» · التفعيلُ والإخلافُ و«مستبدَلة ← #N» · والإنهاءُ 422 ثم يقع) + **حزمةُ انحدارٍ 44 خضراء** | — (بذرُ 2091/2092 يُكنس · و**التعبئةُ الرجعيةُ صفرُ تغييرٍ سلوكي**: القرّاءُ يستثنون `draft` وحدَها والقائمُ كلُّه صار `active`) | ① **الافتراضُ `draft` لا `active`**: البديلُ يجعل كلَّ كاتبٍ ينسى العمودَ يُنتج سياسةً **تسعّر فورًا** — والنسيانُ يجب أن يُسقط لا أن يُسرّب ② **`superseded` و`expired` تُقرآن عمدًا**: «أثرُ الماضي بحكم سياسته النافذة يومَها» (§8.2) — فالقراءةُ بالتاريخ، وإسقاطُ المستبدَلة **يفقد حكمَ الشهر الماضي** وهو عينُ ما تمنعه «لا رجعية» ③ **گوتشا مقيسة**: MySQL يرفض `CHECK` يشير إلى عمود `auto_increment` («cannot refer to an auto-increment column») — فقيدُ «لا تخلف السياسةُ نفسَها» **صار حارسًا في الخدمة**، **معلَنًا في الهجرة** ولا يُدَّعى أنه بنيوي ④ **الفشلُ في ثلاث حزمٍ كان الحارسَ يعمل لا انحدارًا**: بذرُها يُدرج سياساتٍ **حيّةً** بلا تصريحِ حالةٍ فصارت مسودةً — **فصُحّح البذرُ لا الحارس** (`operator_due_policy` · `timesheet_fanout` · `unit_chain_e2e_proof`) ⑤ **حارسُ 409 حزامٌ فوق قيد**: التطابقُ في السريان ممنوعٌ سلفًا بـ`uq_operator_policy` — فالاختبارُ يثبت **القيدَ البنيوي** ولا يدّعي إصابةَ فرعٍ لا يُبلَغ ⑥ **گوتشا في الكنس**: `ck_chp_superseded` يرفض تفريغَ `superseded_by` وحدَه — فالتعقيبُ يعيد الحالةَ مسودةً في الكتابة نفسِها ⑦ **CSRF منفَّذٌ على مسار `Finance/`**: برهانُ HTTP يقرأ رمزَ الصفحة أولًا، وبدونه يُرفض كلُّ POST **بلا رسالة** |
| ③ | M-22 | — | **منجزة** | 2026-07-31 | بطاقةُ جردٍ `docs/specs/M-22_final_settlement.md` **قبل البناء** (§7-⑥) — والمقيسُ فيها أملى كلَّ ما بعدها: `worker_settlement` **إدخالٌ يدويٌّ خالص** (`net_amount` عمودٌ يُكتب · صفرُ صف) و`end_of_service` في `fin_dues` **تصنيفُ دفعةٍ لا احتساب**، و**المصادرُ الأربعةُ حيّةٌ قائمة** — **فالناقصُ قاعدتا الأيام والمحرّك لا مصادرُ القياس** · هجرة `2026_09_15`: `employee_contracts` **+2** (`eos_days_per_year` · `leave_days_per_year` — **NULL = لم تُكتب فلا تُحتسب**) + `employee_final_settlements` (**`UNIQUE(contract_id)` هو «مفتاحُ (العقد × التصفية)» بنيويًّا** · `snapshot_id` وبصمتُها · `net_due_ref` مرجعَ الحدث الواحد · `advances_remaining` · و**أربعةُ `CHECK`**: **فصلُ اليدين** · **الاعتمادُ يلزمه معتمِدٌ ومرفقُ إخلاء** · الإلغاءُ بسببٍ · **والصافي لا يكون سالبًا**) + `employee_final_settlement_lines` بـUQ(تصفية × نوع) و**`computable`** + `fin_dues.source_doc_type` **+`employee_closure`** («الذمّةُ تُسمّى باسمها» — نظيرُ `supplier_closure`) · `FinalSettlementService` بأربع قواعد: **① محسوبٌ لا مُدخَل** (واليدويُّ القديمُ يبقى حيث هو ولا يُبنى عليه) · **② بلا قاعدةٍ مكتوبةٍ لا احتساب** (`computable=0` بسببه لا صفرٌ صامت) · **③ الأساسُ من علَمِه في اللقطة** (`in_eos`/`in_leave_pay` من `snapshot_json` لا من الجداول الحية · و**طريقةُ حسابٍ لا يحملها أساسٌ شهريٌّ ثابت تُعلَن باسمها**) · **④ والمقاصّةُ ظاهرةٌ ومحدودة** (**لا يُقاصّ أكثرُ من المستحق** والباقي رصيدٌ مفتوحٌ يُعلَن) · **والاعتمادُ يقع بحدثٍ ماليٍّ واحد**: ذمّةُ `end_of_service` **بما تعترف به التصفيةُ جديدًا** + استردادُ السلف فعليًّا + `publishFact` بمفتاح عطالةٍ حتميٍّ `emp_fs:عقد:تصفية` · **`ContractSnapshotService` توسيعٌ لا هدم**: `snapshotForSettlement` تقبل الحالاتِ الطرفيةَ الثلاثَ وحدَها، **والأولويةُ للقطةٍ قائمةٍ ≤ تاريخِ الأثر** · الشاشةُ **167** بمعاينةٍ محسوبةٍ **قبل** الفتح | `final_settlement_test` **39/0 ×2** (عقدٌ نافذٌ 422 · **بلا قاعدةٍ: صفرٌ و`computable=0` وسببٌ مكتوب** · **نهايةُ الخدمة 2997 = 29.97 × 100** يدويًّا بلا فرق · **ورصيدُ الإجازة 10.98 يومًا = 1098** والتبادليةُ لم تُستهلك · **سلفةُ 9000 على مستحقِّ 2997 تُقاصّ 2997 و6003 تبقى معلَنةً** · تصفيةٌ ثانيةٌ 409 · اعتمادُ من أعدّ 403 · بلا مرفقٍ 422 **و`CHECK` يرفض الكتابةَ المباشرة** · **ذمّةٌ واحدةٌ 4095 باسمها** · **حقيقةٌ واحدةٌ بمفتاحها الحتمي** · اعتمادٌ ثانٍ 409 · إلغاءُ معتمَدةٍ 423) + **`final_settlement_http_proof` 15/0** (الدورُ 4 يعاين ولا يرى زرَّ الاعتماد · و**الطلبُ المزوَّرُ من يد الإعداد لم يغيّر الحالة** · والدورُ 17 يعتمد) + **حزمةُ انحدارٍ 39 خضراء** (`tests/_regression.php` عدّةً جديدة) | — (بذرُ 2093 يُكنس · و**صفرُ صفٍّ في `worker_settlement`** فلا ترحيلَ ولا مساس · وأثرُ الإصلاح الجانبيِّ على المنشور **صفر**) | ① **الاعترافُ لا يُضاعَف**: الذمّةُ تحمل **`recognized = إجازة + نهاية خدمة`** لا الصافي — والمستحقُّ السابق (`fin_dues` المفتوحة) **اعتُرف به في مصدره**، وإعادةُ الاعتراف به تضاعف الذمّة؛ فالكشفُ يعرضه في طبقته والحدثُ لا يعيده ② **المقاصّةُ محدودةٌ بالمستحق**: البديلُ صافٍ سالبٌ يعني ذمّةً مدينةً على موظفٍ انتهت خدمتُه بلا سندٍ يفتحها — فالقصُّ وقع و**الباقي يُعلَن في `advances_remaining`** ولا يُسقَط ③ **بوابةُ اللقطة وُسّعت ولم تُنقض**: `isReadable` تشترط عقدًا نافذًا، والتصفيةُ **بعد** الإنهاء — فقناةٌ ثانيةٌ مسمّاةٌ للتصفية بالمضمون والبصمة نفسِهما؛ ونقضُ البوابة كان سيفتحها لكل احتساب ④ **قاعدتا الأيام على الرأس لا في اللقطة**: اللقطةُ تحمل **الأسس** (المكوّناتِ ومعدلاتِها) والرأسُ يحمل **القاعدة** — و§5 يقول «نهايةُ الخدمة **بقاعدتها** من اللقطة» فالأساسُ منها والقاعدةُ من العقد ⑤ **«اعتيادية» وحدَها تستهلك رصيدَ الإجازة**: «تبادلية» دورةُ تناوبٍ و«مأمورية» عملٌ، والطارئُ بابُه خصمُ الغياب في المسيّر — وعدُّ الكلِّ إجازةً يُسقط رصيدًا مستحقًّا ⑥ **إصلاحٌ جانبيٌّ مكشوف**: ثلاثةُ `event_key` نطاقُها الأولُ بشرطةٍ سفلية (`employee_contract.*` · `daily_plan.*`) **يرفضها عقدُ §9 صامتًا** فلا تصل الحقيقةُ الجذرَ أبدًا — صُحّحت، و**صفرُ صفٍّ منشورٍ يفقده التغيير** لأنها لم تنشر قط ⑦ `M-22` **لم تستبق القيدَ المحاسبي**: وصلُه بجهات التحمّل معلَّقٌ على مطابقة H-09 (رأسُ السجل) |
| ③ | M-06 | — | **منجزة** | 2026-07-31 | هجرة `2026_09_14`: `claim_lines` **+7 أعمدة** (مستندُ الاعتراض · رافعُه ووقتُه · **`dispute_state`** none/open/resolved · **`resolution`** upheld/rejected بسببه وحاسمه ووقته) و**ثلاثةُ `CHECK`**: **① نزاعٌ بلا سببٍ ومستندٍ مستحيل** · **② والحسمُ يلزمه قرارٌ وحاسمٌ وسبب** · **③ والعَلَمُ مرآةُ الحالة لا رأيٌ ثانٍ** (`dispute_flag=1` إن وإن فقط كان مفتوحًا أو حُسم بإقرار الاعتراض) · **والموروثُ يُرفع بوسمٍ معلَن `legacy_no_ref`** لا يُمحى ولا يُختلق له مستند · `ClaimDisputeService` بأربع قواعد: بسببٍ ومستند · **والبقيةُ تمضي** (البندُ يخرج وحدَه والمستخلصُ لا يتجمّد وعدّادُ النزاع يُقرأ) · **والحسمُ قرارٌ يُسجَّل** (`rejected` يعيد البندَ محتسَبًا و`upheld` يُسقطه) · **ولا نزاعَ على مفوتَر** (423 بنصّ «التصحيح بإشعار» §3-⑥) · **واليدان مفصولتان**: الرفعُ لمن يُعدّ والحسمُ ليدِ الإجازة · **الوصلُ الحي**: عمودُ النزاع في جدول بنود `Contracts/claims.php` بنموذجَي الرفع والحسم — وكان **عرضًا باهتًا بلا فعل** | `claim_dispute_test` **19/0 ×2** (بلا مستندٍ/سببٍ 422 وCHECK · **الصافي 400 من 1000** والمستخلصُ لم يتجمّد · حسمٌ بلا سببٍ 422 · **`rejected` يعيد 1000** و**`upheld` يُسقط إلى 600** · رفعُ العَلَم على محسومٍ **يرفضه CHECK** · رفعٌ وحسمٌ ثانيان 409 · وبعد الفوترة **423**) + **15 حزمةَ انحدارٍ خضراء** + برهانُ HTTP لعمود النزاع في شاشة المستخلصات | — (بذرُ 2100 يُكنس · وصفرُ بندٍ متنازَعٍ عليه في الحي فالوسمُ الموروثُ لم يُطبَّق على شيء) | ① **البناءُ أكمل ما بُني**: `dispute_flag` و`claim_recalc` كانا يعملان — فلم يُعَد بناءُ المحرّك، وأُضيف **الفعلُ والبيّنةُ والقرار** ② **العَلَمُ صار مرآةً بقيدٍ لا باتفاق**: قبلها كان يمكن رفعُ العَلَم بلا حالةٍ ولا قرار، فيخرج بندٌ من الصافي **بلا أثرٍ يُقرأ** ③ **`upheld` يُبقي العَلَمَ مرفوعًا** عمدًا: البندُ **ساقطٌ بقرار** لا معلَّقٌ بنزاع — والفرقُ في `dispute_state` لا في العَلَم ④ **الحارسُ الرابعُ اتّكأ على M-03**: «لا نزاعَ على مفوتَر» يقرأ `TaxInvoiceService::assertEditable` — سياجٌ واحدٌ يخدم البابين ⑤ **گوتشا في البذر**: `uq_claim_line_src` = (مستخلص × نوعُ الأصل × مرجعه) — فلكلِّ بندٍ أصلُه، والقيدُ يُحترم |
| ③ | M-05 | — | **منجزة** | 2026-07-31 | هجرة `2026_09_13`: `fin_payments` **+2** (`bank_ref` · `received_on`) و**`CHECK` يجعل «قبضًا بلا مرجع» مستحيلًا** و**`UNIQUE (شركة × مرجع × مبلغ × يوم)`** يمنع القبضَ مرتين — **والموروثُ يُوسم `legacy_no_ref` ويُعلَن ولا يُمحى** (نمطُ M-11 حرفيًّا) فيمرّ القيدُ على تاريخٍ لم يعرفه + `fin_collection_allocations` (سطرٌ لكل قبض × ذمّة **بأساسه** `explicit`/`oldest_first`) — «التخصيصُ ظاهرٌ في الكشف لا صامتًا» + توسيعُ `claims.state` إلى `VARCHAR(24)` (**«partially_collected» تسعةَ عشرَ حرفًا**) وإضافتُها إلى `claim_states()` · `CollectionService` بأربع قواعد: **① لا قبضَ بلا مرجع** · **② ولا يُقبض مرتين** (409 قبل الكتابة وUQ بعدها) · **③ أقدمُ فاتورةٍ أولًا ما لم يُحدَّد مرجعٌ صريح** والدفعةُ **تُقسَّم** · **④ والفائضُ يُعلَن ولا يُبتلع** · **والارتداد**: بعد كل تخصيصٍ يُعاد وسمُ المستخلص `collected`/`partially_collected` **وتُعلَن قائمةُ ما ارتدّ** · الشاشةُ **166** بالذمم **بأعمارها ملوّنةً** (30/60/90) | `collection_control_test` **24/0 ×2** (بلا مرجعٍ 422 وCHECK · `legacy_no_ref` مرفوضٌ لجديد · **1500 ⇒ 1000 للأقدم و500 للتالية** بسطرين بأساسهما · الأقدمُ `collected` والأحدثُ `partial` · **المستخلصُ ارتدَّ إلى collected** · التكرارُ 409 وUQ · **المرجعُ الصريحُ يتقدّم** · و**5000 ⇒ 2000 مخصَّصةً و3000 معلَنة**) + **16 حزمةَ انحدارٍ خضراء** + برهانُ HTTP للشاشة 166 | — (بذرُ 2099 يُكنس · و**صفُّ التحصيل القائمُ الوحيدُ وُسم `legacy_no_ref`** ولم يُحذف ولم يُختلق له مرجع) | ① **الموروثُ يُوسم لا يُختلق له مرجع**: بديلُه إمّا حذفُ الصفّ (محوُ تاريخ) أو اختراعُ رقمٍ بنكيٍّ (كذبٌ في مستند) — والوسمُ المعلَنُ ثالثُهما، **والخدمةُ ترفضه لجديد** فلا يصير بابًا ② **گوتشا مقيسة**: `claims.state` كان `varchar(16)` — و«partially_collected» **تُقصّ صامتًا** لولا التوسيع ③ **التخصيصُ جدولٌ لا عمود**: العمودُ الواحدُ يستحيل أن يُري دفعةً على فاتورتين — والوثيقةُ تطلب أن **يُرى** ④ **القيدُ الجديدُ كسر بذرَ `client_statement_test`** (تحصيلاتٌ بلا مرجع) — **فصُحّح البذرُ لا القيد** ⑤ العمودُ القديمُ `fin_payments.receivable_id` بقي **مرآةَ أولِ تخصيصٍ** للقائم الذي يقرؤه (`payments_fin.php`) — والمصدرُ جدولُ التخصيص |
| ③ | M-04 | — | **منجزة** | 2026-07-31 | **لا هجرةَ لبنية — قراءةٌ خالصة** (نظيرُ M-14): `ClientStatementService` بطبقات §6 الخمس (مستخلصاتٌ · فواتيرُ · تحصيلاتٌ · محتجزٌ · مقدمة) وكلٌّ من مصدره الحي — و**القاعدةُ الحاكمة**: كلُّ صفٍّ يحمل رابطَ مصدره ومن لا مصدرَ له **يُعلَن `orphan` ولا يُخفى** · وثلاثُ قواعدَ في البنية: **① المحتجزُ والمقدمةُ كلٌّ في طبقته** («لا يُنسى ولا يُخلط بالذمة الجارية» §4 نصًّا) · **② الرصيدُ من الفواتير لا من المستخلصات** — الفاتورةُ **مطالبة** والمستخلصُ **اعترافٌ سابقٌ عليها**، وجمعُهما **يحتسب الدَّينَ مرتين** · **③ لا تُجمع عملتان في رقم** والتعدُّدُ يُعلَن · **والملغاةُ ضريبيًّا تبقى سطرًا بصفرها** تُعلَن ولا تُحتسب ولا تُحذف · **والتحصيلُ يسمّي ذمّتَه** أو يُعلن «بلا ذمّةٍ مخصَّصة» (§4) · هجرة `2026_09_12` **تسجيلُ شاشةٍ فقط** — الشاشةُ **165** قراءةٌ خالصةٌ بلا زرِّ كتابةٍ واحد | `client_statement_test` **25/0 ×2** (الطبقاتُ الخمس · **المحتجزُ 50 في طبقته** · روابطُ المصادر لكل صف · **الرصيدُ 2600 لا 5600** · الملغاةُ باقيةٌ **بصفرها ومعلَنة** · الفترةُ تحكم 3000/3700 · التحصيلُ يسمّي ذمّتَه · والعزلُ محفوظ) + **13 حزمةَ انحدارٍ خضراء** + برهانُ HTTP للشاشة 165 | — (بذرُ 2098 يُكنس · وصفرُ كتابةٍ في المنتج: الخدمةُ قراءةٌ خالصة) | ① **اجتهادٌ مدوَّن**: `contracts` **بلا عمود عميل** (الطرفُ الثاني نصٌّ حر) — فطبقةُ المقدمة تُوصل بعقود **مستخلصات العميل** (`claims.contract_id`)، **وبلا عقدٍ مربوطٍ تُقرأ فارغةً ويُعلَن السبب** ولا يُخترع ربط ② **الرصيدُ من الفواتير** قرارٌ محاسبيٌّ لا تفصيلٌ عرضي: لو جُمع المستخلصُ والفاتورةُ لظهر الدَّينُ ضعفَه — والاختبارُ يثبت **2600 لا 5600** ③ **الملغاةُ لا تُحذف**: حذفُها من الكشف يجعل مجموعَه صحيحًا وتاريخَه كاذبًا ④ **طبقةُ الفواتير لم تكن ممكنةً قبل M-03**: لولا `tax_invoices` لكان «الفواتير» اسمًا مشتقًّا من المستخلص — فالترتيبُ في البطاقة كان مانعًا حقيقيًّا |
| ③ | M-03 | — | **منجزة** | 2026-07-31 | هجرة `2026_09_11`: `tax_invoices` بأعمدة §7 نصًّا + **تسلسلٌ بعمودين** (`serial_year`/`serial_seq`) وفريدَين — `(شركة × رقم)` و`(شركة × سنة × تسلسل)`، **فلا رقمان لسنةٍ واحدة والثغرةُ تُرى** · و`claim_id` **FK بـRESTRICT** كما تنصّ الوثيقة · **وثلاثةُ `CHECK`**: `الإجمالي = الصافي + الضريبة` (**المجموعُ لا يُخترع**) · **ضريبةٌ بمبلغٍ بلا رمزٍ ونسبةٍ مستحيلة** («سطرٌ مستقلٌّ بمرجعها» §5) · والإلغاءُ الضريبيُّ **يلزمه سببٌ مكتوب** · `TaxInvoiceService` بأربع قواعد: **① لا فاتورةَ بلا مستخلصٍ معتمد** (422) والمبالغُ **تُقرأ لا تُكتب** · **② تسلسلٌ نظاميٌّ** والفريدُ هو الحكمُ عند التزاحم · **③ لا تعديلَ بعد الإصدار** (`assertEditable` **423**، و**`claim_recalc` تُعيد المحفوظ ولا تعيد الحساب**) · **④ الضريبةُ بمرجعها** من `fin_tax_codes` (**ورمزٌ غيرُ مسجَّلٍ 422**) · **والحقولُ النظامية لقطةٌ لا اشتقاق** في `tax_fields_json` والناقصُ **يُسمّى** · **الوصلُ الحي**: `claim_approve` كان يولّد `INV-{claim_no}` بنفسه وصار **يستدعي `issueForClaim`** ويكتب التسلسليَّ في المستخلص والذمّة والحقيقة المنشورة · الشاشةُ **164** بالطباعة الرسمية وزرِّ الإشعار | `tax_invoice_test` **22/0 ×2** («review» 422 · **INV-2026-000001** وتكرارُ التسلسل يرفضه UQ · رمزٌ غيرُ مسجَّلٍ 422 · مبلغٌ بلا مرجعٍ وإجماليٌّ مخترَعٌ **يرفضهما CHECK** · الحقولُ ملتقَطة · **`claim_recalc` تُعيد 2000 بعد إضافة بندِ 777** · إلغاءٌ بلا سببٍ 422 وCHECK · **وإعادةُ الإصدار برقمٍ جديد**) + **`claims_test` 51/0** و**16 حزمةَ انحدارٍ خضراء** + برهانُ HTTP للشاشة 164 | — (بذرُ 2097 يُكنس · والمستخلصُ القائمُ الوحيدُ «draft» فلا فاتورةَ له · وأرقامُ الفواتير السابقة **لم تُمسّ**) | ① **گوتشا حقيقيةٌ من التصميم**: `claim_id` بـRESTRICT جعل حذفَ مستخلصٍ مفوتَرٍ مستحيلًا — **وهو المقصود**؛ لكن عُدّةَ `claims_test` كانت تحذف المستخلصَ مباشرةً فبقي حيًّا **صامتًا** وانكسر خطُّ الأساس. فصارت **تكنس المستندَ الضريبيَّ قبل مستخلصه** (كنسٌ لا تأكيد) ② **العلَمُ `approving`**: «Approved → Invoiced» فعلٌ واحدٌ يقع في لحظة الإجازة (§4)، فالمستدعي الذي مرّ بحرّاسه يُصدر من «review» — و**لا يفتح ذلك بابًا لغيره**: أيُّ مستدعٍ آخر يلزمه معتمدٌ فعلًا ③ **تشديدٌ مقصود**: كان رمزٌ ضريبيٌّ غيرُ مسجَّلٍ **يُتجاهل صامتًا** فصار **422** — «الضريبةُ سطرٌ بمرجعها» لا رقمٌ يمرّ بلا سند ④ **الاسمُ في موضعين معلَنٌ ومسدود**: `claims.invoice_no` بقي **مرآةً** يكتبها مُصدِرُ الفاتورة وحدَه — والمصدرُ `tax_invoices` ⑤ `clients` عمودُه `client_name` و`client_code` NOT NULL، و`uq_claim_period` = (شركة × عقد × فترة) — **قيودُ القائم تُحترم في البذر** |
| ③ | M-19 | — | **منجزة** | 2026-07-31 | هجرة `2026_09_10`: **محورٌ ثالثٌ** `subject_type='supplier'` في `equipment_documents` + نوعا المورد النظاميان (`سجل تجاري`/`شهادة ضريبية`/`شهادة بنكية`) — **لا جدولَ ثانيًا لوثائقَ هي وثائق** · `suppliers` **+7 أعمدة** (ضريبي · بنكٌ وحسابٌ وIBAN · **مستندُ التوثيق** · موثِّقُه ووقتُه) و**`CHECK` يجعل «موثَّقًا بلا حسابٍ ومستند» مستحيلًا** · `SupplierDocumentService` بأربع قواعد: **① لا جدولَ ثانيًا** · **② التنبيهُ من التاريخ لا من الحالة** (و**وثيقةٌ نظاميةٌ بلا تاريخِ صلاحيةٍ 422**: «تنبيهٌ آليٌّ» بلا تاريخٍ **وعدٌ لا يُنفَّذ**) · **③ توثيقٌ بلا مستندٍ دعوى** · **④ البوابةُ بعلَمها** `EMS_SUPPLIER_DOC_GATE` (off/**monitor** الافتراض/enforce) · **و`supplier_audit_log` قراءةٌ على `activity_logs`** بمرشِّح نطاق المورد — **لا سجلَّ تدقيقٍ ثانيًا يتباعد** · **الوصلُ الحي**: `SettlementService::approve` لتسويات الموردين — monitor تُسجّل وتمرّ وenforce **ترفض 422 بأسبابها** · الشاشةُ **163** | `supplier_documents_test` **25/0 ×2** (المحورُ الثالثُ في جدوله · 422/409 عند الكتابة · **منتهيةٌ رغم `status='سارية'`** وموشكةٌ بمهلتها وقياسٌ بتاريخٍ ماضٍ · توثيقٌ بلا مستندٍ 422 و**CHECK** · **enforce ترفض الاعتماد 422 والحالةُ لا تتغير** وmonitor تمرّ · والسجلُّ فيه واقعةُ التوثيق) + **16 حزمةَ انحدارٍ خضراء** + برهانُ HTTP للشاشة 163 | — (بذرٌ نسبيٌّ باليوم يُكنس · و`.env` يُستعاد بايت-مطابقًا · وأثرُ الوصل على الحي **صفر**: الافتراضُ `monitor` يقيس ويمرّ) | ① **گوتشا مقيسة**: البوابةُ الحيّةُ تقيس بـ**اليوم**، وبذرٌ بتواريخَ مطلقةٍ في المستقبل يجعل الاختبارَ يقيس ما لا يقيسه المسار — فصارت تواريخُ البذر **نسبيةً باليوم** ② **گوتشا**: `ems_env_all()` **ساكنةٌ داخل العملية** — فقلبُ العلَم يقتضي كتابةَ `.env` **وقياسًا في عمليةٍ جديدة** (نمطُ عدّة E-08 حرفيًّا) ③ **قرارٌ معماريٌّ مزدوج**: لا جدولَ وثائقَ ثانيًا **ولا سجلَّ تدقيقٍ ثانيًا** — التوسيعُ بمحورٍ والقراءةُ بمرشِّح؛ ونسختان تتباعدان بالزمن وتُقرأ إحداهما فقط ④ **الافتراضُ `monitor` لا `off`**: القياسُ يبدأ من اليوم ويظهر في `error_log`، والقلبُ إلى enforce **معلَّقٌ على أسبوع رصدٍ نظيف** (§7-①) ⑤ **أحمرُ سابقٌ كُشف ولم يُخلَق**: `equipment_documents_test` يؤكّد «63 منتهيةً» والواقعُ 34 — **صفرُ صفٍّ تغيّر اليوم** و`expiry_date < CURDATE()` = 34 بالضبط، فالتجديدُ وقع في 2026-07-29 وخطُّ الأساس في التأكيد قديم؛ **يُعلَن ولا يُعدَّل رقمُ تأكيدِ غيره** |
| ③ | M-18 | — | **منجزة** | 2026-07-31 | هجرة `2026_09_09`: `supplier_contracts` **+3 أعمدة** (§2-⑦: ضمانُ الأداء · مهلةُ ردّه · الدفعةُ المقدمة) و**`CHECK` يجعل «ضمانًا بلا مهلةٍ مكتوبة» مستحيلًا** («بعد مهلته» بلا مهلةٍ نصٌّ بلا معنى) + `supplier_contract_closures` (خطواتُ §4 الأربعُ بتواريخها ومراجعها · **UQ(العقد)** = «بمفتاح العقد × التصفية» · **وقيدان**: الإقفالُ يلزمه **شهادةُ إخلاء**، وردُّ الضمان يلزمه **مرجعُ ذمّته**) + `fin_dues` توسعةُ ENUMين (**`guarantee_release`** و**`supplier_closure`**) — «الذمّةُ تُسمّى باسمها لا أخرى ولا تسوية» · `SupplierClosureService` بأربع قواعد: **① التصفيةُ عند الإنهاء لا قبله** · **② الخطواتُ بترتيبها لا بالنيّة** (والرفضُ **يسمّي الناقص**؛ وحصةٌ لم تُستهلك **تُقفل بسببٍ مكتوب يُسجَّل في الحاوية نفسِها**) · **③ ردُّ الضمان بعد مهلته** (423 بتاريخه المحسوب · ثم **ذمّةٌ دائنةٌ بمرجع تصفيتها** · وردٌّ ثانٍ 409) · **④ شهادةُ إخلاءٍ موثَّقة** خدمةً و`CHECK`ًا · **الوصلُ الحي**: `transition` عند «مقفل» وحدَه يمرّ بـ`contractCloseGate` · الشاشةُ **162** | `supplier_closure_test` **27/0 ×2** (ضمانٌ بلا مهلةٍ يرفضه CHECK · «نافذ» 422 · فتحٌ ثانٍ 409 · **موعدُ الردّ 2095-05-30 محسوبًا** · حصةٌ بلا سببٍ 422 ثم تُقفل والسببُ في الحاوية · **رصيدُ سلفةٍ 1000 ⇒ 423 برصيده** · ردٌّ قبل المهلة 423 بتاريخه وبعدها **ذمّةٌ 5000 باسمها** · إخلاءٌ بلا مستندٍ 422 وCHECK · وعقدٌ بلا تصفيةٍ لا يُقفل ثم يُقفل) + **17 حزمةَ انحدارٍ خضراء** (منها حزمُ الحاويات الثلاث) + برهانُ HTTP للشاشة 162 | — (بذرُ 2095 يُكنس · وأثرُ الحجب على الحي **صفر**: صفرُ عقدِ موردٍ في «منتهٍ» أو «مقفل») | ① **گوتشا كبرى**: `op_containers` **حالاتُه ومستوياتُه عربية** (`نشطة/معلَّقة/مقفلة` · `رئيسية/مورد/معدة/مشغّل`) — وكتابةُ `'closed'` كانت **يبتلعها ENUM صامتًا**؛ فالوسمُ بلغة مصدره لا بلغةٍ تُخترع ② **الوصلُ بالمورد لا بعقده**: `op_containers.contract_id` هو **عقدُ العميل** (L1) و`supplier_id` هو رابطُ حصته — فالقياسُ بالمورد **ويُعلَن كذلك**، ولم يُخترع ربطٌ لا وجودَ له ③ **قيودُ الهرم القائم تُحترم في البذر**: `ck_container_alloc` و`ck_container_parent` و`ck_sadv_inst` كلُّها كسرت البذرَ الأولَ فصُحّح البذرُ لا القيد ④ **الاجتهادُ المعلَن**: «استقطاعُ ضمان الأداء واستهلاكُ المقدمة» (§4-التصفية الدورية) **لم يُستبق** — العمودان مكتوبان ووصلُهما بيتُه الاستقطاعاتُ الشهرية لا تصفيةُ الإنهاء ⑤ ردُّ ضمان CON-02 **لم يُعَد استعمالُه**: ذاك ضمانُ عقد العميل بمنطقه، وخلطُهما يوحّد ما فرّقته الوثيقتان |
| ③ | M-17 | — | **منجزة** | 2026-07-31 | هجرة `2026_09_08`: `supplier_evaluation_weights` (وزنٌ لكل مؤشرٍ من الخمسة + **`scale_max`** للمؤشرات العددية) · `supplier_evaluations` (نتيجةٌ · **`weight_measured` تغطيةُ الوزن المقيس** · حالةٌ · قرارُ تجديدٍ — و**قيدان**: المعتمَدُ يلزمه قرارٌ ومقرِّر، و**منعُ التجديد يلزمه سببٌ مكتوب**) · `supplier_evaluation_lines` (سطرٌ لكل مؤشرٍ بقياسه وأساسِه ونسبتِه و**`source_note` يحمل مصدرَه نصًّا**) · `SupplierEvaluationService` بأربع قواعد: **① لا انطباعَ ألبتة** (لا مدخلَ يدويًّا لقيمة مؤشرٍ ولا لنتيجة — والشاشةُ تولّد وتقرأ وتقرّر) · **② لا نتيجةَ بلا وزنٍ مكتوب** (Σ=100 وإلا **422 بالمجموع مسمًّى**) · **③ المؤشرُ بلا مصدرٍ يُعلَن ولا يُقدَّر** (`measurable=0` بسببه · والتغطيةُ **تُحفظ وتُعرض** فلا يختفي التطبيع · و**تغطيةٌ دون 50٪ لا تُعتمد**) · **④ الرقمُ يخبر والإنسانُ يقرّر** · **والمصادرُ الخمسةُ كلُّها حيّةٌ قائمة**: الجاهزيةُ والتغطيةُ من M-16 · المسندةُ من `resp_party='supplier'` · جودةُ المشغّلين من `operator_stop` · والحوادثُ من `tickets` نوع `safety_incident` · **الوصلُ الحي**: `SupplierContractService::transition` عند «مجدَّد» وحدَه يستدعي `renewalGate` — **423 بلا تقييمٍ** و**423 بغير المؤهَّل بفترته ونتيجته وسببه** · الشاشةُ **161** | `supplier_evaluation_test` **36/0 ×2** (Σ=105 ⇒ 422 بالرقم · الحوادثُ بلا مقياسٍ ⇒ تغطيةُ **90٪** ونتيجةُ **91.11** · وبالمقياس **100٪** و**90.00** · و27.6/18.4/16/20 لكل مؤشرٍ بمصدره · وتغطيةُ صفرٍ **لا تُعتمد** · ومحوُ السبب **يرفضه CHECK** · وإعادةُ توليدِ المعتمَد **423** · والتجديدُ 423 ثم يقع) + **17 حزمةَ انحدارٍ خضراء** + برهانُ HTTP للشاشة 161 | — (بذرُ 2094 يُكنس · و**الأوزانُ تُحفظ وتُعاد** لأنها إعدادُ شركةٍ لا بذرَ اختبار · وأثرُ الحجب على الحي **صفر**: صفرُ عقدِ موردٍ في «مجدَّد» — سبعةٌ كلُّها «نافذ») | ① **الوصلُ لم يُبنَ في فراغ**: المؤشراتُ الخمسةُ **كلُّها لها مصدرٌ اليوم** — فلم يُختلق مؤشرٌ ولم يُترك نصٌّ بلا تنفيذ. و«الحوادثُ» رُبطت **ببلاغ السلامة نفسِه** (`safety_incident`) لا بأي بلاغ ② **اجتهادٌ مدوَّن**: النتيجةُ **تُطبَّع على الوزن المقيس** (÷ التغطية) — والبديلُ (القسمةُ على 100) يعاقب الموردَ على نقصٍ في **بياناتنا**؛ فالتطبيعُ وقع و**التغطيةُ تُحفظ وتُعرض في كل شاشةٍ وسطر** فلا يختفي ③ **الحدُّ 50٪** قرارٌ هندسيٌّ معلَن: «تقييمٌ أكثرُ من نصف وزنه بلا مصدرٍ ليس تقييمًا» ④ **گوتشا في العدّة**: الأوزانُ **إعدادُ شركةٍ مشترك** لا بذرٌ معزول — فالاختبارُ يحفظ حالتَها ويعيدها حرفيًّا، وإلا كسر إعدادًا حيًّا (§3) ⑤ **گوتشا**: `TenantDb` بلا `delete($table,$where)` — والكنسُ بـ`deleteRow` صفًّا صفًّا، و`supplier_evaluation_lines` بلا حذفٍ ناعمٍ عمدًا (**قياسٌ يُستبدل لا مستندٌ يُؤرشف**) ⑥ **التجديدُ وحدَه يُحرَس**: باقي انتقالات الحالة لا تمسّها النتيجة — وحارسٌ أوسعُ كان سيحجب ما لا نصَّ فيه |
| ③ | M-16 | — | **منجزة** | 2026-07-31 | هجرة `2026_09_07`: `supplier_capacity` بأعمدة §6 نصًّا (`theoretical_daily` · `min_readiness_percent` · `replace_hours` · UQ(عقد × معدة × سريان)) + `work_model` بالنماذج الأربعة، و**ثلاثةُ `CHECK`** أهمُّها `theoretical_daily > 0` («ومنها **يُقاس** أداءُ المورد» — وطاقةُ صفرٍ تجعل القياسَ قسمةً على عدم) · `SupplierCapacityService` بثلاث قواعد: **① القياسُ من السجل لا من التقدير** — الجاهزية = (المخطط − غيرِ الصالح) ÷ المخطط من `unit_time_log`، و**الوقفةُ المخططةُ والقاهرةُ خارج المقام**، و**غيرُ المسجَّل يُعلَن عددًا مستقلًّا** لا يُطرح صامتًا، و**بلا زمنٍ مخطط لا قياسَ ولا جزاء** · **② مهلةُ الإحلال تنقل ولا تضيف**: نوبةُ التوقف تُجمَّع بالأيام المتصلة، والزائدُ على المهلة **يُنقل** من الجاهزية إلى **التغطية الأشد** — «**يحوّل** التوقفَ» حرفيًّا، فـ**الساعةُ تُجزى مرةً واحدة** · **③ الحدُّ يفعّل والقاعدةُ تحتسب**: `min_readiness_percent` **بوابةُ التفعيل** (وبلا حدٍّ مكتوبٍ **لا جزاءَ جاهزيةٍ ألبتة** ولو قامت القاعدة)، والمبلغُ بقاعدة M-15 بمعدلها **وسقفها**، **واختلافُ الحدّين يُعلَن نصًّا** · **السريانُ مزدوج** (البطاقةُ وعقدُها) · **الوصلُ الحي**: `SettlementService::collectLines` يضيف سطري الجاهزية والتغطية `charge_type='penalty'` · الشاشةُ **160** بلوحةِ قياسٍ ونوبٍ و**تحذيرِ الأثر على الجزاءات** | `supplier_capacity_test` **45/0 ×2** (25 ساعةً مخططةً و**6 مخططةٌ خارج المقام** · 4 غيرَ مسجَّلةٍ **معلَنةً** · 92٪ · ونوبةُ 8 بمهلةِ 3 ⇒ **5 نُقلت و3 بقيت و`3+5=8`** · ومجموعًا **88.89٪** بحدٍّ مرجَّحٍ **85.56٪** · وجزاءٌ **111** و**250** · و**100 مقصوصةً** بسقف 10٪ · وبلا حدٍّ **صفرُ سطر** · والتسويةُ تحمل **361 خصمًا ظاهرًا**) + **16 حزمةَ انحدارٍ خضراء** + برهانُ HTTP للشاشة 160 بالدور 2 | — (بذرُ 2093 يُكنس · وأثرُ الوصل على المال الحي **صفر**: صفرُ بطاقةِ طاقةٍ اليوم فـ`penaltyLines` تعيد صفرَ سطر) | ① **گوتشا بيئية حاسمة**: خدمتا WAMP لا تُشغَّلان بلا رفع، وتشغيلُ `mysqld` بـ`--defaults-file` وحدَه **يفوّت مجموعةَ `[wampmysqld64]`** فيعود `sql_mode` إلى الصارم — و**بذرُ الاختبارات القائمة ينكسر كاذبًا** (`suppliers.phone` NOT NULL بلا افتراض). الصحيحُ نسخُ `my.ini` وإعادةُ تسمية المجموعة إلى `[mysqld]` ② **گوتشا**: `TenantRegistry` **لا يحقن `company_id` لجدولٍ غيرِ مسجَّل** — والإدراجُ يسقط بـ«no default value»؛ فكلُّ جدولٍ جديدٍ يُسجَّل قبل أول كتابة ③ **اجتهادٌ مدوَّن**: «زمنُ الصلاحية للعمل» فُسِّر `tech_breakdown` + `supplier_stop` (المعدةُ غيرُ صالحةٍ بسببها أو بسبب موردها)، و`unlogged` أُخرج من المقام **معلَنًا** لأن إخراجَه يرفع النسبة ④ **ما لا يُقاس يُعلَن**: «إخلالُ المورد **بإحضار** معدة» لا سطرَ له في السجل أصلًا — وقياسُ غيابِ الصفوف تخمينٌ لا قياس، فالمقيسُ **مسارُ الإحلال وحدَه** ⑤ الحدُّ في موضعين (البطاقةُ والقاعدة) **قُصد فيه الفصل**: البطاقةُ تفعّل والقاعدةُ تحمل المال — والمالُ يُقرأ من موضعٍ واحد، والاختلافُ يُعلَن |
| ③ | M-15 | — | **منجزة** | 2026-07-30 | هجرة `2026_09_06`: `supplier_charge_rules` (التحميلاتُ الستُّ نصًّا · طرائقُ التسعير الثلاث · **وسقف** — و**`CHECK` يجعل «قاعدةً بلا سعرٍ مكتوب» مستحيلةً**: `cost` بلا معدل و`cost_plus`/`fixed` **يلزمهما معدلٌ موجب**) + `supplier_penalty_rules` (الأنواعُ الأربعة · عتبةٌ ومعدلٌ بأساسه · **`cap_percent`** · **`inherits_attribution`** — و**`CHECK` يمنع نقضَ الإسناد بلا سببٍ مكتوب**) · `SupplierRuleService` بثلاث قواعد: **① لا تسعيرَ بلا قاعدةٍ — ولا حجبَ بسببها** (تحميلٌ بلا قاعدةٍ يُمرَّر بتكلفته الخام **موسومًا `no_rule`** لا مضروبًا في نسبةٍ مخترَعة — «يُرفض البند لا التسوية») · **② السقفُ يقصّ ويُعلن قصَّه** في التحميل والجزاء معًا · **③ نقضُ الإسناد يلزمه سببٌ مكتوب** والاحتسابُ يُعلن النقضَ وسببَه · **والسريانُ مزدوج**: سريانُ القاعدة **ومدةُ عقدها** معًا · **الوصلُ الحي**: تكلفةُ قطع الغيار والصيانة تمرّ بـ`priceCharge` في `collectLines` والبيانُ يحمل القاعدةَ المطبَّقة أو وسمَ «بلا قاعدة» · الشاشةُ **159** | `supplier_rules_test` **27/0 ×2** (التسعير 1000 ⇒ 1000 · 1150 · **500 مقصوصةً من 1200** · وبلا قاعدةٍ **777 موسومةً** · والجزاء 90٪ لا شيء · 81٪ ⇒ 400 · 20٪ ⇒ 6500 **مقصوصةً إلى 5000**) + **12 حزمةَ انحدارٍ خضراء** | — (بذرُ 2054 يُكنس · وأثرُ الوصل على المال الحي **صفر**: صفرُ قاعدةٍ مكتوبةٍ اليوم فـ`priceCharge` تعيد التكلفةَ الخام) | ① **اجتهادٌ مدوَّن**: §6 يسمّي `formula_json` والمنفَّذُ **معادلةٌ بأعمدةٍ محكومة** و`formula_note` **توثيقًا لا تقييمًا** — صيغةٌ حرّةٌ تُقيَّم وقتَ التشغيل **لا تُحرَس بقيدٍ ولا تُقصّ بسقف**، وهو عكسُ «ولا تتجاوز سقفَها التعاقدي» ونظيرُ قرار M-09 ② **السريانُ المزدوج اكتشفه اختبارُ القبول**: قاعدةٌ مفتوحةُ النهاية كانت تعيش بعد انتهاء عقدها — **صُحّح في المحرّك لا في التوقُّع** (لا عقدَ ⇒ لا تحميلَ بقاعدته) ③ **گوتشا**: أسماءُ FK **عامةٌ على مستوى المخطط** — `fk_penalty_rule_contract` كان مأخوذًا فسُمّي `fk_sup_…` ④ `contract_penalty_rules` القائم **جزاءاتُ عقد العميل** ولا يُخلط بجزاءات المورد ⑤ `supplier_capacity` (**M-16**) هو **مصدرُ القياس** الذي تستهلكه `assessPenalty` اليوم من مُدخَلٍ خارجي — ووصلُه التلقائيُّ يأتي معه |
| ③ | M-14 | — | **منجزة** | 2026-07-30 | **لا هجرة — قراءةٌ خالصة**: `SupplierStatementService` بطبقاته الخمس بترتيب سلسلة القيمة (استحقاقٌ موجبٌ · تحميلاتٌ · **جزاءاتٌ في طبقتها** · **سلفٌ في طبقتها** · سداد) — والقاعدةُ الحاكمة: **كلُّ صفٍّ يحمل رابطَ مصدره، ومن لا مصدرَ له يُعلَن `orphan` ولا يُخفى** (إخفاءُ رقمٍ بلا مصدرٍ **يكذب** وإظهارُه موسومًا **يُصلَح**) · والروابطُ تُحلّ من `source_kind` إلى شاشته (ذمّة · تسوية · دفعة · سلفة · صرفُ مخزون · أمرُ صيانة) · **والرابطُ يشير إلى مصدر السطر لا إلى وعائه** والوعاءُ في عمود السياق · `priceSnapshot` يقرأ **بنودَ عقد المورد النافذة (H-07)** بمعدلاتها وأساسِ استعدادها — «تبويبُ اللقطة يعرض الأسعارَ التي احتُسب بها» · الشاشةُ القائمة **لم تُكسر**: المجاميعُ والجدولان الأصليان كما هما والطبقاتُ **تُضاف تحتها** مع شارةِ رصيد السلف المفتوح وعدّادِ اليتامى في الرأس | `supplier_statement_test` **25/0 ×2** (التصنيفُ الصحيح لكل بند · **يتيمٌ يُعلَن بمبلغه** والعدّادُ يرفعه · الصافي 7400 والرصيدُ 2400 · **الفترةُ تحكم** فسدادُ أغسطس لم يدخل يونيو · واللقطةُ تُعلن حين لا عقدَ نافذ · والعزلُ محفوظ) + **12 حزمةَ انحدارٍ خضراء** | — (بذرُ 2053 يُكنس · صفرُ مساسٍ بتسويةٍ أو دفعةٍ حية) | ① المقيسُ الذي أملى العمل: الشاشةُ كانت **أربعةَ مجاميعَ صمّاء بلا سطرٍ ولا رابط** — فالمورد يرى رقمًا ولا يعرف من أين جاء، وهو **عينُ ما يمنعه §7-القبول** ② **الجزاءُ والسلفةُ كلٌّ في طبقته** لا مدمجَين في التحميلات: الوثيقةُ تعدّهما بندَين باسمهما، ودمجُهما يخفي سببَ النقص ③ **الرابطُ إلى المصدر لا إلى الوعاء**: المصدرُ هو ما «يُقرأ منه المبلغ» (§3)، والتسويةُ سياقٌ يُعرض لا مصدرٌ يُنقر إليه ④ **گوتشا في العدّة**: `fin_payments` يحرسه `ck_pay_fx_pair` (من M-40) فـ`fx_rate` و`base_amount` **يتلازمان** — وبذرٌ يعطي أحدَهما يُرفض صامتًا؛ كُشف لأن العدّة تفحص المُرجَع ⑤ `supplier_entitlements`/`supplier_charges` كجدولين مستقلين بيتُهما **M-15/M-16** — والكشفُ اليوم يقرأ من **أسطر التسوية** وهي المصدرُ الحيُّ القائم |
| ③ | M-13 | — | **منجزة** | 2026-07-30 | هجرة `2026_09_05`: `settlements.state` ⇐ **ثماني حالاتٍ** (`invoiced`+`closed` — **والستُّ القديمةُ محفوظةٌ كلُّها**) + أعمدةُ الفاتورة (رقمُها · تاريخُها · **مبلغُها كما ورد** · الفرقُ · **سببُه ومستندُه** · مُسجِّلُها ومُقفِلُها) + **`CHECK` بنيويٌّ يجعل فرقًا بلا سببٍ ومستندٍ مستحيلًا** + فهرسُ رقم الفاتورة (**ليس UNIQUE عمدًا**: إعادةُ الإصدار بعد إلغاءٍ ضريبيٍّ قرارُ الشؤون لا رفضٌ أعمى — لكنَّ التكرارَ يجب أن يُرى) · `SettlementService::markInvoiced` (حالةٌ خارج المسموح **409** · تسويةُ **عامل 422** «العاملُ لا يُصدر فاتورة» · **فرقٌ بلا سببٍ ومستند 422 بالفرق مسمًّى**) و`close` (للمدفوعة وحدَها **409** · **بندٌ معترَضٌ مفتوح 423**) · الشاشةُ 144 توسّعت (زرُّ الفاتورة ونموذجُ مطابقتها بالصافي المعتمد · زرُّ الإقفال بتأكيدٍ ينصّ على أن التصحيحَ بعكسٍ · وحالتان جديدتان في القاموس) | `settlement_invoice_close_test` **21/0 ×2** — **وقلبُه**: فرقُ 150 بلا سببٍ **422 والحالةُ لم تتغير**، وبهما يُقبل و**الصافي 4700 لم يُمسّ** والفاتورةُ 4850 كما وردت · و`CHECK` يرفض الكتابةَ المباشرة · والمقفلةُ **لا تعود** · + **12 حزمةَ انحدارٍ خضراء** (منها `employee_settlement` 49/0) | — (بذرُ 2052 يُكنس · صفرُ مساسٍ بتسويةٍ حية) | ① **القاعدةُ التي أملت كلَّ عمود**: «الفاتورةُ **لا تغيّر الصافي**» — ولو غيّرته لصار المستندُ الضريبيُّ **مصدرَ الاعتراف**، وهو عكسُ §5 حرفيًّا ② **اجتهادٌ مدوَّن**: §4 يرسم `Approved → Invoiced → PaymentRequested` والمنفَّذُ يولّد طلبَ الدفع **عند الاعتماد** (سلوكٌ قائمٌ مختبَر 49/0). وتقديمُ الفاتورة على طلب الدفع **يعطّل صرفًا مستحقًّا انتظارًا لمستندٍ بيدِ المورد لا بيدنا** — أسوأُ ماليًّا وأكثرُ كسرًا؛ فالفاتورةُ تُستقبل من الحالتين و«الدفعُ لا يكون إلا بتسويةٍ معتمدة» محفوظٌ ③ فهرسُ الفاتورة **ليس UNIQUE**: التكرارُ يُرى ويُحسم قرارًا لا يُرفض أعمى ④ «بندُ تسويةِ الفرق» كسطرٍ ماليٍّ في الدفتر يأتي مع **M-14** حين تُبنى طبقاتُ الكشف — لا يُستبق |
| ③ | M-12 | — | **منجزة** | 2026-07-30 | هجرة `2026_09_04`: `supplier_advance_requests` (ENT-02 §7 حرفيًّا · الأنواعُ الثلاثة نصًّا **نقدًا · نيابةً · عهدةً** · **`doc_ref` NOT NULL + CHECK** · جدولُ استرداد · **`balance` مولَّد** فـ«رصيدُها ظاهرٌ في بطاقته دائمًا» بلا انحراف) + `supplier_advance_recoveries` (**UQ(سلفة × تسوية)** فلا يُسترد بندٌ مرتين · و`doc_ref` يرث سندَ سلفته) · `SupplierAdvanceService`: `open` (بلا سند **422** بنصّ §3) · `approve` (**403 «فصلُ اليدين»**) · `chargeLines` · `applyRecoveries` · `openBalance` · **القاعدةُ الحاكمة في التوقيت**: القسطُ يظهر **بندًا عند التوليد** ولا يُنقص الرصيدَ إلا **عند الاعتماد** — «المسودةُ نيّةٌ والمعتمَدةُ واقعة، ولا يُنقص رصيدٌ بنيّة» · **الوصلان في `SettlementService`**: ① `collectLines` فرعُ المورد يقرأ البندَ **بسنده ورصيدِه قبلَه** («المقاصّةُ الظاهرة» §3) ② `approve` يطبّق الاسترداد · الشاشةُ **158** (مالكُ عقود الموردين 2 يُنشئ · **المالية 17 تعتمد ولا تنشئ**) | `supplier_advances_test` **22/0 ×2** (سلفةٌ بلا سند 422 · كتابةُ `balance` ترفض الصفَّ · **المستردُّ صفرٌ بعد التوليد و300 بعد الاعتماد** · إعادةُ التطبيق صفر · ثلاثةُ أقساطٍ ⇒ `settled` ولا تعود بندًا) + **12 حزمةَ انحدارٍ خضراء** (منها `employee_settlement` 49/0) | — (بذرُ 2051 يُكنس · صفرُ مساسٍ بتسويةٍ حية) | ① **البنيةُ توأمُ `employee_advances` عمدًا** — نمطٌ واحدٌ يُثبت نفسَه مرتين: مستندٌ إلزاميٌّ بنيويًّا · رصيدٌ مولَّدٌ لا يُكتب · واستردادٌ بمفتاحٍ فريد ② **گوتشا في عدّة الاختبار**: `settlement_lines → settlements` قيدٌ يمنع حذفَ الرأس ما بقي سطر — فكنسٌ يحذف أسطرَ نوعٍ واحدٍ يترك التسويةَ حيّةً ويصطدم التشغيلُ التالي بمفتاحها **صامتًا**؛ والكنسُ صار يحذف **كلَّ** أسطرها ③ **گوتشا ثانية**: `uq_settlement_party_period` يمنع تسويتين للطرف نفسِه بالفترة نفسِها — قيدٌ **سليمٌ** يحرس «تسويةٌ واحدةٌ للفترة»، فلكل تسويةٍ في العدّة فترتُها ④ وكلتاهما ظهرتا لأن العدّةَ **تفحص مُرجَع الإدراج وتُعلن فشلَه** — القاعدةُ المستودعية مطبَّقةً على الاختبار لا على المنتج فقط ⑤ M-13→M-19 لم تُستبق: بيوتُها مسمّاةٌ في البطاقة |
| ③ | H-09 | ⑤ المسيّرُ والكشف — **إغلاقُ H-09** | **منجزة** | 2026-07-30 | **لا هجرة** (كلُّ ما تحتاجه مبنيٌّ في ①→④ · والناقصُ الآلةُ والحراسُ والكشف): `PayrollStateMachine` بقائمة سماحٍ من **جدول §5 حرفيًّا** (السبعُ حالات) وحراسها: **صفرُ صفٍّ أحمر** عند المراجعة والاعتماد · **لا اعتمادَ للذات 403** · **مرجعُ الصرف إلزامي 422** · فترةٌ مقفلة **423** · لا سطرَ معلَّقًا عند الإقفال · **والمقفلةُ نهائيةٌ بلا رجوع** («التصحيحُ بعدها بحدثٍ عاكسٍ لا بتعديل») · **وتعريفُ «الأحمر» من النص لا من الرأي** (§7: «صفٌّ بلا لقطةٍ صالحةٍ يظهر أحمرَ ولا يُعتمد») فـ`redRows` ترصد ثلاثةً كلًّا باسمه: سطرٌ لم يكتمل · **مانعٌ مفتوح** (والمستبعَدُ ليس منها) · **لقطةٌ لا تطابق بصمتَها = تلاعبٌ مكشوف** · **«الأثرُ الواحد» (§6)**: حدثُ `payroll.settlement.approved` **واحدٌ لكل (شخص × فترة)** بمفتاح عطالةٍ `payroll_settlement:‹شخص›:‹YYYY-MM›` · `payslip` **بطبقاته** وكلُّ رقمٍ بمصدره ولقطته · `register` صفٌّ لكل شخصٍ **بتذييلٍ ثابتٍ** والأحمرُ **يظهر أحمرَ فعلًا** · الشاشةُ 156 اكتملت (أزرارُ الانتقال بالمسموح · لافتةُ الصفوف الحمراء · السجلُّ · الكشفُ الفردي) | `payroll_run_cycle_test` **32/0 ×2** — **الدورةُ الكاملةُ بلا تدخلٍ يدوي** (فتحٌ←لقطاتٌ←زمنيٌّ←مقاصّةٌ←مراجعةٌ←اعتمادٌ←صرفٌ←إقفال) · **والألوانُ الثلاثةُ تُختبر كلٌّ على حدة** وتُرفض المراجعةُ في كلٍّ ثم تمرّ بعد إصلاحها · والكشفُ 2000−400=**1600** بطبقاته · والسجلُّ **6100** بصفر أحمر · وإعادةُ الاعتماد **لا تكرر** (3 تبقى 3) · + **15 حزمةَ انحدارٍ خضراء** | — (بذرُ 2050 يُكنس · وأحداثُ الاختبار تُكنس بمفتاحها) | ① **تعريفُ الأحمر مأخوذٌ من §7 نصًّا** لا اجتهادًا — وثلاثتُه بأسمائها في الرسالة، فمن يُمنع يعرف أيَّ لونٍ منعه ② **المستبعَدُ ليس أحمرَ**: `kind='excluded'` (مسودةٌ خارج النطاق) لا يمنع اعتمادًا — امتدادٌ لتفريق ENT-01 المسجَّل في ① ③ **مفتاحُ العطالة (شخص × فترة) لا (دورة × شخص)**: فلو أُعيد فتحُ دورةٍ أخرى للفترة نفسِها لم يتضاعف أثرُ الشخص — وهو نصُّ §6 «الأثرُ الواحد» ④ **القيدُ المحاسبيُّ لم يُوصل بعد عمدًا**: الأسطرُ **تحمل جهاتِها ونسبَها سلفًا** من اللقطة، ووصلُها بالمروحة مشروطٌ بمطابقةِ شهرٍ بصفر فرق (ENT-01 §9-الرائدة) — **شرطٌ زمنيٌّ لا عمل** ⑤ **بهذا تكتمل H-09 بشرائحها الخمس** وتُغلق معها **M-20 · M-21 · M-23** المدمَجاتُ بنص PLAN-01 |
| ③ | H-09 | ④ المقاصّةُ والسلف (+M-20+M-21) | **منجزة** | 2026-07-30 | هجرة `2026_09_03`: `employee_advances` (بوابةُ الصرف الواحدة بأنواعها الثلاثة نصًّا · **`doc_ref` NOT NULL + CHECK** · جدولُ استرداد · **و`balance` عمودٌ مولَّد** فلا ينحرف رصيدٌ عن حركته) + `payroll_deductions` (**UQ(دورة×شخص×نوع×مصدر) ⇒ «فلا يُخصم بندٌ مرتين» بنيويًّا** · و`doc_ref` NOT NULL: السطرُ **يرث مستندَ مصدره** فلا خصمَ يتيم) + `payroll_settings.protection_percent` (**NULL افتراضًا** — «حدُّ الحماية **المقرَّر**» فإن لم يُقرَّر يُعلَن ولا يُفترض) · `OffsetService`: `openAdvance` (بلا مستندٍ **422**) · `approveAdvance` (**403 «من أنشأ لا يعتمد»**) · `computeOffsets` بثلاث قواعد: **① لا خصمَ بلا مستند** من السلفة إلى سطر الخصم · **② الحدُّ يقصّ ويعيد الجدولة لا يُلغي** (الباقي يبقى في الرصيد ويُخصم في فترته · والسطرُ موسومٌ `rescheduled` بسببه) · **③ العطالةُ بإعادة البناء** (تعكس خصومَ الدورة عن الأرصدة ثم تعيد الحساب فلا يتضاعف استرداد) · **الوصلان**: ① داخل المسيّر (الصافي = الإجمالي − الخصوم) ② **فجوةُ M-21 المقيسة سُدّت**: `SettlementService::collectLines` كان `if ($partyType !== 'supplier') return` — فتسويةُ العامل تعرض استحقاقَه ولا تعرض ما عليه؛ فأُضيف فرعُ العامل **قبل** الحارس يقرأ أقساطَه المخصومةَ فعلًا **من `payroll_deductions`** بمرجع سندها · الشاشةُ **157** (القوى تُنشئ · **المالية تعتمد ولا تنشئ** — الفصلُ في الصلاحيات نفسِها) و**156** بزرِّ المقاصّة وجدولِ الخصوم | `payroll_offset_test` **28/0 ×2** — **قلبُه معيارُ §6.1-④ حرفيًّا**: (أ) قسطُ 700 وحدُّ 50٪ ⇒ خُصم **500** والباقي 200 **مُرحَّل** والرصيد 400 · (ب) 300 ≤ المسموح ⇒ كاملًا و`settled` · **والفترةُ التالية تخصم الـ400 المرحَّلة وتُقفل السلفة** · + **15 حزمةَ انحدارٍ خضراء** (منها `employee_settlement` 49/0 بعد وصل M-21) | — (بذرُ 2049 يُكنس · والاختبارُ يعيد إعدادَ الحماية إلى ما كان) | ① **`balance` عمودٌ مولَّد عمدًا**: الرصيدُ اشتقاقٌ لا حقلٌ يُصان — ولو صين لانحرف عن حركته يومًا. وكتابتُه ترفض الصفَّ كلَّه (مثبَتٌ في الاختبار) ② **حدُّ الحماية NULL ولا يُفترض**: خصمٌ بحدٍّ مخترَعٍ يظلم، وخصمٌ بلا حدٍّ معلَنٍ يُرى — فالإعلانُ أصدق ③ **الغيابُ لا يدخل `payroll_deductions`**: هو مخصومٌ سلفًا **سطرًا سالبًا** في الشريحة ② — ولو أُضيف هنا لخُصم مرتين (قرارٌ مقصودٌ مدوَّن) ④ **الخصمُ من `payroll_deductions` لا من رصيد السلفة** في التسوية: المخصومُ **واقعةٌ جرت** والرصيدُ **نيّةٌ** — ولا تُحمَّل نيّةٌ على تسوية ⑤ الجزاءاتُ والقيدُ المحاسبيُّ وعكسُ خصوم دورةٍ ملغاة: **معلَنةٌ** لشريحة المسيّر ⑤ |
| ③ | H-09 | ③ المسارُ الإنتاجي | **منجزة** | 2026-07-30 | هجرة `2026_09_02` (توسيعُ `line_kind` بـ`production`/`incentive` **فقط** — لا جداولَ جديدة: المصادرُ كلُّها قائمةٌ والناقصُ **الجسر**) · `ProductionPathService` بثلاث قواعد: **① الكميةُ من السجل القانوني** (`unit_party_awards` بحكم `party='operator'`) و«**بعد اكتمال أحكام الأطراف**» شرطٌ **حرفيّ** (`parties_approved`·`sales_approved`·`converted`) — وواقعةٌ دونها **لا تدخل أجرًا** ويُعلَن استبعادُها في نصّ السطر · **② الوحدةُ يحددها نموذجُ أجر العقد** (per_ton⇒ton · per_trip⇒trip · per_meter⇒meter · hourly⇒hour) فلا تُخلط وحدةُ عقدٍ بوحدةِ آخر · **③ لا تسعيرَ ملفَّق**: كميةٌ بلا معدلٍ في اللقطة · نموذجٌ لا وحدةَ قياسٍ له · صفرُ وحداتٍ محكومة · حافزٌ بأساسٍ ليس كميةً · مستفيدٌ بمسمًّى وظيفيٍّ لم يُحلّ — **كلُّها سطرٌ معلَنٌ بسببه** · والحوافزُ من الكمية نفسِها بأساسها (`unit` كاملةً · `threshold` ما فوق العتبة) ثم **السقفُ يقصّ والحدُّ الأدنى يرفع** ثم **التوزيعُ على المستفيدين بنسبهم Σ=100** · الشاشةُ 156 زرًّا ثالثًا وشارتَي سطر | `payroll_production_path_test` **19/0 ×2** — **قلبُه معيارُ §6.1-③ حرفيًّا**: (أ) بالطن 250×12 + حافزُ عتبةٍ 250 = **3250** · (ب) بالنقلة 40×45 + حافزٌ مقصوصٌ بسقف 50 = **1850** — **بأرقامٍ محسوبةٍ يدويًّا في نصّ الملف** · ومعها: 80 طنًّا لم تكتمل أحكامُها **لم تدخل** · وواقعةُ الشهر التالي لم تدخل · والسقفُ **يُعلن قصَّه** · والعطالةُ لا تضاعف · + **15 حزمةَ انحدارٍ خضراء** | — (بذرُ 2048 يُكنس · صفرُ مساسٍ بواقعةٍ حية) | ① **`hourly` ليس نموذجًا زمنيًّا هنا**: §3-② يعدّ «منفَّذٌ · استعدادٌ · **حضورٌ**» أساسًا إنتاجيًّا — فساعاتُ المشغّل تُقرأ من السجل القانوني كما تُقرأ أطنانُه؛ والزمنيُّ حقًّا ما **لا وحدةَ قياسٍ له** (`fixed_only`·`lump_sum`). صُحّح توقُّعُ الاختبار لا المحرّك ② **گوتشا مكتشفة**: `unit_party_awards.qty_due` عمودٌ **مولَّد** — كتابتُه ترفض الصفَّ كلَّه و`config.php` لا يرمي، فيمضي البذرُ صامتًا وتخرج الدورةُ صفرًا بلا سببٍ ظاهر. القاعدةُ المستودعية حرفيًّا: **افحص المُرجَع لا الاستثناء** ③ المقيسُ الحي: **3 أحكامِ مشغّلٍ فقط وكلُّها `not_due` بصفر** — فأثرُ الشريحة على الإنتاج **صفرٌ اليوم** ويعمل فورَ اكتمال أحكام الأطراف ④ أسسُ الحوافز غيرُ الكمّية (جودةٌ · جاهزيةٌ · سلامةٌ · توفير) **تُعلَن** حتى تُبنى مصادرُ قياسها في M-16/M-17 ⑤ المستفيدُ بالمسمّى الوظيفي يُعلَن وحلُّه إلى أشخاصٍ بيتُه شريحةُ المسيّر ⑤ |
| ③ | H-09 | ② المسارُ الزمني | **منجزة** | 2026-07-30 | هجرة `2026_09_01`: `payroll_absence_types` (كتالوجُ «أيُّ نوعِ غيابٍ يُخصم؟» — سؤالٌ **لا يجيب عنه الموروث**: `worker_leave_absence.event_type` نصٌّ حر) + `payroll_time_inputs` (**`doc_ref` NOT NULL + CHECK** · UQ(دورة×شخص×نوع)) + ثلاثةُ أعمدةٍ على `payroll_lines` (`line_kind` مكوّن/إضافي/**خصمُ غياب** · `entitled_days`/`period_days`) · `TimePathService::compute` بثلاث قواعد: **① التناسبُ يُشتقّ ولا يُدخَل** (تقاطعُ مدةِ العقد ∩ سريانِ المكوّن ∩ مدةِ الدورة — والأيامُ تُخزَّن على السطر فيُرى **لماذا** صار المبلغُ جزءًا من الشهر) · **② الخصمُ سطرٌ ظاهرٌ سالبٌ بمرجع واقعته** لا نقصٌ صامتٌ في مبلغٍ آخر · **③ ما لا مصدرَ له يُعلَن**: نوعُ غيابٍ غيرُ مصنَّفٍ **لا يُخصم تخمينًا** وساعةُ إضافيٍّ بلا معدلٍ **في العقد** لا تصير مالًا · **المسارُ المؤسسيُّ حصرًا** (المشروعيُّ للشريحة ③ — «لا تُحتسب فئةٌ بمسار غيرها» §3) · الشاشةُ 156 توسّعت (زرُّ المسار الزمني · نموذجُ مدخلات الزمن بمستنداتها · وعمودا النوع والأيام) | `payroll_time_path_test` **29/0 ×2** — و**قلبُه معيارُ §6.1-② حرفيًّا**: عشرُ حالاتٍ متمايزةٍ **بأرقامها المحسوبة يدويًّا في نصّ الملف** (شهرٌ كامل · نسبةٌ · بدءٌ ونهايةٌ منتصفَ الشهر · بدلٌ متأخرُ السريان · غيابٌ يُخصم · إجازةٌ مدفوعة · نوعٌ غيرُ مصنَّف · إضافيٌّ بمعدلٍ وبلا معدل) ⇒ **صفرُ فرقٍ في العشرة** · + **15 حزمةَ انحدارٍ خضراء** | — (بذرُ 2047 يُكنس · صفرُ مساسٍ بعقدٍ حي) | ① **المقيسُ أملى التقسيم**: لا جدولَ حضورٍ إطلاقًا ولا مصدرَ للإضافي، و`event_type` لا يقول أيُّ نوعٍ يُخصم — فالتناسبُ يُشتقّ · والغيابُ يحتاج **كتالوجًا** · والإضافيُّ **مدخلًا بمستنده** ② **الكتالوجُ بُذر بـ`deducts=0` لكل نوعٍ موروث**: الافتراضُ الآمنُ ألّا يُخصم من أحدٍ بلا قرار — والقلبُ قرارُ مالكٍ لا اجتهادُ باذر ③ **`doc_ref` إلزاميٌّ على الزيادة كما على الخصم**: «ولا خصمَ بلا مستند» (§4) — وساعةُ إضافيٍّ بلا مرجعٍ رقمٌ يزيد أجرًا بلا سند ④ `per_day`/`per_shift` تُعلَنان لا تُحتسبان: لا عدّادَ أيامٍ ولا ورديّاتٍ مسجَّل ⑤ **گوتشا في عدّةِ الاختبار لا في المحرّك**: بذرُ العقد نافذًا **قبل** إضافة مكوّناته يجعل `addComponent` يرفض 423 بحقّ (حارسُ H-08-②) فتخرج الدورةُ صفرًا — الترتيبُ الصحيح: مسودةٌ ← مكوّناتٌ ← تفعيل |
| ③ | H-09 | ① بوابةُ اللقطة | **منجزة** | 2026-07-30 | هجرتا `2026_08_30/31`: `payroll_runs` (ENT-01 §8 حرفيًّا · الحالاتُ **السبع** نصًّا · **UQ(شركة×من×إلى×فئة) ⇒ «409 دورةٌ قائمةٌ للمفتاح» بنيويًّا**) + `payroll_lines` (**`snapshot_id` NOT NULL بـFK RESTRICT — وهذه هي البوابة**: سطرُ احتسابٍ بلا لقطةٍ **مستحيلٌ في القاعدة** لا مرفوضٌ بفحصٍ يُنسى · و`amount` **يقبل NULL عمدًا** ليُعلَن المنتظر) + `payroll_run_blocks` («قائمةُ الموانع **وروابطها**» صفوفًا لا نصًّا) · `PayrollRunService`: `openRun` (مدةٌ مقلوبة/فئةٌ أجنبية 422 · **فترةٌ مقفلة 423** بحارس M-39 · مكررٌ **409 بمرجعه**) · `bindSnapshots` (**البوابةُ الواحدة**: يقرأ `ContractSnapshotService::snapshotFor` **ولا يقرأ جداولَ العقود مباشرةً** · توليدٌ **ذريٌّ لكل عقد** · سطرٌ لكل (مكوّنٍ × جهةِ تحمّل) بنسبتها من اللقطة) · `verifyImmutability` (كلُّ سطرٍ بلقطته وكلُّ بصمةٍ تطابق مضمونَها) · **حدودُ الشريحة محترمة**: يُحتسب `fixed_amount` و`pct_basic` (لا زمنَ ولا إنتاج) وما عداه سطرٌ `pending_slice` **بلا مبلغ معلَنًا** — «لا احتسابَ ناقصٌ صامت» · الشاشةُ **156** `Workforce/payroll_runs.php` (4 مالكًا · 17 عرضًا) تُظهر المحسوبَ والمنتظرَ وقائمةَ الموانع بأسبابها | `payroll_snapshot_gate_test` **26/0 ×2** — ومنه **الشاهدُ الأكبر**: رفعُ الأساسيّ 1000→5000 بعد الربط **لا يحرّك مجموعَ الدورة** (1250=1250) والبصماتُ تبقى مطابقةً، **ودورةُ الشهر التالي تلتقط 5000** · + **12 حزمةَ انحدارٍ خضراء** (registry 52/0 · snapshot 20/0 · رباعية H-08 · tenant_leak 130/0) | — (بناءٌ جديد · بذرُ 2046 يُكنس) | ① **گوتشا كشفها اختبارُ القبول**: ENT-01 §8 يفرّق نصًّا بين «عقدٍ غيرِ نافذٍ **يُستبعد بسببٍ مكتوب**» و«لقطةٍ مفقودةٍ ⇒ **Blocked**» — ودمجُهما (كما بُني أولًا) **يجعل كلَّ دورةٍ موقوفةً** لمجرد وجود مسودةٍ في السجل. فأُضيف `kind` (هجرة 31): المستبعَدُ يُكتب ويمضي، والمانعُ وحدَه يوقف ② حالةُ العقد تُحكم **في PHP لا في SQL** عمدًا: لو رشّحها الاستعلامُ خارجًا لما وُجد سببٌ يُكتب — فيصير الاستبعادُ صمتًا لا بيانًا ③ `pct_basic` احتُسبت هنا لأنها **حسابُ لقطةٍ خالصٌ بلا مدخلِ زمن**؛ و`pct_gross` أُجّلت لأن «الإجمالي» لا يكتمل قبل ②③ ④ سطرٌ لكل (مكوّنٍ × جهة) لا سطرٌ واحدٌ يُوزَّع لاحقًا — «ولا تُجمع الكلفةُ في مركزٍ واحدٍ ثم تُوزَّع تقديرًا» (ENT-01 §7) ⑤ إعادةُ الربط تبني من الصفر (الأسطرُ مشتقّةٌ لا وقائعُ مستقلة) — والمعتمَدةُ محميةٌ بحارس الحالة فلا يُمسّ محسوبٌ أُجيز |
| ③ | M-25 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_29`: `meter_readings` (UX-10 §8 حرفيًّا: نوعٌ ENUM(hour·km) · تاريخٌ · قيمةٌ · مصدرٌ ENUM(manual·inspection·timesheet·reset) · مرجعُ الواقعة · فارقٌ · **UQ(معدة×نوع×يوم) ⇒ «409 قراءةُ اليوم قائمة» بنيويًّا**) + **`chain_no` سلسلةً**: «تصفيرٌ بقرارٍ موثَّقٍ **يفتح سلسلةً جديدة**» (§8.3-F2 نصًّا) — فالرتابةُ تُفرض **داخل السلسلة** (لولاها لاستحال استبدالُ عدّادٍ معطوب إلا بتزوير) و`CHECK` يُلزم مستندَ التصفير · **ولا CHECK على «≥ السابقة»**: گوتشا «CHECK لا يرى صفوفًا أخرى» — الحارسُ في الخدمة داخل معاملةٍ واحدة · `MeterReadingService`: `record` (نوعٌ/قيمةٌ/تاريخٌ/معدةٌ 422 · **أقلُّ من السابقة 422 بقيمتها وتاريخها ونصِّ التصفير** · **الإدخالُ المتأخر لا يكسر الرتابة** — قراءةٌ تتجاوز لاحقةً 422 · 409 بمرجعها) · `reset` (سببٌ ومرجعُ مستندٍ إلزاميان 422 · والسابقةُ **محفوظةٌ كاملة**) · **`currentMeter` بسلّمٍ معلَن**: قراءةٌ ← افتتاحيٌّ ← **بديلٌ موروثٌ مسمًّى باسمه** (`legacy_proxy` · `is_reading=false` · «⚠ ليس قراءةَ عدّاد») · `preventiveDue` · `staleMeters` · **الوصلان الحيّان**: ① الالتقاطُ في نقطة خنق `mirrorFromTimesheet` (`end_hours/minutes/seconds` عدّادٌ ميدانيٌّ كان يُكتب ولا يُقرأ) — **عاطلٌ ولا يُفشِل كتابةَ الدوام أبدًا** ② `mnt_equipment_actual_hours` (**5 مستدعين أحياء**) صارت واجهةً على `currentMeter` بتوقيعها نفسِه · الشاشةُ **155** `Equipments/meter_readings.php` (الأسطول 7 مالكًا · الصيانةُ 11 تسجّل ولا تصفّر · التشغيل 3 عرضًا) بلوح «لم تُحدَّث منذ 14 يومًا» | `meter_readings_test` **35/0 ×2** + **11 حزمةَ انحدارٍ خضراء** (ts-service 50/0 · fanout 28/0 · attribution 42/0 · claims 51/0 · tenant_leak 130/0) · و`equipment_documents_test` 29/1 **انزياحُ خط الأساس المصنَّفُ سلفًا §3** لا انحدارَ جديد | `docs/reports/M-25_METER_READINGS_20260730.md` + باذرٌ عاطلٌ `m25_meter_backfill.php` (1/1 التُقطت · إعادةُ التشغيل صفر) | ① **العيبُ المركزيُّ المقيس**: `mnt_equipment_actual_hours` كانت تجمع `SUM(operator_hours)` وتُقدَّم عدّادًا — **وليست عدّادًا** (ساعاتُ المشغّل لا الآلة · لا تتأثر بتصفيرٍ · تنمو بعدد المشغّلين)، **والوقائيةُ تُجدول عليها في 5 مواضع**. صارت بديلًا **مسمًّى باسمه** في ذيل السلّم فلا يقفز رقمٌ على البيانات القائمة ولا يُقدَّم ما ليس عدّادًا عدّادًا ② **12 معدةً بـ`operating_hours` بلا تاريخ لم تُحوَّل قراءاتٍ عمدًا**: قيمةٌ بلا تاريخٍ ليست واقعةً واختراعُ تاريخٍ تلفيق ③ المرآةُ `operating_hours` **تُحدَّث** مع كل قراءةِ ساعات وإلا كذبت عشراتُ الشاشات التي تقرؤها — مرآةٌ لا مصدرَ حقيقة ④ **لا عمودَ عدّادٍ على `mnt_inspection`** رغم أن UX-10 يعدّه مصدرًا: لا كاتبَ له اليوم و«لا عمودَ بلا كاتب» — والقيمةُ محجوزةٌ في ENUM ⑤ حقولُ العدّاد في سجل الدوام **قائمةٌ ومهملة** (1 من 366) — ولوحُ «لم تُحدَّث» يجعل الغيابَ مرئيًّا بدل صامت |
| ② | M-09 | — | **منجزة** | 2026-07-30 | ثلاثُ هجرات (`2026_08_26..28`): `contract_price_terms` (المحفِّزاتُ **الثلاثةُ المسمّاةُ في §2-③ نصًّا**: وقودٌ · تضخمٌ · صرف — والمعادلةُ **بأعمدةٍ لا بصيغةٍ حرة**: مرجعٌ · **عتبةُ تفعيل** · **نسبةُ تمرير** · **سقفُ مراجعة** · دورية · سريان) + `contract_price_index_readings` (**`source_ref` NOT NULL + CHECK** — «لا رقمَ بلا مرجع» بنيويًّا) + `contract_price_revisions` **Insert-only** بنتيجةٍ لكل حال (`amended`·`capped`·`below_threshold`·`no_reading`·`no_base_price` — **فالامتناعُ يُسجَّل بسببه ولا يختفي**) و**UQ(شرط×دورة×بند) عطالةً** · `PriceAdjustmentService`: `resolveIndex` (**fx من `fin_fx_rates` حصرًا** — لا مصدرَي حقيقةٍ لرقمٍ واحد · وقودٌ/تضخمٌ من قراءةٍ بمرجعها · **الغيابُ `no_reading` معلَنٌ**) · `evaluate` قراءةٌ خالصة · `applyDue` ذريًّا يولّد **ملحقَ «تغيير أسعار» بـ`effective_from`** (النوعُ قائمٌ سلفًا في ENUM) · `approve` بـ**403 «من أنشأ لا يعتمد»** · **و`contractequipments.equip_price` لا يُكتب عليه أبدًا** · **الوصلُ الحي**: `effectivePrice` في `EffectFanout` جانبِ العميل ⇒ **«لا رجعية» صارت حسابًا لا وعدًا** (سعرُ الواقعة = آخرُ مراجعةٍ معتمَدةٍ سريانُها ≤ يومِها وإلا الأساس) · كرونُ `Contracts/cron_price_adjustment.php` خلف علَمِ قائمةِ عقودٍ `EMS_PRICE_ADJUST` (فارغٌ = مطفأ) · الشاشةُ **154** `Contracts/price_terms.php` (12 مالكًا · 1 و17 عرضًا) بأقسامها الثلاثة | `price_adjustment_test` **38/0 ×2** + **30 حزمةَ انحدارٍ خضراء** (attribution 42/0 · claims 51/0 · fanout 28/0 · ts-service 50/0 · tenant_leak 130/0) — وعُدّل توقُّع `unified_nav` 25→26 بإعلان الشاشة | `docs/reports/M-09_PRICE_TERMS_20260730.md` | ① **گوتشا بنيوية كشفها الاختبار**: `UNIQUE` في MySQL **لا يرى NULL مكررًا** — فشرطٌ على مستوى العقد (`item_id=NULL`) كان يُكتب مرارًا بلا 409 والمفتاحُ زخرفة؛ فصار «كلُّ البنود» **صفرًا صريحًا** (هجرة 28). **القاعدة: لا عمودَ يقبل NULL داخل مفتاحٍ فريدٍ يُراد له المنع** ② **سريانُ التعديل بدايةُ الدورة التالية** (اجتهادٌ مدوَّن): تُراجَع فترةٌ ويسري أثرُها بعدها، فلا تُعاد فوترةُ فترةٍ جرت وقائعُها بسعرها القائم يومَها ③ **فجوةٌ موروثةٌ معلَنةٌ لا مخمَّنة**: الملاحقُ السبعةُ الحية **كلُّها بلا `effective_from`** (بينها ملحقُ سعرٍ فعليٌّ) — واشتقاقُ تاريخٍ لها من `amend_date` تخمينٌ يحرّك مالًا، فتُركت تاريخًا معلَنًا والإلزامُ يبدأ من ملاحق M-09 ④ **أثرُ الوصل على المال الحي صفر** مقيسًا: صفرُ شرطٍ وصفرُ مراجعةٍ معتمَدة ⇒ `effectivePrice` تعود الأساسَ حرفيًّا ⑤ الضريبةُ (رابعُ §2-③) بيتُها الفوترة M-03 · والجزاءاتُ في `PenaltyService` المنفَّذ — لا تُستبق هنا |
| ② | H-07 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_25`: `supplier_contracts` (رأسُ CON-03 §6 حرفيًّا: وصلةُ L1 `client_contract_id` · **UQ(مورد×عقد عميل×بدء)** · قفلٌ تفاؤلي · وصلةُ مصدر) + `supplier_contract_lines` (`work_model` ENUM الأربعة · `unit` **بتسمية محرّك الفوترة نفسِها** · سعرٌ · **`standby_basis` ENUM(none·rate·percent)** ومعدلُه · **UQ(عقد×نموذج×وحدة)** + **قيدا CHECK**: سعرٌ موجبٌ · وأساسٌ ⇔ معدل — فـ«قاعدةٌ بلا سعرٍ مكتوب» **مستحيلةٌ بنيويًّا** لا مرفوضةً بفحص شاشة) · **حالةُ الرأس ENUM عربيٌّ مطابقٌ لـ`ContractStateMachine::ALL`** و`transition()` تعيد استعمال قائمة سماح H-02 (CON-03 لا تعدّد حالاتٍ للمورد — قاموسٌ ثانٍ للعقد نفسِه تلفيقٌ يكسر «الاسمَ في موضعين») · **الترحيلُ قراءةً**: 7 رؤوسٍ = 7 (الحالةُ من أعمدة المصدر الحية لا من التواريخ) و6 بنودٍ مشتقةٍ صدقًا · `SupplierContractService` بحراس §6: نموذجٌ خارج الأربعة 422 · وحدةٌ لا يعرفها المحرّك 422 · سعرٌ ≤0 422 · أساسٌ بلا معدلٍ (والعكس · ونسبةٌ >100) 422 · مكررٌ 409 · **المرحَّلُ 423 بمصدره** · **النافذُ 423 «التغيير بملحق»** · الإنهاءُ بسريانه لا حذفًا · **الوصلُ الحي**: `standbyBasisMap` تُقرأ في `EffectFanout::resolveTimesheet` ويستهلكها `qtyAward` — **فالفجوةُ المدوَّنةُ نصًّا في الكود** («تحويلُ الاستعداد إلى مالٍ يلزمه سعرٌ لا وجودَ له … فيُوسَم `standby_needs_rate`») **سُدّت**: rate ⇒ ساعات×معدل · percent ⇒ ساعات×سعر الوحدة×نسبة · وبلا أساسٍ **null معلَنٌ لا صفرٌ ملفَّق** · الشاشةُ **153** `Suppliers/supplier_contract_lines.php` (مالكُها 2 · الماليةُ 17 عرضًا) — كلُّ حفظٍ عبر الخدمة (صفرُ إدراجٍ خامٍ في الشاشة) | `supplier_contract_lines_test` **38/0 ×2** + **25 حزمةَ انحدارٍ خضراء** (attribution 42/0 · claims 51/0 · fanout 28/0 · tenant_leak 130/0 · الحاويات ×4 · registry 52/0) | `docs/reports/H-07_SUPPLIER_CONTRACT_MIGRATION_20260730.md` (7=7 · 6 بنود · والصفّان المتروكان بسببيهما) | ① **مسارُ حلّ سعر الوحدة لم يُمسّ عمدًا**: الموروثُ يسعّر بنوع المعدة والنموذجُ الجديد بـ(نموذج×وحدة)، فتحويلُ مسار المال قبل مطابقةِ فترةٍ بصفر فرقٍ يخالف §7-② صراحةً — موعدُه ENT-02/M-15؛ والمُضافُ هنا قراءةُ **أساس الاستعداد** وحدَها ② **گوتشا بنيوية مقيسة**: عقدٌ يسعّر نوعَي معدةٍ بسعرين مختلفين لا يُعبَّر عنه في نموذج §6 — الشرطُ `price_variants=1` يمنع اختيارَ أحدهما اختراعًا، والحالةُ لم تقع اليوم (النوعان بالسعر نفسه فانهارا بلا خسارة) ③ `standby_basis='none'` لكل مرحَّلٍ **حقيقةٌ مقيسةٌ لا افتراض** (الموروثُ بلا مفهوم استعدادٍ إطلاقًا) — **فأثرُ الوصل على المال الحي صفر** ④ `supplier_quotas`/`quota_containers` لم يُنشآ: H-06 أثبتت أن op_containers استوعبت الهرم («لا جدولين لحصةٍ واحدة» §6-التوافق) ⑤ أحداثُ §6 (SupplierContracted…) أُجّلت لمستهلكها ENT-02 — «لا علمَ ولا حدثَ بلا قارئٍ واختبار» (N-04 §2-⑤)؛ والتدقيقُ N-02 موصولٌ على كل فعل ⑥ M-15/M-16/M-17 (التحميلُ والجزاءُ والطاقةُ والتقييم) **خارجَ H-07 عمدًا** — بيوتُها الموجة ③ و§7-⑤ تمنع تسليمَ نصف حل |
| ② | N-05 | — | **منجزة** | 2026-07-30 | لا هجرةَ — **تركيبُ أساسَي H-04** (جدولُ onDuty + النقلُ الذري) في كنسٍ يوميٍّ مؤتمت: `RotationSwapService::autoTransferForDate` (OPM-01 §5.2-③ «الحصةُ تنتقل آليًّا في مواعيد التناوب **بلا إعادة بناء التخصيصات**»): لكل حاويةِ معدةٍ نشطةٍ في مشروعٍ مفعَّل — المناوبُ بالجدول ≠ حائزِ الحصة النشطِ الوحيد → **swap ذريٌّ** بسجل قرارٍ آلي «تناوبٌ مجدول» · عطالةٌ (الحائزُ = المناوب فلا نقل) · **الشراكةُ** (أكثرُ من نشطة) ليست تناوبًا فتُتخطى **معلَنةً** · لا مناوبَ بالجدول → يُعلَن ولا يُخترع · **إحكامان في أساس H-04 كشفهما بناءُ الدورة الكاملة**: ① التجميدُ صار **يقفل الحاويةَ على مستهلَكها** (`cap_qty = consumed_qty`) — وإلا ضاعفت دورةُ العودة الرصيدَ (المتبقي انتقل ولم يعد لها) ② `onDuty` تقرأ السلسلةَ **كاملةً** (النشطةُ والمعلَّقة — المقفلةُ وحدَها خارجَها) لأن عضويةَ السلسلة غيرُ حالِ الرصيد: المجمَّدُ بالتناوب **يعود** · `Operations/cron_rotation_transfer.php` (CLI يوميًّا · تاريخٌ اختياريٌّ وسيطًا · بوابةُ النظام بشركة مشروعه) · العلمُ `EMS_ROTATION_AUTOTRANSFER` قائمةَ مشاريعَ (فارغٌ = مطفأ — N-04 §2-③) | `rotation_autotransfer_test` **15/0 ×2** (دورةٌ كاملةٌ ذهابًا وعودةً: Σ caps السلسلة = 300 ثابتةٌ عبر الدورتين — لا فقدَ ولا تضاعف) + `rotation_swap_test` 20/0 + **12 حزمةَ انحدارٍ خضراء** (الحاوياتُ الأربع · daily_plan 26/0 · ts-service 50/0 · tenant_leak 130/0) | — (بذرٌ صوريٌّ 2041 على مشروعٍ افتراضيٍّ 990041 يُكنس — صفرُ مساسٍ بحاويةٍ حية) | ① **العلمُ فارغٌ عمدًا**: يُفتح مع رائد البوابة (10) **بعد** استكمال دوراته من مقبض H-01-③ لا قبله — صفرُ دورةٍ مسجَّلةٍ يعني صفرَ نقلٍ ممكن، والفتحُ الفارغ ضوضاءُ سجلٍّ لا حراسة ② الشراكةُ تُتخطى ولا يُجتهد في اختيار «الخارج» منها — التناوبُ حائزٌ واحدٌ يخلفه واحد ③ **گوتشا الدورة الكاملة**: اختبارُ الذهاب وحدَه كان يمرّ بالتجميد القديم (cap محفوظًا) — والعودةُ وحدَها كشفت التضاعف؛ فاختبارُ دورةٍ ناقصةٍ يخفي عيبَ نظيرتها ④ سجلُّ القرار الآلي يمرّ بنقطة خنق `swap` نفسِها فيرث تدقيقَ N-02 وسطرَ `container_swaps` بحركته |
| ② | H-04 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_24`: `container_swaps` += **moved_qty + to_container_id** (FK) — السجلُّ يحمل حركتَه لا وصفَها (صفرُ صفٍّ حي — لا مساسَ بتاريخ) · `RotationSwapService`: **① الاستبدالُ الذري** `swap` (OPM-01 §5.1 حرفيًّا · runInTransaction): الخارجةُ تُجمَّد **عند رصيدها** (معلَّقة · valid_to · consumed محفوظة) → المتبقي = cap−consumed (صفرُه 422) → البديلةُ: الاحتياطيةُ القائمةُ «بحصةٍ صفرية» **تُفعَّل** (cap+=المتبقي) وإلا تُنشأ بالمتبقي بأبِ الخارجة ودورِها → **الأبُ لا يُمسّ** («حصةُ المورد لا تتغير» جبرًا: Σ الحيُّ = consumed الخارجة + متبقي البديلة = cap الخارجة — مثبَتٌ قبل=بعد) → سطرُ السجل بحركته (من·إلى·الكمية·البديلة·المستند) · حراس: معدة/مشغّل حصرًا 422 · النشطةُ حصرًا 423 · الداخلُ≠الخارج · سببٌ إلزامي «بقرارٍ موثَّق» **② جدولُ المناوبة** `onDuty` («من يعمل في أي تاريخ»): أحدثُ دورةٍ ساريةٍ لكل مشغّل — موقعُ اليوم في (on+off) من anchor · **بلا دورةٍ = مناوبٌ دائمًا** (لا يُخترع نمط) · الترتيبُ بالدور (أساسي←بديل أول←ثانٍ) · «لم تبدأ» لا يناوب — والقائمةُ تشرح كلَّ حلقةٍ بسببها · **الوصلان**: فعلُ «بدّل» في شاشة الحاويات صار نقلًا ذريًّا عبر الخدمة (كان إدراجَ سجلٍّ بلا نقل — الفجوةُ المركزية المقيسة) · خطةُ الغد تقترح «★ مناوبَ اليوم» (اقتراحٌ لا فرض) | `rotation_swap_test` 20/0 ×2 (Σ الأب ثابتٌ مثبَتًا · onDuty بتواريخَ محسوبةٍ يدويًّا 60/30) + 11 حزمةَ انحدارٍ خضراء | — (بذرُ شجرةٍ صورية 2040 يُكنس) | ① «انتقالُ الحصة **المؤتمتُ** في مواعيد التناوب» بيتُه N-05 (تكملةُ H-04 بنص PLAN-01) — أساسُه هنا: الجدولُ + النقلُ الذري ② الاحتياطيةُ القائمةُ تُفعَّل لا تُستنسخ — «بحصةٍ صفريةٍ حتى تُفعَّل» نصًّا ③ گوتشا أثناء الاختبار: تحديثُ cap دون consumed يفشل بCHECK **صامتًا** (افحص المُرجَع) ④ رصيدُ الحي بعد التجميد يُقرأ consumed للمعلَّقة — تقاريرُ السلسلة الكاملة (§5.1-التاريخ محفوظ) تأتي مع تقارير الموجة ④ |
| ② | H-03 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_23`: `daily_plans` (UQ **المشروع×اليوم** · دورة draft→approved→opened→closed · فصلُ واجبات) + `daily_plan_lines` (UQ **الخطة×حاوية المعدة×الوردية** · CASCADE · `operator_container_id` «لا تخصيصَ خارج حاوية») + الشاشة **152** «خطة عمل الغد» (التشغيلُ 3 مالكًا · الموقعُ 5 عرضًا · باب DAILY) · `DailyPlanService` بدورة UX-03 §2.2 حرفيًّا: **الاحتياجُ من حاويات المعدات النشطة** (لا من اليد — مشروعٌ بلا حاويات 422 بنصّ المصدر · عطالةُ التوليد) · **التوزيعُ من سلسلة حاوية المعدة حصرًا** (422 «لا تخصيصَ خارج حاوية» OPM-01 §4) · **التعارضُ الفوري** عبر خطط اليوم كلِّها (مشغّلٌ على معدتين في اليوم×الوردية → **409 بمرجع سطره**) · اعتمادُ الحركة (منشئُها 403) · **فتحُ الغد**: سطرٌ بلا مشغّل → **422 بقائمة النواقص** («لا يُفتح موقعٌ ناقصُ التخصيص») + publishFact إشعارًا · إرجاعٌ بسببٍ إلزامي · **الوصلان الحيّان**: ① `deriveFromEquipment` يشتق المشغّلَ آليًّا من الخطة المفتوحة (المعدة×اليوم×الوردية) — **الفجوةُ المدوَّنةُ نصًّا في الخدمة سُدّت** (passthrough عند الغياب — لا تلفيق) ② سببُ البوابة الرابع `no_open_plan` (سلسلة §6-⑥) خلف علم الرائد ووضعِه (يُرصد أسبوعًا كإخوته الثلاثة — مشروطٌ بentry_date) | `daily_plan_test` 26/0 ×2 (409 حقيقيٌّ بزوجٍ متقاطع السلاسل في المشروع 4) + **16 حزمةَ انحدارٍ خضراء** | — (بناءٌ جديد — بذرُ الاختبار بتواريخ 2039 يُكنس) | ① الورديةُ من حاويات مشغّلي المعدة المميزة والغائبةُ = 1 (لا اختراعَ ورديات) ② سطورُ الاحتياج المتعذرُ توزيعُها كلُّها (سلاسلُ متقاطعةٌ بالكامل) نقصٌ معلَنٌ لا يُلفَّق ③ container_pilot_test حُدّث ببذر خطةٍ مفتوحةٍ للرائد (السببُ الرابع) — وgate-test سليمٌ لأن سياقه بلا entry_date ④ الإشعارُ publishFact (المواقعُ تقرؤه) — دفعُ إشعاراتٍ شخصيةٍ بيتُه H-18 |
| ② | H-06 | ①+② | **منفَّذةٌ سابقًا** | 2026-07-30 | **القياسُ أثبت أن H-01-①② غطّتها**: توليدُ الرئيسية من بنود العقد (`generateMain` — والقرارُ المعلَّل «السقفُ من equip_total_contract لا من commitments اليومية» مدوَّنٌ في الخدمة) · **قيدُ Σ حصص الموردين ≤ الالتزام بنيويًّا** (CHECK «allocated ≤ cap» + `allocate` الذرية: الابنُ وزيادةُ الأب في معاملةٍ واحدة) · حاوياتُ المعدات والمشغّلين بأدوارها + شاشةُ التوزيع (containers.php: توليدٌ · تخصيصٌ بالمستويات الأربعة · إقرارُ المشتق · تبديل) · وحجبُ اعتماد الزائد بنيويٌّ كذلك (CHECK «consumed ≤ cap» + `consume` الذرية بعطالة الجولة) | الدليل: container_transform_test 47/0 · containers_structure_test 35/0 · container_gate_test 39/0 · وصفرُ حاويةٍ حيّةٍ (52) تجاوز مخصَّصُها سقفَها (مثبَتٌ في container_pilot_test) | القياسُ في السجل | ① جدولا supplier_quotas/quota_containers من بطاقة الكتالوج **استوعبتهما** op_containers بمستوياتها — لا جدولين لقاعدةٍ واحدة ② ما بقي من عائلة الحصص هو التفعيلُ (H-01-③ أدناه) |
| ② | H-01 | ③ فتحُ الرائد | **جزئية — الرصدُ جارٍ** | 2026-07-30 | المحرّكُ وُجد كاملًا (assertReady بأسبابه الثلاثة بروابطها · consumeForEntry/reverse بعطالة الجولة — موصولٌ في TimesheetEntryService) والعلمُ فارغًا عمدًا. **الفتحُ**: ① وضعُ رصدٍ جديد `EMS_CONTAINER_GATE_MODE` (enforce افتراضًا — التوافقُ الرجعي؛ monitor يفحص ويسجّل would_block **مهيكلًا** عبر موصل N-02 في activity_logs بأسبابه ومشروعه ويمرّر — نمطُ TenantDb/CSRF وقاعدةُ N-04 §2-④) ② **مقبضُ الدورات** `cnt_action=rotation` في شاشة الحاويات (رابطُ «سجّل دورةَ تناوبه» كان طريقًا مسدودًا — حاويةُ مشغّلٍ حصرًا · دورةٌ موجبة · لا بذرَ آليًّا: اختراعُ أنماط التناوب تلفيق) ③ **فُتح الرائدُ 10** (العقد 7 — الشجرةُ المكتملةُ الصغرى: 1→1→3→14 · مقيسةٌ مع بقية الأشجار) بوضع monitor — الدوراتُ صفرٌ والحجبُ الفوري يوقف الميدان (فخُّ E-08 المدوَّن في رأس البوابة) ④ **مولّدُ تقرير المطابقة الأسبوعي** `Operations/cron_container_gate_report.php` (§13-① الشرطُ الثاني: ما كان سيُحجب مقابل ما مرّ · «أسبوعٌ نظيف ⇒ شرطُ القلب مستوفًى») — وأولُ تقريرٍ كُتب | `container_pilot_test` 16/0 + 7 حزم ختامية خضراء (gate عاد 39/0 بعد تثبيت enforce في عدّته — عقدُه دلالةُ الحجب) · time_lines 16/1 انزياحُ TS#2178 المصنَّفُ سلفًا لا انحدار | العلمُ الحي: `EMS_CONTAINER_GATE=10` · `MODE=monitor` (الرجوع: أفرغ القائمة) | ① **بقيّتُها معلَّقةٌ على الزمن لا على عمل**: أسبوعُ رصدٍ → تقريرٌ نظيف → قلبُ enforce → التعميمُ موقعًا موقعًا — دُوّن في رأس السجل ② monitor أُدخل لأن البوابةَ حجبٌ عند الإدخال بلا رصدٍ وسابقتا E-07/TenantDb أثبتتا النمط ③ الدوراتُ يسجّلها الميدانُ من مقبضها الجديد — والرصدُ يكشف نواقصَها بلا إيقاف ④ قراءةُ المشتقّات القديمة سليمة: صفرُ مساسٍ بأي حاويةٍ قائمة |
| ② | M-10 | — | **منجزة** | 2026-07-30 | لا هجرةَ — **وصلُ ثلاث فجواتٍ مقيسة** (البنيةُ كانت جاهزة: contract_obligations بvalid_from/to وUQ الثلاثي · ENUM الملاحق يحمل «تغيير التزامات» غيرَ مستعمل): خدمةُ `ClientAmendmentEffects` ① `changeObligation` — فعلُ `change_obligation` في معالج إجراءات العقد (المسجَّل auto في action_guard): ملحقُ «تغيير التزامات» بسريانه وقبل/بعد الصادقَين (قبل = الملتزمُ النافذُ الحي) **+ صفُّ المصفوفة مسودةً** بسريانه — **ذريًّا** (runInTransaction · المكررُ UQ → 409 والكلُّ يتراجع) — والإجازةُ تبقى بيد المالية (ق-18: الملحقُ يولّد والماليةُ تُجيز) ② `closePredecessors` — مسارا إجازة المصفوفة (one/all) يقفلان السلفَ المعتمدَ المفتوحَ على (سريان الجديد − 1): «ما قبله بحكمه القديم وما بعده بالجديد» (A6) **بنيويًّا** — وحكمُ السلف لا يُمسّ والإعادةُ عاطلة ③ `rebalanceOps` — ملحقُ النطاق (settlement) يوازن الجذرَ الساعيَّ الواحدَ في **معاملة الملحق نفسِها** (cap_qty ± الكمية) · التخفيضُ دون المخصَّص/المستهلَك → **رفضٌ بمرجع الحاوية** يُسقط الملحقَ كلَّه (Σ ≤ الملتزم UX-05 §8.2 + CHECK حزامًا) · **تعددُ الجذور → موازنةٌ يدويةٌ معلَنة** (لا اجتهادَ في توزيع الكمية — تلفيق) | `m10_amendment_effects_test` 21/0 ×2 (A6 حرفيًّا: وقودٌ من العميل إلينا بسريان الأول) + 13 حزمة انحدارٍ خضراء (attribution 42/0 · reversal 46/0 · containers ×3 · claims 51/0) | — (بذرُ الاختبار عقدٌ صوريٌّ يُكنس — صفرُ مساسٍ بالبيانات الحية) | ① «اعتمادُ الملحق» فُسّر فعلَ المعالج (من له صلاحية تعديل العقد) والتوليدُ **مسودةً** — إجازةُ البند نفسُها بقيت بيد 19 (فصلُ ق-18 لا يُخترق بالملحق) ② إقفالُ السلف يقع عند إجازة الخلف لا عند توليده (وإلا بقي البندُ بلا نافذٍ لو رُفضت المسودة) ③ العقدُ 5 له جذران — قاعدةُ الجذر الواحد تحمي من تلفيق التوزيع ④ **گوتشا CLI مكتشفة**: TenantGateException تحتاج require صريحًا في اختبارات TenantDb اليدوية وإلا ابتلع «Class not found» رسالةَ خرق UQ |
| ② | H-10 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_22`: `employee_contract_amendments` (**القائمُ contract_amendments ملكُ عقود العملاء D02 ولم يُمسّ** — الاسمُ صريح): أنواعُ §4 (أجرٌ·مدةٌ·موقعٌ·نطاقٌ+آخر) · **UQ (عقد×سريان×نوع)** · FK **RESTRICT** · changes_json قبل/بعد حقلًا حقلًا · `EmployeeContractAmendmentService`: الإنشاءُ (النافذُ العائلةَ حصرًا — المسودةُ تُعدَّل مباشرةً 422 · سريانٌ قبل البدء **422** §7.2 · قفلٌ تفاؤلي **409** · مكرر UQ **409** · **«قبل» يُلتقط من الواقع الحي لا من المرسل** · حقولُ head/component/rule بصيغةٍ محكومة) · الاعتمادُ (منشئُه **403** · «قبل» ما زال واقعًا وإلا **409** · تطبيقٌ ذريٌّ runInTransaction · العقدُ **amended** وversion++ · **إبطالُ لقطات H-11 من السريان** — snapshot_invalidated_from في الرد §7.2 نصًّا · publishFact ContractAmended) · الرفضُ بسببٍ إلزامي · **النسخةُ الموقَّعة المقفلة**: attachSignedFile في accepted حصرًا والثانيةُ **423 «ثابتةٌ لا تُستبدل — التصحيحُ ملحقٌ يوضّح»** والانتقالُ Accepted→Signed **يشترطها 422** (حارسٌ في الآلة) · الشاشةُ: قسمُ ملاحقَ (قائمةٌ بقبل/بعد + إنشاءٌ + اعتمادٌ/رفضٌ) ونموذجُ تثبيت النسخة في accepted | `employee_contract_amendment_test` 29/0 (منها N3 §7.3: ما قبل السريان بالقديم وما بعده بالجديد) + 15 حزمةً خضراء — **سلاسلُ الاختبارات الخمسُ حُدّثت بشرط التوقيع** (registry صار 52/0) | — (بناءٌ جديد) | ① التطبيقُ الشرعيُّ يكتب مباشرةً داخل معاملة الاعتماد — لا يمرّ بحارس 423 لأنه ليس «تعديلًا مباشرًا» بل البابَ الذي يشير إليه الحارس ② حقولُ الملحق المسموحة قائمةٌ محكومة (رأسٌ 5 · مكوّنٌ value/rate · قاعدةٌ 4) — إضافةُ/إنهاءُ مكوّناتٍ عبر ملحقٍ تأتي مع حاجتها الفعلية (H-09) ③ فرضُ شرط التوقيع كسر سلاسلَ اختباراتٍ قائمةً فحُدّثت معه في اللقطة نفسِها (لا أحمرَ متروك) ④ M-10 (الملحقُ يولّد التزاماتِه) بيتُها عقودُ العملاء — مصفوفةُ contract_obligations لا هذه |
| ② | H-11 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_21`: `contract_snapshots` **Insert-only** (لا updated_at ولا حذفَ — الصفُّ يُدرج ويُبطل بأعمدته الثلاثة حصرًا): المضمونُ القانوني (الرأسُ + المكوّناتُ الحية + القواعدُ بتوزيعها + التحمّل — فرزٌ ثابت) + `fingerprint` sha1 «ببصمةٍ محسوبةٍ من مضمونها» + `as_of_date` + `amendment_ref` NULL معلَنًا (H-10 لم تُبنَ — لا اختراع) · `ContractSnapshotService`: **بوابةُ القراءة الواحدة** `snapshotFor` (isReadable وإلا 422 «لا يُقرأ إلا نافذ» · عطالةٌ: البصمةُ نفسُها ليومه تُعاد لا تُكرَّر · المرحَّلُ النافذُ تُؤخذ لقطتُه — قراءةٌ لا كتابة) · `verify` (كشفُ التلاعب بالمقارنة) · `invalidateFrom` (الصالحةُ التي as_of ≥ التاريخ **فقط** — «يُعاد احتسابُ ما بعده لا ما قبله») · **الوصلُ الحي**: آلةُ H-08 تُبطل آليًّا عند التعليق/الإعارة/الاستئناف/الإنهاء/الانتهاء من يومها (لا يرمي — الإبطالُ لا يعطّل الانتقال) وملحقُ H-10 سيمرّ بسريانه | `contract_snapshot_test` 20/0 ×2 + 13 حزمة انحدارٍ خضراء (رباعيةُ H-08 كاملةً بعد وصل الإبطال) | — (Insert-only — لا بياناتَ تاريخيةً مُسّت) | ① بذرةُ `unit_party_awards.policy_snapshot` (بلا بصمةٍ ولا إبطال) بقيت لعقود الأطراف التشغيلية — توحيدُها مع مستهلكها H-09-③ لا استباقًا ② لقطةُ كل يومِ احتسابٍ صفٌّ ولو تطابق المضمونُ (كلُّ سطرِ حسابٍ يحمل لقطتَه **ليومه**) ③ إبطالُ الحدث من يومِه يشمل اللقطاتِ المستقبليةَ التواريخ — نصُّ «ما بعده» حرفيًّا ④ لا شاشةَ — بوابةٌ خدميةٌ مستهلكُها H-09 |
| ② | H-08 | ④ التحمّل — **إغلاقُ H-08** | **منجزة** | 2026-07-30 | هجرة `2026_08_20`: `cost_bearers` متعددُ المالك (§7.1 نصًّا: owner_type ENUM(component·rule)+owner_id · bearer_type ENUM الجهات الأربع «مشروعٌ·عقدُ عميل·إدارةٌ داخلية·كيانُ الشركة» + bearer_id NULL لجهة company · percent · **حذفٌ ناعم** — الاستبدالُ يطوي قرارَ التحميل السابق ولا يمحوه) · `setCostBearers` ذريًّا (runInTransaction: طيٌّ ناعمٌ + إدراجٌ — لا حفظَ جزئيًّا أبدًا): **Σ≠100.00 → 422 بالفارق ورفضُ الحفظ** بنص PLAN-01 §5.2-④ · جهةٌ أجنبية/مكررة/بلا معرّفٍ 422 · مشروعٌ وعقدُ عميلٍ يُستوثقان من نطاق البوابة · حراسُ الشرائح (النافذُ 423 «بملحق» · المرحَّلُ محصَّنٌ بنيويًّا) · `costBearersOf` تقرأ الحيَّ وحدَه · الشاشةُ: عمودُ «التحمّل» بشاراته ونموذجِ الحفظ الدفعي في جدولَي المكوّنات والقواعد | `cost_bearers_test` 22/0 ×2 + **20 حزمةَ انحدارٍ خضراء** (كاملُ رباعية H-08: 51+28+23+22 = 124/0) | — (بناءٌ جديد) | ① إشارةُ المكوّن المفردة (الشريحة ②) بقيت اختصارَ الحالة البسيطة والشجرةُ هنا للمتعدد — الغائبُ يسقط إليها ② «اشتقاقُ مركز التكلفة من الجهة في القيد» (§3.3) بيتُه وقتُ الاحتساب H-09/FES لا هنا ③ لا UQ بنيويًّا على الجهة (الطيُّ الناعم يعيدها) — التكرارُ يُرفض في الخدمة ④ **بهذا تكتمل H-08 بشرائحها الأربع** — والتاليةُ H-11 اللقطةُ «تُبنى معها لا بعدها» |
| ② | H-08 | ③ الحوافز (+M-23) | **منجزة** | 2026-07-30 | هجرة `2026_08_19`: `incentive_rules` (§3.3 نصًّا: الاسمُ حرٌّ من الاتفاق · الأساسُ ENUM **7** (وحدة·عتبة·جودة·جاهزية·سلامة·وقود·شرائح) · معدلٌ وعتبةٌ و**سقفٌ** وحدٌّ أدنى ودوريةٌ وشرطُ استحقاقٍ ونطاقٌ (مشروع·نوع معدة·موقع) وسريانٌ وحالة) + `incentive_allocations` (ابنُ قاعدته **CASCADE** · UQ مستفيدٍ/قاعدة · المستفيدُ شخصٌ أو مسمًّى وظيفيٌّ يُحل وقتَ الاحتساب «مشغّلٌ ومساعدٌ ومشرف») · **قيدُ Σ=100.00 في الخدمة بالاستبدال الذري** (`replaceChildren` — گوتشا CHECK لا يرى صفوفًا أخرى): Σ≠100 → **422 بالفارق** (§7.2) والقائمُ لا يُمسّ · التوزيعُ الغائب = 100٪ لصاحب العقد ضمنًا · حراسُ الشريحة ② نفسُها (المحرَّرُ حصرًا · النافذُ 423 «بملحق» = «لا رجعية» §3.3 · المرحَّلُ 423 بمصدره) · الشاشةُ: قسمُ حوافزَ في لوحة العقد (قاعدةٌ + توزيعٌ يُحفظ دفعةً) | `incentive_rules_test` 23/0 ×2 (N1 توزيعُ 80/20 يُحفظ · N2 80/30 → 422 بالفارق 110 والقائمُ محفوظ — حالتا §7.3 حرفيًّا) + 11 حزمة انحدارٍ خضراء | — (بناءٌ جديد — لا حوافزَ مهيكلةً في الموروث إطلاقًا) | ① M-23 دُمجت هنا بنص PLAN-01 «تُدمج في شريحة H-08-③» — الموجة ③ لا تعود إليها ② المستفيدُ نوعان (employee·job_title) لأن «مشغّلًا ومساعدًا ومشرفًا» صفاتٌ قد لا تُعرف أشخاصًا وقتَ العقد — حلُّها وقتَ الاحتساب (H-09-③) ③ التوزيعُ الغائب = صاحبُ العقد (§3.3 «**قد** يُوزَّع») ④ allocations بلا soft عمدًا — replaceChildren يكنسها صلبًا داخل معاملتها وهي إسقاطُ تعريفٍ لا سجلَّ وقائع |
| ② | H-08 | ② المكوّنات | **منجزة** | 2026-07-30 | هجرة `2026_08_18`: `pay_components` سطرُ CON-01 §7.1 حرفيًّا — النوعُ ENUM(**20** — قائمةُ §3.2 نصًّا لاتينيةً والتعريبُ في الخدمة) · طريقةُ الاحتساب ENUM(**10**) · **أعلامُ الدخول السبعة** (تأمينات·ضريبة·إجازة·نهاية خدمة·ساعة·إضافي·وعاء حافز — «خصائصُها السبع» في PLAN-01) + is_variable + periodicity + جهةُ تحمّلٍ مفردة (شجرةُ Σ=100 للشريحة ④) + cost_center FK + سريانٌ وحالة (active·replaced·ended — تصريحُ E-24 لاحقًا مع H-10) · خدمةُ addComponent/updateComponent/endComponent في `EmployeeContractService`: التحريرُ على draft/completed حصرًا والنافذُ **423 «بملحق» (H-10)** والمرحَّلُ **423 بمصدره** · مبلغٌ للثابت ومعدلٌ للنسب/عن-الوحدة 422 · سالبٌ 422 (الخصمُ بيتُه M-11) · **صفرُ نسبةٍ مثبَّتةٍ في الكود** (قاعدة §3.2 — مفحوصةٌ آليًّا بregex) · الشاشةُ توسّعت: `?contract_id=N` لوحةُ مكوّناتٍ بأعلامها وإنهائها وإضافتها (الموديول 151 نفسُه) | `pay_components_test` 28/0 ×2 + 14 حزمة انحدارٍ خضراء (بينها registry 51/0 · tenant_leak 130/0 · ts-service 50/0) | — (بناءٌ جديدٌ — لا تعبئةَ رجعية: بدلاتُ الموروث كلُّها NULL وأسعارُ السياسات كاتبُها مصدرُها N-04) | ① «الخصائصُ السبع» في PLAN-01 فُسّرت أعلامَ الدخول السبعة من الثلاث عشرة (البقيةُ أعمدةُ قيمٍ لا أعلام) — العدّ يطابق تمامًا ② الشرائحُ/المعادلةُ تُقبل بلا قيمةٍ معلَنةً (بيانُ tiers مع مستهلكه في H-09-②) ③ لا تعبئةَ من الموروث عمدًا — لا اختراعَ مكوّناتٍ من NULL ④ حالاتُ المكوّن الثلاث بُذرت والتصريحُ الكامل (انتقالاتُها) بيتُه E-24/H-10 |
| ② | H-08 | ① رأسُ العقد | **منجزة** | 2026-07-30 | هجرتا `2026_08_16/17`: كتالوج `pay_models` (النماذجُ الـ15 من CON-01 §3.1 نصًّا · code لاتيني + label_ar + calc_path) + رأسُ `employee_contracts` حرفيًّا من §7.1 (الفئاتُ الأربع · state ENUM **18** لاتينيًّا · UQ الشخص×الكيان×البداية · UQ المصدر×الشركة · فهرسُ التنبيه (state,end_date) · FK الشخصُ/المشروعُ/النموذج · قفلٌ تفاؤلي version) + **الترحيلُ قراءةً** بوصلة مصدرٍ: worker_contract (1/1 مرآةً بلا إعادة حكم) · drivercontracts (0 — الخريطةُ مكتوبة) · سياساتُ المشغّلين ⇒ **7 رؤوسٍ** (رأسٌ لكل شركة×مشغّل · المعتمدُ وحدَه active · تعددُ الأسس composite) — والمصادرُ لم تُمسّ وتبقى الكاتبَ (N-04 ①) · `EmployeeContractStateMachine` (قائمةُ سماحِ §4 · بوابةُ قراءةٍ واحدة isReadable · hold/resume بسببٍ إلزاميٍّ وعودةٍ إلى حيث كان · اعتمادُ المنشئ 403 · نسخةٌ متغيرة 409 · أرشيفٌ نهائي 423 · تدقيقُ N-02 + publishFact في نقطة الخنق) + `EmployeeContractService` (نموذجٌ أجنبي 422 · تداخلُ مدةٍ 409 بمرجعه · تعديلُ نافذٍ 423 «التغيير بملحق» · **المرحَّلُ محصَّنٌ 423 بمصدره**) · شاشةُ `Workforce/contract_registry.php` (الموديول 151 · مالكُها القوى 4 بجوار كاتبها القديم · أزرارُ allowedFrom حصرًا · شارةُ انتهاء ≤30 يومًا · شارةُ المصدر · no-datatable+stateSave:false) · تسجيلُ الجدولين في TenantRegistry (T_TENANT soft / T_GLOBAL) | `employee_contract_registry_test` 51/0 ×2 + **22 حزمة انحدارٍ خضراء** | `docs/reports/H-08_1_MIGRATION_RECONCILIATION_20260730.md` (8 رؤوس = 1+0+7 · بنودٌ مدوَّنة) | ① «الجداولُ الثلاثة» حُسمت قياسًا: worker_contract (الكاتبُ الحي) · drivercontracts (0 صف) · سياساتُ المشغّلين في contract_hour_policies بنص CON-01 §7.1 «تصير عرضًا لعقدِ نموذجٍ تشغيلي» — لا contracts/supplierscontracts (بيتاهما H-10/H-07) ② ENUM الحالةُ 18 لا 17: عنوانُ §4 يعدّ 17 وجدولُه يسمّي 18 متمايزةً — أُخذ الجدولُ الأدق ③ rejected/declined→draft إعادةُ صياغةٍ قياسًا على رجوعات H-02 ④ «قراءةً» فُسّرت إسقاطَ رؤوسٍ محصَّنًا (423) والكتابةُ في المصدر حتى مطابقة N-04 — لا علمَ رجوعٍ لأن لا كاتبَ حُجب ⑤ عقدُ worker_contract المنتهي تاريخُه «نافذٌ» رُحّل مرآةً ودُوّن (كنسُ Expired الآلي لاحق) ⑥ **گوتشا مكتشفة**: employees بلا is_deleted — شرطُه كان يُفرغ منسدلةَ المسؤول في sites.php صامتًا (أُصلح في الشاشتين) ⑦ أسعارُ السياسات لم تُرحَّل عمدًا — بيتُها الشريحة ② |
| ② | E-07 | — | **منجزة** | 2026-07-30 | إلزامُ مسؤول التوقف «بعد وجود المرجع» (N-03 ✓): `assertStopResponsible` في الخدمة (عائلةُ التوقف السبعُ ثابتةً · موضعا الإدراج كلاهما: الإنشاءُ والتحديث) — توقفٌ بمسؤول none/فارغ → **422** تحت `enforce` ورصدٌ تحت monitor · **ردمُ** الصفَّين التاريخيَّين الوحيدَين **من حكم مصفوفة عقدهما (5)** لا تخمينًا (fuel→client · equipment_readiness→company) بأثر تدقيق N-02 (`database/seeds/e07_stop_resp_backfill.php` بـdry-run) · **قُلب `EMS_RESP_PARTY_STRICT=enforce`** بعد نافذة رصدٍ نظيفة (صفرُ would-reject بعد الردم — قاعدة N-04 §2-④) | `resp_party_stop_test` 12/0 ×2 + حزمُ الوحدات السبع (ts-service 50/0 · time_lines على انزياح خط الأساس 16/1 المصنَّف سلفًا TS#2178 — لا انحدارَ جديد) | الردمُ داخل الاختبار (1925/1926 = حكمُ المصفوفة حرفيًّا) | ① unlogged خارج عائلة الإلزام عمدًا — سلةُ الترحيل القديم «أعطال أخرى» وإلزامُها يكسر مرآةَ legacy ② العملُ الفعليُّ والاستعدادُ resp=none بحقٍّ (ليسا توقفًا) ③ ملتزمُ الوقود في العقد 5 **client** (billable_standby) — الردمُ كشف أن «توقفَ وقودٍ» هناك يُفوتر استعدادًا |
| ② | N-03 | — | **منفَّذةٌ سابقًا** | 2026-07-30 | **القياسُ نقض فرضيةَ الكتالوج**: «إكمالُ التعبئة لـ7 عقود» — والمقيسُ فعلًا **10/10 عقودٍ معبّأةٌ مصفوفتُها** (9 بنودٍ لكلٍّ والعقدُ 810 عشرة · 91 صفًّا حيًّا · كلُّ بندٍ بملتزمٍ من القائمة المحكومة وapproval_state=approved) — بذرُ ق-2 «التسعةُ معًا» (CON-02) أتمّها قبل أن تصل المهمة | استعلامُ التغطية: صفرُ عقدٍ حيٍّ بلا مصفوفة (شرطُ الطبقة ④ «معبّأةٌ لكل عقدٍ نافذ» مستوفًى نصًّا) · و`attribution_test` 42/0 يثبت الأحكامَ الثلاثة آليًّا | القياسُ في السجل | لا بناءَ لقائمٍ (حلقة العمل ③) — والبندُ الرابع للطبقة ④ («صفرُ توقفٍ بلا مسؤول») أُنجز في E-07 التالية |
| ② | H-05 | — | **منجزة** | 2026-07-30 | هجرتا `2026_08_14/15`: جدولُ `sites` (الهرم §2-③: UNIQUE اسمٍ داخل المشروع · FK مشروعٍ ومسؤول · نوعُ mine/site لاتينيًّا عمدًا «المنجمُ حالةٌ من الموقع» · `is_default` لموقع الترحيل) + `contracts.site_id` FK (NULL مسموحٌ حتى إلزام H-03) + **تعبئةٌ صادقة**: موقعٌ افتراضيٌّ لكل مشروعٍ باسمه (19/19) وكلُّ العقود مربوطة (10/10) — «المشروعُ كان الموقعَ ضمنًا» فلا اختراع · تسجيلُ `sites` في TenantRegistry (T_TENANT · soft) · شاشةُ `Projects/sites.php` (الموديول 150 · مالكُها المشاريع 1 · التشغيلُ والموقعُ عرضًا · صلاحيةٌ صارمةٌ بالكود الحرفي · حارسُ «الافتراضيُّ لا يُعطَّل» · تدقيقُ N-02 على الحفظ · scopedQuery) | `sites_entity_test` 24/0 ×2 + 9 حزم انحدار خضراء (عُدّل توقُّع unified_nav 24→25 بإعلان الشاشة) | التعبئةُ داخل الاختبار (19/19 · 10/10 · صفرُ ربطٍ متقاطع) | ① بطاقةُ المواصفة قبل البناء (36054c7) وفق §7-⑥ ② ENUM لاتيني والتعريبُ في الشاشة (گوتشا الترميز) ③ رُصد مفتاحٌ مكررٌ قائمٌ سلفًا في TenantRegistry (`admin_subscription_requests` سطرا 46 T_TENANT ميّت و314 T_PLATFORM نافذ — قائمٌ قبل H-05، لم يُمسّ ويُدوَّن للمراجعة) ④ ربطُ بقية الجداول التشغيلية بالموقع (وحدات · حاويات) يأتي مع مستهلكيه H-03/N-03 لا استباقًا |
| ① | N-04 | — | **منجزة** | 2026-07-30 | `docs/specs/N-04_MIGRATION_ROLLBACK_PLAN_ar.md` — قوننةُ العرف المنفَّذ: المراحلُ الخمس (بناءٌ بجانب القائم ← كتابةٌ مزدوجة ← مطابقةُ فترةٍ بصفر فرق ← رائدةٌ بقائمة نطاقٍ أسبوعًا ← تعميمٌ والقديمُ يُجمَّد لا يُحذف) · قواعدُ علَم الرجوع الخمس (قائمةُ نطاقٍ لا ثنائي · تحريرُ .env يسري فورًا · صفرُ فقد · monitor→enforce→monitor · لا علمَ بلا قارئٍ واختبار) · سجلُّ الأعلام الـ18 الحية بوضعها وطريق رجوعها · إلزامُ التحويلات الخمسة القادمة بعلَمها المقرَّر ورائدتها | `rollback_flag_test` 11/0: قائمةُ النطاق مختبرةٌ (حذفُ وحدةٍ يرجعها وحدَها · الفارغةُ fail-safe) + الأعلامُ الـ18 بمقابضها 18/18 + استيثاقُ المسارَين المختبرَين (writer_flip 19/0 · doc-guard 41/0) + گوتشا «الملفُّ لا متغيّرُ العملية» محروسة | — | ① الخطةُ توثيقُ النمط الناجح لا اختراعٌ — سوابقُه الأربع مسمّاة ② رجوعُ DOC_EXPIRY الفعليُّ في TASK-0 هو برهانُ الرجوع الحي فوُثّق شاهدًا ③ **بإقفالها أُغلقت الموجةُ ① كاملةً** — ملفُّ الفحص `docs/reports/WAVE1_CLOSURE_20260730.md` |
| ① | H-23 | — | **منجزة** | 2026-07-30 | القالبُ الملزم `docs/specs/SPEC00_INVENTORY_TEMPLATE_ar.md` (تجسيدُ SPEC-00 نافذًا: بطاقةُ الجرد بحقولها السبعة · الخللُ الستة · المساراتُ الأربعة · قواعدُ البيانات التاريخية «لا مسح» · بطاقةُ المواصفة بثمانيتها · شرطُ القبول) + الجردُ `docs/specs/INVENTORY_WAVE123_ar.md`: **13 بطاقةً مقيسةً من النظام الحي** (الاستعمالُ من activity_logs بعدِّه ومستخدميه وآخر استخدام · البنيةُ من information_schema) تغطي كلَّ مهامّ الموجتين ②③ — أظهرُ المقيس: ثلاثيةُ سجلات العقود (contracts 48ع/10ص نشطة · supplierscontracts 54ع/7ص · **drivercontracts 54ع/صفر صف شبهُ ميتة**) والرواتبُ خارج النظام كليًّا وكشفُ المورد صفرُ استخدام وquotations عروضُ مبيعاتٍ لا RFQ | فحصُ بنية markdown + مطابقةُ الأرقام للمسبار (وثائقُ — لا كودَ يُختبر) | القياسُ داخل البطاقات نفسِها | ① الموجةُ ① جردُها ضمن بطاقات مهامّها فالجردُ للموجتين ②③ بنص «ما ستمسّه فقط» ② حقلُ الألم الميداني لا يُقاس آليًّا — «يُستكمل بالجرد الميداني» بمالكٍ وموعدٍ (رخصةُ SPEC-00 نصًّا) ③ القالبُ «مكتملٌ تقنيًّا — ينتظر اعتمادَ المالك» (§11) والعملُ لا يقف عليه |
| ① | H-22 | — | **منجزة** | 2026-07-30 | مساعدٌ عام `includes/datatable_server.php` (فكُّ بروتوكول DataTables بتحصين: 50 افتراضًا · سقفٌ صلب 250 يقصّ «الكل» · **فرزٌ بقائمة سماحٍ — لا اسمَ عمودٍ من المتصفح** · بحثٌ في الحقول المعرَّفة) — والاستعلامُ يبقى في شاشته بنمط عزلها · تحويلُ الأكبرَين المقيسَين: **سجلُّ النشاط** (كان 5000 صف SSR من 13,155 — نقطةُ `?ajax=dt` + `getDataTablePage` بعدَّين وLIMIT/OFFSET + الفلاترُ خادميةً بدل `ext.search.push`) و**دفترُ الناقل** (كان كاملًا بلا LIMIT — النقطةُ عبر `scopedQuery` بنطاقَي الشركة والمشروع) · الافتراضُ العام `pageLength: 50` و`searchDelay: 400` (UI-01 §9/§4) يسري على كل الجداول | `datatable_server_test` 34/0 + دخانُ E2E للنقطتين (JSON قياسي بصفحة 3) + 13 حزمة انحدار خضراء | — (قراءةٌ محضة — صفرُ مساسٍ ببيانات) | ① «الجداولُ الكبيرة» أولًا بنص شرط الإغلاق — النمطُ المرجعي بُني بنمطَي العزل (مستودعٌ خام · scopedQuery) وتعميمُ التسعين شاشة مع عقد الشاشة M-42/M-44 (الموجة ④) ② النقطةُ داخل الشاشة (نمطُ `?ajax=logs` القائم) ترث جلستَها وعزلَها ③ تصديرُ الصفحة تحت serverSide مقبولٌ — الكاملُ عبر إطار Excel الموحّد ④ `stateSave:false` في المحوَّلتين (درسُ الفلاتر الخارجية) |
| ① | H-20 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_13`: `users.supplier_entity_id` FK→suppliers (كان العزلُ **مستحيلًا بنيويًّا** — لا عمودَ ربط، والدورُ 8 بـ35 منحةً يرى كلَّ شيء) · `SupplierPortalGuard`: النطاقُ من البنية (العمودُ ثم `employees.supplier_id`) · fail-closed (مقيَّدٌ بلا مورد يرى صفرًا برسالة علاج) · موردٌ أجنبي **403 مسجَّلةٌ** عبر موصل N-02 · **بوابةُ 404 في الطبقة** (`gateScreen` من `enforce_current_page_view_permission` — كلُّ شاشة سايدبار) · سايدبارٌ أضيق (`filterNavItems` في `getUnifiedNavItems`: 10→4 أبواب، إخفاءٌ لا حذفُ منح) · حقنُ النطاق في 7 شاشات موردية + كشف الحساب (منسدلتُه تُقصر على مورده) · AJAX: قراءةُ المعدات منطَّقة · نقطتا نماذج الكتابة ومعالجُ إجراءات العقد محجوبةٌ كليًّا · `main/users.php`: حقلُ ربطٍ إلزاميٌّ للدور 8 يُصفَّر لغيره | `supplier_portal_guard_test` 52/0 ×2 + 19 حزمة انحدار خضراء | — (صفرُ حسابٍ على الدور 8 — لا بياناتَ تاريخيةً مُسّت، والإنفاذُ الفوري بلا علمٍ لهذا آمن) | ① نطاقُ الموجة ① هو العزلُ لا البوابةُ الكاملة (اللوحةُ واعتمادُ الجوال لـH-15/H-18 — وبطاقةُ الجرد تنصّ) ② 403 للمورد الأجنبي و404 للمسار الأجنبي حرفيًّا بنص §8.1 ③ حجبُ نقاط نماذج الكتابة كليًّا بدل تنطيقها (البوابةُ قراءةٌ حصرًا) ④ العمودُ على `users` لا جدولَ صفات — H-15 يرحّله ⑤ أُصلح انزياحُ `unified_nav_test` (20→24 بأربعِ شاشاتٍ معلنةِ المصدر) |
| ① | M-40 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_12`: عمودُ `base_amount` على الدفتر (كان غائبًا بنيويًّا!) + عمودا السعر/المعادل على `fin_payments` + تعبئةٌ رجعيةٌ شاملة بالسعر النافذ بتاريخ كل حركة (دفتر 55 · ذمم 6 · دفعات 3 · قيود 9 · الآثار) + قيدا CHECK تزاوجٍ · الناشرُ يحسب الثلاثيةَ عند النشر (`resolveFxRate` رأسًا وأثرًا) · محرّكُ إعادة التقييم الدورية `ems_fx_revalue_open_dues` (فرقٌ → قيدُ مسودةٍ على حساب 5900 «فروق عملة» — لا مساسَ بالأصل، وغيابُ الحساب يُعلَن لا يُخترع) | `base_equivalent_test` 19/0 + 10 حزم انحدار خضراء | سعرُ SDG→USD وُجد مسجَّلًا (0.000185) — فجوةُ F-04 المؤجَّلة سُدّت فاكتملت قيودُ M-38 المنتظرة | ① خسارةُ/ربحُ التقييم على 5900 مقابل حساب الذمم 2100 ② الأرصدةُ المفتوحة = ذممٌ بلا تسوية ③ گوتشا مقيسة: `CURDATE()` في MySQL قد يسبق `date()` في PHP — تواريخُ الاختبار من جهةٍ واحدة |
| ① | M-39+M-08+M-33 | — | **منجزة** | 2026-07-30 | حارسُ `includes/period_guard.php` (`ems_period_check` 423 + `ems_period_close_blockers`) · موصولٌ في: **الناشر** (نقطةُ خنق الدفتر — قبل كتابة الجذر، والعطالةُ تسبقه بفحصٍ مسبقٍ يحفظ الشفاءَ الذاتي) · حفظُ القيد اليدوي · اعتمادُ المستخلص (423) · الصيانة (423→`retry_pending`) · شاشةُ الفترات (موانعُ Close بقائمتها + الفتحُ الاستثنائي بسببٍ إلزاميٍّ مدقَّق) · M-08: `claim_already_billed_units` والمفوترُ سابقًا 409 بمرجع سطره بدل الاستبعاد الصامت | `period_lock_test` 19/0 + 10 حزم انحدار | — | ① `planned` ليست مقفلة — الإقفالُ فعلٌ على ما فُتح (حجبُها أسقط مروحةَ أيامٍ مشروعة) ② العطالةُ قبل الحارس: إعادةُ نشر المنشور تعيد مرجعَه ولو أُقفلت فترتُه ③ فترةُ حكم الفوترة فترةُ يومِ الإجازة لا فترةُ العمل ④ أُصلح انزياحا `claims_test` المصنَّفان (بكارةُ الجدول + الفترة الفرعية صارت 409 بالمواصفة) |
| ① | N-02 | — | **منجزة** | 2026-07-30 | موصلُ `includes/audit_trail.php` (`ems_audit_change`: diff بقيم قبل/بعد JSON · لا صفَّ للاتغيير · CLI-safe · لا يرمي) موصولٌ في: انتقالات العقد الثلاثة (نقطةُ خنق `emit`) · التسويات (رفعٌ واعتماد) · القيود (ترحيلٌ وحذفُ مسودة) · الصلاحيات بشاشتيها (منح/تعديل/سحب/شامل — مع التقاط «قبل» قبل الحذف) | `audit_trail_test` 26/0 (E2E على عقدٍ حقيقي + مصدريًّا للبقية) | — | ① القياس: 13,111 صفَّ activity_logs وصفرٌ منها بقيمة «قبل» — الوسيطُ HTTP يسجّل الطلبَ لا التغيير ② E2E على العقود والبقيةُ إثباتٌ مصدري + برهانُ الموصل (تكرارُ E2E لكل شاشةٍ HTTP مكلفٌ بلا فائدةٍ برهانية) ③ أُصلح انزياحُ `contract_state_machine_test` (9→≥9) المصنَّفُ سلفًا |
| ① | N-01 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_11`: `UNIQUE(company, parent_kind, parent_ref, effect_type)` على `fin_event_links` (دفترُ عطالة المروحة كان بلا فهرسٍ فريدٍ إطلاقًا — فحصٌ تطبيقيٌّ وحدَه) · عطالةُ `fin_auto_journal` (قيدٌ حيٌّ قائمٌ للحدث يُعاد لا يتضاعف) · مصفوفةُ الإثبات لـ13 مسارًا ماليًّا | `idempotency_resend_test` 14/0 (إعادةُ إرسالٍ فعلية ×2 على 4 مسارات) + 8 حزم انحدار خضراء | `docs/reports/N-01_IDEMPOTENCY_MATRIX.md` | ① برهانُ المصفوفة إحالةٌ لاختباراتٍ خضراءَ قائمةٍ لكل مسار — لا إعادةُ بناءِ بيّناتٍ قائمة ② المطابقةُ البنكية (المسار 13) تُثبت مع H-13 لأنها لم تُبنَ بعد |
| ① | H-12 | — | **منجزة** | 2026-07-30 | 3 هجرات (`2026_08_08..10`): 14 عمودَ عقدٍ على `fin_financial_events` (بصمةُ المستند سطر×نسخة · `event_version` · `causation_id` · `fiscal_period_id` FK · `due_date` · الطرفُ الموحّد · تدقيقُ الفاعلين · `fes_status` ENUM(14)) + جدولُ `fin_event_effects` بمفتاحه الفريد الخماسي (NOT NULL على أعمدته — NULL يقتل UNIQUE) وFK CASCADE (الأثرُ ابنُ رأسه) · خدمةُ `EventStateMachine` (قائمةُ سماح §7.2 · قفلٌ تفاؤلي 409 · ختمُ الفاعلين · مرايا state/event_status) · وصلُ الناشر (Published+طرف+فترة+أثر) ومسارات الاعتماد/الرفض/الإعادة/الترحيل · توسيعُ `immutable_allow` | `fes_event_contract_test` 45/0 + 7 حزم انحدار خضراء | تعبئةٌ رجعية 45/45 حدثًا (fes/فترة/طرف/آثار) | ① `fes_status` عمودٌ حاكمٌ و`state` مرآةٌ — لا تحويل ② رفضُ الشاشة = ReturnedToSource لا Rejected النهائية ③ فاعلو التاريخ NULL لا يُخترعون ④ آثارُ enterprise الـ17 والمصروفاتُ بلا مرجعٍ الـ11 مُسقَطةٌ معلَنةً ⑤ حارسُ الفترة المقفلة مؤجَّلٌ لموضعه M-39 ⑥ FK الأثر CASCADE لا RESTRICT (الحذفُ الصلبُ كنسُ اختباراتٍ حصرًا — الإنتاجُ ناعمٌ محصَّن) |
| ① | M-38 | — | **منجزة** | 2026-07-30 | هجرة `2026_08_07_journal_head_columns.sql`: الأعمدةُ السبعة على `fin_journal_entries` (txn_date · خيطُ الطلب ×3 · ثلاثيةُ العملة) + `cost_center_id` FK على السطور + قيودُ CHECK الثلاثة (توازن Σ · تزاوج fx/base · base=ROUND) + تعبئةٌ رجعية (9 قيود) · وصلُ الكاتبَين: `fin_auto_journal` (يستنتج من الحدث) و`journal_form_fin.php` (منسدلا المركز والعملة · رفضُ غير المتوازن · السببُ إلزامي) · دوالُّ `fin_cost_center_for_project` / `fin_request_thread_for_event` / `fin_cost_center_options` / `fin_currency_options` | `journal_head_columns_test` 27/0 + مالية 5×خضراء | بطاقة `docs/specs/M-38_journal_head_columns.md` | ① currency='SDG' مقيسةٌ من الأحداث التسعة حرفيًّا ② fx/base يبقيان NULL معلَنَين حتى يُدخل سعرُ SDG→USD (فجوة F-04) ③ توازنُ Σ قيدُ CHECK لا فحصُ شاشةٍ فقط ④ مركزُ السطر بتطابق اسم المشروع مع الدليل ⑤ لا عملةَ على مستوى السطر (القيدُ واحدُ العملة) |
| صفر | TASK-0 | — | **منجزة** | 2026-07-30 | `EMS_DOC_EXPIRY_GUARD` أُعيد إلى `monitor` · باذرُ وسم «موروثٌ بلا مرجع» (`database/seeds/doc_expiry_legacy_mark.php` — 26 وثيقةً وُسمت والتواريخُ لم تُمسّ) · استثناءُ أعلام الطاقة غير المخلَّصة من ضحية `document_guard_test` · تحييدُ `EMS_ATTRIBUTION_MATRIX` داخل اختبار آلة الحالات | `timesheet_entry_service_test` 50/0 · `document_guard_test` 41/0 ×2 | `docs/reports/DOC_EXPIRY_RECONCILIATION_20260730.md` | ① سببُ الفشول الستة لم يكن حارسَ الوثائق بل حارسَ الإسناد — حُيّد داخل الاختبار والعلمُ الحي بقي `on` (التغطية في `attribution_test.php`) ② التعبئةُ الرجعية تعذّرت: المصادرُ الثلاثة تحمل التلفَ نفسَه فوُسم بلا تصحيح (عقيدة ⑦) ③ الهوياتُ الثماني المنتهيةُ حقيقةً لا تُمسّ (منبِّهةٌ لا حاجبة) ④ رُصد تكرارُ رقم هويةٍ واحدٍ لثلاثة مشغّلين (23–25) — يخصّ طبقةَ الهوية ويُعالَج في موعدها |

