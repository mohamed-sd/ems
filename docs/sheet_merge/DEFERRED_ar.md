# شاشاتٌ لم تُمَسّ — تنتظر قرارًا

**تاريخ:** 2026-09-07 · بطاقةُ «سجل حقول الورقة» فيها **باقيةٌ كما هي**،
وجدولُها **لم يُدوَّن في دفترِ الحذف** لأنّ لها قارئًا بعد.

| # | الشاشة | سببُ التأجيل |
|---:|---|---|
| 1 | `Clients/client_contacts.php` | لا جدول عرض في الصفحة · جدول القاعدة clients تطالب به أكثر من شاشة |
| 2 | `Clients/clients.php` | جدول القاعدة clients تطالب به أكثر من شاشة |
| 3 | `Clients/quotation_negotiation.php` | جدول القاعدة quotations تطالب به أكثر من شاشة |
| 4 | `Clients/quotations.php` | جدول القاعدة quotations تطالب به أكثر من شاشة |
| 5 | `Contracts/claims.php` | القاعدة ترجّح جدولا جانبيا ضيقا على القائمة الرئيسية |
| 6 | `Contracts/collections.php` | جدول القاعدة غير مؤكد بالاسم |
| 7 | `Contracts/commercial_board.php` | عدة جداول عرض ولا واحد منها يشترك مع الورقة في حقل · جدول القاعدة غير مؤكد بالاسم |
| 8 | `FinRequests/effect_map.php` | جدول القاعدة غير مؤكد بالاسم |
| 9 | `Finance/bank_reconciliation_fin.php` | جدول القاعدة ملتبس: حسابات مصرفية أم أسطر كشف |
| 10 | `Finance/cfo_daily_board_fin.php` | جدول القاعدة غير مؤكد بالاسم |
| 11 | `Finance/financial_statements_fin.php` | جدول القاعدة غير مؤكد بالاسم |
| 12 | `Finance/journal_form_fin.php` | جدول القاعدة غير مؤكد بالاسم |
| 13 | `Finance/tax_fin.php` | جدول القاعدة غير مؤكد بالاسم |
| 14 | `Finance/tre_beneficiary.php` | جدول القاعدة غير مؤكد بالاسم |
| 15 | `Finance/tre_liquidity_board.php` | جدول القاعدة غير مؤكد بالاسم |
| 16 | `Fleet/asset_full_history.php` | جدول القاعدة غير مؤكد بالاسم |
| 17 | `Fleet/fleet_schema_matrix.php` | جدول القاعدة غير مؤكد بالاسم |
| 18 | `Maintenance/dashboard_mnt.php` | لا جدول عرض في الصفحة · جدول القاعدة غير مؤكد بالاسم |
| 19 | `Maintenance/preventive_plans.php` | القاعدة ترجّح جدولا جانبيا ضيقا على القائمة الرئيسية |
| 20 | `Maintenance/workshop.php` | جدول القاعدة غير مؤكد بالاسم |
| 21 | `Operations/attendance.php` | جدول القاعدة غير مؤكد بالاسم |
| 22 | `Operations/monthly_close.php` | جدول القاعدة غير مؤكد بالاسم |
| 23 | `Operations/site_gate_equip.php` | جدول القاعدة غير مؤكد بالاسم |
| 24 | `Portal/my_achievement.php` | جدول القاعدة غير مؤكد بالاسم |
| 25 | `Portal/my_portal.php` | جدول القاعدة غير مؤكد بالاسم |
| 26 | `Portal/my_tasks.php` | جدول القاعدة غير مؤكد بالاسم |
| 27 | `Portal/vp_actions_followup.php` | جدول القاعدة غير مؤكد بالاسم |
| 28 | `Portal/vp_dashboard.php` | جدول القاعدة غير مؤكد بالاسم |
| 29 | `Portal/vp_departments.php` | جدول القاعدة غير مؤكد بالاسم |
| 30 | `Portal/vp_pending_actions.php` | جدول القاعدة غير مؤكد بالاسم |
| 31 | `Procurement/dashboard_proc.php` | جدول القاعدة غير مؤكد بالاسم |
| 32 | `Procurement/proc_packages.php` | القاعدة ترجّح جدولا جانبيا ضيقا على القائمة الرئيسية |
| 33 | `Procurement/proc_po_amendments.php` | عدة جداول عرض ولا واحد منها يشترك مع الورقة في حقل |
| 34 | `Procurement/proc_rfq.php` | القاعدة ترجّح جدولا جانبيا ضيقا على القائمة الرئيسية |
| 35 | `Procurement/stock_proc.php` | جدول القاعدة غير مؤكد بالاسم |
| 36 | `Procurement/wh_count.php` | جدول القاعدة غير مؤكد بالاسم |
| 37 | `Risk/risk_reports.php` | عدة جداول عرض ولا واحد منها يشترك مع الورقة في حقل · جدول القاعدة غير مؤكد بالاسم |
| 38 | `Tickets/gov_dept_crp.php` | لا جدول عرض في الصفحة · جدول القاعدة غير مؤكد بالاسم |
| 39 | `Tickets/ticket_contextual_open.php` | لا جدول عرض في الصفحة |
| 40 | `Tickets/ticket_form.php` | جدول القاعدة tickets تطالب به أكثر من شاشة |
| 41 | `Tickets/tickets_list.php` | جدول القاعدة tickets تطالب به أكثر من شاشة |
| 42 | `Transport/transfer_dashboard.php` | لا جدول عرض في الصفحة · جدول القاعدة transfer_orders تطالب به أكثر من شاشة |
| 43 | `Transport/transfer_fleet.php` | جدول القاعدة غير مؤكد بالاسم |
| 44 | `Transport/transfer_in_transit.php` | جدول القاعدة غير مؤكد بالاسم |
| 45 | `Transport/transfer_order_form.php` | جدول القاعدة transfer_orders تطالب به أكثر من شاشة |
| 46 | `Transport/transfer_orders_report.php` | جدول القاعدة غير مؤكد بالاسم |
| 47 | `Workforce/project_contracts.php` | جدول القاعدة غير مؤكد بالاسم |
| 48 | `Workforce/recruitment_pipeline.php` | جدول القاعدة غير مؤكد بالاسم |
| 49 | `Workforce/worker_worklog.php` | عدة جداول عرض ولا واحد منها يشترك مع الورقة في حقل |
| 50 | `user_capacities.php` | جدول القاعدة غير مؤكد بالاسم |

## ما يحسمها

**سببان لا ثالثَ لهما:**

1. **جدولُ العرض ملتبس** — الصفحةُ فيها أكثرُ من جدولٍ ولا يُقطَع أيُّها «جدولُ الصفحة».
2. **جدولُ القاعدةِ غيرُ مقطوعٍ به** — الصفحةُ تقرأ من عدّةِ جداولَ ولا يُعرَف صاحبُ الحقل،
   أو المرشَّحُ جدولٌ عامٌّ مشترَك (‏`users` · `clients` · `employees` …) يخصُّ شاشاتٍ أخرى.

⛔ **وعمودٌ يُنشَأ في الجدولِ الخطأ خطأٌ دائمٌ في القاعدة** — فلم يُخمَّن.
يكفي لحسمِ كلِّ سطرٍ أن يُقال: أيُّ جدولٍ في القاعدةِ يملك حقولَ هذه الشاشة؟
