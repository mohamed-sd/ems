# وثيقة التسليم — جلسة 2026-07-26

**الفرع:** `feature/inspection-to-maintenance-order` · **الحزم:** 477/477 (16 حزمة) · **شجرة العمل:** نظيفة
**المدخل:** حزمة `docs/update0001` — **16 مستندًا** (وصلت UX-03 في 2026-07-26 مساءً فاكتملت الحزمة)

> **⚡ تحديث (جلسة لاحقة 2026-07-26): §9-③ خطواتها ①→④ أُنجزت** — commits
> `8884363` (①+②) · `a8f64e4` (③ على 50 صفًّا نظيفًا بقرار المالك «لا تعتمد
> القديمة» — صفر فرق) · `c333e65` (سياساتُ المشغّلين UX-02 §8.2 + وصلُ
> المروحة ④). **الأعلام الحية:** `EMS_UNIT_DUAL_WRITE=on` ·
> `EMS_UNIT_FANOUT_SOURCE=legal` · أثرُ employee_due مفعَّلٌ بالسياسات
> (التجريبية محصورةٌ بنافذة 2027-02). **المتبقي من §9-③:** ⑤ إغلاق
> fin_unit_records · ⑥ الشاشات Views/Redirects — وقرارات المالك في §6-جديد.

---

## 1 · ما جرى في هذه الجلسة

| # | الطلب | المخرَج |
|---|---|---|
| ① | قراءة النظام القائم ثلاثًا (آخرها عكسية) ودراسة 15 مستندًا | تقريرُ فهمٍ + **تحقّقٌ عكسيٌّ من الكود والقاعدة الحيّة: كل ادّعاءات الوثائق طابقت حرفيًّا** |
| ② | استخراج التاسكات وترتيبها بثلاثة مستويات | **69 تاسكًا**: 27 سهل · 29 متوسط · 13 صعب + شجرة تبعيات |
| ③ | تنفيذ التاسك الأول | سدُّ خرق الناشر + الردم الصادق |
| ④ | التاسك الثاني | المصدر الموحّد للسايدبار (23 دورًا) |
| ⑤ | شاشة الإدارة + تعميم الأدوار | كلاهما |
| ⑥ | لوحات الأدوار | **12 لوحة — كلُّ الأدوار النشطة 23/23 بالوراثة** (اكتملت بلوحة التشغيل بعد وصول UX-03) |

**11 التزامًا** في الجلسة. **15 التزامًا غير مدفوعٍ** إلى GitHub (مفتاح SSH بعبارة مرورٍ عند المالك).

---

## 2 · ما تطلبه الوثائق (الخلاصة الحاكمة)

### الهرم
```
UX-00 الدستور (14 مبدأً · 9 خطوات)  ⟷  FES-01 معيار الحدث المالي
                    ↓
        UX-01 الدليل (155 شاشة × 13 دورًا)
                    ↓
   UX-02 المالية (تصحيحي أول)  →  SPEC-01 كراسة الـ35 شاشة
                    ↓
  UX-03 التشغيل ★ · UX-04 صيانة · UX-05 موردون · UX-06 قوى
  UX-07 بلاغات · UX-08 مبيعات · UX-09 مشتريات · UX-10 أسطول
```
★ **UX-03 وصلت متأخرةً وهي حجرُ الزاوية** — ستُّ وثائقَ كانت تحيل إليها. راجع §6.
**عند التعارض:** الدستور ← وثيقة الإدارة ← مواصفة الشاشة. ولا يُقدَّم اجتهادُ منفّذٍ على نصٍّ أعلى.

### الحكم المقيس على النظام (UX-02 §1)
| المحور | النسبة |
|---|---|
| إعادة التسمية النصية | 85٪ |
| تنظيم السايدبار شكليًّا | 70٪ |
| **تحويل النظام إلى رحلة مستخدم** | **35٪** |
| **إعادة هندسة شاشات المالية** | **30٪** |
| **الكلي** | **50–55٪** |

> «تحقق Renaming + Sidebar Grouping، ولم يتحقق Task-Based ERP. **ولا تُنفَّذ أي جولة تسميةٍ جديدةٍ قبل إنفاذ هذا الطلب.**»

### الأعمدة الخمسة
1. **الأبواب الستة** لكل إدارة بفصلٍ صارم.
2. **لوحةٌ لكل دور** بسبعة مكوّنات — وإلغاء اللوحة العامة.
3. **الرحلة لا الشاشات** — شاشةٌ أمٌّ بشريط مراحل.
4. **الإدخال مرةً واحدة** — الحقول ثلاثُ طبقات، والإلزاميُّ وحده يُطلب.
5. **FES** — كل حدثٍ بالحقول العشرين والمجموعات الثماني.

### التصحيحات الوظيفية (لا تجميلية)
- إبطالُ **«التطابق الثلاثي» للوحدات** → أحكامُ أطرافٍ مستقلة (UX-02 §8.1).
- **سياسات المشغّلين** الكاملة (4 نماذج × 7 أسس) بدل العمودين (§8.2).
- **الحقول الثلاثة منفصلةٌ أبدًا**: زمنُ العمل · وحدةُ العقد · الإنتاج التحليلي.
- **لا كتابةَ على الدفتر إلا بالناشر** (§9).

### بوابات §9 الأربع
| البوابة | الحال |
|---|---|
| ① سدُّ مسار الكتابة المباشرة | ✅ أُنجزت |
| ② معالجة الصفوف اليتيمة العشرة | ✅ أُنجزت |
| ③ **مصدر حقيقة الوحدات** | ⬜ **الوحيدة الباقية** |
| ④ توحيد مصدر السايدبار | ✅ أُنجزت |

---

## 3 · ما أُنجز تفصيلًا

### 3.1 سدُّ خرق الناشر (`b8f4e0f` وما بعده)
- `Finance/import_events_fin.php` كان يكتب في `fin_financial_events` بـ`$gate->insert` مباشرةً — **صفر استدعاءٍ للناشر** — فولّد 10 صفوفٍ بلا جذر.
- حُوِّلت القناة إلى `EventPublisher::publish`، ورُدمت العشرة **بنسبٍ صادقٍ كامل**: تتبّعتُ كلَّ صفٍّ إلى أمر شرائه (`PRC-PO-0001..0010`) فطابقت **10/10 بالمبلغ والشركة** — فكُتب مصدرُها الحقيقي لا وسمٌ عامٌّ كاذب.
- حزمة الناشر عادت **54/54** بعد فشلٍ ثابت.

### 3.2 المصدر الموحّد للسايدبار (`ba8de33` · `1800b83`)
**السبب الجذري المكتشف:** الظهورُ محكومٌ بـ`modules.owner_role_id` (الملكية) والوصولُ بـ`role_permissions` (الصلاحية) — و**425 منحةَ عرضٍ** لا يعبّر عنها نموذجُ الملكية، فاختُرعت ثلاثةُ مصادرَ إضافيةٍ ترقيعًا.

**قاعدة المالك المعتمدة:** «الدورُ يحدد الروابطَ التي تتبع له، والصلاحيةُ تحدد أتظهر أم لا.»
⇒ `nav_items` (التبعية والمكان) × `can_view` (الترشيح).

- **396 صفًّا لـ22 دورًا** · برهانُ تصييرٍ فعليٍّ **19/19 دورًا تطابقٌ 1:1**.
- **11 رابطًا ميتًا انكشفت وأُغلقت** بنيويًّا (الإعدادات لثمانية أدوار + موروثا المالية للأدوار 18–22).
- التكرارُ الخماسي لـ`main/project_users.php` انحلّ بقيدٍ فريد.
- علم `EMS_NAV_UNIFIED_ROLES` (23 دورًا) — الرجوعُ بحذف رقمٍ بلا نشر كود.
- شاشة `admin/permissions/nav_items.php` بقيدَي الإنفاذ (لا اسمَ يطابق بابًا · لا مصطلحَ معماريًّا محظورًا).

### 3.3 لوحات الأدوار (`27913dd` → `15d188b`)
- محرك `includes/role_board.php` + قالب عرضٍ مشترك `includes/role_board_widgets.php`.
- **12 لوحة**: 4 مخصصة (المالي 17 · الصيانة 13 · المشتريات 16 · الرحلات 23) + **شاشة عامة واحدة** `main/role_board.php` تصيّر ثمانيةَ أدوارٍ من إعدادها.
- علم `EMS_ROLE_BOARD_ROLES` = **22 دورًا** (الكل عدا التشغيل 1 وابنه 7).
- **التنبيهات من نصّ UX-01 §8 حرفيًّا** — والغائبُ مصدرُه **وُثِّق ولم يُلفَّق**.

---

## 4 · قواعد المالك المعتمدة في هذه الجلسة (ملزمة)

| # | القاعدة | الموضع |
|---|---|---|
| ١ | **التبعية تحدد القائمة والصلاحية ترشّح** — رُفض كشفُ الـ425 منحة | السايدبار |
| ٢ | المجموعات التسع تبقى **داخل** الأبواب الستة | السايدبار |
| ٣ | «الرئيسية» تحوّل الدورَ إلى **لوحته مباشرة**؛ والفرعيُّ يرث أباه | اللوحات |
| ٤ | اللوحات الثلاث القائمة **تبقى ويُضاف الناقص** لا تُعاد | اللوحات |
| ٥ | اللوحات للأدوار الـ13 الأم والفرعيُّ يرث | اللوحات |
| ٦ | الردمُ بنسبٍ صادقٍ كامل لا بوسمٍ عام | الناشر |
| ٧ | **قاعدة عدم التلفيق**: مؤشرٌ بلا مصدرٍ لا يُعرض ويُوثَّق غيابُه | عام |

### 4.1 · انحرافٌ واحدٌ معلَنٌ عن نصّ الدليل — «سجل النشاط»

**الوضع:** `ActivityLogs/activity_logs.php` بُذر في باب **الإعدادات والتدقيق (SET)** لدورين (1 و7)، بينما UX-01 §9 وUX-03 §4.1 يضعانه في **التقارير والتحليلات (REP)**.

**الحكم (قرار المنفّذ 2026-07-26، والمالكُ فوّضه):** يبقى في SET. والسببُ نصٌّ لا رأي:

- **الدستور §2-٨ يذكره بالاسم**: «سجلاتُ التدقيق التقنية (الأحداث، روابط الأثر، الناشر، **النشاط**) أدواتُ المراجع خلف صلاحيته — لا نقاطَ عملٍ يومية… تُجمع في باب الإعدادات والتدقيق».
- **الدستور §6 الباب ⑥**: «خلف الصلاحية حصرًا — **ومعها سجلات التدقيق التقنية**».
- **وقاعدةُ التعارض في §1**: «الدستور ← ثم وثيقةُ تطبيق الإدارة ← ثم مواصفةُ الشاشة».

**تخفيفٌ عمليٌّ يجعل القرار رخيصًا:** الموضعُ صار **بياناتٍ لا كودًا** — صفٌّ في `nav_items`. فتغييرُه إلى REP ضغطةٌ واحدةٌ من `admin/permissions/nav_items.php` بلا نشرٍ ولا ترحيل. ونطاقُه دوران فقط.

> **إن رأى المالكُ خلافَ ذلك:** غيّر عمود الباب للصفّين من الشاشة، وحدّث هذه الفقرة.

---

## 5 · گوتشات مكتشفةٌ في هذه الجلسة

1. **تحقّق من ENUM قبل كتابة أي استعلام مؤشر** — افترضتُ `state='ready'` (الصواب `approved`) وحالاتِ الطلبات خطأً. `SHOW COLUMNS` أولًا دائمًا.
2. **شاشةٌ غيرُ مسجَّلةٍ في `modules` = مفتوحةٌ للجميع** — الحارس المركزي يفترض السماح. كلُّ لوحةٍ/شاشةٍ جديدةٍ تُسجَّل بمنحٍ صريحة.
3. **البوابةُ تفشل صامتةً في CLI بلا جلسة** — أوهمني سكربتٌ بفقدان روابط. القياسُ الصحيح **تصييرٌ فعليٌّ بجلسة HTTP**، لا محاكاةُ دوال.
4. **الجلسةُ لا تحمل `parent_role_id`** — يُجلب عند الحاجة للوراثة.
5. **`scopedQuery`**: الإعلانُ عبر `scope`/`enrich` فقط (لا مفتاح `subquery`)؛ والمراجعُ العالمية (`roles`·`modules`·`role_permissions`) تُستعلم بـ`$conn` مباشرةً.
6. **لا تُعدَّل ترحيلاتٌ مطبَّقة** — الـchecksum يوقف `up`؛ التصحيحُ بترحيلٍ جديد.
7. **`bind_param`**: عُدَّ الأنواعَ بدقة (10 وسائط = `isiisssiss`).
8. **الملفات العربية لا تُكتب بـPowerShell** (فسادُ ترميزٍ موثَّق).

---

## 6-جديد · §9-③ ما نُفّذ منه وقراراتُ المالك (جلسة لاحقة 2026-07-26)

### المنفَّذ (commit `8884363`)
- **`app/Services/Unit/TimesheetEntryService.php`** — §8.3 نصًّا: المدخلات الستة ·
  اشتقاق §5.1 (المشروع/العقد/المورد من صف التشغيل) · عطالة (معدة×تاريخ×وردية)
  خدمية ترجع 200 بالمرجع · 422 بقائمة النقص · آلة §8.2 · الأحداث الأربعة.
- **ترحيل `2026_07_26_unit_approvals_round_no.sql`** — round_no + current_round:
  الإعادة بالرقم نفسه (حسمُ مسألة uq_stage_once المرفوعة من تموز).
- **الكتابة المزدوجة حيّة** — خطاف الحفظ في `Timesheet/timesheet.php` وخطاف
  الاعتماد في `Approvals/hours_approval_handler.php` (إعادةُ تشغيل السلسلة L1→L4
  كاملةً عطالةً)، خلف `EMS_UNIT_DUAL_WRITE=on` fail-closed.
- **برهان**: `tests/timesheet_entry_service_test.php` 50/50 (حالات §8.4 الخمس) +
  دخنة HTTP حية 12/12 من الشاشات الحقيقية + المنصة 477/477.

### قرارات المالك الأربعة (مُلزمة لخطوات ③→⑥)
| # | القرار |
|---|---|
| ١ | الأزواج الأربعة المتصادمة على مفتاح اليوم **تُدمج** في واقعةٍ واحدةٍ عند الترحيل ③ — بمرجعَي الصفّين موثَّقين |
| ٢ | قيد الاعتماد يُحل بـ**رقم الجولة** (نُفّذ) |
| ٣ | الموقع الرائد = **مشروع شركة إليانس للتعدين** (id=4، 32 صفًّا، الأنواع الثلاثة) |
| ٤ | هذه الجلسة تقف عند **② الكتابة المزدوجة** — لا تحويلَ مروحةٍ ولا قطع |

### حقائق مقيسة تصحّح ما تحت (§6 القديم)
- «221 بلا أول اعتماد» غير دقيق: **L1=41 · L2=17 · L3=3 · L4=3 · بلا شيء=180**.
- حارس الطاقة **كان مبنيًّا سلفًا** (CapacityGuard + unit_capacity_flags) — استُدعي ولم يُبنَ.
- المصادر ثلاثة لا اثنان: `fin_unit_records` (10 صفوف بمروحة مستقلة) يُغلق في ⑤.
- المشغّل **لا يُشتق آليًّا** — لا مصدرَ له قبل دورة التوزيع (§2.2)؛ يُمرَّر صريحًا.
- الاعتماد القديم L1-L4 = أدوار المشاريع/الموردين/الأسطول/المشغلين — **كلُّه مرحلةُ
  أطراف**؛ لا مبيعاتَ في المسار القديم فالمرآةُ تقف عند parties_approved صدقًا.

### المنفَّذ لاحقًا في الجلسة نفسها (commits `a8f64e4` · `c333e65`)
- **③ على مجموعةٍ نظيفة**: قرار المالك «كل الداتا تجريبية — 50 صفًّا جديدًا ولا
  تعتمد القديمة» ⇒ `seed_unit_reconcile_50.php` (نافذة 2027-02-01→14 عبر
  الشاشة الحقيقية) + `unit_reconcile_test.php` ستةُ محاور **صفر فرق 20/20**.
- **سياسات المشغّلين (UX-02 §8.2)**: `operator_pay_policies` (المجموعات الخمس)
  + محرّك `OperatorDue` (النموذج يرشّح · الأخصُّ يغلب · الحدود تقصّ · المركّب
  يُتخطّى معلَنًا · لا سياسة = سقوطٌ معلَنٌ لـfin_operator_pay) + شاشة
  `Finance/operator_pay_policies_fin.php` (140) حلّت محل الزر القديم في السايدبار.
- **④ وصل المروحة**: `EMS_UNIT_FANOUT_SOURCE=legal` — الكميات والزمن من المرآة
  (برهان 100 حكم صفر فرق) وemployee_due بالسياسة أولًا؛ **أثرُ المشغّل فُعِّل**
  بترحيلٍ بعد نجاح المطابقة (بأرقامٍ تجريبيةٍ موسومةٍ محصورةِ السريان — تُستبدل
  من الشاشة قبل الاستعمال الحقيقي).
- گوتشات الجلسة: سياسةٌ تجريبيةٌ بلا سريانٍ طالت حزمةَ البوابة (سُيّجت
  بالنافذة) · softDelete يتطلب الثلاثي is_deleted/deleted_at/deleted_by ·
  config.php يدوس متغيّر `$db` في السكربتات · «المعدة 12» التاريخية كانت
  operations.id لا معدة.

### ⚠️ ساعاتُ المشغّل (ملاحظة المالك بعد التسليم — حاكمة لخطوتَي ③ و④)
`timesheet.operator_hours` تُقرأ اليوم في المروحة (مستحق المشغّل = operator_hours × المعدّل)
و**لا تُمرآة عمدًا**: نصُّ UX-02 §8.2 «سياساتُ المشغّلين الكاملة (4 نماذج × 7 أسس) **بدل
العمودين**» — المستهدفُ اشتقاقُ مستحق المشغّل من سياسة عقده على سطور الزمن، لا من العمود اليدوي.
المقيس: 156/221 صفًّا فيها العمود، **صفّان فقط يختلفان عن executed_hours** (232: 10≠9 · 351: 1≠8)
و17 صفَّ operator_standby. التبعات: ① مطابقةُ الأسبوع في ③ ستُظهر هذه الفروق — تُحكَم لا تُردم
صامتةً؛ ② تحويلُ المروحة في ④ **يشترط محرّكَ سياسات المشغّلين** وإلا فُقد مصدرُ مستحق المشغّل.
والعمودان باقيان في timesheet بلا مساسٍ حتى القطع ⑥.

---

## 6 · التاسك التالي — §9-③ مصدرُ حقيقة الوحدات · **بمواصفة UX-03 §8**

> **الجديد الحاسم:** كان هذا التاسكُ سيُبنى باجتهاد؛ ووصولُ **UX-03** جعله **مواصفةً كاملة**. لا تجتهد فيما نصّت عليه.

### الوضع المقيس
```
unit_entries    = 0 صف
unit_time_log   = 0 صف      ← أُنشئت بترحيل 2026_07_18 وبقيت بلا كاتب
unit_approvals  = 0 صف
timesheet       = المصدر الحيّ الفعلي (221 صفًّا بلا أول اعتماد)
```

### الحكم النافذ (UX-03 §8.1)
> «جداولُ سجل الوحدات القانوني القائمة (الوحدات · تفصيلُ الزمن · الاعتمادات · أحكامُ الأطراف) **تصير مصدرَ الكتابة الوحيد لوحدات اليوم عبر خدمة الإدخال** — وشاشاتُ الساعات القديمة Views وRedirects.»
> و«محرّكُ تحويل الوحدات القائم **يُعاد استخدامه كما هو** — يُستدعى آليًّا لحظةَ اكتمال السلسلة ليولّد آثارَ FES.»

### ما تعطيه UX-03 جاهزًا — اقرأه قبل أي سطر
| البند | الموضع |
|---|---|
| **آلة حالات الوحدة** كاملةً بمالكِ كل انتقالٍ وشرطِه وأثره: `Draft → Submitted → SiteApproved → PartiesApproved → SalesApproved → ChainCompleted` (+ `ReturnedToSite` بسببٍ **وبالرقم نفسه**) | §8.2 |
| **مفتاح العطالة**: (المعدة × التاريخ × الوردية) — يحلّ تكرارَ المزامنة دون اتصال | §8.2 |
| **`TimesheetEntryService`**: المدخلاتُ الستة · `UnitDTO` · التحقق (422 نقص · 200 لمفتاحٍ قائم · تجاوزُ الطاقة يُحفظ معلَّمًا ويُمنع اعتمادُ الموقع قبل السبب) · الأحداثُ الأربعة | §8.3 |
| **عقدُ الـAPI**: `POST /units` و`POST /units/(id)/approve?stage=site\|parties\|sales` بأخطائه (422 · 409 · 423) | §8.3 |
| **خمسُ حالات اختبار** بقالب FES §10.8 (T1 يوم نظيف · T2 المزامنة · T3 حارس الطاقة · T4 الاكتمال · T5 الإعادة) | §8.4 |
| **منهجُ الترحيل**: تعبئةُ السجل القانوني من مصدر الساعات القديم **موقعًا رائدًا** بمطابقة أسبوعٍ صفرَ فرق، **وعلمُ رجوعٍ لكل موقع** | §8.4 ذيلًا |
| **خريطةُ اشتقاق التايم شيت** (ما يستنتجه النظام / الافتراضي / الإلزامي وحده) + عقدُ الشاشة بحالاته الخمس + **ستةُ سيناريوهاتٍ ميدانية** (نسيَ معدة · تغيّرت المعدة · تغيّر المشغّل · اليوم مقفل · تعديلٌ بعد الاعتماد · انقطاعٌ ودمج) | §5.1 |
| **الحقول الثلاثة منفصلةٌ أبدًا** — نصٌّ صريحٌ في طبقة قاعدة البيانات | §5.1 |

### المنهج المقترح (على نمط ما نجح اليوم)
① بناءُ `TimesheetEntryService` بآلة §8.2 وعطالتها ← ② كتابةٌ مزدوجة (`timesheet` يعمل والخدمةُ تسجّل بالتوازي) ← ③ **موقعٌ رائدٌ** بعلَمه ومطابقةُ أسبوعٍ صفرَ فرق ← ④ تحويلُ المروحة للقراءة من السجل القانوني ← ⑤ إغلاقُ `forUnitRecord` اليدوي ← ⑥ الشاشاتُ القديمة Views وRedirects.

### لماذا يبقى الأثقل رغم اكتمال المواصفة
يمسّ **دستور الوحدة D02** (المروحة · حارس الطاقة · بوابة التحويل) و`timesheet` الذي تعتمد عليه المروحةُ المالية وسجلُّ الدوام. و**34 تأكيدًا** في `unit_source_tables_test` تحرس المخطّط لا الاستعمال — ستحتاج توسيعًا.

### ما يفتحه خلفه
**سجلُّ الزمن الموحّد** (يفكّ UX-04 و UX-10 و UX-06) · وحداتُ الأطراف (UX-02 §8.1) · المستخلص (UX-08) · كشوفُ الأطراف الثلاثة.

### اقرأ أولًا
**`docs/update0001/UX-03 التشغيل-5.docx` §5 و§8 كاملين** · `ARCHITECTURE_CURRENT_SYSTEM_v8_ar.md` §10.5 و§16-ج · ذاكرة `timesheet-fanout-source` · `unit-party-awards` · `effect-fanout-engine`.

---

## 6.1 · ~~لوحات التشغيل الثلاث~~ ✅ **أُنجزت فور وصول UX-03** (commit `4fb14eb`)

- **الدور 1** على اللوحة العامة بمكوّنات §6 نصًّا + تنبيهات §8.1 الثلاثة، والمشرفُ (7) يرثه — برهانٌ رباعيٌّ حي (محمد · مشرف · صافولا · حركة-صافولا = 7/7 لكلٍّ).
- **صار المجموع 12 لوحةً تغطي الأدوارَ النشطة كلَّها (23/23 بالوراثة)** — بابُ اللوحات أُغلق.
- **غائبان عمدًا بلا مصدر (لا تلفيق — محروسان بالحارس):**
  - «انحرافُ الالتزام فوق الحد» — ينتظر **محركَ الالتزام** (تبويب غرفة العمليات ④).
  - «تغطيةُ ورديات الغد» — تنتظر **دورةَ التوزيع** (§2.2)؛ بديلُها الصادق اليوم «تعمل بلا مشغّلٍ نشط».
- **وتصديقٌ مطمئن:** سايدباراتُ الأدوار الثلاثة المبذورة أمسِ طابقت UX-03 §4 **في كل بابٍ ورابط** — قبل أن نرى الوثيقة. الفارقان: موضعُ «سجل النشاط» (§4.1 أعلاه) وتسميةُ «وحدات الأطراف» (تأتي مع إعادة بناء الشاشة).

### ما بقي من UX-03 لغير هذه الجلسة (يُلتقط بعد §9-③ أو معه)
| البند | الموضع | ملاحظة |
|---|---|---|
| **غرفة عمليات التشغيل** — الشاشة الأم بتبويباتها الأربعة (الورديات · التايم شيت · الاعتماد · الالتزام) وتستوعب (77) | §5.3 | تبويب ④ «الالتزام» يجرّ محركَ الالتزام |
| **إعادة بناء التايم شيت** بخريطة الاشتقاق وعقد الشاشة والسيناريوهات الستة | §5.1 | هذا **واجهةُ §9-③ نفسِه** — يُبنى فوق `TimesheetEntryService` |
| **صندوق الاعتماد الواحد** (يستوعب 121 ويُلغيها) بحارس الطاقة | §5.2 | |
| **لوحة التوزيع البصرية** (معدة × وردية × مشغّل) بتحذير التعارض | §2.2 · §3 | تفكّ «تغطية الغد» و«توزيعات تنتظر اعتمادي» |
| مصفوفة أسماء §7 (غرفة العمليات · التايم شيت اليومي…) | §7 | تُنفَّذ مع الشاشات لا قبلها |

---

## 7 · المتبقي بعد ذلك

**محجوزٌ بقرار المالك:**
- ~~UX-03 (التشغيل) الغائبة~~ ✅ **وصلت 2026-07-26 مساءً — أكبرُ حاجزٍ ارتفع.** فُكَّ بها: §9-③ (مواصفةً كاملة) · لوحاتُ التشغيل الثلاث · وطريقُ سجلِّ الزمن الموحّد الذي يحجز UX-04 و UX-10 و UX-06.
- دفعُ الالتزامات إلى GitHub (مفتاح SSH بعبارة مرور).
- كلمةُ مرور `admin@enjaz.com` (تحجب شاشةَ إدارة القوائم).
- زرُّ توليد أحداث الصيانة الأربعة (قرارُ مخصَّص الصيانة المالي).
- الستُّ المعطَّلة للدور 1 (تُفعَّل من شاشة الإدارة).

**ديونٌ صغيرة:** تفعيلُ `nav_redirects` · سجلُّ العدّادات المركزي · فحصُ audit دوري · مبدّلُ المساحات وصفحةُ «مساحاتي» (UX-02 §7.1) · لوحاتُ المالية الفرعية الأربع المستقلة.

**أثقالُ الحزمة الباقية:** دفترُ FES · ~~وحداتُ الأطراف وسياساتُ المشغّلين~~ ✅ **أُنجزتا 2026-07-26** (`c333e65` + `fdb57fc`: §8.1 شاشةً بثلاث بطاقات حكمٍ والمصدرُ الثالث أُغلق · §8.2 جدولًا ومحرّكًا وشاشة) · المستخلصُ والفاتورة الضريبية (UX-08) · RFQ والحاويات (UX-05) · المطابقةُ الثلاثية (UX-09).

**⚡ تحديثُ ⑤ و⑥ (بعد قياس 2026-07-26 مساءً):**
- **⑤ لم تكن «إغلاق شاشة» بل «إعادة بناءها»** (UX-02 §8.1 نصًّا) — نُفّذت: الإدخال اليدوي
  زال، العشرةُ قسمٌ تاريخيٌّ مجمّدٌ بمالها (24 رابطًا لا تُمسّ)، والصفان غير المعتمدَين شاهدان.
- **⑥ بقرار المالك**: «بعد بناء الشاشات الجديدة نأتي للسؤال» — قلبُ شاشات الساعات
  القديمة Views/Redirects مؤجَّلٌ حتى تُبنى غرفةُ العمليات والتايم شيت الجديد (§5).
- **قرار المالك الثالث**: «استخدم أرقامًا جديدة دائمًا في حساب المستحقات» — السياساتُ
  هي المصدر، والأرقامُ الحقيقية تُدخل من شاشة السياسات (140) متى شاء.

---

## 8 · حسابات الاختبار

`12345678` لكل الحسابات: `مديرمالي` (17) · `صيانة` (13) · `مشتريات` (16) · `transport_mgr` (23) · `بلاغات` (24) · `مبيعات` (12) · `مصعب` (2) · `يسن` (3) · `اروينا` (4) · `صافولا` (5) · `حركة-صافولا` (6) · `حسابات` (15) · `محمد` (1).

**الأعلام الحية:** `EMS_NAV_UNIFIED_ROLES` (23 دورًا) · `EMS_ROLE_BOARD_ROLES` (22 دورًا) — الرجوعُ بحذف رقمٍ من أيهما.
