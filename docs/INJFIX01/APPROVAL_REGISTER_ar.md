# سجلُّ الاعتماد — INJ-FIX-01

> هذا سجلُّ اعتمادٍ لا وثيقةَ برنامج. لا يُعاد فيه صوغُ البرنامجِ ولا تُفتح به مراجعةٌ
> وثائقيةٌ جديدة. وظيفتُه واحدة: أن يُثبِت **ماذا اعتُمد وبأيِّ بصمةٍ ومتى**.

---

## ① قيدُ الاعتماد

| البند | القيمة |
|---|---|
| الحزمة | `INJ-FIX-01 v1.1` |
| نوعُ الاعتماد | اعتمادُ **محتوًى** — يجعل الحزمةَ المرجعَ التنفيذيَّ الحاكم |
| الجهةُ المعتمِدة | الرئيس التنفيذي |
| تاريخُ الاعتماد | 2026-08-21 |
| مستندُ الاعتماد | نصُّ المالكِ «اعتماد حزمة الإصلاح وبدء التنفيذ حتى الإغلاق» |
| الحالةُ قبل | `DRAFT — pending approval` |
| الحالةُ بعد | `APPROVED — executing` |
| ما نُسِخ من الاعتماد | تفويضٌ بالتنفيذِ داخلَ حدودِ INJ-FIX-01 v1.1 |

## ② الحزمةُ المعتمَدة — وقد تحقَّقت بصمتُها قبلَ القيد

المالكُ سمّى في نصِّ اعتمادِه سبعةَ ثوابت. وقد حُسبت من **الملفَّين المستلمَين نفسِهما**
لا نُقلت من غلافٍ إلى تقرير:

| المفتاح | ما اعتمدَه المالك | المقيسُ من الملفّ | الحكم |
|---|---|---|---|
| `MANIFEST_VERSION` | `7` | `7` (من `injaz_payload/MANIFEST.json`) | ✔ |
| `FIX_SPEC_VERSION` | `1.1` | `1.1` | ✔ |
| `BASELINE` | `BL-20260821-f0bc3e4e` | `HEAD = f0bc3e4e` وقتَ القيد | ✔ |
| `SPEC_HASH` | `58ee9aedd0939b4c` | `sha256(البرنامج.docx)[0:16] = 58ee9aedd0939b4c` | ✔ |
| `WORKBOOK_HASH` | `9289f83353807464` | `sha256(الدفتر.xlsx)[0:16] = 9289f83353807464` | ✔ |
| `GAP_COUNT` | `33` | `33` صفًّا في ورقةِ بطاقاتِ الفجوات | ✔ |
| `PATH_COUNT` | `16` | `Σ(مساراتٌ حاليةٌ في المجال) = 0+7+2+3+2+2 = 16` | ✔ |
| `DOMAIN_COUNT` | `6` | `6` صفوفٍ في مصفوفةِ التوحيد | ✔ |
| `COMPLETION_CRITERIA` | `16` | `16` شرطًا في الفصلِ ١٥ | ✔ |

**أمرُ إعادةِ الإنتاج:**

```
sha256sum "INJ-FIX-01 — برنامج الإصلاح والتوحيد-3.docx" "INJ-FIX-01 — دفتر التنفيذ والتوحيد-3.xlsx"
unzip -p "DELTA-FIX-01 — حاوية حزمة الإصلاح.docx" injaz_payload/MANIFEST.json
```

## ③ مصالحةُ البصماتِ — السابقةُ واللاحقة

| البصمة | قبلَ قيدِ الاعتماد | بعدَه | تغيّرت؟ |
|---|---|---|---|
| `SPEC_HASH` | `58ee9aedd0939b4c` | `58ee9aedd0939b4c` | **لا** |
| `WORKBOOK_HASH` | `9289f83353807464` | `9289f83353807464` | **لا** |
| `MANIFEST_VERSION` | `7` | `8` | **نعم** |
| `STATUS` | `DRAFT — pending approval` | `APPROVED — executing` | **نعم** |

**التفسير:** المالكُ أمر أن يُسجَّل الاعتمادُ في سجلِّ الاعتمادِ **فقط**. فلم يُمسَّ متنُ
البرنامجِ ولا متنُ الدفتر، وبصمتاهما باقيتان بحرفِهما — وهما البصمتان اللتان يُحكم بهما
على هويةِ الحزمة. والذي تغيّر هو **بيانُ المحتوى** نفسُه: رقمُه وحالتُه. ولهذا صدر
`MANIFEST v8` حاملًا البصمتَين **دون تغيير**، فيُعرف بالنظرِ أن الحزمةَ هي هي وأن
المتغيِّرَ حالتُها الحوكميةُ لا محتواها.

> ◆ ولا يُقرأ ارتفاعُ `MANIFEST_VERSION` إصدارًا جديدًا للحزمة. الحزمةُ واحدة،
> وبيانُ محتواها هو الذي تُرقَّم نسخُه.

- بيانُ المحتوى الجديد: [`MANIFEST.v8.json`](MANIFEST.v8.json)
- والحاوياتُ السابقةُ كلُّها `HISTORICAL_INPUT` ولا يُحكم بها على الحالةِ الحالية.

## ④ ترتيبُ القراءةِ الملزم

1. **برنامجُ الإصلاحِ والتوحيد** — الحكمُ والمعماريةُ وعقودُ التنفيذ.
2. **دفترُ التنفيذِ والتوحيد** — المصدرُ التنفيذيُّ الذي يُملأ ويُقاس منه.
3. **DELTA-FIX-01** — بيانُ المحتوى والبصمات.

## ⑤ ما لم يُفعل بسببِ الاعتماد

- لم يُعَد صوغُ البرنامج.
- لم تُفتح مراجعةٌ وثائقيةٌ جديدة.
- لم تُنشأ وثيقةٌ موازية — والدفترُ هو الدفتر، والسجلُّ هو السجل.
- لم تُعدَّل بطاقةُ فجوةٍ واحدةٍ ولا شرطُ قبولٍ واحد.

## ⑥ التوقيع

| الإجراء | الجهة | الاسمُ والصفة | التوقيعُ والتاريخ |
|---|---|---|---|
| اعتماد | الرئيس التنفيذي | — بنصِّ رسالةِ الاعتماد — | 2026-08-21 |
| قيدٌ في السجل | مكتب هندسة النظم المؤسسية | المنفِّذ | 2026-08-21 |
