# GAP-07 — تشخيصُ مستهلكِ أسعارِ الصرف

> **الحالة:** `DIAGNOSED` — لم تُغلق. والتشخيصُ يقلب فرضيةَ البطاقةِ فيُسجَّل قبلَ أيِّ علاج.
> **البناء:** `5babc4e3` · **بصمةُ المخطط:** `a1d32352d3bc171a` (603 جدولًا · 10092 عمودًا)
> **الأداة:** استعلاماتٌ حيةٌ + مسحُ شجرة · **التاريخ:** 2026-08-21

---

## ① ما تقوله البطاقة

| البند | النص |
|---|---|
| السببُ الجذريُّ المسجَّل | «مستهلكُ الصرفِ متوقفٌ تسعةَ أيامٍ بلا إنذارٍ يغطيه» |
| الدليلُ في الأساس | `F-12`: مؤشر `fx = 6,613/17,985` متوقف منذ 2026-08-12 (متأخر 11,372) |
| معيارُ القبول | المؤشرُ يلحق · إنذارٌ مجرَّبٌ بإيقافٍ مقصود · أثرٌ ماليٌّ مُقاس |

## ② ما أثبتَه القياس — والفرضيةُ مقلوبة

**لا يوجد مستهلكُ صرفٍ أصلًا. الصفُّ يتيمٌ بلا معالج.**

`EventDispatcher::runOnce()` يدور على `$this->handlers` وحدَها — وهي ما سُجِّل بـ`register()`.
ومسحُ الشجرةِ كلِّها يُخرج **نداءَ تسجيلٍ واحدًا في ملفٍّ واحد**:

```
grep -rn "register\s*(\s*['\"]" --include=*.php .   ⇐  cron_events.php:125  فقط
```

و`cron_events.php` يسجّل **مستهلكَين لا غير**:

| # | الاسمُ المسجَّل | الموضع |
|---|---|---|
| ١ | `finance` | `cron_events.php:125` |
| ٢ | `finance_routing` | `cron_events.php:133` (`RoutingConsumer::NAME`) |

بينما `ems_event_consumers` يحمل **أربعةَ صفوف**:

| المستهلك | المؤشر | آخرُ تحديث | له معالجٌ مسجَّل؟ |
|---|---|---|---|
| `finance` | 17985 | 2026-08-16 | ✔ نعم |
| `finance_routing` | 17985 | 2026-08-16 | ✔ نعم |
| `finance_routing_replay` | 17980 | 2026-08-15 | ✘ **لا — يتيم** |
| `fx` | 6613 | 2026-08-12 | ✘ **لا — يتيم** |

ولا يَرِد الاسمان `fx` ولا `finance_routing_replay` في الشجرةِ كلِّها — لا في كودٍ ولا هجرةٍ
ولا بذرة. فهما صفّان أُنشئا مرةً ولم يُسجَّل لهما معالجٌ في هذه الشيفرة.

> ◆ **فليس «توقفًا» بل غيابَ معالج.** والمؤشرُ لا «يتأخر» — لا شيءَ يحرّكه.
> والوصفُ «متوقف منذ 2026-08-12» يصف تاريخَ آخرِ لمسةٍ للصفِّ لا تاريخَ عطبٍ وقع.

## ③ الأثرُ الماليُّ للتأخير — مقيسٌ صفرًا

تحويلُ العملةِ في هذا النظامِ **متزامنٌ في الخدماتِ لا بحدث**: الأساسُ
`base = amount × rate_to_base` يُحسب لحظةَ العملية من `fin_fx_rates`. فلا أثرَ ماليَّ
نتج عن «تأخُّرِ» المستهلكِ اليتيم، لأنه لم يكن في مسارِ أيِّ تسعير.

**ولا يُقرأ هذا تبرئةً للصرفِ كلِّه** — فـ`fin_fx_rates` أربعةُ صفوفٍ فقط
(عملتان: `USD` أساسًا لشركتَين · `SDG` بسعرَين لشركةٍ واحدة)، وهذه **GAP-25** بعينِها
ولا تُخلط بـGAP-07.

## ④ دليلٌ جديدٌ ظهر أثناءَ القياس — يدخل بمسارِ استقبالِ الدليل

**⒜ فضاءان منفصلان لأسماءِ المستهلكين.** `ems_event_deliveries` يحمل أسماءً
لا وجودَ لها في `ems_event_consumers` ولا في `register()`:
`governance_watch` (21322 صفًّا) · `effectfanout` (25) · `balancecalculator` (1).

**⒝ نصٌّ بشريٌّ في حقلِ مفتاح.** ثمانيةُ «مستهلكين» في `ems_event_deliveries` أسماؤهم
جملٌ عربيةٌ من بذرِ UAT — مثل `أُثبت من واقع التشغيل الميداني · UAT-2026`
و`بانتظار اعتماد الإدارة المختصة · UAT-2026`. وهذه صورةُ **GAP-09** في جدولٍ لم تُسمَّه
بطاقتُها (بذورٌ منزاحةٌ إلى حقلِ مفتاح)، فتُضاف إلى نطاقِها لا تُفتح لها بطاقةٌ جديدة.

**⒞ الإنذارُ لا يغطي التعثُّر أصلًا.** `alertDeadLetter()` لا يُطلق إلا عند العزلِ في
`ems_event_dead_letter` — وهو **فارغ**. فلا إنذارَ لتأخُّرِ مؤشرٍ ولا لغيابِ معالج،
وهو ما يفسّر مرورَ صفَّين يتيمَين بلا أن ينتبه أحد.

## ⑤ ما يترتّب على التشخيص

معيارُ القبولِ يبقى كما هو — لكنَّ طريقَ استيفائِه تغيَّر:

| بندُ المالك | ما كان مفترَضًا | ما يستوفيه القياس |
|---|---|---|
| عددُ المتأخرِ قبل | متأخرٌ يُعالَج | لا طابورَ حقيقيًّا — الصفُّ يتيم |
| عددُ المعالَج | يُعالَج بأمان | **صفر** — ولا معالجَ يُعالِج به |
| المتبقي | صفر | صفر بعدَ **حكمٍ** على الصفِّ اليتيم |
| الأثرُ الماليّ | يُقاس | **صفر** — التحويلُ متزامنٌ لا بحدث |
| دليلُ الإنذار | إنذارٌ مجرَّب | يُبنى ويُثبَت بإيقافٍ مقصود |

**والحكمُ على الصفَّين اليتيمَين قرارٌ لا حذف:** إمّا معالجٌ حقيقيٌّ يُسجَّل، وإمّا
تقاعدٌ رسميٌّ معلَن. وهذا نفسُ حكمِ **GAP-31** («صفرُ مسارٍ بلا حكم») و**GAP-05**
(«صفرُ نوعِ حدثٍ بلا حكم») — فيُحسم في مصفوفةِ التوحيدِ لمجالِ الأحداث، لا بحذفٍ
صامتٍ في الموجةِ أ.

## ⑥ تصحيحُ المقام — «١١٬٣٧٢» فرقُ معرِّفاتٍ لا عددُ وقائع

الأساسُ `F-12` يقول «متأخر 11,372»، وهو `17985 − 6613` — **طرحُ معرِّفَين**.
والمقامُ الذي يقرأ منه `runConsumer` هو `fin_financial_events` بشرطِ
`event_key IS NOT NULL AND NOT is_deleted`، والمعرِّفاتُ فيه **متفرقة**:
٥٬٢٤٦ صفًّا فقط تحت معرِّفٍ أقصاه ١٧٬٩٨٥.

| المقياس | القيمة |
|---|---|
| فرقُ المعرِّفات (المكتوبُ في الأساس) | ١١٬٣٧٢ |
| **عددُ الوقائعِ غيرِ المستهلَكةِ فعلًا** | **٥٬٠٥٤** |
| وقائعُ العقدِ كلُّها | ٥٬٢٤٦ |

وقياسٌ ثالثٌ أخطأتُه أنا أولًا حين عددتُ على `ems_business_events` (٢٠٬٧٤٠) —
وهو **جدولٌ آخر** لا يقرأ منه الموزّعُ أصلًا. والثلاثةُ أرقامٌ لثلاثةِ مقامات،
ولا يُنقل أحدُها مكانَ الآخر.

| المستهلك | المؤشر | التأخُّرُ الحقيقيّ | له معالج؟ |
|---|---|---|---|
| `finance` | 17985 | **0** | ✔ |
| `finance_routing` | 17985 | **0** | ✔ |
| `finance_routing_replay` | 17980 | 1 | ✘ يتيم |
| `fx` | 6613 | **5054** | ✘ يتيم |

## ⑦ العلاج المُنفَّذ — إنذارُ التعثُّرِ وإثباتُه بإيقافٍ مقصود

**لم تُنشأ أداةٌ جديدة.** النظامُ يحمل سلفًا نمطَ إنذارِ تعثُّرٍ للمهامِّ المجدولة
(`JobScheduleService::alertStalled()` بوسمِ `[JOB-STALL:…]` وقناةِ `fin_notifications`
ودَورةِ إنذارٍ واحدةٍ في الساعة). فامتُدَّ النمطُ نفسُه إلى المستهلكين بدل اختراعِ
قناةٍ ثانيةٍ لقارئٍ ثانٍ:

| ما أُضيف | الموضع |
|---|---|
| `EventDispatcher::CONSUMER_STALL_SECONDS` | مهلةٌ معلَنةٌ = 3600 ثانية |
| `EventDispatcher::stalledConsumers()` | كشفٌ — قراءةٌ محضة |
| `EventDispatcher::alertStalledConsumers()` | إطلاقٌ بوسمِ `[BUS-STALL:…]` ودَورةِ ساعة |
| نداءٌ **بعد** `runOnce()` | `cron_events.php` |

وحالتان لا واحدة: **يتيم** (مُفعَّلٌ بلا معالج — لا يُقاس بالزمن) و**متعثِّر**
(له معالجٌ ومؤشرُه متأخرٌ ولم يتقدّم منذ المهلة). والخلطُ بينهما يُخفي الأخطر.

### الشاهد — `tests/injfix01_bus_stall_alarm_proof.php`

التوقفُ المقصودُ يقع على **مستهلكٍ اصطناعيٍّ** يُكنس بعدَه، لا على مستهلكٍ حيّ:
فإثباتُ الإنذارِ لا يجوز أن يكون هو نفسُه العطبَ الذي يُنذَر عنه.

```bash
php tests/injfix01_bus_stall_alarm_proof.php     # المتوقَّع: 11/11 · رمزُ خروج 0
```

| # | ما يُثبَت | المتوقَّع | الفعليّ |
|---|---|---|---|
| ١ | مستهلكٌ موقوفٌ عمدًا يُكشف | يُكشف | ✔ |
| ٢ | يُصنَّف «متعثرًا» لا «يتيمًا» | `stalled` | ✔ |
| ٣ | **السكوتُ عن السليم** | لا إنذارَ لـ`finance`/`finance_routing` | ✔ |
| ٤ | الإنذارُ يُطلق | ≥١ | ✔ (٣) |
| ٥ | في قناةِ الإشعاراتِ نفسِها | `fin_notifications` | ✔ |
| ٦ | يقود إلى مؤشرِ الناقل | `admin/bus_monitor.php` | ✔ |
| ٧ | يحمل مقدارَ التأخُّر | نعم | ✔ |
| ٨ | نداءٌ ثانٍ لا يُغرِق | الإجمالي يبقى ١ | ✔ |
| ٩ | اليتيمُ الحيُّ `fx` يُكشف | يُكشف | ✔ |
| ١٠ | صفرُ أثرٍ على المستهلكين الحقيقيين | الحالةُ كما كانت حرفًا | ✔ |

**والعطبُ الذي مرَّ تسعةَ أيامٍ صار مرئيًّا الآن** — إنذاران حيّان في
`fin_notifications` (`#8985` و`#8986`) يسمّيان اليتيمَين ومقدارَ تأخُّرِهما.

## ⑧ ما بقي مفتوحًا — ولا يُغلق في هذه الموجة

معيارُ «المؤشرُ يلحق» **لا معنًى له لصفٍّ بلا معالج**: لن يلحق مهما مضى الزمن.
فالحكمُ على الصفَّين اليتيمَين — معالجٌ حقيقيٌّ أم تقاعدٌ معلَن — **قرارُ مجالِ
الأحداث** في مصفوفةِ التوحيد (GAP-31: «صفرُ مسارٍ بلا حكم»)، والمجالُ في الموجةِ ج.

ولا يُطفأ الإنذارُ ولا يُعطَّل الصفَّان لإسكاتِه: الإنذارُ يقول الحقيقةَ، وإسكاتُه
قبلَ الحكمِ إخفاءٌ للمرضِ في رقم.

| البند | الحالة |
|---|---|
| تشخيصُ سببِ التوقف | ✔ `EVIDENCE_CLOSED` — غيابُ معالجٍ لا توقُّف |
| الأثرُ الماليُّ للتأخير | ✔ `EVIDENCE_CLOSED` — صفرٌ مقيسًا |
| إنذارُ توقفٍ مجرَّبٌ بإيقافٍ مقصود | ✔ `EVIDENCE_CLOSED` — ١١/١١ |
| «المؤشرُ يلحق» / حكمُ اليتيمَين | ◐ مُحال إلى مجالِ الأحداث — الموجة ج |

## ⑨ أوامرُ إعادةِ الإنتاج

```bash
php -r 'require "config.php"; $q=mysqli_query($conn,"SELECT * FROM ems_event_consumers"); while($x=mysqli_fetch_assoc($q)) echo json_encode($x,JSON_UNESCAPED_UNICODE),"\n";'
grep -rn "register\s*(\s*['\"]" --include=*.php . | grep -v storage/backups
php -r 'require "config.php"; $q=mysqli_query($conn,"SELECT consumer,COUNT(*) c FROM ems_event_deliveries GROUP BY consumer"); while($x=mysqli_fetch_assoc($q)) echo $x["consumer"],"=",$x["c"],"\n";'
```

| ما يُثبَت | النتيجةُ المتوقَّعة | النتيجةُ الفعلية | الحكم |
|---|---|---|---|
| عددُ صفوفِ `ems_event_consumers` | ٤ | ٤ | ✔ |
| عددُ نداءاتِ `register()` في الشجرة | ٢ | ٢ (`cron_events.php` وحدَه) | ✔ |
| `fx` له معالجٌ مسجَّل | — | **لا** | ✔ مُثبَت |
| `ems_event_dead_letter` | — | فارغ | ✔ |
