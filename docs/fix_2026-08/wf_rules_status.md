# سلاليمُ الاعتماد — قياسٌ حيّ · 2026-08-10 19:26

```
══════════════════════════════════════════════════════════════════════
 سلاليمُ الاعتماد — قياسٌ حيّ · 2026-08-10 19:26
══════════════════════════════════════════════════════════════════════

── ① جدولُ القواعد
  صفوفٌ **نشطة**: 3 · منها قواعدُ برمزٍ حقيقيّ: **3** · بنصِّ ملاحظةٍ في خانةِ الرمز: **0**
  ومعطَّلٌ باقٍ (مُعلَنٌ لا محذوف): 20 · منه نصوصُ ملاحظاتٍ: 20
  أزواجٌ (كيان:فعل) لها قواعدُ حاكمة: 1
    · scr_deductions:approve          3 خطوة: 1→4 · 2→1 · 3→19
  ✔ ولا نصَّ ملاحظةٍ نشطًا — والعشرون معطَّلةٌ ببصمةٍ تُعلن السبب:
      «وفق المعتمد في محضر الإدارة · UAT-2026-0001 [معطَّل: نصُّ ملاحظةٍ في خانةِ»

── ② الأنواعُ الحيةُ في `approval_requests`
  timesheet:approve             طلبات=12   معتمد=0    معلق=12   مرفوض=0   ✘ **بلا قواعد ⇒ احتياطُ خطوةٍ واحدة**
  contract:renewal              طلبات=2    معتمد=0    معلق=2    مرفوض=0   ✘ **بلا قواعد ⇒ احتياطُ خطوةٍ واحدة**
  project:update                طلبات=2    معتمد=0    معلق=2    مرفوض=0   ✘ **بلا قواعد ⇒ احتياطُ خطوةٍ واحدة**
  scr_deductions:approve        طلبات=1    معتمد=1    معلق=0    مرفوض=0   ✔ له قواعدُ حاكمة (3 خطوة)
  ◆ وطلباتٌ نوعُها **نصُّ ملاحظةٍ** لا رمزُ كيان: 4 — سجلاتُ وقائعَ ملوَّثةٌ (لها تواريخُ وحمولات)، **تُعلَن ولا تُكتب من جديد**؛ فتعديلُ سجلِّ واقعةٍ تلفيقٌ لا تنظيف.
  ◆ أزواجٌ **حقيقيةٌ** تنتظر قرارَ سياسةٍ («كم يدًا؟»): 3
      · timesheet:approve             12 طلبًا
      · contract:renewal              2 طلبًا
      · project:update                2 طلبًا

── ③ كم يدًا مشت كلَّ سلّمٍ مكتمل؟
  طلباتٌ إجمالًا: 21
  منها **مكتملٌ بيدٍ واحدةٍ**: 2 · بيدين أو أكثر: 1
  وطلباتٌ **اعتمدها طالبُها نفسُه** (ولو خطوةً): **3**
    #17  وفق المعتمد في محضر الإدارة · UAT-2026:وفق المعتمد في محضر الإدارة · UAT-2026خطوات=0 أيدٍ=0 طالبُه=4
    #20  مراجَعٌ من المالية ومطابق · UAT-2026:مراجَعٌ من المالية ومطابق · UAT-2026خطوات=0 أيدٍ=0 طالبُه=4
    #22  scr_deductions:approve        خطوات=3 أيدٍ=3 طالبُه=7  ◄ اعتمده طالبُه

── ④ الاحتياطُ المكتوبُ في `includes/approval_workflow.php`
  أزواجٌ في الاحتياطِ المسمّى: 5
    · equipment:deactivate_equipment    أدوار=4,-1     → **خطوةٌ واحدةٌ** بأيٍّ من 2 دور
    · equipment:reactivate_equipment    أدوار=4,-1     → **خطوةٌ واحدةٌ** بأيٍّ من 2 دور
    · driver:activate_driver            أدوار=3,-1     → **خطوةٌ واحدةٌ** بأيٍّ من 2 دور
    · driver:deactivate_driver          أدوار=3,-1     → **خطوةٌ واحدةٌ** بأيٍّ من 2 دور
    · driver:reactivate_driver          أدوار=3,-1     → **خطوةٌ واحدةٌ** بأيٍّ من 2 دور
  والاحتياطُ الأخيرُ (لا قاعدةَ ولا زوجَ مسمّى): **خطوةٌ واحدةٌ للسوبر** — والسوبرُ يطابق أيَّ خطوةٍ بحكمِ المحرّك
  واعتمادُ الخطوةِ الأولى تلقائيًّا لمنشئِ الطلب: **قائم**
  وقاعدةُ «لا يدَ تمشي خطوتين»: ✔ قائمة

── ⑤ الثقبُ البنيويُّ (صدقٌ خلوًّا)
  «أكلُّ الخطواتِ معتمدة؟» ترفض صفرَ خطوة: ✔ نعم
  وطلباتٌ بلا أيِّ خطوةٍ في القاعدة: 4 — منها موسومٌ `approved`: 2 ◄ **سابقةٌ للإصلاح** (حمولتُها نُفِّذت) — تُعلَن ولا تُكتب من جديد
  وسطورُ دَينِ «سلّمٌ بلا قاعدة» المسجَّلة: 0 (وضعُ العلم: monitor)

══════════════════════════════════════════════════════════════════════
الحكم: ✔ صفرُ قاعدةٍ حاكمةٍ بنصِّ ملاحظةٍ · والثقبُ الخلويُّ مغلق
  ◆ ودَينٌ **معلَنٌ بأرقامِه لا مُغلَقٌ**: 3 زوجًا حقيقيًّا بلا قواعد ⇒ سلّمُه
    **خطوةٌ واحدة**، ومنشئُ الطلبِ قد يعتمدها. والعلمُ `EMS_APPROVAL_RULES`
    يسجّل كلَّ استعمالٍ للاحتياطِ في `guard_denials`؛ وقلبُه إلى `enforce`
    **قرارُ مالكٍ** يسبقه تسجيلُ قواعدَ لهذه الأزواج — «كم يدًا لكلٍّ؟».
══════════════════════════════════════════════════════════════════════
```
