# جردُ الشاشات — مقامٌ واحد

══════════════════════════════════════════════════════════════════════
 جردُ الشاشات: مقامٌ واحدٌ — 2026-08-11 09:19
══════════════════════════════════════════════════════════════════════

| المجموعة | العدد | ما هي |
|---|---:|---|
| **أسطحٌ حيةٌ** (تُضمِّن `insidebar.php`) | **356** | ما يُصيَّر شاشةً بقشرةٍ وقائمة |
| **صفوفُ `modules`** | **414** | سجلُّ الصلاحياتِ — يشمل معالجاتٍ لا شاشاتٍ فقط |

| الفرق | العدد | معناه |
|---|---:|---|
| ① **سطحٌ حيٌّ بلا صفٍّ في السجل** | **0** | حارسُ الشاشةِ بلا مرجع ⇒ يُغلق فيُفقد أو **يُفتح بلا صلاحية** |
| ② **صفٌّ بلا ملفٍّ على القرص** | **0** | منحةٌ لبابٍ لا يوجد |
| ③ صفٌّ لملفٍّ قائمٍ ليس شاشةً | 58 | معالجاتٌ وAJAX — **مسجَّلةٌ عمدًا** لحارسِ الأفعال (لا عطل) |

══════════════════════════════════════════════════════════════════════
الحكم: ✔ صفرُ فرقٍ في الاتجاهين — المقامُ واحدٌ ومُعلَن

◆ **المقامُ المُعلَنُ لنسبِ الشاشات: 356** (أسطحٌ حيةٌ تُضمِّن القشرة).
  ولا يُستعمل `modules` (414) مقامًا لنسبةِ شاشاتٍ — فهو سجلُّ
  صلاحياتٍ يشمل 58 معالجًا ليس شاشةً.
══════════════════════════════════════════════════════════════════════
