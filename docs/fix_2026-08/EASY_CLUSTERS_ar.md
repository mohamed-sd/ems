# البنودُ السهلةُ بعناقيدها

══════════════════════════════════════════════════════════════════════
 البنودُ «السهلة» بعناقيدها — 2026-08-11 09:34
══════════════════════════════════════════════════════════════════════

المفتوحُ السهلُ: **110** بندًا

| نوعُ الفجوة | عدد | شاهدٌ مرشَّح |
|---|---:|---|
| Missing Evidence | **42** | مِسبارٌ يقرأ اختبارَ القبولِ ويُثبته على النظامِ الحيّ |
| Wrong Sidebar Placement | **36** | `fix_nav_href_probe` + مِسبارُ موضعٍ جديدٌ يقابل الموضعَ المعتمَدَ بالحيّ |
| Broken Button | **19** | تصييرٌ حيٌّ + فحصُ وجودِ معالجِ الفعلِ في `actions` |
| Wrong Label | **13** | `fix_nav_label_probe` (قائمٌ · يُبلّغ 27 انفصالًا) |

╔══ ② العناقيدُ — بنودٌ تتشارك الشاشةَ أو الإدارةَ فتُغلق دفعةً

### Missing Evidence (42)
  (لا عنقودَ شاشةٍ — كلُّ بندٍ شاشتُه)
  بالإدارات: إدارة الحوكمة والالتزام=6 · المشتريات التشغيلية=5 · النقل والترحيل=4 · إدارة الصيانة=3

### Wrong Sidebar Placement (36)
  عناقيدُ شاشةٍ واحدة:
    · **4** بندًا في «مساحة عملي» — INJ-0125، INJ-0232، INJ-0314، INJ-0405
  بالإدارات: النقل والترحيل=4 · المشتريات التشغيلية=4 · مساحة عملي=4 · إدارة الحوكمة والالتزام=3

### Broken Button (19)
  عناقيدُ شاشةٍ واحدة:
    · **2** بندًا في «سجل المخاطر المركزي» — INJ-0386، INJ-0390
    · **2** بندًا في «طلب العروض ومقارنتها (الترسية)» — INJ-0332، INJ-0333
    · **2** بندًا في «الوصول والتسليم» — INJ-0085، INJ-0308
  بالإدارات: إدارة المخاطر=5 · النقل والترحيل=3 · المخازن=3 · المشتريات التشغيلية=2

### Wrong Label (13)
  (لا عنقودَ شاشةٍ — كلُّ بندٍ شاشتُه)
  بالإدارات: المبيعات والعقود=3 · مركز البلاغات=2 · مساحة عملي=2 · مكتب الرئيس التنفيذي والنواب=2

╔══ ③ عيّنةُ نصوصِ الإصلاح (ثلاثةٌ من كلِّ نوع)

### Missing Evidence
- **INJ-0073** · P1 · العودة للخدمة
  - العيب: شهادةُ الجاهزية لا تُحفظ إطلاقًا: القيمةُ تُقرأ في المتغير $cert وتُستعمل في نصِّ رسالةِ النجاح فقط ($msg) ثم تُهمَل. لا عمودَ ولا جدولَ يستقبلها، ولا
  - الإصلاح: إضافةُ أعمدةِ readiness_note وreadiness_by وreadiness_at إلى mnt_order في هجرة، وكتابتُها في نفس معاملة الإقفال في Maintenance/return_to_service.php:26-28، وعرضُ الشهادة 
  - القبول: إقفالُ أمرٍ بشهادةِ جاهزيةٍ يُخزّن نصَّ الشهادة ومُصدِرَها ووقتَها في صفِّ الأمر، وتظهر في بطاقة الأمر وفي أيِّ تصديرٍ للأوامر المقفلة.
- **INJ-0139** · P2 · طلب تبديل / التوقفات / تقويم الأسطول
  - العيب: ثلاثةُ معالجاتٍ في وحدة التشغيل تكتب في القاعدة بلا أيِّ سجلِّ تدقيقٍ على أيِّ مسار: `Operations/swap_request.php` (تغطياتُ إحلالٍ تقود الاستحقاق) و`O
  - الإصلاح: مرّر كتاباتِ الملفات الثلاثة عبر `ems_tenant_db()` واستدعِ `ems_audit_change()` بعد كلِّ كتابةٍ ناجحة، وانشر الحدثَ المقابل (`operations.swap.created` · `operations.stop.
  - القبول: كلُّ فعلٍ كاتبٍ في وحدة التشغيل ينتج صفَّ تدقيقٍ واحدًا يحمل الجدولَ والمعرّفَ والقيمةَ قبل وبعد.
- **INJ-0149** · P2 · اختبارات وحدة العقود
  - العيب: عشراتُ ملفات الاختبار الخاصةِ بوحدة العقود مصنَّفةٌ ملفاتٍ ميتةً في جرد الشيفرة (لا تُستدعى من أيِّ مسار تشغيلٍ ولا خط تسليم): claims_test · claim_dis
  - الإصلاح: أضِف مشغّلًا موحَّدًا (`composer test` أو سكربت في `scripts/`) يشغّل ملفاتِ tests/ ويُرجع رمزَ خروجٍ، واربطه بخط التسليم (لا يوجد اليومَ في المستودع سوى `composer.json` ب
  - القبول: أمرٌ واحدٌ يشغّل كلَّ اختبارات وحدة العقود ويُرجع صفرًا عند النجاح؛ وخطُّ التسليم يفشل عند سقوط أيِّ اختبار.

### Wrong Sidebar Placement
- **INJ-0059** · P1 · السايدبار الموحَّد (كل الأدوار)
  - العيب: 41 صفًّا غيرَ قابلٍ للتصيير ومساراتٌ مكسورةُ البادئة؛ وفراغُ قوائم الأدوار 31-33 قائمٌ في الوضعين (الموحَّدُ يسقطها بالباب، والقديمُ لا يجد موديولًا ي
  - الإصلاح: ① نفِّذ هجرةً تُسند لكلِّ صفٍّ من 6893-6933 بابًا من الثمانيةِ ومجموعةً في `link_groups` بـ`stage_no`، وتحذف بادئةَ `'../'` من `route`. ② أضِف قيدًا/فحصًا في `tools/u13_g
  - القبول: `SELECT COUNT(*) FROM nav_items WHERE door NOT IN ('HOME','DAILY','APPR','REC','REP','GOV','FIN','SET') OR route LIKE '../%'` = 0؛ وكلُّ دورٍ نشطٍ يعرض عنصرًا واحدًا على 
- **INJ-0125** · P2 · مساحة عملي
  - العيب: لا توجد منطقةُ «مساحة عملي» في السايدبار إطلاقًا: بابُها أيقونةُ حقيبةٍ في الشريط العلوي. وبديلُها في السايدبار عنوانٌ فرعيٌّ غيرُ قابلٍ للنقر «مساحتي
  - الإصلاح: في `includes/unified_nav.php::printStageNav()` أضِف منطقةً ثابتةً قبل حلقة المراحل تطبع العناصرَ الستةَ بالترتيب والأسماء المنصوص عليها من ثابتٍ في الكود (لا من nav_items
  - القبول: فتحُ أيِّ دورٍ من الأدوار الأربعة يعرض في أعلى السايدبار مجموعةً اسمُها «مساحة عملي» بستةِ عناصرَ بالأسماء والترتيب نفسِه حرفيًّا.
- **INJ-0138** · P2 · مساحتي الشخصية (داخل لوحة الإدارة)
  - العيب: مجموعةُ «مساحتي الشخصية» في سايدبار الدور 1 تعرض: مهامي · إنجازي · بوابتي · طلباتي · المراسلات — فتنقص «موافقاتي» و«بلاغاتي» و«التنبيهات» وتزيد «بوابت
  - الإصلاح: اطبع منطقةَ مساحة عملي من ثابتٍ في `includes/unified_nav.php` بالعناصر الستة وترتيبها وأيقوناتها، واحذف صفوفَ nav_items الشخصيةَ المكرَّرةَ من البذرة.
  - القبول: العناصرُ الستةُ تظهر بالترتيب والأسماء نفسِها في سايدبار الأدوار 1 و2 و9 و12، ولا عنصرَ سابعٌ فيها.

### Broken Button
- **INJ-0021** · P1 · شاشات التشغيل المولَّدة (CMP-03)
  - العيب: طبقةُ CMP-03 تنفّذ دالتين اثنتين فقط: `cmp03_store_insert()` و`cmp03_store_rows()` — لا `update` ولا `delete` ولا `approve` في المستودع كلِّه. فإحدى ع
  - الإصلاح: أضِف إلى `includes/cmp03_local_store.php` دالّتَي `cmp03_store_update()` و`cmp03_store_void()` (إبطالٌ ناعمٌ بسببٍ ومستخدمٍ ووقت) وحارسَ صلاحيةٍ داخلهما، ثم اعرض في كلِّ 
  - القبول: تعديلُ صفٍّ في Operations/unit_perf.php يحفظ القيمةَ الجديدةَ ويكتب سجلَّ تدقيق؛ وإبطالُ صفٍّ يُبقيه مرئيًّا موسومًا «مُبطَل» بسببه.
- **INJ-0038** · P1 · فحص شروط الاستحقاق
  - العيب: لا يوجد في الملف أيُّ معالج POST ولا أي استدعاءٍ لـUnitJourneyService؛ عمودُ «إجراء» يحوي رابطًا واحدًا نصُّه «افتح في صندوق الاعتماد» يوجّه إلى Finan
  - الإصلاح: أضِف في Finance/entitlement_gate.php معالجَ POST بفعلين (pass/reject) يستدعي UnitJourneyService::postEntitlement مع سببٍ إلزاميٍّ للرد، وفحصِ الفترة، ورمزِ الحماية، وسطرِ
  - القبول: من البوابة يُمرَّر مستحقٌّ فينتقل إلى المرحلة التالية ويُنشأ سطرُ تدقيق؛ ورفضُه يشترط سببًا ويعيد الواقعةَ معلَّقةً بسببٍ محكوم.
- **INJ-0085** · P1 · الوصول والتسليم
  - العيب: مستندُ التسليم لا يُخزَّن والرابطُ المباشرُ للإقفال لا يظهر، لكن شاشةَ الإقفال تبقى بالغةً من السايدبار مباشرةً فلا تنقطع الدورةُ كليًّا. — [مُصحَّح ب
  - الإصلاح: في Transport/transfer_arrival.php:22 استعمل event_type='status_change' مع new_value='delivered' (كما يفعل trs_log_event في Transport/trs_helpers.php)، وبدِّل شرطَ العدّ ف
  - القبول: بعد الضغط على «تأكيدُ التسليم» يتحول الصفُّ إلى شارة «مُسلَّم» ويظهر رابطُ «أقفل بتكلفته ←»، ويوجد صفٌّ مطابقٌ في transfer_events.

### Wrong Label
- **INJ-0070** · P1 · تصنيف البلاغ وتوجيهه آليًّا
  - العيب: الرابطُ في مجموعة «ثانيًا: التصنيف والتوجيه» عنوانُه «تصنيف البلاغ وتوجيهه آليًّا» ويقود إلى Tickets/ticket_types_config.php — وهي شاشةُ إعداداتِ أنوا
  - الإصلاح: تعديلُ صفِّ nav_items للمرحلة ٢ ليشير إلى Tickets/intake_classify.php بعنوان «تصنيف البلاغ وتوجيهه آليًّا»، وإبقاءُ ticket_types_config.php في المرحلة ٨ وحدَها بعنوان «إع
  - القبول: مجموعةُ «ثانيًا: التصنيف والتوجيه» تفتح شاشةَ تصنيف البلاغات الجديدة؛ وملفُّ إعدادات الأنواع يظهر مرةً واحدةً في السايدبار.
- **INJ-0115** · P1 · طلباتي
  - العيب: صفوفُ السايدبارِ المسمّاةُ «طلباتي» تشير إلى `FinRequests/my_requests.php` (المودول 115 — «طلباتي المالية») لا إلى `Portal/my_requests.php` (المودول 2
  - الإصلاح: غيِّر تسميةَ صفوفِ `nav_items` التي تشير إلى `FinRequests/my_requests.php` إلى «طلباتي المالية» (كاسمِ المودول 115 نفسِه)، وأضِف صفًّا منفصلًا باسم «طلباتي» يشير إلى `Por
  - القبول: في سايدبارِ أيِّ دور: «طلباتي» تفتح `Portal/my_requests.php` وتعرض الطلباتِ من كلِّ الأنواع، و«طلباتي المالية» تفتح بوابةَ المالية.
- **INJ-0147** · P2 · أحكام العقد — العملات والمقدم والمهل
  - العيب: الرابطُ في السايدبار باسم «أحكام العقد — العملات والمقدم والمهل» يفتح `Contracts/contract_card.php` التي عنوانُها الفعليُّ «بطاقة العقد» — أي شاشةَ مل
  - الإصلاح: إمّا بناءُ `Contracts/contract_terms.php` بالأعمدة الستةِ والعشرين المعلَنة، وإمّا توحيدُ التسمية: بدّل تسميةَ الرابط في البذرة إلى عنوان الشاشة الفعليّ وصحّح `$header_ti
  - القبول: كلُّ رابطٍ في سايدبار دور المبيعات يفتح شاشةً عنوانُها مطابقٌ لتسميته حرفيًّا.

══════════════════════════════════════════════════════════════════════
◆ القرار: يُبدأ بأكبرِ عنقودٍ **أوضحِ شاهدًا** — لا بأكبرِ عددٍ مجرَّدًا.
══════════════════════════════════════════════════════════════════════
