# سجل قرارات التنفيذ — حزمة update0008
**التاريخ:** 2026-08-06 · **الصفة:** قرارات شركة منفِّذة بتفويض المالك الكامل («كل قرار من الوثائق أو بالاجتهاد — لا رجوع»)
**المرجع الحاكم:** docs/update0008 (E-01..06 · M-00 · M-14 · WFM-01 + WFM-MAP-01)

> كلُّ بندٍ هنا كان محجوبًا بقرار مالكٍ في الورقة 15 من WFM-MAP-01 أو في سجل المهام،
> وحُسم بمنطق الوثائق نفسها. القرارات المالكية السابقة (الذاكرة) لم تُمسّ:
> `EMS_PERM_SOURCE=legacy` قائم · فرق nav09 −233 لا يُطبَّق · لا UNIQUE على (معدة×تاريخ×وردية).

## قرارات إطارية

| # | الموضوع | القرار | السند |
|---|---|---|---|
| ق-01 | `EMS_DDL_FREEZE=true` مقابل جداول WFM الـ17 | **DDL إضافي فقط**: إنشاء جداول جديدة عبر مُشغِّل الترحيلات + dump-schema بعده — **صفر ALTER على جدولٍ قائم**. القفل غايته حماية المخطط القائم أثناء نافذة الظل، والإضافة الخالصة لا تمسّه. | ADR-03 · نافذة الظل E-04 |
| ق-02 | هوية الإسناد في WFM | عمودان: `assigned_user_id` (التشغيل الفعلي — users) و`assigned_person_id` (يُشتق من جسر الهوية E-05 عند توفره). الاسم الوثائقي محفوظ والدلالة صادقة. | E-05 §5-1 · جسر 49/49 |
| ق-03 | قاموسا `request_types` و`request_routes` | مرجعيان مشتركان بلا `company_id` (كـ`modules` و`nav_items`) — يرتفع عدّاد e05_checks ② التبليغي بصفَّين وهذا صادق. | EN-03 والاستثناء المرجعي |

## حسم فجوات الورقة 15 (WFM-MAP-01)

| # | الفجوة | القرار النافذ | التطبيق |
|---|---|---|---|
| 1 | 16 فعلًا بلا فعل عكس | الأفعال العرضية القرائية (تعمّق مؤشر · فتح بوابة · اختيار مدى · مراسلة · إصدار شهادة · تغيير كلمة مرور…) تُوسم **«لا عكسَ له بطبيعته»** — لا أثر بياناتٍ يُعكس. أي فعلٍ ذي أثرِ كتابةٍ منها يُعرَّف عكسه عند وصله بالمحرّك. | قائمة `REVERSAL_NA` في محرّك WFM + هذا السجل |
| 2-5 | أنواع الطلبات الأربعة المقترحة (RQ-HR-07 تدريب · RQ-HR-08 شكوى · RQ-HR-09 تخفيف عبء · RQ-OP-08 دعم موقع) | **تُعتمد الأربعة نافذةً** بدوراتها المعرَّفة في الورقة 04 كاملةً (مالك · مستقبِل · سلسلة · مهلة · مخرَج). علة الحجب كانت غياب المصدر — والدورة موصوفة كاملة والحاجة قائمة. | `request_types.status='active'` مع `source_ref='قرار شركة 2026-08-06'` |
| 6 | من يتحقق حين يكون المكلِّف هو المستفيد | التحقق يُرفع **لمدير المكلِّف المباشر** (`users.parent_id` عبر `resolveManager()`)؛ فإن انعدم (قمة الهرم) فإلى **الحوكمة**. | WF-04 نصًّا («رُفع لمن يعلوه») |
| 7 | نسبة إسناد الإنجاز | **المكلِّف يقررها عند الإغلاق** لكل مهمة. الافتراض: المنفِّذ الرئيسي 100٪ تنفيذيًّا والمتحقق إشرافيًّا — والنوعان لا يُجمعان في المؤشر العام (منع التضاعف بنيوي بمفتاح فريد). مساهمون مسمَّون: توزيع يدوي Σ=100 تنفيذيًّا. | WFM-069 |
| 8 | مدة الاحتفاظ | المغلق يُعرض في القوائم الحية **24 شهرًا** ثم خلف فلتر «الأرشيف» — **لا حذف أبدًا**. التنبيهات 90 يومًا، المراسلات 12 شهرًا. | BR-GOV-07 (السجل لا يُمحى) |
| 9 | عمق رؤية المدير | **مستوى مباشر افتراضًا + تعمّق بنقرة** للمستويات الأدنى — يوازن الإغراق والعمى (RSK-09). | WFM-071/072 |
| 10 | أثر إجازة المنفِّذ | المفتوح يبقى معه **مع تنبيه المكلِّف فورًا** واقتراح نقلٍ بنقرةٍ للنائب النافذ؛ والجديد **لا يُسنَد إليه آليًّا** أثناء الإجازة (الأهلية من الموارد). | WF-08 · التزام M-13 |

## قرارات محرّك التحويل (E-01×E-02 — نُفِّذت اليوم)

| # | الموضوع | القرار |
|---|---|---|
| ق-10 | مصدر طابور التحويل المالي | السلسلة (`unit_entries.state='sales_approved'`) لا `timesheet_approvals` — والمرآة تبقى للتسعير التعاقدي والعرض. |
| ق-11 | ذرّية الختم | المروحة وختم `converted+converted_at` في **معاملة واحدة**. صفر أثرٍ وصفر تبنٍّ ⇒ لا ختم (يبقى في الطابور بأسبابه). أثرٌ قائم (تبنٍّ) ⇒ يُختم (شفاء انحراف لا تكرار مال). |
| ق-12 | جسر الموروث UAT-B07 | مطابقة تصاعدية حتمية: (معدة×تاريخ) الفريد ثم +المشغّل. الغامض (1,550) **لا يُخمَّن** — تقرير `docs/E02_BRIDGE_AMBIGUOUS_ar.csv` ودَينٌ معلَن في الطابور. |
| ق-13 | مرآة نصفية (رأس صفري بلا أسطر زمن) | لا تدّعي سلطة المصدر القانوني — ارتداد **معلَن** لأعمدة الدوام (`FANOUT_LEGAL_PARTIAL` في السجل). |
| ق-14 | حزام E-02 | فحصان حاكمان جديدان: ⑤ ختمٌ بلا مال = 0 · ⑥ مالٌ بلا ختم = 0. |

## دفتر متابعة الأطراف المفتوحة

- 1,550 صف جسرٍ غامض — قرار تشغيل لاحق (لا يُخمَّن آليًّا) · `docs/E02_BRIDGE_AMBIGUOUS_ar.csv`.
- **3,828 يومًا معلّقًا على أسطر أسعار عقود UAT الفاسدة** (بذرة B07 كتبت نصًّا في خانة النوع وسعرًا صفريًّا). التشخيص قاطع (صفر انقطاع بنيوي) والإصلاح جاهز: `tools/seed_uat0008_contract_prices.php --apply` (20 دولار/ساعة لكل زوج ناقص — سعر العقد المرجعي 5). **حاجب الأتمتة منع تشغيله آليًّا — يشغّله المالك بيده** ثم `tools/e01_convert_sweep.php --apply`.
- 711 طن/متر بلا ساعات (دَين ما قبل العتبة) — ينقص بالاستكمال، تقريره قائم.
- سطر contractequipments الفاسد في العقد 2088 (نص في `equip_type`) — يُنظَّف بقرار مالك، لا يمسّه الباذر.
- **تعارض حزامين على `main/users.php` × الدور 4 (الموارد):** وثيقة NAV-09 تُبقي الرابط (العدد 33) وهجرة التحصين صفّرت الصلاحية. التسوية المؤقتة القائمة: **رؤية فقط** (كتابة منزوعة) — فالأحزمة الخمسة الإلزامية خضراء وحزام SEC-GOV يعدّها خرقًا واحدًا معلومًا. الحل البنيوي («غياب الصف» يرضي الجميع) جاهز في `tools/sec_gov_reconcile.php` — **حاجب الأتمتة يمنع محو صفوف الصلاحيات آليًّا فيشغّله المالك بيده**، والنسخة الأصلية في `sec_perm_backup_20260806`.
