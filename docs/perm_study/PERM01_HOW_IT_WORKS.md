# نظامُ الصلاحيّاتِ الجديد — كيف يعمل · وما يمسُّه · وكيف يُضبَط ويُجرَّب

**دراسةٌ من المصدرِ الحيّ · 2026-09-04 · الالتزام `cc9079cb` · الشركة 4**
*(هذا شرحٌ لعملٍ ليس لي — قرأتُه من شيفرتِه وقِستُه من قاعدتِه.)*

---

# ① الفكرةُ الحاكمة — **الفصلُ على الفعلِ لا على الشاشة**

هذا هو مفتاحُ فهمِ النظامِ كلِّه.

**المشكلةُ التي وُضع لها:** الدوران 34 «منفذ المدفوعات البنكية» و35 «معد المطابقة البنكية» **لهما مجموعةُ الشاشاتِ نفسُها حرفًا** (166 شاشةً مشتركة بقالبٍ واحد `#39`).

> ⇒ فمنعُ **شاشةٍ** أو تضييقُ **قالبٍ** **لا يفصل شيئًا** — لأنَّ الشاشةَ نفسَها مشتركة.

**والحلُّ الذي بُني:** لا يُسأل «أَيملك هذا الشخصُ الشاشة؟» بل **«أَفعل هذا الشخصُ الفعلَ المضادَّ على هذا المستندِ بعينِه؟»**

```
منفِّذُ الدفعِ #907 يفتح المطابقةَ لكشفٍ فيه دفعةٌ يحمل fin_payments.executed_by اسمَه
                              ↓
                    403 · ويُقيَّد المنعُ بالاسم
```

---

# ② النظامُ طبقتان — واحدةٌ **تعمل الآن** وأخرى **مبنيّةٌ ولم تُطبَّق**

| | الطبقة | الحال | الأثرُ اليوم |
|---|---|---|---|
| **①** | **الحدُّ الصلبُ على الفعل** | 🟢 **يعمل حيًّا** | **12 منعًا مقيَّدًا** |
| **②** | **القوالبُ المستهدَفة** | 🟡 **مبنيّةٌ · موقوفة** | **صفر** — لم تُطبَّق |
| **③** | تجميدُ البذرِ وبوّابةُ التفعيل | 🟢 يعمل | مفتاحانِ مغلقان |
| **④** | كاشفُ التعارضات | 🟢 يعمل | **121 تعارضًا مرصودًا** |

---

# ③ الطبقةُ الأولى — الحدُّ الصلب (**تعمل الآن**)

## أين تسكن

| الملف | الدور |
|---|---|
| `app/Services/Finance/ReconSodGuard.php` | **السياسةُ والتقييد** — بلا استعلام |
| `app/Services/Finance/BankReconService.php` | **قارئا الضابط** — عند `auto_match` و`close` |

## كيف تعمل — بالترتيب

```
① يُجمَع نظيرُ كلِّ سطرِ كشف
② يُسأل: أَفي هذه الدفعاتِ ما نفَّذه الفاعلُ نفسُه؟   ← sodExecutedByActor
③ إن نعم:  يُقيَّد المنعُ في sec_sod_denials  ثمَّ  403
④ إن لا :  تُكتب المطابقات
```

> ⛔ **والمسحُ يسبق أوّلَ كتابة** — نصُّ الشيفرةِ حرفًا: «مطابقةٌ جزئيّةٌ ثمَّ رفضٌ أسوأُ من رفضٍ كامل». **وهذا تصميمٌ سليم.**

## والدليلُ أنَّها تعمل — 12 منعًا مقيَّدًا

```
SOD-06 · bank_recon.auto_match · مستخدم 907 · 13:55:47 · «دفعات نفذها الفاعل: 8139»
SOD-06 · bank_recon.close      · مستخدم 907 · 13:55:47 · «دفعات نفذها الفاعل: 8139»
…  (12 صفًّا · كلُّها اليومَ 13:55 – 14:07)
```

**وقبلَ هذا كان `sec_sod_denials` فارغًا** — فلا يُعرَف أوقع منعٌ قطُّ أم لا.

---

# ④ الطبقةُ الثانية — القوالبُ المستهدَفة (**مبنيّةٌ ولم تُطبَّق**)

## المعادلة

> **قالبُ الموظّف = شاشاتُ إدارتِه من «الدليل المعماري.xlsx» + 7 شاشاتٍ شخصيّةٍ إلزاميّة**

**والمصدرُ اختير ورُفض غيرُه:** `nav_placements` (مستوردٌ من ورقتك) **معتمد** · و`gov_target_nav` **مرفوض** لأنَّ 682 من 725 صفًّا فيه **منسوخٌ من الوضعِ القائم** — فالبناءُ عليه إعادةُ إنتاجِ الخطأ.

## الحصيلة — **21 قالبًا · 539 شاشة**

| المساحة | شاشات | الأدوار | | المساحة | شاشات | الأدوار |
|---|---|---|---|---|---|---|
| DEP-01 المبيعات | 27 | 12 | | DEP-10 البلاغات | 20 | 24 |
| DEP-02 الموردون | 38 | 2·8 | | DEP-11 التشغيل | 19 | 1·7 |
| DEP-03 التمويل | 32 | 26 | | DEP-12 الموقع | 26 | 5·6 |
| DEP-04 الأسطول | 37 | 3·10·11 | | DEP-13 القوى | 24 | 27 |
| **DEP-05 المالية** | **28** | **9 أدوار** | | DEP-14 الصيانة | 23 | 13·14 |
| DEP-06 الخزينة | 24 | 21 | | DEP-15 النقل | 20 | 23 |
| DEP-07 الموارد | 31 | 4 | | DEP-16 المشتريات | 19 | 16 |
| DEP-08 الحوكمة | 47 | 15 | | DEP-17 المخازن | 21 | 25 |
| DEP-09 المخاطر | 20 | 28·29·30 | | EX-CEO · EX-DVP · IAF · WS-MY | 33·19·24·7 | — |

## ماذا سيتغيّر لو طُبِّق — **قياسٌ لا تقدير**

| المقياس | الرقم |
|---|---|
| مستخدمون مشمولون | **75** |
| أبوابٌ **تُغلَق** | **10,104** |
| أبوابٌ **تُفتَح** | **110** |
| موظّفٌ يبقى بلا شاشة | **صفر** ✅ |

**وليس كلُّه تضييقًا:**

| الوظيفة | الآن | المستهدَف | |
|---|---|---|---|
| منفذ المدفوعات البنكية | 166 | **28** | يُغلق 139 |
| معد المطابقة البنكية | 166 | **28** | يُغلق 139 |
| إدارة التشغيل | 247 | **19** | يُغلق 228 |
| **المدير المالي** | 15 | **28** | ⬆ **يُفتح 27 — كان محرومًا** |

## ⛔ ولماذا لم يُطبَّق — **حاجزٌ حقيقيّ**

ورقةُ الدليلِ تصف **القائمةَ الجانبيّةَ فقط**. وثمّةَ شاشاتٌ **لا رابطَ لها** تُفتَح **بالنقرِ من شاشةٍ أخرى** (بطاقةُ العميل · صندوقُ الاعتمادات · الملفُّ الشخصيّ).

```
من 359 شاشةً سيُغلقها القالبُ الجديد  ⇐  105 شاشةً من هذا النوع
```

**فلو طُبِّق الآن:** الرابطُ يظهر ويعمل، وعند النقرِ يقول «ممنوع» — **وهذا يكسر العملَ صامتًا.**

---

# ⑤ الطبقتانِ الثالثةُ والرابعة

## تجميدُ السياسةِ — مفتاحانِ مغلقانِ منذ 14:15

| النطاق | السبب |
|---|---|
| `grants` | لا تصدر منحةٌ جديدةٌ حتى إغلاقِ مسارِ الاحتواء |
| `profile_activation` | لا يُفعَّل قالبٌ جديدٌ حتى بناءِ الهدفِ المعياريّ |

**و28 قالبًا نافذًا قُيِّد بـ`GRANDFATHER`** — «نفاذٌ سابقٌ لبوّابةِ التفعيل — **يُقيَّد ولا يُسقَط**».

## كاشفُ التعارضات — من صفرٍ إلى 121

| التركيبة | العدد | التعارض |
|---|---|---|
| `SOD-10` | 28 | مُعِدُّ الرواتب × مُطلِقُ دفعتِها |
| `SOD-03` | 27 | منشئُ المورد × معتمِدُ حسابِه البنكيّ |
| `SOD-07` | 27 | مُعِدُّ المطابقة × معتمِدُها |
| `SOD-11` | 25 | مسجِّلُ الأصل × معتمِدُ التخلّصِ منه |
| `SOD-04` | 14 | معتمِدُ الحسابِ البنكيّ × منفِّذُ الدفع |

**والرصدُ على القالبِ لا على الشخص:** «قالبٌ نافذٌ يجمع حصريَّ طرفَي التركيبة» — فقالبٌ واحدٌ يمنح شاشاتِ الطرفَين لحاملِ أحدِهما **فيبلغ وظيفةَ الآخرِ بلا أن يحمل دورَه**.

*(والثلاثةَ عشرَ تركيبةً في `sec_sod_pairs` بمرجعِ كلٍّ في الورقةِ الحاكمة — `FACC-0058` … `FACC-0070`.)*

---

# ⑥ كيف تُجرِّبه بنفسك

## ⓐ عرضُ القوالبِ المبنيّة — **آمنٌ ويُعاد**

```bash
C:/wamp64/bin/php/php8.2.30/php.exe database/migrations/2028_05_04_perm01_target_profiles.php
```

## ⓑ ماذا سيتغيّر لكلِّ موظّف — **قراءةٌ فقط**

```bash
C:/wamp64/bin/php/php8.2.30/php.exe tools/perm01_target_shadow.php
```

**ولوظيفةٍ واحدة:**

```bash
C:/wamp64/bin/php/php8.2.30/php.exe tools/perm01_target_shadow.php --role=34
```

## ⓒ قياسُ الاحتواءِ والتعارضات

```bash
C:/wamp64/bin/php/php8.2.30/php.exe tools/perm01_p0_containment.php
```

## ⓓ التجربةُ الحيّةُ للحدِّ الصلب — **وهي أصدقُ اختبار**

1. ادخل بحساب **`FIN-PAY@equipation.sd`** (‏#907 · منفذ المدفوعات).
2. افتح **المطابقة البنكية** لكشفٍ فيه دفعةٌ نفَّذتها أنت.
3. اضغط «مطابقة تلقائية» أو «إقفال».
4. **المتوقَّع: 403 ورسالةُ منع.**
5. تحقَّق من التقييد:

```sql
SELECT pair_code, source_kind, subject_user_id, denied_at, detail
  FROM sec_sod_denials ORDER BY denied_at DESC LIMIT 5;
```

## ⓔ التراجعُ الكامل

```bash
C:/wamp64/bin/php/php8.2.30/php.exe database/migrations/2028_05_04_perm01_target_profiles_down.php
```

---

# ⑦ كيف يُضبَط

| ما تريد ضبطَه | أين |
|---|---|
| **تركيباتُ الفصل** (مَن لا يجتمع بمَن) | `sec_sod_pairs` — 13 صفًّا · `active` و`severity` |
| **الأدوارُ في كلِّ طرف** | `sec_sod_pairs.roles_a` · `roles_b` |
| **القالبُ المستهدَفُ لإدارة** | `perm01_target_profile` + `perm01_target_item` — **ويُعاد بناؤهما من الدليلِ لا يُحرَّران يدويًّا** |
| **فتحُ التجميد** | `gov_policy_freeze` — `active=0` وختمُ `closed_at` |
| **إجازةُ تفعيلِ قالب** | صفٌّ في `gov_profile_activation_approval` |

> ⛔ **ولا يُحرَّر `perm01_target_item` يدويًّا**: مصدرُه ورقةُ الدليل. فالتحريرُ اليدويُّ يصنع **مصدرًا ثانيًا يتفرّق** — وهو عينُ ما بُني النظامُ ليمنعه.

---

# ⑧ وما لم يتغيّر — **لا موظّفَ فقد صلاحيّةً**

قِستُ الحارسَ المركزيَّ لكلِّ دورٍ له حسابٌ حيّ:

| المقياس | القيمة |
|---|---|
| أدوارٌ مقيسة | **32** |
| **أدوارٌ بصفرِ سماح** | **0** ✅ |
| القوالبُ النافذة · بنودُها · المنح | 171 · 3,924 · 77 — **كما هي** |

---

# ⑨ 🔴 وعطبٌ كشفَتْه تجربةُ النظامِ الجديد — **جذرُه مقيسٌ الآن**

## ما وقع

تجربةُ الحدِّ الصلبِ اليومَ (13:55 – 14:07) تركت **خمسةَ أحداثٍ يتيمة**:

```
finance.recon_adjustment.posted  ⇐  bank_recon_match#1369 · 1374 · 1379 · 1384 · 1394
```

## والجذرُ — **حدثٌ يعيش بعدَ موتِ كيانِه**

```
bank_recon_matches.statement_line_id  →  bank_statement_lines   ON DELETE CASCADE
```

| الجدول | صفوف | AUTO_INCREMENT |
|---|---|---|
| `bank_statement_lines` | **0** | **2163** |
| `bank_recon_matches` | **0** | **1398** |

**فالتسلسل:**

```
① يُنشأ كشفٌ وأسطرُه            ④ تُحذَف الأسطر (كنسُ التجربة)
② تُكتب المطابقاتُ              ⑤ CASCADE يمحو المطابقات
③ يُنشَر الحدثُ                 ⑥ ⚠ **والحدثُ يبقى — بلا أب**
```

## والمدى أوسعُ من اليوم

| القياس | القيمة |
|---|---|
| أحداثُ `finance.recon_adjustment.posted` | **226** |
| **منها يتيمٌ** | **226 / 226** |
| أوّلُ ظهور | **2026-07-31** |

> ⇒ **كلُّ تشغيلٍ لدورةِ المطابقةِ البنكيّةِ منذ يوليو يترك أيتامًا.** والحارسُ الجديدُ **لم يُحدِث العطبَ** — بل جعله مرئيًّا بتشغيلِ المسارِ اليوم.

## والعلاجُ المبدئيّ

**الحدثُ يُنشَر بعدَ ثبوتِ الكيانِ لا قبلَ زوالِه** — أو **يُشمَل بالـCASCADE** كما شُمِلت المطابقة. وأيًّا كان، **القرارُ لك** لأنَّه يمسُّ سجلَّ الحقائق.

---

# الخلاصةُ في أربعةِ أسطر

1. **يعمل الآن:** الحدُّ الصلبُ على الفعل — 12 منعًا مقيَّدًا · وكاشفٌ رصد 121 تعارضًا · ومفتاحا تجميدٍ مغلقان.
2. **مبنيٌّ ولم يُطبَّق:** 21 قالبًا · 539 شاشة · وسيُغلق 10,104 بابًا ويفتح 110.
3. **الحاجزُ الوحيد:** 105 شاشةً تُفتَح بالنقرِ لا بالرابط — تطبيقٌ الآنَ **يكسر العملَ صامتًا**.
4. **وعطبٌ منفصلٌ ظهر:** `CASCADE` يمحو كيانَ الحدثِ ويُبقي الحدث — **226 يتيمًا منذ يوليو**.
