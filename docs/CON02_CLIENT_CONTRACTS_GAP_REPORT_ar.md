# تقريرُ الفروقات — EQUIP-CON-02-EMS «عقود مشاريع العملاء والالتزامات»

**تاريخ التدقيق:** 2026-07-28 · **الفرع:** `feature/inspection-to-maintenance-order`
**المرجع:** `CON-02 عقود العملاء.docx` v1.0
**طريقةُ التدقيق:** فحصُ المخطط (`SHOW CREATE TABLE`) + قراءةُ الكود + تشغيلُ مسبارٍ حيٍّ على قاعدة `equipation_manage` (شركة 4) يستدعي `EffectFanout::hourPolicy` على عقدٍ حقيقي.

---

## 0 · الخلاصة التنفيذية

**نسبةُ التغطية الإجمالية ≈ 30٪.**

الوثيقةُ تقوم على عمودٍ واحدٍ: **قاعدةُ الإسناد تُعرَّف في عقد العميل، ومنها تُشتق ثلاثةُ أحكامٍ (فوترة · مورد · مشغّل).** النظامُ اليوم يملك **الأحكامَ الثلاثة فعلًا** (`contract_hour_policies` بثلاثة `party_scope`) ويملك **بكتاتِ الزمن ومسؤولَها** (`unit_time_log.ops_state` + `resp_party`) — وهذا أثمنُ ما فيه ويقرّبه من الوثيقة كثيرًا.

لكنّ **الوصلة المفقودة هي المصفوفةُ نفسُها**: النظامُ يشتق المسؤولَ من **حالة الساعة** لا من **بند التزامٍ في العقد**. فلا يوجد `contract_obligations`، ولا `obligor` لكل بند، ولا `effect_on_billing`. وأثرُ ذلك عمليٌّ لا نظري:

> **نفادُ وقودٍ يلتزم به العميلُ في العقد يُسجَّل اليوم `fuel_logistics_stop` → حكمُه `none` "لوجستياتٌ علينا ما لم ينصّ العقد" → لا يُفوتر.** الوثيقةُ تقول: يجب أن يكون **استعدادًا مفوترًا** يُحتسب للمورد والمشغّل (حالةُ الاختبار A1). ولا سبيلَ في النظام الحالي لتحقيق ذلك لأن مكانَ النصّ العقدي غيرُ موجود.

وثلاثةُ أبوابٍ كاملةٍ **صفرٌ تنفيذًا**: مصفوفةُ الالتزامات (§4) · الغراماتُ والحوافز (§6) · الحدُّ الأدنى المضمون بندًا في المستخلص (§5 و A5).

---

## 1 · جدولُ المطابقة بالبنود

| بندُ الوثيقة | الحالة | ما هو قائمٌ فعلًا | الفرق |
|---|---|---|---|
| **§2-① الهوية والنطاق** | ✅ ~90٪ | `contracts` + `projects` + `clients` + `contractequipments` | — |
| **§2-② بنود الالتزام** | ✅ ~80٪ | `contract_commitments` (نوع·وحدة·كمية·دورية·طرف ملتزم·حكمُ العجز·حكمُ الزيادة) + شاشة `Clients/contract_commitments.php` (مديول 127) | لا عمودَ `work_model` صريح · قيدُ «Σ حاويات الموردين ≤ الكمية الملتزمة» غيرُ مفروض |
| **§2-③ التسعير** | ⚠ ~60٪ | سعرُ المعدة في `contractequipments` + عملةُ العقد + أساسُ FX (`fx_*`) | **شروطُ تعديل السعر (وقودٌ·تضخمٌ·صرف) مفقودة** · الضريبةُ غيرُ مفصولة |
| **§2-④ الالتزامات المتبادلة** | ❌ ~10٪ | أربعةُ حقولٍ نصيةٍ حرةٍ في `contracts`: `transportation`, `accommodation`, `place_for_living`, `workshop` | نصٌّ حرٌّ لا يقرؤه كودٌ · لا طرفَ ملتزمًا · لا أثرَ على الفوترة |
| **§2-⑤ الجزاءات والحوافز** | ❌ 0٪ | لا شيء | — |
| **§2-⑥ الاستقطاعات والدفع** | ⚠ ~25٪ | `guarantees`, `paid_contract`, `payment_time`, `payment_date` (نصوصٌ حرة) | لا استهلاكَ محتسبًا للدفعة المقدمة · لا ضمانَ حسن تنفيذٍ محتجزًا |
| **§2-⑦ الأحكام الاستثنائية** | ⚠ ~50٪ | إجراءاتُ العقد (إيقاف·استئناف·إنهاء·دمج) في `contract_actions_handler.php` + حالةُ `force_majeure` في سطور الزمن | لا تحكيمَ · أثرُ التعليق على احتساب الوحدات غيرُ مؤتمت |
| **§3 نماذج العمل الأربعة** | ✅ ~90٪ (2026-07-28) | ساعة ✅ · طن ✅ · متر ✅ · **نقلة ✅**: `'نقلة' => 'trip'` في `CONTRACT_UNIT` و`trips_count` تُقرأ في `resolveTimesheet` (المساران الحيُّ والقانوني) | **خاملةٌ بنيويًّا**: صفرُ عقدٍ يفوتر بالنقلة (المقيس: ساعة×8 · متر طولي×1 · فارغ×2) فصفرُ أثرٍ على فاتورة · لا قاعدةَ للنقلة الناقصة بعد |
| **§3 الحقول الثلاثة منفصلة** | ✅ 100٪ | `unit_time_log` للزمن · `unit_entries` (`record_basis=contract`) للوحدة · (`analytical`) للإنتاج | مطابقٌ للوثيقة |
| **§4 مصفوفةُ الالتزامات (9 بنود)** | ⚠ **بنيةٌ بلا سلوك** (2026-07-28) | ✅ جدولُ `contract_obligations` قائمٌ بالبنود التسعة و`obligor` الخماسي و`effect_on_billing` — **فارغٌ عمدًا** | لا شاشةَ تملؤه (T-11) · لا محلِّلَ يقرؤه (T-02/T-04) · **الفجوةُ الأمُّ قائمةٌ سلوكيًّا** حتى تُبنى القراءة |
| **§5 الأحكامُ الثلاثةُ المستقلة** | ✅ ~70٪ | `contract_hour_policies` بـ`party_scope ∈ {client,supplier,operator}` × `ops_state` × `ruling ∈ {full,pct,none,pending,case_by_case}` مع سريانٍ بالتاريخ | البنيةُ ممتازةٌ ومطابقةٌ روحًا — لكنّ مفتاحَها `ops_state` لا `obligation_type` |
| **§5 مصفوفةٌ لكل عقد** | ❌ 0٪ | **صفرُ صفٍّ** بـ`contract_ref` غير NULL (مسبارٌ حي) — 12 حكمًا كلُّها `company_default` | لا مصفوفةَ عقدٍ واحدة · ولا خطأ `423` عند غيابها؛ يسقط على الافتراضي **صامتًا** |
| **§5 الإسنادُ إلزامي (422)** | ❌ 0٪ | `TimesheetEntryService::safeRespParty()` **يبتلع أي قيمةٍ غير صالحةٍ إلى `'none'`** (سطر 812) · لا حارسَ يمنع الإقفال | أقربُ ما وُجد: نصٌّ إرشاديٌّ للمراجع في `Approvals/hours_approval.php:1619` («ساعتا التوقف بلا مسؤولٍ ومرجع») — اجتهادٌ بشريٌّ لا قاعدة |
| **§5 المسؤولُ من قائمة §4** | ❌ 0٪ | `resp_party` قائمةٌ ثابتةٌ عامةٌ (`company·supplier·operator·client·planned·force_majeure·none`) تُملأ افتراضيًّا من الحالة (`timesheet.php:3225`) | ليست بنودَ التزامِ العقد |
| **§5 الاعتراض الموثق** | ⚠ ~20٪ | `claim_lines.dispute_flag` + `dispute_reason` | اعتراضٌ على **بند مستخلص** بعد الفوترة، لا على **إسنادٍ** عند وقوعه · لا `objection_ref` ولا قرارُ حسمٍ موثق |
| **§5 الحدُّ الأدنى المضمون** | ⚠ ~15٪ | `contract_commitments.commitment_type='min_guaranteed'` (صفٌّ واحدٌ مسجَّل) | **لا بندَ فارقٍ في المستخلص**: `claim_helpers.php` يبني البنودَ من `fin_event_links` ← `timesheet` حصرًا. الفارقُ لا يُحتسب ولا يُفوتر |
| **§6 غرامةُ العجز/التأخر** | ❌ 0٪ | `shortfall_rule='penalty'` قيمةٌ مسجّلةٌ بلا محرّك | لا معادلة · لا سقف · لا خصمٌ ظاهر |
| **§6 الحوافز** | ❌ 0٪ | حوافزُ **الموظفين** (`worker_evaluation`) شيءٌ آخرُ تمامًا | — |
| **§6 الملاحق** | ✅ ~85٪ (2026-07-28) | `contract_amendments` (**13 نوعًا** بعد «تغيير التزامات») + إسقاطٌ حتميٌّ من إجراءات العقد + ✅ `effective_from` | العمودُ **NULL في الصفوف السبعة القائمة** (لا يُشتق من `amend_date`) · لا توليدَ صفوفِ التزامٍ عند الاعتماد بعد · لا إعادةَ موازنةٍ للحاويات |
| **§7-① معالجُ إنشاء العقد** | ⚠ ~50٪ | `Contracts/contracts.php` (1789 سطرًا) — نموذجٌ واحدٌ طويل | ليس معالجَ خطوات · لا حفظَ عند كل خطوة |
| **§7-② ملفُّ العقد بتبويبات** | ⚠ ~40٪ | `Contracts/contracts_details.php` — أقسامٌ ونوافذُ منبثقة | لا تبويبات · **لا تبويبَ التزامات ولا جزاءات ولا حاويات ولا مستخلصات** · لا شارةَ «التزامٌ ينكسر» |
| **§7-③ شاشةُ مصفوفة الالتزامات** | ❌ 0٪ | — | — |
| **§7-④ لوحةُ الإسناد اليومي** | ❌ 0٪ | `Approvals/hours_approval.php` يجمع actual/standby/stop **بلا مسؤول** ولا منعِ إقفال | — |
| **§7-⑤ شاشةُ احتساب الجزاءات** | ❌ 0٪ | — | — |
| **§8 `contract_obligations`** | ✅ **مُنشأ 2026-07-28** | الجدولُ بمواصفة §8 + FK حقيقيٌّ إلى `contracts` + حذفٌ ناعمٌ ثلاثيّ + مسجَّلٌ في `TenantRegistry` | مفتاحُه `id` لا `obligation_id` (بوابةُ العزل تفترض `id` في `softDelete`) · صفرُ صفٍّ (التعبئةُ قرارُ عمل) |
| **§8 `contract_penalty_rules`** | ❌ مفقود | — | — |
| **§8 `unit_attributions`** | ❌ مفقود | بديلٌ جزئي: `unit_time_log` (فيه `minutes`↔`hours` و`ops_state` و`resp_party`) | ينقصه: `billable` · `supplier_countable` · `operator_countable` · `decided_by/at` · `objection_ref` · `obligation_type` |
| **§8 `contract_commitments`** | ✅ موجود | 22 صفًّا على 8 من 9 عقود | — |
| **§8 `AttributionService`** | ❌ 0٪ | — | لا DTO · لا 422/423/409 · لا `POST /units/{id}/attribution` |
| **§8 الأحداثُ الثلاثة** | ⚠ **ثوابتُ فقط 2026-07-28** | `includes/attribution_events.php` — `attribution.decided/objected/overridden` | لا منتِجَ لها بعد (T-04) · **وأسماءُ الوثيقة PascalCase يرفضها `EventPublisher:111` بنيويًّا** فالمنقّطُ هو العقدُ الفعليّ |

### حالاتُ الاختبار §8.1

| الحالة | النتيجة | البيان |
|---|---|---|
| **A1** مسؤوليةُ العميل (وقودٌ 90د) | ❌ **يفشل** | `fuel_logistics_stop` → `ruling=none` ثابتًا. لا يمكن جعلُه مفوترًا لأن بندَ «الوقود على العميل» غيرُ موجود |
| **A2** مسؤوليةُ المورد (عطلٌ 30د) | ⚠ **جزئي** | `tech_breakdown`/`supplier_stop` → `none` للعميل والمورد ✅ · حضورُ المشغّل عبر سياسته ✅ · **تنبيهُ الجزاء ❌** |
| **A3** الإسنادُ الإلزامي | ❌ **يفشل** | لا 422 · الواقعةُ تُقفل بزمنِ توقفٍ بلا مسؤول |
| **A4** الاعتراض | ❌ **يفشل** | لا اعتراضَ على مستوى الإسناد |
| **A5** الحدُّ الأدنى | ❌ **يفشل** | لا بندَ فارقٍ في المستخلص |
| **A6** الملحق (لا رجعية) | ⚠ **جزئي** | `contract_hour_policies` تحترم السريان بالتاريخ ✅ · لكن الملاحقَ لا تولّد صفوفَ سياسةٍ ولا تُغيّر مصفوفةً غيرَ موجودة |

### مقياسُ القبول §9

| معيارُ القبول | النتيجة |
|---|---|
| تعبئةُ مصفوفة عقدٍ كاملةٍ لدى ≥80٪ من مسؤولي العقود | ❌ لا مصفوفةَ تُملأ |
| صفرُ واقعةٍ مقفلةٍ بزمنِ توقفٍ بلا مسؤول | ⚠ **البياناتُ اليومَ نظيفة** (0 سطرِ توقفٍ بـ`none`) — لكن **بالعادة لا بالحارس** |
| مطابقةُ الأحكام الثلاثة لعيّنة شهرٍ مع قراءة العقد | ❌ غيرُ ممكنٍ: العقدُ لا يحوي الأحكام |

**قراءةُ الواقع في `unit_time_log`:** 135 سطرَ عملٍ فعلي · 60 سطرَ استعداد (55 على العميل · 5 علينا) · 61 سطرَ توقف — كلُّها بمسؤولٍ محدد، ولا حالةَ توقفٍ لها أكثرُ من مسؤولٍ إلا `standby`. أي: **الإسنادُ اليومَ مشتقٌّ من الحالة لا من العقد** — وهو بالضبط ما تنهى عنه §5.

---

## 2 · التاسك المطلوبة (ما يطالب به المستند وغيرُ موجود)

مرتّبةٌ بالتبعية: كلُّ مهمةٍ تعتمد على ما قبلها.

### المرحلة ① — الأساسُ البنيوي (تفتح كلَّ ما بعدها)

**T-01 · جدولُ مصفوفة الالتزامات `contract_obligations`** — *أولويةٌ قصوى*
هجرةٌ جديدة: `obligation_id · client_contract_id · obligation_type` (ENUM من بنود §4 التسعة: `fuel · access_road · loading_equipment · equipment_readiness · operators · permits_safety · utilities · catering_camp · force_majeure`) `· obligor (client·company·supplier·operator) · effect_on_billing (billable_standby·non_billable·per_clause) · penalty_rule_id · valid_from/to` — مع `UQ(contract_id, obligation_type, valid_from)`. تسجيلٌ في `TenantRegistry` (T_TENANT + soft).

**T-02 · ربطُ المصفوفة بحكم الساعة**
توسيعُ `contract_hour_policies` بعمود `obligation_type` (nullable للتوافق) — فيصير مفتاحُ الحكم **(بندُ الالتزام × الطرف)** لا (حالةُ الساعة × الطرف) وحدَها. تعديلُ `EffectFanout::hourPolicy()` ليقدّم الأخصَّ: `obligation_type` ← ثم `ops_state` ← ثم الافتراضي.
> بهذه المهمة وحدَها تنجح حالةُ الاختبار A1.

**T-03 · جدولُ الإسناد `unit_attributions` + إحلالُه محلَّ `resp_party` الحر**
`attr_id · unit_record_id · time_bucket (actual·standby·stop) · minutes · obligation_type · billable · supplier_countable · operator_countable · decided_by/at · objection_ref` — `UQ(unit_record_id, time_bucket, obligation_type)`. جسرُ ترحيلٍ من `unit_time_log` القائم (لا كسرَ للقائم — كتابةٌ مزدوجةٌ بعَلَمٍ على نمط `EMS_UNIT_DUAL_WRITE`).

**T-04 · `AttributionService` — خدمةُ الإسناد**
`app/Services/Contract/AttributionService.php`: مدخلٌ (UnitRecordID + بكتاتُ الزمن + مقترحُ المسؤول)، مخرجٌ `AttributionDTO` بالأحكام الثلاثة مشتقةً من **مصفوفة العقد النافذة بتاريخ الواقعة**. مع الأخطاء الأربعة: `422` (فترةٌ بلا مسؤول) · `422` (مسؤولٌ خارج قائمة العقد) · `423` (مصفوفةٌ غيرُ معرَّفة) · `409` (تعديلٌ بعد اكتمال السلسلة — يُصحَّح بعكس الحدث).

**T-05 · حارسُ الإسناد الإلزامي**
إزالةُ الابتلاع الصامت في `TimesheetEntryService::safeRespParty()` (سطر 812) وتحويلُه إلى رفضٍ صريح؛ ومنعُ إقفال الواقعة/اعتمادِها إن بقي زمنُ توقفٍ بلا مسؤول (خطّافٌ في `Approvals/hours_approval.php` وفي بوابة التحويل).
> **گوتشا:** البياناتُ الحالية نظيفةٌ (0 مخالفة) فالتفعيلُ لن يكسر شيئًا — لكن ابدأ بوضع الرصد (monitor) أسبوعًا على نمط `EMS_ACTION_GUARD`.

**T-06 · أحداثُ الإسناد الثلاثة**
`AttributionDecided` · `AttributionObjected` · `AttributionOverridden` عبر `EventPublisher` حصرًا (v8 §15-أ) — تستهلكها أحكامُ الأطراف **قبل** توليد آثار FES.

### المرحلة ② — الجزاءاتُ والحوافزُ والحدُّ الأدنى

**T-07 · جدولُ `contract_penalty_rules`**
`rule_id · client_contract_id · rule_kind (shortfall·readiness·delay·quality·bonus) · formula_json · rate · cap_percent · periodicity · valid_from/to`.

**T-08 · محرّكُ احتساب الجزاءات والحوافز**
خدمةٌ دوريةٌ تقيس الفارقَ بين الملتزم (`contract_commitments`) والمنفَّذ (`unit_entries`) في دوريّته، وتولّد **بندًا ظاهرًا** في المستخلص لا خصمًا صامتًا. مع سببِ إعفاءٍ إلزاميٍّ إن أُعفي.

**T-09 · بندُ الحدّ الأدنى المضمون في المستخلص**
توسيعُ `claim_helpers.php` (اليومَ يبني البنودَ من `fin_event_links` ← `timesheet` حصرًا) بمصادرَ جديدةٍ لـ`claim_lines.source_kind`: `min_guarantee` · `penalty` · `incentive`. **الفارقُ بندٌ باسمه — لا يُدسّ في كميات الوحدات** (§5).

**T-10 · تمريرُ جزاء المورد إلى عقده**
ربطُ ما يقع بمسؤولية المورد بخصمٍ في تسويته (`app/Services/Settlement/` — `CHARGE_TYPES` فيه `penalty` سلفًا) **بلا تجاوزٍ للمُحصَّل** (§6).

### المرحلة ③ — الشاشات (UI-01)

**T-11 · شاشةُ مصفوفة الالتزامات داخل العقد** — صفٌّ لكل بندٍ من التسعة + قائمةُ الطرف الملتزم + عمودُ أثر الإخلال + تحذيرٌ ثابت: «تغييرُ الملتزم يسري من تاريخه ولا يمسّ الوقائعَ السابقة».

**T-12 · لوحةُ الإسناد اليومي** — قراءةٌ + قرار: جدولُ §5 مطبَّقًا على وقائع اليوم، **والزمنُ بلا مسؤولٍ يظهر أحمرَ ويمنع الإقفال**.

**T-13 · شاشةُ احتساب الجزاءات** — بنودُ الغرامة والحافز بمعادلاتها ومراجعها، باعتمادٍ وسببِ إعفاءٍ إلزامي.

**T-14 · تحويلُ `contracts_details.php` إلى تبويبات** — (البنود · التسعير · **الالتزامات** · الجزاءات · الحاويات · المستخلصات · السجل) + شارةُ «التزامٌ ينكسر» وشارةُ «عقدٌ ينتهي».
> **گوتشا:** الملفُّ 1849 سطرًا وفيه 12 نافذةً منبثقة — التبويبُ تغييرٌ واسع؛ افصله عن T-11 حتى لا تتعطل المصفوفةُ بانتظاره.

**T-15 · تحويلُ `contracts.php` إلى معالج خطوات** — الهويةُ ← البنودُ ← التسعيرُ ← المصفوفةُ ← الجزاءاتُ ← الاستقطاعاتُ ← الملخص، بحفظٍ عند كل خطوة ولا اعتمادَ داخل الإنشاء. *(أدنى الأولويات — تحسينُ تجربةٍ لا فجوةُ حوكمة.)*

### المرحلة ④ — إتمامُ النماذج والملاحق

**T-16 · نموذجُ «النقلة» (trip)** — إضافةُ `'نقلة' => 'trip'` إلى `EffectFanout::CONTRACT_UNIT` + عمودُ كميةِ النقلات في مصدر الدوام + قاعدةُ النقلة الناقصة (نسبةً أو لا شيء) في بنود الالتزام.

**T-17 · سريانُ الملاحق** — إضافةُ `effective_from` إلى `contract_amendments` + نوعِ ملحق «تغيير المصفوفة» + توليدِ صفوف `contract_obligations` جديدةٍ بسريانها عند الاعتماد (لا رجعية) + إعادةِ موازنة الحاويات عند تغيّر الكمية.

**T-18 · قيدُ الحاويات** — فرضُ «Σ حصص الموردين ≤ `qty_committed`» (§8-`contract_commitments` · UX-05 §8.2).

**T-19 · شروطُ تعديل السعر** — وقودٌ · تضخمٌ · صرف (§2-③).

### المرحلة ⑤ — الترحيلُ والقبول (§8 التوافق و§9)

**T-20 · العقدُ الرائد** — عقدٌ واحدٌ بمصفوفةٍ كاملةٍ شهرًا، بمطابقةِ ما فُوتر مع ما كان يُفوتر يدويًّا (**صفرُ فرقٍ قيمي**) قبل التعميم — **وعَلَمُ رجوعٍ يعيد الإسنادَ الحر** (منهج FES §10.4).

**T-21 · حالاتُ الاختبار الست** — `tests/attribution_test.php` يغطي A1…A6 حرفيًّا.

---

## 3 · ملاحظاتٌ ونقاطُ انتباه

1. **البناءُ إضافيٌّ لا هادم.** `contract_hour_policies` و`unit_time_log` و`contract_commitments` كلُّها متوافقةٌ اتجاهًا مع الوثيقة؛ المطلوبُ توسيعُها بالمفتاح المفقود (`obligation_type`) لا استبدالُها.
2. **أخطرُ ما في الوضع الراهن ليس ما هو مفقود، بل ما يبدو موجودًا:** `resp_party` موجودٌ ومملوءٌ ونظيف — فيسهل الظنُّ أن الإسنادَ منفَّذ. وهو في الحقيقة **مشتقٌّ من حالة الساعة آليًّا** (`timesheet.php:3225`) ويُبتلع صامتًا إلى `'none'` عند الخطأ.
3. **صفرُ مصفوفةِ عقدٍ اليوم** — كلُّ العقود التسعة على الافتراضي. أي تفعيلٍ لـ`423` يجب أن يسبقه ملءُ مصفوفةِ العقد الرائد.
4. **`case_by_case` هو الحكمُ الافتراضيُّ لـ`standby`** — أي أن أهمَّ حالةٍ ماليةٍ في الوثيقة (الاستعدادُ المفوتر) متروكةٌ اليومَ للاجتهاد بلا قاعدة.
5. **گوتشا معماريّ:** أي أثرٍ ماليٍّ جديد (غرامة · حافز · حدٌّ أدنى) يمرّ عبر `EventPublisher` والجذرِ المحايد ثم إسقاطِ الدفتر — لا كتابةَ مباشرةً في `fin_financial_events` (ADR-15 · v8 §15-أ).

---

## 4 · ما نُفِّذ — دفعةُ الأساس (2026-07-28)

**مبدأُ الدفعة: بنيةٌ لا سلوك.** النظامُ بعدها متطابقٌ سلوكيًّا مع ما قبلها — والبرهانُ مقيسٌ لا مُدَّعًى: حزم `timesheet_fanout_test` · `unit_chain_e2e_proof` · `unit_source_tables_test` · `effect_fanout_test` أعطت **28 · 37 · 34 · 18 ناجحًا وصفرَ فاشل قبل التعديل وبعده** (نتيجةٌ متطابقة)، و`tenant_leak_test` 130/130.

| # | البند | الحالة |
|---|---|---|
| 1–10 | جدولُ `contract_obligations` بالبنود التسعة | ✅ هجرة `2026_07_28_con02_obligations_foundation.sql` |
| 11 | `effect_on_billing` (`billable_standby·non_billable·per_clause`) | ✅ افتراضُه `per_clause` أي «اقرأ البند» — لا حكمَ مشتقًّا صامتًا |
| 12 | «ما لم يُنص عليه يُعدُّ التزامَ الشركة» | ✅ `DEFAULT 'company'` توثيقًا وقيدًا — **ولا محلِّلَ يقرؤها** (الدفعةُ المتوسطة) |
| 13 | نموذجُ النقلة | ✅ `'نقلة' => 'trip'` + `trips_count` في المسارين. **لم يُمسّ** `$unitMap`/`$basisMap` (سطرا ~891/~979) لأن القياسَ أظهر أنهما يحويان `trip` سلفًا و`unit_party_awards.award_unit_type` كذلك |
| 14 | `contract_amendments.effective_from` | ✅ Nullable · **والصفوفُ السبعةُ القائمةُ NULL** (تاريخُ التوقيع ليس تاريخَ السريان) |
| 15 | نوعُ ملحق «تغيير التزامات» | ✅ ENUM صار 13 قيمة — أُدرجت وقُرئت حرفيًّا بلا ترميزٍ مضاعف |
| 16–18 | مفاتيحُ الأحداث الثلاثة | ✅ `includes/attribution_events.php` — ثوابتُ فقط بلا دالةِ نشرٍ (لا كودَ ميت) |
| 19 | عَلَمُ الرجوع | ✅ `EMS_ATTRIBUTION_MATRIX=off` في `.env` و`.env.example` — **لا كودَ يتفرّع عليه بعد** |

### قراراتٌ خرجت عن حرفية الوثيقة — بأسبابها المقيسة

1. **المفتاحُ `id` لا `obligation_id`** — `TenantDb::softDelete()` يبني شرطَه على `id` ثابتًا ([TenantDb.php:220](../app/Core/TenantDb.php#L220))، فجدولٌ مسجَّلٌ `soft=true` بمفتاحٍ مغايرٍ **يرمي «Unknown column 'id'» في مسار حذفه الوحيد**. قيس فعلًا فرمى، ثم صُحِّح والجدولُ فارغٌ قبل الالتزام.
2. **`obligor` خماسيّ بإضافة `none`** — §4 تعدّ «القاهرة» بندًا و§5-⑤ تقول «لا يُفوتر ولا يُجزى أحد»؛ فـ`company` كانت ستقرأ «الشركةُ ملتزمةٌ بمنع الفيضان».
3. **مفاتيحُ الأحداث منقّطةٌ لا PascalCase** — `EventPublisher` يفرض `/^[a-z]+(\.[a-z_]+){1,2}$/` ([EventPublisher.php:111](../app/Core/EventPublisher.php#L111))، فأسماءُ §8 الحرفيةُ **يرفضها الناقل**.
4. **بندُ التحكيم (§2-⑦) مُسقَطٌ بقرارٍ واعٍ** — بندٌ ورقيٌّ لا أثرَ له على فوترةٍ ولا إسناد، وترميزُه عمودٌ يبقى فارغًا.

### ما لم يُمسّ عمدًا (نطاقُ الدفعات التالية)

`contract_hour_policies` (مفتاحًا أو عمودًا — البندُ الأثمنُ في الدفعة المتوسطة) · `unit_attributions` و`AttributionService` وأخطاءُ 422/423/409 · **الابتلاعُ الصامت في `TimesheetEntryService::safeRespParty()` باقٍ كما هو** · `contract_penalty_rules` وأيُّ احتساب · أيُّ شاشةٍ أو صفٍّ في `modules`/`role_permissions` · المستخلصات.

> **التغطيةُ الإجماليةُ لم تتغيّر عمليًّا (≈30٪ → ≈34٪)**: ما بُني مستودعٌ لا محرّك. حالاتُ الاختبار A1…A6 **ما زالت على نتائجها كما هي** — لأن أيًّا منها لا ينجح بجدولٍ فارغٍ لا يقرؤه منطق.
