# سجلُّ التنفيذ — update0005 · CAP-01

**القياسُ القاعدي قبل الموجة ① (2026-08-02):** `op_containers`=94 (أعمدةُ رصيدٍ مخزَّنة: cap/allocated/consumed + remaining مولَّد) · `contract_commitments`=12 بلا نوعِ معدةٍ ولا حقولِ احتياطي · `supplier_contract_lines`=6 ببندِ standby_basis/rate فقط · `seat_assignments`=0 بلا خطةٍ ولا حالةِ تفعيل · `container_consumption`=1 (idem_key بلا مفتاح نسخة) · جداولُ CAP-01 الأربعةُ (الدفترُ · الروابطُ · التغطياتُ · بنودُ التسوية) غائبةٌ كلُّها — كما قاسته الخطة §1.

| # | الموجة | المهمة | ما فُعل | الملفات المتأثرة | نتيجة الاختبار | اجتهاد |
|---|---|---|---|---|---|---|
| 1 | ① | CAP-01 | حقولُ §8.1 السبعةُ + النوعُ والسريانُ على `contract_commitments` (السياسةُ كاملةً — المصدرُ الواحد §5-⑦) والأعدادُ الثلاثةُ على درجة «نوع» في `op_containers` · المفتاحُ `uq_obl_type_from` فهرسٌ فريدٌ مشروطٌ على عمودٍ مولَّد `obl_type_uq_key` (الأحياءُ ذوو النوع حصرًا) | `database/migrations/2026_08_02_cap_w1_standby_obligation_fields.php` | حزام ①/② في `tests/cap_w1_standby_fields_test.php` — أخضر | J-01 · J-02 |
| 2 | ① | CAP-02 | حقولُ §8.2 الثمانيةُ على `supplier_contract_lines` + `contract_obligation_ref` · `saveLine` يتحقق: مرجعُ التزامٍ ميتٍ أو خارجَ عقدٍ نافذ → 422 | الهجرةُ نفسُها · `app/Services/Contract/SupplierContractService.php` | حزام ③ — أخضر (422 للمرجع الميت · NULL يبقى NULL) | — |
| 3 | ① | CAP-03 | خطةُ §8.3 على `seat_assignments`: `planned_qty_month/total` + `measure_code` + `activation_state(active·pending)` + `supplier_contract_line_id` · **CHECK `ck_sa_standby_zero`: الاحتياطيُّ pending صفرُ كمياتٍ بنيويًّا** · «التعديلُ فترةٌ جديدة» تحمله بنيةُ date_from/to وسيفرضه `SeatAssignmentService` (الموجة ④ · C20) | الهجرةُ نفسُها | حزام ④ — أخضر (600 ساعةٍ على pending تُرفض CHECK) | J-03 |
| 4 | ① | CAP-04 | `StandbyCapService::checkRegistration` — عدُّ المسجَّل من `seat_assignments` (دور «احتياطي» نشط) على البند وعلى الالتزام · تجاوزُ أيِّ سقفٍ → **409 بعرض السقفين** · سقفٌ غيرُ معرَّفٍ في أي العقدين → **422 fail-closed** | `app/Services/Contract/StandbyCapService.php` | حزام ⑤ — أخضر (C17) | J-03 |
| 5 | ① | CAP-05 | شاشةُ التزامات العميل: فورمُ §8.1 كاملًا (النوعُ · الأعدادُ · الكميةُ ومقياسُها · المقابلُ **بخيار «لم يُنَصَّ» افتراضًا** · قاعدةُ التفعيل · معالجةُ الساعات · السريان) بقوائمَ بيضاءَ وNULL صريحٍ للفارغ · شاشةُ بنود المورد: §8.2 كاملًا بقائمة التزامات عقد العميل المرتبط | `Clients/contract_commitments.php` · `Suppliers/supplier_contract_lines.php` | حزام ⑦ — أخضر | — |
| 6 | ① | CAP-06 | حارسُ DEC-CAP-A: `StandbyCapService::compensationBasisOrFail` — احتسابُ مقابلٍ بلا نصٍّ عقديٍّ صريح (عميلًا أو موردًا) → **422 مسجَّلةٌ في سجل النشاط** (`action_type=blocked · module=capacity`) · و`none` الصريحةُ ترفض أيضًا | `app/Services/Contract/StandbyCapService.php` | حزام ⑥ — أخضر | J-04 |
