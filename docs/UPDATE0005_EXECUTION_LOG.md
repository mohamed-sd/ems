# سجلُّ التنفيذ — update0005 · CAP-01

**القياسُ القاعدي قبل الموجة ① (2026-08-02):** `op_containers`=94 (أعمدةُ رصيدٍ مخزَّنة: cap/allocated/consumed + remaining مولَّد) · `contract_commitments`=12 بلا نوعِ معدةٍ ولا حقولِ احتياطي · `supplier_contract_lines`=6 ببندِ standby_basis/rate فقط · `seat_assignments`=0 بلا خطةٍ ولا حالةِ تفعيل · `container_consumption`=1 (idem_key بلا مفتاح نسخة) · جداولُ CAP-01 الأربعةُ (الدفترُ · الروابطُ · التغطياتُ · بنودُ التسوية) غائبةٌ كلُّها — كما قاسته الخطة §1.

| # | الموجة | المهمة | ما فُعل | الملفات المتأثرة | نتيجة الاختبار | اجتهاد |
|---|---|---|---|---|---|---|
| 1 | ① | CAP-01 | حقولُ §8.1 السبعةُ + النوعُ والسريانُ على `contract_commitments` (السياسةُ كاملةً — المصدرُ الواحد §5-⑦) والأعدادُ الثلاثةُ على درجة «نوع» في `op_containers` · المفتاحُ `uq_obl_type_from` فهرسٌ فريدٌ مشروطٌ على عمودٍ مولَّد `obl_type_uq_key` (الأحياءُ ذوو النوع حصرًا) | `database/migrations/2026_08_02_cap_w1_standby_obligation_fields.php` | حزام ①/② في `tests/cap_w1_standby_fields_test.php` — أخضر | J-01 · J-02 |
| 2 | ① | CAP-02 | حقولُ §8.2 الثمانيةُ على `supplier_contract_lines` + `contract_obligation_ref` · `saveLine` يتحقق: مرجعُ التزامٍ ميتٍ أو خارجَ عقدٍ نافذ → 422 | الهجرةُ نفسُها · `app/Services/Contract/SupplierContractService.php` | حزام ③ — أخضر (422 للمرجع الميت · NULL يبقى NULL) | — |
| 3 | ① | CAP-03 | خطةُ §8.3 على `seat_assignments`: `planned_qty_month/total` + `measure_code` + `activation_state(active·pending)` + `supplier_contract_line_id` · **CHECK `ck_sa_standby_zero`: الاحتياطيُّ pending صفرُ كمياتٍ بنيويًّا** · «التعديلُ فترةٌ جديدة» تحمله بنيةُ date_from/to وسيفرضه `SeatAssignmentService` (الموجة ④ · C20) | الهجرةُ نفسُها | حزام ④ — أخضر (600 ساعةٍ على pending تُرفض CHECK) | J-03 |
| 4 | ① | CAP-04 | `StandbyCapService::checkRegistration` — عدُّ المسجَّل من `seat_assignments` (دور «احتياطي» نشط) على البند وعلى الالتزام · تجاوزُ أيِّ سقفٍ → **409 بعرض السقفين** · سقفٌ غيرُ معرَّفٍ في أي العقدين → **422 fail-closed** | `app/Services/Contract/StandbyCapService.php` | حزام ⑤ — أخضر (C17) | J-03 |
| 5 | ① | CAP-05 | شاشةُ التزامات العميل: فورمُ §8.1 كاملًا (النوعُ · الأعدادُ · الكميةُ ومقياسُها · المقابلُ **بخيار «لم يُنَصَّ» افتراضًا** · قاعدةُ التفعيل · معالجةُ الساعات · السريان) بقوائمَ بيضاءَ وNULL صريحٍ للفارغ · شاشةُ بنود المورد: §8.2 كاملًا بقائمة التزامات عقد العميل المرتبط | `Clients/contract_commitments.php` · `Suppliers/supplier_contract_lines.php` | حزام ⑦ — أخضر | — |
| 6 | ① | CAP-06 | حارسُ DEC-CAP-A: `StandbyCapService::compensationBasisOrFail` — احتسابُ مقابلٍ بلا نصٍّ عقديٍّ صريح (عميلًا أو موردًا) → **422 مسجَّلةٌ في سجل النشاط** (`action_type=blocked · module=capacity`) · و`none` الصريحةُ ترفض أيضًا | `app/Services/Contract/StandbyCapService.php` | حزام ⑥ — أخضر | J-04 |
| 7 | ② | CAP-07 | `capacity_consumption_ledger` بحقول §13.2 كاملةً (المفاتيحُ الثمانية · المقياسُ والزمنُ التشغيليُّ والإنتاجُ التحليليُّ مستقلاتٍ · role_snapshot · unit_decision_snapshot_id) · **UQ خماسيٌّ يمنع الخصمَ مرتين** · Insert-only بنمط `immutable_key='effect_type'` القائم (لا triggers — عُرفُ المستودع) · CHECK: الكميةُ موجبةٌ والعكسُ بمرجعٍ حصرًا | `2026_08_02_cap_w2_capacity_ledger_structure.sql` · `TenantRegistry` | حزام ①②④ في `tests/cap_w2_ledger_test.php` — أخضر (C24·C25) | J-06 |
| 8 | ② | CAP-08 | `capacity_financial_event_links` — Append-only بـ`UQ(led_id, fin_event_id)` · `immutable_key='fin_event_id'` و`journal_ref` وحدَه موضعُ معالجةٍ لاحق | الهجرةُ نفسُها · `TenantRegistry` | حزام ⑤ — أخضر (409 للربط المكرر) | — |
| 9 | ② | CAP-09 | `substitute_coverages` — الدرجاتُ الثلاث (`own_standby·cross_supplier·source_change`) · `reason_code` من قائمةٍ محكومةٍ بخمسة أسباب + `reason_ref` للبلاغ/أمر العمل · **valid_from/to إلزاميان NOT NULL** · CHECK التواريخ · **وCHECK ضد ابتلاع ENUM الفارغَ** (المتممة w2b — الوضعُ غيرُ الصارم يبتلع صامتًا) | الهجرةُ نفسُها + `2026_08_02_cap_w2b_enum_not_empty_checks.sql` | حزام ⑥ — أخضر (§6.1-①/②) | J-07 |
| 10 | ② | CAP-10 | `coverage_settlement_lines` — `party` رباعيةٌ × `effect` رباعية (billable·gap_kept·exceptional_line·entitlement) بكميةٍ ومقياسٍ ومبلغٍ ومرجعِ تسوية — بندٌ ظاهرٌ لا سطرٌ مدموج | الهجرةُ نفسُها | حزام ⑥ — أخضر (الأطرافُ الأربعة قُيّدت) | — |
| 11 | ② | CAP-11 | `CapacityLedgerService` — `appendLine` (تحققٌ كاملٌ · مكررٌ → 409 بمرجع السطر القائم) · `reverse` (سطرٌ عاكسٌ بمرجع الأصل · الأصلُ باقٍ · تكرارُ العكس 409 · عكسُ العكس 422 · بلا مرجعٍ 422) · `linkFinancialEvent` · تعديلُ سطرٍ عبر البوابة → رفضٌ بنيوي | `app/Services/Capacity/CapacityLedgerService.php` | حزام ②③④⑤ — أخضر (C25·C26) | — |
| 12 | ② | CAP-12 | خرطُ `container_consumption` (صفٌّ واحدٌ حي: entry:2410:r1) إلى الدفتر بمفتاحه — النسخةُ من `revision_no` الحي وإلا من لاحقة idem_key · الأثرُ بدرجة الحاوية · والسالبُ والمتعذّرُ يُعلَنان لا يُلفَّقان · **قبول: صفرُ استهلاكٍ موروثٍ بلا سطرِ دفتر** | `2026_08_02_cap_w2_legacy_consumption_backfill.php` | حزام ⑦ — أخضر (خُرط 1/1) | — |
| 13 | ③ | CAP-13 | `BalanceCalculator` — الرصيدُ من الدفتر والإعكاسات حصرًا: `consumedOfContainer` (Σ الذرية) · `consumedOfRef` (التزامٌ/حصةٌ/بندٌ/مشغّل) · `balanceOf` يقرأ `cap_qty` وحدَه (سقفُ عقدٍ لا رصيد) — والعمودُ المسمومُ بقيمةٍ كاذبةٍ لا يغيّر الحساب (مُثبَتٌ اختبارًا) | `app/Services/Capacity/BalanceCalculator.php` | حزام ② في `tests/cap_w3_balance_test.php` — أخضر | — |
| 14 | ③ | CAP-14 | أعمدةُ الرصيد الثلاثة صارت **مخبأً يُعاد بناؤه**: `rebuildContainerCache` الكاتبُ الشرعيُّ الوحيد · و`consume()` تكتب **مزدوجًا** (العمودُ + سطرُ الدفتر) في المعاملة نفسِها — والعطالةُ في المسارين معًا · والردُّ السالبُ سطرُ عكسٍ بمرجع الأصل | `app/Services/Operations/OperationalTransformService.php` | حزام ③⑤ — أخضر | J-08 |
| 15 | ③ | CAP-15 | علَمُ `EMS_CAPACITY_SOURCE` (columns·ledger) على نمط `EMS_PERM_SOURCE` — `CapacitySourceService`: المصدرُ الحاكم · `freeOf` بحسب العلم · `shadowCompare` يسجّل الفرقَ صفًّا يوميًّا في `capacity_shadow_diffs` (uq يومي) · `flipReadiness` ميزانُ «صفرِ فرقٍ ١٤ يومًا» · **العلمُ الآن columns والقلبُ مؤجَّل** | `app/Services/Capacity/CapacitySourceService.php` · `.env` · الهجرة w3 | حزام ④ — أخضر | — |
| 16 | ③ | CAP-16 | قيودُ الدرجات في القاعدة (DEC-CAP-C): `UQ(obl_id, seat_no)` بعمودٍ مولَّدٍ مشروط · **تطابقُ التزامِ المقعد والتزامِ حصته بقيد FK مركّب** `(parent_id, obl_id)→(id, obl_id)` (C21 بنيويًّا) · ومنعُ تخصيصين مفتوحين فعّالين لمقعدٍ بعمودٍ مولَّدٍ فريد — والاحتياطيُّ pending خارج القيد (C4) والتداخلُ المدَّدُ بحارس الخدمة القائم | `2026_08_02_cap_w3_balance_flip_constraints.php` | حزام ①⑦ — أخضر (C4·C21) | J-09 |
| 17 | ③ | CAP-17 | `SigmaGuard` — قيدُ Σ الواعي بالحالة: تجاوزٌ فوق المتعاقد → **409 بقيمة الفارق** في كل حالة (C1) · المسودةُ/الجزئية تُحفظ بفجوتها الظاهرة (C15) · الاعتمادُ يوجب Σ= أو `sigma_exception_ref` موقَّعًا (C16) · و`transition` يفحص بالحالة المستهدفة قبل الكتابة | `app/Services/Capacity/SigmaGuard.php` + عمودا `plan_state`/`sigma_exception_ref` | حزام ⑥ — أخضر (C1·C15·C16) | — |
