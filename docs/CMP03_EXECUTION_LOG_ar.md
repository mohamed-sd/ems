# CMP-03 · سجلُّ تنفيذ موجات الأعمدة السبع

**الخطة:** docs/CMP03_EXECUTION_PLAN_ar.md · **المقياس الحي:** `php tools/scrdes_compare.php --totals`
**الأساس قبل التنفيذ (2026-08-03):** 🟦218 · 🟨292 · 🟧2945 (حوكمة 1201 + وظيفي 1744) · 🟩828

### الموجة ② · طبقة الحوكمة المشتركة — حوكمة 1201 → **0**
- **المكوّن** `includes/gov_columns.php`: سجل الأعمدة الحاكمة الخمسة والعشرين بشرائحه الثلاث (① السبعة الحاكمة · ② الأثر والعكس · ③ المال والإثبات) بتسميات المستند الحرفية و«لماذا هو حاكم» من ورقة 04 · سياق القيم العامة (الكيان + العملة الأساسية من admin_companies بعزل الجلسة) يُبث `window.emsGovCtx` مركزيًّا من insidebar/inheader.
- **الحشو** في `assets/js/ui-unification.js` (padGovernanceCells داخل normalizeAllTables): خلايا صفوف أعمدة `th[data-gov]` تُحشى قبل حارس structureMismatch وقبل تهيئة DataTables — قيمة السياق أو «—» (توصية المالك ③) · صفوف العنصر النائب يمدّ colspan بدل الحشو (tn/18) · حارس stateSave المركزي القائم (stateLoadParams يرفض حالةً بعدد أعمدة مختلف) يغطي گوتشا إضافة الأعمدة.
- **المطبّق** `tools/cmp03_apply.php` (--diff/--apply): حقن 1109 رأس `<th data-gov>` حرفيًّا في **124 ملفًّا** — الترويسة المستهدفة أفضلُ `<thead>` تشابهًا، والحقن قبل `</tr>`، والفائض فوق 22 يحمل `class="none"` فينهار لسطرٍ تابعٍ عبر DataTables Responsive (توصية المالك ① — وُثّق 60 عمودًا في docs/CMP03_OVERFLOW_LOG_ar.md) · مهام ربط المصادر الصفّية (1109) في docs/CMP03_FOLLOWUP_SOURCES_ar.md.
- **اليدوية الوحيدة** `ActivityLogs/activity_logs.php` (serverSide): 11 عمودًا في الترويسة + خلايا renderLogCells 11-21 + columnDefs — **بربط حقيقي** حيث توفر: الكيان من خريطة الشركات لكل صف (السوبر يرى شركة كل سجل) · المُنشئ من الموظف/الحساب وصفته · تاريخ الإنشاء من السجل.
- **الفحص الدائم** `tools/cmp03_gov_check.php`: «لا شاشة مستندٍ ينقصها عمود حوكمةٍ يطلبه تصميمها» — 138/138 صفر مخالفة · و`tools/cmp03_lib.php` مرآة منطق المقارن (لا تغيير في أحدهما دون الآخر) · و`tools/cmp03_show.php` عرض حكم الشاشة الواحدة · و`tools/cmp03_probe.php` مسح أنماط الجداول.
- **التحقق الحي بالمتصفح**: Contracts/contracts.php (39 رأسًا = 39 خلية بكل صف · 13 حوكمة · الكيان «ايكوبيشن» محشو · DataTable مهيأ) و Equipments/equipments.php (18=18 · 10 حوكمة) — صفر أخطاء كونسول.
- **المقياس بعد الموجة:** 🟦1419 · 🟨292 · 🟧1744 (حوكمة **0** + وظيفي 1744) · 🟩905.

**اجتهاد J-01 (مسجّل):** حشو خلايا الحوكمة من المتصفح لا من حلقات PHP — لأن المقياس والعرض كليهما على `<th>` الحرفي، وحلقات 124 شاشةً متغايرة البنية لا تُمس آليًّا بأمان؛ والقيم الصفّية الحقيقية مهامُ لحاقٍ معلنة (توصية ③). حين يُربط مصدر صفّي، تُستبدل خلية الشاشة في حلقتها فيتوقف الحشو عنها تلقائيًّا (الحشو لا يعمل إلا عند نقصان الخلايا).
**اجتهاد J-02 (مسجّل):** الرباعي كان أحمر قبل الموجة (40≠41 للتشغيل) — قاعدة كتم المالك (707e6e9) كانت في المستورد دون الفاحص؛ نُسخت مرآةً في nav09_verify ($SUPPRESS) فصفر الرباعي.

**الحزام بعد الموجة:** act_checks --enforce ✔ (14/14 وصفر في الحاكمة) · التساعي 90/90 ✔ · الرباعي 0/0/0/0 ✔ · السبعة الحاكمة 0 ✔
