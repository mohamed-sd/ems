# إعادة التسمية النصية — سجلّ التنفيذ والتحقق · 2026-07-24

**المصدر:** مصفوفة الشاشات §9 في دليل تجربة المستخدم `EQUIP-UX-01-EMS` (92 اسمًا معتمدًا).
**النطاق:** نصّي بحت — الأسماء فقط. لا مساس بالمسارات ولا الصلاحيات ولا البنية ولا ترتيب القوائم.

## القرارات المعتمدة (من المالك قبل التنفيذ)
1. **توحيد ذكي:** شاشات المفهوم الواحد توحَّدت على اسم المصفوفة (العملاء ×2 · المشاريع ×2 · العقود ×2 · مركز التقارير ×5)؛ واستُثنيت شاشة المشرفين `main/project_users.php`: تغيّر تسجيل الموردين فقط (الصف 14 → «مشرفو الموردين») وبقيت «المشرفين» و«إدارة المعاونين» و«المعاونين» لبقية الإدارات.
2. **main/users.php:** الصف 3 فقط → «الصلاحيات» (إدارة التشغيل)؛ الصف 60 «إدارة المستخدمين» يبقى لمدير الصلاحيات. العنوان الداخلي للشاشتين المشتركتين لم يُمسّ.
3. **النطاق الكامل:** قاعدة البيانات + ملفّا القوائم + العناوين الداخلية للشاشات الـ92.
4. **رابط التقارير الذكي:** فرع التقارير القديمة → «مركز التقارير»؛ فرع emsreports يبقى «التقارير».

## ما نُفِّذ
| الموضع | العدد | الطريقة |
|---|---|---|
| `modules.name` (قاعدة البيانات) | **86 صفًّا** | معاملة واحدة، كل UPDATE محروسٌ بالاسم القديم (منها تطبيع همزة «الاعدادات»→«الإعدادات» الصف 24) |
| `includes/finreq_badges.php` | 4 تسميات | موافقات إدارتي · مكتب المحاسب · الطلبات المالية · زمن دورة الطلبات |
| `insidebar.php` | 2 | «اعتماد الوحدات التشغيلية» + فرع التقارير القديمة «مركز التقارير» |
| عناوين داخلية (`$page_title` · `$header_title` · `<title>` · سطور h1/h2) | **89 ملفًّا** | سكربت بمعاينةٍ مسبقة + إصلاحان يدويان (transfer_types_config · map_page) |

**إجمالي أثر git:** 91 ملفًّا · 169 سطرًا مستبدلًا (نصوص فقط).

## نتائج التحقق
- فحص قبلي: 86/86 اسمًا حيًّا طابق المتوقع قبل التنفيذ ✓
- فحص بعدي: 86/86 صفًّا بالاسم الجديد حرفيًّا ✓
- `php -l` على 91 ملفًّا معدَّلًا: **صفر أخطاء** ✓
- إعادة تصيير السايدبارات الـ13 (جلسة فعلية لكل دور): **220/220 رابطًا طابق الاسم المتوقع، بالمواضع نفسها** — منها 111 تغيّر اسمه و109 بقي عمدًا ✓
- تصادم اسمٍ واحدٍ لمسارين مختلفين في سايدبارٍ واحد: **صفر** ✓
- بقايا الأسماء القديمة في القاعدة: الصفوف الستة المحفوظة عمدًا فقط (15·16·18 المشرفين، 17 إدارة المعاونين، 107 المعاونين، 60 إدارة المستخدمين) ✓

## التراجع
- **القاعدة:** `rollback.sql` في هذا المجلد (محروسٌ بالأسماء الجديدة — يعيد الأسماء كاملةً).
- **الكود والعناوين:** كانت شجرة git نظيفةً قبل التنفيذ — `git checkout -- .` يعيد كل الملفات، و`git diff` يعرض كل التغييرات للمراجعة.
- لقطتا الجدول قبل/بعد: `before_modules.tsv` · `after_modules.tsv`. البيان التنفيذي: `manifest.json`.

## ملاحظات متعمَّدة
- شاشتا `Tickets/ticket_form.php` (عنوان ديناميكي بوضعين) لم تحتاجا تغييرًا؛ والوحدة 133 في القاعدة صارت «تسجيل بلاغ».
- بعض ترويسات البطاقات القديمة المكتوبة نصًّا حرًّا لا يطابق أي اسمٍ مسجَّل (مثل «تفاصيل العقد» داخل contracts_details) بقيت — لم تكن ضمن أسماء المصفوفة.
