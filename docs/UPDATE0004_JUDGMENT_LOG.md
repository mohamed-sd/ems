# update0004 · سجلُّ الاجتهادات

> كلُّ نزولٍ إلى الدرجة ③ (الكودُ والمشروعُ القائم) أو ④ (المعياريةُ العالمية) من سلّم §1 — صفٌّ هنا. البرومت §1.1.

| # | الموجة | المهمة | السؤال | الدرجة | ما قررتُه | لماذا | ما يترتب لو كان القرارُ خطأً |
|---|---|---|---|---|---|---|---|
| J-01 | ⓪ | D-03 | حذفُ `organizational_levels` من `SEC-01 §11.2①` يترك جملة «إلى ثلاثةٍ منفصلة» ناقصةً — ما الصياغةُ البديلة؟ | ③ | «إلى اثنين منفصلين: `org_units` للهيكل · `job_titles` للمسميات والمستويات» | `SEC-01 §15` يجعل المستوى عمودًا في `job_titles` الموسَّع لا جدولًا مستقلًّا — فالمستويات تُذكر مع المسميات | لو أُريد جدولُ مستوياتٍ مستقلٌّ لاحقًا فالجملة تحتاج تحديثًا نصيًّا فقط — لا أثرَ بنيويًّا |
| J-02 | ⓪ | D-05 | الملحق: العنوانان `# 17` و`## 17.1` — أيصيران `12.6` و`12.6.1`؟ والعنوان `# 13 اختبار الحمل والتزامن` أيُشطر إلى 12.2 و12.3؟ | ① ثم ③ | 13→12.2 · 14→12.3 · 15→12.4 · 16→12.5 · 17→12.6 · 17.1→12.6.1 — **بلا شطر** | إحالاتُ الملحق الداخلية نفسُها (جدول §12) تحيل إلى «الحمل والتزامن (§12.2–12.3)» وهما عنوانان قائمان فعلًا (الحمل ثم سيناريوهات التضارب) — فالترقيمُ يطابقها بلا إعادة تأليف | لو قُصد شطرٌ آخرُ فهو تحريرُ عناوين لا يغيّر مضمونًا |
| J-03 | ⓪ | D-02 | إعادةُ ترقيم TKT-01: هل تُحدَّث إحالاتٌ داخليةٌ أخرى غير المنصوصَين؟ | ③ | مسحتُ كلَّ `§n` في الوثيقة: الإحالةُ الرقميةُ الوحيدةُ هي `مؤشرات §8` في سطر WatchTowerService — لا غيرها | grep على النص المستخرج كاملًا | لو وُجدت إحالةٌ ضمنيةٌ بالاسم لا بالرقم فهي سليمةٌ أصلًا لأن العناوين لم تتغير |
| J-04 | ⓪ | مخرَج الموجة | صيغةُ «النسخ المصحَّحة»: docx أم نصٌّ مستخرَج؟ | ③ | استخراجُ النص إلى `*.extracted.md` والتصحيحُ في `*.corrected.md` مع `*.DIFF_REPORT.md` لكل وثيقة | البرومت §1① يقول «نصوصُها مستخرجةٌ في docs/sources/» — فالنصُّ المستخرَجُ هو صيغةُ العمل، وإعادةُ حقن docx تُفسد التنسيق ولا تضيف | إن أراد المالكُ docx مصحَّحًا فالتحريرُ اليدويُّ من تقرير الفروق دقائقُ معدودة |
| J-05 | ① | ORG-02 | `head_person_id`: عمودٌ مشتقٌّ أم حذفٌ وقراءةٌ بدالة؟ ثم كيف يُعرف «رأسُ» الوحدة؟ | ① ثم ③ | حذفُ العمود كليًّا + منظرُ `v_org_unit_heads` + عمودُ `is_unit_head` في أنواع التكليف (الخمسةُ المركزيةُ ومديرُ الحركة) | الوثيقةُ نفسُها تجيز الخيارين وترجّح مصدرَ حقيقةٍ واحدًا؛ وتمييزُ أنواع الرأس ضروريٌّ لأن الوحدة تحمل تكليفاتِ ضباطٍ ليسوا رأسَها | لو أُريد اشتقاقٌ آخرُ فالمنظرُ يُستبدل بلا مساسٍ بالبيانات |
| J-06 | ① | ORG-04 | لأي شركةٍ تُبذر الوحداتُ الأربعَ عشرة؟ | ③ | للشركة 4 (ايكوبيشن) وحدَها | كلُّ المواقع (19) والمستخدمين والتشغيل في الشركة 4 — والشركة 1 قشرةٌ مجانيةٌ بلا نشاط | إن لزم مستأجرٌ آخرُ فالبذرُ WHERE NOT EXISTS يعاد بشركته |
| J-07 | ① | ORG-05 | `validity_hours` للأنواع التسعة — لا قيمةَ في الوثيقة | ④ | 24 ساعةً افتراضًا · و48 لخروج العامل النهائي (تسويةُ عهدٍ تطول) — صفوفٌ قابلةُ التعديل | الوثيقةُ جعلتها عمودًا بلا قيمٍ؛ ومهلةُ يومٍ عرفٌ متعارفٌ لتصاريح المواقع | تعديلُ صفٍّ لا كود |
| J-08 | ① | ORG-05 | «تخصيصُ مشغّلٍ قائم» في إذن إدخال الخدمة (§5②) — شرطٌ أم موافقة؟ | ③ | صفُّ موافقةٍ بدور `operators` في المصفوفة — وPermitGate (موجة ③) يتحقق منه تحققًا آليًّا من التخصيصات | نمذجةُ §5 «صفوفٌ لا كود» تستوعب الشرطَ صفًّا؛ وفصلُه كودًا يخالف قاعدةَ الجدولية | إن قيل موافقةٌ بشريةٌ زائدةٌ فحذفُ صفٍّ يحلّها |
| J-09 | ① | البنية | أسماءُ قيود FK — `fk_rq_type` مصطدمٌ بقيدٍ قائمٍ في `transfer_requests` | ③ | بادئةُ `fk_permit_*` لقيود جداول الأذونات | أسماءُ قيود MySQL فريدةٌ على مستوى القاعدة | لا أثر |
