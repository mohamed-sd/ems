# UAT · بند التحصين الستة — محضر (2026-08-12 12:09:16)

> «التحصين قبل الاثنتين» (الملحق §12.5) — يُنفَّذ قبل الوظيفية والحمل.

| # | البند | النتيجة | التفصيل |
|---|---|---|---|
| 1 | ① فصل الحسابات | ✔ محصَّن | ems_app محجوب عن mysql.* — حساب مقيَّد لا root — `ems_app / ems_migrator منفصلان في .env` |
| 2 | ② حجب التهيئة | ✔ محصَّن | setup_once.php معطَّل (die قبل أي فعل) — `admin/setup_once.php` |
| 3 | ③ عزل العدة | ✔ محصَّن | .sql/.log/.bak محجوبة في .htaccess — `CLI-only: كل tests/tools تفحص PHP_SAPI` |
| 4 | ④ حماية النماذج (CSRF) | ✔ محصَّن | CSRF مركزي محقون آليًّا · إنفاذ: /Finance/,/FinRequests/,/Contracts/,/Approvals/,/Tickets/ — `includes/security.php` |
| 5 | ⑤ استرجاع منفَّذ فعلًا | ✔ محصَّن | لقطة أُخذت (33 صفًّا) وصفٌّ أُدرج وحُذف واستُعيد العدد — الاستعادة مختبَرة لا موصوفة — `database/backups` |
| 6 | ⑥ المصدر الثاني للأسرار | ✔ محصَّن | .env + .env.example (المصدر الثاني القالب) · صفر سر في الكود — `includes/env.php — ems_env()` |

**الحكم:** **التحصين مكتمل** — يُسمح ببدء الوظيفية والحمل.

## ملاحظة الحسابات (N-23-ب — بند التحصين المشترك)

الحسابان `ems_app` و`ems_migrator` منفصلان ونافذان (القياس الحي أعلاه أثبت حجب التطبيق عن `mysql.*`). تعليقُ `.env` القديم عن «root مؤقتًا» **متقادمٌ** — والقيمة الحية `ems_app`. **المتبقي لتحصين الإنتاج (يُسلَّم ولا يُنفَّذ ذاتيًّا §1.3-⑤):** تدوير كلمتي الحسابين + ملء `FINANCE_CRON_KEY`/`TRANSPORT_CRON_KEY` الفارغين + إلغاء أي حساب عام + مصدر أسرار ثانٍ خارج الملف (خزنة).
