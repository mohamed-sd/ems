# سجلُّ عيوب التجربة التشغيلية الشاملة

> الإصدارُ المثبَّت: `de384aa` · البيئة: `equipation_manage` (MySQL 8.4.7 · WAMP) · الوسم `UAT-2026`
> التصنيفُ الخماسي: ① كسرٌ يمنع الدورة · ② خطأٌ في الحساب أو الأثر · ③ فراغُ سياسة · ④ خللُ شاشةٍ أو رسالة · ⑤ ملاحظةُ تحسين
> **القاعدة:** لا يُصلَح عيبٌ بصمتٍ أثناء التجربة — يُوثَّق ثم يُصنَّف ثم يُصلَح ثم تُعاد الدورة.

---

## UAT-DEF-001 · أداةُ النسخ الاحتياطي معطَّلةٌ صامتة

| البند | التفصيل |
|---|---|
| **الصنف** | ① — يعطّل ضابطَ «التنظيف بالاستعادة» الذي تقوم عليه التجربة كلُّها |
| **الموضع** | `admin/includes/db_tools.php:20` — `ems_env('MYSQL_BIN_DIR', 'C:/xampp/mysql/bin')` |
| **الواقع** | الجهازُ لا يحوي `C:/xampp` أصلًا؛ خادمُ القاعدة الحيُّ **MySQL 8.4.7** في `C:/wamp64/bin/mysql/mysql8.4.7/bin`. فأيُّ نسخةٍ من لوحة المدير الفائق تفشل قبل أن تبدأ |
| **الأثر** | لا نسخةَ احتياطيةَ من الشاشة · ولا استعادة · والجدولةُ (`backup_schedule.json`) معطَّلةٌ أصلًا (`enabled:false`) |
| **سببُ الجذر** | افتراضٌ مكتوبٌ لبيئة XAMPP ولم يُعدَّل لبيئة WAMP، ولا فحصَ وجودٍ عند الإقلاع يُنبّه |
| **الإصلاحُ المنفَّذ** | `MYSQL_BIN_DIR=C:/wamp64/bin/mysql/mysql8.4.7/bin` في `.env` (إعدادُ بيئةٍ لا تغييرُ سلوك) |
| **المقترحُ المؤجَّل** | فحصُ وجودِ الأداة عند فتح الشاشة برسالةٍ صريحة بدل الفشل عند الضغط |
| **الحالة** | **مُصلَحٌ جزئيًّا** — ويبقى `UAT-DEF-003` مانعًا |

## UAT-DEF-002 · النسخةُ الاحتياطية الوحيدة على القرص لا تمثّل القاعدة

| البند | التفصيل |
|---|---|
| **الصنف** | ① — شبكةُ الأمان الوحيدة كانت وهمية |
| **الموضع** | `storage/backups/ems_backup_20260802_124859.sql` (17.8 م.ب · «Server version 10.4.32-MariaDB») |
| **الواقع** | تحوي **315 كائنًا** والقاعدةُ الحيّة **364**. الناقصُ **49**: `client_contracts` (منظر) · كلُّ جداول ORG-01 (`org_units` · `org_assignments` · `assignment_*`) · كلُّ جداول SEC-01 (`permission_*` · `template_permissions` · `sensitive_*` · `sod_conflicts`) · نصفُ جداول TKT · `permit_*` · `persons` · `founding_mode` · **وجدولا `uat_runs`/`uat_evidence` نفساهما** |
| **الأثر** | **استعادتُها كانت ستمحو طبقاتِ `update0004` كلَّها ومعها كلُّ العقود** (`client_contracts` منظرٌ فوق `contracts`) |
| **سببُ الجذر** | الملفُّ من عميل MariaDB 10.4 على خادم MySQL 8.4 — ومحتواه يسبق إنشاءَ الجداول الأربعة والأربعين. لا فحصَ اكتمالٍ بعد النسخ (لا مقارنةَ عددِ كائنات) |
| **الإجراء** | لقطةٌ صحيحةٌ أُخذت وتُعتمد مرجعًا: `storage/backups/uat_pre_20260802_snapshot.sql` (18.1 م.ب · 358 جدولًا + 6 مناظر) · **واستعادتُها مختبرةٌ فعلًا** في قاعدةٍ منفصلة بمطابقة أعداد الصفوف في 15 جدولًا رئيسيًّا |
| **المقترح** | لا تُعتمد نسخةٌ إلا بعد فحصِ اكتمالٍ آليٍّ (عددُ الكائنات = عددُ القاعدة)، وتُوسم الناقصةُ أو تُحذف |
| **الحالة** | **مفتوح** — والملفُّ القديم يجب **ألّا يُستعمل للاستعادة** |

## UAT-DEF-003 · مستخدمُ التطبيق لا يقدر على نسخةٍ كاملة

| البند | التفصيل |
|---|---|
| **الصنف** | ② — ينتج ملفًّا يبدو ناجحًا وهو ناقص |
| **الموضع** | `admin/includes/db_tools.php:83` — الأداةُ تكتب ملفَّ الاعتماد بـ`DB_USER` أي `ems_app` |
| **الواقع** | `mysqldump` بحساب `ems_app` **يتوقف بالرمز 2** عند أول منظر: `SHOW VIEW command denied to user 'ems_app'@'localhost' for table 'client_contracts'` — والمناظرُ الستة تسبق نصفَ الجداول ترتيبًا |
| **الأثر** | كلُّ نسخةٍ من الشاشة ناقصةٌ أو مبتورة · **وهذا على الأرجح أصلُ `UAT-DEF-002`** |
| **سببُ الجذر** | الحسابُ المقيَّد (ADR-04/REV-01) مُنح `SELECT` ولم يُمنح `SHOW VIEW`/`LOCK TABLES`/`PROCESS` — وهي لازمةٌ للنسخ لا للتشغيل |
| **المقترح (قرارُ مالك)** | حسابٌ ثالثٌ مخصَّص `ems_backup` بـ`SELECT, SHOW VIEW, LOCK TABLES, EVENT, TRIGGER` تستعمله الأداةُ عبر مفتاحٍ جديد في `.env` — **لا** توسيعُ صلاحيات `ems_app` |
| **الحالة** | **مفتوح — ينتظر قرارًا** (لم يُنفَّذ شيء: تغييرُ صلاحياتٍ قرارُ مالك) |

## UAT-DEF-004 · جدولةُ النسخ الاحتياطي مطفأة

| البند | التفصيل |
|---|---|
| **الصنف** | ⑤ |
| **الواقع** | `storage/backups/backup_schedule.json` → `enabled:false` · `last_run_at:null` — فلا نسخةَ دوريةً قطُّ |
| **الحالة** | مسجَّلٌ للتقرير — والقرارُ للمالك بعد الإطلاق |

---

## ملاحظاتٌ للفحص في محطاتها (لا عيوبٌ بعد)

| # | الملاحظة | تُحسم في |
|---|---|---|
| ن-١ | `client_contracts` **منظرٌ** فوق `contracts` يحمل `project_id` و`site_id` معًا — أيُّهما حاكم؟ | المحطة ④ (الأبوّة التعاقدية) |
| ن-٢ | `op_containers` يحوي `contract_id=1653` و`contract_id=0` بينما أقصى عقدٍ حيٍّ 810 — مراجعُ معلَّقة؟ | المحطة ⑥ (الحاويات) |
| ن-٣ | `seat_assignments` جدولٌ فارغٌ تمامًا والمقاعدُ تُخزَّن في `op_containers` — جدولٌ ميتٌ أم مسارٌ غيرُ مُفعَّل؟ | المحطة ⑥ |
| ن-٤ | `sites` مطابقٌ للمشاريع واحدًا لواحد — فلا مشروعَ متعددَ المواقع في القاعدة | المحطة ④ |
