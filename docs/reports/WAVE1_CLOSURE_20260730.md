# إغلاقُ الموجة ① «نواةُ الحقيقة» — مكتملةٌ تقنيًّا · تنتظر إعلانَ المالك

> PLAN-01 §12.2: «لا يعلن اكتمالَ طبقةٍ من بناها» — هذا ملفُّ الفحص (§11 من أمر التنفيذ). 2026-07-30.

## شرطُ الإغلاق (أمرُ التنفيذ §5-الموجة①) بحالته المقيسة

| الشرط | الحالة | الدليل |
|---|---|---|
| أعمدةُ الحدث مكتملةٌ ومعبّأةٌ رجعيًّا | ✅ | M-38 (27/0 · 9 قيود) + H-12 (45/0 · تعبئة 45/45 حدثًا) |
| مفتاحُ منع التكرار فعّالٌ **ومختبَرٌ بإعادة إرسالٍ فعلية** | ✅ | N-01 (14/0 · ×2 على 4 مسارات · مصفوفة 13 مسارًا — الـ13 مؤجَّلٌ لH-13 معلَنًا) |
| سجلُّ التدقيق على العقود والقيود والصلاحيات | ✅ | N-02 (26/0 · E2E عقدٌ حقيقي) — و403 مشرف المورد تسجَّل به (H-20) |
| قفلُ الفترة في كل مسار | ✅ | M-39+M-08+M-33 (19/0 · الناشرُ نقطةَ خنق) |
| صفرُ حركةٍ بلا معادل | ✅ | M-40 (19/0 · تعبئةٌ شاملة · إعادةُ تقييمٍ دورية) |
| عزلُ مشرف المورد | ✅ | H-20 (52/0 · fail-closed · 403/404 مسجَّلتان · بوابةٌ في الطبقة) |
| التحميلُ الخادمي في الجداول الكبيرة | ✅ | H-22 (34/0 · سجلُّ النشاط 13,155 صفًّا صار صفحةَ 50 · دفترُ الناقل بلا LIMIT صار serverSide · الافتراضُ العام 50/400ms) |
| قالبُ الجرد معتمد | ⏳ **مكتملٌ تقنيًّا — ينتظر توقيعَ المالك** | H-23 (القالبُ + 13 بطاقةً مقيسة) — الانتظارُ لا يوقف العمل (§11) |
| خطةُ الترحيل موثَّقةٌ **بعلَم رجوعٍ مختبَر** | ✅ | N-04 (`rollback_flag_test` 11/0 · قائمةُ نطاقٍ مختبرة · رجوعٌ فعليٌّ سابقٌ موثَّق DOC_EXPIRY) |

## مؤشرات PLAN-01 §14
- مهامٌّ بدأت ومانعُها مفتوح: **0** · مهامٌّ أُعيد فتحُها بعد الإنجاز: **0**

## الانحدارُ عند الإغلاق
`supplier_portal_guard` 52/0 · `datatable_server` 34/0 · `rollback_flag` 11/0 · `tenant_leak` 130/0 · `publisher` 54/0 · `fes` 45/0 · `period` 19/0 · `journal` 27/0 · `base_equivalent` 19/0 · `fx` 45/0 · `audit` 26/0 · `resend` 14/0 · `unified_nav` 16/0 · `ts-service` 50/0 · `doc-guard` 41/0 — **صفرُ أحمرَ جديد**.

## قائمةُ الاجتهادات الحاكمة (تفصيلُها في سجل التنفيذ لكل مهمة)
H-20: نطاقُ الموجة عزلٌ لا بوابةٌ كاملة (الوريثُ H-15) · H-22: الكبيرتان أولًا والتعميمُ مع عقد الشاشة · H-23: الألمُ الميدانيُّ «يُستكمل» بمالكٍ وموعد · N-04: الخطةُ قوننةُ العرف المنفَّذ لا اختراعُ نمطٍ جديد.

**التالي:** الموجة ② — البدءُ بـ`H-05` (الموقع/المنجم كيانًا مستقلًّا — لا مانعَ له) ثم `N-03` فوقه.
