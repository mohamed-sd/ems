# MIGRATION_PROGRESS — سجلّ تقدّم الهجرة المعمارية (Checkpoint & Resume)

> ملف حالة دائم. يُحدَّث **بعد كل بند**. إن انقطع العمل: اقرأ هذا الملف أولًا، اعرض سطر
> «توقفنا عند [المرحلة/البند] — الخطوة التالية [كذا] — هل أكمل؟» ثم تابع من نفس النقطة.
> الكود هو الحقيقة؛ لا تفترض حالة بندٍ اعتمادًا على آخر.

---

## الحالة الآن
- **المرحلة 0: مغلقة ومقبولة من المستخدم بالكامل (P0-1..P0-4) — 2026-07-08** ✅.
- **المرحلة 1: قيد التنفيذ** — الخطة **مُقرَّة** (`docs/PHASE1_PLAN_ar.md` §7: القرارات الخمسة + التأكيدات الثلاثة، ومنها برهان NULL/UNIQUE العملي).
- **البند الحالي**: **K10 مفتوحة (2026-07-09) — قلب EMS_ACTION_GUARD بالمعيار المختلط الثلاثي؛ لا قلب اليوم** (معياران غير مستوفيين — التفصيل في قسم K10 أدناه). M2 أُغلقت وقُبلت (2026-07-08)؛ عتبة K9 للمرحلة 1 مكتملة: 3 وحدات. **بعد K10 تُغلق المرحلة 1 كاملة → التقرير الختامي (الإنجاز + الزمن من git log)**.
### K10 — قلب حارس الفعل (ADR-06) بالمعيار المختلط — قيد الاستيفاء
- **المعيار الثلاثي (يُثبَت معًا لا الزمن وحده)**: (1) ≥7 أيام مراقبة؛ (2) صفر would-block **حقيقي** بدلالة نشاطٍ كافية لا خلوٍّ أجوف؛ (3) تحليل ساكن يؤكد حراسة كل مسار action.
- ✅ **المعيار 3 مكتمل (2026-07-09)**: 34 قيد سجل ↔ 34 ملفًا مطابقًا لمُسند الاعتراض (get_*/*_handler)؛ صفر غير مسجَّل، صفر stale؛ **33 نقطة فعلية كلها تضمّن config.php** (نقطة الاعتراض تسبق أي كود معالج) + 1 إيجاب كاذب موثَّق (contracts/contractequipments_handler.php **مكتبة دوال** بلا جسم تنفيذي — قيدها دفاع إضافي)؛ عموميّان واعيان (chats get_messages/get_unread_count). حدّ نطاق موثَّق: الحارس يغطي سطح AJAX (سطح R3)؛ الصفحات العادية تحمل فحوص صلاحياتها داخلها.
- ✅ **المعيار 2 مستوفى دلالةً (2026-07-09 — قرارا المستخدم نافذان)**: (1) **إصلاح الشارة**: `get_breakdown_count` عمومي-بوعي (الخيار أ، commit 0c5b0f4) — وفاء ذهبي لسلوك الشارة المعروضة للجميع؛ إخفاؤها بشرط صلاحية = قرار مُلّاك منفصل لاحقًا (موثَّق في تعليق السجل). **إثبات الاختفاء فعليًا**: 5/5 استطلاعات بدور المبيعات نظيفة (200+JSON، صفر قيد جديد)، وعينة تحكم (open_orders_count الباقية view) رُصدت denied لنفس الدور = الحارس حي والpublic لم يعطّله. (2) **مصفوفة دور×نقطة** (`tests/action_guard_matrix.php`، commit 34861b7): 18 دورًا × 34 نقطة × الأفعال = **882 خلية: 404 مسموح، 478 دفاع-عمق، صفر would-block حقيقي** — التصنيف ببوابات الصفحات الحقيقية (شاشات Approvals بقائمة ثوابت ADR-07 الصريحة؛ النداء النسبي المجرد لا يعبر الموديولات)؛ أدوار سلسلة الاعتماد {1..5} نظيفة على النداء العابر approvals→timesheet. **ضجيج موثَّق في السجل**: قيد denied واحد بتاريخ 2026-07-09 (u13 على open_orders_count) = مجسّ التحكم المتعمد؛ قيود 2026-07-07 = 6 عدّة ADR-06 + 12 الشارة المعالَجة.
- ✅ **المعيار 1 — أُغلق بتنازلٍ صريحٍ موثَّق (2026-07-09 00:25)**: طُلب «أكمل K10» مرتين والنافذة في يومها الثاني؛ **رفضتُ القلب مرتين** (المعيار ملزم) وعرضتُ الخيارين صراحةً؛ **قرار المالك النصي: «تنازل صريح — اقلب الآن»** — المسوّغ: دلالة مصفوفة 882 خلية (صفر would-block حقيقي) + إصلاح الشارة المُثبَت + سجل نظيف منذ 07-09، بدل أيامٍ عضوية على جهاز تطويرٍ شبه صامت.
- ✅ **K10 مُنفَّذة ومغلقة (2026-07-09 00:25)**: مسح نهائي نظيف (صفر قيود منذ 07-09) → `EMS_ACTION_GUARD=enforce` → **تحقق حي ثلاثي 5/5**: مخوَّل يمر (u19 صيانة على open_orders_count ‏200+JSON)؛ غير مخوَّل يُحجب (u13 مبيعات نفس النقطة **403 JSON منظم + قيد enforce/denied مسجَّل**)؛ الشارة تعمل لدور بلا صيانة (public نافذ تحت الإنفاذ). دخان الصفحات العادية: golden وحدة النقل — فرق وحيد +31B في transfer_dashboard **بُرهن بيئيًا بمنهج A/B** (تحت monitor الرقم ذاته 37993 — عبور منتصف الليل غيّر حسابات CURDATE في لوحةٍ حية؛ نمط K2 المسجَّل)، والست الأخريات مطابقة. **Rollback القائم**: إعادة monitor سطرًا واحدًا بلا نشر؛ مراقبة لصيقة لسجل enforce/denied أول يوم.
- 🏁 **المرحلة 1 مغلقة كاملةً (2026-07-09)**: ‏K1..K10 كلها مقبولة — البوابة الخماسية القدرات مبنية مختبرة (117/117) ومُنفَذة على 3 وحدات (M0/M1/M2 — 21 ملفًا خلف البوابة بصفر نداء خام)، ناقل الأحداث حي (K3/K4/K5 exactly-once)، جسر المنصب وآلة الحالة (K6/K7)، الترقيم الخادمي (K8)، وحارس الفعل مُنفَذ (K10). **التقرير الختامي مقبول** (`docs/PHASE1_CLOSURE_REPORT_ar.md`، commit 39a0ea7).
### رقابة K10 المضغوطة (أول يوم استخدام فعلي تحت الإنفاذ) — 🟡 مسلَّحة، التأكيد النهائي معلَّق
- **الحالة (2026-07-09 03:33)**: صفر حجب عضوي منذ القلب (00:28)؛ قيدا enforce/denied الوحيدان في السجل كله = المجسّان الموثَّقان (lowbot ‏07-07 قبول ADR-06 + u13 مجس تحقق القلب). **يوم الاستخدام الفعلي لم يبدأ** (ليل) — التأكيد النهائي بعد أول يوم عمل.
- **أمر الفحص**: `grep 'mode=enforce kind=denied' logs/security.log` — **تنبيه توقيت**: طوابع السجل ≈ المحلي −3 ساعات. أي قيد جديد لدور شرعي = مسار فاتَ المصفوفةَ → يُصنَّف فورًا: حجب مسار مشروع ⇒ rollback بسطر (`EMS_ACTION_GUARD=monitor`)؛ دفاع عمق سليم ⇒ يُوثَّق هنا. بعد يوم نظيف: يُعلَن استقرار K10 نهائيًا.
- **قيد تسلسل**: تخطيط المرحلة 3 مسموح؛ **لا تنفيذ (تحويل) قبل إعلان الاستقرار**.
### المرحلة 3 — التخطيط مفتوح (لا تنفيذ): الجرد المقارَن للوحدات الست (2026-07-09)
- **القياس** (grep موحَّد كعدّة T3 + سجل البوابة): Timesheet ‏12ملف/44خام/91JOIN (أثقلها timesheet.php ‏25 JOIN — **جوار مقدس**: الموافقات تقرأ منه وخطاف K5 فيه) · Contracts ‏7/23/14 (الأصغر؛ 3 مسارات عقود + دين L-2 + contractall خارج السجل) · Equipments ‏18/104/58 (كيان مركزي يُثري الجميع؛ خطافات سجل الحركة في 5 ملفات خارجية) · Employees ‏14/60/42 (SSOT ما بعد التوحيد) · Maintenance ‏9/104/30 (كثيف؛ محور حالة مزدوج + **كتّاب عابرون**: movement يفتح mnt_order آليًا؛ 6 جداول بلا soft) · Finance ‏23/182/25 (**الأكبر خامًا** — يعادل M0+M1+M2 مجتمعة تقريبًا؛ كود جديد موحّد النمط، أعقد استعلام 3 JOINs فقط، 19/29 جدولًا soft أصلًا، مستخدموه 6 أدوار جديدة، cron جاهز لنمط forSystem).
- ✅ **قرار المستخدم (2026-07-09): Finance أولى وحدات المرحلة 3** — والترتيب بعدها معتمد (Maintenance → Equipments/Employees → Contracts → Timesheet أخيرًا بعزل M2c لجواره المقدس).
- ⚠️ **تحفّظ ملزم على Finance (نصّ المستخدم)**: الوحدة موطن نظام الأحداث نفسه (fin_financial_events · events_list · الناشر والمستهلك الحيّان) — جرد Finance المفصّل **يميّز صراحةً** بين شاشات Finance العادية (تُهاجَر كالمعتاد) و**المكوّنات الحية للناقل** (تُصان بحذر خاص، لا تُلمس كشاشة). هجرة الوحدة **يجب ألا تكسر الناشر/المستهلك العاملين الآن**.
- 🚧 **التسلسل الملزم قبل أي تحويل (بوابتان)**:
  1. **بوابة الاستقرار**: مرور أول يوم عمل فعلي نظيفًا تحت الإنفاذ → إعلان استقرار K10 (آخر فحص بيني 2026-07-09 03:39: ما يزال القيدان الموثَّقان فقط).
  2. **بوابة الخطة**: عرض جرد Finance المفصّل (نمط جرد M1/M2: توزيع النداءات، الأنماط، حدود الناقل الحي، تقسيم M3a/b/c إن لزم) → **إقرار خطة M3-Finance معًا**.
- **بروتوكول الاستئناف**: عند «أكمل» بعد يوم العمل — فحص `mode=enforce kind=denied` (طوابع السجل ≈ المحلي −3س) وتصنيف أي جديد؛ إن نظف → إعلان الاستقرار ثم عرض الجرد المفصّل والتوقف لإقرار الخطة.
- **خطة Rollback (جاهزة)**: سطر واحد في .env — ‏`EMS_ACTION_GUARD=enforce` → ‏`monitor`؛ نافذ من الطلب التالي بلا نشر كود (نمط CSRF/DDL المجرَّب). سطح الانفجار عند الإنفاذ محصور: 403 JSON على نقاط AJAX فقط؛ الصفحات لا تتأثر؛ CLI مستثنى؛ المدير الأعلى يمر.
- **K9-D معتمد رسميًا من المستخدم**: runInTransaction (§9) وscopedQuery (§10) قدرتان دائمتان في عقد البوابة.
- **M2a منفَّذة (2026-07-08، commit b7d0eea)**: soft-أعمدة (33) للجداول الأحد عشر بنمط K2 الكامل — dump شامل (`transport_11_pre_M2a`) + **up/down مُختبَران على نسخ الأحد عشر معًا (diff بنية مطابق حرفيًا)** + التطبيق عبر المشغّل (273ms) + قلب السجل soft=true×11 + **golden الشاشات السبع مطابق حرفيًا** (transport_mgr u78) + الحزمة 81/81. down: `M2a_down_transport_soft_delete.sql`.
- **📋 سجل الفجوة المرصودة (قيد المستخدم — يمنع إغلاق M2)**: الجداول الأحد عشر كلها الآن soft **وشاشاتها خام لا تفلتر is_deleted** — الفجوة نظرية حتى أول softDelete (لا صف مؤرشف بعد؛ أول كاتب = شاشات M2b/M2c المهاجرة التي تقرأ عبر البوابة ففلترتها آلية). تُشطب كل شاشة من هذا السجل عند هجرتها: [~~configs×3~~ ✅ · ~~requests~~ ✅ · ~~orders_list~~ ✅ · ~~dashboard~~ ✅ · ~~cron~~ ✅ — M2b] · [~~order_form~~ ✅ M2c]. **السجل فارغ — شرط إغلاق M2 مستوفى (2026-07-08)** ✅.
- **خطة M2 المعتمدة**: M2a ✅ → **M2b جارية** → M2c (god-file بنقطة توقف خاصة).
### حالة M2b اللحظية (لو انقطعتُ الآن)
- ✅ **trs_helpers.php محوَّل** (commits 555e035+2c91382): trs_gate + gen_code/gen_order_no (count includeDeleted) + 8 قوائم عبر البوابة (**بلا جسر** — لا مستدعي خارجيًا، درس M1) + project_days بفلتر deleted_at الأصلي حرفيًا + compute_bearer بانتقاء PHP (ORDER BY التعبيري خارج صرامة البوابة) + log_event وsave_proof عبر البوابة. **وفاء موثَّق**: suppliers بincludeDeleted (موردان مؤرشفان يظهران أصلًا). **T3=1 متبقٍ = trs_notify حصرًا** (INSERT IGNORE بdedupe — مؤجلة عمدًا لقرار forSystem لأن مستدعيها الوحيد الـcron). golden السبع مطابق حرفيًا بعد كل خطوة. **درس أمانة**: رسالة commit ادّعت الحصر قبل التحقق (كانت دالتين) — صُحّحت فورًا.
- ⬜ **التالي بالترتيب (نمط M1 المستنسخ، الأساس `m2a_golden_after.txt`)**: transfer_types_config (4ق؛ حذفه الصلب → softDelete — أول تطبيق سياسة الأرشفة على master!) → trs_locations_config (4) → transfer_cost_rules_config (4) → transfer_requests (7) → transfer_orders_list (1 بـ4 joins → scopedQuery) → transfer_dashboard (5؛ 3 GROUP BY → scopedQuery + عدّادات) — لكلٍّ: تحويل/T3=0/golden/commit، والزرع §7 والإنفاذ الاسمي بنهاية M2b.
- ✅ **forSystem مبنية ومقرَّة** (commit 49df2b8): حوكمة TenantContext::forSystem (تفويض CLI/مفتاح متحقق؛ الويب يُرفض؛ أثر مزدوج مسجَّل) + اختبارات 6و f1-f3 + عقد §11. **الحزمة 87/87**.
- ✅ **cron_transfer.php محوَّل وحي** (commit fefa6a5): تعداد الشركات بسياق نظام super+forAllTenants (لا خام)؛ بوابة forSystem($cid) معزولة لكل دورة تُحقن بtrs_gate_override؛ trs_notify عبر البوابة **بالتقاط 1062** (ترجيح المستخدم تأكد — دلالة INSERT IGNORE حرفيًا)؛ ترطيب ثنائي بدل JOIN التصاريح؛ **T3=0 على cron+helpers معًا**؛ تشغيل حي: تنبيهات حقيقية (delayed=2, sixty_day=2) ثم **أصفار عند الإعادة (dedupe مُثبَت)**.
- ✅ **transfer_types_config محوَّل** (commit 359ab2b): **أول softDelete على master بسياسة الأرشفة** — الفرق الدلالي المقصود موثَّق (كان FK RESTRICT يمنع حذف المستعمَل؛ الأرشفة تنجح دائمًا والمراجع التاريخية سليمة والنوع يختفي من قوائم البوابة). T3=0، golden 26750 مطابق. **يُشطب من سجل الفجوة**.
- ✅ configs الثلاث محوَّلة (types 359ab2b · locations 07a1fbe · cost_rules cb8c5c7): softDelete بالسياسة (الفرق الدلالي عن FK RESTRICT موثَّق في types ومُشار إليه في الأخريين)؛ T3=0؛ goldens مطابقة (26750/31459/30350). **تُشطب الثلاثة من سجل الفجوة**.
- ✅ **transfer_requests.php محوَّل** (commit 1e5d415): gen_req_code بcount includeDeleted (فرادة الكود تُحسب على كل الصفوف تاريخيًا)؛ create/approve/reject عبر البوابة (approve/reject بقفل تفاؤلي `state='submitted'` كالأصل)؛ **convert بمعاملة مُدارة §9** — كتابتا الأصل (إدراج الأمر + تحديث الطلب converted/order_id) كانتا غير معاملتين: تقويةُ مسارِ فشلٍ موثَّقة، المسار السعيد مطابق؛ القائمة عبر scopedQuery (r.* + إثراء types/project). T3=0، golden 33211 مطابق. **يُشطب من سجل الفجوة**.
- ✅ **transfer_orders_list.php محوَّل** (commit ea4cb67): الاستعلام الوحيد (4 إثراءات LEFT) عبر scopedQuery بالنص الأصلي حرفيًا + الرمز؛ حُذف `$scope` اليدوي الميت. T3=0، golden 28255 مطابق. **يُشطب من سجل الفجوة**.
- ✅ **transfer_dashboard.php محوَّل** (commit 2fddf2d): 7 عدّادات COUNT عبر `count(whereRaw)`؛ SUM/AVG عبر `trs_scoped_scalar` (scopedQuery)؛ 3 GROUP BY + كانبان المراحل الخمس عبر scopedQuery (الرمز قبل GROUP BY بعمق 0؛ مرحلة الكانبان بمعامل `?` بدل escape يدوي)؛ حُذف trs_scalar الخام (صفر مستدعين خارجيين — grep قبل الحذف، درس M1). T3=0، golden 37962 مطابق. **يُشطب من سجل الفجوة**.
- ✅ **ختام الوحدة**: زرع §7 للشاشات الست (u78 دور 23؛ transfer_orders لشاشتين، والعلامة في basis_note لcost_rules — النص الحر الوحيد المعروض؛ FK لا يفرض تطابق الشركة فزرع الشركة الأخرى ممكن) — **الحزمة 117/117** (87 + 30 شاشيًّا) · **الإنفاذ الاسمي**: الشاشات الست أُلحقت بTENANT_ENFORCE_PATHS (helpers تُنفَّذ تحت SCRIPT_NAME الشاشات؛ الـcron خارج مطابقة المسار أصلًا — صرامته من قناة forSystem الfail-closed ذاتها؛ **order_form ليس في القائمة عمدًا** حتى M2c) · **golden الوحدة الختامي: السبع مطابقة حرفيًا للأساس تحت الإنفاذ** · **T4: صفر would-deny لأي مسار نقل** في security.log (الموجود = مجسّات 6ب المتعمدة من ملف الاختبار).
- ✅ **Checkpoint M2b مقبول من المستخدم (2026-07-08)**: الوحدة الخفيفة كاملة 8/8.
### M2c — transfer_order_form.php (god-file 667 سطرًا · 58% من مخاطر الوحدة) — جارية
- **شروط المستخدم الخمسة**: (1) جرد وتصنيف الـ24 نداءً قبل أي تحويل؛ (2) تمييز المترابط ذرّيًّا (runInTransaction) عن المستقل — لا لفّ الكل ولا ترك ترابطٍ حقيقي؛ (3) الحذف المفرد → softDelete بالسياسة (لا replaceChildren) **مع تأكيد حي**؛ (4) تقسيم داخلي بcommits متعددة بدل commit عملاق؛ (5) golden صارم بعد كل خطوة داخلية. **الخطة تُعرض وتُقرّ قبل التنفيذ.**
- ✅ **الجرد مكتمل (2026-07-08)**: 24 نداءً = 13 قراءة (ملكية ×9 مواضع، حرّاس الانتقالات ×5، عرض: join‑8 للرأس + join‑3 للعناصر + 3 قوائم مسطّحة، كود المركبة) + 7 كتابات مستقلة (رأس إنشاء/تعديل، عنصر، تصريح، انتقال مرحلة، إلغاء) + 3 حذف مفرد (line/cost/permit → softDelete) + **زوجان مترابطان حقيقيان فقط**: add_cost وdel_cost كلاهما + إعادة حساب actual_cost_usd (تابع محلي trs_recompute_actual بsubquery مترابط، مستدعياه في الملف حصرًا).
- **حقائق فنية مثبتة**: جداول الأبناء الخمسة T_TENANT soft=true في السجل (لا T_CHILD)؛ softDelete قناة id-only (شرط ربط order_id الأصلي → تحقّق selectOne منفصل قبله، لا توسيع قناة)؛ جداول الإثراء كلها مسجَّلة؛ golden يقبل query string؛ **الأمر المرجعي للتغطية: id=9 شركة 4** (2 عناصر/3 تكاليف/2 تصاريح/3 أحداث).
- ✅ **الخطة أُقرَّت بتدقيقات المستخدم الأربعة ونُفِّذت كاملة (2026-07-08)**: أثر قبل/بعد لكل كتابة (لا HTTP 200)؛ حقن فشل عدائي للزوج الذرّي؛ سدّ ربط الأبناء؛ golden الموسَّع (bare 30769 + **?id=9 = 53686**) بعد كل C.
- ✅ **C1 قراءات العرض** (commit c374487): رأس join‑8 عبر scopedQuery بالنص الأصلي (8 إثراءات) + عناصر join‑3 (company_id اليدوي سقط) + 3 قوائم عبر select(where order_id). T3 ‏24→19.
- ✅ **C2 الملكية والحرّاس** (819f0fe): owned→count (نقطة تؤمّن 9 مسارات)؛ كود المركبة selectOne (الأصل بلا فلتر شركة — **تشديد عزل موثَّق**)؛ حمولات transition/cancel عبر selectOne؛ حرّاس prepare/close عبر count. T3 ‏19→11.
- ✅ **C3 الكتابات المستقلة** (9a4d000): رأس إنشاء/تعديل، عنصر، تصريح، انتقال (الأعمدة الزمنية بساعة PHP بدل NOW())، إلغاء — **إثبات حي 16/16 بأثر قبل/بعد** (إنشاء بقيمه وحقن الشركة وحدث النظام؛ تعديل يغيّر المعدَّل ويثبّت الباقي؛ حارس plan يرفض بلا كتابة؛ سلسلة plan→prepare→مغادرة بإثبات مرفوع؛ إلغاء بسبب). **درس أمانة**: إخفاق أولي واحد سببه معطى الإثبات (priority خارج ENUM) لا كتابة صامتة — شُخّص وأُثبت وصُحّح الإثبات. T3 ‏11→5.
- ✅ **C4 الأرشفة والزوجان الذرّيان** (a461560): del×3 → softDelete بتحقّق ربطٍ مسبق (سطر أمرٍ آخر لا يُؤرشَف)؛ add_cost/del_cost بمعاملة §9 تلفّ البند+recompute؛ **اكتشاف قناة موثَّق**: رمز {TENANT_SCOPE} يحقن عزل الشركة فقط بلا فلتر soft → recompute بفلتر is_deleted صريح في النص (وإلا بقي المؤرشف في المجموع واختفى من القائمة)؛ runInTransaction يعيد رمي أي فشل ملفوفًا TenantGateException (سلوك عقدي). **إثبات حي 13/13** منها الحقن العدائي: فشل بعد الأرشفة وإعادة الحساب → rollback كامل. **T3=0 على god-file**.
- ✅ **ختام M2c/M2**: transfer_order_form.php أُلحق بTENANT_ENFORCE_PATHS (الوحدة كلها مُنفَذة اسميًا)؛ golden الموسَّع + golden الوحدة السبع **مطابقان حرفيًا تحت الإنفاذ**؛ **T3=0 على ملفات Transport التسعة**؛ الحزمة **117/117**؛ T4 صفر would-deny لمسارات النقل إطلاقًا. §7 لorder_form: استثناء زرعٍ موثَّق (صفحة تفصيل بـ?id — العدّة تزور المسار المجرد؛ التغطية بإثباتات C1–C4 الحية + الإنفاذ)، كنمط استثناء dashboard_proc الموثق في M1.
### تصميم forSystem المعروض (القدرة الخامسة — بمتطلبات المستخدم الأربعة)
1. **شركة واحدة صريحة لكل دورة**: `TenantContext::forSystem($cid)` ينشئ سياقًا أحادي الشركة كسياق مستخدم تمامًا — البوابة تعزل به قراءةً وكتابةً؛ الـcron يدور الشركات تسلسليًا ببوابة جديدة لكل دورة؛ **لا سياق «يرى الكل»** (الرؤية العابرة تبقى حكرًا على forAllTenants بحارسها).
2. **قناة محروسة لا تجاوز**: علم بناء صريح `TenantContext::forSystem(..., $systemAuth)` حيث $systemAuth = (PHP_SAPI==='cli') أو مفتاح cron مُتحقق (hash_equals مع *_CRON_KEY) — **غيابهما = TenantGateException**؛ فمسار ويب مصادَق عادي لا يستطيع تركيب سياق نظام (جلسته تمر بfromSession حصرًا).
3. **مسجَّل كنمط forAllTenants**: كل إنشاء سياق نظام يُقيَّد `tenant_gate_system_context` (company/script/sapi) في security.log.
4. **اختبار تسرّب خاص (قسم 6و)**: دورة forSystem(A) لا ترى B والعكس (قراءة+كتابة)؛ استدعاء بلا تفويض نظام (محاكاة مسار ويب) يُرفض؛ الأثر مسجَّل.
- يُوثَّق §11 في عقد البوابة كخامس قدرة. **لا يُنفَّذ قبل الإقرار.**
- (تاريخي) **✅ M1 مغلقة كاملةً (2026-07-08) — Checkpoint الإغلاق معروض؛ التالي بإذن المستخدم: K9-D النهائي (إقرار القناتين رسميًا) ثم M2 Transport**.
- **خلاصة إغلاق M1**: 11/11 ملفًا خلف البوابة (commits مستقلة e9a498b→3e8937a) · **T3=0 على الوحدة كلها** · **الجسر أُزيل** (شرط الإغلاق — صفر مستدعين، golden العشر مطابق) · قدرتان بقرارَي المستخدم: **runInTransaction** (6د t1-t5: حراسة حية داخل المعاملة، ذرّية مشتركة، ترابط المعرّف الوليد، تبنّي replaceChildren بلا commit ضمني، رفض التداخل — عقد §9) و**scopedQuery** (6هـ e1-e7: عزل نافذ بنيويًا — الرمز في موضع ترشيح المصادر قبل التجميع، UNION/company اليدوي/غير المعلَن مرفوض، تجميع شركتين معزول تجريبيًا — عقد §10) · **issue** عبر المعاملة المُدارة (الأصعب: ترابط line_id + مسح ثلاثي متبنّى + عكس الأثر ذرّيًا في الحذف) · **stock/reordering** عبر scopedQuery بالنص الأصلي حرفيًا · **الحزمة 81/81** · **الإنفاذ الاسمي للأحد عشر** وgolden العشر تحت fail-closed **مطابق حرفيًا للأساس** · صفر would-deny منطوقًا · الحزم الست خضراء · login 200.
- **ثابت كودي موثَّق**: مسح stock_move في issue بref_id+الشركة (القناة أحادية عمود الأب)؛ الكاتب الوحيد اليوم issue وref_type دائمًا 'proc_issue' — كاتبٌ جديد بنوع آخر ⇒ توسيع القناة بشرط معلن.
- (البند السابق تاريخيًا): M1 قيد التنفيذ (الپايلوت M0 أُقرّ؛ إذن M1 صدر بشروط 3).
- **قرار جـ نافذ (2026-07-08)**: الجرد كشف أن حذف سطور proc نمطُ «استبدالٍ كامل» تقني لا حذف أعمال → قناة `replaceChildren` أُضيفت للبوابة بشروط المستخدم الأربعة (نطاق مزدوج إلزامي + ذرّية معاملة + أثر مسجَّل + عقد §8) واختبارات 6جـ الخمسة خضراء **قبل أي استعمال** (commit مستقل). حزمة التسرّب الآن **46/46**.
- **حالة M1 لحظيًا (لو انقطعتُ الآن)**:
  - ✅ golden-before للشاشات العشر مقيَّد (`m1_golden_before.txt` في scratchpad؛ الأطوال: dashboard 28610 · items 38151 · suppliers 31347 · requests 36702 · orders 37494 · receipt 33650 · issue 36481 · stock 21156 · reordering 28770 · master_data 38676) بمستخدم المشتريات u71.
  - ✅ **proc_helpers.php محوَّل** (T3=1 جسرٌ مؤقت موثَّق: `proc_options_from_query` تبقى لثلاث شاشات تمرر SQL خاصًا — orders/receipt/issue — تُزال بعد هجرتها بنهاية الدفعة): gen_code عبر count البوابة (includeDeleted يطابق العدّ الأصلي)؛ دوال القوائم كلها عبر البوابة والتسميات في PHP؛ **is_super عبر forAllTenants الشرعية** (يحفظ سلوك الرؤية العابرة مسجَّلة)؛ التواقيع حُفظت للتوافق أثناء الدفعة. **golden الوحدة كاملة مطابق حرفيًا بعد التحويل**.
  - 🟡 درس جديد للعدّة: أزلتُ دالةً قبل فحص مستدعيها → 3 شاشات 500 التقطتها golden الوحدة فورًا → أُعيدت جسرًا. **قاعدة: grep المستدعين قبل أي إزالة، وgolden الوحدة بعد كل تعديل helpers**.
  - **قرار المستخدم (الخيار أ)**: إكمال الثمانية القابلة بالنمط الحالي أولًا: dashboard (248) → suppliers (267) → receipt (336) → requests (387) → items (393) → master_data (399) → orders (407) → issue (430)؛ ثم عرض **K9-D** بعقده التصميمي التفصيلي في checkpoint نهاية M1؛ **stock + reordering ذيل M1 بعد قرار K9-D** (ضربا عتبة scopedQuery: دفتر تجميعي لا يُرطَّب).
  - 🔒 **شرط إغلاق إلزامي للوحدة (لا تُغلق M1 قبله)**: إزالة الجسر المؤقت `proc_options_from_query` من proc_helpers.php فعليًّا بعد هجرة الشاشات الثلاث المستهلكة له (orders/receipt/issue) + T3=0 على الوحدة كلها.
  - لكل ملف: بوابته الخماسية بنواتجها تُقيَّد هنا فور إغلاقه (لعرضها تفصيلًا في checkpoint النهاية)؛ الإنفاذ باسم الملف في `TENANT_ENFORCE_PATHS` فور اكتماله؛ commit لكل شاشة.
- **الخطوة التالية المقصودة**: هجرة dashboard_proc.php.
### سجل البوابة الخماسية لملفات M1 (يُملأ ملفًا ملفًا)
| الملف | خ1 تحويل (T3) | خ2 golden | خ3 تسرّب | خ4 مراقبة | خ5 إنفاذ+commit |
|---|---|---|---|---|---|
| proc_helpers.php (مشترك) | ✅ 3→1 (جسر موثق مؤقت — شرط الإغلاق) | ✅ الوحدة العشر مطابقة | (ليس شاشة) | ضمن نوافذ الشاشات | ✅ commit e9a498b |
| dashboard_proc.php | ✅ 2→0 (7 عدادات+قائمة عبر البوابة؛ عربية بمعاملات) | ✅ 28610 مطابق | ⬜ (لوحة عدادات — استثناء زرع موثق، تغطيه بوابة العد) | 🟡 جولات القراءة تمت؛ تُستكمل بنافذة الوحدة | ✅ commit dd9a175؛ الإنفاذ بنهاية الوحدة |
| suppliers_proc.php | ✅ 3→0 (insert/update/softDelete/قائمة) | ✅ 31347 مطابق | ⬜ يُضاف زرع proc_supplier عند نافذة الوحدة | 🟡 كما أعلاه | ✅ commit ab0904e؛ الإنفاذ بنهاية الوحدة |
| receipt_custody_proc.php | ✅ 5→0 (**أول replaceChildren حي** لسطور العهدة؛ أب عبر البوابة؛ قائمة الأوامر عبر البوابة بدل الجسر؛ ترطيب موردين+عد سطور) | ✅ 33650 مطابق | ⬜ زرع عند نافذة الوحدة | 🟡 قراءة تمت | ✅ commit (انظر git log) |
| requests_proc.php | ✅ 8→0 (replaceChildren لسطور الطلب؛ ترطيب عدّ السطور) | ✅ 36702 مطابق | ⬜ زرع عند نافذة الوحدة | 🟡 قراءة تمت | ✅ commit eb6290c |
| items_proc.php | ✅ 4→0 | ✅ 38151 مطابق | ⬜ نافذة الوحدة | 🟡 قراءة تمت | ✅ commit 7259082 |
| master_data_proc.php | ✅ 8→0 (كيانا lookup/warehouse) | ✅ 38676 مطابق | ⬜ نافذة الوحدة | 🟡 قراءة تمت | ✅ commit 01ffff9 |
| orders_proc.php | ✅ 8→0 (replaceChildren + قائمة الطلبات بدل الجسر + ترطيب الموردين) | ✅ 37494 مطابق | ✅ مزروعة (66/66) | ✅ صفر would-deny منطوق | ✅ commit 995b559 + إنفاذ اسمي |
| **issue_proc.php** | ⏸️ **محجوز لقرار قدرة** (قُرئ كاملًا): مسار الحفظ عملية مترابطة متعددة الجداول — custody يرتبط بline_id الوليد لكل سطر + stock_move بمفتاح مركّب ref_type+ref_id + الأصل معاملة واحدة لثلاثة أطفال؛ ثلاث قنوات منفصلة تكسر الذرّية المشتركة | — | — | — | يُعرض في checkpoint (اقتراح: توسيع القناة بشرط ثابت معلن + متغيّر ذرّي مشترك أو runInTransaction) |
- **خطوات الوحدة المنفَّذة (2026-07-08)**: §7 مزروعة لأربع شاشات (items/suppliers/orders/requests) → الحزمة **66/66**؛ جولات المراقبة: u71 ×3 لكل شاشة + u1 دور آخر — **صفر would-deny منطوقًا بProcurement**؛ **الإنفاذ الاسمي للسبعة** في TENANT_ENFORCE_PATHS + golden تحت الإنفاذ **مطابق حرفيًا**؛ commit الزرع+الإنفاذ.
- 🔒 شرط الإغلاق قائم: الجسر باقٍ (مستهلكه الوحيد الآن issue_proc) — يُزال فور هجرة issue بعد قرار القدرة. M1 لا تُغلق قبله (+stock/reordering ذيل K9-D).
- **الخطوة التالية بالضبط — الملفان الأخيران**:
  1. **orders_proc.php (407س)**: نمط requests نفسه حرفيًّا (أب proc_order + replaceChildren لproc_order_line بparentCol='order_id') + تحويل `$requests_sql` الجسري لقائمة صفوف بوابة (كما فُعل في receipt بorder_option_rows) + قائمة العرض بترطيب عدّ السطور. الوصفة منسوخة من requests/receipt.
  2. **issue_proc.php (430س) — قرار خاص قبل التحويل**: فيه DELETE على proc_stock_move بمفتاح مركّب (ref_type='proc_issue' AND ref_id) في موضعين (سطر 102 ضمن الحفظ، و175+178 ضمن حذف الصرف مع proc_custody WHERE issue_id). **replaceChildren بparentCol واحد لا يقيّد ref_type** ⇒ خيارات: (أ) توسيع القناة بوسيط شرط ثابت اختياري `extraWhere` (مثل ref_type=?) — تعديل بوابة يحتاج عرضًا على المستخدم لأنه يمس القناة المقيدة؛ (ب) بما أن custody DELETE بissue_id مفرد → replaceChildren يناسبه، وstock_move يُدار بقراءة الصفوف القائمة عبر البوابة ثم حذف عبر... لا حذف مفرد بالبوابة! **⇒ الأرجح عرض الخيار (أ) على المستخدم ضمن checkpoint الثمانية** (توسيع مقيد: شرط مساواة إضافي معلن + اختبار c6 جديد). **اقرأ issue_proc.php كاملًا أولًا ولا تفترض**.
  3. بعدهما: **إزالة الجسر** (شرط الإغلاق — grep مستدعين ثم حذف الدالة) + T3=0 وحدةً + زرع §7 (items/suppliers/orders/requests على الأقل) + نافذة مراقبة الوحدة (3 جولات u71/u1 + would-deny منطوق /Procurement/=0) + إنفاذ `/Procurement/` في القائمة + golden ختامي + commit ختامي للوحدة + **checkpoint M1 الشامل بعرض كل ملف بخطواته الخمس + عرض K9-D التفصيلي** (stock+reordering ذيل M1 بعده).
- **جولات المراقبة المنفذة حتى الآن لكل الملفات الستة**: golden-قبل (u71) + golden-بعد (u71 جلسة جديدة) لكل ملف؛ تبقى جولة دور آخر (u1) + أي CRUD تمثيلي ضمن نافذة الوحدة الختامية. الشاشات الثلاث الأخيرة تستهلك الجسر المؤقت — بعد هجرتها يُزال الجسر (شرط الإغلاق) ثم: زرع §7 للشاشات القابلة + نافذة مراقبة الوحدة (3 جولات) + إنفاذ `/Procurement/` + commit ختامي + **عرض K9-D التفصيلي في checkpoint النهاية مع stock+reordering كذيل**.
- **نمط التحويل المستقر (يُستنسخ)**: قراءة القسم التشغيلي → كتابات عبر `proc_gate(false)` بdata array + try/catch TenantGateException → القوائم عبر `proc_gate($is_super_admin)` → قيم عربية بمعاملات `?` → إسقاط scope/is_deleted اليدوية → لا `company_id` في أي data (البوابة تحقن) → lint + T3=0 + golden مقابل `m1_golden_before.txt` + commit باسم الملف.

### K9-T + M0 منفَّذان ومُثبَتان (2026-07-08)
- **العدّة (commit f7ef497)**: T1 `tests/golden_run.php` — تحقق جاهزيتها بإنتاج **نفس أطوال المرحلة 0 الثلاثة عشر قيميًّا**؛ T2 قسم الشاشات في leak suite (زيارة فعلية بجلسة + علامتا mine/other) — عمل قبل الهجرة أساسًا (40/40)؛ T3..T6 وصفات موثقة في الخطة.
- **M0 (commit 10bd372) — البوابة الخماسية 5/5**:
  1. تحويل كامل: **13→0 نداءً خامًا** (+إزالة helper ميت)؛ أُسقطت شروط company/is_deleted اليدوية والهروب اليدوي (البوابة تحضّر)؛ JOINان → ترطيب ثنائي (clients من الخريطة القائمة = صفر استعلام إضافي؛ creators بجلب واحد `includeDeleted` لمطابقة LEFT JOIN الأصلي)؛ ترقيم OPP- في PHP (تعبير CAST خارج صرامة orderBy).
  2. golden **مطابق بايتًا ببايت** (110,936) قبل/بعد.
  3. حزمة التسرّب **40/40** بقسم الشاشة (ترى علامتها/لا ترى الأخرى/الدخول والاسترجاع سليمان).
  4. نافذة المراقبة: 3 جولات بأدوار مختلفة (قراءة u13 ×2 + **CRUD حي كامل عبر الشاشة**: إضافة بحقن company=4 آليًّا، تعديل، softDelete بهوية الجلسة deleted_by=13 + مسار رفض RBAC لu1) — **صفر would-deny لمسار /Opportunities/**.
  5. الإنفاذ: `TENANT_ENFORCE_PATHS=/Opportunities/` → golden مطابق + CRUD كامل تحت الإنفاذ ✔.
- **قلب معلم K9**: `EMS_TENANT_GATE=monitor` في `.env` (أول وصل شاشة — يبقى طوال K9).
- **درسا عدّة (يُعرضان في المراجعة)**: (١) بوابات حزمة التسرّب كانت ترث وضع البيئة — ثُبّتت **صريحةً enforce** (تختبر الميكانيكا لا .env؛ m1 أعيدت صياغته)؛ (٢) T4 يجب أن يُنطَّق بمسار الشاشة (اختبارات الحزمة نفسها تولّد would-deny عامًّا بطبيعتها).
- **Rollback M0**: `git revert 10bd372` + إفراغ `TENANT_ENFORCE_PATHS` — النسخة الاحتياطية في scratchpad أيضًا.
- **الخطوة التالية المقصودة**: مراجعة الپايلوت المشتركة → إذن المستخدم → M1 Procurement (بدءًا بجرد سطور soft=false وقرارها).

### K7 منفَّذ ومُثبَت (2026-07-08) — StateMachine للجديد فقط (النطاق الضيّق المُقرّ)
- **البنية (ملفات جديدة فقط)**: `app/Core/StateMachine.php` (+Exception) تعريفي بنمط `fin_event_flow` الرسمي؛ ترحيل `ems_state_transitions` (append-only، applied=13، مسجَّل T_RESTRICTED)؛ حزمة `tests/state_machine_test.php`.
- **الحزمة 17/17** وأبلغها: **★ التشديد المُقرّ ثنائي الاتجاه عبر جسر K6** — (دوره 17 ومنصبه يمنح 6 ⇒ **رفض**، المنصب يتقدّم) و(دوره 6 ومنصبه يمنح 17 ⇒ **قبول** بمصدر position في التدقيق)؛ **وfallback الـ31 مؤكَّد**: مستخدم بلا منصب = users.role حرفيًّا (المصدر role) — استهلاك الجسر للجديد لا يقلب سلوك القائمين.
- بقية الإثباتات: حالة فائتة/فعل مجهول/حارس رافض تُرفض والحالة سليمة؛ **سباق حقيقي** (حارس يقلب الحالة خلف المحرك) يصدّه القفل التفاؤلي بلا قيد تدقيق زائف؛ النشر على الانتقال يرث correlation الكيان.
- **براهين عدم المساس**: git diff تحت `Approvals/` = كتلة K5 المقرَّة وحدها (10 أسطر — لا جديد من K7)؛ **إعادة درِل K5**: اعتماد رباعي حي بعد K7 → الخطّاف انطلق عند الرابعة حصرًا (event نُشر gen=79) ثم نُظّف؛ golden شاشتي الاعتماد مطابق؛ كل الحزم خضراء (13/34/15/35/11) + login=200.
- **عقد التبنّي (§9.1)**: الجديد فورًا؛ Finance/approval_*/movement عند دفعات هجرتها؛ الأربعة المقدّسة خارج أي نطاق. **لاتينية للجديد حصرًا** — صفر مساس بأي ENUM عربية.
- **Rollback**: down الجدول + حذف الصنفين (خامل — صفر متبنٍّ حي).
- **K6 مغلق ومقبول (2026-07-08)**: جدول `positions` + `users.position_id` nullable (بحذر K2: dump + copy-test up/down مطابق) · جسر قراءة خامل `includes/positions.php` بfallback كامل (13/13: منصب صالح يمنح دوره نصًّا؛ معطّل/محذوف/عابر-شركات/يتيم = السلوك القائم) · golden الـ13 صفحة مطابق حرفيًّا · الـ31 مستخدمًا الحقيقيين NULL · `positions` مسجَّل T_TENANT+soft (معزول عبر البوابة من المنشأ — مُثبَت) · users.role_id الأثري وُثّق ولم يُمسّ · عقد الاستهلاك التدريجي موثَّق داخل الجسر (التبنّي في الحُرّاس المركزية خلف `EMS_POSITION_BRIDGE`، لا الشاشات) · Rollback: `K6_down_positions_bridge.sql` مُختبَر.
- **جوهر خطة K7 المعروضة**: النطاق ضيّق (وحدات جديدة فقط — الأربعة المقدّسة ومحرّك approval_* الحي في movement/Oprators وfin_event_flow كلها تبقى، ويتبنّون وقت هجرتهم)؛ K7 ملفات جديدة فقط + برهان git diff صفر تحت Approvals/ + إعادة درِل K5؛ الهوية عبر جسر K6 (أول مستهلك)؛ قفل تفاؤلي بنمط المالية؛ لاتينية للجديد حصرًا.
- **قاعدة مثبَّتة بطلب المستخدم**: K9 يسبقه خطة تفصيلية منفصلة كاملة قبل أي تنفيذ (§9.5).
- **الخطوة التالية المقصودة**: انتظار قرارات §9 الثلاثة → بناء K7 → ثم خطة K9 التفصيلية → K10.

### K5 منفَّذ ومُثبَت حيًّا (2026-07-08) — Walking Skeleton الحدثي end-to-end · **العلَم publish نافذ**
- **قرارات المستخدم المطبَّقة**: الخيار أ (لا لمس لمسار الرفض؛ العكسي = K5.1 مستقل؛ اليتيمة تُرصَد بإنذار) · quantity=executed_hours · occurred_at=يوم العمل · amount=0 موسوم `valuation='unpriced'` في الجذر والمشتق (+الجسر enterprise يعزلهما عن مجاميع الشاشات القديمة بنيويًّا).
- **الملف المقدّس**: git diff منذ HEAD = كتلة K5 المعزولة فقط (10 أسطر إضافة، صفر حذف/تعديل) بشرط `$ins && $my_level===4`؛ نسخة استرجاع في scratchpad؛ golden مصادَق مطابق حرفيًّا (off).
- **monitor (مرصود)**: اعتماد رباعي حي (المستخدمون 4→7) — الخطّاف انطلق **مرة واحدة عند الرابعة حصرًا**، المراجع الأربعة حُلّت رقميًّا (eq/sup/proj/emp=1)، جيل a59، صفر كتابة، صفر فشل.
- **publish — شروط المستخدم الأربعة مُثبَتة حيًّا**:
  1. **سلسلة correlation باستعلام فعلي**: `01KX0Y8YWGVDGAK6BT5XBRW3JK` ⇒ الجذر `equipment.hour_logged` (id=135, timesheet:262, qty=2.25) + المشتق `finance.hour_recognized` (id=136, source_event:135, وارث المراجع والكمية) — طرفًا لطرف.
  2. **إعادة cron على الحي = صفر أثر** (root=1 derived=1 قبل وبعد).
  3. **المُصالِح عمليًّا**: فشل نشرٍ مُحاكى (علَم off أثناء اعتماد TS3) → «reconciler: published 1 missed» + الاشتقاق في نفس الدورة. **واليتيمة عمليًّا**: رفض TS4 بعد اكتمال الأربعة → `EVENT_ORPHANED_ALERT` بإنذار صريح مسمّى (event 141 — بانتظار K5.1).
  4. **مسار الفشل المرجعي حيًّا**: operator=999999 → `REF_RESOLUTION_MISS` + **نُشر** بمراجع NULL والخام `"999999"` في payload.raw_refs — بلا أي كسر، والاعتماد 4×200.
- **أول اعتماد حي عبر publish (مراقبة لصيقة)**: الحدث كُتب (event 135) والاستجابات الأربع نجحت كاملةً بخطّاف نشط، **صفر EVENT_PUBLISH_FAILED** طوال الدرِلات.
- **تنظيف كامل**: أحداث الدرِلات (جذور+مشتقات عبر correlation) والسجلات الاصطناعية أزيلت؛ الجدول عاد لأساسه (10)؛ الإنذار اليتيم صامت؛ الحزم 34/15/35/11 خضراء؛ login=200.
- **بنود مسمّاة مفتوحة**: **K5.1** الحدث العكسي المعوِّض (تصميم مستقل) · **تشغيلي**: جدولة `cron_events.php` (محليًّا يُشغَّل يدويًّا/عند الحاجة) · مقياس تأخّر الناقل (خطة K4).
- **Rollback K5**: `EMS_EVENT_HOOKS=off` (خامل فورًا) / استعادة نسخة المعالج / تعطيل المستهلك بصف `ems_event_consumers`.

### K4 منفَّذ ومُثبَت (2026-07-08) — EventDispatcher + Cursor/DLQ + عامل CLI (صفر مستهلك حي حتى K5)
- **البنية**: ترحيل `2026_07_08_event_dispatcher_tables.sql` (applied=10): `ems_event_consumers` (cursor مستقل لكل مستهلك) · `ems_event_deliveries` (عدّاد محاولات) · `ems_event_dead_letter` (UNIQUE consumer+event). down: `backups/K4_down_event_dispatcher_tables.sql` (جداول جديدة صرفة). الثلاثة مسجّلة `T_RESTRICTED`. الصنف `app/Core/EventDispatcher.php` + العامل `cron_events.php` (CLI/GET بمفتاح `EVENTS_CRON_KEY` fail-closed — نمط cron القائم؛ **لا مستهلك مسجَّلًا: تشغيله no-op آمن حتى K5**).
- **دلالة التسليم الصادقة (موثَّقة في docblock)**: التسليم at-least-once والأثر exactly-once عبر إلزام عطالة أثر المستهلك (§8.1-5) — معالج الاختبار ينشر مشتقًا عبر EventPublisher بمفتاح حتمي (نمط مستهلك المالية K5 نفسه) ويرث correlation.
- **تدقيقات المستخدم الثلاثة — 15/15 ×2 تشغيل**:
  1. **انهيار حقيقي عبر العمليات**: عامل طفل فعلي قُتل (exit=97) **بعد الأثر وقبل الـCursor** → الأثر=1 والـCursor راكد → استئناف بعامل جديد → إعادة تسليم فعلية امتصّتها العطالة → **الأثر بقي 1 والـCursor تقدّم**.
  2. **السام → DLQ ويواصل خلفه**: 3 محاولات فاشلة → الرابعة عزلته في DLQ (attempts=3) وتقدّم الـCursor **وعالج السليم التالي في نفس الدورة** — الطابور لا يتجمّد.
  3. **استقلال الـCursors**: ثلاثة مستهلكين بمواضع مختلفة؛ تعثّر ps التاريخي لم يمسّ fx (عالج الثلاثة في دورة واحدة، ومنها حدثُ سمِّ ps — الحساسية لكل مستهلك).
  + الترتيب داخل المستهلك بالمعرّف الخادمي تصاعديًا (مقايضة K8 مطبَّقة).
- **سقف التراكم (تفكيرًا بطلب المستخدم — لا بناء الآن)**: الاستئناف بعد توقف طويل **آمن بالبناء** (append-only + cursor + دفعات batch=100 + عطالة الأثر) — آلاف الأحداث = لحاق عبر دورات متتالية بلا فقدان ولا أثر مزدوج. حدود عملية مقترحة للبناء لاحقًا (K5+/لوحة): مقياس تأخّر `MAX(id) − MIN(cursor للمفعَّلين)` بإنذار ~5,000 وحرج ~20,000 في نمط المراقبة القائم؛ + إنذار على أي صف DLQ؛ + «وضع تصريف» (حلقة runOnce حتى الفراغ) إن لزم لحاق أسرع من إيقاع cron.
- **درسا أمانة**: مفتاح اختبار خالف نمط العقد (رقم في المقطع الأول) — صُحّح الاختبار لا الناشر (النمط صحيح)؛ وتحذير xdebug لوّث JSON الطفل — عولج بالتقاط آخر سطر.
- **حزم اليوم كلها خضراء**: dispatcher 15/15 ×2 · publisher 34/34 · leak 35/35 · serverId 11/11 · العامل no-op · login=200 · **صفر مستدعٍ حي** (grep).
- **Rollback K4**: تعطيل/عدم جدولة العامل (التوقف = تراكم آمن بلا فقدان) + down الجداول الثلاثة + حذف الصنف والعامل.

### K3 منفَّذ ومُثبَت (2026-07-08) — EventPublisher أول كاتبٍ حيٍّ للعقد (صفر مستدعٍ حتى K5)
- **بوابة المصطلح (شرط المستخدم) — مُثبَتة من الكود قبل أي كتابة**: الحقول المرجعية في جدول الأحداث كلها `INT` رقمية نظيفة تقابل `id INT` في جداولها المالكة (`contracts/project/equipments/clients/suppliers/employees`) — **الحدث يحمل مراجع لا نسخًا؛ `contracts` يبقى SSOT ولا جدول موازٍ**. المشكلة النصية upstream فقط (`operations.contract_id varchar(10)`, `project_id varchar(20)`) — موضع ADR-09 وتخصّ خطّاف K5 لا جدول الأحداث. الناشر **يرفض أي مرجع غير رقمي** (مُختبَر بمرجع نصي عربي).
- **فجوة عقدية اكتُشفت وسُدَّت (ترحيل تكميلي `2026_07_08_event_contract_fields_b.sql`)**: أربعة حقول إلزامية في §9 بلا أعمدة (`entity_type/entity_id/occurred_at/payload JSON`) + المرجع السياقي `operator_employee_id` (→employees) + **قيمتا جسرٍ ذيليتان محوكمتان**: `event_type+='enterprise'` (يعزل أحداث العقد عن فلاتر شاشات المالية القديمة = منع عدّ مزدوج) و`source_module+='movement/finance/transport/system'`. نفس حذر K2: dump طازج + **up+down مُختبَران على نسخة (diff بنية مطابق حرفيًا)** + التطبيق عبر المشغّل (applied=9، 289ms). down: `backups/K3_down_event_contract_fields_b.sql`.
- **الصنفان**: `app/Core/EventPublisher.php` (+`EventValidationException`) — نشرٌ على اتصال المستدعي (Transactional Outbox)؛ يولّد event_no (متتالية ذرّية)/correlation (جذر)/idempotency (حتمي)/schema_version؛ يرفض ناقص الإلزامية والصيغ الفاسدة. مقايضة موثَّقة: قفل صف المتتالية يُمسَك حتى commit (نقطة تسلسل لكل شركة داخل المعاملة — مقبول للـMonolith).
- **تدقيقات المستخدم الثلاثة — كلها خضراء (34/34 ×2 تشغيل متتالٍ)**:
  1. **الذرّية بحقن فشل متعمّد وسط المعاملة**: نشر داخل tx → حقن عبارة فاسدة → rollback ⇒ **صفر حدث يتيم** + صف الحدث تراجع كليًّا + **المفتاح تحرّر مع التراجع** (إعادة النشر تنجح).
  2. **رفض كل حقل إلزامي على حدة**: 9/9 حقول أُسقط كلٌّ بمفرده ⇒ رفض + صفر إدراج.
  3. **العطالة بإعادة نشر فعلية على مستوى الدالة**: نفس الحدث المنطقي مرتين ⇒ `duplicate=true` بلا استثناء، نفس id والمفتاح، صفر صف جديد.
- **وراثة Correlation (Fan-Out جاهز لK5 — موثَّق في docblock الناشر)**: الجذر بلا تمرير = توليد؛ **المشتق يمرّر correlation الجذر حرفيًا فيُخزَّن كما هو** — مُختبَر (سلسلة جذر+مشتق تحت معرّف واحد؛ جذر جديد = معرّف جديد). الـDispatcher (K4) يمرّر القيمة لكل مستهلك.
- **الحد الصارم مُحترَم**: صفر مستدعٍ للناشر خارج الاختبارات (grep) — **لا خطّاف Approvals حتى K5**. leak 35/35 · ServerId 11/11 · login=200.
- **درس أمانة**: فشل أول تشغيلٍ كشف فرق `mysqli_report` بين CLI والتطبيق (سُوّي بمطابقة البيئة) وبقايا يتيمة من انقطاعٍ داخل معاملة — حُصّن الـteardown (rollback أولًا + تنظيف بقايا أي تشغيل سابق)؛ الحزمة الآن ذاتية النظافة عبر التشغيلات.
- **Rollback K3**: حذف الصنفين + down التكميلي (شرطه: تنظيف صفوف الجسر — الاختبارات تنظّف نفسها).

### K8 منفَّذ ومُثبَت (2026-07-08) — خدمة الترقيم الخادمي `app/Core/ServerId.php`
- **الآلية ومقايضاتها** (موثَّقة في docblock الصنف):
  - `ulid()`: ULID قياسي (26 حرف Crockford؛ 48-bit زمن ملي + 80-bit عشوائية) — زمنيّ الترتيب معجميًّا عبر الملي-ثواني (جاهز لتعدد المراكز R02 §11.2-4)؛ **مقايضة**: داخل الملي-ثانية الترتيب غير مضمون — ترتيب الاستهلاك الرسمي عبر id/Cursor لا ULID.
  - **Event ID** = `id` AUTO_INCREMENT (خادمي حصري قائم) · **event_no** للناشر عبر `nextNo` بذرّية `ems_sequences` وبادئة منفصلة عن `fin_gen_code` القديم (COUNT+1 السباقي — يبقى لشاشاته حتى هجرتها؛ لا تداخل نطاقات).
  - **Correlation ID** = ULID **للجذر فقط؛ المشتقات ترث ولا تولّد** (وسيط في ناشر K3) — يحقق غرضه المعماري: سلسلة الأثر من أصل واحد طرفًا لطرف.
  - **idempotencyKey** حتمي لا عشوائي (`eventKey:entityType:entityId`، وsha1 لما جاوز 64) — إعادة المحاولة = نفس المفتاح = تصطدم بـUNIQUE.
- **علاقة sync_uuid: تكامل لا توازٍ** — مُثبَت بالفحص: **صفر كتّاب/قرّاء** لـsync_uuid في الكود (عمود خامل محجوز لـADR-11 المؤجَّل = هوية ما قبل الخادم يولّدها العميل الميداني)؛ ServerId يصدر **الهوية النهائية الخادمية** ولا يلمس sync_uuid.
- **برهان التزامن العملي**: 8 عمليات متوازية × 200 سحب من **نفس النطاق** = **1..1600 بالضبط، صفر تكرار، صفر فجوة** (قفل صف ذرّي) · 40,000 ULID عبر 8 عمليات متوازية = صفر تصادم.
- **الحزمة**: `tests/server_id_test.php` **11/11** (صيغة/ترتيب/حتمية/عزل نطاقات) + وضع `--worker` للدرِل · leak suite **35/35** بلا مساس · login=200.
- **البنية**: ترحيل `2026_07_08_ems_sequences.sql` عبر المشغّل (applied=8) · الجدول مسجَّل `T_RESTRICTED` في TenantRegistry (وصول عبر ServerId حصرًا).
- **صفر نطاق انفجار**: لا مستدعي لـServerId خارج الاختبار (أول مستهلك حقيقي = K3).
- **Rollback**: حذف الصنف + `DROP TABLE ems_sequences` (down بسيط — لا بيانات حية تعتمده قبل K3).

### K2 منفَّذ ومُثبَت (2026-07-08) — تجهيز مخطّط فقط: الحقول تبقى NULL حتى K3
> **⚠ العقد ليس «مفعَّلًا»**: K2 أضاف الأعمدة؛ لا كاتب لها قبل الناشر (K3). كل القيم NULL الآن بالتصميم.
- **الترحيل**: `2026_07_08_event_contract_fields.sql` مطبَّق عبر `migrate.php`/`ems_migrator` (283ms + لقطة تلقائية قبل التنفيذ) — 7 أعمدة nullable (`event_key/category/quantity/unit/correlation_id/idempotency_key/schema_version`) + `uq_ffe_idempotency` UNIQUE + فهرسا correlation/event_key. **ENUM `event_type` لم يُمسّ** (قرار §7-1).
- **حذر الإنتاج المُنفَّذ**: dump كامل (`backups/fin_financial_events_pre_K2_...sql`) · **down مُختبَر فعليًّا على نسخة بالبيانات** (diff بنية بعد up+down = مطابقة حرفية) · تحليل ساكن للملفات السبعة (INSERTs بأعمدة صريحة، لا قراءة موضعية).
- **الإثباتات بعد التطبيق**: الصفوف العشرة كلها NULL في الحقول السبعة (تعايش تحت UNIQUE حيًّا) · مسبار INSERT بشكل أعمدة التطبيق عبر `ems_app` نجح والحقول الجديدة NULL تلقائيًّا · تكرار idempotency_key = **1062** (العطالة تُفرَض من القاعدة) · جولة ذهبية بعدية: 5/6 صفحات مطابقة حرفيًّا + cron مطابق؛ فارق `events_list` (−39B) **مُبرهَن بيئيًّا لا ترحيليًّا بA/B حاسم** (down مؤقت على الحي: الطول لم يتغيّر) — سببه إشعارات cron الثلاثة/إعادة احتساب الموازنة بين الجولتين.
- **توسّع صلاحيات ems_migrator (بموافقة صريحة)**: +`SHOW VIEW` (ضرورة بنيوية للقطة المخطّط — الفشل الذري كشفها) +`CREATE VIEW` (حاجة مُثبَتة من ترحيل View تاريخي) — على قاعدة EMS حصرًا؛ تحقق مزدوج: المُرحِّل يملكهما و`ems_app` مرفوض 1142 فيهما. موثَّق في `.env.example` وملحق `docs/MIGRATIONS_GUIDE_ar.md`.
- **تسوية جانبية موثَّقة**: checksum ملف `2026_06_27_employee_unification.php` حُدّث في جدول التتبّع (تعديل P0-4 المقصود) — وتصحيح أمانة: المشغّل يتتبّع `.php` أيضًا خلافًا لقولي السابق. `status`: applied=7, modified=0, pending=0.
- **Rollback K2**: `database/backups/K2_down_event_contract_fields.sql` (مُختبَر) — آمن ما لم يبدأ K3 الكتابة.

### K1 منفَّذ ومُثبَت (2026-07-08) — وضع المراقبة log-only في TenantDb
- **التصميم**: تصنيف نقاط `deny()` — 3 حالات سياق عزلٍ قابلة للمراقبة (غير مسجَّل/مقيَّد/لا شركة) تُسجَّل `tenant_gate_would_deny` وتُمرَّر **قراءةً وإدراجًا فقط**؛ كل حرّاس الكتابة التعديلية والهجمات والأخطاء البرمجية **تُرمى دائمًا** (update/softDelete عبر العبور = strict مرفوض). المسار السوي المعزول **مطابق تمامًا** في الوضعين.
- **الأعلام**: `EMS_TENANT_GATE` (غياب/enforce = fail-closed صافٍ — الافتراض الآمن؛ monitor صراحةً) + `TENANT_ENFORCE_PATHS` (fail-closed لمساراته حتى في monitor — نمط CSRF حرفيًّا، بشرط المستخدم). موثَّقة في `.env.example`؛ **`.env` لم يُمسّ** (لا مفتاح = enforce).
- **الاختبارات**: الحزمة **35/35** ✅ = الـ27 الأصلية بلا مساس + 8 جديدة (m1 افتراض enforce · m2/m3 عبور مُسجَّل · m4 قراءة بلا سياق ترى العلامتين · m5 السوي معزول · m6a/b حرّاس الكتابة صارمة · m7 مسار الإنفاذ يغلب monitor).
- **صفر نطاق انفجار — مُثبَت**: صفر مستدعين خارج tests/config-factory (grep) · `.env` بلا مفتاح · login=200.
- **التوثيق**: قسم 7 جديد في `docs/TENANT_GATE_CONTRACT_ar.md` (المخرَج ⑥) بجدول الدلالة الكامل.
- **Rollback**: git revert لملف `TenantDb.php` (الافتراض enforce يجعل الكود الجديد خاملًا أصلًا بلا مفتاح)؛ حذف مفتاحي `.env.example` توثيقي فقط.
- **خلاصة بنية الخطة**: K1 مراقبة البوابة → K2 عقد §9 على fin_financial_events → K8 الترقيم → K3 Publisher/Outbox → K4 Dispatcher → K5 مستهلك المالية (Walking Skeleton) → K9 دفعات الهجرة (پايلوت Opportunities→Procurement→Transport/Maintenance؛ عتبة المرحلة 1 = 3 وحدات إنفاذًا) → K6 جسر المنصب → K7 StateMachine → K10 قلب حارس الفعل. حصر فعلي: 1,211 نداء/218 ملفًا موزّعة بالجدول في الخطة؛ قيد مكتشف: واجهة TenantDb حاليًّا CRUD فقط (لا JOIN) — قرار مفتوح.

### P0-4 منفَّذ ومُثبَت (2026-07-08) — إزالة root نهائيًّا + Fail-Fast لكل المسارات
- **إزالة root من الكود نهائيًّا** (لا مجرد fail-fast): `config.php` لم يعُد يحمل أي قيمة اتصالٍ افتراضية (`ems_env('DB_USER','root')` أُزيلت)؛ وسكربت الترحيل التاريخي `2026_06_27_employee_unification.php` كان يحمل root حرفيًّا → حُوّل لقراءة زوج المُرحِّل من `.env` بfail-fast (غير مُتتبَّع بالـchecksum — التتبع للـ.sql فقط). مسح شامل: **صفر literal root متبقٍّ في أي مسار PHP**.
- **السياسة**: المفاتيح الأربعة إلزامية الوجود في `.env` (`DB_PASS` يجوز فارغًا **صراحةً** — يحفظ مسار Rollback لroot كخيار مشغّل مقصود لا افتراض كود).
- **Fail-Fast مُجرَّب فعليًّا في كل المسارات** (فقدان .env + تلفه بلا DB_USER):
  - **ويب** (الهدف الصحيح `login.php` — درسٌ مهم: `/ems/` index.php صفحة هبوط **ساكنة لا تتضمّن config** فلا تصلح هدف اختبار): **HTTP 500** + صفحة «خطأ في إعدادات النظام» + قيد FATAL بمسار الويب في php_errors — في الحالتين.
  - **CLI**: `php -r require config` → **exit=1** + رسالة STDERR واضحة؛ `migrate.php status` → **exit=1** في الحالتين.
  - الاستعادة: login=200 والمشغّل يتصل ems_migrator. `.env` سليم بعد التمرين (ems_app/freeze=true/الأزواج الأربعة).
- **نقطة استرجاع config.php**: نسخة قبل التعديل في scratchpad (`config.php.P04.bak`) + `php -l` نظيف + طلب حي 200.
- **Rollback P0-4**: استعادة النسخة الاحتياطية من scratchpad (أو git checkout للملف) — سلوكيًّا لا حاجة: النظام يعمل والسياسة أصبحت «.env إلزامي» وهي حالة `.env` القائمة.
- ملاحظة تشغيلية مكتشفة: Apache يشغّل **PHP 8.3.31** بـini خاص (`apache2.4.65/bin/php.ini`) بينما CLI يستخدم 8.5.0 — لا أثر حاليًّا، تُدوَّن لوعي بيئة الاختبار.

## ✅ خلاصة إغلاق المرحلة 0 (كاملة بالأدلة — 2026-07-08)
| البند | النتيجة | الدليل الحاسم |
|---|---|---|
| P0-1 تحييد الـ21 | محيَّد فعلًا (لا ملفوف فقط) | تدقيق ساكن: كل نداء no-op على المخطّط الحيّ |
| P0-3 الفريز | `EMS_DDL_FREEZE=true` نافذ | صفر EXECUTED منذ القلب؛ 4 BLOCKED حميدة فقط (Approvals) |
| P0-2 المستخدمان | ems_app (DML) + ems_migrator (DDL) على قاعدة EMS حصرًا؛ التطبيق على ems_app | تحقّق سلوكي 7/7 + USER()=ems_app + جولة 13 صفحة مطابقة بايتًا ببايت + migrate.php CLI ينفّذ DDL بينما ems_app مرفوض 1142 |
| P0-4 لا root ولا سقوط صامت | root أُزيل من الكود كليًّا؛ فشل واضح ويب(500)+CLI(exit 1) عند فقدان/تلف .env | تمرين فعلي بالحالتين على المسارين + قيود FATAL مؤرَّخة |
| معيار خروج المرحلة 0 (R02 §3.1) | **مستوفى محليًّا**: مستخدم DML-only قائم ومستعمَل · صفر DDL وقت الطلب · التغيير عبر المشغّل حصرًا · لا أسرار في المستودع | — |

### P0-2ب منفَّذ ومُثبَت (2026-07-08) — فصل المسارين نافذ
- **التحويل**: `.env` → `DB_USER=ems_app` (سطرا root معلّقان فوقه للـRollback الفوري). إثبات الهوية: `USER()=ems_app@localhost` عبر مسار `config.php` نفسه.
- **توصيل المشغّل**: `database/migrate.php` يفضّل `DB_MIGRATOR_*` إن وُجدا (وإلا اتصال config — توافق رجعي)؛ فشل اتصال المُرحِّل = خروج فوري برسالة.
- **شرط 1 — فصل المسارين**: `migrate.php status` شُغّل فعليًّا CLI → اتصل `ems_migrator` ونفّذ DDL جدول التتبّع (`CREATE IF NOT EXISTS schema_migrations`) بنجاح؛ بينما `ems_app` مرفوض **1142** على النداء نفسه حتى مع الجدول قائمًا. ⇒ **المسار الوحيد للـDDL هو CLI المشغّل**.
- **شرط 2 — نقطة التقاء التبعيتين**: تحت `ems_app`+`EMS_DDL_FREEZE=true`: الجولة المصادَقة (13 صفحة) **مطابقة بايتًا ببايت** لجولة الأساس قبل التحويل؛ نداءات Approvals الأربعة = **BLOCKED حميد** (الغلاف يقطع قبل بلوغ SQL أصلًا فلا يصل 1142 إلى MySQL)؛ **صفر أسطر جديدة في php_errors إطلاقًا** (لا 1142/denied)؛ صفر EXECUTED/FAILED. طرد Suppliers هو رفض RBAC القائم مسبقًا نفسه (نفس الطول 10469).
- **Rollback P0-2ب**: أزل التعليق عن سطرَي root في `.env` وعلّق سطرَي ems_app — رجوع لحظي، لا مساس بالبيانات. (والمشغّل يتراجع تلقائيًّا لاتصال config إن حُذف زوجا MIGRATOR.)
- **مؤجَّل بقرار المستخدم (عزل المتغيرات)**: إزالة نداءات Approvals الأربعة غير المشروطة — دفعة مستقلة بعد استقرار التحويل.

### P0-2أ منفَّذ ومُتحقَّق (2026-07-08) — قرارات المستخدم الثلاثة مطبَّقة
1. **المستخدمان أُنشئا بامتياز أدنى على `equipation_manage.*` تحديدًا (لا `*.*`)**:
   - `ems_app`@localhost: SELECT/INSERT/UPDATE/DELETE فقط.
   - `ems_migrator`@localhost: +CREATE/ALTER/DROP/INDEX/REFERENCES على قاعدة EMS حصرًا.
   - **تحقّق سلوكي 7/7**: app→SELECT✓/CREATE مرفوض 1142/ALTER مرفوض/mysql.user مرفوض · migrator→CREATE+DROP✓/CREATE DATABASE مرفوض 1044/mysql.user مرفوض.
   - كلمتا المرور في `.env` حصرًا (`DB_APP_*`, `DB_MIGRATOR_*`)؛ `.env.example` محدَّث بالمفاتيح بلا قيم.
2. **التطبيق ما زال على root** — `DB_USER/DB_PASS` لم يُمسّا (فصل الهوية عن التحويل).
3. **التشغيل المصادَق منفَّذ** (دخول فعلي بجلسة، مستخدم 1، تبديل hash مؤقت أُعيد بايتًا ببايت):
   - 13 صفحة من مسارات الغلاف: كلها HTTP 200 · **صفر Fatal/Parse** · **صفر RUNTIME_DDL_EXECUTED منذ القلب**.
   - **4×RUNTIME_DDL_BLOCKED حميدة** (نداءات Approvals غير المشروطة `CREATE IF NOT EXISTS`؛ الصفحات اكتملت 62KB/24KB لأن الجداول قائمة والقيمة المرجوعة غير مفحوصة هناك).
   - استثناءان **قائمان مسبقًا لا علاقة لهما بالفريز**: طرد Suppliers = رفض RBAC «لا توجد صلاحية عرض الموردين ❌» (منهجي 3/3، مُثبَت بإعادة اختبار معزولة)؛ أجسام صغيرة في movement/Oprators = حُرّاس «لا مشروع مرتبط» (JS redirect).

### خطة P0-2ب (التحويل — بانتظار القرار) + Rollback
- التحويل: `.env` → `DB_USER=ems_app`, `DB_PASS=<DB_APP_PASS>`؛ وتوصيل `migrate.php` ليفضّل `DB_MIGRATOR_*` إن وُجدا (تعديل صغير — المشغّل CLI-only).
- **Rollback فوري**: إعادة `DB_USER=root`, `DB_PASS=` (السطران محفوظان تعليقًا في `.env`) — لا مساس بالبيانات.
- شرط مستوفى: التشغيل المصادَق أعلاه (شرط المستخدم قبل أي تحويل) ✓.
- **توصية مصاحبة (اختيارية)**: إزالة نداءات Approvals الأربعة غير المشروطة (جداولها في ترحيلات اللحاق) لوقف ضجيج BLOCKED — تعديل god-file طفيف من جنس إصلاحات الإنتاج؛ بقرار المستخدم.

### تحقّق P0-3 (2026-07-08)
- `ems_env('EMS_DDL_FREEZE')='true'` → يُحسَب bool true → الغلاف يدخل فرع الحجب (لا تنفيذ).
- `php -l config.php` = لا أخطاء · `curl http://localhost/ems/` = HTTP 200 (77KB) بلا Fatal/Parse · صفر أسطر جديدة في php_errors.log.

### خطة P0-2 الجاهزة (موقوفة للتأكيد — «عناية قصوى: مستخدم القاعدة»)
- **تحليل الصلاحيات (مُثبَت)**: `CREATE TEMPORARY TABLES`=0 · `LOCK TABLES`/`GET_LOCK`=0 · `LOAD DATA`=0 · لا DDL خام خارج الغلاف الـ21. المعاملات لا تحتاج صلاحية خاصة.
- **مستخدم التطبيق** `ems_app`@`localhost`: `SELECT, INSERT, UPDATE, DELETE ON equipation_manage.*` فقط (بلا ALTER/CREATE/DROP).
- **مستخدم المشغّل** `ems_migrator`@`localhost`: DDL كامل (`CREATE, ALTER, DROP, INDEX, REFERENCES`) + DML على `equipation_manage.*` — يستعمله `database/migrate.php` فقط.
- **تبعية محقّقة**: الفريز الآن true ⇒ الغلاف لا ينفّذ DDL ⇒ آمنٌ تحويل التطبيق لمستخدم DML-only.
- **Rollback P0-2**: إبقاء `DB_USER=root, DB_PASS=` في `.env` كتعليق احتياطي؛ الرجوع = إعادة سطر root في `.env` (لا مساس بالبيانات). المستخدمان يُنشآن بلا حذف root.
- **قرار للمستخدم**: هل يُنشأ المستخدمان **ويُحوَّل اتصال التطبيق فورًا** لـ`ems_app`؟ أم يُنشآن ويُؤجَّل تحويل `.env` لخطوة تالية؟ وهل يُستخدم `migrate.php` بمستخدم `ems_migrator` منفصل عن اتصال التطبيق؟

## ⚠ تصحيح حاكم من المستخدم (2026-07-08) — «ملفوف/جاهز ≠ محيَّد/موقوف»
- **لا يُعدّ «تحييد الـ21» مكتملًا** لمجرّد أنها ملفوفة بالغلاف. `EMS_DDL_FREEZE=false` ⇒ الـDDL **ما زال يُنفَّذ وقت التشغيل فعلًا**.
- الإكمال = إثبات سجلّ نظيف من `RUNTIME_DDL_EXECUTED` **ثم** قلب العلَم إلى `true`.
- **تصحيح تبعية إضافي (مُكتشَف من الكود)**: `CREATE TABLE IF NOT EXISTS` يتطلّب صلاحية CREATE حتى لو كان الجدول قائمًا ⇒ **قلب `EMS_DDL_FREEZE=true` يسبق إلزامًا تبديل الاتصال لمستخدم DML-only** (وإلا فشل الغلاف تحت DML-only).

## تحليل سجل RUNTIME_DDL (logs/security.log — 2026-07-08)
- EXECUTED=1 (مسبار اختبار صناعي `wrapper_test :: CREATE TABLE _ddl_monitor_probe`، له ترحيل إسقاط) · FAILED=0 · BLOCKED=1.
- **نافذة السجل ~يوم واحد** (07-07 19:09→22:09) لا أسبوع؛ تغطية تشغيل الصفحات الـ21 غير مؤكَّدة ⇒ **غياب التنفيذ لا يكفي إثباتًا زمنيًّا** ⇒ يُستعاض بالتدقيق الساكن الحتمي.

## ترتيب المرحلة 0 (حسب التبعية الحرجة — مؤكَّد من المستخدم + تصحيح الكود)
| # | البند | الحالة الحقيقية |
|---|---|---|
| P0-1 | **تحييد فعلي مُثبَت** (تدقيق ساكن للـ21) | ✅ **مُثبَت حتميًّا** — كل نداء no-op على المخطّط الحالي (أدلة أدناه) |
| P0-3 | قلب `EMS_DDL_FREEZE=true` (env-driven) | 🟡 قيد التنفيذ |
| P0-2 | إنشاء مستخدم DML-only + مستخدم DDL منفصل + `.env` | ❌ **موقوف لِـCheckpoint** (بند «عناية قصوى» — مستخدم القاعدة) |
| P0-4 | تشديد fallback الاتصال إلى fail-fast | ❌ (يتبع P0-2) |

### دليل P0-1 (تدقيق ساكن 2026-07-08)
- **ALTER محروسة** (users/project_users/suppliers/equipments/equipments_drivers/operations/move_oprators/movement_operations/project_drivers/mnt_breakdown/clients/contracts/projects): كل الأعمدة/الفهارس **مؤكَّدة موجودة** في المخطّط الحيّ → حارس `column_exists`/`db_table_has_column` = false → الغلاف لا يُستدعى.
- **CREATE IF NOT EXISTS غير محروسة** (Approvals×3→timesheet_approvals/notes · register×3→admin_subscription_plans/companies/requests · bootstrap→activity_logs): كل الجداول **قائمة** → no-op.
- **سكربتان يدويان خارج المسار العادي** (admin/setup_once→admins/password_resets غير موجودَين · emsreports/setup_permissions): غير مُستدعيَين بأي include/redirect → لا يمسّان تحييد وقت التشغيل (يُحجبان إن شُغّلا يدويًّا تحت الفريز — مقبول).
- **الحكم**: قلب الفريز إلى true **لا يغيّر سلوك أي صفحة عادية** (حجب no-op = لا أثر). تحييد مُثبَت بالبناء لا بنافذة زمنية.

### خطة Rollback — P0-3 (قلب الفريز)
- **التغيير**: `config.php` يجعل `EMS_DDL_FREEZE` يُقرأ من `.env` (افتراض false)؛ `.env`: `EMS_DDL_FREEZE=true`.
- **الرجوع الفوري**: ضع `EMS_DDL_FREEZE=false` في `.env` (أو احذف السطر) — يعود الغلاف للتنفيذ (no-op) فورًا. **لا مساس بالبيانات ولا بالمخطّط.**
- **إن ظهر أي كسر**: الرجوع أعلاه + مراجعة السجل لـ`RUNTIME_DDL_BLOCKED` غير المتوقَّع.

> P0-2 (مستخدم القاعدة) **موقوف لانتظار التأكيد**: البرومت يخصّه بـ«عناية قصوى/اسأل عند الخطر»؛ صلاحية ناقصة = كسر التطبيق الحيّ. أُقدّم تحليل الصلاحيات + الخطة + Rollback عند Checkpoint قبل تنفيذه.

## قرارات نافذة مؤكَّدة من المستخدم (2026-07-08)
1. **مرجع الأرقام = الكود الحيّ المحلي**: 141 جدولًا أساسيًّا + 3 Views بدل 159؛ 159 = لقطة إنتاج أقدم غير قابلة للتحقق محليًّا (يُدوَّن).
2. **عدّ الترحيلات المُثبَت = 37** ملف `.sql` في `database/migrations/` (البرومت قال 34؛ الوثيقتان لا تذكران رقمًا للترحيلات).
3. **بوابة العزل TenantDb**: تُضاف آلية **وضع مراقبة log-only** (تسجّل ما كانت ستحجبه دون حجب) — بند صريح في ADR-02، وأول مهمة تقنية في المرحلة 1 قبل وصل أي شاشة.

## أرقام مرجعية مُثبَتة ميدانيًّا (2026-07-08، DB=equipation_manage محليًّا)
- 141 جدول أساسي + 3 Views (لا 159).
- 21 ملفًا ملفوفة بـ`ems_runtime_ddl` (مؤكَّد).
- 1,211 نداء تنفيذ (`mysqli_query`/`->query`) في 218 ملفًا (~1,445 رقم تقريبي بتعريف أوسع).
- 37 ترحيل `.sql` (+1 `.php`) في `database/migrations/`.
- 20 جدولًا بلا `company_id`؛ 92 جدولًا بلا `deleted_at`.
- `fin_financial_events`: 26 عمودًا، 10 صفوف، فيه `sync_uuid`؛ تنقصه حقول العقد
  (`correlation_id`, `idempotency_key`, `schema_version`, `category`, `quantity`, `unit`)، و`event_type` نوعه ENUM.
- الأعلام: `EMS_DDL_FREEZE=false` · `EMS_CSRF_ENFORCE=false`+`CSRF_ENFORCE_PATHS=` (مراقبة) · `EMS_ACTION_GUARD=monitor` · TenantDb مبنية fail-closed غير موصولة بأي شاشة.

## جدول تتبّع طبقة التوثيق (✅/🟡/❌)
| البند | الوصف | الحالة قبل | القرار | الحالة بعد |
|---|---|---|---|---|
| توثيق-1 | المرحلة 0 تثبيت حقيقي (مستخدم DML + تحييد 21) | ✅ موثّق (R02 ADR-04/ADR-03/§3.1) | تحقّق فقط | ✅ مؤكَّد بلا تعديل |
| توثيق-2 | ADR-02 تدريجي + **بند مراقبة log-only جديد** | 🟡 موثّق بلا آلية المراقبة | إكمال | ✅ أُضيف (ADR-02 صف §2 + §3.2 كأول مهمة + §13 قيد 13) |
| توثيق-3 | الأوفلاين | مؤجَّل | تجاوز | ⏸️ مؤجَّل (لم يُلمس) |
| توثيق-4 | ADR-10 الناقل المؤسسي + **مثال المضخة + أسماء الأحداث** | 🟡 §8 موجود بلا المثال الملموس | إكمال | ✅ أُضيف (§8.3: مثال المضخة + جدول أسماء الأحداث + §13 قيد 14) |
| توثيق-5 | Event Contract (23 حقلًا) | ✅ §9 | تحقّق فقط | ✅ مؤكَّد (13 إلزامي + 10 سياقي) |
| توثيق-6 | Shadow Columns | ✅ §10.2 | تحقّق فقط | ✅ مؤكَّد (دورة 4 مراحل) |
| توثيق-7 | Walking Skeleton | ✅ §10.1 | تحقّق فقط | ✅ مؤكَّد (8 طبقات) |
| توثيق-8 | تصحيح تسلسل ⑦ → المرحلة 0 | ✅ §1/§3.1 | تحقّق فقط | ✅ مؤكَّد (§1 سطر ⑦) |
| توثيق-9 | Evolution Readiness Review | ✅ §11 | تحقّق فقط | ✅ مؤكَّد (§11.1/§11.2) |
| توثيق-أرقام | تصحيح 159→141+3 + تدوين 37 ترحيلًا | ❌ | تنفيذ | ✅ منفّذ (R01: 4 مواضع+§9 قيد5 · R02: 2 موضع+§13 قيود 12/37) |

## خارج النطاق (مؤجَّل عمدًا — لا يُبنى منه شيء)
- كل الأوفلاين (ADR-11 · sync_uuid · Outbox · Sync · Conflict Resolution).
- بنود الإنتاج/Hostinger (تدوير أسرار الإنتاج · خط أساس الإنتاج · تنظيف تاريخ Git).
- تسريب الاعتماد (عولج مسبقًا).

## المخاطر المفتوحة
- 92 جدولًا بلا `deleted_at`: «الأرشفة لا الحذف» سطحها واسع — ترحيل `deleted_at` جزء من قالب هجرة الشاشة.
- تعبئة Shadow Columns الخلفية: بعميل utf8mb4 حصرًا (الترميز المزدوج).
- لا تحويل رجعي لقيم ENUM العربية القائمة (`تعمل/جاهزة/معطلة`, `سليمة/معطلة`).
